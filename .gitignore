# Sessions worktree (orphan `sessions` branch is checked out here — DEC-014)
.sessions-worktree/

# Node / TS (stack lands at M4 — DEC-013; these are harmless before then)
node_modules/
dist/
build/
*.tsbuildinfo
coverage/

# Env / secrets
.env
.env.*
!.env.example

# Local DB scratch (in-memory/SQLite throwaway persistence for M0–M3)
*.sqlite
*.sqlite3
*.db

# OS / editor
.DS_Store
*.log
