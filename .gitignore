# Sessions worktree (orphan `sessions` branch is checked out here — DEC-S014)
.sessions-worktree/

# Node / TS
node_modules/
dist/
build/
*.tsbuildinfo
coverage/

# Next.js (M4 — DEC-020)
.next/
next-env.d.ts
.vercel

# Env / secrets
.env
.env.*
!.env.example

# Local DB scratch (in-memory/SQLite throwaway persistence for M0–M3)
*.sqlite
*.sqlite3
*.db

# Python bytecode (e.g. from .claude/scripts/throughput.py)
__pycache__/
*.pyc

# OS / editor
.DS_Store
*.log
.env*
