/**
 * Placeholder landing — M4 framework skeleton (DEC-020). The real surfaces (admin
 * roster/builder/assignment, crew ask/my-shifts/card) land in later tasks
 * (1.5b/#12, 1.6/#13); this page only proves the app builds and renders.
 */
export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>Muster</h1>
      <p>Crew engine — M4 skeleton. Surfaces arrive next.</p>
    </main>
  );
}
