# Shard F — workflow / skills / agents / versioning / velocity / phase arithmetic

Run: doc consistency audit 2026-07-25. Subject docs: `CLAUDE.md`, `.claude/CLAUDE-context.md`,
`docs/AGENTS.md`, `docs/CHEATSHEET.md`, `docs/VELOCITY_AND_POKER_GUIDE.md`,
`docs/PROJECT_PLAN.md`, `docs/DEV_REFERENCE.md`.
Evidence: `.claude/skills/**/SKILL.md`, `.claude/agents/*.md`, `docs/DECISIONS.md`,
`docs/RETROSPECTIVES.md`, `package.json`, git tags/branches, `gh issue list`,
`.claude/project-type` (`webapp`), `.claude/seeds-version` (`4`).

Verified against `main` @ `12f6cb7`. `package.json` = `1.0.19`; latest tag `v1.0.19`;
`origin/production` exists at `v1.0.19`, 24 commits behind `origin/main`.

**53 rows — MISMATCH 32 · PLACEHOLDER 1 · CODE-CONTRADICTS 9 · UNVERIFIABLE 1 · NOISE 10.**

| # | doc:line | claim (verbatim, trimmed) | checked against | verdict | proposed bucket |
|---|----------|---------------------------|-----------------|---------|-----------------|
| 1 | `CLAUDE.md:1` | `# [Project Name] — Claude Code Project Context` | `.claude/CLAUDE-context.md:1` "`# Muster — Project Context`" | PLACEHOLDER | doc-wrong |
| 2 | `CLAUDE.md:64` | "`/retro` … Compute per-session active time (wall − breaks) from `started`/`ended` + transcript break inference. Aggregate one phase velocity (active h/pt)." | `docs/VELOCITY_AND_POKER_GUIDE.md:19` "That metric is **retired**"; `.claude/skills/retro/SKILL.md:37` "DEC-S026 retired the `active = wall_clock − breaks` model. **No session transcript is read in this step.**"; `#525` | MISMATCH | doc-wrong / known |
| 3 | `docs/PROJECT_PLAN.md:18` | "lifetime (P1–P3) **0.136 h/pt** is the forecast number" | `docs/VELOCITY_AND_POKER_GUIDE.md:19` "That metric is **retired**"; same doc `:23` "**P4 onward: throughput, not h/pt (DEC-S026).**" | MISMATCH | doc-wrong |
| 4 | `docs/VELOCITY_AND_POKER_GUIDE.md:9` | "The full decision record is DEC-S026 in `DECISIONS.md`." | `docs/DECISIONS.md` — zero `## DEC-S` headings exist (`grep -cE '^## DEC-S'` = 0); only 4 in-body mentions, all DEC-S022 (`:1144`, `:1666`, `:1667`, `:1670`). Same gap applies to every DEC-S citation in the subject docs (S007/S009/S010/S011/S013/S014/S018/S019/S022/S024/S026/S029/S031) | CODE-CONTRADICTS | decision |
| 5 | `docs/VELOCITY_AND_POKER_GUIDE.md:9` | "lives in [`THROUGHPUT_QUICKREF.md`](THROUGHPUT_QUICKREF.md)" | `docs/THROUGHPUT_QUICKREF.md` does not exist; `#525` | CODE-CONTRADICTS | doc-wrong / known |
| 6 | `docs/VELOCITY_AND_POKER_GUIDE.md:33`, `:36-38` | "run the **throughput extractor** (`dev/claude/scripts/throughput.py`)… `python3 dev/claude/scripts/throughput.py ~/bushel`" | `dev/claude/scripts/throughput.py` does not exist in this repo (seeds-relative path); repeated at `.claude/skills/retro/SKILL.md:283` | CODE-CONTRADICTS | doc-wrong |
| 7 | `CLAUDE.md:66` | "`/promote-production` … ff-merge `main` → `production` (deploy-only; tag already on the commit), push" | `.claude/skills/promote-production/SKILL.md:52` "## Step 3 — Release the trunk (patch-bump + tag)"; `CLAUDE.md:133` "`/promote-production` bumps + tags on each ship" | MISMATCH | doc-wrong |
| 8 | `CLAUDE.md:120` | "(the version tag is already on the commit from the bump — promotion does not tag)" | `.claude/skills/promote-production/SKILL.md:84` `git tag "v<NEW_VERSION>"`; `:87` `SHIP_TAG="v<NEW_VERSION>"`; `CLAUDE.md:137` "by `/promote-production` (patch, on ship)" | MISMATCH | doc-wrong |
| 9 | `CLAUDE.md:64` | "`/retro` … patch-bump per merged PR + minor-bump at close" (stated unconditionally) | `.claude/skills/retro/SKILL.md:207` "**Skip this entire step if a `production` branch exists**"; `CLAUDE.md:133` makes the per-PR patch conditional on *no* production branch; this project has one | MISMATCH | doc-wrong |
| 10 | `docs/CHEATSHEET.md:27` | "patch bumps      automatic in /its-dead on PR merge." | `.claude/skills/its-dead/SKILL.md:82` "No version bump. No CHANGELOG. No tag."; `CLAUDE.md:62` "no version bump (those moved to `/retro`)" | CODE-CONTRADICTS | doc-wrong |
| 11 | `docs/AGENTS.md:250` | "`/promote-production` … ff-merge `main` → `production` (deploy-only), push" | `.claude/skills/promote-production/SKILL.md:7` "this skill **patch-bumps + tags** the trunk (dev projects), then ff-merges" | MISMATCH | doc-wrong |
| 12 | `docs/DEV_REFERENCE.md:21` | "Auto-maintained by `/retro` and `/bump-major` (DEC-S013 — `/its-dead` no longer touches it)." | `.claude/skills/promote-production/SKILL.md:74` "Prepend a `CHANGELOG.md` entry"; `:81` `git add package.json CHANGELOG.md` — a third writer the list omits | MISMATCH | doc-wrong |
| 13 | `docs/PROJECT_PLAN.md:612` | "Version bumps via `/retro` (patch per merged PR + minor at close) once `package.json` exists." | `.claude/CLAUDE-context.md:179` "This project has a `production` branch, so **`/promote-production` patch-bumps + tags on each ship**"; `.claude/skills/retro/SKILL.md:207` skips Step 8.2 here | MISMATCH | doc-wrong |
| 14 | `.claude/CLAUDE-context.md:179` | "The `<VersionTag />` component is **available but not yet wired** — pull `templates/VersionTag.tsx` from seeds into a layout when a deployed build needs the stamp" | `components/ui/version-tag.tsx` exists and is imported/rendered in `app/(crew)/crew/page.tsx:8,239,253,588`, `app/(crew)/crew/open/page.tsx:15,123`, `app/(crew)/crew/calendar/page.tsx:9,135`, `app/(crew)/crew/time-off/page.tsx:9,157`; `#525` | CODE-CONTRADICTS | doc-wrong / known |
| 15 | `CLAUDE.md:143` | "Component source: `dev/claude/templates/VersionTag.tsx`." | `docs/DEV_REFERENCE.md:15` `import { VersionTag } from "@/components/VersionTag";` vs actual `components/ui/version-tag.tsx`; `.claude/CLAUDE-context.md:179` gives a third path (`templates/VersionTag.tsx`). Neither documented path exists | MISMATCH | doc-wrong |
| 16 | `docs/AGENTS.md:234` | "\| @architect \| Opus 4.8 \| Before design decisions \|" | `CLAUDE.md:80` "\| @architect \| Opus 5 \|" | MISMATCH | doc-wrong |
| 17 | `.claude/CLAUDE-context.md:207` | "The shell's `## Model Selection` is the standing policy (Opus 4.8 default, Sonnet for cheap/scoped work)." | `CLAUDE.md:91` "**Opus 5 is the standing model**"; `:96` "\| Default \| `claude-opus-5` \|" | MISMATCH | doc-wrong |
| 18 | `.claude/CLAUDE-context.md:207` | "**Fable is disabled (seeds DEC-S029)**" | `CLAUDE.md:97` "\| Frontier (rare) \| `claude-fable-5` \| $10 / $50 \| Reach for it only after Opus 5 at `max` has actually failed the task." — shell presents Fable as available | MISMATCH | decision |
| 19 | `.claude/CLAUDE-context.md:208` | "**`@architect` runs `claude-opus-4-8`** (`.claude/agents/architect.md` frontmatter is authoritative)" | `.claude/agents/architect.md` frontmatter: `model: opus` — no `4-8` pin. (Per run rules the `opus` alias is not itself a mismatch against "Opus 5"; the mismatch is the doc asserting a specific pinned string the file doesn't carry) | MISMATCH | doc-wrong |
| 20 | `.claude/CLAUDE-context.md:209` | "**`@ui-reviewer` stays Sonnet** but is worth bumping to Opus 4.8 for vision-heavy mockup-vs-build review" | `CLAUDE.md:96` `claude-opus-5` is the default tier; no 4.8 tier exists in the shell's table (`:93-97`) | MISMATCH | doc-wrong |
| 21 | `CLAUDE.md:82`, `:84`, `:85`, `:86`, `:103` | "@pm \| Sonnet"; "@sync-config \| Sonnet"; "@tape-reader \| Sonnet"; "@doc-consistency \| Sonnet"; ":103 Reviewers (`@code-review`, `@pm`, `@doc-consistency`, `@tape-reader`) and `@ui-reviewer` stay Sonnet" | `.claude/agents/pm.md`, `sync-config.md`, `tape-reader.md`, `doc-consistency.md` carry **no `model:` key at all** — they inherit the caller's model, so nothing pins them to Sonnet. Only `code-review.md`, `ideas.md`, `ui-reviewer.md` (`model: sonnet`) and `architect.md` (`model: opus`) are pinned | CODE-CONTRADICTS | code-wrong |
| 22 | `docs/AGENTS.md:68` | "**Output:** Scored report (X/10) with prioritized issues and exact Tailwind class fixes." | `.claude/agents/ui-reviewer.md:178` "There is **no score.** A number adds nothing the findings table doesn't already carry" | CODE-CONTRADICTS | doc-wrong |
| 23 | `docs/AGENTS.md:33` | "@code-review … **When to invoke:** … Optional — skip if it's slowing you down" | `CLAUDE.md:81` "After every commit (wired into `/kill-this`)"; `.claude/CLAUDE-context.md:160` "`@code-review` runs on every task (wired into `/kill-this`)"; `.claude/skills/kill-this/SKILL.md:55` runs it unconditionally | MISMATCH | doc-wrong |
| 24 | `docs/AGENTS.md:110`, `:240` | "Also runs unattended via the nightly sync Routine" | `CLAUDE.md:84` gives @sync-config's trigger as "`/push-seeds` and `/pull-seeds`" only — no Routine. (`.claude/agents/sync-config.md:26` confirms `mode: auto` for "the nightly sync Routine (DEC-S010)", so `CLAUDE.md` is the incomplete side) | MISMATCH | doc-wrong |
| 25 | `docs/AGENTS.md:76` | "@doc-consistency **When to invoke:** ad-hoc only — there is no scheduled or phase-boundary trigger… It is not a ritual; don't run it on a calendar." | `docs/PROJECT_PLAN.md:605`+`:613` "## Phase Boundary Checklist … 5. Review docs against intent; `/doc-consistency-check` if multiple docs moved." — a phase-boundary trigger; also `CLAUDE.md:70` "(no scheduled trigger)" | MISMATCH | decision |
| 26 | `docs/CHEATSHEET.md:8-44` | Skill roster (SESSION / PHASE / SEMVER / REFLECT-SYNC / INFRA / DOMAIN / BUILT-IN) — `/doc-consistency-check` appears nowhere in the file | `CLAUDE.md:18` calls this doc the "One-page printable skill reference"; `CLAUDE.md:70` and `docs/AGENTS.md:251` both list `/doc-consistency-check`; the skill exists at `.claude/skills/doc-consistency-check/SKILL.md` | MISMATCH | doc-wrong |
| 27 | `docs/CHEATSHEET.md:40` | "  /session-start-hook" | No such skill: absent from `.claude/skills/` (13 dirs, none matching) and `~/.claude/skills/` does not exist. Every other INFRA entry (`/update-config`, `/fewer-permission-prompts`, `/keybindings-help`, `/simplify`, `/loop`, `/init`) resolves | CODE-CONTRADICTS | doc-wrong |
| 28 | `docs/CHEATSHEET.md:13-14` | "/kill-this       end pt 1. build + commit + PR + @code-review." / "/its-dead        end pt 2." | `CLAUDE.md:61` "`/kill-this` \| **Per task** (DEC-S013) … Run N times per session — one per task"; `.claude/skills/kill-this/SKILL.md:3` "May run multiple times in one Claude window". The `:3` flow diagram (`/its-alive -> [ work ] -> /kill-this -> /its-dead`) encodes the same retired one-shot model | MISMATCH | doc-wrong |
| 29 | `docs/AGENTS.md:157`, `:183` | "Runs the build check from `CLAUDE.md §Commands` (skips if none defined)" | `CLAUDE.md` has no `Commands` section; commands live at `.claude/CLAUDE-context.md:60` `## Commands`, where `:62` states the per-task gate is "**`npm run verify`**" and `:73` "`build` alone is **not** the gate". (`.claude/skills/kill-this/SKILL.md:29` and `pause-this/SKILL.md:20` carry the same stale pointer — evidence-side, not filed) | MISMATCH | doc-wrong |
| 30 | `docs/AGENTS.md:186` | "4. Opens a PR (base = `main`) with `closes #<issue>`" | `.claude/CLAUDE-context.md:100` (DEC-059) "A feature shipping across multiple PRs … lands on a long-lived `feature/<name>` branch off `main` — its task PRs target *that* branch"; two such branches are live (`origin/feature/reservations`, `origin/feature/messaging`) | MISMATCH | doc-wrong |
| 31 | `docs/AGENTS.md:214` | "**During a work session:** 3. Spec → Build → Test → Verify mobile screenshot" | `CLAUDE.md:27-35` Micro Workflow is 9 numbered steps; AGENTS.md's 4-step compression drops step 2 (**Plan it** — "Wait for explicit approval") and step 3 (**Cut the branch**), the two steps `CLAUDE.md:156-161` treats as mandatory | MISMATCH | doc-wrong |
| 32 | `docs/AGENTS.md:225` | "10. `/retro` → close out the phase, write retro, version bumps (pgTAP, Playwright, external audits as the Phase Boundary Checklist demands)" | `.claude/CLAUDE-context.md:97` "**No pgTAP** — there's no Supabase/RLS"; `:146` "No pgTAP (no RLS)"; `docs/PROJECT_PLAN.md:607-613` Phase Boundary Checklist names neither pgTAP, Playwright, nor external audits | MISMATCH | doc-wrong |
| 33 | `docs/AGENTS.md:222-227` | "**End of a phase:** 8. `@code-review` … 9. `@ui-reviewer` … 10. `/retro` … 11. Return to primary planning chat" (4 items) | `docs/PROJECT_PLAN.md:605-613` "## Phase Boundary Checklist … 1. All tests green 2. @pm phase retrospective 3. Write retrospective entry 4. Version bumps via `/retro` 5. Review docs against intent" (5 items). Neither list is a subset of the other: AGENTS.md omits tests-green and @pm; PROJECT_PLAN omits @code-review and @ui-reviewer | MISMATCH | decision |
| 34 | `docs/PROJECT_PLAN.md:61` | "**Phase 0 total: 15 pts planned**" | Effort column `:55-59` sums to **17** (0.1=3, 0.2=5, 0.3=2, 0.4=5, mid-phase docs row=2). Same line's split (8 pre-ritual + 9 tracked) also sums to 17, not 15 | MISMATCH | doc-wrong |
| 35 | `docs/PROJECT_PLAN.md:87` | "**Phase 1 total: 43 pts**" | Effort column `:78-85` sums to **51** using 1.5a's recorded re-estimate ("5→13"), or 43 at the as-planned 5; velocity tables `:32` and `:581` both record Phase 1 as **55** points. Three values for one phase | MISMATCH | doc-wrong |
| 36 | `docs/PROJECT_PLAN.md:164-166` | "**Phase 4 total: 28 pts planned → 28 shipped, all 8 tasks `[x]`** … Estimate calibration: 2 re-pointed (4.1 pilot 5→8; Unit C +1), net drift +4 pts under." | Self-contradictory: 28 planned + net drift +4 ⇒ 32 shipped, not 28. Effort column `:155-162` sums to 28 (with 4.1 at its planned 5). `:35` repeats "re-est'd 2, drift +4" against a 28-pt total | MISMATCH | doc-wrong |
| 37 | `docs/PROJECT_PLAN.md:249` | "**Phase 6: planned 39 pts; shipped 43 pts**" | Same passage `:250` "net drift +7" and velocity row `:37` "43 pts … drift +7 (6.6 split a/b, 6.1b added)" ⇒ planned **36**, not 39. Effort column `:237-247` sums to 46 gross, 43 net of the deferred 6.9 (3 pts) — the 43 is right; 39 is unreachable from any of the stated arithmetic | MISMATCH | doc-wrong |
| 38 | `docs/PROJECT_PLAN.md:455` | "**Phase 10 total: ~25 pts (rough — poker at start).**" | Effort column `:447-453` sums to **27** (~5+~5+~5+~3+~3+~3+~3); `:436` records the closed phase as "**24/24 pts**"; `docs/RETROSPECTIVES.md:30` likewise. Three values | MISMATCH | doc-wrong |
| 39 | `docs/PROJECT_PLAN.md:500` | "**Phase 11 total: ~31 pts (rough — poker at `/start-phase`).**" | Effort column `:490-498` sums to **34** (5+3+8+5+3+2+3+2+3) | MISMATCH | doc-wrong |
| 40 | `docs/PROJECT_PLAN.md:578-586` | "## Velocity Table … \| 0 \| … \| 3 \|" (rows for phases 0–3 only) + "*(Same table as §Estimation Method above — keep both in sync at retro.)*" | `docs/PROJECT_PLAN.md:29-40` — the §Estimation Method table carries rows 0–**9**. The two tables the doc instructs to keep in sync differ by six rows | MISMATCH | doc-wrong |
| 41 | `docs/PROJECT_PLAN.md:29-40` | §Estimation Method velocity table ends at "\| 9 \| 5 \| 55 \|" | `docs/RETROSPECTIVES.md:30` "## Phase 10 — 2026-07-08 … → **v1.0.0**" and `docs/PROJECT_PLAN.md:436` "24/24 pts, 2-day burst, 0 estimate drift" — Phase 10 closed with a written retro but has no velocity row in either table | MISMATCH | doc-wrong |
| 42 | `docs/PROJECT_PLAN.md:490-498` | Phase 11 task rows carry no `[x]` and no PR links; `:500` still reads "rough — poker at `/start-phase`" | `gh issue list --label phase:11`: **8 closed, 1 open**; `phase:12` is materialized (21 open, 1 closed) so the phase advanced; `docs/RETROSPECTIVES.md` has entries for phases 0–10 and **no Phase 11**; `package.json` is `1.0.19` (still `1.0.x`), so `/retro` Step 8.3's phase-close minor bump never ran. Phase 11 was never closed by `/retro`, and every phase-close artifact it owns is missing | CODE-CONTRADICTS | decision |
| 43 | `CLAUDE.md:126` | "Then repoint the host's production branch from `main` to `production` (e.g. Vercel → Settings → Git → Production Branch)" | Cannot confirm the Vercel project's configured Production Branch from the repo. `vercel.json`'s `git.deploymentEnabled.main:false` (cited at `.claude/CLAUDE-context.md:117`) is consistent with it but not proof. Settled by Vercel dashboard or authorized Vercel MCP (unauthorized this session) | UNVERIFIABLE | decision |
| 44 | `docs/CHEATSHEET.md:37-39` | "/stripe-best-practices  /stripe-projects  /upgrade-stripe" | Actual invocation names are plugin-namespaced (`stripe:stripe-best-practices`, `stripe:stripe-projects`, `stripe:upgrade-stripe`). Cosmetic; the skills exist and resolve | NOISE | — |
| 45 | `docs/CHEATSHEET.md:1` | "SEEDS WORKFLOW CHEATSHEET                                v2026-06-12" | The body has been revised past that stamp — `:21` already carries DEC-S026 throughput, adopted after Phase 4 (2026-06-12 onward). Stamp is stale, not a factual conflict with another doc | NOISE | — |
| 46 | `docs/PROJECT_PLAN.md:562` | "**Phase 12 total: 16 tasks, 86 points** (pokered 2026-07-18)." | Verified: 16 task rows, Effort column `:545-560` sums to exactly **86**. GitHub `phase:12` now carries 18 pointed issues totalling 94 (#472 12.1b +5, #476 12.3b +3 added mid-phase) plus 3 unpointed bugs — expected divergence under `CLAUDE.md:74` "PROJECT_PLAN.md … Untouched mid-phase" | NOISE | — |
| 47 | `docs/PROJECT_PLAN.md:136`, `:204`, `:316`, `:368`, `:423` | Phase 3 "28 pts", Phase 5 "28 pts", Phase 7 "20 pts", Phase 8 "19 pts shipped", Phase 9 "~55 pts" | All five match their Effort-column sums exactly (28 / 28 / 20 / 19 / 55). Phase 5's sub-total "5.1–5.4 + 5.R = 23 pts" also checks. Phase 2 is the only phase with no stated total (nothing to check; sum is 16, matching velocity row `:33`) | NOISE | — |
| 48 | `docs/PROJECT_PLAN.md:585` | "**Lifetime velocity (active, Phases 1–3):** 13.50h / 99 pts = **0.136 h/pt**" | Arithmetic verified against rows `:581-583`: active 8.00+2.08+3.42 = 13.50; points 55+16+28 = 99; 13.50/99 = 0.1364. Internally correct (the metric itself is retired — see row 3) | NOISE | — |
| 49 | `CLAUDE.md:118-120`, `.claude/CLAUDE-context.md:155` | "`main` is the always-active trunk… `production` … never a PR base"; "**`production` branch + `/promote-production` are live**" | Verified: `origin/production` exists, `git merge-base --is-ancestor v1.0.19 origin/main` passes, `production` sits at tag `v1.0.19` = `package.json` version, 24 commits behind `main`. Branch scheme claims hold | NOISE | — |
| 50 | `CLAUDE.md:58-70`, `docs/AGENTS.md:242-254` | Skill roster tables | All 13 skills named in `CLAUDE.md` exist at `.claude/skills/<name>/SKILL.md`, and `.claude/skills/` contains exactly those 13 — no documented-but-absent and no present-but-undocumented project skill. Descriptions match each SKILL.md frontmatter except where filed above (rows 9, 10, 11) | NOISE | — |
| 51 | `CLAUDE.md:31-32` | "Playwright integration test + pgTAP if RLS-touching"; "`supabase test db` if RLS-touching" | Explicitly overridden by `.claude/CLAUDE-context.md:97-98` under the documented `## Workflow Overrides` mechanism (`CLAUDE.md:41`). Not drift — the override channel working as designed | NOISE | — |
| 52 | `CLAUDE.md:112` | "Keep ≤3 open PRs. Prefer 1." | `gh pr list --state open` returns 0. Compliant | NOISE | — |
| 53 | `.claude/CLAUDE-context.md:199` | "**`@ui-reviewer` is installed but inert until `.claude/ui-context.md` exists** — it hard-stops without it." | `.claude/ui-context.md` exists; `.claude/agents/ui-reviewer.md:22` confirms the hard-stop behavior. Conditional statement still true as written | NOISE | — |

## Triage amendments — after re-checking seeds against `origin/main`

The sweep (and the first triage pass) read the local `~/seeds` checkout, which was **11 commits
behind `origin/main`**. Four rows change as a result. **Verify seeds-related findings against
`origin/main`, not the local checkout** — `git show origin/main:docs/DECISIONS.md`.

Seeds `origin/main` carries **32 `DEC-S` entries through DEC-S035**. All 9 of this project's
distinct `DEC-S` citations resolve upstream. **Zero dangling references.**

| row | amendment |
|-----|-----------|
| 4 | **Resolved.** Not `CODE-CONTRADICTS` — the DEC-S series is intentionally upstream-only, and no citation is broken. The real defect was that nothing in `DECISIONS.md` explained the `S` namespace while `:3` claimed every ID is `DEC-NNN`. Closed by the namespace paragraph added to `docs/DECISIONS.md` head. Re-bucket: `doc-wrong`, **fixed**. |
| 5 | **Re-bucketed to `pull-seeds-debt`.** `docs/THROUGHPUT_QUICKREF.md` exists at seeds `origin/main` — it has never been pulled into this project. The link isn't wrong; the file is missing locally. |
| 6 | **Re-bucketed to `pull-seeds-debt`.** `dev/claude/scripts/throughput.py` exists at seeds `origin/main`. The path is correct *from seeds*; `VELOCITY_AND_POKER_GUIDE.md` is a synced template stating a seeds-relative path, unresolvable inside a project checkout. Fix is a wording change upstream, not here. |
| 18 | **Resolved — `CLAUDE.md` is the correct side.** DEC-S029 ("Fable disabled") was **deleted** upstream by seeds commit `e1c7bca` "Re-enable Fable". So `.claude/CLAUDE-context.md:207` cites a withdrawn decision. Re-bucket: `doc-wrong` against `CLAUDE-context.md` (not a `decision`), and the Opus-4.8 claim on the same line is stale for the same reason (DEC-S034 made Opus 5 standing). |
| 43 | Unchanged — still `UNVERIFIABLE`, needs Vercel dashboard or authorized Vercel MCP. |

**New bucket introduced:** `pull-seeds-debt` — the doc is right, this project's copy of the seeds
material is behind. Fix is `/pull-seeds`, not an edit. Expect more of these in shard E.

## For shard Z

- `docs/DECISIONS.md` contains **no `## DEC-S` headings at all** (0 matches), yet the DEC-S series is cited ~40 times across `CLAUDE.md`, `.claude/CLAUDE-context.md`, `docs/AGENTS.md`, `docs/CHEATSHEET.md`, `docs/VELOCITY_AND_POKER_GUIDE.md`, `docs/DEV_REFERENCE.md`, and `docs/PROJECT_PLAN.md` (S007, S009, S010, S011, S013, S014, S018, S019, S022, S024, S026, S029, S031). Only 4 in-body mentions exist, all DEC-S022 (`:1144`, `:1666`, `:1667`, `:1670`). Whether these should resolve locally is a DECISIONS-structure question, not filed here.
- `docs/DECISIONS.md:1670` "**Backport:** this is a gap in the shared seeds workflow … backport to the DEC-S series + the shell `## PR Workflow` via `/push-seeds`" — an unactioned backport instruction inside DECISIONS. Not investigated.

## Noted, not filed (outside shard F's subject)

- `.claude/CLAUDE-context.md:90` "`docs/DECISIONS.md` carries DEC-001–012 … 013–014 … and the M4 / DATA / MSG / ROLE / 033 series" — stale doc-index claim (already in `#525`). Subject is the DECISIONS index, not workflow; belongs to whichever shard owns doc indexing.
- `docs/PROJECT_PLAN.md:289` "A 4th crew surface, recorded as a knowing exception to BRAND's 'three surfaces' (DEC-074)" — the crew-surface count conflict is `#525` Bucket C and shard G/C territory, not workflow.
