# SPEC §2.8 / §2.10 conformance audit — booking & payment

**Started:** 2026-08-29 · **Finished:** 2026-09-03 · **Status: §2.8 COMPLETE — 24 of 24 criteria
verdicted.** §2.10 is not enumerated. · **Scope:** read-only.

*(Both counts in this file — this one and the ⚠ tally under §The 24 criteria — are recounted from the
table, never advanced by arithmetic. This one had drifted to 21 while the table held 22 verdicts, and
code review caught it going to 23. `grep -c '| — | |'` over the table rows is the count; it now
returns 0.)*

**What §2.8's 24 criteria came to.** Recounted from the table, not tallied as they were written:

| Verdict | Count | Criteria |
|---|---|---|
| **BUILT** | 9 | 3, 13, 14, 16, 17, 18, 21 outright; **8** and **10** by the hold rather than the pending row — the behaviour is met, the mechanism is the one decision 2 replaces |
| **BUILT, with a named gap** | 2 | 22 (the "and no others" exclusion is untested), 23 (⚠ on the third shape) |
| **PARTIAL** | 6 | 1, 2, 4, 12, 19, 20 |
| **UNPROVEN** | 2 | 7 (holds in code, no test abandons a checkout), 11 (vacuously) |
| **NOT BUILT / NOT MET** | 5 | 5, 6, 9, 15, 24 |

9 + 2 + 6 + 2 + 5 = 24.

Two of those matter more than their row: **#15** is the only one where money is taken and no booking
results with nobody told, and **#24** is the only one where the code is not merely missing the
guarantee but shaped to break it the moment decision 1 lands. Everything else is either built, or
unbuilt in a way that stays still.

Does the code do what `docs/SPEC.md` §2.8 (Booking & Payment) and §2.10 (Reservations) say it does.
§3.3 (cancel cascade) and §3.4 (disputes) are deliberately **out of scope** — booking-adjacent, not
booking.

**This file is the audit's state.** The work stops every time it finds a flaw in the spec, and
each pause is its own fix task on its own branch. Nothing here may live only in a session: a
finding recorded in a window that ends is a finding nobody has.

### Resuming this

**Start from current `main`, and run `npm run check:audit` before you trust a line number.** Every
citation here was checked against the tree at the time it was written, and line numbers drift. If one
does not say what this document claims, trust the code and correct the document — one wrong citation
in an audit is worse than a missing one, because it reads as verified.

**That paragraph used to be the whole mechanism, and it did not work.** It was written on 2026-08-31
and broken the same day: PR #869 (DEC-164) added a net 23 lines to §2.8, this branch merged after it
without a conflict, and **thirteen citations silently began pointing at the wrong sentences**. Nothing
failed. So the rule is now a gate — `scripts/check-audit-citations.mjs` pins a citation as a line
*plus the text that must be on it*, which makes a moved line a mechanical correction and a **changed**
line a stop-and-re-read. It reports its own coverage, because it pins about a third of what this
document cites and a gate that implied more would be the same failure wearing a checkmark.

**It is not in `npm run verify`, deliberately.** The pinned lines cover roughly 40% of `docs/SPEC.md`,
which every booking task edits, so wiring it into the universal gate would hard-fail unrelated PRs
whose authors have never heard of this file. Run it yourself when you pick the audit up.

Three findings here were fixed by work that shipped **while the audit was open**, on separate
branches: DEC-161 (occupancy in hold minutes), DEC-162 and DEC-163 (the operator booking surface).
**If you branch this file forward, merge `main` into it first** — an audit whose "fixed" status points
at a fix not present in its own checkout will send someone to re-litigate settled ground. Merging is
necessary and not sufficient: it is what produced the thirteen, because a merge that cannot conflict
also cannot warn.

The verdict table is the index. Each `PARTIAL` or `NOT MET` has a section under **Criterion
findings** carrying the evidence; criteria with `—` are not started. **Seven** are marked ⚠ — they are
written in `pending`/`expired` vocabulary the code does not have, and cannot return `BUILT` until
decision 2 lands.

*(Recount that number from the table; do not adjust it by arithmetic. It has been wrong twice, both
times because someone decremented it after unmarking a row instead of counting what was left.)*

**Two mistakes this document already made**, recorded so the next reader knows the failure modes
rather than trusting it flatly: a citation copied from a sibling branch where the line numbers had
shifted, and an absence claim ("read nowhere") that was true about occupancy and false about
existence. Both were caught by review, not by the gates. Neither is the kind of thing `npm run
verify` can see.

**Method.** The unit is one normative claim, not one subsection — §2.8.4a alone stacks several
independent must-statements, and a per-subsection verdict would read "partly" twenty-four times.
The claim list is §2.8's own **24 acceptance criteria** (`docs/SPEC.md:2053-2097`), which already
enumerate the behaviour at the right granularity.

**Verdicts.** `BUILT` — code does it, and a test exercises it. `UNPROVEN` — code does it, nothing
checks it still does. `PARTIAL` — some of it. `NOT BUILT`. `INVALID` — the criterion cannot be
evaluated as written because the spec is wrong; a spec flaw, not a code defect.

`UNPROVEN` is a separate verdict from `BUILT` on purpose. §2.8 was written **after** the code it
describes, so "there is code that does this" and "something would catch it breaking" have come
apart, and collapsing them is how a retrofitted spec launders itself.

---

## Decisions taken during the audit

Recorded here because each one changes how later criteria read.

**1. §2.8.1 is the authority; the code is incomplete.** (operator, 2026-08-29)

`src/domain/entities.ts:625` is `export type ReservationStatus = "booked" | "cancelled"` — two
states where §2.8.1 (`:1488-1499`) specifies four. Nothing writes anything else: `write-booking.ts:136`
writes `booked`, `cancel-reservation.ts:141` writes `cancelled`, and the Xola importer's own type at
`src/import/import-reservations.ts:70` is the same pair. `pending` and `expired` appear nowhere in
the repo outside `docs/SPEC.md`. No decision record designs them and no migration writes them; the
`reservations` table (`db/migrations/0001_init.sql:81`, `status` at `:88`) types it as free text under the
no-CHECK posture, so the column would accept them and nothing puts them there.

`eventId` (`entities.ts:645`) is non-nullable, where §2.8.2 makes null-until-confirm *"the contract,
not a convenience"*.

*(2026-09-04, task 14.2: the union at `entities.ts:625` is now `pending | booked | cancelled`, `eventId`
at `:645` is `EventId | null`, and `20260904220345_reservations_pending_nullable_event.sql` dropped the
column's NOT NULL. The importer's type at `import-reservations.ts:70` is the entity union minus
`pending`. Nothing writes a `pending` row yet — that is 14.4.)*

*(2026-09-04, task 14.1: §2.8.1 now specifies **three** states, not four. `expired` was struck —
a lapsed `pending` row is computed from its reserved time on every read and never stored — and
§2.8.8's sweeper went with it. The gap this decision names is one state narrower; the decision
itself stands. DEC-109 was converted to a `withdrawn` signpost pointing at §2.8.1 in the same task.)*

**2. The `checkout_holds` table goes; the `pending` reservation replaces it.** (operator, 2026-08-29)

`CheckoutHold` (`src/domain/entities.ts:729-771`, table from
`db/migrations/20260718142705_claim_hold_mutex.sql:33`, DEC-109) is the built design. §2.8 forbids it
twice — *"Nothing else claims a slot"* (`:1503`) and *"No separate hold object"* (`:2022`).

Every property of the hold has a §2.8 home on the pending row:

| Hold property | Where §2.8 puts it |
|---|---|
| Mutex — unique index on `(vessel_id, date, time)` | the hull-day lock, §2.8.3 `:1525-1528` — **broader**; slot identity is what let 13:30 and 14:00 both through (`hull-busy.ts:4-9`) |
| Lazy expiry, no cron (`entities.ts:744`) | §2.8.8 `:1834` — *"every reader tests `pending AND expiry > now`"* |
| `holderToken`, matched by possession (issue #575) | §2.8.5 `:1746-1747` — opaque httpOnly cookie, *"never the typed email or phone"* |
| Explicitly **not** the authority (`entities.ts:739-741`) | the pending row **is** the authority |

The work that survives is DEC-109's lazy-expiry rule, the possession-token rule and the overlap fix
— all three already restated in §2.8. Dropping the table is a consolidation, not a loss of design.

**3. Duration vocabulary.** (operator, 2026-08-29)

| Term | Means | BrewBoat |
|---|---|---|
| **hold minutes** | the whole time a boat is committed to one departure — on the water and back at the dock | 120 |
| **trip time** | how long the customer is actually on the water | 100 |
| **turnaround** | the boat sitting at the dock between trips; hold minutes minus trip time | 20 |

Turnaround is **not stored** — it is the difference. Occupancy is measured with **hold minutes** and
never with trip time. "Hold" on its own stays reserved for the checkout hold row, so a duration is
never called a hold.

These three are owed entries in `docs/dictionary.yml` with `trip length` registered as a forbidden
alternate of `trip time`. That turns `npm run verify` red on every remaining occurrence in the
gate's corpus (`docs/SPEC.md`, `docs/decisions/*.md`, `CLAUDE.md` — not the rest of `docs/`), which
is the gate working. Register the term and run `npm run check:dictionary` to enumerate; do not
sweep by hand.

---

## Spec flaws

Flaws in `docs/SPEC.md` itself. Each pauses the audit and ships as its own fix task.

### Flaw #1 — occupancy is specified in trip time, and must be hold minutes

**Status: FIXED in the spec 2026-08-29 (DEC-161). The code defect stands.** Criteria #3, #4 and #5
are unblocked for audit; #4 now names hold minutes and carries the 13:30-versus-15:15 case.

`Offering` carries two durations. `entities.ts:356` `tripLengthMinutes` — *"Minutes on the water
(12.8) — display config; the customer sees '1h 40min'"*. `entities.ts:358` `holdMinutes` —
*"Minutes the boat is held per departure, **turnaround included**"*.

Occupancy must be measured with the second. Everything measures it with the first:
`claim.ts:203`, `availability.ts:400`, `availability.ts:424`, `write-booking.ts:117`.

`holdMinutes` is read by **no occupancy decision**. It round-trips through the whole stack — the
admin form and its validation (`src/admin/offering-admin.ts:34,170,215`), the repository both ways
(`src/adapters/postgres-repository.ts:264,1160`), and a contract test that pins the round-trip
(`src/adapters/repository-contract.ts:1236,1222`). The operator can set it and it is stored
faithfully. Nothing measuring a hull ever asks for it. Both duration fields are commented
"Display/config", which is presumably why neither was wired to occupancy.

*(An earlier draft of this paragraph said `holdMinutes` was "read nowhere" and appeared twice in the
repo. That was wrong in the same way DEC-161's first draft was wrong, and caught the same way — by
review. The claim the finding actually needs is about occupancy, not about existence.)*

**And §2.8 reproduced the bug** — in the wording DEC-161 replaced. §2.8.3 said a reservation occupies
*"the hull for the **trip's duration**"*, and §2.8.4a froze *"the **trip length**"*. Built exactly as
written, the new design would have carried the same defect as the old one.

**Neither sentence is in the tree, and the quotes above are the record of the flaw rather than
citations.** §2.8.3 now reads *"What a reservation occupies is the hull for the trip's hold
minutes"* (`:1523`), and §2.8.4a carries *"Also frozen: both durations"* (`:1642`).

**Failure, with BrewBoat's real numbers.** Trip time 100, hold minutes 120, departures every 120.
A 13:30 departure occupies 13:30–15:30 in reality. The code measures 13:30–15:10.

- *Latent today.* On one offering at 2-hour spacing the next published departure is 15:30, which was
  free anyway, and off-grid times are refused (§2.8.3 `:1541`). The error never fires.
- *Live on the second offering.* Another offering with a 15:15 departure on that hull reads free at
  15:10 and sells. Two parties, one boat, twenty minutes apart.

§2.8.3 anticipated exactly this case and then measured it with the wrong number. Its replacement says
the right thing — *"Each row is measured by its own hold minutes, not the asking offering's"*
(`:1544`).

**Fix, as landed (DEC-161):** §2.8.3 measures with hold minutes; §2.8.4a freezes hold minutes
**and** trip time, because the customer is quoted the water time and occupancy needs the
commitment; §2.8.1 and §2.8.8 store a reserved time and derive expiry rather than storing it;
§2.10.2's frozen booking carries both durations. Three dictionary terms registered.

**One thing deliberately not done.** `trip time` is registered **without** `trip length` as a
forbidden alternate. Registering it produced 13 failures, five of them in DEC-041, DEC-077 and
DEC-145 — all three frozen at `docs/decisions-baseline.txt:50,86,139`, where editing a record fails
the build and the sanctioned fix is converting it to schema v1 and deleting its baseline line. That
is three separate jobs. The eight `docs/SPEC.md` occurrences were corrected regardless, so the spec
is right; only the catching of a future relapse waits. (operator, option (a), 2026-08-29)

### Flaw #2 — §2.8's opening sentence is false

**Status: open, unscheduled.** Does not block any criterion.

`docs/SPEC.md:1484`: *"The reservation payment path is being built from scratch (2026-08-23). This
section specifies it. **It is not a description of anything that currently runs.**"*

Operator, 2026-08-29: Stripe payments on an offering can be taken and turned into a shift today.
`src/reservations/` holds ~35 modules covering the path — `write-booking.ts`, `claim.ts`,
`confirm-booking.ts`, `booking-webhook.ts`, `create-departure-payment-intent.ts` — each with tests.

The sentence makes every reader treat §2.8 as forward-looking, which is why this audit was nearly
scoped as "what isn't built yet" rather than "what disagrees".

---

## Code defects found so far

Filed or fileable independent of any spec flaw.

**#825 confirmed — rival holds measured with the asking offering's trip length.**
`claim.ts:203` computes `tripMinutes` from `offering.tripLengthMinutes` — the offering being
*bought*. `claim.ts:221` then uses that same number as the duration of every **rival** hold. The
comment on `:220` says *"A hold's own offering sets its trip length"*; the code does not do that and
cannot, because `CheckoutHold` (`entities.ts:753-771`) carries `offeringId` but no duration.

Superseded in part by decision 2 — the hold table goes — but the defect survives the rewrite: a
rival **pending reservation** must be measured by its own frozen hold minutes, which is what
§2.8.4a's freeze exists for.

---

## The 24 criteria

From `docs/SPEC.md:2053-2101`. **Eight** are phrased in the `pending`/`expired` vocabulary that
decision 1 adopts and the code does not yet have; those are marked ⚠ and cannot return `BUILT` until
the status union and the nullable `eventId` land.

*(This line said "Nine" from the first draft and the table has never carried nine ⚠ rows — it was
seven until DEC-165 rewrote criterion 23 into that vocabulary, and it is eight now. The count is
`awk` over the table, not a recollection. Criterion 20 also says "pending" but is unmarked, correctly:
it is about what an in-flight reservation is charged, and the answer does not move when the row gains
a status.)*

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| 1 | Off-grid, out-of-season or already-departed cannot be reserved — no row, no payment | **PARTIAL** | see §Criterion 1 |
| 2 | ⚠ "Book & pay" writes a `pending` reservation **before** any Stripe call, and that row alone removes the trip from `/book` | **PARTIAL** | see §Criterion 2 |
| 3 | Two concurrent bookings on one hull at **overlapping** times (13:30 and 14:00) produce exactly one sale, against real Postgres, repeatedly | **BUILT** | see §Criterion 3 |
| 4 | Two offerings with different hold minutes on one boat block each other by **their own** durations | **PARTIAL** | see §Criterion 4 |
| 5 | The calendar and the write refuse the *same* set of trips | **NOT MET** | see §Criterion 5 |
| 6 | ⚠ A pending reservation's `eventId` is null and stays null until booked | **NOT BUILT** — but the guarantee it protects holds today by another mechanism, **UNPROVEN**. Read the section before acting on the verdict | see §Criterion 6 |
| 7 | Abandoning checkout leaves no `Event`, no customer record, no booking code, and sends nothing | **UNPROVEN** — all four hold in the code; no test abandons a checkout | see §Criterion 7 |
| 8 | The trip comes free the instant the window passes, **with the sweeper stopped** | **BUILT (by the hold, not the pending row)** — the behaviour is met and tested; the mechanism is the one decision 2 replaces | see §Criterion 8 |
| 9 | ⚠ An abandoned checkout survives on disk as an `expired` reservation carrying slot, party size, created time and expiry | **NOT BUILT** — §2.8.8 asks for three mechanisms and one is built, on the wrong object | see §Criterion 9 |
| 10 | ⚠ A declined card retried reuses the same reservation at its original expiry, matched by cookie token — **not** email or phone | **BUILT (by the hold, not the pending row)** — and the identity half is structural, not a guard | see §Criterion 10 |
| 11 | ⚠ `payment_intent.payment_failed` does **not** expire the reservation | **UNPROVEN (vacuously)** — satisfied by absence, three times over; carries no information about the built design | see §Criterion 11 |
| 12 | A superseded payment that succeeds late still resolves to its reservation and is refunded, not dropped | **PARTIAL** — refunded, well tested; "resolves to its reservation" is structurally impossible | see §Criterion 12 |
| 13 | Killing the webhook entirely still books a customer who reaches the success page | **BUILT** — unqualified; the criterion is named in the code and has a test written from its words | see §Criterion 13 |
| 14 | Closing the browser at the moment of payment still books, via the webhook | **BUILT** — #13's twin from the other side | see §Criterion 14 |
| 15 | ⚠ Webhook killed + browser closed + past the window: still booked by the reconciler, and the sweeper has **not** marked the paid row `expired` | **NOT BUILT** — and the most consequential one in this audit: money taken, no booking, nobody told | see §Criterion 15 |
| 16 | Confirming the same payment three times produces one booking, one Event, one payment record | **BUILT** — three claims, one mechanism: a deterministic id plus a conditional insert | see §Criterion 16 |
| 17 | Confirming produces a shift for that vessel-day, with seats, and notifies crew already committed to it | **BUILT** — all three halves, plus the negative control; the best-covered criterion in this audit | see §Criterion 17 |
| 18 | Confirming a previously-booked-then-cancelled slot succeeds, with the new booking's frozen values | **BUILT** — both halves, and the second is tested by a case written from this criterion's own words | see §Criterion 18 |
| 19 | A booking charge matching no reservation SMS-alerts every active admin, naming the amount; a throwing alert still returns success to Stripe; unconfigured SMS leaves only a log line | **PARTIAL** — five sub-claims, four different answers; the throwing-alert half is **NOT BUILT** | see §Criterion 19 |
| 20 | Editing the offering's price, hold minutes, trip time or schedule mid-pending changes neither the charge nor the occupancy | **PARTIAL** — the charge is safe on all four; **trip time changes what the booking occupies**, and the test named for it proves the wrong window | see §Criterion 20 |
| 21 | A balance payment never creates or confirms a reservation (guards a dormant path) | **BUILT** — refused at three depths, and tested on both entry paths | see §Criterion 21 |
| 22 | The tip pool divides across confirmed holders of **required** seats only, cents exact, and a pool with no confirmed crew warns rather than vanishes | **BUILT, with "and no others" UNPROVEN** — the exclusion is one conjunction, correct, and never tested against a seat it should refuse | see §Criterion 22 |
| 23 | ⚠ A reservation with no payment recorded still occupies its hull — no occupancy decision consults a payment row (**rewritten by DEC-165**; it named Xola, which the cutover removes) | **BUILT on both shapes that exist** — and structurally, not by a correct guard: `availability.ts` contains no reference to a payment. The third shape, a `pending` row with no `Event`, is decision 2's | see §Criterion 23 |
| 24 | ⚠ The public booking-recovery lookup returns nothing for a `pending` or `expired` reservation | **NOT BUILT** — and unlike criterion 11's vacuous pass, this one is **pre-loaded to fail**: `find-booking.ts:76` is a deny-list on `cancelled`, so decision 1's statuses are admitted by default | see §Criterion 24 |

## Criterion findings

### Criterion 1 — off-grid, out of season, already departed — **PARTIAL**

Two of the three guards are built and proven. The third does not exist. Both negative assertions —
no row, no payment — hold.

| Guard | State | |
|---|---|---|
| off the schedule grid | **BUILT** | `claim.ts:171` → `isOnScheduleGrid` → `unbookable: "off_schedule"` |
| outside the season | **BUILT** | same predicate — `availability.ts:145` compares against `seasonStart`/`seasonEnd` |
| **already departed** | **NOT BUILT** | nothing, anywhere — issue #824 |
| no row written | **BUILT** | a refusal returns at `claim.ts:154-173`, before `acquireCheckoutHold` |
| no payment created | **BUILT** | `create-departure-payment-intent.ts:98-104` returns before pricing or the intent mint |

**Grid and season are one predicate, not two.** `isOnScheduleGrid` (`availability.ts:135-148`)
checks four things in sequence: the date parses, it round-trips (so `2026-09-31` cannot roll into
October and pass the weekday check for the wrong day), it falls inside the season, its weekday is
one the offering runs, and the time is a published departure. That is worth knowing because it
corrects the refusal table under Criterion 5 below.

Proven: `claim.test.ts:177` refuses a date outside the season using **2026-09-05, a Saturday past
`seasonEnd`** — right weekday, wrong season, so it isolates the season check from the weekday one.
`claim.test.ts:185` covers the weekday. `create-departure-payment-intent.test.ts:115` is named *"an
off-grid slot is refused before any hold or Stripe call (#799)"* — which is this criterion's second
half, tested. And `claim.test.ts:164`'s docstring states the invariant directly: *"A rejection
writes NO hold row."*

**Already departed is absent, and it is worse than a missing edge case.** `claim.ts` calls `now()`
exactly twice, at `:177` and `:293`, both for hold expiry. Neither compares the clock to the
departure. `availability.ts` takes a `dateRange` from its caller and never refuses a past date on
principle — `:385-386` intersects that range with the season and nothing else.

The failure is not someone crafting a past date in a URL. **A 13:30 departure is still on the grid,
still in season, still on the right weekday at 15:00 that same afternoon**, so today's already-sailed
departures stay bookable and chargeable for the rest of the day, through the ordinary calendar. That
is issue #824, confirmed at the source, and the same-day case is the one that will actually happen.

### Criterion 2 — a row before Stripe, and that row alone clears the departure — **PARTIAL**

The first half holds in a different shape. The second half is true only of the departure's own slot.

**A row is written before any Stripe call, and it is a `CheckoutHold`, not a `pending` reservation.**
`create-departure-payment-intent.ts:94` acquires the hold; the PaymentIntent is minted at `:144`.
A refusal returns at `:98-104`, before either. So the ordering property the criterion asks for is
real — decision 2 changes which row it is, not whether there is one.

**The read path sees a hold, and `/book` uses it.** `app/(public)/book/page.tsx:92` reads
`listLiveCheckoutHolds(asOf)` and passes them into `deriveVirtualAvailability` at `:173`;
`availability.ts:439` renders the slot `held`. Lazy expiry applies on this side too — `:333` tests
`h.expiresAt > input.asOf`, so a stale row is inert. Covered by `availability.test.ts:578`, `:587`,
`:596`, `:601` and `:612`.

**But a hold clears only its own slot, and the write refuses more than that.**
`availability.ts:334` keys held slots on `slotIdentity(vesselId, date, time)` and `:431` looks them
up by exact identity. A live hold on Brew 4 at 13:00 marks 13:00 `held` and leaves **13:30 on the
same boat reading `available`**, even though the hull is committed until 14:40.

The write does not agree. `claim.ts:245-248` measures rival holds by **overlap**, so it refuses the
13:30 the calendar just advertised. That is **issue #826** — *"the calendar and the write disagree
about what a hold occupies"* — confirmed at both lines.

**And it is the exact case `hull-busy.ts:20-21` claims is impossible:** *"shared by
`deriveVirtualAvailability`, `candidateVessels`, and the write CAS, so the read path and the
backstop cannot disagree about what 'busy' means."* True of **events**, which both sides run through
`busyIntervalsFor`. False of **holds**, which never reach those functions on the read path — the
sharing guarantee is scoped to a type that holds are not.

**Untested rather than enshrined.** No test on the read path covers a hold overlapping a *different*
time on the same hull. The nearest, `availability.test.ts:622` — *"a hold on a DIFFERENT slot
doesn't touch this one"* — uses a different **date** (2026-07-11 against 2026-07-04) and is
correct as written. The overlap case simply has no test, on either side of the divergence.

### Criterion 3 — one sale across two overlapping departures — **BUILT**

The hull-day lock exists and is the right shape. `postgres-repository.ts:1441-1443` takes
`pg_advisory_xact_lock(hashtext('<vesselId>|<date>'))` as step (0) of `saveBookingIfSlotFree`,
before the overlap query, released with the transaction. Its comment gives §2.8.3's own reasoning:
a row lock cannot help, because the rival row is a phantom until it inserts.

**It is the transaction-scoped variant, and that matters in production.** `docs/DEPLOY.md:28` says
`DATABASE_URL` is the pooled PgBouncer endpoint used by app queries, and `:149` says PgBouncer
discards session state between transactions. A session-scoped `pg_advisory_lock` would break there.
`pg_advisory_xact_lock` holds, because a pooler pins a connection for the life of a transaction.

Proof: `postgres-repository.test.ts:442-495`. Real Postgres, two `saveBookingIfSlotFree` calls in one
`Promise.all`, 13:30 and 14:00 on one hull, asserting exactly one `won` **and** asserting `count = 1`
on the data rather than on the return values. The docstring names its own negative control. It ran —
`npm run verify` reported `2384 passed (2384)`, passed equal to total, so nothing was skipped and
`dbUp` was true.

**Three things that survive this verdict**, each of which becomes live under decision 2:

1. **The overlap predicate reads one table.** `postgres-repository.ts:1450` is `select 1 from events`.
   Today a checkout hold is caught separately in `claim.ts`. Once a `pending` reservation is what
   occupies the boat and materializes no `Event` (§2.8.2), the write CAS looks at `events`, sees
   nothing, and sells a boat somebody is mid-checkout on. The lock would serialize it perfectly and
   the answer would still be wrong.
2. **The predicate coalesces on `events.duration_minutes`**, filled from
   `offering.tripLengthMinutes` at `write-booking.ts:117`. Hold minutes has to be frozen onto the
   `Event`, not only onto the reservation.
3. **The lock is advisory, so it binds only callers who ask.** Exactly two places take it —
   `postgres-repository.ts:1441` and `:1084`. Nothing in the database stops a third write path from
   skipping it, and that discipline currently lives in comments.

Note against the criterion's own wording: it says *"repeatedly"* and the test runs the race once.
Deterministic given the lock, so this is a note, not a defect.

### Criterion 4 — each row measured by its own duration — **PARTIAL**

Three of the four paths are right and one is not.

| Path | Measures rivals by | |
|---|---|---|
| `busyIntervalsFor` (`hull-busy.ts:92`) | each event's own `durationMinutes ?? XOLA_TRIP_MINUTES` | correct |
| write CAS (`postgres-repository.ts:1458`) | `coalesce(duration_minutes, XOLA_TRIP_MINUTES)` per row | correct |
| `availability.ts:400` | the materialized event's own duration | correct |
| **`claim.ts:221`, rival holds** | **`tripMinutes` from `claim.ts:203` — the ASKING offering** | **wrong (issue #825)** |

`CheckoutHold` (`entities.ts:753-771`) carries `offeringId` but no duration, so the claim path has
nothing to measure a rival by without a lookup that does not exist. The comment at `claim.ts:220`
says *"A hold's own offering sets its trip length"* and the code does the opposite.

**The test that should catch this cannot.** `claim.test.ts:336-355` is named *"skips a boat whose
overlapping departure is already held by a rival"* and reads as coverage for exactly this rule. Its
rival hold is created with `offeringId: OFF` — **the same offering the buyer is asking for**
(`claim.test.ts:348`). With one offering, "its own length" and "the asking offering's length" are the
same number, so the test passes identically under the bug and under the fix. There is no test
anywhere in `src/reservations/` that puts two offerings with different durations on one hull.

**And every path above measures trip time, not hold minutes** — flaw #1's code side, unfixed
everywhere. So even the three correct rows are correct about the wrong quantity.

### Criterion 5 — the calendar and the write refuse the same set — **NOT MET**

They refuse different sets, and the difference is enumerable.

| Refusal | Read (`availability.ts` + `claim.ts`) | Write (`writeSlotBooking` → `saveBookingIfSlotFree`) |
|---|---|---|
| offering missing / not live | `claim.ts:154-155` | — |
| invalid guest count | `claim.ts:156` | — |
| off the schedule grid | `claim.ts:171` | — |
| ~~out of season / wrong weekday~~ | **correction:** also `claim.ts:171` — the season is inside `isOnScheduleGrid` (`availability.ts:145`), not read-path-only as first written. See Criterion 1 | — |
| party does not fit the hull | `candidateVessels` | — |
| operator block | `candidateVessels` | — |
| a rival's live hold | `claim.ts:245-248` | — |
| hull busy over the departure | `hullIsBusy` (TypeScript) | the SQL predicate at `postgres-repository.ts:1449` |
| **already departed** | **nothing** | **nothing** — issue #824 |

`writeSlotBooking` (`write-booking.ts:88-153`) performs **no** grid, season, block, fit or departure
check. It builds the candidate `Event` from the request and calls the CAS. The only guard inside the
hull-day lock is the events-overlap query.

**This is an ordering difference from §2.8, not just a missing check.** §2.8.4 step 3 puts the
off-grid, out-of-season, already-departed and overlap refusals *inside* the hull-day lock, at the
moment the `pending` row is written. The code checks grid, season, blocks and fit at **hold**
time, unlocked, in `claim.ts`; then takes the lock minutes later, at webhook time, and re-asks only
one of them. §2.8.6's *"Nothing is re-validated at confirm"* sanctions that for **confirm** — but in
the code there is no locked pending-write between the two, so those checks are never inside a lock
at all.

**The two overlap implementations are separate code.** `hull-busy.ts:20-21` states the guarantee:
*"Shared by `deriveVirtualAvailability`, `candidateVessels`, and the write CAS, so the read path and
the backstop cannot disagree about what 'busy' means."* That is true of the in-memory adapter
(`in-memory-repository.ts:604-616`, which calls the real functions) and **false of Postgres**, which
reimplements the arithmetic as SQL — `split_part(time, ':', 1)::int * 60 + …` at
`postgres-repository.ts:1455-1458`.

So the guarantee as *stated* — one shared function — holds in the adapter that never runs in
production and not in the one that does.

**But the behaviour is pinned even though the code is not**, and the first draft of this finding
missed it. `repository-contract.ts:730`, `:745` and `:788` run `saveBookingIfSlotFree` against
**both** adapters and assert the overlap outcomes, including the `#691` overlapping-time case. The
two implementations therefore cannot silently diverge on anything the contract covers. They already
differ on malformed input, which it does not cover — `hull-busy.ts:86-88` blocks the whole day and
the SQL has no equivalent branch.

**What is genuinely unpinned is the read-versus-write comparison**, which is what this criterion
actually asks for. The contract suite pins write-against-write across adapters. Nothing anywhere
asserts that the calendar and the write refuse the same set, and given the refusal table above they
demonstrably do not.

### What the operator pause changes here (2026-08-29, issue #864)

Operator-caused change and customer-caused change are different problems and only one is code. A
rival customer taking the hull is what the lock exists for. An operator blocking a boat or editing a
schedule mid-checkout is answered by pausing reservations, not by re-validating settings across the
checkout-to-webhook gap.

So of the refusals missing from the write path, the settings-shaped ones — block, season, grid, fit
— are **covered by the pause** and are not defects. What remains a real gap is the overlap re-check,
which the code already does, and the already-departed check, which is a clock rather than a setting
and is absent from both sides (issue #824).

### Criterion 6 — a pending reservation's `eventId` is null until booked — **NOT BUILT**

`docs/SPEC.md:2064`. The first ⚠ criterion verdicted, and it does not resolve the way the other eight
are likely to. **The literal criterion is not built. The guarantee it exists to protect holds anyway,
by a mechanism the criterion does not describe, and nothing tests it.**

**Nothing here is a defect to fix today.** The verdict column says NOT BUILT because the entity the
criterion names does not exist; it does not mean the crew manifest is currently leaking in-flight
checkouts. It is not. What follows is where the protection actually comes from, and what happens to
it under decision 2.

**The literal half, which decision 1 had already settled.** `entities.ts:625` is
`"booked" | "cancelled"` — no `pending`. `entities.ts:645` is `eventId: EventId`, no `?`, so the
field cannot hold null. There is no pending reservation, so there is no `eventId` on one to be null.
Nothing new is established by re-deriving that here; it is restated because a reader arriving at
criterion 6 from the table should not have to find it under "Decisions taken".

**The half that is new: no `Event` is materialized before the payment succeeds.**

| | |
|---|---|
| `writeSlotBooking` is what creates the `Event` | `write-booking.ts:87` |
| its only non-test caller is the webhook | `booking-webhook.ts:362` |
| the live checkout entry point mints a PaymentIntent and parks a hold, and writes no reservation | `create-departure-payment-intent.ts`, called from `app/(public)/book/checkout/actions.ts:173` |

Both checkout entry points say so in their own comments, in the same words — *"The SLOT — no eventId
(the Event doesn't exist yet; the webhook materializes it)"* (`create-departure-payment-intent.ts:170`,
`create-departure-checkout.ts:149`).

So §2.8.2's actual requirement — *"An abandoned checkout therefore materializes nothing"*
(`:1516`) — is satisfied. It is satisfied by there being **no row at all** in the interval, with the
`CheckoutHold` holding the boat, rather than by a pending row whose `eventId` is null. That is
decision 2's substitution seen from the other end: the criterion describes the destination, and the
current code reaches the same place by the road it is being asked to leave.

**Which makes §2.8's own stated stake worth re-reading.** `:1911-1913` says the crew manifest *"is
protected today only because a pending reservation has no `eventId`"* — *"one careless line from being
lost, and it is why the null is a contract."* Today the protection is stronger than that and rests
somewhere else: the manifest reads reservations by event, and before payment there is neither. The
sentence is written about the design being built, not the code running, and a reader checking it
against the tree will find it describes neither state exactly.

**Nothing pins any of it — this is the finding with a cost.** Two tests look like they cover it and
do not:

- `create-departure-payment-intent.test.ts:94` and `create-departure-checkout.test.ts:83` both assert
  `metadata.eventId` is `undefined`, each with the comment *"no Event yet"*. They assert the absence
  of a **key in the Stripe metadata payload**, which is a different claim from the absence of an
  `Event` **row**. Both would pass unchanged if the checkout materialized an Event and simply declined
  to name it to Stripe.
- `create-departure-checkout.test.ts:72` is named *"acquires a hold on the smallest fitting boat and
  carries the SLOT (no eventId)"*. The name is honest about the metadata payload; it is the two
  comments that over-read it.

There is **no assertion anywhere that no `Event` exists between checkout and payment.** Every
`getEvent(…)` in the reservations suite that runs on this path is a post-webhook
`.not.toBeNull() // materialized` — `create-departure-payment-intent.test.ts:256`,
`create-departure-checkout.test.ts:123,201,202`. The two negative `getEvent(…).toBeNull()` in the repo
are elsewhere and unrelated (`import-reservations.test.ts:57`, `repository-contract.ts:865`). Nor does
anything assert that a successful checkout leaves zero reservations: the
`listAllReservations()).toHaveLength(0)` assertions at `create-departure-payment-intent.test.ts:336`
and `:347` are webhook-refusal cases, not the pre-payment interval.

By this document's vocabulary the behaviour is `UNPROVEN`, and it is the kind of thing `UNPROVEN`
exists to name: the code does the right thing, the tests would not notice it stopping.

**Where this lands when decision 2 goes in.** The guarantee changes hands. Today it holds because no
row exists; afterwards it holds because a row exists with a null `eventId` — precisely the "one easy
line" §2.8.2 warns about (`:1518-1521`), and the moment at which the field becomes populatable. The
test that does not exist today is the one that has to exist before the pending row does, or the
protection is silently handed from a structural impossibility to a convention nobody checks.

### Criterion 7 — abandoning checkout leaves nothing and sends nothing — **UNPROVEN**

`docs/SPEC.md:2065`. **All four halves hold, and the reason they hold is structural rather than
guarded: the checkout path writes exactly one row and reads everything else.** The first verdict in
this audit where the code is right and the only finding is that nothing would notice it going wrong.

| What must not exist | Why it doesn't |
|---|---|
| an `Event` | `writeSlotBooking` has one non-test caller, `booking-webhook.ts:362` — see §Criterion 6 |
| a customer record | `resolveCustomerId` is called from `write-booking.ts:122` and `import-reservations.ts:261`. Both are post-payment or Xola import; neither is reachable from checkout |
| a booking code | `ensureBookingCode` (`ensure-booking-code.ts:49`) takes a `reservationId`. Its callers are `app/lib/booking-confirmation.ts:59` and `:118` and `recover-booking-link.ts:98` — all of which need a reservation that does not exist yet |
| anything sent | every sender is a `WebhookDeps` field — `alertPaidButUnbooked`, `notifyCustomerSoldOut`, `sendConfirmation` (`booking-webhook.ts:69,76,84`). Call sites are in `booking-webhook.ts` **and one in `confirm-booking.ts:48`**; every one of them is downstream of a *succeeded* PaymentIntent, which an abandoned checkout never produces |

**The checkout path is reads plus one write.** `create-departure-payment-intent.ts` calls
`getOffering` (`:89`, `:116`), `listEvents` (`:119`), `getVessel` (`:132`) and `getPaymentConfig`
(`:141`), then acquires the hold. Nothing else touches the repository. So the four absences are not
four guards that could each be forgotten — they are one property of the path, which is a much stronger
position than the criterion asks for.

**Nothing sweeps, so abandonment has no code path of its own.** Hold expiry is lazy and read-side
(DEC-109): `availability.ts` and `claim.ts` test `expiresAt` when they look. There is no job, and
therefore no place a notification could be attached. Abandoning a checkout is the absence of a second
event, not an event.

**One thing this audit cannot settle from the repo, and it is the only real gap.**
`create-departure-payment-intent.ts:166` sets `receiptEmail` on the PaymentIntent when the customer
gave an email, and its own comment says *"Absent ⇒ Stripe sends no receipt; present ⇒ it does, in live
mode regardless of the account's email settings."* Whether Stripe sends anything for an intent that
never succeeds is **Stripe's behaviour, not ours**, and it is not answerable by reading this
codebase. The expected answer is no — a receipt follows a successful charge — but "sends nothing to
**anyone**" is a claim about a third party here, and this document does not assert third-party
behaviour it has not read the documentation for. Verifying it is one look at Stripe's receipt docs,
and it should be done before this criterion is called `BUILT`.

**An abandoned checkout does leave a `CheckoutHold` row**, and that is not a violation. The criterion
enumerates four things and a hold is none of them. It is worth naming because criterion #9 asks for
the **opposite** — that an abandoned checkout survive on disk as an `expired` reservation carrying
slot, party size and reserved time. The hold row is what will be measured against that, and under
decision 2 the two criteria converge on one row.

**Nothing tests any of it.** No test in the suite abandons a checkout. The four assertions that come
closest are all about something else:

- `resolve.test.ts:91` and `:201` assert `listCustomers()).toHaveLength(0)`, but both are about a
  **phone number that will not parse** — the first over five bad inputs, the second confirming a
  booking still succeeds without a customer link. Neither involves an abandoned checkout.
- `availability.test.ts:587` (*"an EXPIRED hold contributes nothing"*) and `claim.test.ts:357`
  (*"an EXPIRED overlapping hold does not occupy anything"*) do exercise expiry — but they pin the
  **slot coming free**, which is criterion #8. Neither looks at events, customers, codes or sends.

So the verdict is `UNPROVEN` in its purest form: the code does the right thing for a structural
reason, and the suite would be equally green if a future refactor moved `resolveCustomerId` up into
the checkout path.

### Criterion 8 — the trip comes free the instant the window passes — **BUILT (by the hold, not the pending row)**

`docs/SPEC.md:2067`. **The second `BUILT` in this audit, and the first one where the test does the
hard half.** There is no sweeper to stop, both paths compute expiry at read time, both adapters
implement it, and the contract suite pins the boundary to the millisecond across both.

*(2026-09-04, task 14.1: the spec no longer has a sweeper to stop either. The criterion now reads
"with no job running", §2.8.8 says "There is no sweeper", and lapsed is derived on every read and
never stored (§2.8.1). The verdict and the design that survives are unchanged; the qualifier below
about the sweeper describes the spec as it read on 2026-08-31.)*

**Read the qualifier before reading the verdict.** This criterion, unlike #6, names no object — it
says a trip comes free when a window passes, and is indifferent to what holds the trip. That is why
it can return `BUILT` where #6 could not. But §2.8.8 behind it is not indifferent: it says *"every
reader tests `pending AND expiry > now`"* (`:1834`), and every reader today tests
`hold.expiresAt > now`. **Built, and not to the spec.** A bare `BUILT` would read as "nothing to do
here", and that is wrong — decision 2 moves all four comparison sites onto a different table and the
boundary test has to be rewritten against it. What survives is the *design*: lazy expiry, no job, one
comparison, both adapters, the boundary pinned. What moves is every line that implements it.

**"With the sweeper stopped" is trivially satisfied, because there is no sweeper.** Three cron routes
exist — `app/api/cron/doorbell-tick`, `tick`, `xola-pull` — and none of them reference
`CheckoutHold` in any form. Nothing scheduled deletes or expires a hold.

**Expiry is one comparison, and both sides make it the same way.**

| | |
|---|---|
| read path | `availability.ts:333` — `h.expiresAt > input.asOf` |
| write path | `claim.ts:215` skips on `h.expiresAt <= at0`; `:266` requires `h.expiresAt > at0` |
| Postgres | `postgres-repository.ts:1728` — `select * from checkout_holds where expires_at > $1` |
| in-memory | `in-memory-repository.ts:769` — `h.expiresAt > asOf` |

Same boundary in all four: a hold is dead at exactly its `expiresAt`, not one tick later. That is
what "the instant" asks for, and it is not an accident of one implementation — `claim.ts:211` names
the rule as DEC-109's and says it is the deriver's.

**There IS a sweep, and finding it is what makes the verdict interesting rather than a formality.**
`acquireCheckoutHold` deletes expired rows — `repository-contract.ts:1127` is named *"acquireCheckoutHold
sweeps EVERY expired hold, not just its own slot (issue #713)"*, and `:1149` pins that it *"never
touches a LIVE hold on another slot."* So cleanup exists; it rides the next write instead of a
schedule. **Correctness does not depend on it**, because every reader already filters by `expiresAt` —
the sweep reclaims storage, not availability. That is precisely the shape §2.8.8 asks for, arrived at
without a job to stop.

**The proof is the boundary, and it runs against both adapters.** `repository-contract.ts:1123-1124`:

- `listLiveCheckoutHolds("2026-07-01T12:15:00.000Z")` → **0**, on a hold expiring at 12:15
- `listLiveCheckoutHolds("2026-07-01T12:14:59.999Z")` → **1**

One millisecond either side of the instant, asserted. `runRepositoryContract` is invoked twice —
`postgres-repository.test.ts:97` and `in-memory-repository.test.ts:9` — so this is pinned against
**real Postgres**, not only the test substrate. The Postgres half is gated on `dbUp`
(`postgres-repository.test.ts:84`); the last full run reported `2412 passed (2412)`, passed equal to
total, so nothing was skipped.

**This is the answer criterion #5 wanted and did not get.** There, the read and write overlap
predicates were separate code and the guarantee was scoped to a type holds are not. Here the two
adapters *are* separate code — SQL against an in-memory filter — and the contract suite is what stops
them diverging. Same risk, actually pinned.

**The ⚠ on this row was removed, and the verdict carries the warning instead.** The marker means a
criterion is *written in* `pending`/`expired` vocabulary the code does not have, and this one is not —
it names no state. But that definition turned out to be narrower than what a reader wants the marker
for, because "will be rebuilt when decision 2 lands" is true here and the marker could not say it. So
the qualifier went into the verdict column rather than widening ⚠ across rows nobody has re-read.
Eight of the twenty-four remain marked. **If a later pass widens the marker's meaning, this row is the
first one to revisit.**

**One narrow divergence the contract does not cover.** Postgres compares a timestamp column against a
cast parameter; the in-memory adapter compares ISO strings **lexically**. Those agree for
`Z`-suffixed strings at fixed millisecond precision, which is the only shape the test uses and the
only shape `now()` produces. They would disagree on an `asOf` written `+00:00` instead of `Z`, or
without milliseconds — lexically `"…00.000Z" > "…00+00:00"`, where as timestamps the two are equal.
Latent, not live, and unpinned. Worth one assertion if anyone ever widens what may be passed as
`asOf`.

### Criterion 9 — an abandoned checkout survives on disk — **NOT BUILT**

`docs/SPEC.md:2068-2070`. **NOT BUILT for the same reason as #6: the entity does not exist.** The row
this criterion describes is an `expired` **reservation** — `SPEC.md:1500`, *"The payment window ran
out. A state, not a deletion (2.8.8)"* — and there are no `pending`/`expired` states (decision 1). So
nothing to keep, because nothing is written.

*(An earlier draft of this section led with "the row the spec wants kept is the row issue #713 was
written to delete." That is wrong and worth recording as a failure mode: #713 deletes **holds**, and
§2.8 removes the holds table outright. Different rows in different tables. The real relationship is
narrower and appears at the end of this section — the two decisions solve the same **problem**
oppositely, which is not the same as one deleting the other's data.)*

**The nearest thing today is a hold, and it does not survive either.** `CheckoutHold`
(`entities.ts:753-784`) carries
`vesselId`/`date`/`time` (the slot), `guestCount` (party size), `createdAt` (the reserved time) and
`expiresAt`. Every field criterion 9 asks for already exists on the object that already holds the
boat. Nothing needs designing.

**Then the next acquire deletes it — every expired hold, everywhere, not just the one being
replaced.** `postgres-repository.ts:1681` is
`delete from checkout_holds where source='muster' and expires_at <= $1`, and
`in-memory-repository.ts:744-748` is the same loop. Pinned by `repository-contract.ts:1127`,
*"acquireCheckoutHold sweeps EVERY expired hold, not just its own slot (issue #713)."*

**The sweep's own comment explains why, and it is a good reason.**
`postgres-repository.ts:1672-1675`: *"Scoped to the slot identity, an abandoned checkout on a
departure nobody re-attempts was unreachable by any cleanup path and grew the table without bound;
there is no scheduler in this codebase, so an acquire is the only moment a sweep can happen."*

**§2.8 solves the same problem and reaches a different answer, which is the useful comparison.** #713
had no scheduler available, so it deleted at the only moment it could — an acquire — and had to delete
*everything* expired to catch rows nobody would ever re-attempt. §2.8 assumes a job exists and splits
the work in two, which is what lets it keep the data: the **sweeper** relabels and *"never deletes,
and this is load-bearing rather than tidy"* (`:1849`), and a separate **reaper** *"runs rarely, on a
long horizon, and deletes old `expired` rows so the table does not grow without bound"* (`:1843`).
Same unbounded-growth problem, answered with a retention horizon instead of immediate deletion.

**So the query the criterion names is not a query.** *"How many checkouts were started and walked
away from last month"* cannot be answered from this database — the rows are gone, deleted by the next
person to start any checkout on any boat. Nothing archives them first: **there is no hold history
table.** `grep -rl checkout_holds db/migrations/*.sql` returns five files — the create
(`20260718142705_claim_hold_mutex.sql:33`), a token column, an index, a foreign-key era change and
the refund-leases migration that cites the pattern — and none of them creates an archive. And
`audit_events` (`0024_audit_events.sql:1`) is the **crew** log — add/drop/change keyed on
`crew_member_id`, per DEC-118 — with no reservations writer anywhere in `src/reservations/`.

*(That claim first cited `ls db/migrations/*checkout_hold*` as "three files". It returns **two**, and
the glob does not match the migration that creates the table, because that one is named
`claim_hold_mutex`. The conclusion survived; the evidence for it did not. Third absence claim in this
document to arrive with bad evidence, which is the pattern rather than the incident.)*

**The data does exist, at Stripe.** `create-departure-payment-intent.ts:94` acquires the hold and
`:153` mints the PaymentIntent, so every abandoned checkout leaves an intent behind. What Stripe
retains and for how long is Stripe's, not readable here — the same discipline criterion 7 applied to
`receiptEmail`. But the shape is the §2.8.5 finding again: **a thing §2.8 wants on our own row is
currently only at the payment provider.**

*(2026-09-04, task 14.1: §2.8.8 was rewritten out from under the rest of this finding. There is
now **no sweeper and no `expired` state** — the union is `pending | booked | cancelled`, a lapsed
row is computed from its reserved time on every read and stays `pending` on disk, and the reaper
is the only job, deleting old lapsed rows that carry no payment id. The criterion now reads "as a
lapsed `pending` reservation … no status was written to say so". The verdict stands — nothing
survives on disk today — but the table and the two paragraphs after it quote sentences the spec no
longer contains; what they say about the sweeper is history, and what they say about the reaper's
horizon and the `admin` exemption still holds.)*

**What is actually missing: two of three mechanisms, and the one that exists is on the wrong object.**

| §2.8.8 asks for | Built today |
|---|---|
| **the clock** — the window passes, the boat is free immediately, nothing has to run (`:1834`) | **yes** — on `CheckoutHold`, verdicted BUILT under criterion #8 |
| **the sweeper** — relabel a lapsed `pending` row to `expired`, never delete (`:1837`, `:1849`) | **no.** There are no `pending`/`expired` states to relabel between |
| **the reaper** — rare, long horizon, deletes old `expired` rows (`:1843`) | **no** |

So the build is not "keep the row instead of deleting it." It is a status union, a nullable `eventId`,
and two jobs — and §2.8.8 attaches a rule to each that is easy to lose. The sweeper *"never labels a
row it cannot prove was unpaid"* (`:1840`): a row with a payment id belongs to the reconciler, or a
webhook outage lasting longer than the payment window turns a paying customer into an `expired` row.
And an `admin`-sourced pending row is exempt from all of it (`:1827-1831`, DEC-163) — no window, never
swept — which a reader has to branch on **first**.

**The reaper's horizon is the retention policy and §2.8 gives no number** (`:1843` says only "rarely,
on a long horizon"). That is the correct posture for a value the host's env owns, but it means the
period is undecided, and it is the single knob trading table size against how far back the operator
can ask the question this criterion exists to answer.

*(An earlier draft of this paragraph claimed §2.8 had no answer to the unbounded growth #713 closed,
and that building §2.8 would silently revert it. Both false — `:1842-1845` is that answer, and it
additionally names distinguishing scripted abuse from real abandonment as part of the build. The
error came from reading §2.8.8's title, "Expiry is a clock, not a job", and stopping there; the
subsection goes on to specify two jobs.)*

**The second clause is true of the built system and false of the spec, and the difference is the whole
point of the criterion.** (operator, 2026-08-31: *"we do not have a mechanism to check abandoned
checkouts, so that question doesn't make any sense."* — correct about today.)

*"…and how long did each hold a boat?"* is unanswerable now, because abandonment is silent: it is the
absence of a second event, nothing marks a hold as abandoned, and the row is deleted anyway. **§2.8
knows exactly what the clause is for**, and says so at `:1849-1859`. The payment window can be wrong in
two directions and only one is already visible:

- **Too short** — real buyers cancelled mid-payment. Each leaves a refund and a sold-out message, so
  the operator already sees it.
- **Too long** — hulls tied up for people who were never going to buy. Visible *"**only** if expired
  rows survive, carrying their slot, party size and reserved time."*

That asymmetry is the reason the sweeper never deletes. The duration is the payment window on every
row, which is exactly what makes the **count** meaningful: forty abandoned checkouts times a
fifteen-minute window is ten hours of hull nobody could buy, and that number is the instrument for
deciding whether the window is too long. **So this clause is not a report to build — it is the
justification for retention**, and the thing to preserve when someone later argues the expired rows
are clutter.

### Criterion 10 — a retry is matched by the cookie token — **BUILT (by the hold, not the pending row)**

`docs/SPEC.md:2071-2072`. **The best-built thing this audit has found, and the only criterion so far
whose hardest half is satisfied by construction rather than by a check.** Same qualifier as #8: the
criterion says *reservation* and the object is a `CheckoutHold`, so decision 2 moves it.

| The criterion asks | Where |
|---|---|
| reuses the same hold on a retry | `claim.ts:262-270` — matched before the fit-and-fallback loop runs |
| at its **original** reserved time | `claim.ts:281` comments it and `:284` does it — `mine` is returned unmodified from the array read once at `:186`, never recomputed |
| matched by the **cookie** token | `app/(public)/book/checkout/actions.ts:102-111` — `httpOnly`, `secure`, `path: "/"`, 30-minute `maxAge` |
| **not** by email or phone | `DepartureHoldRequest` (`claim.ts:120-134`) **has no email or phone field** |

**The last row is the finding.** §2.8.5 forbids matching on typed identity (`:1746-1750`), and the code
does not merely avoid it — the claim path never receives those fields at all. There is no guard to
forget and no branch to get wrong, which is a stronger position than the criterion asks for. It is the
same shape as criterion #7: a property of the path rather than a check on it.

**And it is stronger still, because the token is a session id in everything but name.**
`holder-token.ts:26-27` mints 32 bytes of CSPRNG as base64url; `:32` validates against an anchored
`/^[A-Za-z0-9_-]{43}$/` so a crafted cookie cannot arrive as a lookalike. The cookie is `sameSite:
"lax"` rather than `Strict` for a documented reason — *"the 3-D Secure return from Stripe is a
top-level GET navigation, which Lax sends and Strict would drop, losing the token exactly when the
customer comes back to finish"* (`actions.ts:103-105`).

**This was built the wrong way first, and that history is the evidence.** `holder-token.ts:4-11`
records it: the first cut keyed reuse on the buyer's **email or phone**, and `/security-review` killed
it. Two exploits fell out — *"supply a victim's email and you were handed their hold and a
PaymentIntent on the boat they were buying; supply it with an absurd guest count and the
fit-revalidation branch deleted their hold outright."* So §2.8.5's *"It must never be the typed email
or phone"* is not a precaution written in advance; it is a rule this codebase earned.

**Two conditions on reuse that the criterion does not ask for and that matter more than it does.**
Both are in `claim.ts` and both close holes a naive reading would leave open:

1. **A token is required on both sides** (`:257-259`). A tokenless hold is never reused and never
   matches another tokenless hold — otherwise two pre-cookie sessions would match on `undefined ===
   undefined` and share a boat. Pinned by `claim.test.ts:498`, *"two sessions with NO token never
   share a hold."*
2. **A reused hold gets the same scrutiny a fresh one would** (`:271-280`) — `candidates` re-applies
   fit and blocks, `vesselIsAvailable` re-applies booked and hull-busy. The comment names the case:
   an operator blocking a vessel for a mechanical fault at 10:00 would otherwise hand the 10:02 retry
   that same boat and take payment for it. *"The world can change inside a 15-minute hold."*

**Six tests, and the negative controls are the useful half.** `claim.test.ts:397` (same hold, not a
second boat), `:410` (*"does NOT extend the expiry — a retry cannot park a boat indefinitely"* —
whose assertion at `:421` is deliberately **outside** the type guard, because *"with every assertion
inside the type guard, a regression that returned `soldOut` would run zero expects and pass green"*),
`:439`
(a retry that no longer fits takes a bigger boat and leaves the old hold alone), `:498` (tokenless
never share), `:513` (an expired hold of the same buyer is not reused), `:524` (a hold from a different
trip is not reused).

**The gap, and it is in the plumbing rather than the logic.** **Nothing tests `mintHolderToken` or
`isHolderToken`** — no test file anywhere imports either. The ten `holderToken` mentions in the unit
suite are split five and five between `claim.test.ts` and `create-departure-payment-intent.test.ts`,
and both pass fixture strings through to matching; neither mints or validates one. Nor does anything
assert the cookie's attributes: `httpOnly`, `secure` and `sameSite` are set in one server action and
pinned by nothing — a repo-wide search for those attribute names finds no assertion outside
`actions.ts` itself, so a refactor could drop `httpOnly` and every test would stay green. The
criterion's own subject — the matching — is well covered; its delivery mechanism is not.

*(The sentence above first said all ten mentions were in `claim.test.ts`. They are 5/5 across two
files. The conclusion was right and the attribution was wrong — **the fourth time in this document
that an absence claim has arrived with faulty evidence**, and this time inside the paragraph meant to
be careful about exactly that. The pattern is now clear enough to state as a rule: in this document,
run the search again before writing the sentence that describes it.)*

### Criterion 11 — `payment_failed` does not expire the reservation — **UNPROVEN (vacuously)**

`docs/SPEC.md:2073`. **The first criterion satisfied by nothing existing, and the first where passing
tells you nothing.** It is a *negative* — a requirement that something not happen — and a system with
no handler, no state to move it to, and no operation to move it with satisfies every negative it is
asked. That is worth a verdict of its own rather than a `BUILT` a future reader would take as
reassurance.

**Three independent reasons it cannot happen, none of them a guard.**

```
$ grep -rn "payment_failed" --include=*.ts src/ app/
(no output)
```

1. **No handler.** The adapter normalizes Stripe's shape into a domain union and recognizes four
   families — `checkout.session.completed` (`stripe-payment.ts:194`), `charge.refunded` (`:209`),
   the three `charge.dispute.*` (`:235-237`) and `payment_intent.succeeded` (`:265`). Anything else
   falls to `return null` at `:277`, which is the only fall-through in the function. The test double
   has its own normalizer (`fake-payment.ts:76`) and does not recognize the event either, so the
   suite cannot pass on a divergence between them.
2. **Nothing downstream runs.** `booking-webhook.ts:143` is `if (!event) return { handled: false }`,
   and `app/api/webhooks/stripe/route.ts:46` turns that into a 200. Verified, acked, ignored — which
   is the right posture: a non-2xx would make Stripe retry an event nobody wants.
3. **There is nothing to expire.** No `expired` state exists (decision 1) and no operation moves a
   reservation to one (criterion #9). Even a handler that wanted to do this could not.

**Whether the event even arrives is not readable from this repo**, and the codebase says so:
`route.ts:21` records that an earlier design *"rested on a Stripe dashboard nobody can read from the
repo (#544)"*. The endpoint's subscribed event list lives at Stripe. Same discipline criterion #7
applied to `receiptEmail` — this document does not assert third-party configuration it cannot read.
It does not matter for the verdict, because reason 1 holds whether or not the event is delivered.

**The nearest test exercises the path and not the claim.**

```
$ grep -rn "payment_failed" --include=*.test.ts src/
(no output)
```

`booking-webhook.test.ts:320` is *"handled:false for a non-checkout event"* and passes the literal
body `"null"` — it pins that an unrecognized event is acked rather than erroring, through a
degenerate input rather than a real `payment_failed` payload, and it asserts only the return value.
The one test that also asserts **nothing was written** (`:82-85`, reservations flag off) is a
different case. So even the ack behaviour is only half pinned.

**The forward-looking half is the reason this criterion exists, and it is not yet a hazard.** §2.8
puts it in the list because once a `pending` row exists, `payment_failed` becomes a tempting handler:
an event arrives saying this payment failed, and the obvious thing is to mark the row `expired`. That
would be wrong — a declined card is retried on the same reservation (criterion #10), so expiring on
failure destroys the row the retry needs. The guard §2.8 already writes for the neighbouring case is
§2.8.8's *"The sweeper never labels a row it cannot prove was unpaid"* (`:1840`); this criterion is
the same rule aimed at the webhook instead of the job.

*(2026-09-04, task 14.1: there is no sweeper any more, so that sentence is gone. The rule survives
on the reaper — §2.8.8 now says it "never deletes a row it cannot prove was unpaid" (`:1844`) — and
the temptation this criterion guards against is smaller, because there is no `expired` state for a
`payment_failed` handler to write. The reading below is unchanged: ack and ignore.)*

**So the useful reading is: nothing to fix, and one thing not to build.** When the `pending` row
lands, the correct handling of `payment_intent.payment_failed` is still to ack and ignore it — and
that will need a test naming the event, because at that point the absence will no longer be
structural.

### Criterion 12 — a late-succeeding superseded payment — **PARTIAL**

`docs/SPEC.md:2074-2075`. **The money half is built and well tested. The identity half cannot be built
in the current shape, and the reason is the sharpest structural finding in this audit so far.**

**The relationship §2.8.5 assumes is inverted in the code.** §2.8.5 (`:1734-1737`) requires that a
reservation carry many payment ids over its life, and warns that *"one overwritable column loses the
first id and turns a late success into an unrecognisable charge."* There is no such column to
overwrite — because the arrow points the other way:

| | |
|---|---|
| `confirm-booking.ts:55` | `key: pi.paymentIntentId` |
| `booking-webhook.ts:280` | `const idempotencyKey = charge.key` |
| `booking-webhook.ts:309` | `const reservationId = reservationIdFor(idempotencyKey)` |
| `write-booking.ts:35-38` | `reservationIdFor` = `sha256(idempotencyKey)` |

**The reservation id is derived from the payment id.** So two PaymentIntents cannot resolve to one
reservation — they derive two different reservation ids, by construction. A booking payment can never
be *superseded* on a single row, because there is no single row for it to be superseded on.

**Storage is not the constraint.** `Payment` (`entities.ts:827-831`) is its own row carrying
`reservationId`, so many-payments-per-reservation is representable, and the **balance** path already
does it (`recordBalancePayment`, `booking-webhook.ts:787-801`). Post-trip gratuity deliberately does
**not** — `booking-webhook.ts:749` says *"**Writes NO `Payment`** — a Payment would inflate the 'paid'
sum and drive `balanceOwedCents` negative → a false overpay alert"* — and puts the money on its own
`Gratuity` row instead. So it is only the **booking** payment that is welded to the reservation's
identity.

**What actually happens to the late success, and it is the right outcome by a wrong road.** The
second PaymentIntent books. The first succeeds late, derives a *different* reservation id, reaches
`writeSlotBooking`, finds the slot taken — by the customer's own booking — and returns `lost`. That
lands in the DEC-109 residual-race branch (`booking-webhook.ts:509-529`): auto-refund keyed on
`refund_${charge.key}` so a redelivery cannot double-refund, plus a "sold out while you were paying"
notice, with a loud manual-refund alert as the fallback when the refund itself throws.

So the customer is refunded and nobody is charged twice. **But the system cannot tell the two cases
apart** — a stranger who lost the race and the same customer's own superseded payment take the
identical branch, and the second one is told the trip sold out while they were paying, which is not
what happened to them.

**And the money leaves no trace on our side.** `booking-webhook.ts:505` states it as a deliberate
choice: *"The money moved, and is NOT recorded here (#613 — no reservation to reference)."* No
`Payment` row is written for a losing or superseded charge, so the record that money moved and came
back exists only at Stripe. That is the same shape as the §2.8.5 finding under the sales-tax section:
a thing §2.8 wants on our own row is currently only at the payment provider.

**The refund half is tested five ways, including both failure modes** —
`create-departure-payment-intent.test.ts:350` (*"residual race on the PI path: loser auto-refunded
keyed on the PI id"*), `create-departure-checkout.test.ts:139` (two paid sessions, one booked one
refunded and notified), `:169` (**no `payment_intent` to refund → manual alert**), `:183` (**the
auto-refund itself throws → falls back to the manual alert**), and `confirm-booking.test.ts:134`
(the success page does *not* refund — that is the webhook's job), whose `:162` comment notes an
assertion added specifically to prove the test reaches the residual-race branch at all.

**The supersession case IS tested — under another name, and what it pins is the behaviour §2.8 calls
wrong.** `create-departure-payment-intent.test.ts:350` is named for the residual race and its comment
at `:353` says *"Both buyers minted intents for the SAME small boat."* But `:356` builds **one**
metadata object and replays it for both events:

```js
const m = pay.intents[0]!.metadata;
const first  = await processBookingWebhook(deps, piEvent("pi_fake_1", 27570, m), FAKE_SIGNATURE);
const second = await processBookingWebhook(deps, piEvent("pi_fake_2", 27570, m), FAKE_SIGNATURE);
```

Same customer name, same email, same slot — **only the PaymentIntent id differs**, which is exactly
one buyer with two payment attempts. `create-departure-checkout.test.ts:143-144` is the same shape:
`slotCompleted()` hardcodes the customer and is called unmodified for both session ids. Nothing in the
outcome logic reads customer identity anyway — `write-booking.ts:92-94,151-152` decides `already`
versus `lost` purely on `getReservation(reservationIdFor(idempotencyKey))`.

So the criterion's own case runs on every suite. **What the test asserts is
`{ handled: true, outcome: "lost" }` and a refund** — it pins the payment *failing* to resolve to a
reservation, which is the current behaviour and the opposite of what this criterion requires. The
test is not wrong; it documents the built design faithfully. But it means the gap here is not
coverage. It is that the covered behaviour is the one §2.8 replaces, and the test will have to be
rewritten rather than added to.

*(An earlier draft of this paragraph claimed the opposite — that every residual-race test is two
different buyers and the supersession case is untested. That was wrong, and it was **the fifth
faulty absence claim in this document, written one criterion after adopting the rule against exactly
this**. The grep for `supersed` returned nothing and I let a word search stand in for reading the
fixtures. The rule was "write the command and its output"; the failure mode it did not cover is
running a command that answers a different question than the sentence does.)*

**What it costs when decision 2 lands.** §2.8.6's `confirmReservation(paymentId)` finds the pending
reservation *by the payment id recorded against it*, which is a lookup, not a derivation. That is a
different identity model from the one built, and adopting it means the reservation id stops being a
function of the payment — which in turn is what makes the deterministic `ON CONFLICT (id)` idempotency
at `write-booking.ts:32-33` work today. **Whatever replaces it has to carry that weight**, or the
redelivered-webhook double-write this hash currently prevents comes back.

### Criterion 13 — killing the webhook still books from the success page — **BUILT**

`docs/SPEC.md:2076`. **Unqualified, and the only criterion so far that the code cites back.**
`app/(public)/book/success/page.tsx:5` opens *"**This page CONFIRMS the booking** (issue #827, SPEC
§2.8 criterion 13)"*, and `confirm-booking.test.ts:86` is named
*"books a succeeded PaymentIntent, given only its id and no webhook at all"* — the criterion, in a
test title, asserting the criterion.

**It was built because the old reasoning was right about the premise and wrong about the conclusion.**
`page.tsx:6-10` records it: the page used to be static copy, on the reasoning that a redirect is not
proof of the write and the webhook is what books. True — *"and wrong about the conclusion: it left
`payment_intent.succeeded` as the ONLY path that ever books, so a delayed or misconfigured endpoint
meant the customer had paid and nothing had happened, with no bound on when anyone would notice."*

**The redirect is still not trusted.** Stripe appends `payment_intent` and `redirect_status` to the
return URL and both are text in an address bar, so `confirmBookingByPaymentIntent`
(`confirm-booking.ts:87-95`) resolves the id **against Stripe** —
`deps.payments.getSucceededPaymentIntent(paymentIntentId)` at `:91` — and returns `ignored` on
anything Stripe does not report as succeeded.

**And the adapter checks the status, not merely that the intent exists**, which is the difference
between the claim holding and not: `stripe-payment.ts:160` is `if (pi.status !== "succeeded") return
null`, with a `catch` returning `null` at `:167-169`. A forged id that happens to name a **real but
unpaid** intent resolves to `null` and books nothing — the case a bare existence check would have
missed.

**Failure is swallowed on purpose**, and the reasoning is worth keeping: `page.tsx:52-56` catches
everything, because *"a customer who paid should never see a stack trace because our fast path
failed, and the webhook is still coming for exactly this case."* The page is the fast path; the
webhook is the guarantee. Neither is load-bearing alone, which is the shape the criterion asks for.

**Five tests, and three of them are negative controls:**

| `confirm-booking.test.ts` | |
|---|---|
| `:86` | books from the PI id alone, **no webhook at all** — this criterion |
| `:103` | idempotent — the webhook landing afterwards produces no second booking |
| `:121` | refuses a PaymentIntent Stripe does not report as succeeded — *"the redirect is not proof"* |
| `:134` | does **not** refund or notify on a residual-race loss; that stays the signed webhook's job |
| `:169` | does **not** book the metadata-less intent behind a hosted balance checkout |

`:134` matters beyond this criterion: `confirmBookingByPaymentIntent` passes
`notifyOnResidualRaceLoss: false` (`confirm-booking.ts:93-95`), so a public page cannot trigger a
refund or a sold-out notice. Compensation belongs to the signed webhook only.

**One gap, disclosed by the code itself and verified rather than repeated.** `page.tsx:25-29` names
issue #831: *"`saveBookingIfSlotFree` reports who observes the row, not who inserted it, so two
callers racing inside the same instant can both be told `booked` and both send the customer their
booking link."* That is accurate. `postgres-repository.ts:1564-1568` decides the outcome with
`select 1 from reservations where id=$1` and `won.rowCount === 1` — a **read after** the write, not
an insert rowcount — and `:1352` records the limitation as deliberate. `write-booking.ts:151-152`
does the same on the other side, distinguishing `already` from `lost` by re-reading.

**The defect is pre-existing and this page makes it likelier**, which is exactly how the docstring
characterises it. **The duplicate is a second notification, not a second sale**, and that is two
citations rather than a reassurance: `booking-webhook.ts:454` gates `sendConfirmation` on
`result.outcome === "booked"` alone — *"never the idempotent `already` … or the customer gets
re-texted on every retry (DEC-122)"* — so a caller told `already` sends nothing, while a caller
wrongly told `booked` does. And the money write is insert-only against a deterministic id
(`in-memory-repository.ts:521` — *"Insert-only … a payment row is immutable once written, so a
re-delivered webhook is a no-op, not an overwrite"*), so the racing caller's `savePayment`
is a no-op. One sale, one Payment row, potentially two texts.

So it does not touch this verdict, but it is the thing to fix before a third confirm caller is ever
added. The race window is `write-booking.ts:92-94`'s early `getReservation` check, which is TOCTOU
against a concurrent caller holding the same reservation id.

### Criterion 14 — closing the browser still books, via the webhook — **BUILT**

`docs/SPEC.md:2077`. **Criterion #13's twin from the other side.** #13 asked whether the page books
without the webhook; this asks whether the webhook books without the page. The answer is one test:
`booking-webhook.test.ts:126` calls `processBookingWebhook` alone — no browser, no success page, no
client anywhere in it — and asserts the reservation is written, the `Payment` recorded with its
amounts and both Stripe ids, and `sendConfirmation` fired **once** with the freshly-booked row.

**"Produces a booking" has to mean one the customer can reach, and it does.** The webhook path mints
the booking code before composing the message (`app/lib/booking-confirmation.ts:59`, *"there is no
link to send without one"*) and sends over email and/or SMS (`:44-45`). A customer who closed the tab
gets their link without ever loading a page.

**Three degradations, all leaving the booking intact — and the third is silent.** If neither channel
is configured, `:45-52` logs and returns. A throwing `sendConfirmation` is swallowed rather than
bubbled, because a 500 would make Stripe retry the whole event; pinned by
`booking-webhook.test.ts:292`, *"a throwing sendConfirmation never breaks the committed booking
(best-effort, DEC-122)."* Both log in a production deploy and fall back to the operator's resend,
which `booking-confirmation.ts:58` names as the recovery.

**The third has no log at any level, and it is a documented trap.** `booking-confirmation.ts:32` is
`if (process.env.MESSAGING === "false") return` — before the `APP_BASE_URL` check, before the channel
check, and with nothing written anywhere. `docs/DEPLOY.md:105-113` records why (issue #761):
`messagingEnabled()` asks whether the flag is **on**, while this file and two others treat
`"false"` as a hard kill, so **`MESSAGING=0` leaves confirmations on and `MESSAGING=false` silently
kills them** while `messagingEnabled()` reads off either way. **This is the one thing that falsifies
the reachability half of this criterion in production**, and it does so with no trace — the booking
is written, the operator sees a sale, and the customer never hears. The verdict stands because the
criterion asks whether a booking is produced and it is; the reachability sentence above is scoped to
a correctly-configured deploy, and this is what "correctly configured" is load-bearing for.

*(The quoted test docstring credits **DEC-122**, which is **retired** — `DEC-122-…:9-11`, *"Every
ruling in this decision was adjudicated DEAD during the reservations decision decomposition — see
issue #816."* The best-effort rule it names is real and is restated in §2.8; the citation is the
test's, quoted here, not this document's own. Recorded rather than repointed, because a dangling
credit inside a test docstring is its own small finding.)*

**The boundary the criterion does not reach.** "At the moment of payment" splits in two. If the
browser closes *after* Stripe captures, the webhook books — that is this criterion and it holds. If it
closes *before* the payment succeeds — mid-3DS, say, which `checkout-form.tsx:189-193` sets up with a
`return_url` — then no payment succeeded, and booking nothing is correct rather than a gap. Which of
the two Stripe reports for any given card is Stripe's behaviour and is not readable here; the audit
only claims that a **succeeded** payment books with no client involvement.

### Criterion 15 — webhook killed, browser closed, past the window — **NOT BUILT**

`docs/SPEC.md:2078-2079`. **The most consequential finding in this audit.** Criteria #13 and #14 are
the two halves of the same safety net — the page books without the webhook, the webhook books without
the page — and this criterion removes both at once. §2.8.9 exists for precisely that case and is
entirely unbuilt.

**The code says so itself, in two places, in the same words.** `confirm-booking.ts:14` and
`booking-deps.ts:4` both read *"§2.8.6 requires the success page, the webhook and **(later) the
reconciler** to run the SAME idempotent confirm."* Two of three exist. The parenthetical is the
verdict.

```
$ grep -rln "reconciler" --include=*.ts src/ app/ | grep -v '\.test\.ts'
src/reservations/confirm-booking.ts
app/lib/booking-deps.ts

$ grep -rln "sweeper" --include=*.ts src/ app/ | grep -v '\.test\.ts'
(no output)
```

Both hits are the same forward-looking comment. Neither is an implementation.

**What actually happens today.** Stripe captures. The webhook is dead, so nothing books and no
`alertPaidButUnbooked` fires — **every call site is inside the two paths this criterion kills**:

```
$ grep -rc "alertPaidButUnbooked(" --include=*.ts src app | grep -v ':0$'
src/reservations/booking-webhook.ts:13
src/reservations/confirm-booking.ts:1
```

The browser is closed, so the success page never renders. The payment window passes, the hold expires
by the clock (criterion #8), and **the boat goes back on sale**. The customer has paid, holds a Stripe
receipt, has no booking, and nobody at the operator's end has been told anything. §2.8.9 describes
this exactly — *"money taken, no booking, nobody told"* (`:1865`) — and answers it with the sentence
that names the whole point: **"The reconciler exists so that no human is ever the one who notices."**

**Four places that might have caught it, each checked and ruled out.**

| | Why it does not |
|---|---|
| a scheduled job | `vercel.json:7-9` schedules `tick` and `doorbell-tick` only. `app/api/cron/xola-pull/route.ts:10-16` says *"NO CRON IS ATTACHED, AND NONE IS COMING"* — and it is Xola-side regardless |
| `/admin/purchases` | `app/(admin)/admin/purchases/page.tsx:106-116` reads `listAllReservations()` and `listAllPayments()` — **our own rows, never Stripe** — and in this scenario there is no row to show, because both checkout entry points write no reservation pre-payment |
| the integrity report | `src/admin/integrity.ts` checks referential integrity **between our own tables**. A charge that exists only at Stripe is invisible to it |
| the customer's own recovery | `app/b/find/actions.ts:71` filters candidates to `r.event !== undefined`. No `Reservation` row was ever written, so `/b/find` has nothing to find |

The last row is the one that closes the escape hatch: the customer cannot rescue themselves either.
No Stripe-dashboard or Stripe-email fallback is documented anywhere in `docs/` as a substitute
control.

**The one mitigation, and it is a delay rather than a control.** §2.8.9's own framing is that Stripe
*"retries on a backoff and eventually stops"* (`:1864`), so the webhook has to stay dead for the whole
retry window before the money is lost. That widens the window; it detects nothing, and the spec cites
it as the reason the reconciler is needed rather than as a reason it is not.

**Half of §2.8.9 is buildable today and half is blocked.** Its **primary** path — *"any reservation
still `pending` past its window is a work list"* (`:1870`) — needs the `pending` state that decision 1
adopts and the code does not have. Its **fallback** — Stripe's undelivered-event feed (`:1873-1876`),
for the case where the payment exists and our write did not — needs no `pending` row at all and could
be built against the design as it stands. That is worth knowing, because the fallback is the half that
covers a *total* webhook outage, which is this criterion's scenario.

**"Buildable" there means unblocked, not started.** `src/adapters/stripe-payment.ts` has no
event-listing capability at all — the port exposes `getSucceededPaymentIntent` for a single id and
nothing that enumerates. The fallback needs a new port method before it needs a job.

**The second clause is vacuously satisfied, and here that is bad news wearing good news' clothes.**
*"The sweeper has **not** marked their paid reservation `expired`"* is true — there is no sweeper
(command above) and no `expired` state (criterion #9). But it is true because nothing tracks the
reservation at all, not because a guard held. Criterion #11 had the same shape and there the vacuity
was harmless; here the same absence is what the first clause is complaining about.

**Where this lands in a rebuild.** §2.8.9 attaches three rules that are easy to lose and that the
build should carry as written: cadence in **minutes, not hours**, because *"detection latency is the
schedule"* (`:1878`); safe to run **at any time, in any order, more than once**, calling the same
idempotent confirm as the other two callers (`:1881-1883`); and **the operator's pause does not stop
it** (`:1885-1886`) — pausing the crew engine stops asking people to work, and must not stop
reconciling money that has already moved.

### Criterion 16 — three confirms, one of everything — **BUILT**

`docs/SPEC.md:2080`. **Two of the three are protected by never reaching the insert again; the third
reaches it every time and is rejected.** That distinction is the whole finding, and an earlier draft
of this section got it wrong by describing all three as one mechanism.

**The short-circuit is the guard, and it fires first.** `write-booking.ts:92-94`:

```js
const id = reservationIdFor(req.idempotencyKey);
const prior = await repo.getReservation(id);
if (prior) return { outcome: "already", reservation: prior };
```

That returns at `:94`. `eventIdForSlot` is not called until `:96` and `saveBookingIfSlotFree` not until
`:140`. **On a sequential re-confirm, the reservation and the Event are never written a second time —
the writes are not attempted at all.**

| What must not double | Why it doesn't, on a repeat |
|---|---|
| the **booking** | not re-attempted — `write-booking.ts:94` returns `already` on the deterministic id |
| the **Event** | not re-attempted — the Event write is downstream of that same return |
| the **payment** | **re-attempted every time and rejected.** `booking-webhook.ts:394-395` calls `recordPayment` on `booked` **and** `already`, so the insert genuinely runs; `postgres-repository.ts:1280` is `on conflict (id) do nothing` — *"a payment row is immutable once written… Idempotent by construction"* — matched by `in-memory-repository.ts:521` |

**So `postgres-repository.ts:1481`'s `on conflict do nothing` on the Event insert is a backstop for
*concurrent* races, not the sequential guard.** It is real and it matters — it is what covers two
callers arriving inside the same instant, which is issue #831 from criterion #13 — but this
criterion's own scenario never exercises it.

**This document already knew the load-bearing line and did not connect to it.** Criterion #13 names
`write-booking.ts:92-94` as *"the race window… TOCTOU against a concurrent caller on the same id."*
The same three lines are the sequential idempotence guarantee. One reading is the hazard, the other is
the protection, and they are the same code.

**And the slot guardrail is the interesting half.** `repository-contract.ts:865` pins a case the
criterion does not ask about: when an operator-created Event already occupies the slot, the booking
**claims that row** rather than materializing a second one — `expect(await repo.getEvent(SLOT_ID))
.toBeNull(); // no duplicate materialized`. So "one Event" holds not only against a repeated confirm
but against a confirm arriving at a slot that already had an Event under a different id. That runs in
the contract suite, which executes against **both** adapters (criterion #8).

**There is also an outcome gate, and it is deliberately not the thing being relied on.**
`booking-webhook.ts:454` confirms only on `booked`, never on `already`, so a redelivery does not
re-text the customer. `create-departure-payment-intent.test.ts:440-441` says exactly why that is a
separate concern: *"idempotency here is the deterministic id plus `on conflict do nothing`, **NOT the
outcome gate** — so running on `already` cannot double-record."* The gate protects the customer's
phone; the ids protect the data.

**No single test does the criterion, and the pieces sit at three different repeat-counts.**

| | repeats | asserts |
|---|---|---|
| `booking-webhook.test.ts:152-167` | **2** | one reservation, one payment, `confirm` called once |
| `create-departure-payment-intent.test.ts:448-452` | **3** | one *gratuity* — and nothing else |
| `booking-webhook.test.ts:563-564` | **1** | exactly one Event — but after a single booking, never a repeat |

So the Event count is never checked after a repeated confirm. **That gap is closed by construction
rather than by coverage** — the repeat returns at `write-booking.ts:94` and the Event write is never
reached — which is why this reads `BUILT` rather than `UNPROVEN`. A test doing all three at three
repeats would be one assertion block and is worth writing when someone is next in this file.

**The payment leg is the one where coverage matters most**, because it is the only one where the
insert actually fires on a repeat. It is covered — `booking-webhook.test.ts:163-164` asserts one
payment after two confirms — and the guarantee is a database constraint rather than control flow, so
it holds under concurrency where the short-circuit does not.

**On "three times" specifically.** §2.8.6 names three callers — the success page, the webhook and
(later) the reconciler — so the realistic version of this criterion is three *callers*, not three
retries. Two of the three exist (criterion #15), and the pair that does is covered in the order it
actually happens: `confirm-booking.test.ts:103`, *"is idempotent — the webhook landing afterwards
produces no second booking."* When the reconciler lands it becomes a third caller of the same
idempotent function, and this criterion is what says that is safe.

### Criterion 17 — the shift the booking earned — **BUILT**

`docs/SPEC.md:2081-2082`: *"**Confirming produces a shift for that vessel-day**, with seats, and a crew
already committed to that day is notified that it changed."* Three claims. All three are built, all
three are tested, and the third has a negative control — which is what makes this the strongest
criterion in the audit rather than merely a passing one.

**This is also the first criterion that crosses into the crew engine**, and the crossing is one object
wide. The booking writes an `Event`; `formShifts` reads events and derives everything else. Nothing in
`src/reservations/` knows what a Seat is.

| Claim | State | |
|---|---|---|
| a shift for that vessel-day | **BUILT** | `booking-webhook.ts:437-440` calls `formShifts`, which groups events by vessel + day |
| with seats | **BUILT** | `form-shifts.ts:415` → `deriveSeats(vessel, shiftId)` off the vessel's manning |
| crew already committed is notified | **BUILT** | `booking-webhook.ts:439` passes `notifyTripChanges: true`; `:441` forwards the result to the relay |

**Both confirm paths get it, and that is structural rather than remembered.** `confirm-booking.ts:54`
delegates to `processBookingCharge`, the same function the webhook runs, so the formation at `:436`
is downstream of *both* entry points — the signed webhook (`app/api/webhooks/stripe/route.ts:42`) and
the success page (`app/(public)/book/success/page.tsx:51`). Criterion #13 established that the success
page can be the confirming caller; this criterion is what says a booking taken that way is still
crewable.

**The relay is an optional dep, and the wiring is deliberately single-sourced.** `relayFormNotices` is
`?:` on `WebhookDeps` (`booking-webhook.ts:95`), so a route that forgot it would book correctly and
tell nobody. `app/lib/booking-deps.ts` exists for exactly that: *"it is defeated just as thoroughly by
two callers assembling different dependencies"* (`:4-11`). One `bookingDeps()` at `:29`, wiring
`relayFormNotices: forwardFormNotices` at `:46`, imported by both routes above. There is no second
assembly site to drift.

#### The notify half, end to end

The criterion says "notified", and the booking tests stub the relay — so the chain past that stub is
worth walking, because a stubbed relay proves the *call* and not the *notice*:

| link | |
|---|---|
| the diff is computed | `form-shifts.ts:525` pushes onto `changedCrew` |
| it reaches the relay | `booking-webhook.ts:441` — `await relayAndAudit(deps, form)` |
| the relay maps it to a notice | `form-notices.ts:41-53` — `changedCrew` → `action: "changed"`, operator filtered out |
| the notice is sent | `channel.ts:94` — `forwardNoticesToOutbox(formNoticeChanges(...))` |
| and persisted for the app banner | `channel.ts:95` → `recordFormChanges`, `:117-133` |

`form-notices.test.ts:61` — *"maps changedCrew → changed, excluding the operator (#350)"* — pins the
mapping link on its own. So the coverage is compositional rather than one end-to-end test, which is
the right shape here: the relay is shared by six callers and testing it once per caller would prove
the same thing six times.

#### Why the notify half is the interesting one

`booking-webhook.ts:414-419` records that `notifyTripChanges` used to be **off**, on the reasoning
*"nobody is on this shift yet — it is being born."* True of the shift being born and false of the
call: `formShifts` groups by vessel + day, so a second booking onto an already-crewed Saturday joins
that shift's trip set. Somebody's committed day grew a trip and they were told nothing.

That is issue #765, and it is tested from both sides:

| test | asserts |
|---|---|
| `booking-webhook.test.ts:605` — *"a booking that GROWS an already-crewed day tells that crew (#765)"* | `getShift(...).eventIds` goes to 2, and `changedCrew` is exactly `["cap-765"]` (`:642`, `:651`) |
| `booking-webhook.test.ts:654` — *"a booking that creates a BRAND-NEW shift still tells nobody (#765)"* | `createdShiftIds` is 1 and `changedCrew` is `[]` (`:670-671`) |

**The second one is why the first means anything.** Its own comment says so at `:657`: *"Without this,
'fires on a booking' and 'fires on every booking' look identical."* A criterion about notifying the
right people needs the test that nobody wrong is notified, and it is here.

#### "With seats" is guarded at both ends

Seats derive from the vessel's manning list, which is operator-editable — so "with seats" is
conditional on that list being non-empty, and a vessel with none would form a shift that reads fine
and can never be crewed. The fixture comment says it outright (`booking-webhook.test.ts:513`): *"Real
manning, or the shift forms with zero seats and 'crewable' means nothing."*

Two guards close it, neither of them in the booking path, and only the second is general:

- `vessel-admin.ts:81` — `if (manning.length === 0) return { ok: false, code: "crew_required" }`.
  Empty manning cannot be saved **through the admin screen** (#861).
- `derive.ts:83-84` — since #582 `deriveShiftState` **throws** *"Shift has no required seats — the
  vessel has no manning rule"*, rather than reporting it vacuously Crewed. `admin/shifts/page.tsx:314`
  catches it and logs rather than shrugging.

**The first guard is not the only write path, and the section originally said it was.** The other one
is `crew-admin.ts:36`, which checks `m.count < 1` and never `manning.length === 0` — and `.some()` on
an empty array is vacuously false, so an empty rule passes. `updateVessel` calls `createVessel` and
inherits it. Nothing live reaches that path today (`seed-brewboat.ts:73` and tests are the callers),
so the operator-facing guarantee stands — but it stands on the **second** guard, not the first. That
is why "with seats" is BUILT: not because a seatless vessel is unsaveable everywhere, but because a
seatless shift cannot be quietly read as fine.

#### Both failure legs are best-effort, and both are pinned

The booking is committed and paid before any of this runs, so neither formation nor relay may 500 —
Stripe would redeliver, resolve `already`, and re-run nothing (`booking-webhook.ts:407-412`).

- `booking-webhook.test.ts:690` — the shift store throws; the webhook still returns `booked` and the
  confirmation still sends.
- `booking-webhook.test.ts:674` — the relay throws; same.

And the catch at `booking-webhook.ts:442-450` carries a finding of its own: *"'The tick will re-form'
is true of the shifts and FALSE of the notices (#766)"* — a partial run's shift rows are durable, so
the next tick sees no diff and says nothing. Hence `if (e instanceof PartialFormError) await
relayAndAudit(deps, e.partial)` at `:446`, relaying what it worked out before giving up.

#### A separate finding: the code's own "only two triggers" claim is wrong

The formation code justifies itself with a count, and the count is false. `booking-webhook.ts:422`:
*"this webhook and the cron tick are the only formation triggers left."* The same claim appears
twice more in the same file — `:92` (*"one of only two `formShifts` triggers"*) and `:431` — and
twice again in the tests (`booking-webhook.test.ts:583-585`, `:609-611`). Five statements of it.

`grep -rn "formShifts(" src app --include=*.ts --include=*.tsx | grep -v test` returns seven lines —
the definition at `form-shifts.ts:131` and **six callers**:

| caller | turned off by the cutover? |
|---|---|
| `src/reservations/booking-webhook.ts:437` | no |
| `app/api/cron/tick/route.ts:75` | no |
| `src/reservations/cancel-reservation.ts:174` | **no** — a cancel is not an import |
| `src/builder/merge.ts:97` | **no** — operator command, wired at `app/(admin)/admin/shifts/actions.ts` |
| `src/builder/split.ts:80` | **no** — same |
| `src/import/xola-pull.ts:182` | yes |

So the cutover removes **one** of six, leaving five. `channel.ts:102` already says *"there are six
callers"* in its own doc comment, so the repo states both numbers about the same set.

**And DEC-126 does not say what the comment attributes to it.** Its ruling is one sentence — the flip
is a cutover rather than a natural drain — and the record states outright that it used to carry eight
more rulings including *"when the pull stops"*, which are *"gone rather than amended"*
(`docs/decisions/DEC-126-the-flip-is-a-cutover-with-a-one-time-full-xola.md:29-33`). Its only `file`
claim is `app/api/cron/xola-pull/route.ts`. The cutover certainly implies the pull stops; it rules
nothing at all about the formation trigger set.

**This does not move criterion #17's verdict.** The formation and the relay are correct whatever the
trigger count is. It matters because the count is load-bearing *rhetoric* — it is the argument for why
`notifyTripChanges: true` cannot stay off, and it is what a reader would use to scope a future change
to "just these two callers", silently skipping the cancel and split/merge paths. Filing it is the
right move; the fix is three comments and a test docstring.

**The real §2.8 gap is smaller and still worth a sentence.** A reader of §2.8 alone cannot see that
confirming has a crew half at all — the shift, the seats and the notice are the whole subject of this
criterion and appear nowhere else in the section. That is the sentence the spec is missing.

### Criterion 18 — selling a cancelled slot again — **BUILT**

`docs/SPEC.md:2083-2084`: *"Confirming a trip whose slot was previously booked and then cancelled
succeeds, and the Event carries the new booking's frozen values."* Two claims, both BUILT, and the
second is covered by a test whose title is almost the criterion:
`repository-contract.ts:1634` — *"resurrecting a cancelled slot re-freezes price and duration
IDENTICALLY on both adapters (#616)."*

| Claim | State | |
|---|---|---|
| the rebook succeeds | **BUILT** | `postgres-repository.ts:1514-1527` re-materializes in place; proven against real Postgres at `postgres-repository.test.ts:574` |
| the Event carries the **new** frozen values | **BUILT** | the same `update` sets `capacity`, `price` and `duration_minutes` unconditionally; `repository-contract.ts:1682-1686` asserts it on both adapters |

#### The hazard is sharper than "the rebook fails"

Without the resurrect, a cancelled slot is **bricked permanently**, and the two halves of the system
disagree about it:

- **The surface says free.** `availability.ts:346` puts an event in `eventBySlot` only
  `if (e.source === "muster" && e.status === "scheduled")`, so a cancelled row drops out of the
  materialized branch and the slot returns to the **virtual** grid — at the offering's current base
  price, not the dead booking's. `hull-busy.ts:80` agrees: `if (e.status !== "scheduled") continue`.
- **The write says lost, forever.** `events_muster_slot_identity` is
  `on events (vessel_id, date, time) where source='muster'`
  (`db/migrations/20260718142705_claim_hold_mutex.sql:28-30`) — **it does not mention status.** So the
  cancelled row still owns the identity: the insert at `postgres-repository.ts:1478-1481` conflicts
  into a no-op, and the `status='scheduled' … for update` select at `:1528-1533` finds nothing and
  returns `lost` at `:1536`.

A customer would therefore see the slot, buy it, and land in a residual-race loss — money taken, no
booking — on every attempt, for that slot, permanently. That is the failure this criterion is really
guarding, and `postgres-repository.test.ts:498-516` states it in the same terms.

#### Why the in-memory adapter could not have caught it

`postgres-repository.test.ts:509-513` is worth quoting because it is the DEC-131 trap again, one
criterion after #16 raised it: *"`InMemoryRepository` cannot show this, and would actively hide it:
its slot lookup filters on `status === "scheduled"`, misses the cancelled row, and then does
`#events.set(event.id, …)` on the SAME deterministic id — an accidental resurrect-by-overwrite that
passes the test while clobbering the row's frozen price."*

So the in-memory double would have been **green on claim 1 and silently wrong on claim 2**. That is
exactly the divergence `repository-contract.ts:1634` was written to close, and its own comment says
code review caught it: *"postgres writes `price`/`duration_minutes` from `?? null` unconditionally, so
a candidate carrying neither NULLS them. An in-memory version that only overwrote when defined kept
the DEAD booking's numbers — a resurrected slot priced at the previous customer's fare, on one
adapter only."*

**That quote is now history, and the comment carrying it does not say so.** The in-memory adapter
handles the case deliberately today — `in-memory-repository.ts:644-649` looks the cancelled row up on
purpose, and `:663-666` deletes `price` and `durationMinutes` before re-setting them only when the
candidate defines one, which is how it reaches "absent" rather than "inherited". Its own comment
(`:634-640`) says the accident is what it replaced: *"here the same case used to be handled BY
ACCIDENT, and only sometimes… a slot backed by an operator OVERRIDE row carries a different id, so
the old code left the cancelled override in place and added a SECOND event at the same identity."*

So `postgres-repository.test.ts:509-513` describes in the present tense a shape that no longer
exists. The sentence around it still holds — the Map has no partial unique index, so the **bricked
slot** genuinely cannot manifest there, which is why those tests live in the Postgres file — but a
reader checking the quote against `in-memory-repository.ts` will not find the code it describes. Minor,
and the same class as criterion #17's trigger count: a comment that was true when written and became
a record of the fix it caused.

**It also settles `dock`.** Both adapters state the same field policy independently —
`in-memory-repository.ts:642-643` (*"Same field policy as the pg adapter: re-freeze
capacity/price/duration from the new candidate… leave `dock` alone"*) and
`postgres-repository.ts:1507-1509`. So the carve-out below is a deliberate shared rule, not one
adapter's quirk.

The contract test's assertions are the criterion, stated as data: rebook with **no** price and **no**
duration, and `capacity` must be the new `8` rather than the dead `12`, with `price` and
`durationMinutes` coming back `undefined` rather than inherited (`:1650`, `:1661-1665`).

#### Two deliberate exceptions to "carries the new booking's frozen values"

Neither is a gap; both are reasoned in place and worth recording, because a future reader checking
this criterion against the code will find fields the sentence does not cover.

- **`dock` is not re-written** (`postgres-repository.ts:1507-1509`): *"it is a property of where the
  boat leaves from, not of the booking, and the candidate never carries one — writing `null` would
  erase an operator's dock assignment on every resale."* So "frozen values" means the money and the
  shape of the trip, not every column.
- **The re-freeze is scoped to `status='cancelled'`** (`:1511-1513`, and the `and status='cancelled'`
  in the SQL at `:1518`). It runs inside the hull-day advisory lock, but a predicate that could also
  match a **live** slot would let a losing concurrent booking overwrite the winner's frozen price.

#### What the real cancel path does, and where the coverage stops

`cancel-reservation.ts:139-145` writes the reservation cancelled, then `:160` calls
`cancelEventIfUnclaimed` — deliberately not a read-then-`saveEvent`, because cancelling the
reservation frees the slot *immediately* and a new buyer can win it between the read and the write
(`:153-159`). That is the same two-row state the tests set up by hand.

Coverage sits at two levels and stops below the webhook:

| level | test | adapters |
|---|---|---|
| `writeSlotBooking` — book, cancel both rows, rebook | `postgres-repository.test.ts:574` — *"the same slot can be SOLD AGAIN after a cancellation"*, asserting on SQL: one event row, `scheduled`, exactly one `booked` party | **real Postgres only** |
| `saveBookingIfSlotFree` — the re-freeze | `repository-contract.ts:1634` | **both** |
| the confirm path — webhook or success page | **nothing** | — |

**The missing level is the trivial one.** `writeSlotBooking` builds a fresh `candidateEvent` from the
new request on every call (`write-booking.ts:101-120`) and has no branch on whether the slot was ever
cancelled; confirming reduces to it through `processBookingCharge` (criteria #16 and #17). The
short-circuit at `write-booking.ts:94` cannot interfere either, since a rebook is a new payment and
therefore a new idempotency key. So this reads `BUILT` rather than `UNPROVEN` — but the two tests that
carry it are named for the *repository*, and someone reading §2.8 looking for this criterion would not
find them.

Also worth noting: `postgres-repository.test.ts:604` asserts the neighbouring-departure half in **both**
directions on purpose, because *"this slot's own re-sale is governed by the reservation check
(`status='booked'`) while its NEIGHBOURS are governed by the hull-overlap query
(`status='scheduled'`). Two different predicates, two different rows, and a one-sided test cannot tell
them apart."* Criterion #18 only asks about the first; the second is what makes a cancelled 13:30 keep
14:00 unsellable, which criterion #3 and #4 rest on.

### Criterion 19 — the unmatched booking charge — **PARTIAL**

`docs/SPEC.md:2085-2088`. **The longest criterion in §2.8 and the only one that spells out its own test
plan**, which is what makes the result awkward: the delivery half is the best-tested money code in the
repo, and the two things the criterion explicitly says to *assert* are the two nothing asserts.

| # | sub-claim | verdict |
|---|---|---|
| a | alerts **every active admin** by SMS | **BUILT** |
| b | **naming the amount** | **PARTIAL** — named on the two booking-shaped branches, absent on three others, and no test reads an amount out of a booking alert |
| c | never dropped silently | **BUILT** |
| d | *"Make the alert path throw and assert the webhook still returns success"* | **NOT BUILT** |
| e | *"Run it once with SMS unconfigured and confirm the log line is the only trace"* | **UNPROVEN** |

#### First: which branch is "a booking-charge payment that matches no reservation"?

The criterion does not say, and **seven** branches fit the words. They behave differently, and only
two name the amount:

| branch | situation | self-identifies as a booking? | names the amount? |
|---|---|---|---|
| `booking-webhook.ts:300` | booking session carries no slot (the retired legacy shape, #693) | yes | **yes** — `${charge.amountCents} ${charge.currency}` |
| `:329` | booking metadata unparseable | yes | **yes** |
| `:272` | verified booking charge while `RESERVATIONS` is off | yes | **no** — `${charge.key}` only |
| `:511` | residual-race loss, no `payment_intent` to auto-refund | yes | **no** — `${charge.key}, ${who}` |
| `:538` | residual-race loss **and** the auto-refund failed | yes | **no** |
| `:178` | checkout session with an unrecognised `purpose` | **no** | **no** |
| `confirm-booking.ts:48` | PaymentIntent with an unrecognised `purpose` | **no** | **no** |

**The last two are the arguable ones and are listed rather than dropped.** `booking-webhook.ts:177`
fires only when `purpose` is present and is none of `booking`, `balance`, `gratuity` — so the charge
has declared itself to be something else, and on a narrow reading it is not a *booking* charge at all.
On the reading that matters to an operator it is: verified money captured, no reservation, and nobody
able to say what it was for. `${who}` at `:501` does not smuggle an amount in either — it is
`${m.customerName || "customer"} party of ${partySize}`.

The first two rows are the closest reading — a booking charge that resolves to no reservation because
the metadata cannot produce one — and both name the amount. But `:509` and `:536` are also booking
charges matching no reservation, arriving by the route §2.8.8 and §2.8.9 are about, and they carry the
customer and the charge key instead of cents. **Whichever the criterion meant, it should say so**; as
written it is satisfied and unsatisfied at once depending on which branch a reader picks.

*(This table was five rows until code review; the two `purpose` branches were missed, and omitting
them without stating an exclusion rule is the same defect this document keeps finding in the code.)*

#### (a) and (c) are BUILT, and well

`forward-money-alert.ts:45` fans out through `listActiveAdminRecipients`, best-effort per recipient so
*"one dead number cannot mute the rest"* (`:29`). Its test file is unusually pointed — its own header
says the behaviour worth pinning *"is not 'a message was composed'"*:

| `forward-money-alert.test.ts` | asserts |
|---|---|
| `:51` | two active admins texted, a **deactivated** one excluded, `kind: "admin_alert"`, the link |
| `:80` | one carrier rejection ⇒ the other admin still gets it (`sent` is 1) |
| `:96` | no admins, and an admin with **no phone** ⇒ `0`, not a throw |
| `:106` | a repository outage ⇒ resolves `0` — *"the webhook must not 500 over a failed alert"* |

And "never dropped silently" is structural at the edge: `alert.ts:59` writes the log line **first and
unconditionally**, before any repo or channel call — *"the log line is the floor, not the fallback"*
(`:45-47`). `:66` adds a second line when the fan-out reaches nobody, so *"nobody to tell"* and *"told"*
are distinguishable in the log.

#### (b) The amount is in the code and in no assertion

`booking-webhook.test.ts:184` is the only test of an unmatched **booking** charge —
*"refuses a legacy eventId-shaped session: alerts, books nothing (#693)"* — and its assertion on the
message is `:196`: `expect(alert.mock.calls[0]![0]).toContain("REFUND MANUALLY")`. Not the cents, not
the currency. The test is good at what it is for (`:198-200` also asserts **no reservation**, so it
cannot pass by booking-and-alerting) — it simply does not cover this clause.

`forward-money-alert.test.ts:66` does assert an amount survives delivery — `expect(body)
.toContain("53625")` — which covers the *transport*, not the composition. And its fixture is worth a
note of its own, below.

#### (d) NOT BUILT — and the production guarantee is real but lives elsewhere

`grep -rn "alertPaidButUnbooked" --include=*.test.ts src/ app/ e2e/ | grep -i "throw\|reject"` returns
**nothing** (exit 1). No test makes the alert path throw, which is the sentence the criterion spends
two of its four lines on.

It matters more than a missing assertion usually would, because **the core does not survive it**. Of
the 14 `alertPaidButUnbooked` call sites across `booking-webhook.ts` and `confirm-booking.ts`, exactly
**one** guards against the alert throwing — `:326-335`, whose `.catch(() => {})` exists so *"an alert
failure must not replace the underlying error"*. The other thirteen `await` it bare, including `:536`,
which sits inside a `catch` block and would throw out of it. A throwing alert therefore propagates to
`app/api/webhooks/stripe/route.ts:57`, which returns **500** — precisely the *"Stripe retry storm"* the
criterion forbids.

Counted rather than eyeballed:

```
grep -n "alertPaidButUnbooked" src/reservations/booking-webhook.ts src/reservations/confirm-booking.ts \
  | grep -v "alertPaidButUnbooked:" | wc -l          → 14
grep -n -A8 "alertPaidButUnbooked(" <same two files> | grep -c "\.catch("   → 1
```

**The guarantee does exist, but it lives at the edge rather than here.** `alert.ts:58-70` wraps
everything after the log line in `try/catch` and is documented as *"Never throws, for the same reason
the core sender doesn't: the only callers are Stripe webhooks, where an exception becomes a 500 and a
500 becomes a redelivery loop"* (`:49-51`). It is held by the one adapter that happens to be wired —
not by the function the criterion is about — and **nothing asserts either half**. `app/lib/alert.ts`
has no test file (`ls app/lib/alert.test.ts` → no such file).

**No claim here about what is deployed.** Reservations are not live; this whole document reads code
and tests, and "would 500" throughout means *would, when this runs*, not *does today*.

That is the shape worth acting on: a core that assumes a property of its injected dependency, an
adapter that provides it, and no check anywhere that the two still agree. A second adapter — or a test
double that throws — turns it into a redelivery loop against a charge that already succeeded.

#### (e) UNPROVEN — the SMS-unconfigured path

`alert.ts:63-64` does exactly what the criterion describes: `makeTwilioChannel` returns null on a
Twilio-dark deploy and the function returns, leaving *"the log line above… the whole alert"*. Nothing
tests it — there is no test of `app/lib/alert.ts` at all, and `grep -rn "makeTwilioChannel"
--include=*.test.ts` returns nothing. Same class as issue #761 (`booking-confirmation.ts:32` silently
returning on `MESSAGING === "false"`), which this document raised under criterion #7.

#### Separate finding: a fixture that says "verbatim" and is not

`forward-money-alert.test.ts:19-22` introduces its fixture as *"Verbatim from `booking-webhook.ts` —
the copy that actually ships."* It is not. Side by side:

| | text |
|---|---|
| the test (`:20-22`) | `…which matches NO payment in Muster - RECONCILE MANUALLY.` |
| what ships (`booking-webhook.ts:656-657`) | `…which matches NO payment in Muster. The ledger is unchanged; RECONCILE MANUALLY (this is expected for a Xola-era or hand-taken charge).` |

The test is still sound — it asserts the id and the amount survive the channel, and both do. But the
docstring claims a coupling that does not exist: the string is a hand-copy, not an import, so it
cannot detect drift in the shipping copy and never could. That matters because this fixture is the
repo's stated defence of the #777 ASCII rule (`forward-money-alert.ts:16-19`) — one em dash in the
real message doubles the SMS bill and truncates the payment-intent id off a notification preview.
**A hand-copied fixture cannot catch a character introduced into the original.** The honest fix is
either to export the message builders and assert on them, or to delete the word "verbatim".

Note also that this fixture is the **refund** branch (`booking-webhook.ts:654`), not a booking charge —
so the one place an amount is read out of an alert body is not testing the criterion's own case.

#### What would close this criterion

Small, and worth doing when the reconciler lands (criterion #15) since that adds a third caller:

1. One test injecting `alertPaidButUnbooked: async () => { throw … }` and asserting the webhook still
   resolves — the criterion's own words, currently unwritten.
2. Either catch at all 14 call sites, or state in `booking-webhook.ts` that the dep is required never
   to throw and assert that property of `alertMoneyProblem` in a test of the edge.
3. Assert the cents on the booking-charge alert at `booking-webhook.test.ts:196`, which is a one-line
   addition to a test that already runs.
4. Decide which branch the criterion means, and say so in §2.8.

### Criterion 20 — editing the offering mid-checkout — **PARTIAL**

`docs/SPEC.md:2089-2090`. ⚠, and the vocabulary substitution is the usual one: *"while a reservation is
pending"* is, in today's code, *"while a `CheckoutHold` is live"* — the same mapping criteria #8 and
#10 use. The window is **checkout-start → confirm**, default 15 minutes (`claim.ts:70`).

**The charge is protected on all four fields. The occupancy is not — trip time changes it.** The fix
is the freeze §2.8.4 step 3 already specifies, not a new guarantee and not the pause — see the closing
section, which is the part to read if you only read one.

| the operator edits | charge? | occupancy? | |
|---|---|---|---|
| **price** | **safe** | n/a | resolved at intent creation and carried through Stripe |
| **hold minutes** | **safe** | **safe** | vacuously — `Offering.holdMinutes` is not read on this path at all |
| **trip time** | safe | **CHANGES IT** | read from the offering at *confirm*, not at checkout-start |
| **schedule** | **safe** | **safe** | vacuously — confirm re-validates nothing (criterion #5) |

#### Price is frozen at checkout-start, and that is the real freeze

`create-departure-payment-intent.ts:127` resolves `priceCents` once — `slotEvent?.price ??
resolveBasePrice(offering, hold.date)` — and `:177` stamps it into the Stripe metadata. At confirm,
`write-booking.ts:111` writes `price: req.priceCents`, which came back out of that metadata. **The
offering's price is never re-read on the confirm path.** An edit inside the window cannot reach the
charge, and could not even if someone wanted it to.

**The moment is right; the mechanism is one DEC-164 rejects.** That record — sitting in the same
amendment banner as DEC-161, two lines above the §2.8.4 text quoted below — rules that the frozen
money is *"one `booking_invoice` on the reservation — not a column per component, and **not in Stripe
metadata**"*, and says the current arrangement has no decision behind it: *"§2.8.5 and §2.8.14 already
forbid it, and issue #812 asks for the retool"* (`:23-25`). Its sharp edge is
`Number(charge.metadata.taxCents ?? 0)` — *"a dropped key produces a booking recorded with zero tax,
and nothing raises"* (`:27-28`). *"Rejected: leaving it at Stripe. A payment processor is not a store
of record"* (`:36-37`).

**That converges with this criterion rather than competing with it**, and the convergence is the
useful part: DEC-164 moves the money onto the reservation, DEC-161 puts both durations on the same
row, and decision 2 is what creates that row. **They are one piece of work, not three.** When the
`pending` reservation lands carrying `booking_invoice` and both durations, criterion 20 goes green as
a side effect — which is the strongest argument yet for not building anything bespoke for it.

#### Trip time is not — and this is the finding

`write-booking.ts:117-119`:

```js
...(offering?.tripLengthMinutes !== undefined
  ? { durationMinutes: offering.tripLengthMinutes }
  : {}),
```

That is a **live read of the offering at confirm time**, four lines after the request-carried price.
So an edit landing inside the checkout window is picked up, and it lands on the Event's
`durationMinutes` — which is precisely what occupancy is measured by: `hull-busy.ts:92` pushes
`{ start, end: start + (e.durationMinutes ?? XOLA_TRIP_MINUTES) }`.

**Concretely.** A customer starts checkout on a 13:30 departure whose offering declares 100 minutes.
Eight minutes later the operator edits `tripLengthMinutes` to 180 and saves — `offering-admin.ts` has
no guard on editing an offering with live holds, and grepping it for `hold`, `live` or `pending`
returns only field names and the status list. The customer pays at minute twelve. The Event
materializes occupying **13:30–16:30** instead of 13:30–15:10. A 15:15 departure that was sellable is
now blocked, and `shiftEndFromEvents` moves — which `entities.ts:604-605` says drives the completion
sweep, and so crew reliability.

#### The comment says these two are frozen for the same reason, and they are frozen at different moments

`write-booking.ts:112-114` introduces the duration as *"Trip length FROZEN off the offering at
materialization (#570), **the same reason** `price` is frozen one line up: a later edit to the
offering must not change how long a trip that already ran was."*

Both halves of that sentence are true and they are about different windows:

| | fixed at | protects against an edit… |
|---|---|---|
| `price` | **checkout-start** (Stripe metadata) | during checkout **and** after |
| `durationMinutes` | **materialization = confirm** | after confirm only |

`entities.ts:599-602` states the intent in the same terms — frozen *"never resolved from the offering
on read… because a later edit to the offering must not retroactively change how long a trip that
already ran was."* **That is exactly right about the post-confirm window and says nothing about this
criterion's window**, which is the one §2.8 asks about.

#### The test named for this proves the other window

`write-booking.test.ts:109` — *"FROZEN, not resolved on read: editing the offering later leaves the
booked event alone"*. Its body:

```js
const repo = await slotRepo({ tripLengthMinutes: 240 });
const r = await bookSlot(repo);              // ← confirm happens HERE
await repo.saveOffering(offering({ tripLengthMinutes: 90 }));   // ← edit lands AFTER
expect((await repo.getEvent(r.eventId))?.durationMinutes).toBe(240);
```

The edit is at `:119`, **after** the booking at `:115`. The test is correct and the behaviour it pins
is real — but criterion 20's edit lands *before* `bookSlot`. Its own comment (`:110-111`) repeats the
*"same reason `price` is frozen"* framing, which is how the gap stayed invisible: the file contains a
test named for freezing trip length, so the property reads as covered.

**This was run, not reasoned.** A throwaway probe against `InMemoryRepository` — offering saved at
100, edited to 180, *then* `writeSlotBooking` — asserted the Event should still occupy 100 minutes and
failed:

```
AssertionError: expected 180 to be 100
  expect((await repo.getEvent(r.eventId))?.durationMinutes).toBe(100)
```

The probe was deleted rather than committed; this document is read-only and the real version belongs
in `write-booking.test.ts` beside the test it mirrors. But the verdict rests on an observed failure
rather than a reading of the code.

Nothing anywhere edits an offering between hold and confirm:

```
grep -rn "mid-checkout\|while a hold\|edits the offering\|changes the offering" \
  --include=*.test.ts src/ app/ e2e/     → exit 1, no matches
```

*(This sentence first reported "one unrelated hit in `asks/lean.test.ts`", which code review could not
reproduce — correctly. The command that produced that hit also included `mid-flight`, and the term was
dropped when the command was transcribed into this document while its output was kept. The written
command returns nothing, which is the stronger form of the same claim. Logged rather than quietly
fixed because it is this document's own recurring failure — an output that belongs to a different
command than the one shown — and because it sits in the ~131 prose citations `check:audit` does not
pin, which is exactly the drift that tool's summary line warns about.)*

#### Two things are called "hold minutes" and only one of them is the offering's

Worth pinning, because the criterion says *"the offering's… hold minutes"* and that field is inert
here:

| | what it is | who reads it |
|---|---|---|
| `Offering.holdMinutes` (`entities.ts:358`) | *"Minutes the boat is held per departure, turnaround included (12.8). **Display/config**"* | nothing on the claim or confirm path — flaw #1's code side, unfixed (criterion #4) |
| `HOLD_MINUTES` (`claim.ts:70`) | the **checkout window**, env-driven, *"Resolved once at import — a change… needs a restart"* (`:68-69`) | `holdExpiry` (`:84-86`), which `claim.ts:308` stores on the hold row at acquire |

So editing the offering's hold minutes changes nothing on this path — **but not because anything
freezes it.** Nothing reads it. And a live hold's expiry is durable regardless, because it is computed
once and written to the row. If the criterion meant the *payment window*, the answer is different
again: that is an env constant, so it cannot be edited mid-checkout at all without a restart.

#### Schedule is safe for the reason criterion 5 found alarming

Criterion #5 established that `writeSlotBooking` performs **no** grid, season, block, fit or departure
check — *"It builds the candidate `Event` from the request and calls the CAS."* §2.8.6 sanctions this
outright: *"Nothing is re-validated at confirm."* So deleting the departure time from the schedule
mid-checkout cannot change what the booking occupies, because confirm never looks. The criterion is
satisfied by the same absence that made criterion #5 **NOT MET**.

#### The spec already answers this, and the answer is the freeze — not the pause

**This section was first written the other way round and that was wrong.** It argued that every field
here is operator-caused, therefore the whole criterion belongs to issue #864's reservation pause, and
therefore the fix is a screen rather than code. The first step is true and the conclusion does not
follow, because §2.8 already specifies the freeze.

§2.8.4 step 3 (`docs/SPEC.md:1570-1571`) — *"write the `pending` reservation with its reserved time;
**freeze the money, the hold minutes and the trip time onto it**."* And `:1577-1580` says what freeze
means and why, naming this exact case: *"Nothing recomputes from live settings, because a tax rate, a
service fee, a price or **a trip time can change between the quote and the payment**, and the customer
must get what they were shown."*

**DEC-161 rules the same thing** — *"Both durations freeze onto the reservation"* (`:39`), and its
`amends_spec` declares *"2.8.4a freezes both durations"* (`:18`).

So criterion 20 is not asking for a new guarantee to be invented. It is asking for a freeze the spec
and a decision have both already specified, and it costs **one more field in a write that already
happens**.

#### Two things that look alike and must stay apart

This is the distinction the first draft collapsed, and it is the reason the pause is right in one
place and wrong here:

| | what it means | verdict |
|---|---|---|
| **Re-validating settings at confirm** | re-check grid, season, blocks, fit across the checkout→webhook gap | **rejected** — complex, written for an audience of one. Issue #864's pause replaces it. This is criterion #5 |
| **Freezing a value at checkout-start** | write the trip time onto the pending row, beside the price already frozen there | **already specified** — §2.8.4 step 3. This is criterion #20 |

The pause answers a question about *whether a sale should still be allowed*. The freeze answers a
question about *which number the customer was shown*. Only the first needs operator judgement.

#### Why the code reads it late today

`write-booking.ts:118` resolves the duration at confirm because **there is nowhere earlier to put
it**. Decision 2's `pending` reservation does not exist yet, so the only row that can carry a frozen
duration is the `Event`, and the `Event` is not written until the payment succeeds. `CheckoutHold`
(`entities.ts:753-771`) carries `offeringId` and no duration — which is also criterion #4's finding,
from the other direction.

**So this is a symptom of the missing row, not a design choice to revisit.** When the `pending`
reservation lands, the freeze goes where §2.8.4 step 3 already puts it, and this criterion goes green
without anything being invented for it.

#### What issue #864 still covers, and what it does not

The pause is right, and the window list added to it (Eric, 2026-09-02) is right — a pause whose effect
you have to guess at is worse than none. What the pause covers here is the **settings-shaped refusals**
of criterion #5: block, season, grid, fit. What it does not cover is this criterion, because a frozen
value is not a refusal and needs no human in the loop.

A correction to that effect has been appended to issue #864, since the first comment there oversold
the pause for this case.

#### Why the verdict is PARTIAL

Neither mechanism exists today. The freeze is specified and unbuilt (it needs decision 2's row); the
pause is filed and unbuilt (issue #864 is open, and `isEnginePaused` at `repository.ts:732` is the
*crew* engine's switch — DEC-054, a different one, which does not gate reservations). `PARTIAL`
records what exists.

**The test is worth writing regardless**, because it pins which window is covered:
`write-booking.test.ts:109` reads as covering both and covers one.

### Criterion 21 — a balance payment never books — **BUILT**

`docs/SPEC.md:2091-2092`. A negative on a path the criterion itself calls dormant, so the question
worth asking is not *"does it hold?"* but *"does it hold for a reason, or only because nothing goes
down this road?"* It holds for a reason, at three depths, and both entry paths are tested.

#### The three depths

| | |
|---|---|
| **1. The dispatch returns first** | `booking-webhook.ts:175` — `if (purpose === "balance") return recordBalancePayment(deps, completed);` — nine lines before `processBookingCharge` at `:184`. The comment above it (`:172-173`) is the criterion in the code's own words: *"A balance payment records against the existing reservation; it must NEVER reach the booking path (no eventId → an orphan reservation)."* |
| **2. The function it lands in cannot write one** | `recordBalancePayment` spans `:787-857`. Every reservation-write and confirmation call in the file — `writeSlotBooking` (`:362`), `sendConfirmation` (`:461`) — sits inside `processBookingCharge`, above line 570. **None falls inside the balance function.** |
| **3. Even a mangled balance session refuses** | if the metadata lost its `purpose`, `:177`'s guard needs `purpose !== undefined` and falls through to `processBookingCharge` — where `isSlotBooking` (`:298`) requires `vesselId && date && time && offeringId`. A balance session carries `reservationId` instead, so `:299` refuses it as `unbookable` and alerts. It still does not book. |

The falsifying command for depth 2, since an absence claim needs one:

```
grep -n "saveReservation\|saveBookingIfSlotFree\|writeSlotBooking\|saveEvent\|sendConfirmation" \
  src/reservations/booking-webhook.ts
→ 41, 84, 285, 286, 312, 361, 396, 406, 407, 434, 460, 474, 562, 567
```

Highest is `:567`; `recordBalancePayment` starts at `:785`. What the function *does* write is one row —
`savePayment` at `:824` — and only after `:810-819` has confirmed the reservation already exists,
because *"the insert would violate the FK, throw, and take this alert with it"*.

#### Both entry paths are tested, and they are different tests

Confirming has two doors (criteria #13, #16, #17), so this criterion needs both shut:

| door | test | asserts |
|---|---|---|
| **checkout session**, `purpose: "balance"` | `booking-webhook.test.ts:366` — *"records a Payment{kind:'balance'} against the existing reservation — **no second reservation**, no alert"* | `:379` — `listReservationsForEvent(EVENT)` is **1**, with the comment *"did NOT run the booking path (no new reservation off the balance session id)"* |
| **PaymentIntent**, the metadata-less shadow behind a hosted balance checkout | `confirm-booking.test.ts:169` — *"does NOT book the metadata-less intent behind a hosted balance checkout"* | `:187-188` — outcome is not `booked` and `listAllReservations()` is **0** |

The second is the sharper one and its comment says why (`:173-176`): `/book/success` is *also* the
hosted-checkout landing for the balance flow, and every hosted session has an underlying PaymentIntent
that succeeds carrying **no** metadata, because the adapter never sets `payment_intent_data.metadata`.
That is DEC-134's double-write guard, and *"booking from that would sell a second reservation for a
balance top-up."* Not a hypothetical door — the same URL a real customer lands on.

#### "Never confirms" is by construction, not by assertion

The balance describe block (`booking-webhook.test.ts:365-480`) asserts `alert` was not called
(`:380`) and never asserts `confirm` was not called. Every `expect(confirm).not.toHaveBeenCalled()`
in the file — `:86`, `:197`, `:283` — is on a booking-path test.

That leaves the second verb of the criterion covered by construction rather than coverage:
`sendConfirmation` is called at exactly one place, `:460`, inside `processBookingCharge`, which the
balance branch returns before reaching. Same shape as criterion #16's Event count, and read the same
way — **BUILT**, because the guarantee is structural and the structure is asserted, not because
someone checked the verb. A one-line addition to the test at `:366` would close it properly.

#### Why the path is dormant is more interesting than that it is

The criterion's parenthetical says *"Muster charges in full at booking, so no balance payment is
created."* True, and it is held shut by **two** layers rather than by nothing being wired.

**The pane hides the button, and that is the layer the operator meets.** `createBalanceLink` is a live
admin server action (`actions.ts:82`), but the form that posts to it is conditional
(`reservation-detail-pane.tsx:516-521`): `money.priceKnown && money.balanceCents > 0 && v.status ===
"booked" && money.refundedCents === 0 && !money.disputed`. Its comment says why — *"Shown only when
money is actually owed — a 'collect balance' button on a settled booking is a trap"* (`:495-496`). In
full-charge mode `balanceCents` is 0 the moment the booking lands, **so the button does not render at
all.**

**The server refuses it too, and covers the cases the pane cannot.**
`create-balance-checkout.ts:53` — `if (owed <= 0) return { ok: false, reason: "no_balance" };` — with
the comment one line up stating the coverage directly: *"`<= 0` folds no_balance, already-paid, and
full-mode into one predicate."* So a stale tab, a re-post, or any caller that skips the pane is
refused. `create-balance-checkout.test.ts:167` pins it; `payment-config.test.ts:66` says the same from
the other side — *"full mode charges fare + tax + fee in one go — and leaves NO balance behind."*

*(An earlier draft of this subsection said "the button is there today" and named the server guard as
the reason it is dormant. Code review corrected both: the button is not rendered in the scenario the
criterion describes, and the pane is the first layer, not an afterthought. The related comment at
`create-balance-checkout.ts:60-61` — *"the pane's button is a courtesy; this is the thing that makes
minting the charge impossible"* — belongs to the **disputed** guard at `:63`, not to `no_balance`, and
is not evidence about this case.)*

**Where the button does come back is the interesting part.** A refund or a chargeback re-opens the
balance — `countsAsPaid` excludes both, so `owed` jumps back up — and at that point the server guard
would happily mint the link, because from its side money genuinely is owed. That is why the pane
carries three conditions beyond `balanceCents > 0`, each added for a real trap
(`reservation-detail-pane.tsx:500-515`): re-billing a customer the operator just refunded, or one
whose bank is currently clawing the money back, *"which is the fastest way to turn one dispute into
two"* (`create-balance-checkout.ts:57-58`).

**Worth recording because "dormant" invites the wrong maintenance instinct.** This is not dead code
awaiting deletion — it is a complete, tested path held shut by arithmetic and a render condition, and
one of its guards exists precisely because the path *does* wake up after a refund. The criterion says
it is *"kept for when deposits return"*, and the code is in the state that makes that cheap: waiting
on a config value rather than a rebuild.

### Criterion 22 — the tip pool — **BUILT, with "and no others" UNPROVEN**

`docs/SPEC.md:2093-2095`: *"The tip pool for a trip divides evenly across the confirmed holders of
its **required** seats and no others; the cents add up exactly to the pool; and a trip with a tip but
no confirmed crew produces a warning rather than a silent loss."* Four claims. Three are built and
tested. The fourth — *no others* — is built and has never been tested against a seat it should refuse.

**The whole criterion lives in one file**, `src/admin/gratuity-payroll.ts`, in two layers. The split
itself is pure (`splitGratuity`, `:48`): it takes gratuities and an `eventId → crew[]` map and knows
nothing about seats. Who is in the map is decided by the report wiring (`buildGratuityPayroll`,
`:102`), which walks the window's shifts and, for each, collects the crew on its seats that are
`Confirmed` **and** `required` **and** assigned (`:114`), into a `Set` (`:112`) — the dedup the spec
asks for at `SPEC.md:1648`. Every event on the shift gets that same set (`:118-121`), which is what
*"the confirmed holders of that shift's required seats"* means: a two-trip day pays both pools to the
crew who worked the day.

**Evenly, cents exact — BUILT.** `:69-70` floors the share and takes the remainder; `:71-73` hands one
extra cent to the first `rem` crew in lexical order of id, which is the spec's *"crew sorted by id, the
first `pool mod n` of them"* (`SPEC.md:1653`). `gratuity-payroll.test.ts:41-53` feeds 1001¢ to three
crew in unsorted order and asserts 334/334/333 **and** that the shares sum back to 1001 (`:52`). The
even case is `:28-39`. Both are the pure layer; the report layer's own even-split assertion is
`:121-131`, which also proves the Gusto join.

**No confirmed crew warns — BUILT, at the layer that can see it.** `:65-68` pushes *"… has $10.00 in
gratuity but no confirmed crew — unsplit"* and skips the event; `totalCents` does not include it, so
the operator's total and the sum of the rows agree and the missing money is named. Tested at the pure
layer, `gratuity-payroll.test.ts:66-74`. The wiring that produces the empty crew list for a live shift
with nobody confirmed is `:120` (a shift with zero qualifying seats still writes `[]` for each of its
events), and a probe through the report with one `Tentative` seat produced exactly the one warning.
The operator does see it: `app/(admin)/admin/payroll/page.tsx:193-200` renders `tips.warnings` in a
`Notice`, and `payroll-reconcile.ts:141` carries the same list into the combined report.

**"And no others" — UNPROVEN.** The filter is one conjunction at `:114`, and it is correct. But no
committed test puts a seat on the shift that the filter should refuse. `gratuity-payroll.test.ts:100-103`
builds every seat as `kind: "required", state: "Confirmed"` and nothing overrides either — so the
suite would pass unchanged if `:114` read `seat.state === "Confirmed" && seat.assignedCrewMemberId`,
with the supernumerary condition deleted. The other caller's fixtures do the same: `seed-gratuity.ts:91-98`
builds every seeded seat `required`/`Confirmed` too, so the seed-driven test adds no coverage here
either. The only supernumerary test anywhere in payroll is `payroll-reconcile.test.ts:308-315`, which
is about *hours* — a supernumerary owes no punch — and never puts a gratuity in play. The spec calls
the exclusion *"deliberate, not an oversight"*
(`SPEC.md:1650-1651`); a deliberate rule with no test is one refactor from becoming an oversight.

A probe (not committed) seeded a live shift with: two required Confirmed seats for A and B, a second
required seat also held by A, a supernumerary Confirmed seat for C, and a Tentative required seat for
C. The report returned A 500¢, B 500¢, total 1000¢. All three exclusions hold today. That is the test
the file is missing, and it is a fixture override away.

**One boundary the warning does not cover.** The *"never silently dropped"* guarantee is a property of
the split's zero-crew branch, not of the report. A gratuity whose event is on a **cancelled** shift, or
on **no shift**, is filtered out at `:131` — `eventsInWindow` only ever holds events of non-cancelled
shifts (`:110`, `:118-119`) — before the split sees it. Two probes confirmed: rows empty, total zero,
**warnings empty**. The existing test at `gratuity-payroll.test.ts:147-154` exercises the cancelled
case and describes it as *"nobody paid"*; it asserts on rows and total and says nothing about warnings,
so the silence is neither tested for nor tested against.

Is the state reachable? No — and the reason is a guard one level below the one you would reach for.

**A shift-canceller exists and runs today.** `form-shifts.ts:353-359` cancels a shift when
`scheduled.length === 0`, and `cancel-reservation.ts:174` calls `formShifts` on every cancellation for
exactly that purpose (`:163`, *"Re-form so the shift collapses and its crew are told"*). So the
filter at `:111` is not waiting on a future feature; it fires in the product as it stands.

What keeps the money safe is that the two conditions cannot hold at once. The shift only collapses
when **every** event on that vessel-day is cancelled, and the tip's own event cannot be among them
while its booking is live: the only release is `cancelEventIfUnclaimed` (`cancel-reservation.ts:160`),
which refuses on an event a `booked` reservation still holds. Cancel that reservation to free the
event, and the gratuity is now on a `cancelled` row, which `:128` excludes for the separate and
correct reason above. Either the booking is live and its event keeps the shift up, or it is cancelled
and the tip was refunded. There is no third state.

Muster gratuity rows are written in exactly two places, both after a booking commits
(`booking-webhook.ts:481-482` and `:766`), and the Xola importer cancels Xola events, which carry no
Muster gratuity. So the silent branch is unreachable by construction rather than by absence — worth
recording, because the thing holding it shut is an event-level claim check two modules away from the
shift-level filter that would drop the money.

*(An earlier draft of this paragraph said the shift-canceller did not exist yet and that the state
would need "a future operator action… if one is ever built". Code review found `form-shifts.ts:359`,
reached from the same function this section already cited. The verdict was right and the argument for
it was wrong — the trace named the reservation and the event and never named the writer of the field
its own filter reads.)*

**One comment overstates.** `:45` says the exact-allocation property is *"a contract-test invariant"*.
There is no such contract test — `repository-contract.ts:1476-1513` covers `saveGratuity`'s
idempotency and `listAllGratuities`, and never calls the split. The invariant is held by
`gratuity-payroll.test.ts:52`, a unit assertion, which is enough; the comment names a stronger guard
than exists.

**Not in scope here, but adjacent.** The pool only counts gratuities on `booked` reservations (`:127`),
so a cancelled booking's tip leaves the report without a warning. That is correct by `SPEC.md:1912`
(*"the crew tip pool pays out on `booked`"*) — the money went back to the customer through the refund
path, and there is nothing to allocate. Decision 1's four-status union changes nothing here: `pending`
and `expired` rows will never carry a gratuity row, because `:480` runs after the booking commits.

### Criterion 23 — occupancy without a payment — **BUILT on both shapes that exist** (⚠ the third is decision 2's)

**This criterion was rewritten before it was audited, and that is the finding.** It read *"an imported
Xola reservation, which has no payment recorded, still occupies its hull."* Xola is removed at the
cutover (DEC-126), so the line expired with the importer — and it was the only criterion in §2.8 that
fails if someone makes occupancy conditional on a `Payment`. DEC-165 restates it as the rule, and
`SPEC.md:2089-2092` is now the text audited below.

The old wording was wrong twice over. Xola was never the subject — and neither was the reservation.

**The rule holds in the strongest form available: there is nothing to get wrong.** `availability.ts`
is 446 lines and the string `payment` does not appear in it once (`grep -ic payment` returns 0). Same for the write path on both
adapters — `postgres-repository.ts:1421` and `in-memory-repository.ts:593`, the two implementations of
`saveBookingIfSlotFree`, read no payment and no paid flag anywhere in their bodies. This is not a
guard that happens to be correct; it is the absence of an input. A verdict of BUILT on a negative
usually means UNPROVEN in disguise, so it is worth being precise about which one this is: the claim
*"no occupancy decision consults a payment"* is structural, and the three shapes below are what give
it teeth.

**Shape 1 — an imported booking, through its `Event` — BUILT, both sides, contract-level.**
Hull occupancy is built at `availability.ts:359-360` by walking **every** scheduled event with no
source filter, which is `#615`'s fix and is spelled out in the comment above it (`:351-357`). The read
side is tested at `availability.test.ts:359-369`, *"a XOLA trip on the hull takes the slot off the
market"* — and the fixture is the interesting part: it passes `events` only, **no reservations at
all**. So the test proves the *Event* occupies the hull, which is the second thing the old criterion
had backwards. Delete the imported reservation row and the boat is still busy.

The write side is `repository-contract.ts:730-743`, *"saveBookingIfSlotFree: LOSES when a Xola trip
already holds the hull"* — again seeding an event alone, and running on **both** adapters, so this one
is proven against real Postgres rather than the in-memory substrate. Two more contract cases carry the
same fixture into harder ground: the overlap-not-identity case at `:745-756` and the untimed-trip case
at `:758-773`, where an imported row with no duration is measured at the standing length rather than
the new booking's.

**Why an imported reservation has no payment is structural, not incidental.** `src/import/` contains no
`savePayment` call at all. The importer writes an `Event` (`import-reservations.ts:241`) and a
`Reservation` (`:302`) and nothing else, both tagged `source: "xola"` per DEC-106. So the premise the
criterion tests is not a fixture convenience — the product cannot produce a paid imported row.

**Shape 2 — a live checkout hold, through its expiry — BUILT.** `availability.ts:333` admits a hold on
`h.source === "muster" && h.expiresAt > input.asOf`: a clock comparison, no payment. Tested at
`availability.test.ts:578-585` (a live hold marks the slot `held`) and, from the other side, at
`:587-594` (an expired one contributes nothing). A hold is unpaid **by definition** — it exists to
occupy a boat during the minutes before the money arrives — so this shape is the criterion's cleanest
case and the one least likely to regress.

**Shape 3 — a `pending` reservation, through its own row with no `Event` — NOT BUILT.** Nothing
occupies a hull without an `Event` today, because the pending row does not exist; §Criterion 6 carries
that verdict and §Criterion 2 carries the *"that row alone removes the trip from `/book`"* half. This
limb is decision 2's to deliver and is ⚠ for the same reason nine other criteria are.

**That the rewrite added an unbuilt limb is deliberate, and worth stating plainly.** The old criterion
would have gone BUILT and then died silently at Xola sunset. Worse, both shapes that pass today are on
their way out — the importer at the cutover, and `checkout_holds` at decision 2, which replaces the
hold with the pending row (see §Criterion 8, *"BUILT by the hold, not the pending row"*). A criterion
whose every example is scheduled for removal proves nothing about the system that comes after. The
third limb is what gives this one a future, and it overlaps §Criterion 2 only in the fixture, not in
the failure it catches: criterion 2 asks whether the row is written before Stripe and shows up on
`/book`; this one asks whether anything downstream consults the money.

### Criterion 24 — the recovery lookup and the unpaid row — **NOT BUILT** (⚠), and it is pre-loaded to fail

`docs/SPEC.md:2101`: *"The public booking-recovery lookup returns nothing for a `pending` or `expired`
reservation."* Neither status exists (`entities.ts:625`), so nothing can violate this today. That much
it shares with §Criterion 11 — and there the resemblance stops.

**Criterion 11 is safe by absence. This one is unsafe by absence.** Eleven asks that a Stripe event not
expire a reservation, and there is no handler, no downstream, and nothing to expire — three
independent reasons, so the day `expired` arrives, nothing starts doing the wrong thing. Twenty-four
asks that a matcher exclude two statuses, and **the matcher is written as a deny-list.** The moment
decision 1 lands, it admits them.

**One line decides it.** `find-booking.ts:76`:

```
const live = candidates.filter((c) => c.reservation.status !== "cancelled");
```

`!== "cancelled"` rather than `=== "booked"`. Everything that is not cancelled counts as live, and
`:77` then prefers `live` over the full candidate set. A `pending` row is not cancelled, so it is
live. So is an `expired` one.

A probe (not committed) put decision 1's statuses through the matcher against the wired query shape.
Five cases, all passing — which is to say, all wrong:

| Probe | Result |
|---|---|
| A single `pending` row | returned — the criterion's exact forbidden case |
| A single `expired` row | returned |
| `pending` vs `cancelled` | `pending` wins; it is sorted as the live one |
| `pending` at a nearer date vs `booked` at a later one | `pending` wins on "soonest upcoming" |
| status `"banana"` | returned — the deny-list admits anything it has not been told to refuse |

The last row is the one worth keeping. The filter does not encode *which* statuses may be recovered;
it encodes one that may not. Any status added later — by decision 1 or by anything after it — is
admitted by default, silently, with no test failing.

**What a wrong match actually does, which is worse than returning a row.** The matcher's result is
handed to `ensureBookingCode` (`recover-booking-link.ts:98`), which **writes**: `saveBookingCode` at
`ensure-booking-code.ts:63`, minting a durable code if the reservation has none. Then
`resendBookingLink` sends it to the stored contact. So recovering a `pending` row would mint a booking
code against an unpaid reservation and text the customer a link to it — and *"abandoning checkout
leaves no booking code behind"* is §Criterion 7, one of this audit's own criteria. The two would
contradict each other through a path neither one names.

**Nothing upstream filters, either.** The wired edge maps **every** row from `listAllReservations()`
(`app/b/find/actions.ts:66-71`) with no status predicate, and `recoverBookingLink` adds none —
`:94` passes the rows straight to the matcher. Status is consulted in exactly one place in this
chain, and that place is line 76.

**There is an accidental net, and it is worth naming precisely because nobody chose it.** The edge
joins each reservation to its event (`actions.ts:69`) and drops any row that does not resolve
(`:71`). Under decision 2 a `pending` reservation has a null `eventId` (§Criterion 6), so
`eventById.get(String(null))` misses and the row never reaches the matcher. The criterion would
therefore *appear* satisfied at the surface — via an event join written to avoid rendering an
orphaned row, not to enforce a status rule. It holds only while every unpaid row lacks an `Event`,
and it protects only this one caller. This is the same shape as §Criterion 19's alert guarantee,
which also rests on the wired edge rather than on the module that states it.

**Coverage.** `find-booking.test.ts:116-125` is the only status-aware case and it tests the cancelled
rule, both directions — a cancelled row loses to a live one, and is returned when it is all there is.
That rule is deliberate and well covered (`find-booking.ts:74-75` explains it: the receipt lives
behind the same link). Nothing tests what happens to a status the filter has never heard of, which is
the case that decision 1 creates.

**The fix is a one-character-class change, and it should land with decision 1 rather than after it.**
`=== "booked"` — or, if a cancelled row must stay recoverable, an explicit two-element allow-list.
Either turns the failure mode from "admits everything new" into "refuses everything new", which is the
correct default for a surface that mints a credential and sends it to a customer.

## §2.10 criteria

Not yet enumerated. §2.10 is `docs/SPEC.md:2344-2560`.

## Sales tax — and the thing standing next to it

Opened because **DEC-107** is titled *"Sales tax is read live, not frozen onto the booking"* while
§2.8.4a freezes every component. That reads as a live contradiction from the index. It is not, and
saying why matters more than the flag did.

**DEC-107 is already retired.** `status: "superseded"`. So the record does not contradict the spec;
it records a position the spec replaced.

**But nothing says what replaced it.** `superseded_by` is unset, and no decision in the tree declares
`supersedes: [DEC-107]` — `grep -rln "supersedes:" docs/decisions/*.md | xargs grep -l "DEC-107"`
returns nothing. `CLAUDE.md`'s convention is a pair: the new record carries `supersedes`, the old one
flips to `status: superseded`. Half of that pair happened. **`npm run check:decisions` passes**, so
the pairing is not enforced — a record can be marked dead with nothing pointing at what killed it,
and the gate is content. That is a record-integrity gap, not a reservations one, and it is likely
not unique to DEC-107.

**And the booking path does freeze the tax.** `create-departure-payment-intent.ts:133` computes
`taxCents` once at checkout from `config.taxRateBps` and never recomputes it. DEC-107's live read
was about the **balance** path, which §2.8.10 already documents as dormant and non-conforming. So
the contradiction I flagged from the title was wrong about scope, and the honest finding is
somewhere else — three lines further down the same file.

### Where the frozen money actually goes — **NOT MET**, and this is the serious one

Every frozen component is written into **Stripe metadata**, and §2.8.5 forbids that in as many words:

> For the human reading the Stripe dashboard, `description` — a first-class Stripe field — carries
> the offering, the trip, the party size and who booked. **So the booking charge sends no
> metadata at all.** Empty is a rule that can be checked; "send some but never read it" is a
> discipline that decays.

`create-departure-payment-intent.ts:160-181` sends **eighteen** metadata keys: `purpose`,
`offeringId`, `vesselId`, `date`, `time`, `guestCount`, `priceCents`, `extrasCents`,
`gratuityCents`, `gratuityBps`, `serviceFeeCents`, `kind`, `taxCents`, `customerName`, `email`,
`phone`, `waiverConsentAt`, `waiverVersion`. That is the entire booking — the slot, the money and
the customer — with Stripe as the store of record between checkout and confirm.

`booking-webhook.ts:614-621` reads it back to build the reservation. §2.8.14 names this too: *"No
booking assembled from data Stripe hands back."*

**The failure mode is silent and it is money.** The read is
`Number(charge.metadata.taxCents ?? 0)` — a metadata key that is missing, truncated or dropped
produces a booking recorded with **zero tax**, and nothing raises. Same shape for `gratuityCents`
(`:614`) and `serviceFeeCents` (`:618`), where a lost key means crew money and operator revenue
quietly reading zero.

This is **issue #812** — *"Retool the Stripe payment workflow — a booking must not depend on
metadata surviving the round trip."* Confirmed, and the audit adds the specific defect: the
defaulting is what turns a transport problem into a wrong number rather than an error.

It also explains why §2.8.5 puts the payment id on **our** row and reads nothing from the payment
object but its id and status. The freeze is supposed to live on the reservation. Today it lives at
Stripe.

## Spec gaps — things §2.8/§2.10 never said

Not conformance findings. The code does not disagree with the spec here; the spec is silent. Found
while auditing, and recorded with the operator decisions already taken so they survive a window
ending. Each is its own spec task with its own decision record.

The spec was written after the code and was never finished. These three are what that produced.

### Gap A — there is no admin booking surface

**Nothing in `app/(admin)/` creates a reservation.** No `writeSlotBooking`, no `saveReservation`, no
reservation construction anywhere in the admin tree. The admin surfaces are purchases, calendar,
blocks, customers — all of which read or modify bookings that already exist. So the operator cannot
take a booking over the phone at any notice, at any price.

**Why it was missed, which the design doc explains without meaning to.**
`docs/design/reservations-admin.md:31` says the surface set was enumerated from the Xola seller UI
screenshots — *"experiences/schedules config, purchases list + detail, dashboard, event detail pane,
customers/contacts."* Xola has a book-for-a-customer flow; it is not one of the captured screens, so
it never entered the enumeration and every document downstream inherited the hole. It was not lost
in the shuffle — it was never written down.

Decisions taken (operator, 2026-08-29):

- **The admin sends a payment link.** The reservation is written, holds the boat, and stays `pending`
  until the customer pays. **No card is read over the phone** — a card field on an admin form
  defeats SAQ-A, which applies precisely because card numbers never reach our servers.
- **No other payment methods.** No cash, no "already paid elsewhere". Everything goes through Stripe,
  which also keeps §3.3's refund path intact.
- **`source` stays `"muster"`.** `entities.ts:569` is `"xola" | "muster"` and `:563` notes widening is
  one line, but there is no use case for telling an admin booking from a web one. Not added.
- The payment window for a phone booking probably wants to be longer than the public one. Open.

*(2026-09-04, task 14.2: both of those were reversed by DEC-163 — an operator's booking has no
window and is never reaped, and the reader has to be able to tell, so it carries `admin` as its
source. `entities.ts:569` is now `"xola" | "muster" | "admin"`. Nothing writes the value yet; the
operator surface is Phase 16.)*

### Gap B — the booking cutoff

**A length of time before a departure during which the public cannot book it.** Measured backwards
from the departure. Inside it the public site refuses; the admin surface (Gap A) still books.

Decisions taken (operator, 2026-08-29):

- **A system setting, in `app_settings`** — the key/value table from
  `db/migrations/0006_app_settings.sql:18`, which already holds the `payment.%` rates
  (`postgres-repository.ts:1235`), `engine_paused` (`:2312`) and
  `self_claim_requires_confirmation` (`:2329`). A row plus a code fallback, and **the fallback is not
  the value** — same posture §2.8.4a describes for the tax rate. **No number goes in the spec.**
- **Computed, never stored.** A clock comparison at read time, like expiry — no rows, no job, nothing
  to sweep. It moves forward on its own.
- **Its own slot state**, not `blocked`. `availability.ts:410` currently renders `available`,
  `booked`, `blocked`, `unavailable`, `held`. A separate state keeps the operator's Blocked count
  honest and lets the customer be told *"too late to book online, call us"* rather than a flat
  unavailable — which only matters once Gap A means someone can take that call.
- It lands where blocks already land, on **both** paths — `availability.ts:426-429`'s precedence and
  `candidateVessels` — so the calendar and the write do not diverge again.

**Naming trap.** `docs/SPEC.md:347` already defines **booking horizon** — *"knowable far out; gates
the sale"* — which is the far end of the same axis. And `:324` carries *"a lead-time cutoff"* as an
explicitly **rejected** idea for crew asks in §1.3. The new term is **booking cutoff** and it needs a
dictionary entry to keep all three apart.

**Not the staffing horizon.** The operator picks the value by asking how much notice they want to
give crew, but the rule is not a staffing rule and must not be welded to
`STAFFING_HORIZON_LEAD_DAYS` (`src/builder/derive.ts:148`). The rationale for choosing a number is
not the definition of the number.

### Gap C — discounts

**Partial or full, applied by the operator today and plausibly by a guest promo code later.** Both
belong in one spec task.

Decisions taken (operator, 2026-08-29):

- **The discount comes off the fare-plus-extras base.** Then **tax, tip and the service fee all
  recompute on the discounted base.** The service fee explicitly follows the discount — discount the
  trip and the operator is paid less for it, so the 3% is 3% of the smaller number.
- Partial and full are both required.

**A full discount has nothing to confirm it, and this is the hard part.** §2.8.6 is
`confirmReservation(paymentId)` and it finds the pending reservation *by the payment id recorded
against it*. Comp a booking to zero and there is no payment, no id, no webhook, and no success page.
The reconciler will not find it either — it works a list of rows whose payments succeeded. So a
zero-balance booking needs a second entry point into the one function §2.8.6 exists to keep
singular, and that is a decision, not a detail.

**Also unresolved:** whether a discount is an amount, a percentage, or both; and whether it is frozen
onto the reservation like every other component in §2.8.4a (it must be).

## Open

- The five `docs/design/` documents that nothing points at — `the-booking-1.md`,
  `reservations-model.md`, `reservations-admin.md`, `the-living-link-1.md`,
  `BUILDER-RECONCILIATION.md` — are a second authority over the reservations surface and are **not
  listed** in the Additional Docs table at `.claude/CLAUDE-context.md:62-71`, which carries only
  `DESIGN-REFERENCE.md` and `mockups/`. Two of the five have been read. Conflicts between them and
  §2.8 go to the operator to adjudicate, one line each; the audit does not decide them.
