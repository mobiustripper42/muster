# DEC-105 — worksheet

**Source:** `docs/decisions/DEC-105-reservations-go-live-in-2026-as-a-muster-native.md`, 4,563 bytes, one banner amendment (reversed in part by DEC-126), 2026-07-11. Carries `amends_spec` claims on §0.3 and §4.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Evidence provenance.** Gathered 2026-08-26 against commit `f626cdf`, then re-verified in full: all 155 file citations across these worksheets resolve and none points past the end of its file. One apparent miss, `src/actions/enrollments.ts` in DEC-107e, is a path in the sibling `sailbook` repository rather than this one.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-105

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Muster starts selling and taking payment for inventory it owns end to end, in 2026 rather than 2027. | `app/(public)/book/` resolves; `create-departure-payment-intent.ts`, `booking-webhook.ts`, `payments` table (`20260715024322_payments.sql`) all exist. | DEAD |  |
| b | Muster and Xola run alongside each other permanently, not toward a cutover. | The record's own banner says this was reversed by DEC-126. `app/api/cron/xola-pull/route.ts` still exists and runs. | DEAD |  |
| c | Xola-originated bookings keep importing until they drain, and their money stays in Xola. | `events.source` and `reservations.source` default `'xola'` (`0023_reservations_source_partition.sql:28-29`). Xola rows carry no `payments` link. | DEAD |  |
| d | Muster shows no refund or cancel action on a Xola booking — at most a pointer saying to do it in Xola. | Not verified here: `cancel-reservation.ts` and `refund-payment.ts` exist, but whether the detail pane gates them on `source` was not checked. | DEAD |  |
| e | Muster-originated bookings are full-stack: public site, Stripe, atomic claim, native manifest. | `app/(public)/book/`, `stripe-payment.ts`, `claim.ts`, `hull-busy.ts` all exist with tests. | DEAD |  |
| f | There is no historical Xola migration, no forced cutover and no de-listing sweep. | No migration under `db/migrations/` backfills Xola history into Muster-native rows. `0023_reservations_source_partition.sql:28-29` backfills `'xola'` on existing rows instead. | DEAD |  |
| g | The whole reservations build is additive and dark behind a flag, touching neither the Xola pull, the ask tick, nor the shift and seat state machine. | `RESERVATIONS` flag at `app/lib/flags.ts:38`. Contradicted in part: `booking-webhook.ts:27,416` calls `formShifts`, so a confirmed booking does reach the shift builder. | DEAD |  |
| h | Rolling back is a single flag flip; the worst case is a handful of bookings hand-keyed into Xola. | Flag exists (`app/lib/flags.ts`, `env.example:39`). Whether a flip is sufficient is not mechanically checkable; issue #621 reports admin reservations routes ignore the kill switch. | DEAD |  |
| i | Muster never writes bookings, guides or refunds back into Xola. | No write-back code: the only `writeBack`/`write-back` hits are in `src/reservations/booking-webhook.ts` and `src/crewapp/shift-card.test.ts` as prose. The Xola adapter is read-only. | DEAD |  |
