# DEC-106 — worksheet

**Source:** `docs/decisions/DEC-106-coexistence-partition-whole-vessel-day-an-event-is.md`, 2,950 bytes, no amendments, 2026-07-11. Retired in part by DEC-149.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-106

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | A boat on a given date is owned by exactly one system — Muster or Xola — never split. | Retired by DEC-149 per this record's own banner. No owned-day allowlist symbol found in `src/reservations/` or `src/import/`. | FALSE | SPEC |
| b | Every departure and booking carries a marker saying which system sold it. | `alter table events add column if not exists source text not null default 'xola'` and the same on `reservations`, at `db/migrations/0023_reservations_source_partition.sql:28-29`. | FALSE | SPEC |
| c | Muster reads remaining capacity only from its own bookings, so double-counting on the next import is impossible by construction. | Superseded by the fix DEC-126's own correction describes: the guard now counts any scheduled trip on the hull regardless of source (`src/reservations/hull-busy.ts`, issue #615). | FALSE | SPEC |
| d | The importer skips a Xola trip that lands on a Muster-owned boat-day and reports it as a counted, itemised skip rather than silently overwriting. | The owned-day concept is retired (DEC-149), so there is no such day to skip. `src/import/` carries the counted-skip idiom for other cases. | FALSE | SPEC |
| e | A Muster-owned boat-day is de-listed in Xola. Operator discipline, not a mechanism. | Nothing in the repo enforces or checks this. Absence, not a judgement. | FALSE | SPEC |
| f | The partition is the whole boat-day rather than per-departure, because mixing both systems inside one day means one shift's manifest draws from two truth sources. | `formShifts` groups by vessel-day (`src/builder/form-shifts.ts`); the grain still exists even though the ownership rule on top of it is retired. | FALSE | SPEC |
