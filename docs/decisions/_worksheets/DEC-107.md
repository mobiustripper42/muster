# DEC-107 — worksheet

**Source:** `docs/decisions/DEC-107-payments-stripe-hosted-checkout-deposit-balance.md`, 13,885 bytes, four amendments, 2026-07-11 → 2026-08-19.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-107

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Customers pay a deposit to hold the boat and the rest later, rather than the whole price up front. | `depositMode: "full" \| "deposit"` at `src/reservations/payment-config.ts:23`; the default at `:59` is `"full"`. `payment-config.test.ts` exists. | DEAD |  |
| b | Card fields appear inside the Muster booking page. Stripe handles the card data; we never see or store a card number. | `create-departure-payment-intent.ts` exists with a test; called from `app/(public)/book/checkout/actions.ts:166`. `create-departure-checkout.ts` also exists but has no caller outside a docstring reference at `create-departure-payment-intent.ts:3`. | TRUE | SPEC |
| c | When the balance comes due, Muster makes a fresh Stripe payment link. We never charge a saved card on our own. | `create-balance-checkout.ts` exists with a test. One caller: `app/(admin)/admin/calendar/[reservationId]/actions.ts:6`. No saved-card or off-session charge path found in `src/adapters/stripe-payment.ts`. | DEAD |  |
| d | A booking is written when Stripe's server tells our server the money moved, never because the customer's browser came back to our page. | `app/api/webhooks/stripe/route.ts` resolves; `src/reservations/booking-webhook.ts` exists with a test. `app/(public)/book/success/` exists. Whether the success page writes anything: not checked mechanically here. | TRUE | SPEC |
| e | Charge and refund code is lifted from the sailbook project and audited rather than written again. Taking a deposit is the only new part. | `/home/eric/sailbook` exists on disk; `refunds.create` appears there in `src/actions/enrollments.ts`. Muster's own `refunds.create` is at `src/adapters/stripe-payment.ts:116`. No mechanical check can show one was derived from the other. | DEAD |  |
| f | Nothing writes the balance owed to the database. It is worked out at the moment the customer clicks, so it cannot go stale when a price or deposit share changes. | `balanceOwedCents` at `src/reservations/payment-config.ts:155`. The `reservations` table (`db/migrations/0001_init.sql:81-89`) has no balance column, and no later migration adds one. | DEAD |  |
| g | A partial refund comes out of the tip and the service fee before it comes out of the fare, so a tip-only refund never re-bills the customer for the boat. | Netting inside `balanceOwedCents`, `payment-config.ts:155-171`. `payments.gratuity_cents` added by `db/migrations/20260718235345_gratuity.sql`; `payments.service_fee_cents` by `20260723150000_payment_service_fee_cents.sql`. `payment-config.test.ts` exists; whether it pins this order is not checked here. | FALSE | DECISION |
| h | The fare the balance is based on is the base price plus the extra-guest charges as they stood when the customer booked, frozen onto the booking. | `reservations.extras_cents` added by `db/migrations/20260719185042_reservation_extras_cents.sql:11`, nullable. | TRUE | SPEC |
| i | When two customers pay for the last boat, the one who loses is refunded automatically and told the boat sold while they were paying. | `sold-out-notice.ts` exists with a test. Auto-refund branch at `booking-webhook.ts:505-530`, keyed `refund_${charge.key}`; the no-payment-intent case at `:507` alerts for a manual refund instead. | UNKNOWN |  |
| j | Every refund other than that one is done by hand in the Stripe dashboard. | Contradicted by files that exist: `refund-payment.ts` exists with a test and is called from `app/(admin)/admin/calendar/[reservationId]/actions.ts:17`. The docstring at `src/adapters/stripe-payment.ts:6-7` still reads "All other refunds stay manual in the Stripe dashboard." | UNKNOWN |  |
| k | The sales tax rate is read fresh each time the balance is worked out, not frozen onto the booking. Safe only because no web page can change it. | `taxRateBps` at `payment-config.ts:27`; rate stored in `app_settings` (`db/migrations/0006_app_settings.sql:18`), not on `payments` or `reservations`. `setPaymentConfig` is declared at `src/ports/repository.ts:437`; grep over `app/` returns callers of `getPaymentConfig` only, none of `setPaymentConfig`. | FALSE | DECISION |
| l | The tax rate is 8%, Cuyahoga County. | The code default is `taxRateBps: 725` at `payment-config.ts:61`, commented "Ohio state sales tax 7.25%". No 800 default in the repo. The deployed value is in `app_settings` per deployment and is not answerable from this checkout. | DELETE |  |
| m | Card disputes are handled by hand in the Stripe dashboard. | Contradicted by handlers that exist: `charge.dispute.created/updated/closed` parsed at `stripe-payment.ts:211-213`; `recordDispute` dispatched at `booking-webhook.ts:161`; ledger states at `:655-671`; delivery-order guard at `postgres-repository.ts:1347`. | TRUE | SPEC |
| n | The refund cascade — an operator cancelling a whole shift and fanning refunds across its bookings — is not built. | No `cancelShift` symbol in `src/` or `app/`. Grep for `cascade` hits `src/builder/cascade-coexistence.test.ts`, `src/domain/entities.ts` and `payment-config.ts` only — no cascade surface under `app/`. | DEAD |  |
| o | There is no screen inside Muster for money operations. One waits until Muster carries real volume. | `app/(admin)/admin/purchases/page.tsx` exists (a read-only revenue list). Refund and cancel controls live on `admin/calendar/[reservationId]/reservation-detail-pane.tsx`. No standalone refunds or payments route under `app/(admin)/admin/`. | TRUE | SPEC |
