# DEC-108 — worksheet

**Source:** `docs/decisions/DEC-108-public-surface-app-public-single-flip-book-now-entry.md`, 4,375 bytes, one amendment, 2026-07-11.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-108

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | There is a public route group for customer-facing pages. | `app/(public)/` resolves, with `book/`, `book/checkout/`, `book/success/`, `book/cancel/`. | DEAD |  |
| b | Phase 11 ships a throwaway unstyled form; the real designed surface is Phase 12. | No `startBooking` harness remains (only a docstring mention at `app/(public)/book/checkout/actions.ts:16`). `checkout-form.tsx` is the built surface. | DEAD |  |
| c | Availability is a read-only pure deriver with no login. | `src/reservations/availability.ts` with `availability.test.ts`; `availability-screen.ts` with test. No auth check under `app/(public)/book/`. | DEAD |  |
| d | A departure is available only if it carries no live Muster booking and the party fits that departure's capacity — never a running seat subtraction. | `claim.ts` with test; `Event.capacity` on the entity. Superseded in part by DEC-109's own correction — the guard is now hull-over-duration (`hull-busy.ts`), not a per-event mutex. | DEAD |  |
| e | The manage link is a deep link, not a login. | `app/b/[code]/page.tsx` resolves with no session check; `find-booking.ts` with test. | DEAD |  |
| f | "Living link" is an internal name and never appears in customer or crew copy; to a customer it is "your booking link". | No "living link" string in `app/` or `src/` outside decision records. | DEAD |  |
| g | The confirmation email and text both carry the link and tell the customer to save it. | `booking-confirmation.ts` with test; `email-channel.ts` carries a `receipt` kind. Whether the copy says "save this link" is not checked here. | DEAD |  |
| h | Lost-link recovery resends to the contact already on file and never displays the booking or link from typed input. | `recover-booking-link.ts` with test; `resend-booking-link.ts` with test. | DEAD |  |
| i | The recovery response is neutral whether or not a booking matched, so nobody can probe for who has a booking. | `recover-booking-link.test.ts` exists; whether it pins the neutral response is not checked here. | DEAD |  |
| j | Recovery is rate-limited per address and per target contact. | Built and tested: throttle claimed before matching (`recover-booking-link.ts:40`), keyed on canonical contact (`recover-booking-link.test.ts:206`), port entry at `src/ports/repository.ts:403` (issue #460). The per-IP half is not found — the key is the contact. | DEAD |  |
| k | The public "Book Now" entry is a single flag pointing at either Muster or Xola, so rolling back is one setting. | `reservationsEnabled()` at `app/lib/flags.ts:38`. No Muster-or-Xola entry-point toggle found; issue #621 reports admin reservations routes ignore the kill switch. | DEAD |  |
