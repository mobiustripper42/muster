# DEC-109 — worksheet

**Source:** `docs/decisions/DEC-109-atomic-capacity-claim-on-public-booking-the-customer.md`, 6,686 bytes, two amendments + one correction, 2026-07-11 → 2026-08-06.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-109

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Two customers checking out the last of a boat must not both succeed. The check is atomic at the data layer, never read-then-write. | `saveBookingIfSlotFree` on the port (`src/ports/repository.ts:291`); conditional insert in `postgres-repository.ts`. Contract-tested at `repository-contract.ts`; real-Postgres race at `postgres-repository.test.ts:429-472`. | DEAD |  |
| b | An over-capacity booking whose payment already went through is refunded and the customer told, never a silent oversell. | Auto-refund branch at `booking-webhook.ts:505-530`; `sold-out-notice.ts` exists with a test. | DEAD |  |
| c | The claim is a whole-boat rule: claim only if the departure carries no live Muster booking. | `claim.ts` exists with a test. Superseded in part by the record's own 2026-08-06 correction — the guard is now hull-over-duration (`hull-busy.ts`), not event identity. | DEAD |  |
| d | There is no database uniqueness rule on a booking's departure, so more than one booking per departure stays possible as a future policy change rather than a migration. | No unique index on `reservations.event_id` in any migration; `0001_init.sql:90` creates a plain index. | DEAD |  |
| e | The customer never picks a boat. They pick offering, time and guest count, and the boat is chosen inside the claim. | `claim.ts` selects the vessel; `app/(public)/book/` collects offering, date, time and party size (`SPEC.md:1536-1541` describes the same). | DEAD |  |
| f | On losing a boat, the claim falls back to the next boat that fits, then to sold out. | Fallback loop in `claim.ts`; `sold-out-notice.ts` exists. Boat-selection policy (smallest-that-fits) noted as open in the record; `SPEC.md:1541` states smallest-that-fits. | DEAD |  |
| g | A 15-minute soft hold is placed when checkout starts, so the second customer usually never begins paying. | `checkout_holds` table at `db/migrations/20260718142705_claim_hold_mutex.sql:33`, unique on `(vessel_id, date, time)` at `:46-47`. | DEAD |  |
| h | The hold is an optimisation on top of the atomic claim and never a replacement for it. | Both exist: `checkout_holds` (`20260718142705`) and the write-time claim (`saveBookingIfSlotFree`). | DEAD |  |
| i | If a hold expires mid-payment and someone else buys the slot, both paid, one wins, and the loser is refunded and told the slot sold while they were paying. | `booking-webhook.ts:505-530` refunds keyed `refund_${charge.key}`; `:507` alerts for a manual refund when there is no payment to refund. `sold-out-notice.ts` with test. | DEAD |  |
| j | The identity guard applies both when the hold is taken and when the booking is written. | Unique index on `checkout_holds` (`20260718142705:46`) and the conditional insert on `events_muster_slot_identity`. | DEAD |  |
| k | A customer's checkout hold and an operator's block on a boat are different things with different lifetimes and must stay separate. | Separate tables: `checkout_holds` (`20260718142705`) and the block family, both listed at `20260722170000_fk_reservations_era.sql:7`. | DEAD |  |
| l | The claim guarded exact slot identity, so two overlapping trips on one boat both succeeded. The guard is now the boat over the trip's whole duration, serialised by a lock on the boat-day. | `hull-busy.ts` exists with a test; advisory lock in `postgres-repository.ts`. Regression pinned against real Postgres at `postgres-repository.test.ts:429-472`. | DEAD |  |
