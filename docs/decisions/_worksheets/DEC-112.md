# DEC-112 — worksheet

**Source:** `docs/decisions/DEC-112-reservation-price-resolves-per-event-nullable-event.md`, 2,255 bytes, one fired revisit recorded in the preamble, 2026-07-11.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-112

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Each departure can carry its own price, rather than one flat rate for the experience. | `alter table events add column if not exists price integer` at `db/migrations/0023_reservations_source_partition.sql:31`. | DEAD |  |
| b | The price column is nullable and source-agnostic — Xola departures leave it empty, because their money lives in Xola. | Same line: nullable, commented "null = no Muster price (DEC-112)". `events.source` defaults `'xola'` at `:28`. | DEAD |  |
| c | In Phase 11 the price comes from the departure alone; there is no catalog or schedule default to fall back to. | Superseded by code that exists: `src/reservations/availability.ts:289-293` resolves `offering.priceVariations` against the date and falls back to `offering.basePriceCents`. The record's own preamble records this revisit as fired. | DEAD |  |
| d | The customer's party cap reads the departure's own capacity, not the boat's legal maximum. | `Event.capacity` in `src/domain/entities.ts`; no `coiMaxPax` read under `app/(public)/book/`. | DEAD |  |
| e | The schema column is not owner-gated and gets built; the price value, deposit share and balance timing are the owner's call. | Column exists (`0023:31`). `depositPercent` and `balanceDueDaysBeforeEvent` are config at `payment-config.ts:60,63`, not code constants. | DEAD |  |
| f | The catalog landed, so a default cascade now exists: the offering's price variations resolve against the date and fall back to its base price, by a fixed amount or a percentage. | `availability.ts:289-293`; `20260720100500_offering_catalog_fields.sql` exists. `availability.test.ts` exists; whether it pins the cascade is not checked here. | UNKNOWN |  |
