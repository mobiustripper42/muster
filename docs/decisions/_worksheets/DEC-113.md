# DEC-113 — worksheet

**Source:** `docs/decisions/DEC-113-flex-insurance-is-a-boolean-selector-on-the.md`, 2,538 bytes, two corrections, 2026-07-11 → 2026-07-25.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Evidence provenance.** Gathered 2026-08-26 against commit `f626cdf`, then re-verified in full: all 155 file citations across these worksheets resolve and none points past the end of its file. One apparent miss, `src/actions/enrollments.ts` in DEC-107e, is a path in the sibling `sailbook` repository rather than this one.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-113

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Flex insurance is a yes/no flag on the booking that changes which cancellation window applies, not a priced product. | `hasFlex?: boolean` is a function parameter at `src/reservations/refund-terms.ts:66,84` — **not** a column: no `has_flex` in any migration and no field on `Reservation` in `src/domain/entities.ts`. | DEAD |  |
| b | With flex, the free-cancel window is 72 hours before the trip instead of 14 days. | `FLEX_CANCEL_HOURS_BEFORE = 72` at `refund-terms.ts:39`; `STANDARD_CANCEL_DAYS_BEFORE = 14` at `:36`; selected by `cancelWindowHours(hasFlex)` at `:52-53`. `refund-terms.test.ts` exists. | DEAD |  |
| c | It rides the existing refund policy's terms and needs no new machinery of its own. | `hasFlex` threaded through `refundOwedCents` at `refund-terms.ts:84-86`. No flex-specific module, table or route exists. | DEAD |  |
| d | General add-ons stay parked. Model them Xola-shaped only if ever built. | Contradicted by an entity that exists: `db/migrations/20260721000000_add_ons_entity.sql`; admin route `app/(admin)/admin/add-ons/` resolves. The record's own 2026-07-25 correction records this. | DEAD |  |
| e | The flag does nothing until the refund policy exists in code. | The policy now exists — `refundOwedCents` at `refund-terms.ts:81`, `operatorCancelRefundCents` at `:101`, both called from `quoteCancelRefund` (`cancel-reservation.ts:76`). `hasFlex` defaults to `false` at `:84` and no caller passes it. | DEAD |  |
| f | The refund policy function this decision names does not exist, so flex insurance remains unbuilt. | No `refund_owed` symbol anywhere in the repo. Nothing under `app/(public)/book/` reads or writes a flex value, so it cannot be sold. Tracked as issue #683. | DEAD |  |
| g | Add-ons shipped as a first-class entity, wider than this decision anticipated. Flex insurance is still a policy flag and must not be modelled as one of those add-on rows. | `add_ons` entity at `20260721000000_add_ons_entity.sql`; `Offering` carries add-ons separately (`src/domain/entities.ts:394`). `refund-terms.ts:20` restates the rule: "this file must never learn about" add-ons. | DEAD |  |
