# DEC-122 — worksheet

**Source:** `docs/decisions/DEC-122-customer-booking-link-stateless-hmac-capability-url.md`, 4,561 bytes, no amendments, 2026-07-13. Reversed in mechanism by DEC-154.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-122

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | The manage link is a stateless signed address with no stored row and no migration. | Gone: `reservationLinkToken`, `verifyReservationLinkToken` and `RESERVATION_LINK_SECRET` have **0 mentions** anywhere in `src`, `app`, `db`, `e2e` or `docs/DEPLOY.md`. Replaced by a stored row — `booking_codes` at `db/migrations/20260814123030_booking_codes.sql:25`. | DEAD |  |
| b | It uses its own secret, separate from the session secret, so rotating link secrets never logs anyone out. | `RESERVATION_LINK_SECRET`: 0 mentions. Not in `env.example`. | DEAD |  |
| c | There is no per-link revocation — a leaked link dies only by rotating the secret, which kills every link at once. | Superseded: `booking_codes` carries `revoked_at` (`20260814123030_booking_codes.sql:25-37`). | DEAD |  |
| d | That was accepted because the protected thing is a view plus an out-of-band cancel request, not payment credentials. | `booking-change-request.ts` with test; `MessageKind` includes `booking_request` at `src/ports/channel.ts:33`. | DEAD |  |
| e | The confirmation goes out only when a booking is newly written, never on a repeat message from Stripe. | `booking-confirmation.ts` with test; the `already` outcome is a distinct branch in `write-booking.ts:76` (`SlotBookingResult`). | DEAD |  |
| f | The customer recipient is its own shape, so the crew rule that every recipient carries a crew id stays enforced by the compiler. | `GuestRecipient` in `src/ports/channel.ts`; `requireCrewId` in the crew adapters. | DEAD |  |
| g | Sending the confirmation is best-effort and can never fail the payment webhook. | Guarded at both layers per the record; `booking-webhook.test.ts` exists. Whether a throwing send still returns webhook success is unasserted — issue #816's 5A lists this as the unpinned half. | DEAD |  |
| h | It sends on whichever channels exist; a failure raises a low-severity notice, not the urgent money alert. | `alertPaidButUnbooked` is the urgent path (`booking-webhook.test.ts:62`). A separate low-severity path exists in the same module. | DEAD |  |
| i | The confirmation text is transactional, so it does not route through the crew consent gate. | `SmsConsent` gate lives on the crew ask path; `booking-confirmation.ts` does not read it. | DEAD |  |
