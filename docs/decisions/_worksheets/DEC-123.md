# DEC-123 — worksheet

**Source:** `docs/decisions/DEC-123-reservations-gets-its-own-calendar-customer-centric.md`, 8,497 bytes, one correction + one refinement, 2026-07-15 → 2026-07-16.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Evidence provenance.** Gathered 2026-08-26 against commit `f626cdf`, then re-verified in full: all 155 file citations across these worksheets resolve and none points past the end of its file. One apparent miss, `src/actions/enrollments.ts` in DEC-107e, is a path in the sibling `sailbook` repository rather than this one.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-123

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Reservations gets its own calendar, customer-centric, beside the crew-centric shift view. | `app/(admin)/admin/calendar/` and `app/(admin)/admin/shifts/` both resolve. `calendar-grid.ts`, `calendar-detail.ts` exist with tests. | DEAD |  |
| b | The two surfaces share the boat and the boat-day and cross-link to each other. | One `Vessel` entity in `src/domain/entities.ts`; `formShifts` derives shifts from events (`src/builder/form-shifts.ts`). Whether the surfaces cross-link is not checked here. | DEAD |  |
| c | The catalog is net-new: create and edit an offering with its content, photos, location, boats, schedule and prices. | `offerings` table at `db/migrations/20260718045012_reservation_catalog_tables.sql:24`; `app/(admin)/admin/offerings/` resolves; `src/admin/offering-admin.ts` with test. | DEAD |  |
| d | Blackout blocks are their own surface, not part of the offering. | `blocks` table at `20260718045012:49`; `app/(admin)/admin/blocks/` resolves separately from `offerings/`. | DEAD |  |
| e | Purchases and customers are net-new: an order list and detail, plus the customer contact record. | `app/(admin)/admin/purchases/page.tsx` and `app/(admin)/admin/customers/[customerId]/page.tsx` resolve. `purchases-view.ts` with test. | DEAD |  |
| f | Cards on file stay with Stripe and are not rebuilt. | No card or payment-method table in any migration; `payments` records charge references only (`20260715024322_payments.sql`). | DEAD |  |
| g | Unsold departures are first-class display rows on the calendar — showing what is not booked is the ordering system. | `calendar-grid.ts` with `calendar-grid.test.ts` and `calendar-grid-draws.test.ts`. | DEAD |  |
| h | Those display rows are computed, not written — do not read "first-class rows" as "write a row". | `deriveVirtualAvailability` at `src/reservations/availability.ts:112-117`. | DEAD |  |
| i | Whole-boat selling is a rule in the service layer, not a shape in the schema, so multi-party selling is not welded shut. | No unique constraint on `reservations.event_id`; `0001_init.sql:90` creates a plain index. The rule lives in `claim.ts`. | DEAD |  |
| j | Do not add a uniqueness rule on a booking's departure — it would freeze today's business rule as a structural fact. | Still absent. The only slot-level uniqueness is `events_muster_slot_identity`, which is a different key (one boat-slot, not one reservation). | DEAD |  |
| k | Reports, marketing, distribution, app store, store credit and the questionnaire are cut from this phase. | No route for any of them under `app/(admin)/admin/`. | DEAD |  |
| l | The word is "offering", not Xola's "experience". | `Offering` in `src/domain/entities.ts`; `offerings` table; no "experience" symbol in `src/` or `app/`. | DEAD |  |
| m | A departure is Open or Sold, never a seat count. Party size is a fact about the booking, never inventory to subtract. | `claim.ts` is a whole-boat predicate; no running-capacity subtraction in `availability.ts`. | DEAD |  |
| n | "Seat" is a crew word. The reservation side counts guests against boat capacity. | `Seat` is a crew entity in `src/domain/entities.ts`; no `Seat` reference under `src/reservations/`. | DEAD |  |
| o | Capacity is a fact of the boat, set on the boat screen, never overridden per offering. | `Vessel.coiMaxPax` in `src/domain/entities.ts`; `app/(admin)/admin/vessels/` resolves. No capacity field on `Offering`. | DEAD |  |
| p | One schedule per offering. Boats needing a different schedule are a different offering. | `OfferingSchedule` is a single field on `Offering` (`src/domain/entities.ts`), not a collection. | DEAD |  |
| q | Location is a first-class entity carrying pickup details and a route description, and is what a location block targets. | `locations` table at `20260718045012:38`; `app/(admin)/admin/locations/` resolves; `LocationId` in `src/domain/ids.ts`. | DEAD |  |
| r | Tax and the service fee are system-wide settings with an optional per-offering override. | System-wide in `app_settings` (`0006_app_settings.sql:18`) via `PaymentConfig` (`payment-config.ts:21`). A per-offering override is noted as an inert catalog field at `src/domain/entities.ts:279`; whether one is readable is not checked here. | DEAD |  |
| s | An offering is draft, live or hidden. Hidden is a reversible soft delete — the only safe way to retire an offering people have booked. | `Offering.status` draft/live/hidden in `src/domain/entities.ts`; `offering-admin.ts` with test. No hard-delete path for offerings. | DEAD |  |
