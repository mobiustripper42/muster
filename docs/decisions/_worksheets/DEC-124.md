# DEC-124 — worksheet

**Source:** `docs/decisions/DEC-124-tips-come-into-muster-as-collect-and-expose-xola-tip.md`, 3,554 bytes, one refinement + one amendment, 2026-07-15 → 2026-07-18. Carries an `amends_spec` claim on SPEC.md §4.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-124

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Muster collects tips and reports them, reversing an earlier decision that tips were not Muster's job. | `gratuity` table created by `db/migrations/20260718235345_gratuity.sql:20`. `src/admin/gratuity-payroll.ts` exists with a test. | DEAD |  |
| b | A tip is taken at checkout and is required, chosen from tiers that mirror the live Xola settings, with no way to decline. | `{ kind: "pre", tiersBps: GRATUITY_TIERS_DEFAULT, defaultBps: GRATUITY_DEFAULT_BPS, required: true }` at `src/reservations/pricing.ts:67`. Tier UI at `app/(public)/book/checkout/checkout-form.tsx:58,86`. | DEAD |  |
| c | Tipping after the trip is supported, through the booking link. | `create-gratuity-checkout.ts` exists — no test file — called from `app/b/[code]/actions.ts:54`. `gratuity.kind` takes `'post'` (`20260718235345_gratuity.sql:24`). | FALSE | SPEC |
| d | Tip settings live per offering, in the catalog. | `gratuityKinds?: GratuityKindConfig[]` at `src/domain/entities.ts:389`; per-kind shape at `:400`. Resolver `gratuityKindsFor` at `pricing.ts:87`. Admin route `app/(admin)/admin/offerings/` resolves. | DEAD |  |
| e | A tip is crew money, not revenue: exempt from tax and from the service fee, and reported as crew pay rather than sales. | `serviceFeeBps` applies to the fare only (`payment-config.ts:28-30`); `taxCentsFor` takes a taxable base excluding gratuity (`:67`). `payments.gratuity_cents` netted in `balanceOwedCents` (`20260718235345_gratuity.sql:38`). `src/admin/gratuity-payroll.ts` emits crew pay. | DEAD |  |
| f | Gratuity is its own table keyed by when it was taken, not an add-on, because an add-on gets taxed and charged a fee like revenue. | `create table if not exists gratuity` with `kind text not null -- 'pre' \| 'post'` at `20260718235345_gratuity.sql:20,24`. Separate `add_ons` entity at `20260721000000_add_ons_entity.sql`. Add-on revenue treatment noted at `src/domain/entities.ts:394`. | DEAD |  |
| g | Add-ons stay a separate mechanism for real upsells such as an extra hour, catering or photos. | `db/migrations/20260721000000_add_ons_entity.sql` exists; admin route `app/(admin)/admin/add-ons/` resolves with `page.tsx` and `actions.ts`. | DEAD |  |
| h | Muster builds its own even split and payroll export for its own tips, lifted from the operator's existing tool. | `src/admin/gratuity-payroll.ts` exists with a test. Export routes `app/(admin)/admin/payroll/tips.csv` and `gusto.csv` resolve. `src/crew/gusto-import.ts:2` names the lift source. | DEAD |  |
| i | Joining Muster's tips to Xola's is deferred. During the overlap the operator adds the two lists by hand. | No union or combined-export symbol found in `src/` or `app/`. `src/crew/crew-cli.ts:40` points at a path inside the sibling tool (`../xola-tip-extractor/api/lib/guide-gusto-map.json`) rather than reading its tips. | DEAD |  |
| j | When Xola retires, the whole apparatus moves into Muster and the operator's tool is retired with it. | Forward-looking; nothing to check mechanically. `src/config/tenant.ts:54` and `src/crew/gusto-import.ts:2` record parts already ported. | FALSE | DECISION |
| k | Tipping after the trip means the customer's booking link outlives the trip — "manage your booking" becomes "tip your crew". | Route `app/b/[code]/` resolves and posts the gratuity action at `actions.ts:54`. `booking-code.ts` and `find-booking.ts` exist with tests. | FALSE | SPEC |
