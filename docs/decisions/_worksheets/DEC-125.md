# DEC-125 — worksheet

**Source:** `docs/decisions/DEC-125-virtual-availability-the-schedule-is-a-rule-event.md`, 8,718 bytes, two guardrail notes + a build-resolution block, 2026-07-16 → 2026-07-18 (PR #470).

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Evidence provenance.** Gathered 2026-08-26 against commit `f626cdf`, then re-verified in full: all 155 file citations across these worksheets resolve and none points past the end of its file. One apparent miss, `src/actions/enrollments.ts` in DEC-107e, is a path in the sibling `sailbook` repository rather than this one.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-125

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Muster does not write a row for every potential departure. The schedule is a rule and open slots are computed on read. | `deriveVirtualAvailability` described at `src/reservations/availability.ts:112-117`, returning one `VirtualSlot` per enumerated departure. `availability.test.ts` exists. | DEAD |  |
| b | Open slots are the schedule across boats and dates, limited to Muster-owned days, minus blocks and bookings. | Same deriver, `availability.ts:112-117`. `blocks` table at `db/migrations/20260718045012_reservation_catalog_tables.sql:49`. | DEAD |  |
| c | Muster must never offer a slot on a Xola-owned boat-day. | `events.source` / `reservations.source` at `0023_reservations_source_partition.sql:28-29`. Pilot-only per the record; moot after the DEC-126 cutover. | DEAD |  |
| d | A row is written only when a slot acquires real state — booked, overridden, held or blocked. | `eventIdForSlot` at `availability.ts:167-172` mints a deterministic id from the slot identity; `claim.ts` writes on first booking. | DEAD |  |
| e | An operator's hold on a boat writes a block row, not a departure. | `blocks` table (`20260718045012:49`); `checkout_holds` is the separate customer hold (`20260718142705_claim_hold_mutex.sql:33`). | DEAD |  |
| f | A season is a few dozen rows rather than thousands, and editing a schedule recomputes rather than rewrites. | Follows from the deriver; no per-departure materialization job exists under `db/` or `app/api/cron/`. | DEAD |  |
| g | A draft offering publishes no rule and generates no slots. | `offerings` table at `20260718045012:24`; `Offering.status` draft/live/hidden in `src/domain/entities.ts`. | DEAD |  |
| h | An offering edit applies forward to unbooked, unmodified slots, with nothing to rewrite. | Virtual slots recompute by construction (`availability.ts:112-117`). | DEAD |  |
| i | A per-departure override wins and is never silently overwritten by an offering edit. | `Event.price` per-departure column at `0023_reservations_source_partition.sql:31`; precedence described at `availability.ts` (materialized event wins its slot). | DEAD |  |
| j | A booked slot is frozen — a price change never retroactively alters what a customer paid. | Frozen money on the reservation: `reservations.extras_cents` (`20260719185042`), `events.duration_minutes` (`20260730031500`), and per-payment frozen figures. | DEAD |  |
| k | A time or capacity change to a booked trip goes through the customer-notify path, never a silent bulk update. | `booking-change-request.ts` with test; `sold-out-notice.ts` with test. No bulk-update script under `db/`. | DEAD |  |
| l | Blackout is scoped blocks rather than per-departure toggles, in three kinds: a location for a time window, a boat for a date range, and a single-slot operator hold. | Three-kind `Block` union in `src/domain/entities.ts`; `blocks` table at `20260718045012:49`; `block-impact.ts` with test. | DEAD |  |
| m | There is exactly one materialised departure per physical boat-slot, enforced on both adapters. | `slotIdentity` at `availability.ts:155`; `events_muster_slot_identity` partial unique index; contract-tested at `repository-contract.ts`. | DEAD |  |
| n | A booking's fare is the departure's base price plus extra-guest charges plus the tip. | `composeFare` in `src/reservations/pricing.ts`; `extraGuestPriceCents` on `Offering` (`entities.ts`); `reservations.extras_cents` (`20260719185042:11`). | DEAD |  |
