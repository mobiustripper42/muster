# DEC-126 — worksheet

**Source:** `docs/decisions/DEC-126-the-flip-is-a-cutover-with-a-one-time-full-xola.md`, 6,346 bytes, one correction, 2026-07-17 → 2026-08-06. Carries `amends_spec` claims on §0.3 and §4.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-126

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | The flip is a cutover, not a natural drain. After it, Muster is the single source of truth for bookings. | Not built: no cutover import script under `db/` or `src/import/`. `app/api/cron/xola-pull/` still exists and runs. | TRUE | DECISION |
| b | A one-time import brings across this season's forward book only. Already-run trips and prior years stay in Xola, read-only. | Not built (see a). The bound was narrowed from "all reservations" on 2026-08-11; the record states nothing was built against the wider version. | DEAD |  |
| c | The import is bounded by its own reason: a trip that has already run occupies no future slot, so importing it buys the availability check nothing. | Reasoning only; nothing to check mechanically. | DEAD |  |
| d | Money stays in Xola for imported bookings. Muster manages the reservation, so a booking may point at money held in either system. | `reservations.source` at `0023_reservations_source_partition.sql:29`; Xola rows carry no `payments` link. | DEAD |  |
| e | The ongoing Xola import stops after the cutover. Xola is no longer an input. | Contradicted by a route that exists and runs: `app/api/cron/xola-pull/route.ts`. | DEAD |  |
| f | Muster can cancel a Xola-originated booking inside Muster, freeing the slot, with no write back to Xola. The money side is the operator's manual click in Xola. | `cancelReservation` at `src/reservations/cancel-reservation.ts:121` with test. Whether the pane routes on `source` is not checked here. No Xola write path exists. | DEAD |  |
| g | Which path a cancel takes is read off the existing source marker. No new money-home flag. | `source` exists (`0023:28-29`); no money-home column in any migration. | DEAD |  |
| h | The cutover must be reversible: it must not destroy or mutate Xola's records, and Muster's forward book must be re-keyable back into Xola while rollback is credible. | Not built: no export or re-key script under `db/` or `scripts/`. | DEAD |  |
| i | The availability check did not look at imported bookings — every layer of the sell funnel filtered to Muster's own, so a Xola booking did not make its boat unsellable. | Fixed: the guard now counts any scheduled trip on the hull whichever system sold it — `src/reservations/hull-busy.ts`, pinned against real Postgres at `postgres-repository.test.ts:429-472` (issue #615). | DEAD |  |
