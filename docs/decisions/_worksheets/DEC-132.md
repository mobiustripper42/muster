# DEC-132 — worksheet

**Source:** `docs/decisions/DEC-132-customer-is-a-contact-record-keyed-by-phone.md`, 4,048 bytes, no amendments, 2026-07-22.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Evidence provenance.** Gathered 2026-08-26 against commit `f626cdf`, then re-verified in full: all 155 file citations across these worksheets resolve and none points past the end of its file. One apparent miss, `src/actions/enrollments.ts` in DEC-107e, is a path in the sibling `sailbook` repository rather than this one.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-132

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | A customer is a contact record with its own id and a unique phone number. | `create table if not exists customers` at `db/migrations/20260722143000_customers.sql:18`; unique index on `phone_e164` at `:32`. | DEAD |  |
| b | Phone is identity but deliberately not the primary key, because numbers get changed and recycled by carriers. | Surrogate PK plus the separate unique index at `20260722143000_customers.sql:32`. | DEAD |  |
| c | Phone is required for now. Whether identity should be phone-or-email is deferred, and the Xola importer will force it at cutover. | `src/customers/identity.ts:5` states all resolution is concentrated in that one module. Whether the column is `not null` today is not checked here. | DEAD |  |
| d | Every customer carries a short readable code — six characters from an alphabet with no letters that sound alike. | `mintDisplayCode` at `src/customers/identity.ts:151`; unique index on `display_code` at `20260722143000_customers.sql:36`. | DEAD |  |
| e | The code is an operator convenience for saying on the phone, not a customer-facing account number. | No `displayCode` render found under `app/(public)/` or `app/b/[code]/`. | DEAD |  |
| f | A booking's customer link is optional but backed by a real foreign key, so historical bookings stay unlinked permanently and acceptably. | `customerId?: CustomerId` at `src/domain/entities.ts:673`; FK `on delete restrict` at `20260722143000_customers.sql:51`. | DEAD |  |
| g | The booking path creates or finds the customer by phone at the moment of the write, with the unique constraint settling the concurrent-first-booking race. | `src/customers/identity.ts` is the resolver; the write path is now `writeSlotBooking` (`write-booking.ts:87`) — the record cites the retired `writeBooking`, corrected by DEC-151. `db/backfill-customers.ts` exists as `db:backfill:customers`. | DEAD |  |
| h | A customer is not an account — no password, no login, no customer-facing sign-in. | No auth check under `app/b/[code]/`; `src/auth/` addresses crew and admin only. | DEAD |  |
| i | Deleting is soft and keeps the personal data, so true erasure would be a separate scrub and is out of scope. | `active` column per the DEC-123 posture; no scrub or erasure script under `db/`. | DEAD |  |
| j | Lifetime value sums non-cancelled bookings at base plus frozen extras, with tips excluded. | `reservations.extras_cents` (`20260719185042`); gratuity held separately in the `gratuity` table (`20260718235345:20`). `app/(admin)/admin/customers/[customerId]/page.tsx` resolves. | DEAD |  |
