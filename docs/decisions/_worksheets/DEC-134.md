# DEC-134 — worksheet

**Source:** `docs/decisions/DEC-134-customer-checkout-is-inline-stripe-elements-over-a.md`, 3,129 bytes, no amendments, no dated status line in the record (task 12.5, issue #458).

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Evidence provenance.** Gathered 2026-08-26 against commit `f626cdf`, then re-verified in full: all 155 file citations across these worksheets resolve and none points past the end of its file. One apparent miss, `src/actions/enrollments.ts` in DEC-107e, is a path in the sibling `sailbook` repository rather than this one.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-134

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | The card widget lives inside the Muster booking page, styled with our own tokens. The customer never leaves the site to pay. | `app/(public)/book/checkout/checkout-form.tsx` exists; `@stripe/react-stripe-js` at `package.json:79`. `create-departure-checkout.ts` (the hosted redirect) has no live caller. | DEAD |  |
| b | The widget loads before any charge exists, so changing the tip updates the amount in the browser without creating anything at Stripe. | Tier selection held in component state at `checkout-form.tsx:86`; `tiers` prop at `:58`. Whether the Element mounts in deferred mode is not checked mechanically here. | DEAD |  |
| c | Pressing "Book & pay" checks the waiver, checks the tip, holds the boat, prices the held slot, freezes every money field onto the charge, then hands the browser what it needs. | `createDeparturePaymentIntent` at `src/reservations/create-departure-payment-intent.ts`, called from `app/(public)/book/checkout/actions.ts:166`. Test file exists. | DEAD |  |
| d | The booking write is keyed on the payment's own id, so a repeated message from Stripe produces one booking and not two. | `booking-webhook.ts` dispatches `payment_intent.succeeded` (`stripe-payment.ts:241`); idempotency keys built from `charge.key` at `booking-webhook.ts:280`. `booking-webhook.test.ts` exists. | DEAD |  |
| e | Only charges carrying a stated purpose are processed. A hosted session's bare charge is acknowledged and ignored, so one payment can never make two bookings. | Guard reasoning at `booking-webhook.ts:167-168`; metadata read at `:279`. `createCheckoutSession` sets no `payment_intent_data.metadata` — see `stripe-payment.ts:88`. | DEAD |  |
| f | The Stripe endpoint must be subscribed to the inline payment event as well as the hosted one, and nothing in the repo can check that. | Both event types parsed at `stripe-payment.ts:170,241`. Ops note at `app/api/webhooks/stripe/route.ts:15`. No repo-side verification exists; tracked as issue #544. | DEAD |  |
| g | The service fee is 3% of the fare — the base price plus extra-guest charges — and is not charged on tax or on the tip. | `serviceFeeBps` at `payment-config.ts:30`, default `300` at `:62` commented "3% of the fare (operator-adjustable), untaxed + untipped". `feeCentsFor` at `:74`. | DEAD |  |
| h | The service fee is charged once, in full, with the first charge. A later balance charge carries no fee. | `chargeNowCents` at `payment-config.ts:83`, full branch at `:89` adds `feeCents`. `payments.service_fee_cents` added by `db/migrations/20260723150000_payment_service_fee_cents.sql`, whose note at `:7-8` says balance payments carry no fee. | DEAD |  |
| i | The fee is frozen onto the charge and taken out of the balance the same way the tip is. | Netting inside `balanceOwedCents`, `payment-config.ts:155-171`. Worked example in the comment at `:196-201`. | DEAD |  |
| j | The tip tiers are 15, 20 and 25 per cent, 20 is preselected, and a tip is required at checkout. | `GRATUITY_TIERS_DEFAULT = [1500, 2000, 2500]` at `src/reservations/pricing.ts:60`; `{ kind: "pre", …, required: true }` at `:67`. `pricing.test.ts` exists. | DEAD |  |
| k | Two Stripe browser libraries are new dependencies, loaded only inside the one checkout island. Everything else on the screen renders on the server. | `@stripe/react-stripe-js` and `@stripe/stripe-js` at `package.json:79-80`. `checkout-form.tsx` is the only client component under `app/(public)/book/`. | DEAD |  |
| l | A missing Stripe key renders a loud configuration error rather than a silently broken widget. | Key read at `app/(public)/book/checkout/page.tsx:192`; error state rendered at `:197`. Declared in `env.example:87`. | DEAD |  |
| m | The throwaway booking harness retires. The checkout page is the real front door. | No `startBooking` symbol in `src/` or `app/` — the only hit is a docstring reference at `app/(public)/book/checkout/actions.ts:16`. | DEAD |  |
| n | Hosted Stripe pages survive for the balance charge and for the after-trip tip. | `create-balance-checkout.ts` and `create-gratuity-checkout.ts` both exist and both call `createCheckoutSession`. `create-gratuity-checkout.ts` has no test file. | DEAD |  |
