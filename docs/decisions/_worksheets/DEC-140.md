# DEC-140 — worksheet

**Source:** `docs/decisions/DEC-140-spec-1-3-rewritten-to-the-dec-125-model-availability.md`, 6,304 bytes, no amendments, 2026-07-25. Doc-only. Carries an `amends_spec` claim on §1.3.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-140

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | The availability section of the spec is rewritten to describe what shipped, rather than a rule engine that was never built. | `docs/SPEC.md:267` — "1.3 Availability — two questions, two mechanisms". `npm run check:decisions` reports the declared spec amendment landed. | DEAD |  |
| b | Availability is two mechanisms, not one: a computed booking set, and per-candidate crew evaluation. | `SPEC.md:287` "Booking availability — a computed set (DEC-125)" and `:327` "Crew eligibility — the composite that stayed a filter". `src/reservations/availability.ts` and `src/oracle/` are separate modules. | DEAD |  |
| c | The old property rules are re-homed rather than deleted, with a mapping table showing where each one landed. | `SPEC.md:364` "Parked from the original §1.3". | DEAD |  |
| d | There is no per-offering minimum party size and no booking turnaround buffer; the turnaround in the builder is the crew fatigue rule, a different idea sharing the word. | No minimum-party field on `Offering` in `src/domain/entities.ts`. `turnaround` appears in `src/builder/derive.ts` only. | DEAD |  |
| e | The satisfiability finding survives: the eligible pool is computed upstream of the ask, so the problem is a filter on who gets asked rather than a solver. | `SPEC.md:267-343`; `src/oracle/` is a per-candidate filter with no solver. | DEAD |  |
| f | A shift outside its staffing horizon is Pending — crew rules abstain rather than fail. | `SPEC.md:343` "The two horizons — the surviving frame". Shift states derived per DEC-005 (`src/builder/`). | DEAD |  |
| g | Hard-versus-soft rule severity, tenant downgrade, the verdict wrapper and the evaluation modes are parked, with no current consumer, because Muster is single-tenant. | No `Verdict` type in `src/oracle/` or `src/domain/entities.ts`. `SPEC.md:364` records the park. | DEAD |  |
| h | A vessel certificate's expiry is not a booking rule and will not become one, while a crew member's credential expiry is enforced. | `SPEC.md:318-319` records the closure. Crew side: the oracle's date-valid check (`SPEC.md:538-539`); no COI-expiry check under `src/reservations/`. | DEAD |  |
| i | There is no lead-time cutoff, because refusing short-notice bookings would delete the last-minute flow the spec treats as a feature. | No lead-time constant or check in `src/reservations/claim.ts` or `availability.ts`. The better shape is parked in `docs/FUTURE_IDEAS.md`. | DEAD |  |
