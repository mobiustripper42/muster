# DEC-151 — worksheet

**Source:** `docs/decisions/DEC-151-retire-the-legacy-booking-write-path.md`, 5,734 bytes, no amendments, 2026-08-08 (issue #693).

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Evidence provenance.** Gathered 2026-08-26 against commit `f626cdf`, then re-verified in full: all 155 file citations across these worksheets resolve and none points past the end of its file. One apparent miss, `src/actions/enrollments.ts` in DEC-107e, is a path in the sibling `sailbook` repository rather than this one.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-151

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | The older of the two booking write paths is deleted rather than guarded. | None of the six named symbols has a live definition anywhere in `src/` or `app/`: `writeBooking`, `BookingRequest`, `BookingResult`, `saveReservationIfUnclaimed`, `createBookingCheckout`, `isSlotBooking`. Remaining hits (8, 2, 0, 7, 3, 3 mentions) are comments, docstrings and test names. | DEAD |  |
| b | A Stripe session carrying a departure id instead of a slot is refused and alerted, never booked. | Slotless-session guard at `booking-webhook.ts:301` — "PAID but NOT booked - booking session … carries no slot … The legacy eventId booking path was retired". `booking-webhook.test.ts` exists. | DEAD |  |
| c | Deleting beat guarding because two write paths mean every future money rule has to be applied twice, and the path nothing exercises is the one that quietly misses the next guard. | Only one write path remains: `writeSlotBooking` at `src/reservations/write-booking.ts:87`, with `SlotBookingRequest` at `:52` and `SlotBookingResult` at `:76`. | DEAD |  |
| d | The whole-boat claim did not go. The surviving path takes the same mutex and adds what the retired one lacked. | `saveBookingIfSlotFree` on the port (`src/ports/repository.ts:291`), plus the hull-overlap guard (`hull-busy.ts`) and the boat-day advisory lock the retired path never had. | DEAD |  |
| e | Four tests were converted rather than deleted because their contracts survive. One was deleted outright, because a slot booking creates its own departure, so a missing departure is no longer a failure. | Same-slot Postgres race and the hull-overlap race both present at `postgres-repository.test.ts:429-472`. No "event missing" test remains in `booking-webhook.test.ts`. | DEAD |  |
| f | Four other decisions cite the retired names; none of their holdings changed, and the corrections are scoped to the citations so a reader who greps a dead name lands here rather than concluding the decision is stale. | DEC-107, DEC-122, DEC-131 and DEC-132 each carry a "Corrected by DEC-151" line. `npm run check:decisions` passes, so no reference dangles. | DEAD |  |
