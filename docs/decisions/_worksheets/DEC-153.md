# DEC-153 — worksheet

**Source:** `docs/decisions/DEC-153-cancel-and-refund-in-muster-both-routes-reconcile.md`, 16,318 bytes, one dated amendment, 2026-08-10 → 2026-08-21. Carries an `amends_spec` claim on SPEC.md §3.3.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Evidence provenance.** Gathered 2026-08-26 against commit `f626cdf`, then re-verified in full: all 155 file citations across these worksheets resolve and none points past the end of its file. One apparent miss, `src/actions/enrollments.ts` in DEC-107e, is a path in the sibling `sailbook` repository rather than this one.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-153

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | The operator can cancel a booking from inside Muster, on the reservation detail pane. | `cancelReservation` at `src/reservations/cancel-reservation.ts:121` with a test. Route `app/(admin)/admin/calendar/[reservationId]/` resolves; `cancel-start` and `cancel-confirm` exercised at `e2e/calendar.spec.ts:342-345`. | DEAD |  |
| b | Cancelling a booking also cancels its departure, not just the reservation. | `cancel-reservation.ts:155` calls `cancelEventIfUnclaimed(reservation.eventId)` — conditional on the event being unclaimed, not unconditional. `postgres-repository.ts:1107` writes `status='cancelled' where id=$1 and status='scheduled'`. | DEAD |  |
| c | After a cancellation the crew are re-formed and told. | `formShifts` re-run at `cancel-reservation.ts:169`, with a failure fallback to the tick at `:187`. `src/builder/form-shifts.test.ts` exists. | DEAD |  |
| d | Re-booking a slot whose booking was cancelled brings that cancelled departure back to life in place, carrying the new customer's price, capacity and trip length, and leaving the dock alone. | `postgres-repository.ts:1493` — "RESURRECT a cancelled slot (#616)". `events_muster_slot_identity` is status-agnostic. `postgres-repository.test.ts` exists; whether it pins this is not checked here. The in-memory adapter is not checked here. | DEAD |  |
| e | The operator types one refund figure and Muster splits it across the customer's charges, newest first. | `refundReservation` at `src/reservations/refund-payment.ts:160`; newest-first sort at `:209`; `RefundAllocation` at `:49`. `refund-payment.test.ts` exists. | DEAD |  |
| f | The whole refund is planned and checked before any money moves, so a request that cannot be met is refused outright rather than half-issued. | Plan built at `refund-payment.ts:213` after the lease is acquired at `:194`; read-inside-the-lease reasoning at `:176-181`. | DEAD |  |
| g | When we cancel, the customer gets back everything still refundable. | `operatorCancelRefundCents` at `src/reservations/refund-terms.ts:101`; base is `refundableTotalFor` (`refund-payment.ts:145`) via `quoteCancelRefund` (`cancel-reservation.ts:76-82`). Asserted at `e2e/calendar.spec.ts:398`. | DEAD |  |
| h | When the customer cancels far enough ahead they get back what they paid minus a fixed cancellation fee. Inside the window, nothing comes back. | `refundOwedCents` at `refund-terms.ts:81`; zero-inside-window branch at `:86`; never-negative note at `:73`. `refund-terms.test.ts` exists. | DEAD |  |
| i | The window is 14 days and the fee is $50. | `STANDARD_CANCEL_DAYS_BEFORE = 14` at `refund-terms.ts:36`; `CANCELLATION_FEE_CENTS = 5000` at `:30`. Published wording quoted in the docstring at `:9-10`. Boundary is inclusive per `:76`. | DEAD |  |
| j | Who cancelled decides which figure applies, not how much notice was given. | `quoteCancelRefund` takes `cancelledBy` (`cancel-reservation.ts:76`); the notice-is-irrelevant case noted at `cancel-reservation.test.ts:243`. Two separate functions so no caller can reach the fee path by passing the wrong notice (`refund-terms.ts:81,101`). | DEAD |  |
| k | Both figures are shown next to their option on the confirm screen. | Two-quote render asserted at `e2e/calendar.spec.ts:371-398`; confirm renders "We cancelled" at `:345`. | DEAD |  |
| l | The reason for a cancellation is not stored. The refund amount is the only record of which branch ran. | Contradicted by a column that exists: `reservations.cancelled_by` added by `db/migrations/20260820032840_cancelled_by.sql:17`; `cancelledBy?: CancelledBy` at `src/domain/entities.ts:691`. | DEAD |  |
| m | A second press of the refund button is refused when the ledger has moved since the form was drawn. | `expectedRefundedCents` parameter at `refund-payment.ts:164`, compared at `:198` returning `"stale"`; same `"stale"` for a lost lease at `:194`. `expectedRefunded` posted as a hidden field at `reservation-detail-pane.tsx:617,660`. | DEAD |  |
| n | A refund the operator takes in Stripe's own dashboard reaches Muster's ledger. | `charge.refunded` parsed at `src/adapters/stripe-payment.ts:185`. `getPaymentByIntentId` at `src/ports/repository.ts:457`, implemented at `postgres-repository.ts:1307`; `markPaymentRefunded` at `repository.ts:467`. Index at `db/migrations/20260810011500_payment_intent_lookup.sql:17`. Contract test at `repository-contract.ts:1669`. Whether the live endpoint subscribes cannot be checked here (issue #544). | DEAD |  |
| o | Refunds are recorded in every deployment, whether the reservations feature is switched on or off. | `booking-webhook.ts:159` places the refund path outside the `RESERVATIONS` gate; the gate is at `:251`, on the new-booking path only. Flag read at `app/api/webhooks/stripe/route.ts:51`. | DEAD |  |
| p | Cancelling and refunding are one press. | The compound press exercised at `e2e/calendar.spec.ts:404` ("this booking has a payment, so the press cancels AND refunds"). A comment at `reservation-detail-pane.tsx:649` reads "Refund became a two-step", and a separate refund confirm renders at `:614-638`. | DEAD |  |
| q | Refund also stays a standalone control, for a goodwill refund on a trip that is still sailing. | Standalone `refund-form` posting `startRefund` at `reservation-detail-pane.tsx:655`, with its own amount box at `:667`. | DEAD |  |
| r | The refund amount box on the cancel confirm starts blank, and blank means "work out the figure for the reason actually posted". Typing a figure overrides it. | Blank input at `reservation-detail-pane.tsx:753`, `placeholder="blank = the amount for your reason"` at `:756`, no `defaultValue`. The standalone refund box at `:667` does carry `defaultValue={actions.refundPrefill}` at `:670`. `parseDollarsToCents` at `refund-payment.ts:111`. | DEAD |  |
| s | Typing zero cancels the booking without refunding anything, which is a real outcome rather than a validation error. | `parseDollarsToCents` at `refund-payment.ts:111` returns a number or `null`; no zero-rejecting branch found. No test named for the zero case. | DEAD |  |
| t | A small piece of browser code fills the amount box, and nothing depends on it. With browser code switched off the box stays blank and the server works the figure out. | `components/admin/refund-amount-sync.tsx` exists, rendered at `reservation-detail-pane.tsx:767`. `e2e/calendar.spec.ts:756` runs with `javaScriptEnabled: false`. | DEAD |  |
| u | The cancellation is committed first and is never undone if the refund fails. | `cancel-reservation.ts:146-155` writes the cancellation through `cancelEventIfUnclaimed` before any refund call; `refundReservation` is a separate module with no rollback path back into `cancelReservation`. | DEAD |  |
| v | The cancel control sits last in the actions block, below refund and resend. | `reservation-detail-pane.tsx` renders the refund form at `:655` and the cancel confirm at `:753`. `e2e/calendar.spec.ts:118-126` names the pane's controls; whether an overlap assertion exists is not confirmed here. | DEAD |  |
| w | Shift-level cancel, with refunds fanned out across a shift's bookings and the customer told, is not built. | No `cancelShift` symbol in `src/` or `app/`. `cascade` hits only `src/builder/cascade-coexistence.test.ts`, `src/domain/entities.ts` and `src/reservations/payment-config.ts`. | DEAD |  |
| x | Card disputes and failed payments are out of scope here and filed separately. | Dispute half contradicted by handlers that exist: `charge.dispute.created/updated/closed` parsed at `stripe-payment.ts:211-213`; `recordDispute` at `booking-webhook.ts:161`; ledger states `:655-671`; ordering guard at `postgres-repository.ts:1347`. `payment_intent.payment_failed`: no hits in `src/` or `app/`. | DEAD |  |
