# DEC-154 — worksheet

**Source:** `docs/decisions/DEC-154-booking-link-is-a-stored-short-code.md`, 7,767 bytes, one amendment, 2026-08-14 → 2026-08-15 (issue #741).

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-154

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | The customer's manage link is a short code in the path — 43 characters instead of 129. | Route `app/b/[code]/` resolves with `page.tsx`, `load.ts`, `actions.ts`, `calendar/`. | TRUE | SPEC |
| b | The code is 14 characters from an alphabet with no letters that sound alike, minted from a cryptographic random source, one row per code. | `BOOKING_CODE_LENGTH = 14` at `src/reservations/booking-code.ts:37`; `randomBytes` imported at `:27`, defaulted at `:53`; alphabet at `:30`. `booking_codes` table at `db/migrations/20260814123030_booking_codes.sql:25`. `booking-code.test.ts` exists. | TRUE | SPEC |
| c | The length was sized against guessing any live booking rather than against collision — the first time the number was chosen deliberately. | `booking-code.ts:15` and `:36` record the sizing ("Decided deliberately (#741), not derived from a hash width"). | TRUE | SPEC |
| d | The code is a credential, so its randomness comes from a cryptographic source — unlike the customer display code, which shares the alphabet but not the source. | `booking-code.ts:43` states no caller should pass anything weaker; `mintDisplayCode` at `src/customers/identity.ts:151` takes `Math.random` by default. | TRUE | SPEC |
| e | Booking ids are unchanged. The code is a separate row pointing at one. | `booking_codes.reservation_id` FK with its own index at `20260814123030_booking_codes.sql:37`. Deterministic `resv-` ids unchanged in `write-booking.ts:35`. | TRUE | SPEC |
| f | Minting a new code revokes every earlier code for that booking. | `revoked_at` column at `20260814123030_booking_codes.sql:25-37`; `ensure-booking-code.ts` with test. | TRUE | SPEC |
| g | A customer holding a replaced link lands on a page telling them how to get a new one, rather than one that merely refuses. | `find-booking.ts:55` — refused "rather than compared loosely, because a loose compare here reaches a real person's" booking. Whether the page copy offers a route to a new link is not checked here. | TRUE | SPEC |
| h | A resend is not a reissue — it puts the same live code back where it already was. | `resend-booking-link.ts` and `ensure-booking-code.ts` are separate modules, each with a test. | TRUE | SPEC |
| i | There are three outcomes: fine, refused and unknown. Unknown covers a bad code and a database error alike, so a hiccup never tells a customer their good link is dead. | `find-booking.ts` with test; the refused-vs-loose-compare reasoning at `:55`. Whether all three outcomes are pinned by the test is not checked here. | TRUE | SPEC |
| j | Expiry exists as a column and nothing sets it, so a policy would be a value rather than a migration. | `expires_at` in `booking_codes` (`20260814123030_booking_codes.sql:25-37`). No writer found for it in `src/` or `db/`. | TRUE | SPEC |
| k | The old signed-address scheme is deleted rather than kept alongside as a second credential path. | `reservationLinkToken`, `verifyReservationLinkToken`, `RESERVATION_LINK_SECRET`: **0 mentions** across `src`, `app`, `db`, `e2e`, `docs/DEPLOY.md`. | TRUE | SPEC |
| l | On production the manage link appears only on the single render immediately following a reissue, and nowhere else. | Reveal driven by redirect params per the amendment; `reservation-detail-pane.tsx` renders the pane. Whether the production gate is conditional as described is not checked here. | TRUE | SPEC |
| m | The code never travels in the address bar; the pane re-reads it on the server. | `app/b/[code]/load.ts` reads server-side. The reveal path in the admin pane is not traced here. | TRUE | SPEC |
