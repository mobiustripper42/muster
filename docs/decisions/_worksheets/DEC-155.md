# DEC-155 — worksheet

**Source:** `docs/decisions/DEC-155-full-payment-is-the-default-posture.md`, 4,521 bytes, no amendments, 2026-08-16.

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-155

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | Full payment is the default: one charge at booking covering fare, extras, tax, service fee and gratuity, with nothing deferred. | `depositMode: "full"` at `src/reservations/payment-config.ts:59`; the full branch at `:89` returns `priceCents + taxCents + feeCents`. `payment-config.test.ts` exists. | DEAD |  |
| b | Deposit mode is not removed. Every part of it stays and it turns back on with one setting. | Deposit branch still present at `payment-config.ts:89`; `balanceOwedCents` at `:155`; `create-balance-checkout.ts` exists with a test and an admin caller. `depositMode` read at `create-departure-payment-intent.ts:142` and `create-departure-checkout.ts:138`. | DEAD |  |
| c | The deposit share stays at 25 per cent, so opting in remains one setting rather than two. | `depositPercent: 25` at `payment-config.ts:60`, with the reasoning restated in the docstring at `:50`. | DEAD |  |
| d | Existing bookings are unaffected, because the balance is derived from recorded payments and never from the live setting. | `balanceOwedCents` at `payment-config.ts:155` takes a `payments` array; no `getPaymentConfig` call inside it. The `reservations` table carries no balance column. | DEAD |  |
| e | The default is what matters, because an unconfigured deploy inherits it and full payment has no second step for anyone to forget. | `PAYMENT_CONFIG_DEFAULTS` at `payment-config.ts:58` is the fallback when `app_settings` holds no row (`db/migrations/0006_app_settings.sql:18`). Reasoning restated at `payment-config.ts:39-41`. | DEAD |  |
| f | Full payment is the choice for every environment. There is no per-environment setting to make and none to forget. | No `depositMode` row written by any migration or seed script; `setPaymentConfig` (`src/ports/repository.ts:437`) has no caller under `app/`. The deployed `app_settings` contents are not answerable from this checkout. | DEAD |  |
| g | Deposits should not return until something collects the balance without a person having to remember. | Nothing reads `balanceDueDaysBeforeEvent` on a schedule — the constant is declared at `payment-config.ts:33`, defaulted to `14` at `:63`, and referenced only in docstrings (`:43`, `create-balance-checkout.ts:12`) and a test comment. Tracked as issue #712. | DEAD |  |
| h | Changing the tax rate gives every settled booking a balance it does not owe, in either mode. | `balanceOwedCents` at `payment-config.ts:155` takes `taxRateBps` as a live argument while each `Payment` row carries its own frozen figures. Tracked as issue #574. No test named for this case found. | DEAD |  |
