# DEC-156 — worksheet

**Source:** `docs/decisions/DEC-156-guests-are-chosen-first-and-filter-availability.md`, 4,907 bytes, no amendments, 2026-08-16 (issue #715).

Evidence is mechanical only — does the file exist, does the column exist, does the route resolve, is there a test. Absence is stated as absence. Evidence never decides a verdict.

**Verdicts — six values.** The question every verdict answers: *does this ruling govern what I'm building now?* Code existing is evidence, not verdict.

| verdict | means | action |
|---|---|---|
| **TRUE** | Governs current work and is accurate. | Rewrite into schema, keep the DEC id. |
| **FALSE** | Governs current work but is wrong — the system doesn't behave this way. | Rewrite into schema, `status: superseded`. **Open an issue.** |
| **DEAD** | Doesn't govern current work. Superseded, or covers a feature cut from §2.8. Code may still exist and even be tested; irrelevant to the verdict. | Drop the ruling from the rewritten record. **No tombstone banner.** Issue only where dead rulings orphan real code — one per group, not per ruling. |
| **UNKNOWN** | Can't be adjudicated. Never tested, or provenance unclear. | Keep the ruling. **Open an issue** to investigate. |
| **MOVE** | Not a ruling at all — a fact about the world, not a choice that could have gone otherwise. Tax rates, jurisdictions, vendor limits. | Remove from the record. SPEC names **where the value lives**, never what it is. |
| **DELETE** | A concrete value swallowed into the record. Rates, thresholds, timings, limits. | Remove. Write the value nowhere. If no document names its source, open an issue for that. |

**MOVE vs DELETE:** MOVE is a fact that needs documenting somewhere; DELETE is a value that must not be copied anywhere. A row can be both — the fact moves, the number dies.

**Whole-record archive** (`docs/decisions/archive/`) applies only when every ruling is DEAD. Filename and ID preserved so citations resolve. Never delete a record file.

## DEC-156

| # | Ruling | Evidence | Verdict | Disposition |
|---|--------|----------|---------|-------------|
| a | The guest count is chosen before the date and is carried in the address alongside offering, date and time. | `Search = { offering?, date?, time?, guests? }` at `app/(public)/book/page.tsx:54`; parsed at `:145-151`; order stated at `:15`. | TRUE | SPEC |
| b | The guest count filters the calendar and the departure list to boats that can take the party. | `dayState(slots, date, today, guestCount)` at `src/reservations/availability-screen.ts:108,156`; `availability-screen.test.ts` exists. | TRUE | SPEC |
| c | A day whose boats are all free but all too small renders in its own state — not "sold out", not "nothing runs". | `DayState` union includes `"toobig"` at `availability-screen.ts:84`; `if (fitting.length === 0) return "toobig"` at `:117`. | TRUE | SPEC |
| d | Sold-out and too-big stay distinct, so a large party knows to bring fewer people rather than concluding they were unlucky. | Reasoning recorded at `availability-screen.ts:99-102`. Presentation is open — issue #752. | TRUE | DECISION |
| e | The fit is the same check the claim already ran, moved from the end of the funnel to browse time. | `candidateVessels` in `src/reservations/claim.ts` filters an offering's vessels; the browse path filters the same `VirtualSlot[]`. | TRUE | SPEC |
| f | The customer is quoted the smallest fitting boat's capacity, because that is the boat the claim will hand them. | `boatCapacity` at `availability-screen.ts:179`, computed as `Math.min(...pool.map(s => s.capacity))` at `:219`; reasoning at `:176`. | TRUE | SPEC |
| g | The stepper keeps local state with a debounced address update, so the number moves under the thumb but the links stay in step. | `app/(public)/book/book-controls.tsx` is the client island; hrefs carry `guests` (`page.tsx:151`). | TRUE | SPEC |
| h | Skipping the address update when nothing appears to change is wrong, because every day cell, departure row and month pager has the count baked into its link. | `hrefGuests` threaded at `page.tsx:151`. | TRUE | DECISION |
| i | The stepper's floor is one and its ceiling is the offering's largest boat. A party that does not fit the largest boat is not a booking this system takes. | Bounds read off the offering's vessels; `Vessel.coiMaxPax` in `src/domain/entities.ts`. No over-ceiling path under `app/(public)/book/`. | TRUE | SPEC |
