---
session: 5
dev: eric
slug: 1.3-m2-autoform
branch: task/1.3-m2-autoform
started: 2026-06-04T20:50:36Z
ended:
points:
pr_numbers: [19, 21]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/3d3af781-f91f-4f11-b084-f0217f73731d.jsonl
---

# Session 5 — 1.3-m2-autoform

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 1.4a / M3 — Oracle eligible-pool (composite satisfiability) + reliability logging (closes #9)

**Completed:**
- `src/oracle/eligibility.ts` — pure hard crew rules (`is_active`, `has_rating`, `mmc_valid_on_date`, `not_double_booked`, `not_on_pto`), structured `RuleResult` reasons (payloads, not sentences — §1.3), `evaluateCandidate` collect-all verdict. MMC is the universal hard gate (SPEC §2.1 "MMC is universal" — applies to every role, not captain-only; `role==="captain"` would violate DEC-ROLE-1). Date boundary aligned with `credential-health.ts`. medical/TWIC are soft/"M" rules, not gates (§1.3).
- `src/oracle/oracle.ts` — `eligiblePool` (per required seat, arbitrary order — ranking is 1.4b) + `solveShift`, the **DEC-003 composite shared-pool solve** (greedy; one person can't fill two seats). Double-booking is day-grained (not time-slot — documented simplification).
- `src/oracle/reliability-log.ts` — ask-lifecycle + commitment logging helpers; deterministic ids (codebase convention), injected `now` (DEC-008); decline-neutral / ignore-negative / bail-lateness encoded as distinct helpers.
- `src/ports/repository.ts` + `src/adapters/in-memory-repository.ts` — added `savePtoWindow` / `listPtoWindowsForCrew` (the oracle's PTO rule needs data the port didn't expose).
- 27 oracle tests incl. the DEC-003 false-yes trap; full suite **82 green**; typecheck + build clean, no `*.test.*` in `dist/`.

**Code review:** @code-review — solid, well-tested, clock-free. Headline "MMC-gates-all-roles bug" dispositioned as **not a bug** (SPEC §2.1 "MMC is universal" + role-agnostic acceptance criterion); added a comment so it isn't re-misread. Took 2 cleanups (`solveShift` `break`-on-starve, tightened boundary comment). `removePtoWindow` port asymmetry deferred — no caller in 1.4a, lands with the PTO-management admin surface.
**PR:** [#19](https://github.com/mobiustripper42/muster/pull/19)
**Points:** 5
**Branch:** task/1.4a-m3-oracle
**Opened at:** 2026-06-04T22:44:00Z

## Task 2: Phase 1.4b / M3 — Tier-1 ask/confirm loop + thin assignment view (closes #10)

**Completed:**
- `src/asks/ask-loop.ts` — the seat machine: `broadcastAsk` / `assignPerson` (two protocols, one machine — DEC-007), `recordResponse` (confirm-iff-open contested guard + **intra-shift double-booking guard**, first-acceptable-yes-wins; decline→neutral, all-declined reopens), `confirmSeat`, `bail` (DEC-019), `expireAsks` (clockless `ask_ignored` sweep, `respondedAt`-without-`response` = timed-out stamp), `manualOverride`, `rankPool` (Phase-2 insertion point), `resolveProtocol` (branch-free per DEC-ROLE-1). `now` injected throughout.
- `src/asks/assignment-view.ts` — thin `buildAssignmentView` + text render; `silent` first-class & distinct from `declined` (§2.4).
- **DEC-019** (`Bailed` is a transition, not a resting state): `bail()` re-asks → seat `Asked` on success, rests `Bailed`→AtRisk only on exhausted pool (the only AtRisk the clockless loop produces). `derive.ts` ⚠️ comment re-homed to the staffing-horizon task.
- **Issue #20** filed: deferred 1.3 shrink gaps (manning-shrink prune, all-cancelled→Cancelled) — builder-reconciliation, out of ask-loop scope per @architect.
- 17 asks tests; full suite **99 green**; typecheck + build clean, no `*.test.*` in `dist/`.

**Code review:** Up-front @architect (→ DEC-019, horizon-agnostic loop, deferred gaps→#20) + post-commit @code-review. Real bug caught & fixed: **cross-seat double-booking on a 2-crew shift** (eligiblePool excludes the current shift; added `committedOnShift` guard at ask + claim time — DEC-003 shared-pool). Also fixed a `derive.ts` comment that contradicted DEC-019, documented `manualOverride` displace-occupant, hoisted an N-query. No new DEC beyond 019.
**PR:** [#21](https://github.com/mobiustripper42/muster/pull/21)
**Points:** 5
**Branch:** task/1.4b-m3-ask-loop
**Opened at:** 2026-06-05T02:47:20Z

**Next Steps:**
- **Merge order:** #19 merged. #21 is stacked on `main` (cut after #19 merged) — merges clean.
- **Staffing-horizon task** (not yet an issue) — introduces the clock: Pending→Filling birth at the horizon, the early-vs-late-bail → Filling-vs-AtRisk refinement (the residual DEC-019 gap), and the "fills by" countdown the assignment view omits. Model horizon as DEC-004 list-of-one. **@architect** when scoping.
- **#20** (builder reconciliation: manning-shrink prune + all-cancelled→Cancelled) — likely wants the horizon clock too.
- Phase 2 reliability scorer plugs into `rankPool` (the single insertion point) + `solveShift` ordering (still arbitrary greedy).
- Per-role protocol **default storage** on `RoleType` (a `defaultProtocol?` field) — `resolveProtocol` takes it as a param today; storing it is a small DEC-ROLE-1-aligned follow-up.
- `removePtoWindow` + DEC-016 fleet manning/capacity validation — carried, land with the PTO-management / operator-confirm surfaces.

**Context:**
- The MMC-universal call is load-bearing: do **not** "fix" the oracle to gate MMC on captain-only — SPEC §2.1 says universal, and role→credential mapping is DEC-ROLE-1 tenant data (a `RoleType` field that doesn't exist yet), not a hardcoded role check.
- `solveShift` (oracle) is greedy, not a full bipartite matching — can miss an assignment a max-matching would find. The **ask loop** does NOT use `solveShift`; it works seats independently via `broadcastAsk`, with `committedOnShift` enforcing the shared-pool invariant the per-seat path would otherwise miss.
- DEC-019 hinges on `deriveShiftState` deriving AtRisk *only* from a `Bailed` seat — that's why `bail()` must rest at `Bailed` (not `Open`) when the pool is exhausted. Don't "simplify" bail to always reopen to Open; it silently kills the only AtRisk path.
