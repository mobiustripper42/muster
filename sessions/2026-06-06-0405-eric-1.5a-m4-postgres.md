---
session: 6
dev: eric
slug: 1.5a-m4-postgres
branch: task/1.5a-m4-postgres
started: 2026-06-06T04:05:00Z
ended:
points:
pr_numbers: [25, 27, 28, 29]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/f4318ab7-20a1-45ce-849d-61a03325e566.jsonl
---

# Session 6 — 1.5a-m4-postgres

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 1.5a / M4 (PR 3/3) — self-rolled magic-link + channel port + no-FK integrity discipline (closes #11)

**Completed:**
- **Magic-link auth** (`src/auth/magic-link.ts` + `index.ts`, `db/migrations/0002_auth.sql`): `issueMagicLink`/`verifyMagicLink`, **sha256 hash-only** token storage (plaintext secret never persisted), **single-use consume via a port CAS** (`consumeMagicTokenIfUnused`) — sibling of `saveSeatIfState`, closes the replay race on real Postgres. Injected `now` + `mintSecret` (clockless/deterministic core convention; `randomSecret()` = the one legitimate crypto-randomness exception, it's a secret not an id). Same mechanism admin + crew (DEC-010/DEC-020). **Session/renewal is explicitly 1.5b** — `verify` returns the subject; the longer-lived renewable session the PWA/native client stores builds on top without reopening auth.
- **Channel port** (`src/ports/channel.ts`) + **fake/log adapter** (`src/adapters/fake-channel.ts`): outbound-only `ChannelPort` seam (DEC-MSG-3) — web-link *or* Telegram both satisfy it, no hardcode; the fake records sends, doubles as the dev magic-link sink. Replies stay domain logic (re-enter via `recordResponse`), so the port is outbound-only by design.
- **No-FK integrity discipline (DEC-DATA-1)** — the debt Eric flagged, paid: (a) `src/domain/iso-date.ts` runtime validators (`isIsoDate`/`isIsoDateTime`/`isClockTime` + asserts; reject `2026-13-99`/`2026-02-30` via re-serialization) applied at external write boundaries — credential expiry (`crew-admin.ts`), Xola import dates (`import-reservations.ts`, batch-safe skip), and the auth path; (b) `src/admin/integrity.ts` `checkIntegrity` orphan diagnostic over the whole spine (seat→shift, credential→crew, token→crew, …; skips the append-only reliability log + admin-subject tokens), run in the contract over **both adapters** and wired into `/api/health`.
- **Port +10 methods** (`src/ports/repository.ts`): 3 magic-token ops + 7 `listAll*` enumerators; implemented in both adapters; contract cases added (`repository-contract.ts`).
- **146 core tests** (+17) + **20 pg contract** green; `verify` gate green; Postgres contract validated against real Docker Postgres then torn down.

**Code review:** @code-review (Sonnet) — CAS atomic + SQL parameterized, only the hash stored, validators correct, integrity coverage matches the model. **One security finding fixed in-PR:** unauthenticated `/api/health` was serializing the full integrity report (crew ids, topology) → now returns only `{ok, violationCount}`, detail logged server-side. Three deferred (see Next Steps): magic_tokens reaper, `mtk-<hash>` id embeds the hash, in-memory concurrent-consume test is structurally (not racily) green — Postgres is the real proof.
**PR:** [#25](https://github.com/mobiustripper42/muster/pull/25) (PR 3 of 3 for #11 — closes it)
**Points:** ~5 of the re-estimated 13 for #11 (the final third)
**Branch:** task/1.5a-m4-magiclink
**Opened at:** 2026-06-06T16:59:42Z

## Task 2: CI — GitHub Actions verify gate with a real Postgres

**Completed:**
- `.github/workflows/ci.yml` — one `verify` job on `pull_request`→main + `push`→main. Runs the **same `npm run verify`** as `/kill-this`, but stands up **postgres:17** as a service container (matching `docker-compose.yml`) so `postgres-repository.test.ts` runs the **contract suite instead of its skip-clean path**. That makes the in-memory↔Postgres adapter equivalence a required automatic check — the one gap the Docker-free local gate can't self-enforce (it was NOT checked on the day's merges; I ran it by hand). Service health-gated so a DB that fails to start fails the build (no false green).
- Node 22 (matches local); `actions/checkout@v5` + `setup-node@v5` (Node 24 runtime — dodged the v4/Node-20 force-deprecation 2026-06-16, no baked-in debt).
- **Validated end-to-end:** the PR's own CI run went green (`verify in 1m10s`, all steps incl. container init + full gate); locally confirmed verify-with-DB runs **166 tests / 20 files, 0 skipped** (vs 146 + 1 skipped Docker-free).
- **Out of scope (deliberate):** branch protection / required status checks — team-scaling, deferred with the deploy era (DEC-022). This PR is the *check*, not merge-blocking enforcement.

**Code review:** none (config-only, self-validating via its own green run). User asked for it explicitly — dislikes both money debt and technical debt; CI closes the silent pg-skip gap and the action-deprecation was fixed in the same PR.
**PR:** [#27](https://github.com/mobiustripper42/muster/pull/27) (merged)
**Points:** ~1
**Branch:** task/ci-github-actions
**Opened at:** 2026-06-06T18:14:43Z

## Task 3: Phase 1.5b / M4 (#12) — crew tap-in: the ask + my-shifts + magic-link landing + Tailwind foundation

**Completed:**
- **First real web UI.** Establishes the frontend stack + the app↔core data path (server component → repo → domain, after only the health route had touched the core).
- **Tailwind v4 foundation (DEC-021):** `app/globals.css` `@theme` tokens harvested from the mockups (colors, IBM Plex via `next/font`, radius), `postcss.config.mjs`, layout wiring. **No component library yet** — deferred to an `@architect`/research pass when the heavy admin surfaces land (every lib the owner uses branches from Tailwind, so picking later is free).
- **Session layer** (`src/auth/session.ts`, framework-free): HMAC-SHA256 signed, stateless, self-expiring tokens + sliding renewal (`shouldRenew`), constant-time compare, NaN-expiry guard. Built on `verifyMagicLink` (PR 3). `app/lib/auth.ts` = the Next cookie glue (httpOnly, **lazy** prod-fail-fast on `SESSION_SECRET`).
- **Crew surfaces** (`app/(crew)/crew/page.tsx`): the ask (push-style card, inline **In/Out** via a single server-action form, no client JS, green/red polarity), my-shifts (confirmed-upcoming, soonest-first), non-comparative standing chip; signed-out + DB-down states. Server action (`actions.ts`) double-guards (session + ask-ownership) then `recordResponse`.
- **Magic-link landing** (`/crew/auth`): verify-and-consume → set session cookie on the redirect response → `/crew`. Dev-only issuer (`/crew/dev-link`, prod-404'd) + committed dev seed (`npm run db:seed:crew`).
- **Read model** `src/crewapp/crew-view.ts` (+ test). **Docs:** DEC-021, `.claude/ui-context.md` (design system for `@ui-reviewer`), `SESSION_SECRET` in `.env.example`.
- **158 core tests** green (+ session/crew-view); `verify` green; **live end-to-end smoke passed** (dev-link → landing sets cookie → authed `/crew` renders; anon → signed-out).

**Code review:** @code-review — **one security fix landed:** `SESSION_SECRET` prod fail-fast (lazy, so `next build` doesn't trip it) — an unset secret in prod would be a repo-public signing key → session forgery. NaN-expiry guard added. HMAC/constant-time compare, ask-ownership guard, cookie-on-redirect, dev-link prod-gating, framework-free `src/` all verified. @ui-reviewer (8.5/10) — fixed WCAG `text-faint`→`text-muted`, In→green, two forms→one, 52px targets, deckhand-voice standing; hierarchy/empty-is-success/neutral-decline/non-comparative all correct.
**PR:** [#28](https://github.com/mobiustripper42/muster/pull/28)
**Points:** est. 5, **actual ~8** (Tailwind foundation + session layer + dev seed + smoke) — flag for retro re-estimate.
**Branch:** task/1.5b-m4-crew-tapin
**Opened at:** 2026-06-06T19:18:01Z

## Task 4: Phase 1.6 / M5 (#13) — shift card: call vs departure, dock pin, per-event manifest

**Completed:**
- **The hinge (SPEC §2.6.3)** — the crew member's single source of truth on the dock. Closes the slice's Xola-split concern.
- **`Event.dock?`** (per-event departure location — same boat can leave different docks): `entities.ts` + DDL `0003_event_dock.sql` + pg adapter mapper/save + contract present/absent round-trip (in-memory needs none). 20 pg contract green.
- **Call time = flat fleet-wide 45-min lead** (`CALL_LEAD_MINUTES`, call = earliest departure − 45) — the explicit ask was "45 minutes, period." The richer model (per-vessel prep + **additive** per-event positioning, ultimately auto-computed from a storage→dock distance) is parked in `FUTURE_IDEAS` — Eric's own domain nuance (prep is per-vessel, transit is per-event/route).
- **`src/crewapp/shift-card.ts`** (`buildShiftCard`, framework-free + tested): call/departure times, per-event **booked** manifest (name + party + phone, **no waiver** — DEC-012), pax, dock, `sharedDock` (one prominent pin when all events share a dock), co-crew contact. **Viewer-must-be-confirmed-crew guard** (no peeking at others' manifests).
- **`/crew/shift/[shiftId]`** surface (Tailwind, `<details>` no-JS collapsible manifest, tel:/sms:/maps links); my-shifts rows now `<Link>` to it (the #12-deferred affordance + chevron). Enriched dev seed.
- **164 core tests** (+6) + 20 pg contract; `verify` green; live smoke proved the full card (call 14:15 / departure 15:00, manifest 10/2/12 pax, shared dock pin, co-crew Hooper, ownership guard).

**Code review:** @code-review clean (dock equivalence, ownership guard, cancelled-exclusion, call math each asserted; framework-free). @ui-reviewer 8/10 — call-vs-departure distinction passes; **fixed in-PR:** dock hoisted to a top-level pin when shared (was buried in a collapsed manifest), 44px tap targets, visible expand chevron, `aria-hidden` emoji, empty-departures note. *(Two live-smoke "failures" were false alarms — React SSR comment markers + the RSC flight payload duplicating strings in the served HTML; the page renders correctly.)*
**PR:** [#29](https://github.com/mobiustripper42/muster/pull/29)
**Points:** ~5 (lighter than #12 — no new stack work)
**Branch:** task/1.6-m5-shift-card
**Opened at:** 2026-06-07T03:42:50Z

**Next Steps:**
- **#13 closes the M0–M5 vertical slice.** Likely a phase-boundary moment → consider `/retro` (Phase 1 velocity, re-estimate #12 as ~8 / #11 as 13-split, version bumps) once #29 + the open PRs merge. Real-weekend dry run is the rule-of-thumb gate before promoting anything from FUTURE_IDEAS.
- **Deferred from #13 review** (not yet issues): shift-card **bail** action (the "bailing is as easy as accepting" principle — `bail()` exists, needs button + confirm + server action); **credential nudge** (expiring MMC); **live-changed indicator** ("changed since last viewed" — needs last-viewed tracking); iOS native-maps handoff (`geo:`); `minusMinutes` midnight wrap (inert today).
- **Deferred from #12 review** (not yet issues): (1) **N+1 in `buildCrewAppView`** — `listAllSeats`/`listAllAsks` table-scans + per-row sequential `getShift`/`getVessel`/`getRoleType`; fine at BrewBoat scale, wants a batched/date-bounded read before the tables grow. (2) **My-shifts tap affordance** (rides #13). (3) **In/Out live-click automated test** — no UI test harness (Playwright) yet; write path is unit-tested via `recordResponse` + the action guards, but not exercised through a real click.
- **Component-library decision (DEC-021 trigger):** choose via `@architect`/research when the first heavy admin surface (assignment view / at-risk board / roster) is next — layered on Tailwind without rework.
- **2 moderate npm-audit vulns** = Next's transitive `postcss` (upstream; `audit fix --force` would downgrade Next — not taken). Clears when Next bumps.
- **CI now gates every PR** against a real Postgres — but no branch protection yet, so green is advisory, not merge-blocking. Add required-status-checks when a 2nd contributor or the deploy era arrives (DEC-022).
- **#11 closes with PR #25 merge → M4 infra standup done.** Next is **1.5b / #12** (crew tap-in: the ask, my-shifts, magic-link landing) and **1.6 / #13** (manifest on the card). 1.5b is where the **session layer** lands (cookie/token + silent renewal on top of `verifyMagicLink`) and where **`.claude/ui-context.md`** must be authored (from `docs/BRAND.md` + `docs/design/DESIGN-REFERENCE.md` + mockups) before `@ui-reviewer` can run.
- **Auth tech-debt deferred from PR-3 review** (not yet issues): (1) **`magic_tokens` reaper** — expired-unconsumed tokens accumulate forever; add a scheduled `delete ... where expires_at < now` (or a sweep), wants the horizon clock. (2) **Token id `mtk-<sha256>` embeds the hash** — fine today (hash ≠ secret), but consider an opaque id so id ≠ credential-hash if the id ever gets logged/returned. (3) Authenticated admin **integrity-detail surface** (the full violations the health route now withholds).
- Carried from S5, still open: **staffing-horizon task** (introduces the clock — Pending→Filling birth, early-vs-late-bail Filling-vs-AtRisk, the "fills by" countdown; the reaper above likely rides this); **#20** builder reconciliation (manning-shrink prune + all-cancelled→Cancelled); Phase-2 reliability scorer → `rankPool` + `solveShift` ordering; `RoleType.defaultProtocol?` storage; `removePtoWindow` + DEC-016 fleet manning/capacity validation; hand-author the held-back seeds (Postgres-DDL migration protocol, data-fetching/auth conventions per DEC-DATA-1, `<VersionTag/>` wiring).

**Context:**
- **The no-FK / dates-as-text bet is now executable, not aspirational** (PR #25): validators are the "door", `checkIntegrity` is the loud-failure relocated to our schedule. Do **not** add FKs back — it breaks the in-memory↔Postgres equivalence the contract proves. The validators live in the **service layer** (DEC-DATA-1), not the adapters. ISO-date enforcement is **runtime validators**, not branded types (chosen this session for the 5pt budget); full branding of the `string` date fields is a deferred follow-up.
- **Magic link ≠ session.** The link is single-use + minutes-short; the session it authorizes is days-long + renewable + client-stored — and is **1.5b's** job. `verifyMagicLink` returning the subject is the seam; don't reopen auth to build sessions.
- **Delivery is channel-agnostic.** The magic link is delivered through the channel port like any message — email/dev-stub today, SMS/push/Telegram/web-link by swapping the adapter. The auth service never changes.
- **`node:crypto` is fine in the core** — it's Node stdlib, not a framework; the core stays framework-free (no Next/React). Randomness is allowed *only* for the magic-link secret; ids stay deterministic.
