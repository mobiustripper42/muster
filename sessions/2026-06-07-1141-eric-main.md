---
session: 7
dev: eric
slug: main
branch: main
started: 2026-06-07T11:41:54Z
ended:
points:
pr_numbers: [33, 34, 35]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/68bb7044-1d45-4681-82d6-46245f071178.jsonl
---

# Session 7 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 2.1 / Pass A (#30) — reliability scorer: blended score from logged events

**Completed:**
- **The Pass-A mechanism** (`src/oracle/reliability-score.ts`, framework-free + pure): `computeReliabilityScore(events, now, opts?) → {score, eventCount, windowEvents}` turns a crew member's append-only `ReliabilityEvent` log into one blended **ranking signal** (SPEC §1.4, DEC-008). Neutral-0 baseline (cold start = mid-pool, lines up with `rankPool`'s `?? 0`), additive **sum** over a **count-based rolling window** (most recent `WINDOW_EVENTS`, default 40 ≈ a season, tunable), **decline-neutral** (`ask_declined` = 0), **`ask_ignored` the lone ask penalty**, **bail-lateness-scaled** penalty (`metadata.latenessMs`, higher=later=worse, linear) on top of a flat bail weight, `no_show` the floor. All weights + window tunable via `DEFAULT_WEIGHTS`/`opts` — the Pass-A tuning lever, deferred until real data.
- **Design pivot (Eric, post-review): count-based window, not calendar.** Original cut used a 90-day calendar window; Eric flagged that BrewBoat is seasonal — an off-season gap (~8 months) is longer than any sane calendar window, so every returning veteran would reset to neutral each spring (flat ranking exactly when staffing up). Count-based window carries history across the gap (off-season has no events to consume the count). Added a **seasonal regression test** for it.
- **Parked in `docs/FUTURE_IDEAS.md`** (Eric, explicitly v1-deferred): (1) **volume-neutral scoring** — sum means rookies sit near the bottom; weighted-average/confidence blend is fairer but changes the number's meaning, wants real data. Sum stays for v1. (2) **inverse-exponential bail-lateness** — linear under-penalizes the truly last-minute bail; steep near-call ramp is the real curve. Linear stays for v1.
- `scoreCrewMember(repo, crewId, now, opts?)` — thin port bridge (`reliabilityEventsFor` → pure fn), `now` injected, no clock read.
- Exported from the oracle barrel (`src/oracle/index.ts`).
- **Contract fix:** rewrote the contradictory `latenessMs` doc-comment on `src/domain/reliability.ts` to the higher=later=worse contract the scorer relies on. No producer computes it yet — this sets the contract for #31's bail path.
- **22 unit tests** (`reliability-score.test.ts`) against seeded logs; `npm run verify` green.
- **Scope split (confirmed with Eric):** #30 = scorer mechanism only. `rankPool` wiring + manual boost/floor = **#31**; crew standing = **#32**. The score is not consumed anywhere yet — `rankPool` still reads the flat `reliabilityScore` field (null everywhere).

**Code review:** @code-review (Sonnet) — correct, pure, framework-free; every acceptance criterion honored. All findings cleanup-level and **fixed in-PR:** missing barrel export, a mislabeled netting test (replaced with a real netted-to-zero case), and coverage gaps (malformed timestamps, inclusive window edges, independently-tunable bail-lateness lever).
**PR:** [#33](https://github.com/mobiustripper42/muster/pull/33)
**Points:** ~5 (the #30 estimate)
**Branch:** task/2.1-reliability-scorer
**Opened at:** 2026-06-07T13:16:00Z

## Task 2: Phase 2.2 / Pass A (#31) — rank eligible pool by reliability + manual boost/floor

**Completed:**
- **The #30 scorer now drives ask order.** One ranking authority in `src/oracle/reliability-score.ts`, flat `rankPool` retired: `effectiveRankScore(score, crew)` (pure — `manualFloor` raises/never lowers, `manualBoost` adds, cold-start = neutral 0) + `rankByReliability(repo, crew, now, opts?)` (async — scores each from their log, sorts best-first, deterministic id tie-break) + `rankEligibleIds(repo, ids, now)` (shared hydrate-ids-then-rank fan-out).
- **Wired all three consumers:** `ask-loop.rankedEligible` (broadcast/re-ask order), `oracle.solveShift` (greedy pick reliability-ordered — ranks the **union** of eligible crew once, reuses global order across seats → no per-seat N+1), `assignment-view` (pool Spink sees). `now` threaded through each (solveShift + buildAssignmentView had no prod callers → low ripple).
- **DEC-007 untouched:** ranking sets ask *order* only; first-acceptable-yes still wins. New test pins it (lower-ranked crew who answers first keeps the seat).
- **Display vs ranking split** flagged: `CrewMember.reliabilityScore` marked **display-only** (ranking is log-derived). Reconciling the crew-view/roster display read to `scoreCrewMember` is **#32**; field stays null/flat (DEC-008) so no divergence in practice today.
- 197 tests (196 pass + 1 Docker-only pg skip); `npm run verify` green.

**Code review:** @code-review (Sonnet) — correctly wired, `now` clean, DEC-007 genuinely untouched. All findings fixed in-PR: stale `rankPool` module doc; display-vs-ranking documented (→#32); **per-seat N+1 in solveShift removed** (rank union once); fetch-and-rank duplication → `rankEligibleIds`; `void a` smell; +negative-floor and DEC-007 first-yes-over-rank coverage.
**PR:** [#34](https://github.com/mobiustripper42/muster/pull/34)
**Points:** ~3 (the #31 estimate)
**Branch:** task/2.2-rank-by-reliability
**Opened at:** 2026-06-07T16:30:00Z

## Task 3: Phase 2.3 / Pass A (#32) — crew own-standing: real score + plain reasons

**Completed:**
- **The crew-facing half of Pass A.** `src/crewapp/standing.ts` (pure, framework-free): `summarizeStanding(events, now)` → individual, non-comparative deckhand reasons joined by ` · ` ("answered fast · showed 8/8 · one late bail"). Positives-first; factual negatives stated neutrally (no grade/leaderboard/streak/scolding — BRAND, DEC-008). `ask_ignored` → plain "missed N asks", not a nag. Three-way bail bucketing (late ≥ `LATE_BAIL_MS` / early / plain when lateness unrecorded — don't assert a fact the log lacks). Cold-start neutral; "Still building a track record" fallback when history exists but nothing notable.
- **`reliability-score.ts`:** extracted `windowedEvents` + `resolveWindowCap` so standing and score read the **exact same** windowed set (can't disagree). `computeReliabilityScore` behavior unchanged.
- **`crew-view.ts`:** `buildCrewAppView` derives standing from `reliabilityEventsFor(me)` — **drops the stale `me.reliabilityScore` read**, closing the #31 display-vs-ranking divergence.
- **Crew page:** standing moved from a cramped header pill → quiet muted subline under the name (`aria-label`, `leading-snug`, comment guarding against ever coloring negatives). `ui-context.md` de-staled (chip → subline).
- 212 tests (211 pass + 1 Docker-only pg skip); `verify` green.

**Code review:** @code-review (Sonnet) — solid, on-voice, framework-free; `windowedEvents` extraction behavior-preserving. Fixed in-PR: neutral "bail" for unrecorded lateness (was overclaiming "early bail"), DRY'd window-cap via `resolveWindowCap`, +plain-bail/future-dated/digit-fallback coverage. @ui-reviewer 8.5/10 — subline-over-chip endorsed; a11y + leading-snug fixed; contrast AA-verified.
**PR:** [#35](https://github.com/mobiustripper42/muster/pull/35)
**Points:** ~3 (the #32 estimate)
**Branch:** task/2.3-crew-standing
**Opened at:** 2026-06-07T17:25:00Z

**Next Steps:**
- **#32 closes Phase 2 (Pass A — reliability ranking).** Scorer (#30/PR33), pool ranking + boost/floor (#31/PR34), crew standing (#32/PR35) all shipped. Remaining Phase-2 issue: **#20** (builder reconciliation — manning-shrink seat prune + all-cancelled→Cancelled), unestimated. After #20 + merges → likely `/retro` for Phase 2 (velocity, version bump v0.4.0).
- **Deferred from #32 review:** admin `roster.ts` still reads the stale `reliabilityScore` field (separate comparative band view, §1.4) — reconcile to `scoreCrewMember` if/when that surface is built out. `buildCrewAppView` N+1 now +1 (the standing log read) — fold into the batched fetch at the Postgres adapter.
- Tuning placeholders awaiting real data (Pass-A payoff): `WINDOW_EVENTS=40`, `DEFAULT_WEIGHTS`, `FAST_REPLY_MS=30m`, `LATE_BAIL_MS=24h`, `bailLatenessPerHour`. Parked FUTURE_IDEAS: volume-neutral scoring (rookies), inverse-exponential lateness.

**Context:**
