---
session: 9
dev: eric
slug: main
branch: main
started: 2026-06-08T23:31:07Z
ended: 2026-06-09T13:39:14Z
points: 5
pr_numbers: [45]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/65958bf0-b0e5-4c18-9e97-d22979563158.jsonl
---

# Session 9 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 3.1a / #39 — staffing-horizon clock (the time dimension)

**Completed:**
- **Introduced the clock to the shift state machine** — the core was deliberately clockless through M0–M2; this is the foundational Phase-3 unblocker. Preceded by an **@architect pass** (2026-06-09) captured as **DEC-022** (horizon derived not stored; composition layer) + **DEC-023** (explicit `tick`, no scheduler v1), both written to `docs/DECISIONS.md` before building, and **#39 split** from 8→ this 5pt task + #44 (3.1b token reaper, 2pt).
- **`src/builder/derive.ts`:** `deriveShiftState(seats)` kept seat-pure (DEC-005); added `STAFFING_HORIZON_LEAD_DAYS` (7d default, tune-later), `staffingHorizonFromEvents`/`staffingHorizonFor` (earliest scheduled event − leadDays; clock times UTC, documented v1 simplification), and **`resolveShiftState(seats, {now, horizon, poolExhausted})`** — the composition layer overlaying time on the seat-fold (pre-horizon→Pending, post-horizon born Filling, exhausted→AtRisk, Crewed/Cancelled as-is). No `Crewed→Filling` edge — DEC-019's transient Bailed handles early-bail at the seat level.
- **`src/builder/tick.ts` (new):** `tick(repo, now)` sweeps shifts, resolves via the overlay, persists changes, and **eagerly fires Tier-1 asks** (`broadcastAsk`) for newly-Filling shifts. `poolExhausted` via `solveShift`'s **distinct-assignment composite** (DEC-003), not bare per-seat pools. Skips Cancelled/Completed.
- **`src/builder/form-shifts.ts`:** opt-in `now`/`leadDays` so a shift imported past its horizon is born Filling; omitting the clock keeps the old Pending birth (backward-compatible — all prior tests green).
- **Seam held:** port / `entities.ts` / Postgres DDL / adapters **untouched** (the stored-horizon temptation avoided, per DEC-022). +20 tests; `npm run verify` → 238 pass / 1 Docker-pg skip / build green.

**Code review:** @code-review (Sonnet) — overlay logic correct, purity/now-injection clean. **One real bug fixed in-PR:** `poolExhausted` used bare `eligiblePool` (per-seat), so a half-filled 2-crew shift whose last candidate is already committed to a sibling seat read as fillable → stuck Filling instead of AtRisk; switched to `solveShift` distinct-pool + added the pinning test. Also reconciled DEC-022/023 text to the shipped signature + documented the between-ticks staleness invariant, and added multi-shift-sweep coverage.
**PR:** [#45](https://github.com/mobiustripper42/muster/pull/45)
**Points:** ~5 (the 3.1a estimate)
**Branch:** task/3.1a-staffing-horizon-clock
**Opened at:** 2026-06-09T13:19:17Z

**Next Steps:**
- **#44 (3.1b magic-token reaper, 2pt)** is the small sibling split from #39 — a separate-aggregate sweep over expired `MagicToken` rows. Good next pickup.
- Phase 3 remaining after that: **#40** (Tier 2 escalation, 5), **#41** (At-Risk derivation, 5), **#42** (At-Risk board surface, 5), **#43** (decision surface: lean, 3). #40/#41 depend on this horizon clock.
- **No scheduler yet (DEC-023):** `tick` is exercised by tests/manual only; wire the Vercel cron caller at first hosted deploy. The persisted shift badge is eventually-consistent between ticks — `tick` is the sole reconciler.

**Context:**
