---
session: 11
dev: eric
slug: pull-seeds-2026-06-10
branch: task/pull-seeds-2026-06-10
started: 2026-06-10T03:30:15Z
ended:
points:
pr_numbers: [51, 52]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/59b87523-7676-4ece-ab64-7a4456b7dd29.jsonl
---

# Session 11 — pull-seeds-2026-06-10

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 3.3 / #41 — At-Risk derivation (board membership + urgency sort)

**Completed:**
- **Phase-grouping call first (new task-splitting params):** remaining Phase 3 (13pt) ships as **two units** — #41 alone (decision-bearing domain deriver) then #42+#43 together (board UI + lean action, one eyeball pass). Split point = the domain/UI seam; @architect concurred.
- **@architect pass on the drafted #41 plan → 3 real modifications adopted:** (1) core membership route (a) = **resolved-state `AtRisk` recomputed on read** (DEC-023 corollary) so eligibility-exhaustion boards immediately at any distance — only willingness-exhaustion waits for the 48h threshold (the documented asymmetry: "no one *may*" is urgent now, "no one *wants to*" is urgent near the dock); (2) row gains **`available`** (SPEC §2.5's who's-still-available, the lean's targets); (3) credential-lapse covers **Claimed+Confirmed** and reuses `mmcValidOnDate` (one boundary with the oracle). **DEC-025 recorded** — captain > mate expressed as pool-thinness, never a role-name check (DEC-ROLE-1).
- **`src/admin/at-risk-board.ts` (new):** `deriveAtRiskBoard(repo, now, opts?)` → `AtRiskRow[]` most-urgent first. Reasons: core / regression (rested-`Bailed`, DEC-019 makes can't-auto-refill true by construction) / credential_lapse (checked on `Crewed` too — the boat that looks fine). One distinct-pool solve per shift (trail's `exhausted` doubles as `poolExhausted`). Urgency = flat additive (time + thinness + `REGRESSION_URGENCY=72`), `EXHAUSTED_THRESHOLD_HOURS=48` = the DEC-TBD knob, now pointed at from DECISIONS.md.
- **`src/builder/derive.ts`:** exported `earliestScheduledStart`; horizon fn refactored over it (behavior-preserving).
- **Tests:** 20, ordinal by design (regression beats similar-time never-filled but does NOT bury a trip leaving in 3h; thin **mate** pool outranks deep **captain** pool — the DEC-025 pin). `npm run verify` → 282 pass / 1 Docker-pg skip (+20 over main's 262).

**Code review:** @code-review (Sonnet) — predicates/one-solve/boundary/ordinal arithmetic verified. 2 consistency findings **fixed in-PR**: `available` now excludes the shift's bailers (matches the re-ask's DEC-019 exclusion — was offering the walker unmarked); pre-horizon regression boarding decided + documented as intent (a rested bail IS pool-exhaustion). Test gaps closed (48h-exact inclusive bound, Claimed lapse, unreachable Bailed-with-occupant fixtures).
**PR:** [#51](https://github.com/mobiustripper42/muster/pull/51)
**Points:** ~5 (the 3.3 estimate)
**Branch:** task/3.3-at-risk-derivation
**Opened at:** 2026-06-10T04:08:00Z

## Task 2: Phase 3.4+3.5 / #42+#43 — At-Risk board surface + lean (one PR, closes Phase 3)

**Completed:**
- **Run as one 8** per the task-splitting call from Task 1 — the board UI and its lean action share one review mode (eyeball + @ui-reviewer); #43 alone is a button on #42's rows.
- **@architect pass first → DEC-026**, three modifications adopted: (1) **ping = detection-now / delivery-later** — `tick` derives the board (single membership source) after the advance/escalate sweep (order is load-bearing: a fresh nudge keeps its shift off the board that tick) and logs `board_landed` per **(shift, reason)** — so a rescued shift that later *regresses* re-pings; actual delivery waits for the DEC-MSG-3 pilot adapter like every other outbound message. (2) **Lean = `assignPerson` + `nudged{manual: true}`** — no new event type; the manual flag keeps "the human stepped in" derivable from the one log; lean-accept is the named future `at_risk_rescue` hook. (3) Reschedule/cancel render **disabled** with honest copy (live cancel without its cascade would violate "cancel is never delete" worse).
- **Domain:** `src/asks/lean.ts` (guards: live-ask, bailer, eligibility/distinct-pool; reopens a resting-`Bailed` seat — its empty-pool precondition no longer holds if a target exists; machine error codes); `tick.ts` landing detection + `resolveShiftStateOnRead` (the shared DEC-023 on-read recipe); `at-risk-board.ts` gains `credentialLapsed` (who) and `available` redefined as **exactly lean's accept-set**.
- **App (first admin surfaces):** `/admin/at-risk` board (admin-gated; empty=success; regression pinned/distinct; SYSTEM-TRIED trail; per-person Lean form buttons, no client JS; feedback via **coded** redirect params mapped to copy server-side — pattern recorded in CLAUDE.md Error Handling) + thin read-only `/admin/shift/[shiftId]` click-through (badge resolved on read; silent ≠ declined). Dev-link mints `?admin=` links; admin magic links land on the board. `npm run verify` → 297 pass / 1 skip (+15 over 282).

**Code review:** @ui-reviewer — 2 High **fixed in-PR** (text-faint contrast → muted incl. the departure time; seat role labels on assignment cards) + warn token darkened for AA, visible disabled-action copy, wrap-safe trail. @code-review — no bugs; 1 consistency finding **fixed in-PR** (board offered names `lean()` would reject — one definition of "leanable" now) + coded params (no raw-text reflection in a trusted surface), action try/catch, `TIGHT_HOURS` named.
**PR:** [#52](https://github.com/mobiustripper42/muster/pull/52)
**Points:** ~8 (the 3.4 + 3.5 estimates)
**Branch:** task/3.4-3.5-at-risk-board
**Opened at:** 2026-06-10T05:25:00Z

**Next Steps:**
- **Merging #52 closes Phase 3** (all of #41–#43 done) → next session: `/retro` (per-session time math, velocity, version bumps), then `/start-phase` for Phase 4 (Pass C fast-follows).
- **Follow-up candidates surfaced by review (file at Phase 4 planning):** (1) expose a real **"fills by"** deadline on `AtRiskRow` (mockup's decision deadline — domain has no fill-deadline concept yet; don't fake it in UI); (2) extract shared `components/ui/shell.tsx` + `notice.tsx` (third copy-paste of Shell/Notice, toned variant as superset); (3) cockpit-era: `buildAssignmentView` shows no pool for a `Bailed` seat — the regression click-through shows less than the board did; (4) multi-trip times on board rows (needs eventIds or times on the row).
- **DEC-MSG-3 pilot adapter** remains the delivery seam for BOTH crew asks and the new admin ping — one wiring moment.

**Context:**
- The board's `available` and `lean()` deliberately share one accept-set definition (Open/Bailed pools minus bailers minus live-ask holders) — if either changes, change both (comments cross-reference).
- Same-reason board re-landings don't re-ping (accepted v1 wrinkle, DEC-026).
- `--color-warn` darkened to #92400e for AA at pill sizes — mockup ambers are lighter; don't "fix" it back.
- Admin sessions: `subjectId` is a plain string handle (no admin entity); `/crew/dev-link?admin=<handle>` is the dev path in (still prod-hard-stopped).

**Context:**
