---
session: 12
dev: eric
slug: main
branch: main
started: 2026-06-10T18:10:36Z
ended:
points:
pr_numbers: [61, 62, 63]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/5cbcb79c-83a4-4382-899d-1638aed986f7.jsonl
---

# Session 12 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 4.8 / #60 — UI chassis: shared Shell/Notice extraction

**Completed:**
- New `components/ui/shell.tsx` (Shell with `width` prop — `md` default / `2xl` / `3xl`, literal class strings for the Tailwind scanner) + `components/ui/notice.tsx` (the board's toned `ok`/`bad` variant as the one superset definition).
- Replaced **four** hand-rolled copies (issue said three): `app/(crew)/crew/page.tsx`, `app/(crew)/crew/shift/[shiftId]/page.tsx`, `app/(admin)/admin/at-risk/page.tsx` (`width="3xl"`), `app/(admin)/admin/shift/[shiftId]/page.tsx` (`width="2xl"`). Relative imports (no `@/` alias in this repo).
- `npm run verify` green — 298 pass / 1 Docker-pg skip, build clean. No visual change by construction (class sets byte-equivalent per call site).

**Code review:** @code-review — clean, class-equivalence verified per call site. Advisories: a **fifth** inline Shell copy in `app/(admin)/admin/page.tsx:11` (convert when 4.2 touches that area); crew shift card + board page still >200 lines (pre-existing, both shrank here).
**PR:** [#61](https://github.com/mobiustripper42/muster/pull/61)
**Points:** 1
**Branch:** task/4.8-ui-chassis
**Opened at:** 2026-06-10T18:35:00Z

## Task 2: Phase 4.2+4.3 / #54+#55 — Assignment cockpit + warming view (Unit B)

**Completed:**
- **@architect pre-pass → DEC-027**, five forks ruled: (1) four actions only — assign (`assignFromPool`, NEW guarded rail: lean's accept set per seat, no nudge log; `manualOverride` stays the one unguarded path), nudge=`lean`, confirm, override; broadcast/widen deferred (widen has no rail by DEC-024). (2) **Automation-pause = emergent** — escalate needs no-live-asks, manual actions create live asks; no flag in v1, explicit toggle parked in FUTURE_IDEAS with its trigger. (3) **Warming = candidate predicate minus real `deriveAtRiskBoard` rows** (membership single-sourced, DEC-026 — my draft's state-check disjointness was the forbidden second membership check, architect caught it). (4) Honest header — "departs in" + horizon fact; "fills by" NOT faked, lands with #59. (5) Assign server-side re-validation (crafted post ≠ unlabeled override).
- **Build deviation from the architect draft, documented in DEC-027 §3:** the answered/asked response-rate signal reads 0% the instant a broadcast fires (instant-warm on every broadcast = anti-conservative); membership signals became **ghosted** (≥1 silent) OR **quiet** (asked>0, pending=0); responseRate survives as a display fact over settled asks.
- **Core:** `src/asks/assignment-view.ts` (pools on Open/Asked/Bailed with SHIFT-wide exclusions; header facts trips/pax/tripStart/horizon/roleName), `src/asks/lean.ts` (+`assignFromPool`, shared guards), `src/admin/warming.ts` (new deriver). **App:** cockpit page + 4 server actions (codes-in-params) + `components/assignment/{seat-card,candidate-row,bits,warming-panel}.tsx`; admin home Shell conversion (the 4.8 fifth-copy advisory). Seeds E (claimed/confirm demo) + F (warming Kettle); RUNNING.md cockpit recipe; FUTURE_IDEAS pause-toggle.
- `npm run verify` 345 pass / 0 skip (pg suite live); seed + warming derive dry-run against real Postgres (warming = exactly Kettle).

**Code review:** @ui-reviewer — 1 High (AA: `text-faint` on interactive control) + 3 Medium (horizon UTC-truncation date bug; Asked seats must name who's in flight; lean is shift-level → honest tooltip) + 5 Low, **all fixed in-PR**. @code-review — 1 real bug **fixed in-PR + 2 pinning tests**: view exclusions weren't shift-wide (bailer rendered "said yes" on the seat they walked; Assign offered names the action refuses); also `assignFromPool` required-only + component split <200 lines.
**PR:** [#62](https://github.com/mobiustripper42/muster/pull/62)
**Points:** ~11 (the 4.2 + 4.3 estimates)
**Branch:** task/4.2-4.3-assignment-cockpit
**Opened at:** 2026-06-11T02:25:00Z

## Task 3: Phase 4.4+4.5 / #56+#57 — Crew bail flow + credential nudge (Unit C, stacked on #62)

**Completed:**
- **DEC-028** (@architect pass): `latenessMs` = notice shortfall vs the staffing horizon, clamped (`bailLatenessMs` in derive.ts); raw signed `noticeMs` logged alongside (DEC-008 — the derived value bakes in today's leadDays; history must stay re-derivable); server-side only. Closes the definition gap DEC-019 left open. **DEC-027 amended:** cockpit inventory = five actions — "Reports a bail…" (admin half of #56, also the mis-tapped-override recovery; scope add recorded on the issue).
- **Core:** `bailWithDerivedLateness` (ask-loop) — ONE home of the events→lateness→notice glue + **occupant pin** (`expectedBailer` — an occupant swap between reads can't log against the wrong person); `worstCredential` (credential-health); `credentialNudge` on CrewAppView; `mySeatId` on ShiftCardView.
- **App:** crew card "I can't make it…" (closed details, 44px summary, neutral copy) → `bailFromSeat` (ownership gate = respondToAsk pattern); crew home credential line (warn tokens) + suppressible post-bail notice (no re-asking claim); cockpit "Reports a bail…" → `reportBail`.
- **Seeds:** Quint MMC ~30d (nudge demo); **stale-seat reaper** — dry-running caught a zombie `seat-soon` row (old seed id) hijacking `mySeatId` on a 3-seat 2-crew boat. RUNNING.md crew + reports-a-bail recipes (both-seeds honesty: rest-Bailed vs re-ask).
- Verify: **365 pass / 0 skip** (pg live); bail rail dry-run on real pg (re-ask path: lateness 0 at 23.5d notice, noticeMs logged).
- **Mid-task on #62 (user feedback, 3 copy commits pushed to its branch):** posture line rewritten (what "won't fight you" means); **"Assign" → "Ask to fill"** (assign sends an ask; only override places); board link "Open in Assignment" → "Assignment".

**Code review:** @ui-reviewer 8/10 — all fixed in-PR (44px hit area, stale copy, post-bail honesty + crafted-param suppression, tone reword). @code-review — 3 consistency findings fixed in-PR (glue dedup → helper; wrong-person TOCTOU → occupant pin + tests; raced ≠ outage). Parked: log-before-seat-write atomicity (pre-existing; integrity-tripwire candidate).
**PR:** [#63](https://github.com/mobiustripper42/muster/pull/63) — **stacked, base = task/4.2-4.3-assignment-cockpit; merge #62 first**
**Points:** ~5+1 (4.4 + 4.5 estimates + the recorded admin-reporter add)
**Branch:** task/4.4-4.5-crew-bail
**Opened at:** 2026-06-11T03:40:00Z

**Next Steps:**
- **Merge order: #62 then #63** (GitHub retargets the stack automatically).
- Phase 4 remaining: #58 builder nudge (solo), #59 "fills by" deadline (decision-bearing, @architect, next DEC), 4.1/#53 still blocked on the channel pick (web-link vs Telegram — ask Eric each session).
- **Pending user decision:** lean-notice "watch it ↗" link to the cockpit (the row leaves the board when an ask goes in flight — offered option (a), no answer yet).
- **Review follow-ups parked:** MiniButton tap-targets ~26px if the cockpit goes dockside-mobile; cockpit page.tsx 312 lines (split when next touched); bail log-before-write atomicity → service-layer integrity tripwire list.

**Context:**
- The accept-set is now defined in ONE place per layer: domain guards in `lean.ts` (shared by lean + assignFromPool), view exclusions in `assignment-view.ts` mirror them shift-wide (bailers everywhere; live-ask holders visible only where their ask rides), board `available` unchanged. Change one → change all (comments cross-reference).
- `deriveWarming` derives the BOARD first (subtraction) — it's only computed when the panel is opened (`?warming=1`), so the cockpit's default render doesn't pay for it.
- Seed scenario E/F reset on re-run like the others; `shift-soon` on the live board is leftover from `db:seed:crew`, not at-risk seed.

**Context:**
