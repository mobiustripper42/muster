---
session: 14
dev: eric
slug: main
branch: main
started: 2026-06-12T04:18:05Z
ended: 2026-06-15T03:21:46Z
points: 15
pr_numbers: [71, 74, 79, 80]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/f2dda592-ecf9-4f0d-9ce0-d1e8f5675142.jsonl
---

# Session 14 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: "Fills by" deadline + multi-trip times (4.7, #59, DEC-031)

**Completed (built by main session, @architect pass up front — Fable):**
- **DEC-031:** "fills by" = `earliestScheduledStart − FILL_DEADLINE_HOURS`, **derived, no migration**. Architect's synthesis of the operator's tune-later lean: the constant already existed as the board's willingness-exhaustion threshold (`EXHAUSTED_THRESHOLD_HOURS = 48`) — so *name* it, don't mint a twin. One constant ⇒ displayed deadline IS the escalation instant, can't drift.
- Core: `src/builder/derive.ts` — `FILL_DEADLINE_HOURS`, `fillDeadlineFromEvents`/`fillDeadlineFor`, `scheduledStarts` (all trips). `src/admin/at-risk-board.ts` — `EXHAUSTED_THRESHOLD_HOURS` re-exports the one constant; `AtRiskRow` gains `fillsBy` + `tripStarts` (threads `deadlineHours` so override moves both). `src/asks/assignment-view.ts` — cockpit `fillsBy`.
- UI: board (`page.tsx` + `risk-row.tsx`) renders `fills by …` (overdue honest/null absent) + every `departs …`; cockpit header fills-by line. `app/lib/format.ts` — shared `fmtDeadline` (board+cockpit, review fix).
- Seed: two-trip Tidewater scenario (`extraTrips` on `shipShift`); RUNNING.md eyeball steps. Dry-ran the derivation on live PG — Tidewater two times, <48h rows overdue, ~4–5d rows future.

**Code review:** @code-review — clean bill, zero blocking. Fixed 2 (shared `fmtDeadline`, keyed `departs` by index); kept `fillDeadlineFor` for `staffingHorizonFor` parity.
**Verified:** `npm run verify` 418 tests / 39 files green, build clean. **Unverified:** board/cockpit pixels at 375px (no dev-server curl) — eyeball per RUNNING.md.
**PR:** [#71](https://github.com/mobiustripper42/muster/pull/71)
**Points:** 3
**Branch:** task/4.7-fills-by-deadline
**Opened at:** 2026-06-12T05:05:00Z

## Task 2: Pull-seeds — DEC-S026 throughput retro (#74)

**Completed:**
- `/pull-seeds` of seeds `main` (DEC-S026): `.claude/skills/retro/SKILL.md` rewritten to the **throughput** velocity model (points/calendar-week from GitHub issue `closedAt` + `points:N` labels + estimate-calibration tally; no transcript active-time math). Pulled the standalone extractor `.claude/scripts/throughput.py`, plus `docs/VELOCITY_AND_POKER_GUIDE.md` + `docs/CHEATSHEET.md` rewrites and the stale `/retro` cell in `CLAUDE.md`. Added `.gitignore` `__pycache__/`/`*.pyc` guard.
- **First attempt scrapped + re-pulled:** the initial pull shipped a pre-fix `throughput.py` (didn't count `points:1`; traceback on non-int `--window`). Handed the two fixes to the user → fixed upstream in seeds (`d07246d`) → re-pulled clean on `task/pull-seeds-2026-06-12b`. A stray `__pycache__/*.pyc` from attempt 1 got committed; now gitignored.
- Declined (not pulled): `.claude/settings.json` (security policy → `/update-config`), `.claude/scripts/dec_s_sweep.py` (one-shot DEC migration).

**Code review:** Skipped a redundant pass — `throughput.py` reviewed in the scrapped attempt (2 findings, both fixed upstream + verified). Rest is verbatim seeds docs/skill + trivial edits.
**Verified:** `throughput.py --self-test` green (26 assertions). No `.pyc` staged. No TS/Next code touched (verify N/A).
**PR:** [#74](https://github.com/mobiustripper42/muster/pull/74)
**Points:** 2
**Branch:** task/pull-seeds-2026-06-12b
**Opened at:** 2026-06-12T14:45:00Z

**Loose end:** stale local + remote branch `task/pull-seeds-2026-06-12` (junk `.pyc` commit, no PR) — sandbox blocked deletion; delete manually.

## Task 3: Vessel-local time (Phase 5.3, #77, DEC-032)

**Completed (full DEC; @architect design + Explore audit up front):**
- **First Phase 5 task.** Times stored as vessel-local wall-clock, interpreted + rendered in the **vessel** tz (`TENANT_TIMEZONE` = `process.env.TENANT_TZ ?? "America/New_York"`, env-overridable), **never the viewer's** (operator confirmed: BrewBoat is **Eastern**, not the placeholder Seattle; vessel-local for all). The real bug was time math vs `now` (eventStart minted wall-clock as UTC → hoursToTrip/fills-by/horizon/bail-lateness off by the offset).
- New `src/config/tenant.ts` — `TENANT_TIMEZONE` + `Intl` helpers (`zonedWallClockToInstant` **two-pass DST-correct**, `tzOffsetMs`, `vesselDateOf`). No new dep, no DDL.
- One mint seam (`eventStart`) + `tz` threaded (default the constant) through derive chain + read-models + `tick` + `bailWithDerivedLateness` (like `leadDays`). Renderers format in tenant tz; raw `event.time` strings unchanged; 5 date-only `fmtDate` labels pinned UTC. Credential gate → date-only **ISO-string** comparison (tz-invariant, no day-shift).
- Tests pin `tz: "UTC"` for engine fixtures + new **EDT/EST + spring-forward** DST block. Seeds store Eastern wall-clock (sensible times, exact hoursToTrip); dock Seattle→NY placeholder. DEC-032 finalized; E2E-doc tz note updated.

**Code review:** @code-review — 1 real bug (one-pass DST mis-mint on spring-forward AM) **fixed** via two-pass + test; 5 date-label pins fixed; form-shifts default-only noted. Offset signs / threading / DST tests verified clean.
**Verified:** `npm run verify` 423 tests green, build clean. Live-PG seed+board dry-run: Eastern times, exact hoursToTrip.
**PR:** [#79](https://github.com/mobiustripper42/muster/pull/79)
**Points:** 5
**Branch:** task/5.3-vessel-local-time
**Opened at:** 2026-06-13T00:00:00Z

**Heads-up:** seed dock is a placeholder ("Pier 11, East River, New York") — correct to BrewBoat's real dock when known. `TENANT_TZ` env-overridable.

## Task 4: Hosted-deploy plumbing (Phase 5.1, #75, DEC-033)

**Completed (buildable half of #75; deploy itself is owner-action):**
- **Provider pick: Vercel Postgres (Neon-backed)** — operator chose "everything on Vercel"; free at pilot scale, plain Postgres behind the port. DEC-033 (was OPEN) now decided.
- `app/api/cron/tick/route.ts` — the DEC-023 tick trigger as a Vercel Cron route (nodejs runtime, CRON_SECRET Bearer, `tick`→`forwardToOutbox`). Cron design defended vs Claude-chat pushback: state is derived **lazily on read** already; the cron drives only the irreducible outbound sends (firing asks), which "no babysitting" requires an autonomous trigger for.
- `vercel.json` (buildCommand `next build --webpack` + cron `*/15`), `app/lib/repo.ts` serverless pool tuning, `docs/DEPLOY.md` go-live runbook (provision→secrets→migrate-via-UNPOOLED→deploy→smoke).

**Code review:** @code-review — clean bill, zero blocking. Auth fail-closed, no double-send on overlap, pool sane. Folded 3 ops fixes: smoke-check sources `.env.local`; runbook flags cron's APP_BASE_URL dependency + the Pro-plan cron-cadence requirement.
**Verified:** `npm run verify` 401 tests green, `/api/cron/tick` registers dynamic. **Unverified (owner-action):** the live deploy — provision/secrets/migrate/deploy can't run from here.
**PR:** [#80](https://github.com/mobiustripper42/muster/pull/80)
**Points:** 5
**Branch:** task/5.1-hosted-deploy
**Opened at:** 2026-06-13T01:30:00Z

**Next on 5.1:** merge #80 → cut `production` off `main` (DEC-S022 adoption, now deploy-ready) → operator provisions Neon + deploys per DEPLOY.md. Then 5.2 auth (#76), 5.4 import (#73).

**🚀 SESSION MILESTONE — Muster is LIVE (first hosted deploy).** `https://muster-sigma.vercel.app` on
Vercel + Neon Postgres. `/api/health` ok (db reachable, integrity clean), `/api/cron/tick`
authenticates + ticks (200; all-zero counters = empty prod DB). `production` branch cut off main
(DEC-S022 adopted); Vercel Production Branch set to `production`. Schema migrated to the Neon prod DB.

**Next Steps:**
- **Phase 5 in flight: 5.3 (#77) + 5.1 (#75) DONE and the slice is deployed.** Remaining gating:
  **5.2 auth (#76)** — prod operator sign-in (dev-link 404s in prod; needs the mint path), and
  **5.4 import (#73)** — the Xola import surface (prod DB is empty; needs data to demo asks firing).
  Then 5.R runbook, 5.5 e2e.
- **Deploy gotchas hit + fixed in DEPLOY.md** (next deployer beware): `vercel env pull` writes a FILE
  not the shell (source it); Neon vars are **Sensitive** so `env pull` returns them empty — grab the
  direct/unpooled connection string from the dashboard for migrations; DB vars are Prod+Preview
  scoped (pull `--environment=production`). Production-Branch setting moved to **Settings →
  Environments → Production**.
- **#75 closed.** Phase 4 long since done. Phase-5 retro is later (after 5.2/5.4/etc.).
- The DEC-027 header line still reads "'fills by' deferred to the fill-deadline decision" — historical, now superseded by DEC-031. Left as-is (DEC entries are append-only record); a doc-consistency pass may flag it.
- The DEC-027 header line still reads "'fills by' deferred to the fill-deadline decision" — historical, now superseded by DEC-031. Left as-is (DEC entries are append-only record); a doc-consistency pass may flag it.

**Context:**
- DEC-031's constant binding is the load-bearing invariant: if anyone later wants a *different* "fills by" lead from the escalation threshold, that's a real DEC change (split the constant) — not a tweak. The whole point is they're the same instant.
- `fillDeadlineFor(shift, allEvents)` is exported + tested but has no production caller (parity with `staffingHorizonFor`). Kept deliberately; not dead-code-by-accident.
