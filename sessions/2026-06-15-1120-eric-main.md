---
session: 15
dev: eric
slug: main
branch: main
started: 2026-06-15T11:20:52Z
ended:
points:
pr_numbers: [82, 83]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/e0ea3134-85d1-4c4f-96d0-4f39868636c2.jsonl
---

# Session 15 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Production auth path — operator magic-link mint (5.2, #76, DEC-034)

**Completed:**
- `db/mint-link.ts` (new) + `db:mint` script — mints an operator (`--admin=<handle>`) or crew (`--crew=<id>`) single-use magic link against a live DB, run out-of-band exactly like `db:migrate`. Reuses the tested core (`issueMagicLink`/`randomSecret`); **requires `APP_BASE_URL`** (a CLI has no Host header → no host-spoofable links); 60-min default TTL (`--ttl-min` override); `try/finally` repo close like `db/tick-dev.ts`. No new test — ops tooling, core already covered.
- `/crew/dev-link`'s prod 404 untouched; crew still flow via the DEC-030 relay.
- `docs/DEPLOY.md` step 7 (operator sign-in); DEC-034 flipped Proposed→Accepted with build specifics (TTL, APP_BASE_URL refusal, no-test rationale).
- `.gitignore`: ignore `.env*`, exempt tracked `.env.example`.

**Code review:** @code-review — solid, consistent with sibling `db/` scripts, security guard confirmed correct. 1 actionable finding (`.env*` shadowed tracked `.env.example`) **fixed** via `!.env.example`. 1 non-blocking note (lenient `Number()` on `--ttl-min`) left as-is for an operator tool.
**Verified:** `npm run verify` green (400 tests, build clean); all 4 script validation paths exit correctly; full mint dry-run against local PG persisted admin (60m) + crew (15m) tokens with correct TTLs + well-formed URLs (test tokens cleaned up); `.env.example` exemption confirmed. **Unverified (owner-action):** the live prod mint → sign-in → `/admin/at-risk` (eyeball per DEPLOY.md step 7).
**PR:** [#82](https://github.com/mobiustripper42/muster/pull/82)
**Points:** 3
**Branch:** task/5.2-prod-auth-mint
**Opened at:** 2026-06-15T15:44:33Z

## Task 2: Operator xlsx import surface — seam split + Vercel-safe reader (5.4a, #73, DEC-037)

**Completed (architect-split #73 → 5.4a now / 5.4b fast-follow; @architect on Opus, Fable down):**
- **Seam split** (`src/import/import-reservations.ts`): `decodeXlsxRows` (Land) + `importRecords` (Map/Reconcile) on a `RawReservationRecord` intermediate (date/time normalized before the seam — the 5.4b API adapter reuses it). `importReservations` is now the composed wrapper; behavior-preserving.
- **Ghost-shift fix:** `importRecords` derives `event.status` from booked-ness (all-cancelled event → `cancelled` → `formShifts` cancels its shift). Nothing in the codebase ever set event.status before — a re-import that cancelled out an event left a live shift (crew asked for a dead trip). Architect landmine #1, fixed + tested.
- **Vercel-safe xlsx reader** (`src/import/xlsx-extract.ts`): the `unzip` shell-out would ENOENT on Vercel → rewritten pure-Node (`node:zlib` + minimal ZIP central-dir parse, no binary/no dep), buffer-based. Upload security: magic-byte check, 5MB cap, per-entry decompress cap (zip-bomb), Reservations-sheet only, no formula eval. `importXlsx` → `readXlsxSheetFile`.
- **`/admin/import`** (new): admin-gated single-step upload → import → `formShifts` → board live. DEC-026 count-only redirect feedback (skipped product *names* → server logs; no operator product-map UI). `next.config` `serverActions.bodySizeLimit: 6mb`. Linked from admin home.
- **DEC-037** records the split + every build decision (single-step, Vercel-safe reader, event-status fix, capacity-stomp noted-not-fixed).

**Code review:** @code-review — no blockers; ZIP parser memory-safe (subarray clamps, no OOB, no 500s). Fixed: malformed-input errors were misclassified `unavailable`→ now classified by source (parse vs repo) + local-header bounds guard; upload-derived `rid` escaped before RegExp.
**Verified:** `npm run verify` green (430 tests, +8; build clean). Real Xola export end-to-end (in-memory smoke): 82 events / 98 reservations / 49 shifts / 87 seats, 1 trial ignored. **Unverified (no dev-server curl):** the rendered `/admin/import` at 375px + browser upload→redirect flow — eyeball steps in the PR.
**PR:** [#83](https://github.com/mobiustripper42/muster/pull/83)
**Points:** 8
**Branch:** task/5.4a-xlsx-import-surface
**Opened at:** 2026-06-17T13:18:09Z

**Next Steps:**
- **5.4b** (Xola API Land adapter) is the fast-follow — creds in hand. Port `fetchOrders`/`fetchEvents` into strict TS at the Next edge → same `importRecords`; confirm DEC-036's two field shapes (`organizer.phone` populated? partySize source) against a live response. Open a tracking issue.
- **Crew-touch idea** (Eric, 2026-06-17): periodic "didn't need you this week, stay warm" message to crew on light weeks — keeps roster engagement + reliability signal alive; fits the existing cron. Park in `docs/FUTURE_IDEAS.md` (standalone, DEC-014). NOT built.
- Pilot sequencing: 5.4a unblocks the demo-seed shakedown → real import → crew test.

**Context:**
