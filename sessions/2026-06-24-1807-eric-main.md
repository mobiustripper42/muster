---
session: 24
dev: eric
slug: main
branch: main
started: 2026-06-24T18:07:45Z
ended: 2026-06-25T07:43:15Z
points: 7
pr_numbers: [134, 136]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/006881da-d238-4f92-9ead-5cecf343427b.jsonl
---

# Session 24 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 6.1 — Message store (#111, opens Phase 6)

**Completed:**
- New framework-free `src/messaging/` substrate for full-mesh in-app chat (cohort/shift/all-staff/DM): `entities.ts` (Thread/Participant/Message + deterministic `standingThreadId`/`dmThreadId` helpers — DM id from the sorted crew pair so a re-tap reuses the one thread), `membership.ts` (pure `deriveMembers` dispatched via a `Record<ThreadKind, resolver>` registry — kind is data, not a branch, DEC-051/DEC-ROLE-1), barrel + unit tests.
- **Membership derived at read** for the three standing kinds, persisted only for DMs (DEC-051): **cohort = day-wide across vessels**, shift = the shift's seats, all-staff = roster, dm = participant rows. A `membership.test.ts` "reassign a seat, the cohort follows" case proves the derived-not-snapshotted guarantee (DEC-009 spirit).
- `db/migrations/0008_messaging.sql` — `threads`/`thread_participants`(DM-only)/`messages`; house style (text PK, ISO-text dates, no FK — DEC-DATA-1), additive. Port + both adapters + shared contract suite extended; `+3` branded ids (`ThreadId`/`ParticipantId`/`MessageId`).
- **DEC-051 corrected**: original "cohort = vessel+day grouping" wording collapsed cohort into shift; patched to the artifact §2 reading (day-wide across vessels) with a dated note. The #111 AC copied the same wrong wording — flagged in the DEC. Operator confirmed the day-wide reading.

**Code review:** `@code-review` — clean bill of health, no bugs or security issues. Three forward-looking notes, none blocking: (1) message tie-break assumes ASCII-comparable ids (`collate "C"` only if ids go non-ASCII); (2) `all_staff` resolver returns the roster as given — the future read surface must pass a tenant-scoped roster + add a cross-tenant isolation test; (3) optional `messages(thread_id, created_at, id)` composite index once threads get long.

**Verification:** `npm run verify` green (482 passed, 1 skipped). Real-Postgres contract exercised separately (`db:up` → `test:pg`, migration `0008` applied): 31 passed. Eyeball migrate command dry-run: prints `Applied: 0008_messaging.sql`.

**PR:** [#134](https://github.com/mobiustripper42/muster/pull/134)
**Points:** 5
**Branch:** task/111-message-store
**Opened at:** 2026-06-24T18:55:38Z

## Task 2: Preview magic-link signin — gate dev-link on VERCEL_ENV (#135, DEC-057)

**Completed:**
- `/crew/dev-link` (the crew `?crew=` + admin `?admin=` magic-link minter) was gated on `NODE_ENV === "production"`; Vercel sets `NODE_ENV=production` on **preview** deploys too, so it 404'd previews — no crew/admin signin to smoke-test a preview before promoting. Now gated host-agnostically: `VERCEL_ENV === "production" || (!VERCEL_ENV && NODE_ENV === "production")` — hard-404 on Vercel prod **and** self-hosted prod, live on previews + local (`app/(crew)/crew/dev-link/route.ts`).
- DEC-057 logged: posture + two-leg containment (isolated Neon preview branch DB + `SESSION_SECRET`-required-or-mint-throws via `auth.ts`). Pairs with a Vercel step (operator's): `SESSION_SECRET` on Preview, `APP_BASE_URL` scoped Production-only so preview links resolve to the preview host.

**Code review:** `@code-review` — one real finding, **fixed in-PR**: the initial `VERCEL_ENV`-only gate was fail-open on a non-Vercel prod (`next start`/Docker, `VERCEL_ENV` absent); added the `NODE_ENV` fallback so the prod-404 is host-agnostic. Added the `SESSION_SECRET` containment leg to DEC-057 per review. XSS/host-spoof surfaces already handled.

**Verification:** `npm run verify` green (471 passed, 1 skipped). The gate flip only changes Vercel-**preview** behavior (local is live before+after), so the behavioral test is the preview signin in the PR's AC — can't be run from here. Gate truth table (4 envs) in the PR body.

**PR:** [#136](https://github.com/mobiustripper42/muster/pull/136) — closes #135
**Points:** 2
**Branch:** task/preview-dev-link-gate
**Opened at:** 2026-06-25T06:43:01Z

**Next Steps:**
- **Deploy issue #138 (open, do first):** git `main` deployments hit the **prod Neon DB** (not an isolated branch) — Vercel-Neon integration treats repo-default `main` as production, vs Vercel Production Branch = `production`. Fix: point integration production-branch at `production` (Vercel → Storage → Neon settings), or disable `main` preview deploys. **Resume with dashboard screenshots** (CC Desktop) to locate the setting. Tabled mid-debug.
- **Interim lockdown asked of operator:** remove `SESSION_SECRET` from the Vercel **Preview** env scope to close the dev-link→prod mint path while #138 is open (re-add to restore preview signin).
- **Confirm prod engine pause/resume state** — the preview engine-pause toggle wrote the prod `app_settings` while `main`/preview shared the prod DB; verify it's set to what you actually want.
- **Next Phase 6 build:** #112 Presence / `PresencePort` (6.2) — stacks on the #134 message store.
- **Open PRs:** #137 (admin-roles idea-park) to merge whenever; #136 merged.

**Context:**
- **Per-deployment Neon branching is ON.** PR/task branches get isolated DB branches (verified: #136 preview was a separate DB, crew signin worked). Only git `main` still routes to prod (#138). "main (default)" in the Neon dashboard = Neon's own root branch (= prod DB), NOT git main — a naming collision.
- **dev-link is now live on previews** (DEC-057, #136 merged): gated `VERCEL_ENV==='production' || (!VERCEL_ENV && NODE_ENV==='production')`. Needs `SESSION_SECRET` on Preview + `APP_BASE_URL` scoped Production-only.
- **Crew dev-link needs REAL pilot ids** — `crew-<firstname>-<lastname>` (e.g. `crew-paul-learman`), not the local-seed `crew-quint`. Admin signin uses any handle (`?admin=spink`) since admin = any `kind:"admin"` session — no admin entity/roles yet (idea parked HIGH, #137).
- **Prod migrate is manual** (Vercel never runs migrations): `DATABASE_URL=<direct-unpooled> npm run db:migrate`. 0008 (message store) applied to local; prod still needs it before any reader ships.
