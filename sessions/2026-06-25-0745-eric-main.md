---
session: 25
dev: eric
slug: main
branch: main
started: 2026-06-25T07:45:29Z
ended:
points:
pr_numbers: [139]
status: open
transcript: C:\Users\eric\.claude\projects\C--Users-eric-OneDrive-Documents-GitHub-muster\ecfd13c2-9567-4eef-92c7-3c7510cf7a87.jsonl
---

# Session 25 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Disable main-branch Vercel deploys (#138)

**Completed:**
- Root-caused #138 across this session's back-and-forth: `main` is a Vercel *preview*-target deploy (Production Branch = `production`), but the Vercel-Neon native integration maps the **git default branch** (`main`, confirmed via `gh repo view`) to the **production** Neon branch — so `main` pushes ran against prod data while PR/task branches correctly got isolated branches. The asymmetry (PR previews isolated, `main` not) was the tell. No clean dashboard lever to remap without changing the git default branch.
- Fix = stop `main` deploying at all: `vercel.json` `git.deploymentEnabled: { "main": false }` (3-line add). No deploy → `main` can't reach any DB. `production` deploy + PR-branch previews intentionally untouched.
- Considered & deferred A′ (disable previews entirely): operator wants to watch the next Vercel bill first before deciding whether preview branch churn justifies turning them off. A keeps the on-demand phone/magic-link smoke-test surface.

**Code review:** `@code-review` — clean. Verified `git.deploymentEnabled` schema, confirmed `main`-only scope (`production`/previews provably unaffected), `crons`/`buildCommand` intact. Forward note: crons run on the Production deployment, not `main` — confirm `/api/cron/tick` still fires post-merge (in the PR eyeball list).

**Verification:** `vercel.json` validated as well-formed JSON. `npm run verify` deliberately skipped — config-only change; `next build`/typecheck/Vitest don't read `vercel.json`, CI is the backstop. Behavioral proof is the post-merge eyeball (no `main` deployment in Vercel → Deployments).
**PR:** [#139](https://github.com/mobiustripper42/muster/pull/139) — closes #138
**Points:** 1
**Branch:** task/138-disable-main-deploy
**Opened at:** 2026-06-25T08:12:32Z

**Next Steps:**
- **Merge #139**, then eyeball: next `main` push shows no Vercel deployment (#138 dead); a `task/*` push still previews; `/api/cron/tick` still fires. If Vercel ignores the committed `deploymentEnabled` (reads project settings instead), flip it in the dashboard (Settings → Git).
- **Watch the next Vercel bill** — A keeps PR previews on, so Neon branch churn continues. If it stings, pull the A′ lever (disable previews entirely; on-demand smoke tests via Vercel CLI).
- **Carried from Session 24:** prod migrate of `0008_messaging.sql` is manual and **not yet applied to prod** — must run before any message reader ships. Confirm prod engine pause/resume state. Close stranded issue #111 (PR #134 merged but lacked `closes #111`). Merge idea-park PR #137 whenever.
- **Next Phase 6 build:** #112 Presence / `PresencePort` (6.2) — stacks on the merged message store.

**Context:**
- #138 mechanism: two independent "which branch is production" decisions disagree — Vercel deploy-target (= `production`, correct) vs the Neon integration's DB pick (keys off git default = `main`). That's why `main` was a preview hitting prod.
- A vs A′: A (this PR) = `main` off, PR previews stay. A′ = all previews off, `production`-only. Operator deferred A′ pending the Vercel bill.
- Repo default branch is `main` and stays `main` (changing it has PR-base/tooling blast radius — explicitly avoided per #138).

**Context (carried, Session 24):**
- Per-deployment Neon branching is ON; PR/task branches get isolated DBs (verified #136). "main (default)" in Neon console = Neon's root branch = prod DB, NOT git main (naming collision).
- Crew dev-link needs real pilot ids (`crew-paul-learman`), not local-seed `crew-quint`. Admin signin takes any handle.
- Prod migrate is manual (Vercel never runs migrations): `DATABASE_URL=<direct-unpooled> npm run db:migrate`.
