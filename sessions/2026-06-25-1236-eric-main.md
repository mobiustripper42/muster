---
session: 26
dev: eric
slug: main
branch: main
started: 2026-06-25T12:36:40Z
ended:
points:
pr_numbers: [140, 142, 143]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/57f73278-68b3-454d-851e-713a00757518.jsonl
---

# Session 26 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Preview/local environment tell (4px top line)

**Completed:**
- `app/layout.tsx` — server-rendered env indicator: 4px line pinned to the top, **red** local (`NODE_ENV==='development'`), **yellow** preview (`VERCEL_ENV==='preview'`), none in prod. No client JS; keys off the same `VERCEL_ENV` as the dev-link gate (DEC-057). A standing visual guard after the #138 main→prod-DB scare (fixed in #139).
- Added `pointer-events-none` per review so the bar can't intercept clicks.

**Verification:** `npm run build` clean (16 routes); `typecheck:app` clean. Dry-ran locally — `next dev` + fetched `/` → HTML has `bg-red-600`, not `bg-yellow-400` (correct local case). Yellow only confirmable on a real preview deploy.

**Code review:** `@code-review` — clean. Confirmed mutual exclusivity (Vercel forces `NODE_ENV=production` on preview+prod → red is local-only; `VERCEL_ENV` separates preview/prod), server-component env read needs no `NEXT_PUBLIC_`. One precautionary `pointer-events-none` applied.
**PR:** [#140](https://github.com/mobiustripper42/muster/pull/140)
**Points:** 1
**Branch:** task/preview-env-banner
**Opened at:** 2026-06-25T17:52:44Z

## Task 2: 6.1b — canonical messaging Subject model (DEC-058)

**Completed:**
- Split out of #112 per @architect: #112 was secretly two things (subject identity + presence). This lands the identity model; 6.2 stacks on it. Issue #141, closes via PR #142.
- `src/domain/entities.ts` — canonical `Subject { kind: AuthSubjectKind; id }`, the one identity for everyone addressable (crew + operator/office; future customer = one-line widen).
- `src/auth/magic-link.ts` — `AuthSubject` is now an alias of `Subject` (one type, not two — the architect's BLOCKER: don't mint a second identity vocab).
- `src/messaging/entities.ts` — `MessageSenderKind = AuthSubjectKind`; `"operator"`→`"admin"`; docs.
- `src/adapters/repository-contract.ts` — operator message round-trips as `senderKind: "admin"`, `senderId: "crew-spink"` (DEC-030 §7 convention).
- `app/lib/operator.ts` — DEC-058 pointer (operator posts/rings as crew-spink + "admin"; no operator entity minted — that's the parked DEC-020 revision).
- `docs/DECISIONS.md` — **DEC-058**: canonical subject; **doorbell rings on thread membership, NOT DEC-052 read-visibility** (binds 6.4); customer = type/schema-free but resolution surfaces cost bounded edits later.

**Verification:** typecheck core+app clean (the load-bearing check — convergence is type-enforced); `vitest` messaging/contract/auth = 59 passed; `npm run build` clean. pg contract self-skips without docker (opaque-text value, low parity risk).

**Code review:** `@code-review` — clean. Alias breaks no consumer; no `"operator"` runtime value missed; no stored-data concern (`sender_kind text`, no CHECK, no seed/prod rows). One cosmetic finding (stale 0008 column comment) recorded in DEC-058 rather than hand-patching the applied migration.
**PR:** [#142](https://github.com/mobiustripper42/muster/pull/142)
**Points:** 2
**Branch:** task/6.1b-subject-model
**Opened at:** 2026-06-25T18:08:09Z

## Task 3: 6.2 — PresencePort (coarse activity signal, #112)

**Completed:**
- Built off main (after #142 merged), keyed on the DEC-058 canonical `Subject`. Closes #112.
- `src/ports/presence.ts` — `PresencePort` (`recordActivity` write / `lastActiveFor` read-only seam); observed-only (DEC-046), no realtime vendor / realtime-swap-as-later-adapter (DEC-047).
- `src/adapters/{in-memory,postgres}-presence.ts` + `presence-contract.ts` + two adapter test files — one shared contract, both adapters (the #111 parity guard). pg `lastActiveFor` is a batched `unnest` join on the composite key.
- `src/domain/subject.ts` — `subjectKey` (`${kind}:${id}`, Map-key only; DB keys two-column).
- `src/messaging/presence.ts` + test — pure `isPresent`, fail-toward-ringing, window as a param (value deferred to 6.3 spike).
- `db/migrations/0009_presence.sql` — composite-PK `(subject_kind, subject_id)` store.
- Wired `getPresence()` (`app/lib/repo.ts`); exported presence from messaging barrel; broadened `test:pg`.
- Capture call-sites deferred to 6.6/6.7 (substrate-first, like #111).

**Verification:** typecheck core+app clean; `npm run test:pg` against a real compose Postgres = **37 passed** (Repository 30 + new PresencePort 7) — exercised 0009, composite-PK upsert, the unnest read, same-id-across-kinds on real pg; in-memory + pure tests green; `npm run build` clean. Brought up the compose DB to run this (left running — standard dev DB).

**Code review:** `@code-review` — clean. unnest read correct/injection-safe/duplicate-tolerant; in-memory↔pg parity holds; isPresent fail-toward-ringing is the safe direction; composite PK needs no extra index. Two nitpicks applied: dropped `PostgresPresence.close()`/`fromConnectionString` (close() would end the SHARED app pool — footgun) and aligned the 0009 kind-ordering comment.
**PR:** [#143](https://github.com/mobiustripper42/muster/pull/143)
**Points:** 3
**Branch:** task/112-presence-port
**Opened at:** 2026-06-25T18:29:40Z

**Next Steps:**
- **Prod migrate** before any doorbell *reads* in prod: `0008_messaging.sql` AND `0009_presence.sql` both still unapplied to prod (manual: `DATABASE_URL=<direct-unpooled> npm run db:migrate`). 6.2/6.1b ship no reader, so not yet blocking.
- **Next Phase 6 build:** #113 notification-interval spike (6.3, 2pt) → feeds the doorbell window value isPresent takes as a param; or #114 the doorbell decider (6.4, 8pt) — DEC-058 bound it to ring on thread *membership*, not DEC-052 read-visibility.
- Merge #143 (+ #142 if not yet). Capture call-sites for presence land with the messaging UI (6.7) / doorbell tick (6.6). — cut task/112-* off main (message store from #111/PR #134 is already merged to main; no stacking needed).

**Context:**
- Picked up on the Linux box (mill-dev). Session 25 was a Windows/OneDrive window left open; closed retroactively this session (PR #139, disable main-branch Vercel deploys, merged).
- Carried blocker: prod migrate of `0008_messaging.sql` is manual and NOT yet applied to prod — must run before any message *reader* ships. PresencePort is behind its own port, likely no prod-migrate dependency to start.
- Issue #111 (6.1 message store) is functionally done (PR #134 merged) but still open — PR lacked `closes #111`. Close it when convenient.
- Stray branch `claude/muster-next-phase-dfupeo` (4 Phase-6 planning commits, no PR) is superseded by PR #126; safe to delete.
