---
session: 27
dev: eric
slug: main
branch: main
started: 2026-06-25T19:08:01Z
ended:
points:
pr_numbers: [144, 146]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/fc274025-1db8-4379-89a1-39a8e9194230.jsonl
---

# Session 27 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 6.3 — doorbell window defaults (DEC-060, closes #113)

**Completed:**
- Researched + ratified two doorbell timing windows (cited: Slack push/idle defaults, Discord idle, SMS ~90s response cadence, debounce-to-digest norms). Eric ratified 90 s / 5 min.
- `src/config/tenant.ts` — `DOORBELL_BATCH_WINDOW_MS` (90 s, §7.2 hold/cancel) + `DOORBELL_PRESENCE_WINDOW_MS` (5 min, §7.1 `isPresent` staleness), both env-overridable via an `envMs()` helper (falls back on non-numeric/non-positive).
- `src/config/tenant.test.ts` — defaults, presence>batch invariant, env-tunability AC (6 tests).
- `docs/messaging-smart-doorbell.md` — §7.2/§14 "~1 min" placeholder resolved.
- `docs/DECISIONS.md` — **DEC-060** (decision + cited rationale + tradeoff); DEC-TBD doorbell bullet marked resolved.
- Substrate-only — no consumer wired (6.4 imports these). Posted the cross-field-invariant enforcement handoff to #114.

**Verification:** `vitest` tenant test 6/6; `typecheck` core + app clean; `npm run build` clean (16 routes).

**Code review:** `@code-review` — clean. Two low-severity notes, both addressed: env integer-only caveat (`90_000`→NaN trap) documented; presence>batch cross-field invariant handed to the 6.4 decider (#114) since `envMs` validates each value in isolation.
**PR:** [#144](https://github.com/mobiustripper42/muster/pull/144) → base `feature/messaging` (DEC-059)
**Points:** 2
**Branch:** task/113-notification-interval
**Opened at:** 2026-06-25T20:09:41Z

## Task 2: Auto-confirm a winning "in" — pilot fix (DEC-061)

**Completed:**
- Operator-reported: crew "in" landed `Claimed` (tentative); the only path to `Confirmed` was the per-shift cockpit confirm button, nowhere near triage. Chose auto-confirm over adding an approve button.
- `src/asks/ask-loop.ts` — new core composition `recordResponseAndConfirm`: unchanged `recordResponse` then, **gated on `outcome.claimed === true` (never seat state)**, unchanged `confirmSeat`. A winning accept → `Asked → Claimed → Confirmed` in one step; contested-loser/double-book/decline pass through unconfirmed.
- Both surfaces routed through it: crew `respondToAsk` (`app/(crew)/crew/actions.ts`) + operator-as-crew `recordResponseAs` (`src/asks/answer-as.ts`, ownership gate kept in front). Applies to both protocols (DEC-007).
- M4 breadcrumbs: `ports/channel.ts` + `adapters/{web-link,fake}-channel.ts` comments repointed at the composition (the inbound SMS-reply adapter must use it, not raw `recordResponse`).
- Docs: **DEC-061** (DECISIONS.md); SPEC §2.4/§2.6 supersession notes. "In" now means committed — retraction is a penalized `bail()` (operator-confirmed semantics).
- Filed adjacent pre-existing bug **#145** (`recordResponse` re-logs `ask_accepted` on a re-tap).

**Verification:** full `vitest` **535/535** pass (5 new ask-loop cases + 1 updated answer-as); `typecheck` core + app clean; `npm run build` clean.

**Code review:** `@architect` proceed-with-changes (all required changes folded in — the `claimed`-gate is the load-bearing one); `@code-review` clean, no regressions (claimed-gate enforced twice over; every `Claimed` consumer tolerates the momentary state). One jsdoc fix applied; the redundant-but-honest `getAsk` re-read left by design.
**PR:** [#146](https://github.com/mobiustripper42/muster/pull/146) → base `main` (independently-shippable pilot fix, DEC-059)
**Points:** 3
**Branch:** task/auto-confirm-in
**Opened at:** 2026-06-25T22:31:51Z

**Next Steps:**
- **Phase 6 resumes on `feature/messaging`:** #114 the doorbell decider (6.4, 8pt) — now unblocked by 6.3 (#113/PR #144); it owns the presence>batch cross-field invariant (handoff posted to #114). Then #115 doorbell harness ★, #116 tick/delivery.
- Merge order is free: PR #144 → `feature/messaging`; PRs #146 → `main`. DEC-060 reaches `main` when `feature/messaging` merges (expected 060-gap on `main` meanwhile).
- Bug #145 (re-tap double-log of `ask_accepted`) open — small, slot into a Phase 6 or pilot window.

**Context:**
- Pilot-driven work landed straight on `main` (DEC-059 independently-shippable path), separate from Phase 6 messaging on `feature/messaging`.
- Operator applies prod migrations out-of-band — 0008/0009 already on prod (corrected a stale Session-26 Next-Step). Promoted `main`→`production` untagged at the session start (no bump, per operator).
- The cockpit `confirmInto` action was *already* unwired (no UI caller) before DEC-061 — its "vestigial backstop" framing is moot; candidate for a future cleanup pass.

**Context:**
