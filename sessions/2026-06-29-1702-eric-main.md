---
session: 32
dev: eric
slug: main
branch: main
started: 2026-06-29T17:02:21Z
ended: 2026-06-30T00:50:47Z
points: 9
pr_numbers: [190, 191, 192, 193]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/f077e668-c325-4098-a1c1-97e5ec689a6a.jsonl
---

# Session 32 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Crew front door — 6-digit email code sign-in + sign-out (7.0a)

**Completed:**
- **Code-login as a new primitive (DEC-081), not magic-link.** `src/auth/login-code.ts` (request/verify, clockless/injected) on its own `login_codes` table — subject-keyed (a 6-digit code isn't unique, so it can't ride `magic_tokens`' unique hash), attempt-capped (5) / 10-min TTL / 60s cooldown, hash-only. Port + both adapters + contract tests; entity `LoginCode`.
- **Email-entry flow** (not phone — email is universal, phone isn't/Henry): signed-out `/crew` → email → code → session. No-client-JS server actions + httpOnly pending-email cookie (`app/lib/login-cookie.ts`); no-enumeration generic response. **Sign-out** wired to `endSession()`, ships **live**.
- **Flag-gated** `CREW_SELF_SERVE` OFF in prod (DEC-059 — `main` promotable; 7.0a uses `FakeChannel` + a dev-only `/crew/dev-code` echo, 404-in-prod). `crew_members.email` left **nullable** (dropped NOT NULL — broke the contract's optional-email shape; flow safe without it).
- Migration `0012_code_login.sql`; `login_codes` classified in `reset-pilot`. Docs: DEC-081 + SPEC §3.2. Tracker: #180→7.0a (pts 5→3), **#189** filed for 7.0b (real email).
- Tests: `verify` 708 ✓, `test:pg` 52 ✓ (login-code SQL/CAS/concurrent-race on real PG), e2e 6 ✓ desktop+375px (form, enumeration parity, wrong-code, sign-out, **real code→session round-trip**).

**Code review:** `@code-review` — solid, nothing blocks shipping behind the flag. Fixed: un-env-gated plaintext-code `console.log` (prod-log leak risk), duplicated cookie literal, divergent dev-code gates. Carried to 7.0b (#189): don't `await` the email send on the hot path (timing oracle); re-mint resets the attempt cap (wants a daily re-mint cap).
**PR:** [#190](https://github.com/mobiustripper42/muster/pull/190)
**Points:** 3
**Branch:** task/7.0a-crew-front-door
**Opened at:** 2026-06-29T20:53:34Z

## Task 2: Real email delivery for the crew code login (7.0b)

**Completed:**
- **`EmailChannel implements ChannelPort`** (`src/adapters/email-channel.ts` + 4 unit tests) — Resend HTTP API over an **injected `fetch`** (no SDK, the Xola env-key+fetch precedent), subject-by-kind, throws on missing email / non-2xx. Green with no live send.
- **Wired into the login-code seam** (`app/lib/auth-delivery.ts`): `sendLoginCodeEmail` (real send, no-op + visible prod warning when `RESEND_API_KEY`/`EMAIL_FROM` unset) + `echoLoginCodeForDev` (the 7.0a dev echo, kept synchronous). `app/(crew)/crew/actions.ts` schedules the send via **`after()`** (post-response) — a roster match returns as fast as a miss (the timing-oracle half of no-enumeration, the carried-forward hardening item).
- `.env.example`: `CREW_SELF_SERVE` / `RESEND_API_KEY` / `EMAIL_FROM`. No migration, **no new DEC** (DEC-081 covers it). Still dark in prod until the DKIM flip.
- Tests: `verify` 715 ✓, e2e 6/6 desktop ✓ (fake fallback intact, round-trip deterministic). UI unchanged → 375px unaffected.

**Code review:** `@code-review` — load-bearing props confirmed clean (`after()` off the hot path, prod gate never logs the code, single `res.text()` read, full Resend request contract tested). Fixed: half-config silent no-op now logs a visible prod skip-warning; real send wrapped in try/catch with a controlled log line (crewMemberId + reason, never code/body); 422 test asserts the provider detail.
**PR:** [#191](https://github.com/mobiustripper42/muster/pull/191)
**Points:** 2
**Branch:** task/7.0b-crew-email-delivery
**Opened at:** 2026-06-29T22:28:33Z

## Task 3: nativeRole + two-door eligibility (7.1)

**Completed:**
- **`nativeRole(crew)`** (`src/oracle/eligibility.ts`) — highest-precedence role via `ROLE_PRECEDENCE` (dual-rated → captain; sole role otherwise; null if undeterminable). DEC-076's acknowledged hardcoded-precedence wart.
- **New `src/oracle/claimable.ts` — `claimableSeatsFor(repo, crewId, now, windowDays=45)`** — the self-claim door: native-role-only **layered on** the §1.3 eligible-pool gate (`evaluateCandidate`), Open+required seats on Pending/Filling shifts in `[today, today+45d]`. Reuses the oracle's `committedDatesByCrew` (exported it) for the double-book check. Returns structural `ClaimableSeat`s for the 7.3 view.
- Operator-assign door untouched (full `ratings` via `isRatedFor`). Pure domain — no schema, no UI, **no new DEC** (DEC-076 exists).
- `src/config/tenant.ts`: added `addDays` next to `vesselDateOf` so the core doesn't depend on the import adapter.
- Tests: `claimable.test.ts` (13) — `nativeRole`, every filter, the §1.3 gate, window boundaries, and the **both-doors dual-rated** case. `verify` 724 ✓.

**Code review:** `@code-review` — eligibility logic correct. **Caught a real bug:** the claim window was computed in **UTC** not vessel-local, sliding the whole 45-day window forward a day in evening-Eastern hours. Fixed with `vesselDateOf`/`addDays` (DEC-032) + boundary + evening-`now` regression tests that lock it. Forward notes (7.2/7.3): the date-grained floor still lists an already-departed-today boat; `committedDatesByCrew` is a full scan (durable-adapter index later).
**PR:** [#192](https://github.com/mobiustripper42/muster/pull/192)
**Points:** 3
**Branch:** task/7.1-native-role-eligibility
**Opened at:** 2026-06-29T23:34:06Z

## Task 4: VersionTag blank in prod — read version from package.json (#193)

**Completed:**
- `next.config.ts`: `NEXT_PUBLIC_APP_VERSION` now reads `package.json` directly (`readFileSync`), not `process.env.npm_package_version`. Vercel's `next build --webpack` bypasses npm → the env var was undefined → the tag rendered nothing in prod. Proven: a Vercel-style build (`-u npm_package_version`) inlines `0.8.1` into the admin/crew page chunks.
- Shipped fast (no `/kill-this` ceremony). The "still can't see it" chase was a **browser cache** — hard refresh showed it. Code + deploy were both fine throughout.

**Code review:** none (small config fix).
**PR:** [#193](https://github.com/mobiustripper42/muster/pull/193) (merged)
**Points:** 1
**Branch:** task/version-env-fix

**Next Steps:**
- **Operator out-of-band (#189 close-out):** add `crew.brewcle.com` to Resend + DKIM/SPF DNS → verify → set `RESEND_API_KEY`/`EMAIL_FROM`/`CREW_SELF_SERVE=1` in Vercel → real-inbox eyeball. Front door is live once this lands.
- **7.0b hardening (deferred, on #189):** the re-mint cap (5-attempts-per-code isn't a per-target bound).
- **7.2 (#182)** claim service (`claimSeat`: Open→Confirmed, guarded) — the next build-order step; consumes `claimableSeatsFor`. Flag the departed-today-boat guard there.
- **7.3 (#183)** `/crew/open` surface — the first human-visible rendering of `ClaimableSeat`s.

**Context:**
- **Promoted main → production at v0.8.1 this session** — manual patch bump *outside* `/retro` (so `/retro` continues forward from 0.8.1 at Phase 7 close; reconcile per-PR versioning there). Two promotes: v0.8.1 (aef31ea), then again after #193 (f405637).
- **Crew self-serve is LIVE in prod but DARK** (`CREW_SELF_SERVE` unset). Migration `0012` applied to prod out-of-band. Only the #189 DKIM/env steps stand between dark and live.
- **Merge-train DEC collision** recurred: #188 took DEC-080 (Xola) on main while 7.0a used it locally → renumbered crew code-login to **DEC-081**. Stack DEC-touching PRs to avoid the repeat.
- VersionTag display is build-env-fragile — fixed in #193; `<VersionTag/>` reads `package.json` now, works under any build command.
