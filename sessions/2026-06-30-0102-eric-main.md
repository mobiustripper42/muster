---
session: 33
dev: eric
slug: main
branch: main
started: 2026-06-30T01:02:30Z
ended: 2026-06-30T17:18:10Z
points: 17
pr_numbers: [194, 197, 199, 200, 201]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/cedbbd12-4ba6-4627-b070-14ff921544aa.jsonl
---

# Session 33 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Claim service — claimSeat (Open→Confirmed) + releaseSelfClaim (7.2)

**Completed:**
- **`src/asks/claim.ts` (new) — the self-claim write door** (DEC-075/078), the inverse of 7.1's `claimableSeatsFor`. Lives in the `asks` layer so it composes oracle reads + ask-loop writes (asks→oracle, never reverse).
  - **`claimSeat`** never trusts the passed `seatId`: re-validates the *full* claimable predicate server-side (shift `Pending`/`Filling`, vessel-local window per DEC-032, seat `Open`+`required`, native-role per DEC-076, §1.3 pool) → guarded `saveSeatIfState(..,"Open")` CAS (REQ-CLAIM-1). One winner on a race, loser `just_taken`. **No reliability event** (assignment, not responsiveness). Conflict (DEC-078) reuses the oracle's `notDoubleBooked` → distinct `conflict` code; no bespoke guard (one definition of double-booked, DEC-DATA-1).
  - **`releaseSelfClaim`** = thin authorization wrapper pinning `expectedBailer=crewId` over `bailWithDerivedLateness` → can only release your own seat; re-opens, re-asks inline, emits one **lead-time-weighted** `shift_bailed`.
- **Typed flag pair** `selfClaimRequiresConfirmation()`/`setSelfClaimRequiresConfirmation()` across port + both adapters + `runRepositoryContract` (`app_settings` KV, absent⇒false, mirrors `engine_paused`; reuses migration `0006`, **no new migration**). Flag-on → `requires_confirmation` stub, **no write** — the reserved Held tier is NOT built (DEC-075).
- Exported `refreshShiftState` (ask-loop) + `CLAIMABLE_SHIFT_STATES` (claimable) so read door ↔ write door share constants.
- Tests: `verify` green; `claim.test.ts` 18 ✓ (success/`Crewed`, both native-role doors, non-Open/supernumerary/wrong-state/out-of-window, lapsed-MMC, conflict, **race**, flag stub, zero-event claim, gone; release re-ask, single weighted event, near-vs-far weighting, occupant pin). Service-only — no UI (that's 7.3 #183).

**Code review:** `@code-review` — five load-bearing properties confirmed (race safety, authorization, conflict guard, reliability semantics, flag stub). One finding addressed by documentation: the read-then-CAS guards one seat, not the cross-seat invariant → two concurrent same-crew claims for *different* same-date shifts can double-confirm. Pilot-accepted (same window class as `recordResponse`; no-FK store can't enforce crew+date uniqueness) → delegated to the integrity tripwire, documented in-code. **May warrant a DEC-078 amendment at retro.** Plus two doc-only fixes (release docstring, intra-shift conflict comment).
**PR:** [#194](https://github.com/mobiustripper42/muster/pull/194)
**Points:** 5
**Branch:** task/7.2-claim-service
**Opened at:** 2026-06-30T04:31:14Z

## Task 2: /crew/open self-serve pull surface — browse + claim (7.3)

**Completed:**
- **`src/crewapp/claimable-view.ts` (new, +7 tests) — the read side of the pull surface.** Decorates `claimableSeatsFor` (7.1) rows with vessel/role names, live scheduled-trip times, and the DEC-041 call→back window. Range-filters (today / weekend / from–to) within the [today,+45d] clamp; reads through suppression (PTO viewer → empty).
- **Extracted `committedWindow()` into `src/crewapp/shift-card.ts`** and **folded both inline copies** (`shift-card.ts` + `crew-view.ts`) onto it — one DEC-041 computation, no third copy (24 existing tests still green).
- **`app/(crew)/crew/open/{page,actions}.tsx` (new) — the 4th crew surface (DEC-074).** Server-rendered, no client JS: GET-link filter presets, native `<details>` confirm sheet per row with the DEC-077 whole-day copy (live trip count + window), Claim `<form action>`. DEC-042 guardrails: `force-dynamic`, no polling/live counts (bare row count only), neutral ink. Flag-gated on **both** page (`notFound()`) + action (`redirect`) — dark in prod (DEC-059). `claimSeat` action maps `just_taken`/`conflict`/success; never trusts `seatId` (service re-validates + CAS).
- `app/(crew)/crew/page.tsx`: flag-gated **"Pick up a shift"** entry link + `claimed` success note. Seed: a claimable captain shift for Quint (`shift-open`, 2 trips). **Release reuses the existing shift-card bail** (DEC-078) — not rebuilt.
- Tests: `verify` 755 ✓; `e2e/crew-open.spec.ts` **4/4 at 375px** (empty-today, full claim flow incl. confirm-sheet copy → My shifts, just_taken + conflict banners). Ran the e2e locally.

**Code review:** `@code-review` — "clean, convention-consistent, ship it." Confirmed: flag gating on both doors, untrusted `seatId`, `back` param is **not** an open-redirect (origin discarded), `committedWindow` extraction behavior-preserving. Applied one cleanup (tolerate a backwards from–to range). The "Added for you" badge on self-claims is deferred to **#196** (needs seat provenance).
**PR:** [#197](https://github.com/mobiustripper42/muster/pull/197) (based on `main`; 7.2/#194 merged mid-session)
**Points:** 5
**Branch:** task/7.3-crew-open
**Opened at:** 2026-06-30T12:24:32Z

## Task 3: Cascade coexistence — verify pull/push coexist (7.4)

**Completed:**
- **`src/builder/cascade-coexistence.test.ts` (new, 3 tests) — verification-only, NO production change** (the issue predicted coexistence holds by construction; it does). Drives the real `tick` alongside `claimSeat`/`releaseSelfClaim`:
  1. **Claim-before-horizon skipped:** a seat self-claimed during `Pending` → `Confirmed`; at the horizon crossing the cascade fills the still-Open mate seat but fires **no ask** for the Confirmed seat (tick fill loop skips non-Open/Asked, `tick.ts:227`). Anti-vacuity: asserts the mate seat *was* asked.
  2. **Release re-enters the cascade:** `releaseSelfClaim` → seat back to `Asked`, next candidate (`cap2`) re-asked inline (not the releaser); a follow-up tick keeps it a live `Asked` seat.
  3. **Abstain holds:** a tick during `Pending` fires zero asks (§1.1) while a crew pull still confirms — push/pull orthogonal.
- Also **closed #189** (7.0b real email — Resend live in prod, code shipped in #191).

**Code review:** `@code-review` — sound, **mutation-tested** (broke the prod guards → test 1 fails as designed; fixtures genuinely satisfy eligibility/window/horizon). Applied one nit: tightened test 2's final assertion from near-inert smoke to a real post-tick state check.
**PR:** [#199](https://github.com/mobiustripper42/muster/pull/199)
**Points:** 2
**Branch:** task/7.4-cascade-coexistence
**Opened at:** 2026-06-30T14:56:32Z

## Task 4: Ring relay — no-phone crew relayable via Web Share (#186)

**Completed:**
- **`components/outbox/ring-outbox-card.tsx`** — the ring card gated the whole Send on `smsHref`, dead-ending a no-phone crew at "No phone on file" even on a Web-Share device. Mirrored the ask card: always render `RelaySend` (nullable `smsHref`), and added the select-all `shareText` + `CopyButton` block so RelaySend's "copy the message above" fallback is true on the ring card too. No client JS added (card stays a server component; RelaySend is the existing island).
- **`db/seed-outbox-dev.ts`**: a no-phone crew ("Nora No-Phone", `phone:""`) + a pending ring entry (canonical `ring-${threadId}-${crewId}` id) → exercises the "New messages" path.
- **`e2e/outbox-relay.spec.ts`** (375px): the no-phone ring shows Send (not the warn) and relays to the sent flip. Non-vacuous (reverting renders the warn → fails).
- Tests: `verify` 755 ✓; outbox-relay e2e 3/3 at 375px.

**Code review:** `@code-review` — fix correct, e2e non-vacuous. Addressed: RelaySend's "copy the message above" fallback was a dead-end (ring card rendered no copyable text) → added the shareText + Copy block; aligned the seed ring id to the canonical shape. Noted-and-accepted: sent rings read "awaiting reply" (RelaySend) vs the old "awaiting read" — consistent with phone-rings.
**PR:** [#200](https://github.com/mobiustripper42/muster/pull/200)
**Points:** 2
**Branch:** task/186-ring-nophone-webshare
**Opened at:** 2026-06-30T15:57:26Z

## Task 5: Seat provenance — stop badging self-claims "Added for you" (#196)

**Completed:**
- **`db/migrations/0013_seat_acquired_via.sql`** (new) — nullable `acquired_via` on `seats` (metadata-only, no backfill/FK). `Seat.acquiredVia?: "operator" | "self_claim"`.
- **Postgres adapter**: map `acquired_via` in `toSeat` + `saveSeat` + `saveSeatIfState` (CAS `$7`); in-memory carries it via `structuredClone`.
- **Write paths**: `claimSeat` → `self_claim`; `manualOverride` → `operator`; `bail`/`vacateSeat` clear it on re-open (provenance is the occupant's, never stale). Invariant comments at the two Asked→Open reopen sites (Asked carries none).
- **`crew-view.ts`**: `addedByOperator = Confirmed && (acquiredVia === "operator" || (acquiredVia === undefined && !iAccepted))` — provenance badges operator-placements; accepted-ask is **inferred** (left absent), and the legacy fallback keeps pre-`0013` operator-placements badged.
- Tests: `verify` 760 ✓; `test:pg` 54 ✓ (provenance round-trip on real PG); crew-open e2e 4/4 at 375px (claimed row → **no** "Added for you"). Plus crew-view provenance, override→operator, bail clears both branches, contract round-trip + CAS-preserve.

**Code review:** `@code-review` — lifecycle sound, ship it (verified no stale provenance reachable, CAS wiring, all 4 badge cases, migration safe, exactOptional clean). Addressed: dropped the never-written `"ask"` enum value (accepted-ask inferred, not stored); documented the Asked-no-provenance invariant; added the bail-Bailed clear test.
**Migration:** `0013` applied to **dev** + **prod** (out-of-band) by the operator. Safe to apply ahead of code (nullable, no reader breaks pre-#196).
**PR:** [#201](https://github.com/mobiustripper42/muster/pull/201)
**Points:** 3
**Branch:** task/196-seat-provenance
**Opened at:** 2026-06-30T16:21:48Z

**Next Steps:**
- **Phase 7 fully done** (7.0–7.4 + the two follow-ups #186/#196). Once all PRs merge → `/retro` to close Phase 7 (throughput math + version bumps + RETROSPECTIVES.md).
- Open PRs to merge: #199 (7.4), #200 (#186), #201 (#196). Plus #195 (Tiller docs, not mine).
- Then `/its-dead` to close this session.
- **At close:** Phase 7 retro ran (20 pts, burst ~1.1d, drift 0) → bumped to **v0.9.0**, promoted `main` → `production`. **#189 closed** (Resend live). All 5 PRs merged.
- **Phase 8 not yet planned** — no rows in PROJECT_PLAN; `/start-phase 8` needs a plan first.
- **PM forward-notes worth a real home** (not just here): the 7.2 cross-seat double-confirm race may want a DEC-078 amendment; `committedDatesByCrew` is a full scan wanting a durable index before the roster grows.

**Context:**
- **Crew self-serve is now LIVE in prod** — operator flipped `CREW_SELF_SERVE` on after the v0.9.0 promote. The front door + `/crew/open` are open (no longer dark). Supersedes S32's "live but DARK" note.
- Migration **0013** (`seats.acquired_via`) applied to **dev + prod** (out-of-band).
- v0.9.0 = 7 per-PR patches (0.8.2–0.8.8) + minor close, all tagged on `main`; `production` ff-merged to the same commit (6bc844a).
