---
session: 34
dev: eric
slug: main
branch: main
started: 2026-07-01T14:48:59Z
ended:
points:
pr_numbers: [212, 213, 217, 218, 219, 221]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/18f7c4ec-59e5-4a62-91e7-1094dc2df584.jsonl
---

# Session 34 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Shift split-suggestion detector (gap/span) — 8.1

**Completed:**
- `src/builder/derive.ts` — `suggestSplit` pure advisory detector + env-knob thresholds (`SPLIT_SUGGEST_GAP_MINUTES` 120 / `SPLIT_SUGGEST_SPAN_MINUTES` 600) + `SplitSuggestion`. Two triggers (large inter-trip dead-gap, long total-span), gap-over-span, cancelled ignored, <2 trips → null. **Never auto-splits** — `formShifts` untouched (idempotency contract).
- `src/admin/all-shifts.ts` — `splitSuggestion` wired into `deriveAllShifts` / `AllShiftsRow` for 8.2 to render.
- Tests: `split-suggestion.test.ts` (13) + an `all-shifts` wiring case — **19 green**. Four reporter cases + strict-`>` boundary pins + precedence / cancelled / env-override / order.
- `npm run verify` green (typecheck + typecheck:app + Vitest + build).

**Code review:** @code-review — clean & correct (math hand-traced vs all four cases; purity honored). Addressed the two coverage findings (boundary pins + `deriveAllShifts` wiring test). Deferred non-blocking signature-order nit. Note for 8.2: `minutes` = gap-min (large-gap) vs span-min (long-span).
**PR:** [#212](https://github.com/mobiustripper42/muster/pull/212)
**Points:** 3
**Branch:** task/8.1-split-suggestion-detector
**Opened at:** 2026-07-01T16:46:30Z

## Task 2: Builder View mode — next-7-days default + split cue — 8.2a

**Completed:**
- `app/(admin)/admin/shifts/page.tsx` — the Shift Builder **View mode**, in place: default window widened **today → next 7 days** (operator pilot need); Today / Next 7 days / This weekend presets; renders 8.1's `splitSuggestion` as a muted "could be two shifts" cue (neutral, actionless — DEC-042 intact). No dead View/Edit toggle stub (that + Edit mode = 8.2b).
- `db/seed-atrisk-dev.ts` — scenario H (Barrel, 11:00+18:00 ~2d out, Crewed/off-board) to exercise the cue.
- `e2e/shifts-view.spec.ts` — 2 Playwright tests (7-day default; one split cue). **Not run in-sandbox** (no Postgres/:3100) — CI runs it; mirrors `all-shifts.spec` + code-reviewed.
- `docs/DECISIONS.md` — DEC-042 amendment (default today→next-7).
- `npm run verify` green (types + Vitest + build).

**Code review:** @code-review — correct; e2e/seed/math verified clean. Two doc findings addressed (DEC-042 amendment + stale docstring). **@architect** blessed the DEC-042 widening (guard is *shape* not *width*; watch: no per-day scoreboard).
**PR:** [#213](https://github.com/mobiustripper42/muster/pull/213)
**Points:** 3 (the View half of 8.2's re-est 8)
**Branch:** task/8.2a-builder-view-mode
**Opened at:** 2026-07-01T19:10:36Z

## Task 3: Crew My Shifts cards — shift times + co-crew — #216

**Completed:**
- `src/crewapp/crew-view.ts` — `MyShiftView` gains `departureTime`/`shiftEndTime` + `coCrew`; shared `departureWindow` helper (ask card + My shifts, no dup); co-crew = other Confirmed/Claimed seats (deduped, minus me, **sorted by name**).
- `app/(crew)/crew/page.tsx` — `ShiftWhenWhat` component + `timeWindow`/`withCrew`; date + window (**Mono**) leads, `vessel · role · with X` muted line, "Added for you" badge moved to a quiet bottom line (only when force-placed) → same 2-line height. Designed via the **frontend-design skill, brand-bound**.
- `crew-view.test.ts` — new #216 test (window + co-crew; solo/no-events). `npm run verify` green.

**Code review:** @code-review — clean & well-tested (departureWindow refactor behavior-preserving; co-crew filter closes every exclusion). Applied the one finding: sort coCrew by name (stable +N display at 3+ crew). Not e2e'd (no Postgres locally; layout is an eyeball concern).
**PR:** [#217](https://github.com/mobiustripper42/muster/pull/217)
**Points:** 2
**Branch:** task/216-my-shifts-times
**Opened at:** 2026-07-01T21:26:38Z

## Task 4: Manual split ENGINE (cut-time partition) — 8.3a

**Completed:**
- **DEC-083** (@architect ×2 — cut-time model; the operator's "import after split" wrinkle resolved) + migration **0014** (`split_cut_time`, nullable) + `Shift.splitCutTime` + adapters + contract round-trip.
- `src/builder/form-shifts.ts` — extracted **`formOneShift`** (un-split path **byte-identical** — reconcile harness green) + the gated cut-time split branch (side A `< cut` on the canonical id keeps its crew, side B `>= cut` on `-b`; new trips auto-land by time; a collapse is a Cancelled husk, the split PERSISTS) + `splitDaysChanged` import-diff detection.
- `src/builder/split.ts` (new) — `splitShift` service (cut validation, non-empty-partition / already-split / `-b` guards). `ImportRunSummary.splitDaysChanged` for the 8.3b cue (audit-derived, no lock — DEC-082).
- `split.test.ts` — 11 tests. `npm run verify` green (96 engine tests).

**Code review:** @code-review — engine solid (formOneShift behavior-preserving; detection no false-pos/neg). Applied all findings: cut-time validation (the latent bug), hot-path read reuse, historical-run default, DEC wording. **@architect** blessed the cut-time model (re-pass — simpler + Xola-robust).
**PR:** [#218](https://github.com/mobiustripper42/muster/pull/218)
**Points:** 5 (the engine half of 8.3; 8.3b = toggle + UI)
**Branch:** task/8.3a-split-engine
**Opened at:** 2026-07-01T22:55:20Z

## Task 5: Manual split UI — Shift Builder Edit mode (8.3b)

**Completed:**
- `app/(admin)/admin/shifts/page.tsx` — **View · Edit toggle** (`?mode=edit`, window-preserving); Edit-mode **Split control** (no-JS `<select>` of the day's own departure times → `splitAction`, defaulting to the suggested gap boundary); split-pair tag (`split · before/from HH:MM`); **import-diff cue** reading latest `ImportRun.splitDaysChanged`; `split_ok`/`split_err` feedback notices (#202).
- `app/(admin)/admin/shifts/actions.ts` (new) — `splitAction` → `splitShift`; codes-not-prose (DEC-026), `redirect` outside try, `back` preserves the window.
- `src/admin/all-shifts.ts` — `AllShiftsRow.split` (side A carries cut; side B borrows via O(shifts) pre-scan, survives a Cancelled husk) + 2 tests. `npm run verify` green (29 touched-suite tests).

**Code review:** @code-review — security-sensitive parts all clean (redirect placement, no open-redirect via `back`, no XSS, `-b` slicing, param preservation). 2 low: same-time-trip cut options **fixed** (dedupe+floor); ok/bad notice tones **kept deliberately** (transient interaction feedback #202, not row-state colour — noted in PR).
**PR:** [#219](https://github.com/mobiustripper42/muster/pull/219)
**Points:** 3
**Branch:** task/8.3b-split-ui
**Opened at:** 2026-07-01T23:20:56Z

## Task 6: Manual Merge + crew assignment-change notices (8.4, DEC-083 inverse + DEC-084)

**Completed:**
- **Merge engine** `src/builder/merge.ts` (+`merge.test.ts`, 6) — `mergeShift` tears down `-b` + seats, clears `splitCutTime`, re-forms to one shift (side A crew preserved); `freedCrew` = side-B occupants MINUS side-A survivors (a dual-side crew member isn't falsely told "off"). New `repo.removeShift` (port + in-memory + postgres + contract).
- **DEC-084 notice subsystem** — third operator-relay sibling: `src/ports/notice.ts`, `NoticeOutboxEntry` + `notice_outbox` (**migration 0015**), `outbox-notice-channel.ts` (deterministic id, terminal-on-sent, preserves a sent record) + `fake-notice-channel.ts`, `forward-notices.ts`, `notice-outbox-view.ts`, `components/outbox/notice-outbox-card.tsx`, `recordNoticeSent` + a third `/admin/outbox` "Assignment changes" section. (+ channel/view/forward tests.)
- **Wiring** — Edit-mode **Merge** control on split pairs (side A, fallback to side B); `mergeAction` excludes the operator (DEC-072/084), relays "you're off" **best-effort** (never flips a committed merge to error), `merge_ok=<count>`/`merge_err`. `forwardNoticesToOutbox` in `app/lib/channel.ts`. DEC-084 recorded; PROJECT_PLAN 8.4 → 5 pts.

**Code review:** @code-review — 1 real bug (freedCrew missing side-A-survivor subtraction → false "you're off") **fixed** + test; 3 items fixed (relay error-seam, docstring parity claim, sent-notice reset). Rest sound (teardown, cut-clear, deterministic id, SQL↔entity parity, repo parity, redirect-outside-try, codes-not-prose).
**PR:** [#221](https://github.com/mobiustripper42/muster/pull/221)
**Points:** 5 (re-estimated from 3 — carries the notice subsystem)
**Branch:** task/8.4-merge
**Opened at:** 2026-07-02T05:08:57Z

**Next Steps:**
- Shipped + merged: 8.1 (#212) + 8.2a (#213) + DEC-082 (#214) + My Shifts (#217) + 8.3a (#218) + **8.3b split UI (#219, merged)**. **8.4 Merge + DEC-084 notices → PR #221 (open)**; idea-park #220 (open). Apply migrations **0014 + 0015** out-of-band.
- **Next: 8.5 seat/manning override** — last Phase 8 task. Phase 8 = 8.1✓ 8.2a✓ 8.3a✓ 8.3b✓ 8.4✓ + 8.5.
- **DEC-084 fast-follow** (own task): wire the assignment notice into the OTHER add/drop sites — import-cancellation (`formShifts` cancel path), `vacateSeat`/`bail` (removed), `overrideSeat` (added) — + the Twilio/SMS adapter. Each must exclude the operator + solve the re-pull dedup (they run inside `formShifts`).
- Cleanup issue #215 (retire dead lock scaffolding) still open.

**Context:**
- **Phase 8 planned + materialized THIS session before the formal `/its-alive`.** PROJECT_PLAN Phase 8 committed to `main` (`55245bb`); issues [#204](https://github.com/mobiustripper42/muster/issues/204)–[#209](https://github.com/mobiustripper42/muster/issues/209) created. Xola-write-back idea filed → standalone docs PR [#210](https://github.com/mobiustripper42/muster/pull/210). Also filed #202 (button feedback) + #203 (absorbed into 8.1/8.3/8.4).
- **Posture shift (owner):** slice is over, pilot healthy, heading to real production → **fully-baked interfaces, no thin stubs** from here (still hold scope discipline).
- **Done this session:** salvaged 6 MAX_SLICE ideas → FUTURE_IDEAS (#211 merged); **cut locking** (DEC-082, #214) — Xola is truth, dropped 8.2b + 8.6, Phase 8 24→17; added the **frontend-design skill brand-binding** to `ui-context.md` (Muster-local, in #214); filed cleanup #215 + crew-card #216. Xola-write-back idea #210 merged. Stale local branch `task/8.2b-builder-edit-mode` couldn't auto-delete (permission) — harmless.
- Carried from S33 PM notes: 7.2 cross-seat double-confirm race may want a **DEC-078 amendment**; `committedDatesByCrew` full scan wants a durable index before the roster grows.
- Prod state (S33): crew self-serve LIVE, migration 0013 applied dev+prod, v0.9.0 on `main` = `production` (`6bc844a`).
