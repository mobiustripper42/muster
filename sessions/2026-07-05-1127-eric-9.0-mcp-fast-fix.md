---
session: 37
dev: eric
slug: 9.0-mcp-fast-fix
branch: task/9.0-mcp-fast-fix
started: 2026-07-05T11:27:12Z
ended:
points:
pr_numbers: [257, 258]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/2015d94a-7eb7-4bfe-98d3-a5787322364a.jsonl
---

# Session 37 — 9.0-mcp-fast-fix

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Retire dead lock scaffolding (DEC-082) — #215

**Completed:**
- Session opened on merged/stale `task/9.0-mcp-fast-fix`; switched to `main`, ff-pulled to `966001a`. Adopted the empty open Session 37 shell (transcript repointed) rather than mint a duplicate.
- **Batch triage** of all open Phase 9 issues. Verified-and-closed **#203** (gap/span split suggestion) as already shipped in 8.1/#212 — `suggestSplit()` + board surfacing + tests all present.
- **#215:** deleted `src/builder/lock.ts` + test; dropped the `./lock.js` barrel export, the `lockedAt` preservation in `formShifts`, the "changed since reviewed" nudge in `shift-cockpit.tsx`, `Shift.lockedAt` + its postgres round-trip + contract assertions, and seed scenario G (the nudge demo).
- Kept `Reservation.updatedAt` (reworded doc — raw change signal for a future Xola-diff-anchored cue). Physical `shifts.locked_at` column left inert → deferred DROP migration filed as **#256**.

**Code review:** Clean bill of health — no dangling lock refs, INSERT-drop safe (nullable/no-default column), nudge removal surgical, scenario G's ids went with it.
**PR:** [#257](https://github.com/mobiustripper42/muster/pull/257)
**Points:** 3
**Branch:** task/215-retire-lock-scaffolding
**Opened at:** 2026-07-05T12:24:00Z

## Task 2: Two-pane independent-scroll columns (#253)

**Completed:**
- Operator-chosen approach (asked): CSS independent-scroll columns, no client JS.
- `components/ui/shell.tsx` — opt-in `fill` prop: `lg:h-[calc(100dvh-3.25rem)] lg:min-h-0` (viewport-fit below the sticky nav). Default false.
- `app/(admin)/admin/shifts/page.tsx` — two-pane grid `lg:flex-1 lg:min-h-0`; each column `lg:overflow-y-auto lg:min-h-0`. Window never scrolls on desktop → opening/switching a pane can't snap the list. Below lg untouched (full-screen drill-in). Added board-col/pane-col test ids.
- `e2e/pane-scroll.spec.ts` (2) + `e2e/admin-nav.spec.ts` nav-height guard (≤52px, lg-only). Neighboring board specs (13) + verify (850) green.

**Code review:** 1 finding — the `3.25rem` nav-height constant was an unpinned duplicate of the real ~45px nav (7px slack). Fixed: added the e2e height guard so future nav growth fails CI instead of silently reopening #253; documented the coupling in shell.tsx.
**PR:** [#258](https://github.com/mobiustripper42/muster/pull/258)
**Points:** 3
**Branch:** task/253-pane-scroll-preserve
**Opened at:** 2026-07-05T12:52:00Z

**Next Steps:**

**Context:**
