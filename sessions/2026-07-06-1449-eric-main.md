---
session: 39
dev: eric
slug: main
branch: main
started: 2026-07-06T14:49:14Z
ended:
points:
pr_numbers: [289, 290, 291, 294, 295, 296, 303, 304]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/46e5896a-c186-464f-88a5-ba201e15be85.jsonl
---

# Session 39 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: e2e prod-server webServer — dodge the per-dir next-dev lock (no issue)

**Completed:**
- **Root fix:** Playwright's `webServer` now switches on `E2E_PROD` (defaults `!CI`): local → `next start` + `VERCEL_ENV=preview` (takes NO per-dir dev lock → coexists with the operator's live `next dev` on :3000); CI → `next dev` unchanged. `VERCEL_ENV=preview` keeps `/crew/dev-link` live under a prod server (isProdDeploy false, `dev-link/route.ts:32-35`). Rewrote the stale "two deliberate constraints" header that claimed a prod server couldn't run these. (`playwright.config.ts`)
- `package.json`: `test:e2e:prod` / `test:e2e:dev` scripts — the memorized env incantation ([[e2e-while-dev-lock-held]]) is retired; plain `npm run test:e2e` now works alongside a live dev server.
- **@code-review caught a real bug:** the prod `next build` shared the operator's `.next` → manifest-corruption footgun (the exact coexistence problem, quieter). Fixed with a dedicated `.next-e2e` distDir keyed on `E2E_PROD` (`next.config.ts`); gitignored; Next auto-added `.next-e2e` type globs to `tsconfig.json` (committed, mirrors `.next/dev/types`, avoids a recurring dirty tree). Also: stricter `E2E_PROD` parse (only `1`/`true` enable) + `reuseExistingServer` staleness documented.
- Memory `[[e2e-while-dev-lock-held]]` + MEMORY.md pointer rewritten to the one-command path.

**Proof:** prod path `auth-crew` 2/2 + `trainee-staffing` canary 1/1 (outbox routing holds); confirmed build isolated to `.next-e2e`, `.next` untouched. CI path (`E2E_PROD=0`) `auth-crew` 2/2. `npm run build` green.
**Code review:** 3 findings — 1 bug (shared `.next`) fixed, 2 minor (reuse staleness documented, flag parse tightened).
**PR:** [#289](https://github.com/mobiustripper42/muster/pull/289)
**Points:** 2
**Branch:** task/e2e-prod-server
**Opened at:** 2026-07-06T15:00:00Z

## Task 2: promote-production migration-ledger pre-promote check (#282, part b)

**Completed:**
- **Reframed 10.1/#282 with live evidence.** The Neon preview→prod *backdoor* was already fixed 2026-06-25 in `vercel.json` (`git.deploymentEnabled.main:false`, #138) — verified via the Vercel MCP: zero `main` deploys, prod only from the `production` branch. The stale [[neon-preview-deploy-gotcha]] memory (tabled "pending screenshots") is corrected. So #282's remaining work is only the migration-ledger guardrail.
- **(b) — project-owned check** in `.claude/CLAUDE-context.md` (§ Migration Protocol): before `/promote-production`, read prod's applied migrations via the **Neon MCP** (`run_sql`, project `delicate-art-65084110` / neon-red-pendant, org `org-spring-feather-31353161`, default branch = prod) — `select filename from _migrations` — diff vs `db/migrations/*.sql`; repo-ahead → STOP, prod-ahead → warn, MCP-down → operator pastes. Also states prod migrations are hand-applied out-of-band.
- **Split rationale (seeds Claude's catch):** muster's `promote-production` is a **logic-class seeds shell** — a local skill edit gets byte-overwritten by the nightly **downstream** sync (confirmed: every auto-sync PR is `auto-sync/downstream/*`; the only upstream path is explicit `/push-seeds`). So (a) — broadening seeds Step 0.5 to "run the project's pre-promote checks" — is being authored on the **seeds** repo (owner: operator), reaching muster via `/pull-seeds`. Guardrail is fully live only after (a) syncs down.
- **Dry-ran the documented check:** repo 17 == prod 17 (0001–0017), MISSING empty (promote allowed), EXTRA empty. No drift today; verdict matches intent.

**Code review:** Self-reviewed (docs/config note). Neon IDs confirmed live (`autumn-heart` is a different app; `delicate-art` holds muster's ledger); diff logic dry-ran correct.
**PR:** [#290](https://github.com/mobiustripper42/muster/pull/290)
**Points:** 2
**Branch:** task/promote-migration-check
**Opened at:** 2026-07-06T15:20:00Z

## Task 3: pull-seeds — generalize promote-production Step 0.5 (#282, part a)

**Completed:**
- Pulled seeds `e8dbd2b` into `.claude/skills/promote-production/SKILL.md` (verbatim; logic-class shell, no concretions). Step 0.5 went from "confirm QA'd" → "run the project's pre-promote checks" (reads `CLAUDE-context.md` § Migration Protocol + any Pre-promote checks; STOP on any failed/unconfirmed gate; QA-confirm kept as the common gate). This is the generic hook that makes Task 2's migration-ledger note actually gate a promote.
- **Avoided the full /pull-seeds @sync-config sweep** — operator refocused; only this one file was wanted. Seeds checkout ff'd 16 commits to origin/main first (both at seeds-v4). Targeted single-file pull, confirmed byte-identical to seeds post-copy.

**Code review:** Self-reviewed — verbatim seeds pull (diff IDENTICAL), authored/reviewed seeds-side.
**PR:** [#291](https://github.com/mobiustripper42/muster/pull/291)
**Points:** 1
**Branch:** task/pull-seeds-promote-production
**Opened at:** 2026-07-06T15:35:00Z

**Note:** #290 + #291 are inert alone — **merge both** to make the migration guardrail live. Together they close #282's guardrail half; the backdoor half was already done (#138).

## Task 4: Admin entity + per-person revoke (Phase 10.2, DEC-092, closes #283)

**Completed:**
- **@architect-gated** (GO, minimal): admin becomes a first-class, individually-revocable auth identity, revising DEC-020's "admin is a non-identity" clause (not the "no platform" one).
- **Every admin is also crew** (operator's framing) → `admins.id` IS the crew id; `{kind:"admin", id}` vs `{kind:"crew", id}`, `kind` disambiguates. New `admins` table (`db/migrations/0018_admins.sql`, text PK, no FK, dates-as-text), seeded roster eric/brendan/drew, idempotent.
- **Revoke gate** (`app/lib/auth.ts` readSubject): one `active` lookup for admin subjects only → deprovision (active=false) kills the session on the next request, immediately, others untouched; **crew subjects skip the DB hit** (stateless hot path preserved). Replaces rotating SESSION_SECRET (global-only lever).
- Port + Postgres + in-memory `saveAdmin`/`getAdmin`/`getAdminByHandle`/`listAdmins` + contract tests. `db:mint` + `dev-link` resolve handle→id, refuse unknown/inactive (helpful error lists active handles).
- **DEC-092** written; `operator.ts` singleton flagged for removal; `DEPLOY.md` got roster + deprovision SQL. **Peeked bushel** — its auth was ported FROM muster; bushel's `is_active` is *eventual* revoke (blocks new logins only), muster's readSubject check is *immediate* — deliberate upgrade.
- **Scope held** (architect bound): no roles/RBAC, admin UI, session-version col, passwords, or audit log — seams left. **Follow-up #293** files operator-singleton retirement (a DEC-030/058 messaging refactor, split out for reviewability). Switcher idea parked (#292).

**Tested:** verify green; contract 60/60; migration applied+idempotent; **25 admin e2e green** across 8 specs (new revoke spec 3/3: sign-in→cockpit, deprovision→bounced, per-person 2nd admin survives). Full suite = operator's call; changed path is the single `signInAsAdmin` chokepoint, validated broadly.
**Code review:** 1 finding (stale `mint-prod --admin=spink` doc example) → fixed. Auth mechanics confirmed solid (gate scoped/pre-renewal, crew branch DB-free, migration idempotent, no double-close).
**PR:** [#294](https://github.com/mobiustripper42/muster/pull/294)
**Points:** 5
**Branch:** task/10.2-admin-entity
**Opened at:** 2026-07-06T18:15:00Z

## Task 5: Crew ↔ admin view switcher (DEC-093)

**Completed:**
- A dual-role person switches crew↔admin without re-auth: `admins.id` IS the crew id (DEC-092), so switching = re-minting the other-kind session for the same id. Two server actions (`app/lib/switch-actions.ts`, reuse `startSession`): `switchToCrew` (admin→crew, always allowed) + `switchToAdmin` (crew→admin, **privilege escalation** gated on the DEC-092 `getAdmin(active)` check).
- UI (operator-placed): crew home "Switch to admin" beside Sign out, active-admins-only (best-effort lookup, fails closed); admin nav "Crew view" by Muster·date (SubmitButton per DEC-090).
- **The front door now:** email is wired in prod ([[crew-code-login-live-prod]]) → crew code login (DEC-081) is live, so everyone logs in once as crew, admins switch up. Retires `db:mint --admin` as the normal admin path.
- DEC-093 (security seam flagged for 10.3); e2e 5/5.

**Code review:** escalation seam confirmed sound (gate = readSubject's, redirects halt before mint, CSRF-mitigated sameSite/POST, best-effort lookup fails closed). 1 finding — revoked test proved the button hid, not the action → **fixed**: added a stale-click test proving `switchToAdmin` bounces server-side.
**Security note:** `switchToAdmin` is the app's one privilege-escalation seam. 10.3 must audit it.
**PR:** [#295](https://github.com/mobiustripper42/muster/pull/295)
**Points:** 3
**Branch:** task/switcher
**Opened at:** 2026-07-06T18:45:00Z

## Task 6: db:admin CLI + drop provisional seed (DEC-092)

**Completed:**
- **db:admin CLI** — `add` (`--email` resolves crew id via a roster scan, or `--crew=<id>`; validates the id is a real crew member + handle uniqueness), `revoke`/`reactivate`/`list`. Core in `src/admin/admin-cli.ts` (framework-free, port-only, 10 unit tests); thin `db/admin.ts` entrypoint connects via `DATABASE_URL` (auto-sources `.env.local`, inline wins) like db:mint/db:migrate.
- **`0019_drop_provisional_admins.sql`** — removes 0018's guessed roster (scoped to the exact id+handle pairs; a CLI-added real admin survives). Admins are CLI-managed now: real crew ids + emails come from the roster, not migration-authoring time.
- **DEPLOY.md** — admin provisioning + deprovision runbook rewritten around the CLI. Crew ≠ Xola customers (Xola imports booking/customer emails, not crew) → crew emails set on the crew record.
- **Debug detour (operator's local login):** `crew-eric` had no email → no-enumeration silence (not Resend). Root cause of the failed fix: a combined `psql -c "update…; update admins…"` ran as ONE transaction; the `admins does not exist` error rolled back the email update too. Standalone statements fixed it. Env was fully wired (Resend + EMAIL_FROM + CREW_SELF_SERVE).

**Code review:** solid except one real bug — `add --crew=<id> --name=<n>` skipped crew-id validation (name lookup short-circuited) → phantom admin that breaks the DEC-093 switcher → **fixed**: `--crew` validates existence unconditionally.
**PR:** [#296](https://github.com/mobiustripper42/muster/pull/296) (stacked on #295, rebased onto main after it merged → clean diff)
**Points:** 3
**Branch:** task/db-admin
**Opened at:** 2026-07-06T19:25:00Z

## Task 7: 10.3 security audit — report + clear-win fixes (#284)

**Completed:**
- **Full pre-production audit**, 5 dimensions fanned out in parallel, each finding **adversarially verified**. Deliverable: `docs/SECURITY_AUDIT.md`.
- **Posture: no confirmed code launch-blocker.** Authorization boundary (no-RLS) HOLDS (every id-taking crew action re-checks ownership); admin gates 100% (9/9 pages, 23/23 actions); switcher escalation correctly gated; no secret committed/logged/shipped; input clean.
- **The headline CRITICAL was FALSE** — dev-link→prod-admin assumed main hits prod DB; verified `vercel.json` `git.deploymentEnabled.main:false` (#138) → main never deploys → no main-preview URL. Agent ran off my stale memory note. Downgraded LOW.
- **Fixed (unambiguous):** `/api/health` was an unauth ~12-full-table-scan per hit (DoS) → cheap `select 1`; `..env.local.swp` untracked + `.gitignore *.sw[a-p]`; `?claimed` note gated on the viewer's own shifts (was an existence oracle).

**3 operator DECISIONS (filed, not code bugs):** #297 login-code attempt-cap concurrency race (HIGH — recommend fix; admins log in via this path), #298 operator-reads-crew-DMs disclosure, #299 guest-phone-to-crew. Also filed: #300 (crew-session revocation), #301 (low hardening bundle), #302 (xola-pull cron not scheduled). Extends #189.
**Code review:** self-reviewed (3 small verified fixes + report). verify green.
**PR:** [#303](https://github.com/mobiustripper42/muster/pull/303)
**Points:** 5
**Branch:** task/10.3-security-audit
**Opened at:** 2026-07-06T20:00:00Z

## Task 8: Atomic login-code attempt cap — close the concurrency race (#297)

**Completed:**
- Operator's 3 audit decisions: **#297 → fix** (this task); **#299 → by design** (guest phone to crew accepted, closed); **#298 → deferred** ("add disclosure if anyone ever uses messages" — commented, left open).
- **The fix:** `verifyLoginCode` checked a stale `attempts` then bumped separately → K concurrent guesses bypassed the 5-cap (admins log in via this path). Replaced `bumpLoginCodeAttempts` with atomic `claimLoginAttempt` — one row-locked `UPDATE … SET attempts=attempts+1 WHERE consumed_at IS NULL AND attempts < max RETURNING …`; concurrent claims serialize, exactly `max` succeed. Port + Postgres + in-memory + contract test (10-concurrent-vs-cap-3, real row contention on the 10-conn Postgres pool).

**Code review:** deep pass, **no bugs** — race closure confirmed (row-lock; no new claim↔consume TOCTOU), in-memory parity, no lockout regression. Added a comment for the claim-before-expiry tradeoff.
**PR:** [#304](https://github.com/mobiustripper42/muster/pull/304) (off main, independent of the audit #303)
**Points:** 3
**Branch:** task/297-login-code-race
**Opened at:** 2026-07-06T20:30:00Z

**Next Steps:**
- **All three launch gates (10.1/10.2/10.3) are addressed.** 10.3's only remaining code item (#297) is fixed; #298 (DM disclosure) deferred to messaging-adoption; #299 accepted.
- **Merge the open PRs** (#303 audit, #304 login-code) when reviewed.
- Deferred/hardening backlog: #300 (crew-session revocation — decision), #301 (low hardening bundle), #302 (xola-pull cron), #293 (operator-singleton retirement), #189 (code-login per-IP throttle). None launch-gating.
- Prod: apply `0018`+`0019` at promotion (#282 guardrail flags it), then `db:admin add --email=…` for the real admins.
- **Prod admin↔roster reconciliation:** apply `0018`+`0019` to prod at promotion (#282 guardrail flags it), then `db:admin add --email=…` for eric/brendan/drew AFTER the crew roster + their emails exist. Crew emails are yours to set (not from Xola).
- **#293** operator-singleton retirement (messaging refactor).
- **Stale-comment cleanup** (when touched): `playwright.config.ts` "off until 7.0b" — crew code login is live now ([[crew-code-login-live-prod]]).

**Context:**
