---
session: 41
dev: eric
slug: main
branch: main
started: 2026-07-09T16:38:37Z
ended: 2026-07-10T04:07:12Z
points: 17
pr_numbers: [324, 325, 326, 327, 328, 329]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/2c44d377-ec61-4f6b-b1ef-1043fa5f9be4.jsonl
---

# Session 41 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Guest manifest on the operator cockpit (closes #319)

**Completed:**
- Extracted `buildShiftManifest(repo, shift)` + `ShiftManifestView` from `buildShiftCard` (`src/crewapp/shift-card.ts`) — viewer-independent assembly; the crew card now calls it (behavior-preserving).
- New shared `<ShiftManifest>` component (`components/assignment/shift-manifest.tsx`); crew shift page (`app/(crew)/crew/shift/[shiftId]/page.tsx`) renders via it, byte-identical.
- Cockpit (`components/assignment/shift-cockpit.tsx`) renders the per-event booked-guest manifest above `<ManningSection>`. No parallel query; DEC-012 no-waiver preserved.
- +3 unit tests; new `e2e/cockpit-manifest.spec.ts` (passes desktop + 375px mobile, added to the mobile project); crew card e2e regression green. Full `verify` = 898 tests + typecheck + lint + build.

**Code review:** Core refactor + crew render confirmed behavior-preserving. One "bug" finding was a false positive (reviewer misread the seed name — e2e passes). Two advisory cleanups deferred as follow-ups: cockpit's redundant `getShift` (buildAssignmentView already loads it), and `tel`/`mapHref` duplicated between the component and the crew page.
**PR:** [#324](https://github.com/mobiustripper42/muster/pull/324)
**Points:** 3
**Branch:** task/319-cockpit-manifest
**Opened at:** 2026-07-09T18:26:52Z

## Task 2: Name the trip on skipped import rows — date/time/event (closes #320)

**Completed:**
- `SkippedRow` gains `date`/`time` (`src/import/import-reservations.ts`); both skip paths ("no resolvable boat", "record missing event id") now carry date/time/product/reservationId from the record.
- `/admin/import/run/[id]` (`app/(admin)/admin/import/run/[id]/page.tsx`) renders each reservation skip as `Sat Jul 11 · 3:30 PM · <event> — <reason>` (a list); rows without trip facts fall back to the bare reason. New UTC-anchored `fmtSkipDate` (DEC-032), returns null on a bad date → degrades to reason.
- +2 unit assertions; full `verify` = 898 tests + typecheck + lint + build.
- Reservationist deferred (not captured from Xola today — prod is .xlsx, API `seller` = query account). Its own task if wanted.

**Code review:** Clean; one hardening suggestion applied (fmtSkipDate null-guard on malformed date). No import-run e2e harness exists → render is eyeball-verified.
**PR:** [#325](https://github.com/mobiustripper42/muster/pull/325)
**Points:** 2
**Branch:** task/320-import-skip-detail
**Opened at:** 2026-07-09T18:46:38Z

## Task 3: Group /crew/open by day (closes #313)

**Completed:**
- `app/(crew)/crew/open/page.tsx` — added `fmtDayHeader` + `groupByDay` (verbatim mirror of admin board), grouped the flat claimable list into day sections with full-weekday headers + per-day open count. Rows keep time order within a day; no view-model change; DEC-042 guardrails intact.
- `e2e/crew-open.spec.ts` — +1 grouping test (date-agnostic), passes desktop + 375px mobile (12/12). Full `verify` = 898 tests + typecheck + lint + build.

**Code review:** Clean bill of health — bucketing/ordering sound, h1→h2 hierarchy correct, empty-state untouched, DEC-042 intact. No follow-ups.
**PR:** [#326](https://github.com/mobiustripper42/muster/pull/326)
**Points:** 2
**Branch:** task/313-crew-open-grouping
**Opened at:** 2026-07-09T20:11:45Z

## Task 4: 'archived' crew status — off every list incl. manual override (closes #323)

**Completed:**
- `CrewStatus` += `archived` (`src/domain/entities.ts`) — out of ask pool AND override picker/guard; distinct from `inactive` (placeable bench). No migration (status is `text`).
- Defense-in-depth: `overrideSeat` rejects archived (new `code`, `src/asks/ask-loop.ts`) + `shift-cockpit.tsx` filters archived from `ratingsById` (picker) + error copy + `actions.ts` mapping.
- `db:crew archive|unarchive` verbs + `✗` list marker + USAGE (`src/crew/crew-cli.ts`).
- Tests: ask-loop (archived-reject/inactive-allow), crew-cli (archive/unarchive+marker), contract (setCrewStatus archived). DEC-096. Full `verify` = 901 tests + typecheck + lint + build.

**Code review:** Clean bill of health — archived verified out of every path (isActive gate, doorbell, picker, guard, CLI), no binary-switch gaps, no migration needed.
**PR:** [#327](https://github.com/mobiustripper42/muster/pull/327)
**Points:** 3
**Branch:** task/323-archive-crew (first of stack #323→#321→#317)
**Opened at:** 2026-07-09T22:08:24Z

## Task 5: At-Risk board N+1 → render-scoped read cache (closes #316)

**Completed:**
- New `src/adapters/memoizing-repo.ts` — `memoizingRepo(repo)`: Proxy-based `Repository` read cache, memoizes idempotent hot-path reads (reliability/creds/PTO/crew/shifts/events/seats/asks). Proxy (not Object.create) for Postgres `#pool` private-field safety.
- `deriveAtRiskBoard` wraps its repo in it → collapses THREE per-shift × all-crew N+1s at once (escalationTrailFor reliability scan, solveShift/eligiblePool creds+PTO, bailers loop) from shifts×crew to crew. No shared-signature change, no migration, no DEC.
- Issue under-scoped it (named only the bailers loop); real diagnosis found all three. Architect-reviewed the cache approach.
- Tests: memoizing-repo unit (dedup/single-slot/delegation) + at-risk-board integration (no per-crew key re-fetched across 3 shifts). Full `verify` = 902 tests + typecheck + lint + build.

**Code review:** Nothing to fix — pure-read path (no masked writes), correct `this` binding both adapters, single-arg keying. Added a doc caution (cached results shared/read-only).
**PR:** [#328](https://github.com/mobiustripper42/muster/pull/328)
**Points:** 5 (issue estimated 2-3; re-scoped once the real N+1s surfaced — flag for retro)
**Branch:** task/316-atrisk-n1
**Opened at:** 2026-07-09T23:57:57Z

## Task 6: Manual override → sorted dropdown (closes #312)

**Completed:**
- `components/assignment/seat-card.tsx` — replaced the `flex-wrap` per-crew button list with a no-JS `<select name="crewMemberId">` ("Select crew…" default + rated crew) + one "Place" button. Empty-state when no candidates; removed unused `MiniButton` import.
- `components/assignment/shift-cockpit.tsx` — override roster sorted alpha by first name (tiebreak full name).
- New `e2e/cockpit-override.spec.ts` (select shape + sort + place→confirmed), added to 375px mobile project. Full `verify` = 901 tests + typecheck + lint + build; e2e 2/2 desktop+mobile.
- **Stacked on #323** (base `task/323-archive-crew`) — same cockpit surface; retargets to main when #327 merges.

**Code review:** Clean bill of health — empty-default select + action guard = no accidental placement; sr-only label wired; MiniButton removal safe; sort before DEC-064 filter, crash-safe on single-token names.
**PR:** [#329](https://github.com/mobiustripper42/muster/pull/329)
**Points:** 2
**Branch:** task/312-override-dropdown
**Opened at:** 2026-07-10T00:35:43Z

**Next Steps:**
- Version tag: left as-is (already the Sailbook-style fixed bottom-right click-through tag on /crew, /admin, /crew/open) — operator OK with it once located. No change.
- Parked stack (unbuilt): #321 (admin/shifts quick date filters; "Week After" label for days 8–15) then #317 (Cohort subject prefix + whole-day cohort button on admin cockpit).
- Open PRs to review/merge: #325, #326, #327→#329 (stacked), #328.
- Parked clarifications for the stack (answered): #317 = whole-day cohort (not single-shift); #321 days-8–15 label = "Week After".

**Context:**
- **All 6 PRs merged mid-session; shipped v1.0.3 to prod** — bumped `main` (CHANGELOG + tag `v1.0.3`, PRs #324–#329), ff-merged `main`→`production`, pushed. `/retro` will patch/minor-bump from GitHub, so DON'T double-bump; v1.0.3 already tagged.
- **Issues filed this session (backlog):** #319–#323 (built), #331 (audit trail of asks — browse surface + delivery record, promoted from FUTURE_IDEAS, cross-linked), #332 (time-off surfaces — crew add/remove blackout + admin view; data layer already complete, surfaces-only).
- **Open bug NOT filed (operator's call — "fine for now"):** on shift `shift-vessel-brew-1-2026-07-11`, Eric was `nudged` **twice** (Jul 4 22:15 + Jul 9 14:20) despite `escalate.ts`'s "at most once per shift" guarantee. Likely a re-opened-seat re-cycle or a manual nudge; possibly the once-guard not holding. Revisit if it recurs.
- **Nudge mental model (operator was confused):** a nudge IS an ask (Tier-2 direct re-ask via `assignPerson`); reads identically to a broadcast ask, only the trail distinguishes. System auto-nudges silent ghosters; declining excludes you (`escalate.ts:81`). The `/admin/at-risk` "needs you" SMS = operator alert to all active admins (DEC-095), never crew.
- **Prod debug tip:** MCP can't see the prod Neon project; use `psql "$(cat ~/.muster-prod-db)" -c "..."` for read-only prod queries (give the operator single-command, comment-free SQL — heredocs + inline comments confused copy-paste).
