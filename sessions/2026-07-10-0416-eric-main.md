---
session: 42
dev: eric
slug: main
branch: main
started: 2026-07-10T04:16:02Z
ended:
points:
pr_numbers: [334, 337, 339, 344, 346, 348, 349, 351, 353]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/d332f694-f986-426b-b037-5aa534a59c2d.jsonl
---

# Session 42 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Time-off surfaces — crew + admin add/remove/scan blackout dates (closes #332)

**Completed:**
- **Port:** added `removePtoWindow(id)` (`src/ports/repository.ts`) + both adapters + contract test (remove + idempotent double-remove). The issue's "no port change" was optimistic — no delete existed; mirrors `removeCredential`.
- **Domain** `src/crew/time-off.ts` (+`.test.ts`): `addTimeOff` (ISO-date + `start ≤ end`, returns result codes not throws), `removeTimeOff`, `ownsTimeOff` (crew authz), `buildAllTimeOff` (grouped/sorted, carries `archived`, skips no-FK orphans).
- **Crew** `/crew/time-off` (page+actions): "Days I'm off" add/remove, ownership-gated remove (forged id = silent no-op), crew-home link. DEC-009 line held (subtractive, not availability).
- **Admin** `/admin/time-off` (page+actions): view everyone + operator set/remove anyone (unrestricted); archived crew greyed+tagged. Admin-home link. Far-future end = indefinite/active-duty stopgap.
- Shared `fmtDateRange` in `app/lib/format.ts` (both surfaces). Half-days parked as #333.
- `npm run verify` = 917 unit tests + typecheck (core+app) + lint + build; e2e `time-off.spec.ts` 6/6 desktop+375px.

**Code review:** Solid. Two consistency fixes applied: duplicated formatter → extracted to format.ts; archived crew greyed in the admin scan list (DEC-096 revisit trigger), not just filtered from the add dropdown. **Flagged not fixed (pre-existing, systemic):** stateless crew sessions mean an archived crew member with a live cookie (≤14d) can still edit own windows — same gap as every crew surface (DEC-092 scopes revoke to admins); candidate for its own issue.
**PR:** [#334](https://github.com/mobiustripper42/muster/pull/334)
**Points:** 5
**Branch:** task/332-time-off-surfaces
**Opened at:** 2026-07-10T04:48:29Z

## Task 2: Ask history browse surface (closes #331 half 1)

**Completed:**
- **Domain** `src/admin/ask-trail.ts` (+`.test.ts`): `buildAskTrail(repo, filter)` — resolves each ask's seat→shift for date/vessel/role labels, derives 4 outcomes (accepted/declined/timed_out/waiting, matching the engine's `expireAsks` stamping), filters per crew/shift/day (day = trip date, not send date), newest-first. Pure over the port (escalation-trail idiom — no aggregate, no DDL, no port method). Handles no-FK orphans (null labels, still listed).
- **Surface** `app/(admin)/admin/asks/page.tsx`: admin-gated, no-JS GET filter form (crew dropdown + trip-date), deep-linkable `?shift=`, calm neutral outcome pills (all-shifts/DEC-042 style). Admin-home link. Re-ask/nudge = its own row → the 2026-07-09 double-nudge shows as two rows without SQL.
- `npm run verify` = 910 unit tests + typecheck (core+app) + lint + build; e2e `ask-trail.spec.ts` 4/4 desktop+375px.
- **Scope:** v1 = Ask rows only. Delivery-record half (Twilio SendResult persistence — needs migration + PII pass) split to **#336**.

**Code review:** Clean bill of health, no findings. Verified outcome derivation matches `expireAsks`, day-vs-send-date semantics, archived-crew name resolution (DEC-096), exactOptional discipline, admin gating.
**PR:** [#337](https://github.com/mobiustripper42/muster/pull/337)
**Points:** 5
**Branch:** task/331-ask-trail (off main; trivial admin/page.tsx link-list conflict expected vs #334 whichever merges 2nd)
**Opened at:** 2026-07-10T10:54:11Z

## Task 3: Import run — reconcile the skipped-rows count (5-but-3-shown)

**Completed:**
- Diagnosed an operator-reported import discrepancy (Jul 10 run: "5 skipped, only 3 shown"). Root cause: the run-detail headline sums two disjoint buckets — itemized `import.skipped` (per-row) + `mapSkipped` (count-only of BOOKED items dropped at the pull, `xola-pull.ts:167` keeps only `.length`) — but lists only the first. `app/(admin)/admin/import/run/[id]/page.tsx` Notice now breaks the count into both buckets so 3+2=5 reconciles. Copy-only.
- Prod verified (psql read): the 3 itemized skips (Jul 25 ×2 + Jul 28 ×1, all "Party Boats with Captain") have 0 events + 0 reservations — genuinely dropped, `unmappedResources: []` (no boat-map gap). By pipeline shape they're cancelled boat-less bookings (booked+boatless is intercepted earlier as mapSkipped). Jul 25 boated trips (brew-1/2/4 @ 7:30 PM) imported fine — Xola/Muster in sync.
- `npm run verify` = 905 unit tests + typecheck + lint + build. No import-run e2e harness (eyeball-verified per convention).

**Code review:** Self-reviewed (copy-only, 6-line diff).
**PR:** [#339](https://github.com/mobiustripper42/muster/pull/339)
**Points:** 1
**Branch:** task/import-skip-count-copy (off main)
**Opened at:** 2026-07-10T11:21:04Z

## Task 4: Surface booked trips with no boat as an import alert (closes #338)

**Completed:**
- Operator can now SEE a booked (paying) trip Xola hasn't boated — it can't crew a shift; previously invisible in the count-only `mapSkipped`. `xola-client` tags the booked-boatless skip with date/time + `category: "booked_no_boat"`; `xola-pull` classifies against the pulled window into `bookedNoBoat[]` (in-window = actionable, out-of-window = benign edge stays in the tally); summary (jsonb, no migration) + persistence carry it; run-detail renders a loud `tone="bad"` itemized alert above the benign skips.
- **Backward-compat:** normalized `bookedNoBoat` → `[]` in `toImportRun` (the central read seam, alongside `splitDaysChanged`/#206) so runs persisted pre-#338 don't 500.
- Extracted shared `<SkippedRowList>` (booked-no-boat + skip tally shared JSX). Boundary tests: inclusive window start/end + one-day-past.
- `npm run verify` = 909 unit tests + typecheck + lint + build. No import-run e2e harness (eyeball per convention).

**Code review:** 3 findings, all fixed — central guard (not ad-hoc call-site), window-boundary tests, shared list component.
**PR:** [#344](https://github.com/mobiustripper42/muster/pull/344) — **stacked on #339** (merge #339 first).
**Points:** 3
**Branch:** task/338-booked-no-boat (base task/import-skip-count-copy)
**Opened at:** 2026-07-10T14:13:40Z

## Task 5: Manifest Call/Text buttons per guest (operator ask)

**Completed:**
- Guest manifest rows now show ✆ Call / ✉ Text per phoned guest (seat-card tel:/sms: idiom), dropped the raw number (in hrefs + `title` for desktop hover). Shared component → operator cockpit + crew shift card both get it. `components/assignment/shift-manifest.tsx` + `aria-label="Manifest"`.
- Hoisted triplicated `tel`/`sms` helpers → `app/lib/format.ts` (seat-card, crew shift page, manifest all import it now).
- e2e asserts Call/Text hrefs for a phoned guest + none for phone-less. `verify` = 905 + e2e 4/4 (manifest + override) desktop/375px.

**Code review:** Clean; both cleanups applied (hoist helpers, title-restore the number for handler-less desktop).
**PR:** [#346](https://github.com/mobiustripper42/muster/pull/346)
**Points:** 2
**Branch:** task/manifest-call-text (off main)
**Opened at:** 2026-07-10T14:46:09Z

## Task 6: Guest intro-text preload on the manifest Text button (#345 Part A)

**Completed:**
- Manifest Text button now opens Messages with the guest number + the intro PRELOADED ("Hi, this is [sender first] with BrewBoat — taking you out at [time] today. Confirm pickup: Canal Basin Park near Flatiron Cafe. [maps pin]"). Kills the hand-typing.
- `src/crewapp/intro-text.ts` (+test): pure `buildIntroText` (first-name-only, tenantName param, empty-name fallback, literal "today"). `src/config/tenant.ts`: `PICKUP_LOCATION`/`PICKUP_MAP_URL` (hard-coded one spot; per-event later; env-overridable). `app/lib/format.ts`: `sms(phone, body?)` → `?&body=` cross-platform. Sender name resolved at each page's gate, threaded via `ShiftCockpit` prop + crew `Card` prop.
- `verify` = 909 + e2e 2/2 (Text href carries encoded intro). Answers from operator: fixed location + his maps pin `https://maps.app.goo.gl/A2vG7Q9LjKdZJpod9`, sender = whoever taps, Part A first.

**Code review:** Clean; both fixes applied — `tenantName` param (not hardcoded), operator name resolved once at the cockpit page (dropped a tripled getAdmin).
**PR:** [#348](https://github.com/mobiustripper42/muster/pull/348) — **stacked on #346** (merge #346 first). **Part A only — #345 stays OPEN for Part B.**
**Points:** 3
**Branch:** task/345a-intro-text (base task/manifest-call-text)
**Opened at:** 2026-07-10T16:31:29Z

## Task 7: All-crew "contacted" tracking on the manifest Text button (#345 Part B, closes #345)

**Completed:**
- Tapping a guest's Text records who texted them → manifest shows "✓ Texted by [first] · [time]" to every crew member on the shift (no double-texting/nobody missed).
- **Migration 0020 guest_contacts** (upsert-latest by reservation_id, no-FK, denormalized name). Entity + `recordGuestContact`/`listGuestContactsForShift` port + both adapters + contract test. `ManifestGuest` += reservationId + contact; `buildShiftManifest` attaches contacts (+unit tests).
- **First client island:** `GuestTextButton` (`"use client"`) — keepalive POST to `/api/guest-contact` on tap, THEN sms: opens (no-JS still opens Messages; progressive enhancement). API resolves WHO from session (unforgeable). `shiftId` threaded from crew page + cockpit. **DEC-097** records the exception.
- `verify` = 912 + e2e 4/4 (tap → 204 → ✓ on reload).

**Code review:** Clean, no code changes. Accepted tradeoff (in code + DEC-097): POST authorizes any signed-in subject (doesn't verify poster is on the shift) — worst case a false ✓ mark, no PII leak.
**PR:** [#349](https://github.com/mobiustripper42/muster/pull/349) — **stacked on #348** (merge #346→#348→#349). **Closes #345.**
**Points:** 5
**Branch:** task/345b-contacted-tracking (base task/345a-intro-text)
**Opened at:** 2026-07-10T17:16:52Z

## Task 8: Admin payroll hours report (closes #347)

**Completed:**
- Biweekly pay-period dropdown → each assigned crew member's estimated hours (DEC-041 committed window). Pay periods ported VERBATIM from sibling xola-tip-extractor (anchor 2026-01-05, env `PAY_PERIOD_ANCHOR`) so dates match date-for-date (found the sibling at `/home/eric/xola-tip-extractor/public/index.html`).
- `src/admin/pay-periods.ts` (currentPeriod/periodsForYear/periodLabel +test pins 5/25–6/7 etc); `committedMinutes` (shift-card.ts, DEC-041 span, midnight-safe); `buildPayrollReport` (required Confirmed seats only, no supernumerary, Cancelled skipped/Completed counted, deduped per shift +test); `/admin/payroll` surface (dropdown → crew·shifts·hours table). Pure read, NO migration.
- `verify` = 914 + e2e 2/2 (pick period → Quint+Hooper 5h10m).

**Code review:** 1 real bug fixed — per-seat sum double-counted when override seats one person twice on a shift → deduped per (shift,crew) +test. Plus period-param validation + comments.
**PR:** [#351](https://github.com/mobiustripper42/muster/pull/351) — off main, independent.
**Points:** 5
**Branch:** task/347-payroll-hours
**Opened at:** 2026-07-10T18:52:16Z

## Task 9: Notify assigned crew when an import changes their shift's trips (closes #350)

**Completed:**
- Bug: import adding a trip to an already-crewed shift didn't notify. Root cause: `formShifts` only recorded cancel/resurrection transitions, never "a trip on a shift you're still on changed." The relay (runXolaPull→forwardFormNotices→formNoticeChanges→forwardNotices) was wired; nothing to relay.
- `FormResult.changedCrew` (surviving shift whose scheduled trip set changed, diff-gated/idempotent, excludes cancel/resurrect/new). `AssignmentAction += "changed"`; formNoticeChanges maps it; forwardNotices body "your <date> - <vessel> shift changed - check the app" (GSM-7). No migration (action is text).
- **Code review caught scope creep:** formOneShift is shared with manual split/merge → was firing spurious "changed" texts on every split/merge. Fixed: gated behind `opts.notifyTripChanges`, passed ONLY by pullXola (import). +gate test. Split/merge keep their own notice story.
- `verify` = 909 unit + build. No e2e (rides the pre-existing shipped notice rails; new core seams each unit-tested).

- **Extended (same PR, operator's call):** split/merge commands now ALSO opt into the notice (was deferred). Split → side-A crew (shorter half) get "changed"; merge → surviving side-A crew (longer day) get "changed", dropped side-B keep "you're off". Each command passes `notifyTripChanges` (satisfies the "from the command, not the re-form" posture). +split/merge tests, verify=911.

**Code review:** 1 real scope-creep bug fixed (gate to opt-in flag); dual-side double-text accepted+documented; migration comment updated. Split/merge extension self-reviewed (flag flip + tests).
**PR:** [#353](https://github.com/mobiustripper42/muster/pull/353) — off main, independent. Retitled to cover import + split/merge.
**Points:** 5
**Branch:** task/350-import-change-notice
**Opened at:** 2026-07-10T19:32:13Z

**Next Steps:**
- **⚠️ #349 migration 0020 → apply to PROD by hand** before promoting (dev/test auto-migrate; prod only is manual). Ledger: prod's `_migrations` must have 0020 before the code ships.
- **⚠️ #348 needs a REAL-IPHONE check** before production: `sms:` body-prefill is fiddly across OS — tap Brody's Text on Eric's actual iPhone, confirm Messages opens with number + full text. `?&body=` is the knob if it misbehaves.
- **#345 Part B (still open):** all-crew "contacted" tracking — onClick records who texted which guest + when, shown on the manifest so no double-texting / nobody missed. Needs a JS island (record → open sms:), a migration (guest-contact events, no-FK), + a DEC for the client-JS exception (server-rendering-default). Design Qs: contacted granularity (per-reservation), display (✓ by whom/when).
- **#347 (payroll):** approx hours/week per crew from confirmed-seat call→shift-end windows, date-range filter. Pure read, no migration.
- Other open: #330 filter-shifts-by-crew, #321/#317 parked stack, #333 half-days, #335 stateless-crew authz, #336 delivery record.

**Context:**
- Import skip mechanics: `mapXolaOrders` drops booked+boatless items to `mapSkipped` (count-only); `importRecords`' itemized "no resolvable boat" is only reachable by non-booked (cancelled) items → the itemized skips are effectively cancelled boat-less bookings. `unmappedResources` (unknown boat, ACTION) is a separate audit field, empty this run.
- Prod reads this session via `psql "$(cat ~/.muster-prod-db)" -c "..."` (MCP can't see prod Neon). Confirmed working.
