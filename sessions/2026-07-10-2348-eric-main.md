---
session: 43
dev: eric
slug: main
branch: main
started: 2026-07-10T23:48:24Z
ended:
points:
pr_numbers: [356, 358, 359]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/c41c17db-8103-4501-b792-09594b3cf549.jsonl
---

# Session 43 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Admin-gate fallback — switch-up / sign-in instead of a dead end (closes #352)

**Completed:**
- Operator-reported (#352): signed in as crew, hitting an admin page gave a dead-end "You're signed out. Tap an operator magic link" with no path forward. Root cause: `subject.kind !== "admin"` conflated *no session* with *signed-in-as-crew* (every admin is also crew, DEC-092/093).
- **New** `components/admin/admin-signed-out.tsx` — shared async server component `<AdminSignedOut subject>` that branches: crew-who-is-active-admin → **Switch to admin →** button (reuses `switchToAdmin`); crew-not-admin → "This surface is Spink's" + link to `/crew`; no session → "Signed out" + **Sign in →** link to `/crew`.
- Replaced all **12** duplicated `SignedOut()` copies across `app/(admin)/admin/**/page.tsx` with the shared component; dropped the now-orphaned `Notice` import in `shift/[shiftId]`.
- e2e `admin-gate.spec.ts` — all 3 subject branches against `/admin/shifts`.
- `npm run verify` green (typecheck core+app, lint, 952 unit, build); e2e 3/3 desktop.

**Code review:** Clean — security boundary unchanged (escalation still server-gated in `switchToAdmin`, TOCTOU-safe), no orphaned imports, no missed pages. Nits: trailing newlines (fixed); consolidated per-page fallback titles/widths into one gate (deliberate DRY trade-off). Duplicated active-admin check vs crew/page.tsx noted for a future `isActiveAdminCrew` helper — not worth its own PR.
**PR:** [#356](https://github.com/mobiustripper42/muster/pull/356) — off main, independent.
**Points:** 3
**Branch:** task/352-admin-switch-fallback
**Opened at:** 2026-07-11T01:20:00Z

## Task 2: Shifts filters — 2 preset chips + filter-by-crew (closes #321, #330)

**Completed:**
- **#321:** added `2 weeks out` (days 8–15 = today+7..+14) and `30 days` (today..+29) preset chips to `/admin/shifts`, wired through `resolveWindow`/`filterParams`/`Filter`. Inside the existing horizon clamp. Label "2 weeks out" confirmable (issue flagged TBD).
- **#330 (@architect-gated, DEC-042 amendment):** crew-filter dropdown. Match by **id** in `deriveAllShifts` (`opts.crewMemberId`) against the raw seat's `assignedCrewMemberId` — name-match barred by no-FK posture; NO `AllShiftsSeat` DTO field added (in-core match). Seat read hoisted → short-circuits before per-shift derivation. Match set: state ∈ {Claimed, Confirmed}, required + supernumerary. **Window widens to today..+45 on a bare crew pick** (person-lookup JTBD — deviates from architect's −30 past bound: forward-only keeps the next shift on top). Presets still narrow. `crew` param threaded through every nav surface. No per-crew scoreboard (DEC-042 held).
- @architect consulted on #330 up front (operator's explicit request on the issue).
- `npm run verify` green (957 unit incl. 5 new crew-filter, typecheck core+app, lint, build); e2e `shifts-view` **7/7 desktop + 7/7 at 375px** (added shifts-view to the mobile allowlist — filter bar is now chip+dropdown-dense).
- DEC-042 amendments (#321, #330) recorded.

**⚠️ Operator judgment call (flagged in PR):** crew filter counts **Claimed (tentative) + Confirmed** — flip to Confirmed-only if wanted (1-line).
**Follow-up filed:** #357 — extract `Filter`/`ShiftRow` from page.tsx (now 876 lines, >4x the 200-line ceiling; kept out of this PR for reviewability).
**Code review:** Correctness clean — predicate, short-circuit (non-crew path + split-cut pre-scan intact), widen priority, param-threading all verified. Nits: file size (→#357), empty-state copy "in"→"for" (fixed), harmless opts-forward (left).
**PR:** [#358](https://github.com/mobiustripper42/muster/pull/358) — off main, independent. Shares `admin/shifts/page.tsx` with #356 but different regions (gate top vs filter) — merge any order.
**Points:** 8
**Branch:** task/330-321-shifts-filters
**Opened at:** 2026-07-11T01:35:00Z

## Task 3: Crew iCal calendar feed (closes #355)

**Completed:**
- Muster's **first persistent bearer credential** (DEC-098, @architect-gated). Crew mint a subscribable iCal URL; `GET /api/calendar/<token>.ics` (unauth, token=credential, 404 on miss) serves live `text/calendar` of their confirmed shifts → Google/Apple subscribe once (push, not pull).
- **Token:** hash-only (`sha256`), show-once (flash cookie), rotate-to-revoke. New table `0021_calendar_feeds` (one-per-crew PK, no FK per DEC-DATA-1); reuses magic-link `randomSecret`/`hashSecret`. `CalendarFeed` entity + 5 port methods + both adapters + contract test.
- **Feed** (`src/crewapp/calendar-feed.ts`): `buildCalendarFeed` (confirmed seats only, today−7d forward, DEC-041 committed window as instants) + `renderCalendar` (hand-rolled RFC-5545 — escaping + 75-octet folding, UTC instants = DEC-032 render exception, stable per-shift UID). **PII boundary:** skeleton only (vessel/role/times/dock/co-crew FIRST names) — never guest manifest/phones.
- **UI** `/crew/calendar` (mint/show-once/regenerate/turn-off, no-JS actions) + crew-home link.
- DEC-098 recorded. `npm run verify` green (963 unit + build); postgres contract 61 (migration + SQL); e2e `calendar-feed` 4/4 desktop + 375px.

**⚠️ DEPLOY:** migration **0021 must be hand-applied to prod** before ship (inert until then; mint/route 500 pre-migration). Product calls flagged in PR: confirmed-only, pax count omitted.
**Code review:** Security-focused — token model, PII boundary, ICS escaping/folding/instants, adapter parity, no-oracle 404 all clean. **1 real bug fixed:** flash cookie set on `path=/crew/calendar` but deleted on default `path=/` → show-once didn't clear → now deletes on matching path + e2e reload proves it. Touch-failure now logged. `origin()`/`baseUrl()` dup left (base-url.ts security-sensitive).
**PR:** [#359](https://github.com/mobiustripper42/muster/pull/359) — off main, independent.
**Points:** 13
**Branch:** task/355-ical-feed
**Opened at:** 2026-07-11T02:05:00Z

**Next Steps:**

**Context:**
