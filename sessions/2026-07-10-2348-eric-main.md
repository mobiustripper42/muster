---
session: 43
dev: eric
slug: main
branch: main
started: 2026-07-10T23:48:24Z
ended:
points:
pr_numbers: [356]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/c41c17db-8103-4501-b792-09594b3cf549.jsonl
---

# Session 43 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Admin-gate fallback — switch-up / sign-in instead of a dead end (closes #352)

**Completed:**
- Operator-reported (#352): signed in as crew, hitting an admin page gave a dead-end "You're signed out. Tap an operator magic link" with no path forward. Root cause: `subject.kind !== "admin"` conflated *no session* with *signed-in-as-crew* (every admin is also crew, DEC-092/093).
- **New** `components/admin/admin-signed-out.tsx` — shared async server component `<AdminSignedOut subject>` that branches: crew-who-is-active-admin → **Switch to admin →** button (reuses `switchToAdmin`); crew-not-admin → "This surface is Spink's" + link to `/crew`; no session → "Signed out" + **Sign in →** link to `/crew`.
- Replaced all **12** duplicated `SignedOut()` copies across `app/(admin)/admin/**/page.tsx` with the shared component; dropped the now-orphaned `Notice` import in `shift/[shiftId]`.
- e2e `admin-gate.spec.ts` — all 3 subject branches against `/admin/shifts`.
- `npm run verify` green (typecheck core+app, lint, 952 unit, build); e2e 3/3 desktop.

**Code review:** Clean — security boundary unchanged (escalation still server-gated in `switchToAdmin`, TOCTOU-safe), no orphaned imports, no missed pages. Nits: trailing newlines (fixed); consolidated per-page fallback titles/widths into one gate (deliberate DRY trade-off). Duplicated active-admin check vs crew/page.tsx noted for a future `isActiveAdminCrew` helper — not worth its own PR.
**PR:** [#356](https://github.com/mobiustripper42/muster/pull/356) — off main, independent.
**Points:** 3
**Branch:** task/352-admin-switch-fallback
**Opened at:** 2026-07-11T01:20:00Z

**Next Steps:**

**Context:**
