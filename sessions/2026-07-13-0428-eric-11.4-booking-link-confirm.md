---
session: 48
dev: eric
slug: 11.4-booking-link-confirm
branch: task/11.4-booking-link-confirm
started: 2026-07-13T04:28:28Z
ended:
points:
pr_numbers: [401, 403, 405]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/9cca4acb-4918-445d-bdf5-2bda645a3415.jsonl
---

# Session 48 — 11.4-booking-link-confirm

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 11.4 — booking-link generation + confirmation emit (closes #370)

**Completed:**
- **Capability URL (DEC-119):** `src/reservations/booking-link.ts` (+test) — **stateless HMAC** "manage my booking" URL, `token = base64url(HMAC-SHA256(RESERVATION_LINK_SECRET, "reservation-link:v1:" + reservationId))`, `…/reservations/manage?r=<id>&t=<token>`. Dedicated secret (not `SESSION_SECRET`), no stored row, no migration, constant-time verify paired for P12. Re-openable (bearer), not single-use like the crew magic-link.
- **Guest recipient:** `src/ports/channel.ts` (+test) — `Recipient` split into `CrewRecipient | GuestRecipient` + `requireCrewId`; the crew "always has a crewMemberId" invariant stays compiler-enforced (guest never smuggled in via an optional field). Four crew adapters (twilio/web-link/outbox-notification/outbox-notice) narrow through it.
- **Confirmation emit:** `src/reservations/booking-confirmation.ts` (+test) — compose body with the link inline; best-effort email + SMS, whichever channels exist. `booking-webhook.ts` fires `sendConfirmation` on **`booked` only** (never the redelivered `already` → no re-text). **Structurally best-effort at both layers** (core webhook guard + app-wiring try/catch) so a confirmation throw never 500s a committed booking → no Stripe retry storm.
- **Wiring:** `app/lib/booking-confirmation.ts` + `app/api/webhooks/stripe/route.ts` — dark-until-configured, `MESSAGING` kill-flag guard (#390 future-proof).
- **@architect** design pass first (proceed-with-changes: discriminated union, booked-only, observable failure, DEC head-on); all folded in. `npm run verify` green (**1044 tests + build**).

**Code review:** 1 structural finding (best-effort enforced by only half the call chain) — **fixed** at both layers + regression test. Rest clean (union narrowing, booked-only gate, HMAC scheme all verified).
**⚠️ DEPLOY:** new env **`RESERVATION_LINK_SECRET`** must be set in Vercel before Muster booking goes live (unset = confirmation loud no-op, booking still succeeds). Migrations `0021`–`0024` still owed on next push.
**RELEASE GATE:** the manage-page verifier is **P12** — emitted links 404 until then; the "Book Now" flag stays off-for-Muster until P12 (DEC-119).
**PR:** [#401](https://github.com/mobiustripper42/muster/pull/401) — into **`feature/reservations`** (DEC-111). Base≠main → CI skipped; `verify` local.
**Points:** 3
**Branch:** task/11.4-booking-link-confirm
**Opened at:** 2026-07-13T15:30:00Z

## Task 2: Phase 11.7 — verify Muster-native reservations surface on the manifest (closes #373)

**Completed:**
- **Verification, not a rebuild** — the manifest read path was already source-agnostic (DEC-012). Scoped it first (`buildShiftManifest` `src/crewapp/shift-card.ts:229` filters by `status==="booked"` only, never `source`; both repo adapters select by `event_id` with no source predicate; shift-forming groups all events regardless of source; no write-back path exists). **No production change** — only `src/crewapp/shift-card.test.ts` touched.
- **+2 tests** + a `seedWithMusterEvent()` fixture: a booked `source:'muster'` reservation surfaces on `buildShiftManifest` (name/party/phone, `paxTotal` counts it) AND on the full `buildShiftCard` (sorted last by departure). Regression guard: adding a xola-only filter later fails both.
- `npm run verify` green (**1027 tests + build**).

**Code review:** Clean (scoped) — tests are meaningful (exercise the real unfiltered path, would fail if a source filter were added); arithmetic + sort correct; fixture matches `seed()` conventions.
**PR:** [#403](https://github.com/mobiustripper42/muster/pull/403) — into **`feature/reservations`** (DEC-111). Base≠main → CI skipped; `verify` local.
**Points:** 2
**Branch:** task/11.7-manifest-hinge
**Opened at:** 2026-07-13T15:52:00Z

## Task 3: Phase 11.5 — liability-waiver consent on the Muster-sold reservation (closes #371)

**Completed:**
- **Hard gate (DEC-110):** `createBookingCheckout` (`src/reservations/create-booking-checkout.ts`) rejects `waiver_required` before any Stripe session — no consent, no charge, so no paid reservation can exist without a recorded waiver. It's the sole Stripe-session call site (@code-review confirmed).
- **Stored:** `waiverConsentAt` + `waiverVersion` on `Reservation` (`src/domain/entities.ts`); migration `db/migrations/0025_reservation_waiver.sql` (two nullable text cols, additive/inert). Both absent on Xola/pre-11.5.
- **Server-authoritative:** version from `WAIVER_TERMS_VERSION` config (`src/config/tenant.ts`, env-overridable, not form-trusted); timestamp = the submit/agreement instant. Carried form → checkout metadata → webhook → `writeBooking` → reservation. Postgres adapter: both insert sites (incl. the 11.3 whole-boat CAS — `$10`/`$11` appended, mutex untouched) + row mapper.
- **Form:** required checkbox + `WAIVER_TERMS_URL` link on `/book`; server-side enforcement in `actions.ts` beyond the HTML `required`.
- **Tests:** `verify` green (**1030**) + `test:pg` green (**80, real Postgres** — 0025 applies + waiver columns round-trip via the contract). A rejection test asserts **zero Stripe sessions** created without consent.

**Code review:** Clean bill of health — gate integrity, CAS param numbering, server-authoritative version, type narrowing, migration all verified.
**⚠️ DEPLOY:** migration `0025` owed to prod (alongside 0021–0024). `WAIVER_TERMS_URL`/`_VERSION` optional env (safe defaults; bump version when terms text changes).
**PR:** [#405](https://github.com/mobiustripper42/muster/pull/405) — into **`feature/reservations`** (DEC-111). Base≠main → CI skipped; `verify` + `test:pg` local. No new DEC (DEC-110 governs).
**Points:** 2
**Branch:** task/11.5-waiver
**Opened at:** 2026-07-13T16:05:00Z

**Next Steps:**
- **Merge #401 (11.4) + #403 (11.7) + #405 (11.5) into `feature/reservations`** (independent branches; only #401 & #405 both touch `booking-webhook.ts` but in different regions — 3-way merges clean). Remaining Phase 11: 11.2b (deposit balance-link — NOT gated), 11.8 (go-live + runbook, flag flip — the feature→main gate).
- **Prod migrations owed:** `0021`–`0025` on next push (verify which already applied): `DATABASE_URL=<neon-direct> npm run db:migrate`.
- Before Muster go-live: set `RESERVATION_LINK_SECRET` (11.4) in Vercel; P12 manage page is the release-gate dep for the link.
- **Set `RESERVATION_LINK_SECRET` in Vercel** before any Muster go-live; the P12 manage page is the release-gate dependency for the exit gate.
- **DEC-119** (this task) vs **DEC-118** (`task/400-crew-audit-log`, Session 49) — both append at the DECISIONS.md tail; expect a merge-train conflict there, resolve by keeping both.

**Context:**
- **Reclaimed from a wrong abandon:** a parallel window (opening Session 49, crew-audit-log) marked this session `abandoned` as "stranded" — it wasn't; this window did Phase 11.4. Reopened + re-slugged from the stale `392` (which Session 47 actually shipped as #398/#399).
- **Opened cold on `task/392`** on the /its-alive recommendation, then redirected by the operator to **Phase 11 → 11.4**. Work built in the `/home/eric/muster-reservations` worktree on `task/11.4-booking-link-confirm` (off `feature/reservations`); the primary checkout stayed on `task/392`.
- **DEC collision fixed:** `task/400-crew-audit-log` (Session 49) claims **DEC-118**; this task's DEC renumbered to **DEC-119** to avoid the merge-train clash. Main is at DEC-117.
- **Concurrent sessions:** 47 (pwa-screenshots, closed), 49 (crew-audit-log, open). Session points advisory per cross-machine reality.
- **Orphan branch** `claude/great-heisenberg-04ddjl` (1 commit, no PR) — left as-is by operator decision, revisit later.
