---
session: 50
dev: eric
slug: main
branch: main
started: 2026-07-13T23:58:51Z
ended:
points:
pr_numbers: [417, 418, 420]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/4dfe60fc-ca75-47fc-9254-f0d73a91a5fc.jsonl
---

# Session 50 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: #415 — hide cancelled/completed shifts on crew My-shifts + asks

**Completed:**
- Prod-diagnosed phantom: `crew-eric-stoffer` held a `Confirmed` seat on shift `shift-vessel-brew-3-2026-07-18` whose state is `Cancelled` — showed on `/crew` My shifts, absent from admin board. Confirmed live via prod psql read.
- Root cause: `src/crewapp/crew-view.ts` was the ONLY read surface not filtering `shift.state === Cancelled|Completed` (board, calendar-feed:86, at-risk:212, lean all do). A Xola re-import cancels the shift (`form-shifts.ts:304-310`) but DEC-084 leaves the seat → orphan.
- Fix: guard added to both the My-shifts loop and the open-asks loop in `crew-view.ts`.
- Test `src/crewapp/crew-view.test.ts`: Confirmed + Claimed seats on a Cancelled shift, and a live ask on a Cancelled shift, all absent; live ones untouched. `npm run verify` green (1029).
- No prod write; orphaned seat stays but invisible (consistent with app's persisted-seat model). Calendar feed already filtered.

**Code review:** 1 finding (missing Claimed-on-cancelled test) — addressed. Otherwise clean; guards match the four sibling surfaces.
**PR:** [#417](https://github.com/mobiustripper42/muster/pull/417)
**Points:** 2
**Branch:** task/crew-hide-cancelled
**Opened at:** 2026-07-14T12:45:57Z

## Task 2: #416 — admin Shifts "Show cancelled" toggle (additive, default off)

**Completed:**
- Follow-up to #415: operator needs to SEE cancelled shifts to diagnose crew phantoms. `deriveAllShifts` gains `opts.includeCancelled` — keeps `Cancelled` (flagged `cancelled:true`), `Completed` always excluded. `src/admin/all-shifts.ts` + `AllShiftsRow.cancelled`.
- Page `app/(admin)/admin/shifts/page.tsx`: reads `?cancelled=1`, no-JS toggle href preserving window/crew/mode/sel, `filterParams` persistence. Toggle chip in `components/admin/shifts-filter.tsx` (`☐/☑ Show cancelled`). Greyed row (`opacity-60`) in `shift-row.tsx`.
- Additive chosen over cancelled-only mode (keeps DEC-042 calm default). DEC-042 amended in `docs/DECISIONS.md`.
- Test `src/admin/all-shifts.test.ts`: fold-in + `state === "Cancelled"` + Completed still excluded. `npm run verify` green (1029).

**Code review:** 2 real bugs caught + fixed — (1) `resolveShiftStateOnRead` folds SEAT states, never yields Cancelled → a dead row would relabel to a live state incl. a bogus At-Risk "needs attention" link; short-circuited `state` to `Cancelled`. (2) Edit-mode Split/Merge + split-suggestion reachable on cancelled rows (`splitShift`/`mergeShift` don't guard state) → gated on `!row.cancelled`.
**PR:** [#418](https://github.com/mobiustripper42/muster/pull/418)
**Points:** 3
**Branch:** task/admin-show-cancelled
**Opened at:** 2026-07-14T12:57:07Z

## Task 3: #419 — crew My-shifts + ask card show call→end window (not departure→end)

**Completed:**
- Bug: My-shifts list + ask card showed raw departure as the START but DEC-041 shift end (with teardown) as the END — mismatched, and disagreed with the detailed shift card (which shows call→back).
- Fix `src/crewapp/crew-view.ts`: helper `departureWindow`→`committedWindowFor` returns `{callTime, shiftEndTime}` (committedWindow already computed callTime, was discarding it). `MyShiftView`/`OpenAskView` `.departureTime`→`.callTime`. Render in `app/(crew)/crew/page.tsx` (both `timeWindow` + ask card).
- **User decision:** BOTH surfaces use call time (not just My-shifts) for full consistency. 11:00 departure → shows 10:15 (−45m call lead).
- Tests updated (15:00→14:15 ask; 11:00→10:15 my-shifts). `npm run verify` green (1028).

**Code review:** Clean bill of health — rename mechanical + complete, both-or-neither window invariant holds, no stray departureTime consumers, per-event departure still on the shift card manifest (separate path).
**PR:** [#420](https://github.com/mobiustripper42/muster/pull/420)
**Points:** 2
**Branch:** task/crew-shift-window
**Opened at:** 2026-07-14T13:43:52Z

**Next Steps:**
- **3 PRs open, all green:** #417 (#415 crew phantom), #418 (#416 admin toggle — incl. below-the-fold move), #420 (#419 crew call→end window). Merge any order. #417 + #420 both touch `crew-view.ts` in different regions — should auto-merge; trivial if GitHub flags it.
- Eyeball both at 375px / desktop on mill-dev (steps in each PR body). #418 needs a cancelled shift in the dev window to see the greyed row (PR gives a psql one-liner to force one).
- Orphaned prod seat `seat-shift-vessel-brew-3-2026-07-18-role-captain-1` stays (Confirmed on a Cancelled shift) — invisible everywhere after #417; not cleaned up by design.
- **#413 closed** by user (My-shifts already future-only, `crew-view.ts:189`; user was mistaken).
- **Pilot-data cleanup PENDING (prod, user to run):** bottom crew scores polluted by a 2026-06-30 pilot seed batch. Clean cut = delete `reliability_events` where `timestamp < '2026-07-08' and crew_member_id <> 'system'` (~214 rows across Brendan/William/Brandon/Eric). Backup CSV first, then delete, then `db:crew rank`. **Herman Bowman is NOT pilot** (all post-0708, genuinely ignores every ask → real -57, leave him). Commands given in-session.
- **#411** parked (recurring weekday time-off — real feature: migration + oracle rule + 2 UIs + DEC + @architect; a backend-only CLI-set slice is the quick first increment when picked up). Low-pri bundle (#301/#376/#279) still deferred.

**Context:**
