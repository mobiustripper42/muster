---
session: 52
dev: eric
slug: main
branch: main
started: 2026-07-14T17:30:19Z
ended:
points:
pr_numbers: [423]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/4fcd0fa0-1fea-457c-b31a-ee8ad4fd0419.jsonl
---

# Session 52 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: #422 — refreshShiftState resurrects Cancelled/Completed shifts on seat mutation

**Completed:**
- Prod firefight: two cancelled shifts had self-resurrected and fired asks to real crew. **brew-3/18th** — operator (eric) bailed a leftover Confirmed seat → Cancelled→Filling → 17 asks. **brew-1/19th** — auto-resurrected: a crew member tapped "no" on a leftover ask → `recordResponse`→`refreshShiftState` uncancelled it → tick fired a fresh 6-person mate broadcast.
- Root cause: `refreshShiftState` (`src/asks/ask-loop.ts:48`) persists `deriveShiftState(seats)` — a pure seat-fold that never yields Cancelled — with no lifecycle guard, so any seat mutation on a cancelled shift (DEC-084 keeps seats) overwrites `Cancelled` with a live state. Write-path twin of #415 (read-path).
- Fix: early-return guard on `shift.state === "Cancelled" || "Completed"`. Single choke point → all 18 seat-mutation callers (bail/decline/claim/manual-override/split/merge). Importer's own resurrect path (`form-shifts.ts:354`, #244) writes state directly, unaffected.
- Tests `src/asks/ask-loop.test.ts`: 3 new (crew decline → 19th, operator bail → 18th, Completed inert). **Confirmed red without the guard, green with it.** `npm run verify` green (1033).
- **Prod already repaired by hand** (not via this PR): both shifts set `state='Cancelled'`, 17 live asks on brew-3 deleted. brew-1 re-imported to Cancelled (user); its **6 live asks NOT yet cleared** — see Next Steps.

**Code review:** Clean bill of health (@code-review) — guard placement complete, single choke point verified, no importer collision, bail test exercises the full re-ask branch. No changes requested.
**PR:** [#423](https://github.com/mobiustripper42/muster/pull/423)
**Points:** 2
**Branch:** task/refresh-state-cancelled-guard (linked worktree ../muster-cancel-guard)
**Opened at:** 2026-07-14T18:13:20Z

**Next Steps:**
- **Engine is PAUSED** (user paused it during the firefight). Un-pause after #423 merges + deploys.
- **brew-1/19th still has 6 live mate asks** (re-import cancelled the shift but doesn't clear asks). Until #423 deploys, a "no" tap re-resurrects it. Clear them: `delete from asks a using seats s where a.seat_id=s.id and s.shift_id='shift-vessel-brew-1-2026-07-19' and a.responded_at is null;`
- **Reliability review (user-requested, after fix deploys):** the phantom asks may have logged `ask_ignored`/`ask_declined` reliability_events against REAL crew for cancelled shifts. Audit `reliability_events` for the affected crew on brew-3/18th + brew-1/19th, back these out. Distinct from the S50 test-account reset (that was simulation data; this is real-crew pollution from a bug).
- **`/kill-this` bug:** puts issue ref in PR *title* only, not `Closes #N` in the body → GitHub never auto-closes. Why #415/#416 stayed open. Fix the skill (seeds-managed → `/push-seeds` candidate) + retro-close #415/#416 by hand.

**Context:** Concurrent with open Session 51 (empty, another window live per user). Sibling worktrees live on Phase 11 (11.1/11.2b/11.3) — this task kept to `src/asks/` to avoid their lanes. Fix built in linked worktree `../muster-cancel-guard` (node_modules symlinked from main).
