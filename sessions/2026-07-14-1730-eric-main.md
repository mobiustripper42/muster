---
session: 52
dev: eric
slug: main
branch: main
started: 2026-07-14T17:30:19Z
ended: 2026-07-15T02:14:44Z
points: 18
pr_numbers: [423, 427, 429, 430, 433, 435]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/4fcd0fa0-1fea-457c-b31a-ee8ad4fd0419.jsonl
---

# Session 52 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: #422 — refreshShiftState resurrects Cancelled/Completed shifts on seat mutation

**Completed:**
- Prod firefight: two cancelled shifts had self-resurrected and fired asks to real crew. **brew-3/18th** — operator (eric) bailed a leftover Confirmed seat → Cancelled→Filling → 17 asks. **brew-1/19th** — auto-resurrected: a crew member tapped "no" on a leftover ask → `recordResponse`→`refreshShiftState` uncancelled it → tick fired a fresh 6-person mate broadcast.
- Root cause: `refreshShiftState` (`src/asks/ask-loop.ts:48`) persists `deriveShiftState(seats)` — a pure seat-fold that never yields Cancelled — with no lifecycle guard, so any seat mutation on a cancelled shift (DEC-084 keeps seats) overwrites `Cancelled` with a live state. Write-path twin of #415 (read-path).
- Fix: early-return guard on `shift.state === "Cancelled" || "Completed"`. Single choke point → all 18 seat-mutation callers (bail/decline/claim/manual-override/split/merge). Importer's own resurrect path (`form-shifts.ts:354`, #244) writes state directly, unaffected.
- Tests `src/asks/ask-loop.test.ts`: 3 new (crew decline → 19th, operator bail → 18th, Completed inert). **Confirmed red without the guard, green with it.** `npm run verify` green (1033).
- **Prod already repaired by hand** (not via this PR): both shifts set `state='Cancelled'`, 17 live asks on brew-3 deleted. brew-1 re-imported to Cancelled (user); its **6 live asks NOT yet cleared** — see Next Steps.

**Code review:** Clean bill of health (@code-review) — guard placement complete, single choke point verified, no importer collision, bail test exercises the full re-ask branch. No changes requested.
**PR:** [#423](https://github.com/mobiustripper42/muster/pull/423)
**Points:** 2
**Branch:** task/refresh-state-cancelled-guard (linked worktree ../muster-cancel-guard)
**Opened at:** 2026-07-14T18:13:20Z

## Task 2: #411 — recurring weekday time-off, backend + CLI slice (DEC-119)

**Completed:**
- Per-crew recurring weekday blackout ("never works Sundays"), subtractive alongside PtoWindow (DEC-009). @architect ratified the model first (column not table; `not_recurring_off` sibling rule in BOTH evaluators; weekday = pure string math on already-vessel-local `shift.date`, no tz read).
- `CrewMember.weekdaysOff?: number[]` (Mon=0…Sun=6), stored `weekdays_off jsonb default '[]'` (migration `0025`), mirroring `ratings`. `mondayZeroWeekday` promoted `derive.ts`→`config/tenant.ts`.
- Eligibility rule `not_recurring_off` in `evaluateCandidate` + `evaluateTraineeCandidate` (`src/oracle/eligibility.ts`) — covers ask/claim/browse (shared choke point) with zero new repo fetch.
- Port + both adapters: targeted `updateCrewWeekdaysOff` (DEC-094-safe). Optional-and-omitted-when-empty (like email); absence round-trips (shared contract test).
- CLI `db:crew days-off`: **roster (no id)** / show (id) / `--days=sun,sat` / `--clear`, dedupe+sort, all-7 warning. (User asked for the list form mid-build — added.)
- DEC-119 written. `npm run verify` green (**1047**). Built in linked worktree `../muster-411`.
- **Existing PtoWindow time-off VERIFIED untouched** (user asked): diff changes no PTO logic — `not_recurring_off` is an independent sibling gate; 0025 only adds a crew column.

**Code review:** Clean bar one non-blocking note — `updateCrewWeekdaysOff` lacks 0–6 validation below the CLI (CLI's `parseDays` guarantees range; no current caller can pass bad data; fails open harmlessly). Follow-up if an admin API caller lands.
**PR:** [#427](https://github.com/mobiustripper42/muster/pull/427) — **part of #411** (not a close; crew UI #426 + admin UI remain)
**Points:** 5
**Branch:** task/411-recurring-weekday-off (linked worktree ../muster-411)
**Opened at:** 2026-07-14T19:52:56Z

## Task 3: #425 — reliability retune, reward responsiveness + bail floor/ramp (DEC-120)

**Completed:**
- @architect pass first (both open dials). Weights (`src/oracle/reliability-score.ts` `DEFAULT_WEIGHTS`): `ask_accepted` +1→+2, `ask_declined` 0→+1 (reverses decline-neutral DEC-008/025 — In>Out>silence), `shift_bailed` −5→−3, `bailLatenessPerHour` −0.5→**−0.05**. `ask_ignored` unchanged −3.
- **@code-review caught a real bug:** architect's ramp assumed 48h; real `STAFFING_HORIZON_LEAD_DAYS` default is **7 days (168h)**, so −0.2 (my first cut) drove a zero-notice bail to −36.6, below `no_show (−15)`. Recalibrated to −0.05 (zero-notice ≈ −11.4, above floor). Added a **floor-guard test** composing real `bailLatenessMs` at the default horizon — the guard that would've caught it.
- DEC-120 (`docs/DECISIONS.md`, amends DEC-008 + DEC-028); SPEC §1.4 cross-ref; FUTURE_IDEAS park (horizon-coupled ramp, via @ideas). Tests: 2 reversed-premise rewrites + floor guard. `npm run verify` green (1048).
- **Caught a session-log hazard:** `/kill-this`'s `grep -l status:open | head -1` picked the stale concurrent **Session 51** (1619); logged to the correct Session 52 by hand.

**Code review:** Caught the bail-ramp/horizon bug (headline) + doc/SPEC drift — all fixed. Unit-test rewrites confirmed numerically correct.
**PR:** [#429](https://github.com/mobiustripper42/muster/pull/429)
**Points:** 3
**Branch:** task/425-reliability-retune (main checkout, not a worktree)
**Opened at:** 2026-07-14T20:51:40Z

## Task 4: #426 — crew UI for recurring weekday time-off (self-service)

**Completed:**
- New `/crew/days-off` (`app/(crew)/crew/days-off/page.tsx` + `actions.ts`): 7 no-JS checkboxes (Mon=0…Sun=6) pre-checked from `weekdaysOff`, one Save → whole-set replace via DEC-094-safe `updateCrewWeekdaysOff`. Unchecked submit nothing → uncheck-all = clear. Subtractive (DEC-009). Sibling link added on `/crew/time-off` (the hub).
- Server-rendered, no-JS (DEC-026), mobile-primary (DEC-085) — mirrors the time-off page. **Targeted e2e passed** (`e2e/days-off.spec.ts`, desktop): check→save→persist, uncheck-all→clear. `npm run verify` green (1048).
- **@ui-reviewer 9/10 caught a real divider bug** (`last:border-b-0` on the label matched every row → no dividers); moved to `<li>`, dropped redundant h2 + dead checkbox classes (fix commit 27494d0). @code-review clean.
- **Checkout got yanked to `feature/reservations` mid-`/kill-this`** (concurrent/cross-machine session, not me) — my commit was safe on origin; applied the UI fix in a throwaway worktree (`../muster-426fix`) to avoid disturbing the reservations checkout, then removed it.

**Code review:** @code-review clean; @ui-reviewer 9/10 (divider bug + 2 minors, all fixed).
**PR:** [#430](https://github.com/mobiustripper42/muster/pull/430) — **part of #411** (admin UI still under #411).
**Points:** 3
**Branch:** task/426-crew-days-off-ui
**Opened at:** 2026-07-14T21:37:45Z

## Task 5: #414 — crew/open filter default 7 Days, presets 2 Weeks / 30 Days

**Completed:**
- Operator wanted the `/crew/open` pull-surface filter to default to a **week**, not "This weekend." Replaced presets in `app/(crew)/crew/open/page.tsx`: default **7 Days** (`[today, today+7]`), + **2 Weeks** (`range=2w`), + **30 Days** (`range=30d`). Dropped "This weekend" (default) and "Next 4 weeks" (`4w`). `RangeLabel`/`resolveRange`/`backHref` updated, `weekendRange` helper removed.
- `e2e/crew-open.spec.ts` rewritten to assert the new preset set (old ones gone), robust to the today+7 boundary. `npm run verify` green (1048); **crew-open e2e green (12, desktop+mobile)**.
- **PR body carries `Closes #414`** (dogfooding the auto-close fix — first PR this session to put the keyword in the BODY, not just the title).
- @code-review clean bar one stale doc comment (fixed). Worked directly in `~/muster` (no worktree — per new standing preference [[work-in-main-checkout-not-worktrees]]).

**Code review:** @code-review clean; `resolveRange`/`backHref` total for all labels, stale `range=4w` falls through to 7d default harmlessly, 30d inside the [today,today+45] clamp.
**PR:** [#433](https://github.com/mobiustripper42/muster/pull/433) — Closes #414.
**Points:** 2
**Branch:** task/414-crew-open-filter
**Opened at:** 2026-07-15T01:15:57Z

## Task 6: #434 — admin/shifts 'Split candidates' filter

**Completed:**
- Operator wanted the split-suggestion candidates (long mid-day gap days) easy to see. They were already shown per-row (muted "could be two shifts" advisory + Edit→Split), but not scannable. Added a **'Split candidates' toggle next to 'Show cancelled'** on `/admin/shifts` — narrows rows to `splitSuggestion != null && split == null && !cancelled`. No-JS, param-preserving (mirrors #416); empty-state + count copy adapt.
- Files: `app/(admin)/admin/shifts/page.tsx` (`?split` param, filterParams persistence, row filter, splitHref, copy), `components/admin/shifts-filter.tsx` (toggle chip + preservation), `e2e/all-shifts.spec.ts`.
- Discussed the deeper option (auto-split on import) — declined for now: import forms one shift per vessel-day by design; splitting stays operator-driven (suggest + manual Split). Auto-split-on-import is a heavier @architect call (DEC-083 re-import idempotency) — NOT filed, left as the alternative in #434's body.
- `npm run verify` green (1048); all-shifts e2e green (4, incl. the toggle against the seeded gappy "Barrel" day — one caption assertion needed disambiguating from the toggle label via the `·` separator).

**Code review:** @code-review clean bill of health (param round-trips every nav path, merge-side logic unaffected, toggle-flip mirrors cancelledHref, copy plural-correct).
**PR:** [#435](https://github.com/mobiustripper42/muster/pull/435) — Closes #434.
**Points:** 3
**Branch:** task/admin-split-candidates-filter
**Opened at:** 2026-07-15T01:47:06Z

**Next Steps:**
- **#428 filed:** `no_show (−15)` weight has NO emitter — the floor is theoretical until a no-show can be recorded. Related to DEC-120.
- **#427 migration `0025` must be applied to prod out-of-band** before the prod app reads `weekdays_off` (metadata-only add-column, no backfill). Also apply to the dev DB before the CLI works locally.
- **#426 (crew UI for recurring days-off)** — scheduled for end of THIS session per user.
- **#411 admin UI** (set another crew member's days) — the 2nd UI from the issue, still under #411.
- Follow-up from #427 review: add a shared 0–6 validator on `updateCrewWeekdaysOff` if an admin-API caller appears.
- **#423 SHIPPED: promoted main→production as v1.0.10** (manual bump — package.json + CHANGELOG + tag v1.0.10 on main, then ff-merge to production). Migration-ledger gate passed (prod _migrations 0001–0024 = repo, no drift). **Verify deploy:** prod `<VersionTag/>` reads v1.0.10, then **un-pause the engine** (user paused it during the firefight). Once v1.0.10 is live the guard holds, so brew-1's 6 leftover asks are inert (user: don't care about them, left in place).
- **#424 filed (@architect pass — ask distribution: bail/import/remove):** blast-vs-drip is keyed on seat *provenance* (bail→instant blast to whole pool, fresh import→15-min drip), not *time-to-trip*. Want it urgency-driven with a **tunable ~3-day** blast window (at-risk board bails SHOULD blast; weeks-out bails should drip). Unify bail/vacate/import onto the tick's urgency-aware path.
- **#425 filed (@architect pass — reliability retune):** DECIDED In +1→+2, Out 0→+1 (reverses DEC-008/025 decline-neutral; reward responsiveness, completion +5 still dominates). OPEN: silence −3 relative to positive Out; early-bail relief (bail flat −5 with no good-notice relief below −5). Both #424/#425 go to @architect before coding.
- **`/kill-this` bug:** puts issue ref in PR *title* only, not `Closes #N` in the body → GitHub never auto-closes. Why #415/#416 stayed open. Fix the skill (seeds-managed → `/push-seeds` candidate) + retro-close #415/#416 by hand.
- (Dropped: the reliability *cleanup* of the 6 phantom events — user doesn't care about them; the retune #425 is the real interest.)

**Context:** Concurrent with open Session 51 (empty, another window live per user). Sibling worktrees live on Phase 11 (11.1/11.2b/11.3) — this task kept to `src/asks/` to avoid their lanes. Fix built in linked worktree `../muster-cancel-guard` (node_modules symlinked from main).
