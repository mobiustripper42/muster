---
session: 54
dev: eric
slug: main
branch: main
started: 2026-07-15T04:31:54Z
ended: 2026-07-16T21:44:30Z
points: 0
pr_numbers: [441, 443, 446, 448]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/5416e859-0e05-4a18-a107-f1dd5100024d.jsonl
---

# Session 54 — main

<!-- Task blocks appended by /kill-this, one per task. -->
<!-- This session ran outside the /kill-this-per-task ritual — work shipped via direct PRs. points: 0. -->

**Next Steps:**

_P12 reservations — design capture is DONE and merged (#446). The remaining work, roughly in order:_

1. **@architect review of DEC-123 / 124 / 125** — all three are "pending @architect." DEC-125 (virtual availability) especially, since it corrects DEC-123 and is the load-bearing data-model call everything hangs on.
2. **Remaining customer-facing mockups:** confirmation + manage page (the DEC-122 living link — where post-trip gratuity lives), and the link-recovery flow ("lost your link?" → email/phone + last name → re-send). The two customer entry surfaces (availability picker + booking form) are mocked and published to Drew.
3. **Remaining admin mockups:** the reservation detail pane (where "Move a booking" / block-triage resolution actually lives — Blocks links to it), and the purchases & customers area (DEC-123 §3).
4. **Then poker P12 into real tasks + `/start-phase 12`.** This is what kicked off S54 — it couldn't run because P12 had no task table. Now there's a rich design + DEC-123/124/125 to poker against. The admin half is confirmed the larger half.
5. **Then P12 build begins** — all of the above is DESIGN (mockups + DECs on `main`). No reservation *code* exists yet; that rides `feature/reservations`. Phase 11 exit gate (one real paid booking) is the first real code.

_Open design questions parked for the operator (Drew): deposit % + timing (DEC-107); price-variation stacking (Sun + holiday); waiver provider (DEC-110); per-guest roster now vs dock; Flex-insurance default on/off._

_Mockups live on the sessions… no — on `main` under `docs/design/mockups/` (reservation-calendar, reservation-calendar-mobile, offering-catalog, blocks, booking-form, availability-picker). Serve with `python3 -m http.server 3100 --directory docs/design/mockups` and hit `mill-dev:3100/<name>.html` (NOT 3000 — that's the dev server)._

**Context:**

- **Two distinct efforts this session.** (a) A P12 reservations **design** marathon — six mockups + DEC-123/124/125 + a full SPEC §0.2/§0.3/§2.2/§2.3/§4 reconcile against DEC-105/082/043/036/040. All merged via #446 to `main` (spec-on-main; code stays on `feature/reservations`). (b) A **prod side-quest**: shipped **v1.0.14** (#441 Manning UI withdrawal e2e fix + #443 self-claim) and **v1.0.15** (#448 — import-cancel audit gap + the cancelled-shift double-book/thread-phantom family), both promoted to production.
- **The reservation model, settled:** whole-boat-private; customer picks **offering + party, never a boat** (system assigns; max = best available boat for the slot); **virtual availability** (schedule is a rule, rows materialize on state — NOT 3,060 eager rows; DEC-125); **gratuity ≠ tips ≠ add-on** (own table, kind pre/post, routes to crew, tax/fee-exempt; DEC-124); "**seat**" is crew-only, reservation side says **guest**; **Location** is a new first-class entity (pickup + route); tax/fee are **system settings** + per-offering override; **Draft/Live/Hidden** lifted from bushel's soft-delete; **Blocks** are scoped availability-subtractions (location/vessel/hold) on their own surface, and that screen is the **single registry** for all blocks incl. calendar-made single-slot ones (the anti-Xola: unify visibility, not creation).
- **#449 filed** (bug, deferred) — the cohort/doorbell membership leak (same cancelled-shift-kept-seat family as #448; lower severity).
- **Two customer mockups published to claude.ai for Drew** (private): availability picker `321c6442-aedd-46d5-8717-f6acbe354199`, booking form `31d0166d-b551-4d54-b611-15f5b76370fe`. To update either, republish the same scratchpad file path from a session or pass the URL.
- **Bug-hunting method that paid off** (worth reusing): sweep for *shapes* — "a consumer handles a subset of a result's parallel lists" and "sibling scans over the same data with divergent state filters." Both #448 siblings (split/merge audit gap, thread-list phantom) came from that, not from tripping over them.
