---
session: 55
dev: eric
slug: main
branch: task/12.0-reservations-admin-spec
started: 2026-07-15T18:58:07Z
ended:
points:
pr_numbers: []
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/98e69909-e113-4f51-9a01-74b9ed8a4aba.jsonl
---

# Session 55 — main

<!-- Task blocks appended by /kill-this, one per task. -->

**Next Steps:**
- **#440** filed — Pick My Shift hides uncrewed seats (`Asked` seats + `AtRisk` shifts invisible to self-claim). Unlabeled for phase; both fixes land in `src/oracle/claimable.ts`, `src/asks/claim.ts` follows in lockstep. Open question in the issue: copy for the crew member out-raced on a seat.
- **Manning-override trap — undocumented, no issue yet.** Decide the shape first: flag off / rip out / fix the deadlock. Eric's call; he didn't want supernumerary seats this phase at all, so flag-off is the likely answer.
- **Tell Whalen / Learman / Scaffide** the July 19 Brew 4 second-captain push was bogus. Their pushes are still on their phones; the seat is gone.
- **Unverified:** does the ask-response path fail gracefully when the seat is deleted? Not checked.

**Context:**
- **Prod hand-fix applied ~18:55Z** (no session/PR — direct psql, no seat CLI exists): deleted `seat-shift-vessel-brew-4-2026-07-19-ovr-role-captain-1` + its 3 pending `asks` rows, via `psql "$(cat ~/.muster-prod-db)"`. Verified after: Brew 4 / Jul 19 has exactly 2 derived seats (captain-1 Confirmed = eric, mate-1 Asked), shift still `Filling` (no repair needed), 0 leftover asks. Swept fleet-wide: **zero `override` seats remain in prod.**
- **Root cause chain** — operator-added override required seat (`+ Required hand`, `manning-section.tsx:162`) → `tick.ts:119` asks on it (`kind === "required"`, **no override filter**) → `Open`→`Asked` deletes the assignee (`ask-loop.ts:520`) → remove blocked by `manning.ts:75` (needs `state === "Open"`), vacate blocked by `actions.ts:256` (needs `Confirmed` + assigned), Manning section shows "occupied — vacate to remove" from `occupied: s.state !== "Open"` (`shift-cockpit.tsx:168`). **Deadlock: no UI or crafted-POST path out.** Left alone, `expireAsks` logs `ask_ignored` (−3) against innocent crew then reopens the seat → tick re-asks → grinder.
- **Seat add/remove is NOT audited** — structural, not an oversight: `audit_events.crew_member_id` is `not null`, so a subjectless seat event can't be expressed in the schema. Only forensics available are the `-ovr-` id prefix and the `override` column. Eric flagged this himself ("we don't audit this").
- `ask_sent` is weight **0** (`reliability-score.ts:94`) — left the 3 `ask_sent` reliability_events in place as true history. No `ask_ignored` ever fired; nobody's score was touched.
- **S54 is a concurrent live window** on this same checkout (`task/12.0-reservations-admin-spec`). Left `status: open` deliberately — not stale. This session cut no branch and wrote no code, so no linked worktree (per standing preference: no per-task worktrees).
- Uncommitted on the checkout: `docs/design/mockups/reservation-calendar.html` — S54's, untouched here.
