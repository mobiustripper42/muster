---
session: 55
dev: eric
slug: main
branch: task/12.0-reservations-admin-spec
started: 2026-07-15T18:58:07Z
ended: 2026-07-16T03:06:06Z
points:
pr_numbers: [441, 443]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/98e69909-e113-4f51-9a01-74b9ed8a4aba.jsonl
---

# Session 55 — main

<!-- No `## Task <N>` blocks: an unplanned prod incident + its two fixes. Both PRs
     were opened by hand rather than `/kill-this`, so `points:` is empty, not 0 —
     the work was never estimated. #441 + #443 both MERGED before close. -->

## Prod incident — phantom captain seat on Brew 4 / Jul 19 (2026-07-15)

One stray click on "+ Required hand" created an extra required captain seat. The
tick asked on it (`tick.ts:119` filters `kind === "required"` with **no override
filter**), pushing **three real captains** — Whalen 18:00, Learman 18:15,
Scaffide 18:45 — for a seat nobody meant to create. It was unremovable through
any UI or crafted POST once `Asked`.

Fixed by hand at ~18:55Z (no seat CLI exists): deleted the seat + its 3 pending
asks via `psql "$(cat ~/.muster-prod-db)"`. After: Brew 4 / Jul 19 holds exactly
2 derived seats (captain-1 Confirmed = eric, mate-1 Asked), shift still
`Filling`, 0 leftover asks. Swept fleet-wide — **zero `override` seats remain in
prod**. No `ask_ignored` ever fired; nobody's score was touched.

**PR #441 — Manning UI withdrawn (MERGED).** Render dropped from
`shift-cockpit`, `addManningSeat`/`removeManningSeat` hard-reject (a server
action stays POST-reachable after its UI is gone). `src/builder/manning.ts` +
tests kept — a withdrawal, not a teardown.

**PR #443 — #440, an uncrewed seat always lists (MERGED).** `Asked`/`Bailed`
seats and `AtRisk` shifts now reach Pick My Shift. `CLAIMABLE_SEAT_STATES` is
derived as the exact complement of `COMMITTED_SEAT_STATES` so the two can't
drift. `claimSeat`'s CAS re-pinned from the literal `"Open"` to the state it
actually read.

**Next Steps:**
- **Tell Whalen / Learman / Scaffide** the Jul 19 Brew 4 second-captain push was
  bogus. Their pushes are still on their phones; the seat is gone. Not done.
- **Trainee staffing UI went with #441** — it lived inside `ManningSection`.
  `staffTraineeSeat`/`unstaffTraineeSeat` + the `trainee_on`/`trainee_off`
  notices are intact but unreachable (supernumerary seats can no longer be
  created). Decide whether that's wanted or needs its own surface.
- **Reviving manning** needs the tick to skip `override` seats, a confirm step,
  or both — recorded in the `addManningSeat` doc-comment.
- **Row-level audit for seats** — `audit_events.crew_member_id` is `not null`, so
  a subjectless seat event cannot be expressed. That's a migration, not a missing
  logger call. No issue filed.
- **#440's papercut, deliberately unfixed:** if the tick asks an `Open` seat
  inside `claimSeat`'s own read→CAS window (ms), the member sees "someone grabbed
  that one first" when nobody did. Retry succeeds.
- S54 (the concurrent window) was still `status: open` at this close.

**Context:**
- **Seat add/remove is NOT audited** — structural, per above. The only forensics
  are the `-ovr-` id prefix and the `override` column. The *tick* is the usable
  clock: it runs every 15 min (`:00:45`/`:15:45`/`:30:45`/`:45:45`) and only
  writes an ask when a seat needs one, so ask timestamps bracket when a seat
  appeared. That's how creation was pinned to a 15-minute window.
- **Cause was a bare form submit** — no confirm, no undo (`<form
  action={addManningSeat}>`). Not carelessness; a UI design failure.
- **`:3100` is held by a `python3` process** (the mockup server). Playwright's
  `reuseExistingServer` does **not** check *what* is listening — it reused it and
  sent every test to the wrong server, producing a bogus "no sign-in button"
  failure across 14 specs. Use `E2E_PORT=3101`. The config warns about the
  stale-`next start` version of this trap, not the wrong-process-entirely one.
- `npm run verify` does **not** run Playwright. Both PRs' e2e were run by hand.
- **`recordResponse` logs before it reads seat state** (`ask-loop.ts:284`,
  `:318`), so an out-raced In/Out already credits reliability — the operator's
  "any In or Out is a +" rule needed no code. `answeredNoticeCode`'s `default:`
  arm already mapped `already_filled → "filled"` (#161), so #440's new race falls
  into copy that already existed.
- Prod psql reads/writes go through `psql "$(cat ~/.muster-prod-db)"` — the file
  is a bare connection string, not `KEY=VALUE`.
