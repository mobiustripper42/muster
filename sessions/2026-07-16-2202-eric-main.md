---
session: 56
dev: eric
slug: main
branch: main
started: 2026-07-16T22:02:50Z
ended: 2026-07-18T04:13:18Z
points: 0
pr_numbers: []
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/5416e859-0e05-4a18-a107-f1dd5100024d.jsonl
---

# Session 56 — main

<!-- Task blocks appended by /kill-this, one per task. -->
<!-- No /kill-this this session — design + planning shipped via direct commits to main and /start-phase. points: 0. -->

**Next Steps:**

- **P12 build begins on `feature/reservations`.** First pull is **12.0 (#453)** — the virtual-availability read model; everything else reads from it. Then 12.2/12.3 (pricing, gratuity) are safe additive follow-ons.
- **Two `@architect`-gated mechanism tasks want a design pass reading `sailbook` at build:** **12.1 (#454)** the claim + 15-min hold + pessimistic backstop, and **12.14/12.15 (#467/#468)** the cutover import + reversible rollback. Don't inline-design these.
- **Waiver 12.13 (#466) is a decision spike** — pick the provider, then the integration re-pokers.
- **Post-P12 / Xola-sunset follow-on:** move the full tip→Gusto apparatus (split + CSV + Muster+Xola union + final export) into Muster; the transition mechanism is TBD (operator hand-combines two lists meanwhile).
- The 16 issues are `phase:12` + `points:N`. At phase end, `/retro`.

**Context:**

- **Branch model settled + reconciled this session:** design/DECs/SPEC/PLAN live on `main` (spec-on-main); `feature/reservations` **absorbed main (0 behind)** — code PRs target `feature/reservations`, which merges to `main` **once**, at the customer-ready flip. The main→feature merge hit the usual dual-DEC-append conflict (feature's ≤122 + main's 123–126) — resolved by keeping both; package-lock took feature's (has Stripe) with version reconciled to 1.0.15.
- **P12 = 16 tasks / 86 pts** (#453–#468). Three 8s (12.1 claim, 12.8 catalog, 12.11 calendar) are near-certain splits → realized points run higher. Admin half (12.8–12.12) confirmed the larger half.
- **DECs added/amended this session:** DEC-126 (new — cutover w/ one-time Xola import, Muster = reservation SoT, reversible); DEC-109 amended (guest-count claim + 15-min hold from sailbook + permanent pessimistic backstop; refund-loser); DEC-124 amended (Muster builds its OWN Gusto report lifted from `xola-tip-extractor`; Xola union deferred; end-state apparatus-in-Muster at sunset); DEC-123/125 wording + the `× muster-owned-days` mask + Event-slot uniqueness guardrail; SPEC §0.2/§0.3/§4 re-reconciled to coexistence→cutover.
- **`Fable` (`claude-fable-5`) is available** despite CLAUDE.md's DEC-S029 "disabled" line (stale — a seeds reconcile if you want it fixed). Good fit for the big split-y builds (12.8, 12.11).
- **Prod:** crew engine live 5 days, ~all of July crewed — strong adoption. Shipped **v1.0.15** earlier (#448 import-cancel audit + cancelled-shift family); **#449** parked (cohort/doorbell membership leak, lower severity).
- A vocab-lint **hook was tried and reverted** — operator vetoed changing the build for one word; if revisited, it's a hook, not a memory. Standing lesson reinforced: don't dig to validate my own flags; scope edits to the named surface; customer copy is guests/passengers, never seat/head.
