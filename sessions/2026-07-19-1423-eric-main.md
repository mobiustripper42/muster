---
session: 59
dev: eric
slug: main
branch: main
started: 2026-07-19T14:23:11Z
ended: 2026-07-20T12:12:07Z
points: 8
pr_numbers: [481, 482]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/4db9b4d4-c04d-486b-bd31-a359a43f6378.jsonl
---

# Session 59 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: #474 — balance deriver composes party fare (deposit-mode extras undercollection)

**Completed:**
- Root cause: `balanceOwedCents(event.price, …)` used the frozen per-departure BASE (DEC-125), but since 12.2 the customer is charged `composeFare` = base + extra-guest. Deposit-mode balance undercollected by `extras + tax(extras)`. Latent — RESERVATIONS flag off.
- Freeze the extras portion at booking on **`Reservation.extrasCents`** (`entities.ts`) + nullable additive migration `20260719185042_reservation_extras_cents.sql`. Booking property, not on Event (keeps DEC-125 base invariant), not recomputed from a live Offering (would break the "never a config recompute" rule).
- Threaded end-to-end: departure-checkout metadata (already carried `extrasCents`) → `booking-webhook.ts` → `writeSlotBooking` (conditional set) → reservation → row mapper + all 3 postgres insert sites (`postgres-repository.ts`, 12 fields).
- `balanceOwedCents` first arg now the composed **fare**; both callers (`create-balance-checkout.ts` + the `recordBalancePayment` overpay guard) pass `event.price + (reservation.extrasCents ?? 0)`.
- Tests: deriver 42000 (vs 35565 bug value), checkout integration, webhook threading, no-extras regression. `npm run verify` green.
- @architect-gated design (freeze-not-recompute, Reservation-not-Event). DEC-107 amendment on `main` = standalone PR #480.

**Code review:** Clean bill of health — threading verified at every hop, `opt()` preserves 0, migration ordering clean, test arithmetic holds by hand.
**PR:** [#481](https://github.com/mobiustripper42/muster/pull/481) (base `feature/reservations`)
**Points:** 3
**Branch:** task/474-balance-extras
**Opened at:** 2026-07-19T18:56:00Z

## Task 2: P12.9 — Vessel + Location admin (#462)

**Completed:**
- Vessel + Location settings twins (`/admin/vessels`, `/admin/locations`) — server components + server actions, no-JS forms, inline admin gate; greenfield UI over existing catalog persistence. Master–detail + read-only Runs/Used-by reverse lookups.
- New `Vessel` fields (`entities.ts`) hue/homeLocationId/notes + nullable additive migration `20260719210749_vessel_admin_fields.sql` (no backfill). Repo mapper + upsert (8 cols).
- **Hue is now a stored `Vessel.hue`** and replaces the crew-surface derivation: `vesselHueClass(vesselId, storedHue?)` prefers it, threaded through 3 view models (`all-shifts`, crew-view MyShift, `claimable-view`) → shift board + crew lists reflect the operator's colour. Absent ⇒ prior PINNED/hash, nothing recolours until set.
- Domain validated upserts `src/admin/{vessel,location}-admin.ts` (`{ ok }` result, preserve-on-edit for manning + includedGuestCount). Nav link in `admin-nav.tsx`. Seed vessel gains a representative hue.
- **Decisions:** capacity coiMax-only (includedGuestCount → Offering, #461; existing field left intact); out-of-service (block) + Retire (soft archive) deferred.
- Tests: domain unit + `vessel-hue` unit + e2e `vessel-location-admin.spec.ts` (create location → set as vessel home + change capacity/colour + persist; create new vessel). `npm run verify` green (1292 tests); e2e passes desktop + 375px (ran locally).

**Code review:** Clean bill of health. Non-blocking: vessels page 224 LOC (within ~300 guideline); no UI path to unset a hue once chosen (domain supports it, punted).
**PR:** [#482](https://github.com/mobiustripper42/muster/pull/482) (base `feature/reservations`)
**Points:** 5
**Branch:** task/12.9-vessel-location-admin
**Opened at:** 2026-07-19T21:40:00Z

**Next Steps:**
- **Agreed P12 admin build order: #462 (done) → #461 Offering catalog → #463 Blocks surface → customer chain (#457/#458/#459).** Customer-facing last (service layer 12.0/12.2 already done; Xola still source of truth until cutover).
- **#461 (12.8 Offering catalog, 8pts) is next.** Will also move `includedGuestCount` from Vessel → Offering (pricing refactor: `composeFare`/`effectiveIncludedGuests`), per this session's model decision. Vessel admin ships without it (coiMax-only).
- Merge order: #480 (docs → main), #481 + #482 (code → feature/reservations) all independent; merge whenever.
- **DB test on a fresh DB** (deferred per operator): (a) #474 — deposit booking over included count, confirm balance link charges base+extras; (b) #462 — recolour a boat, confirm the dot changes on /admin/shifts + /crew. Fold into next-task testing.
- #474 was the gate before deposit mode ships — clear once #481 merges.

**Context:**
- Reservations code rides `feature/reservations` in the `muster-reservations` worktree; this task branched off it there (not the main checkout). Docs/DEC changes went to `main` separately (spec-on-main / code-on-feature split).
- Possible latent app bug flagged last session (`currentPeriod` vs `periodsForYear` pay-period boundary) still open — unrelated to #474.
- **Side quest → bug #483 (crew-engine, main):** prod repro confirmed via Neon read — `bail()` + `vacateSeat()` fire asks INLINE (whole-pool blast, `Promise.all(pool.map(fireAsk))`), bypassing the tick + staffing horizon. A captain bailed an Aug-8 shift (13d pre-horizon) → all 6 captains asked at the same ms. @architect-designed fix: both re-open seat → Open → new `refreshShiftStateHorizon`, tick is sole ask-writer; retires `Bailed` resting state; accepts loss of board `regression` re-ping. DEC-127. NOT built — queued.
- **#484 (input contrast, app-wide):** white `bg-card` inputs on white cards w/ near-white `border-line` = near-invisible. Settings screens now use shared `components/admin/settings-field.tsx` (`bg-bg` fill). App-wide rollout pending a token decision + @ui-reviewer.
- **UI lesson (vessel/location mockup match):** "match the mockup" = screenshot-diff the built page against `docs/design/mockups/*.html` and reconcile, NOT rebuild "in the spirit." Missed width (used 3xl not 6xl), fonts (app IS Plex; mockup file just doesn't load the webfont), and — the recurring one — `AppLink` wraps children in a label node, so a `flex gap` on the link never reaches its contents (put flex on an inner span). Save-success banner (`Notice tone=ok`) shifts layout — removed; a dirty-state Save button is the eventual saved/unsaved strategy.
- **#485:** parked idea — hide/retire Vessel/Location (soft-archive), distinct from Block #463.

**Context:**
