---
session: 60
dev: eric
slug: main
branch: main
started: 2026-07-20T13:30:43Z
ended: 2026-07-22T03:32:29Z
points: 28
pr_numbers: [487, 492, 494, 496]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/6c098f61-0779-4fef-9801-904ed28dcfbb.jsonl
---

# Session 60 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: #461 — Phase 12.8 Offering catalog (largest single P12 build, DEC-123)

**Completed:**
- Prereq: merged `main` → `feature/reservations` (14 commits: new CLAUDE.md shell + hooks + settings, DEC-107 docs, MESSAGING flag) — clean, verify green, pushed. Feature now carries the current workflow rules for concurrent sessions.
- Greenfield `/admin/offerings`: offerings list (master, w/ Show-hidden toggle) + 5-section editor (Details/Schedule/Pricing/Gratuity/Add-ons; Photos deferred) mirroring the #462 Vessel/Location pattern. `src/admin/offering-admin.ts` validated write door + 15 unit tests; page + actions extend the existing `repo.saveOffering`.
- Pricing refactor: `includedGuestCount` Vessel→Offering; `effectiveIncludedGuests(offering, vessel)` two-entity read. New entity fields (`description`, trip/hold/arrive minutes, `addOns`, per-kind `gratuityKinds`). Two additive migrations (`...100000` includedGuestCount, `...100500` catalog fields + gratuity fold).
- Built as one Fable pass (xhigh) in 5 discrete commits. `@code-review` found 1 bug (gratuity migration folded only `pre` → fixed to pre+post), 1 validation gap (`includedGuestCount` vs vessel capacity → added `included_over_capacity` + test), 1 reviewability (page.tsx 655→215 LOC, sections extracted to `offering-sections.tsx`). All fixed in commit 5.
- Scope calls (operator-confirmed): photos deferred (no upload infra), tax/service-fee override deferred, gratuity modeled per-kind.
- `npm run verify` green (1314 tests); e2e `offering-catalog` 2/2 (desktop + 375px). Did NOT pixel-diff the mockup (internal admin surface) — flagged to operator.

**Code review:** 3 findings (1 bug + 2 cleanups), all fixed + re-verified before PR.
**PR:** [#487](https://github.com/mobiustripper42/muster/pull/487) (base `feature/reservations`)
**Points:** 13 (was pokered 8; realized the plan's predicted 8→13 balloon)
**Branch:** task/12.8-offering-catalog
**Opened at:** 2026-07-20T14:45:00Z

## Task 2: #463 — Phase 12.10 Blocks surface (DEC-125)

**Completed:**
- `/admin/blocks` — the single availability-subtraction registry (location/vessel/calendar-hold), each row server-computed impact ("removes N slots" + "⚠ M booked ($X) conflict" reusing `deriveVirtualAvailability`/`isSlotBlocked`). Create aside for the two scoped kinds; no-JS two-step Lift (`?lift=` → Confirm/Cancel). Data layer (entity/table/adapters/deriver) already shipped in 12.0 — NO migration; this is write door + UI.
- `src/admin/block-admin.ts` (`saveBlockAdmin` location/vessel + real `removeBlockAdmin`), `src/reservations/block-impact.ts`, page/actions/sections mirroring the offering/vessel/location pattern + nav link.
- **`db:seed:reservation`** (operator-requested): prod-guarded dev seed — LIVE offering + owned days + 2 booked trips on Brew 3 (Aug 12 13:30 $549, Aug 13 15:30 $439) so the conflict count is real. Dry-ran the CLI; output matches the PR steps. + loose drift test.
- Built by an Opus agent; **my review caught a real e2e bug** (agent wrote but didn't run it — fragile `.last()` selector matched the create-form `<select>`, not the row). Fixed with a `block-row` testid + scoped selectors.
- `npm run verify` green (1333 tests, +19); blocks e2e 2/2 (desktop + 375px); seed CLI dry-run ✓.
- Scope calls: impact server-computed on rows (not a live-in-form island, keeps no-JS); vesselHold read-only (calendar #464); per-trip drill-down deferred (#465).

**Code review:** Manual full-diff review; found + fixed the e2e selector bug. Build otherwise clean.
**PR:** [#492](https://github.com/mobiustripper42/muster/pull/492) (base `feature/reservations`)
**Points:** 5
**Branch:** task/12.10-blocks
**Opened at:** 2026-07-21T00:00:00Z

## Task 3: #491 — Add-ons as first-class entities (attach to offerings)

**Completed:**
- Reverses the inline `Offering.addOns` jsonb (from #461): `AddOn` is now a standalone entity (new `AddOnId` brand; `id`/`tenantId`/`label`/`type`/`amountCents`/`required` [global]/`active` [soft-retire]). `Offering.addOns` → `addOnIds: AddOnId[]` (jsonb `add_on_ids`, mirrors `vesselIds`).
- New `/admin/add-ons` CRUD (page + actions + `src/admin/add-on-admin.ts`) mirroring the Vessel/Location twin — ?sel master-detail, retire via Active, "used by N offerings" reverse-lookup. Nav link. Repo `listAddOns`/`getAddOn`/`saveAddOn` (port + both adapters + contract).
- Offering editor Add-ons section = checkbox picker over active add-ons (attach by id, like the vessels group); `offering-admin` validates each id exists (`bad_add_ons`).
- Migration `20260721000000` — `add_ons` table + `offerings.add_on_ids`; deprecates `offerings.add_ons` in place (no drop, no data migration — empty). No consumers (write-only today), so near-zero blast radius.
- Built by an Opus agent. **I paused a premature /kill-this** (branch was mid-build, uncommitted, 0 commits) until the agent finished. Then reviewed the full diff (entity/migration/offering-admin picker all correct), re-ran verify, ran both e2es, dry-ran the flow.
- `npm run verify` green (1341 tests); e2e `add-ons` + updated `offering-catalog` 4/4 (desktop + 375px).

**Code review:** Manual full-diff review; clean (house-style migration, id-validation on attach, picker mirrors vessels). No consumers to rewire.
**PR:** [#494](https://github.com/mobiustripper42/muster/pull/494) (base `feature/reservations`)
**Points:** 5
**Branch:** task/491-add-ons-first-class
**Opened at:** 2026-07-21T01:30:00Z

## Task 4: #464 — Reservation calendar Day·Grid (read-only slice, 12.11)

**Completed:**
- Greenfield `/admin/calendar` — the blocks "On calendar →" target. Minimum read-only slice: one day as boats × time, blocks positioned `top%`=start / `height%`=duration over `deriveVirtualAvailability` (one-day, holds omitted → operator holds show as blackout). Offering-colored (derived palette, #495), All/Booked/Open/Blackout filter + `?date=`/`?filter=` nav.
- Joins: Offering (`tripLengthMinutes` height + color) + Reservation (name/party for booked, via `isActiveMusterClaim` keyed on eventId). `src/reservations/calendar-grid.ts` pure geometry+color helpers (unit-tested); page mirrors blocks/page.tsx data assembly.
- Deferred (operator-confirmed minimum): detail pane, sell-from-calendar, week/month/timeline, mobile list, offering-color DB decision (#495). Calendar mockups landed on `main` (c909f89); DEC-126/127 left for the sailbook session to renumber.
- **Root-cause fix folded in:** `add_ons` was missing from the postgres contract truncate list (#491 bug) — the build agent had made verify green via a manual test-DB row-delete; I fixed the truncate list so it's stably green.
- Built by an Opus agent; I reviewed the geometry/joins, caught + replaced the manual-DB hack, re-verified + re-ran e2e.
- `npm run verify` green (1351 tests); calendar e2e 6/6 (desktop + 375px).

**Code review:** Manual full-diff; geometry correct vs mockup sanity values, holds omitted, reservation join correct; caught the manual-DB-hack and fixed the real bug.
**PR:** [#496](https://github.com/mobiustripper42/muster/pull/496) (base `feature/reservations`)
**Points:** 5
**Branch:** task/12.11-calendar
**Opened at:** 2026-07-22T00:00:00Z

**Next Steps:**
- **Plan (a) — the shared reservation detail pane** (roster · change arrival · cancel · refund · resend link · message): wire it to a booked calendar block (finishes #464 interactivity) AND reuse it for #465 Purchases & customers' order detail — one detail component, two front doors. Operator flagged it's a PREREQUISITE (needed before the customer build, so operators can manage/cancel/refund reservations customers create), not far-deferred. Spec against the mockups + confirm scope before cutting.
- #465 Purchases & customers list/customer-record is the remaining admin surface (folds in with plan a's detail).
- Customer-facing chain (#457 picker / #458 checkout / #459 manage) still waits on the sailbook customer-flow mockup redesign. Before those mockups come into the repo, the other session renumbers sailbook's DEC-126/127 (they collide with main's DEC-126 Xola-cutover + DEC-127 topic-index).
- #483 crew-engine inline-ask bug (main-side, DEC-127-designed) still queued.

**Context:**
- **Admin half: 5 of 6 done + merged** (catalog #461/#487, vessel-location #462, blocks #463/#492, add-ons #491/#494, calendar-DayGrid #464/#496). Only #465 (+ the detail pane) left.
- **Calendar seed gotcha:** `db:seed:reservation` seeds FIXED dates — Brew 3, owned Aug 10–16 2026, 2 booked (Marcus Webb Aug 12 13:30, Dana Cho Aug 13 15:30). The calendar defaults to TODAY, which is empty → navigate to `?date=2026-08-12`. Empty-today grid is correct, not a bug.
- **Deferred from the calendar (minimum slice):** detail pane, sell-from-calendar, week/month/timeline, mobile list, offering-color DB decision (#495). Offering colors derived from the vessel palette for now.
- **Parked issues:** #491-followup add-ons picker polish; #493 unified date/time picker + Block date/time model; #495 offering colors DB-vs-derived. Calendar mockups landed on `main` (c909f89). Fixed a test-isolation bug (add_ons missing from the postgres truncate list).
