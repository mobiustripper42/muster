---
session: 62
dev: eric
slug: main
branch: main
started: 2026-07-22T11:15:44Z
ended:
points:
pr_numbers: [498, 499, 500]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/b07c8ec4-08e1-413a-bbd1-a427e67822d3.jsonl
---

# Session 62 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 12.11 — reservation detail pane (read-only, routed page, #464)

**Completed:**
- `src/reservations/calendar-detail.ts` (+20 unit tests) — pure view model. Money routes through the existing authorities only: fare = `Event.price` + frozen `extrasCents` (#474/DEC-107 amend), tax via `taxCentsFor`, residual via `balanceOwedCents`; gratuity reads off `Gratuity` rows and stays out of fare AND balance (DEC-124).
- Extracted the loader + grid out of `app/(admin)/admin/calendar/page.tsx` into `calendar-view.tsx` so both calendar routes render one implementation; `page.tsx` reduced to consume it.
- New `app/(admin)/admin/calendar/[reservationId]/` — route + `reservation-detail-pane.tsx`. **Read-only, no actions** (operator-confirmed scope); sell-from-calendar still deferred to the 12.1 claim.
- One route, two native layouts, zero client JS: desktop = grid + sticky pane with the open reservation ringed; mobile = grid CSS-hidden, pane is the page. A server-rendered href can't vary by viewport, so the split is in layout, not the link.
- Three mockup rows the model can't source, encoded rather than faked: add-ons section OMITTED (`addOnIds` is Offering-level, #491 — no per-reservation selection exists), waiver is ONE consent row with a three-way state (DEC-012/DEC-110, no per-attendee roster), date labelled "Updated" not "Booked" (`Reservation` has no `createdAt`, DEC-029). The mockup's 3% service fee is Xola's and isn't modelled.
- `e2e/calendar.spec.ts` +3 specs: open-from-grid + money, the desktop/mobile layout split, deep-link day resolution, unknown-id 404.

**Two seams found the hard way:**
- `Event` carries **no `offeringId`** — the offering is only reachable via the availability deriver's slot (DEC-125). So the route resolves the reservation FIRST and defaults the grid to that reservation's own day; a deep link with no `?date` otherwise renders today's grid beside a reservation from another week.
- **Next hands the dynamic segment percent-ENCODED** and reservation ids contain a colon (`resv-demo-2026-08-13-15:30`) — every lookup 404'd until decoded. Hrefs encode; the route decodes with a malformed-`%` fallback.
- Two defects only a screenshot caught: booked blocks needed `spinner="overlay"` (AppLink's default inline spinner wraps children in one element, collapsing the block's two stacked lines), and the pane was repeating the page's `<h1>`.

**Code review:** Clean bill of health — @code-review diffed the extracted grid line-by-line against the original and confirmed identical behavior (filter/dim, gutter ticks, columns, legend, per-status blocks); money, the encode/decode seam, and the degradation paths all checked out. No action items.
**PR:** [#498](https://github.com/mobiustripper42/muster/pull/498) — bases on `feature/reservations`, so CI skips e2e/verify; both run locally.
**Points:** 3
**Branch:** task/12.11-calendar-detail
**Opened at:** 2026-07-22T12:47:00Z

## Task 2: Phase 12.12b — Customer contact record + the constraint-posture correction (#465)

**Completed:**
- **DEC-131 + DEC-132** on `main` ([#499](https://github.com/mobiustripper42/muster/pull/499)). Operator challenged the no-FK rule as arbitrary — he was right. `DEC-DATA-1` (`docs/DECISIONS.md:438-470`) governs LOGIC PLACEMENT (no domain logic in RLS/triggers) and **never mentions foreign keys**; the rule was minted in `db/migrations/0001_init.sql`'s header and ~19 migrations cited DEC-DATA-1 as authority. New line: **the DB never holds business rules, may hold structural invariants** (FK/UNIQUE/NOT NULL). Existing graph ratified as-built, NO retrofit (contract suite deliberately writes dangling refs, `repository-contract.ts:441-455`). DEC-123's `eventId` guardrail survives — one-party-per-boat is a business rule.
- **`Customer` entity** ([#500](https://github.com/mobiustripper42/muster/pull/500)): surrogate PK + UNIQUE canonical E.164 phone + `C-XXXXXX` Crockford base32 code minted with retry against a UNIQUE index. `src/customers/{identity,resolve,customer-view}.ts` + 52 unit tests.
- **First real FK in this schema**: `reservations.customer_id` nullable, ON DELETE RESTRICT. In-memory double stays DUMB (no UNIQUE, no FK) — asymmetric strictness is deliberate; 6 contract cases run against REAL Postgres so the constraints are actually exercised.
- **Booking-path get-or-create in scope** (my first spec omitted it — the entity would have been stillborn, backfill-once then unlinked forever). Both `writeBooking` and `writeSlotBooking`; UNIQUE index arbitrates the race via `on conflict do nothing` + re-select. A paid booking is NEVER lost to a phone problem — unusable phone ⇒ unlinked, not thrown.
- `/book` phone required SERVER-side; backfill script (dry-run default); `customers` classified into `reset-pilot.ts` CLEAR beside `reservations` with the truncate-cascade hazard named; seed books Marcus twice with the same number two ways; read-only `/admin/customers` list + detail.
- Lifetime = fare + frozen extras on booked trips, **gratuity excluded** (DEC-124), stated on the page.

**Code review:** No blocking findings. Verified the highest-risk item — `customer_id` needed threading through THREE inserts (`saveReservation` + two claim inserts); all three carry it. 3 nits, all fixed in `850f925` (retry burned budget on non-collision errors; misplaced comment; `/book`'s `?err=` cast rather than validated → blank message on a bogus value).
**PR:** [#499](https://github.com/mobiustripper42/muster/pull/499) (DECs → main) + [#500](https://github.com/mobiustripper42/muster/pull/500) (code → feature/reservations). **#499 must merge first.**
**Points:** 8
**Branch:** task/12.12b-customer-decs (docs) + task/12.12b-customers (code)
**Opened at:** 2026-07-22T15:05:00Z

**Not run:** the backfill against real data — dry-run by default, worth eyeballing on prod before `BACKFILL_CONFIRM=yes`. Prod migration is the operator's to apply (dev DB migrated locally).

**Next Steps:**
- Merge #499 (DECs) before #500 (code).
- **Deferred and scheduled, not forgotten:** phone-or-email identity. The Xola importer forces it at cutover — Xola rows carry email reliably, phone not always. Relaxation is `DROP NOT NULL` + a one-file policy change (all identity logic is in `src/customers/identity.ts`).
- #465's other half (Purchases/orders list + order-number scheme) still open — operator talked himself out of order numbers for now.
- #464 is NOT closed — the detail-pane actions (message, guests, change arrival, cancel, refund, resend link) are the remaining half. Refund waits on #472.
- 11.2b has a built, tested, **caller-less** balance flow (`createBalanceCheckout` has zero non-test callers) — it needs a door. Natural home: the reservation detail pane, which shows "Balance due" and offers no way to act on it.

**Context:**
- Reservations work continues on `feature/reservations` in `/home/eric/muster-reservations` (merges to main once, at the P12 customer-ready flip).
- The e2e harness reuses a warm server; a stale build 404s brand-new routes. If a new route 404s in e2e, rebuild before debugging the route.
- Spec/DECs land on `main`; only code rides `feature/reservations`. This session that meant two PRs per task where a DEC was involved.
- Next hands dynamic route segments **percent-encoded**, and reservation ids contain a colon. Any new `[id]` route over reservation ids must decode (cost a debugging round on #464).
- The admin nav renders overlapped at 1280 in headless screenshots — pre-existing, and the operator has since said the nav is a **known dumping spot to be fixed later**. Don't re-raise it.
- Operator's standing frame: build the reservation model out first, polish in one batched pass later. "Extremely basic but it does the job" is the accepted bar for these admin surfaces right now.
