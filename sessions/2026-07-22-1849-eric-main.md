---
session: 63
dev: eric
slug: main
branch: main
started: 2026-07-22T18:49:53Z
ended: 2026-07-23T14:41:31Z
points: 3
pr_numbers: [507, 509]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/b78d7bab-1923-443c-b43a-67f2f70dde4f.jsonl
---

# Session 63 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Admin referential-integrity diagnostic on real data (#501)

**Completed:**
- `app/(admin)/admin/integrity/page.tsx` — `checkIntegrity` against the live DB from an admin-gated, zero-client-JS surface. **Idle until `?run=1`**: auth kills the compute-amplification vector that pulled the check off `/api/health`, but it doesn't make twelve full-table `listAll*` reads cheap, so the scan is an explicit GET rather than a page load.
- `src/admin/integrity-view.ts` (+7 unit tests) — pure view model. Groups violations by entity, biggest group first (one bad backfill orphans dozens of rows; flat they're unreadable), scanned counts as ordered rows so "clean" can never be confused with "didn't run". Trusts the report's own `ok` rather than re-deriving it.
- Linked from the `/admin` hub, **not** `AdminNav` — the nav is the operator's five daily screens, and it's a known parallel-PR conflict file.
- Two stale comments corrected: `src/admin/integrity.ts` still cited the no-FK rule to DEC-020/DEC-DATA-1 (DEC-131 superseded it yesterday) and still said "run it from the healthcheck"; `app/api/health/route.ts`'s "(issue filed)" now points at the built page.
- `e2e/integrity.spec.ts` — 6 specs: unauthenticated gate, crew-only gate, idle-scans-nothing, clean verdict + counts, **a planted orphan seat** (a `shiftId` pointing at nothing, written straight to Postgres — accepted precisely because there's no FK) rendering entity/id/ref/missingId, and the hub link.
- `npm run verify` green (1084 unit tests); 375px screenshot-checked.

**Two things found the hard way:**
- The first 375px screenshot pass came back showing `/admin`, not the new page — the warm `.next-e2e` server was frozen on a pre-change build, so `/admin/integrity` fell through to the admin catch-all's `redirect("/admin")`. It **fails silently as a redirect**, not a 404, which is worse than the note in the last session implied. Kill :3100 before screenshotting a brand-new route.
- The branch was cut from `a689a4c`, not `7a498f8` — the concurrent Session 62 committed onto local `main` in this shared checkout between the `git pull` and the `git checkout -b`. Harmless here (that commit shipped as #505 and is already on `origin/main`), but two sessions in one working copy means `main..HEAD` lies; diff against `origin/main`.

**Code review:** One real finding, fixed in `c875ae1` — the "Run check" link lacked `prefetch={false}`. The route is `force-dynamic`, so a prefetch on hover/viewport fetches its RSC payload and runs the twelve-table scan with nobody clicking; the idle design was sound in intent and open in practice. Gate placement (runs before `sp.run` is read) and the no-false-clean path both verified clean.
**PR:** [#507](https://github.com/mobiustripper42/muster/pull/507)
**Points:** 2
**Branch:** task/501-integrity-diagnostic
**Opened at:** 2026-07-22T20:05:00Z

## Task 2: Crew login-code round-trip — investigated, un-skipped (#504)

**Completed:**
- **The issue's hypothesis was wrong.** `next start` does NOT serve the server action and `/crew/dev-code` from different workers: an instrumented probe against a hand-started prod server caught the mint and the read-back in ONE process (server log `[login-code] → Quint <quint@brewboat.test>: 240431`, echo returned the same digits, `200`). The `globalThis` Map in `app/lib/auth-delivery.ts` is shared under the prod build exactly as under `next dev`.
- **Not a branch difference either.** The whole sign-in path — `auth-delivery.ts`, `dev-code/route.ts`, `crew/actions.ts`, `flags.ts`, `src/auth/`, `next.config.ts`, `crew-sign-in.spec.ts` — is byte-identical between `main` and `feature/reservations`, where it was reported. Only a seed-script entry and the mobile `testMatch` differ.
- **Full suite, fresh server** (`CI=1`, so `reuseExistingServer` off): **162 passed, 6 skipped, 0 failed** (6.8m). Reconciles with the prior 154/8 baseline: +2 un-skipped here, +6 from #501. Remaining skips are all #447.
- `e2e/crew-sign-in.spec.ts` un-skipped, assertion untouched. `app/lib/auth-delivery.ts` records that the shared-Map assumption now holds under `next start` too (previously only tested against `next dev`).
- **The actual cause was never pinned down** — gone before it could be caught. Recorded as such rather than invented. One unconfirmed candidate written down for next time: a stale reused server (`reuseExistingServer` serving a prior run's `.next-e2e`; a code minted under one build read back under another gives exactly the reported empty 204).

**Code review:** One finding, fixed in the second commit — both new comments had upgraded that stale-server theory into stated fact, which is precisely the cite-facts/label-proposals rule. Both now mark it unconfirmed. Reviewer separately confirmed un-skipping isn't papering over flakiness: `playwright.config.ts:46-47` pins `fullyParallel: false` / `workers: 1`, so mint and read-back are strictly sequential in one process.
**PR:** [#509](https://github.com/mobiustripper42/muster/pull/509)
**Points:** 1
**Branch:** task/504-crew-code-roundtrip
**Opened at:** 2026-07-23T01:40:00Z

**Not chased (out of scope, flagged only):** the suite logs a server-render mismatch dump for `<details open="">` on `/crew/shift/[shiftId]`. Every test passes; it predates this work.

**Next Steps:**
- **Both PRs merged and shipped.** `/promote-production` released **v1.0.19** (`ec41f1c`) — 16 commits / 5 PRs: #507, #509, #505, #499, #508 + the reservation-calendar design docs. Verify `<VersionTag />` reads v1.0.19 on prod.
- **#447 is the last thing keeping the suite from all-green** — 6 skips remain, all admin-nav overflow. Deliberately not patched around: letting the row wrap busts the 52px height budget `Shell fill` subtracts (#253). Needs a real layout call, not a cosmetic fix. The operator has separately called the nav a known dumping spot to be fixed later.
- **#501's follow-on:** if the integrity check ever fires on a specific edge, that edge becomes a candidate for a real FK under DEC-131's incremental rule. The scheduled-job half of #501 was deliberately left unbuilt — on-demand was the whole ask.
- **Side quest, unresolved:** Drew reports `www.brewcle.com` "isn't working according to Google". Apex and www both resolve to the same Cloudflare pair and both served the WordPress site fine when checked. Neither `ops/` monitor is installed on mill-dev, and `brewcle-probe.sh` watches the apex only (`PROBE_HOST` default) — www is a blind spot by construction. Need the literal text of what Drew saw before chasing it. Adding a www watcher is a second unit with its own `PROBE_DIR`.

**Context:**
- **The `.next-e2e` stale-server trap is worse than "a new route 404s".** It fails as a *silent redirect*: `/admin/integrity` on a frozen build fell through the admin catch-all to `redirect("/admin")`, so the screenshot showed a plausible page rather than an error. Kill :3100 before screenshotting or debugging anything new. This box had `next-server` processes with 7-day and 1-day uptimes still listening.
- **Two sessions in one working copy is a real hazard.** Session 62 ran concurrently in `/home/eric/muster`; my `git checkout -b` picked up its commit because it landed on local `main` between my pull and my branch cut. Harmless that time (already merged upstream), but `main..HEAD` lies under concurrency — diff against `origin/main`.
- `/admin/integrity` on prod issues twelve full-table reads against Neon per click. Admin-gated and deliberate, but it's a button to press on purpose, not to leave sitting open.
- Pre-promote migration check via Neon MCP is quick and worth keeping: `select filename from _migrations order by filename` against `delicate-art-65084110`, diffed against `db/migrations/*.sql`. Clean at 25/25 this release.
- Concurrent with Session 62 (still `status: open`). Working in the main checkout `/home/eric/muster`, no per-task worktree.
- Orphan `origin/claude/sailbook-ui-redesign-dh9chf` — 6 docs commits, never PR'd; its DEC-131/132 renumber collides with main's DEC-131 (constraint posture) / DEC-132 (Customer). Parked by operator decision.
