---
session: 66
dev: eric
slug: main
branch: main
started: 2026-07-24T18:30:20Z
ended: 2026-07-24T23:34:33Z
points: 5
pr_numbers: [516, 517, 519]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/099b84fb-f616-47b4-88c3-64a9e17f2410.jsonl
---

# Session 66 — main

<!-- Task blocks written at /its-dead, not /kill-this — see Context. -->

## Task 1 — Remove the pilot crew seed (DEC-134 on main → DEC-136 on reservations)
Points: 1
PR: #516 (merged)

`db/seed-pilot-crew.ts` carried 21 real crew members' names, emails, and phone numbers.
Operator-stated reason: real crew don't belong in the test database — a dev DB exists to have
test SMS fired at it. Roster bootstrap moves to `db:crew add` (DEC-094), which also seeds the
DEC-044 placeholder MMC, so nothing the seed did is lost. Records remain in git **history** —
purging needs a `filter-repo` rewrite, deliberately deferred as its own decision.

## Task 2 — `db:all` (DEC-135 on main; RETIRED by DEC-137 before it reached reservations)
Points: 3
PR: #517 (merged to main, then deleted at the reservations merge)

One always-destructive dev-DB command: localhost guard → migrate + truncate → seed registry →
promote `crew-eric` to admin. Refactored the five dev seeds into exported `seedX(repo)` +
CLI-entry guards. Shipped with `e2e/db-all.spec.ts` covering the full email→code→session
round-trip.

**Superseded on arrival.** `db/reset-dev.ts` (PR #511) already existed on `feature/reservations`
and is better: `--fresh`, `--seeds` selection, host AND db-name allowlist guard, and it covers
the reservation seeds. ~3 points duplicated. Points recorded because the work happened, but the
artifact is gone — see DEC-137.

## Task 3 — Merge main → feature/reservations
Points: 1
PR: #519 (OPEN at session close)

Forward-merge carrying the updated `CLAUDE.md` model advice and the pilot-seed removal. Six
conflicts. Resolved by deleting `db:all` (all seed + package.json conflicts take
`feature/reservations`) and renumbering main's DECs 134/135 → 136/137, since the reservations
line already held 134 (inline Elements, 12.5) and 135 (manage page, 12.6). Verified: typecheck,
typecheck:app, lint, 1536 unit tests, `db:reset:dev` end to end.

**Next Steps:**
- **Merge #519** into `feature/reservations` — it's the only PR still open.
- `main` still contains `db/all-dev.ts` + `e2e/db-all.spec.ts` until `feature/reservations`
  merges back; that merge carries the deletion. Expect to see the files on `main` until then.
- **PR #518** (`claude/its-alive-memory-loading-jlsbzs` → `main`) is open and was not authored
  this session — nobody has looked at it.
- 12.7 / #460 (link recovery) is the next unstarted reservations task.
- Deployed-env Stripe webhooks (preview + prod) still need endpoints subscribing to BOTH
  `payment_intent.succeeded` AND `checkout.session.completed`; preview needs a stable
  branch-alias URL + deployment-protection bypass or Stripe's POST 401s.

**Context:**
- **S65's blocker is dead, and it was environmental.** `/book` + `/book/checkout` had all client
  interactivity dead at once (guest stepper, tip tiles, Stripe Elements). On this machine, with
  every Stripe env var actually set, payment ran end to end — PaymentIntent → webhook → Purchases
  row → Calendar. The stale-server theory stayed unconfirmed and is now moot. Untestable
  hypothesis, recorded not asserted: `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` is build-inlined, so a
  missing key makes the Stripe SDK throw on module load and takes down the whole client bundle —
  which would kill three unrelated islands on one page load. The old machine is gone; it stays
  unproven.
- **Fresh machine.** `gh` and the Stripe CLI were both absent at session start and were installed
  mid-session. Nothing pushed for the first several hours because `gh` is wired as git's
  credential helper.
- **Stripe CLI webhook secret** (stable across listener restarts, already in `.env.local`):
  `stripe listen --print-secret` returns it without holding a listener open. The Stripe account is
  `Baybranchfarm` (`acct_1EZjzlGeXrNCYVLH`) — a real business account, so keep the test-mode key
  pair. `~/.config/stripe/credentials.json` holds an unencrypted API key (no system keyring on
  this box), valid 90 days.
- **`~/muster-reservations` is gone by decision.** Reservations work happens in `~/muster`;
  `main` hotfixes get a temporary worktree instead. Only `.sessions-worktree` remains.
- **Port allocation:** 3000 = Eric, 3100 = Playwright e2e (`playwright.config.ts`, `muster_test`),
  3200 = Claude (may run permanently), 3300 = sailbook. An unexplained "port 3000 in use" this
  session was a Claude-started server; Next's error names the `Dir:` and `PID:`.
- **Screenshots work over remote control** — pasted images are readable, so browser console errors
  can go straight into the conversation instead of being retyped. Truncated/collapsed output stays
  truncated; expand before capturing.
- **No `/kill-this` ran this session** — PRs were opened by hand, so the task blocks above were
  written at close rather than per-task. `/retro`'s velocity math would otherwise have seen zero.
- First `npm run test` on a fresh machine fails one file: two pg specs migrate an empty
  `muster_test` concurrently. Second run is clean (1536 passed). Bites once per machine.
