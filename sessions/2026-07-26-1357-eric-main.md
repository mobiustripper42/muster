---
session: 71
dev: eric
slug: main
branch: main
started: 2026-07-26T13:57:05Z
ended: 2026-07-27T15:26:51Z
points: 0
pr_numbers: [549, 550, 551, 552, 553, 559, 563, 566, 568]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/78238959-7562-4ac2-a1ac-90e795d76c1d.jsonl
---

# Session 71 — main

<!-- Task blocks appended by /kill-this, one per task. -->

**Next Steps:**
- **#564 — the DECISIONS.md do-over, fresh session.** Split the 3,949-line / ~105k-token file into
  one file per decision (`docs/decisions/DEC-NNN-slug.md`) with a **generated** index. Unblocked now
  that reservations is on `main` — it needed one reconciled file and no second branch. The generator
  reads frontmatter (`id`, `title`, `topic`, `amends`) and writes `docs/DECISIONS.md`; CI fails on a
  duplicate id, a dangling reference, or a stale index. Encode Z2's remaining 13 one-way back-pointers
  as `amends:` frontmatter during the split rather than hand-writing them twice.
- **#568** — waiting on CI, then merge. Last thing outstanding from this session.
- **#567** — derive the At-Risk heading from `FILL_DEADLINE_HOURS` (reads "~2 days" at 72). Small.
- **#408** — Crew Roster surface, specced at a **5**. Branch off `main`. Credentials explicitly out
  of scope (no credential data in the DB, none expected).
- **#565** — SPEC owes two v1.1 unlocks: DEC-045 messaging/doorbell, and **§2.8 reservations, which
  does not exist at all**. Best written now that reservations has stopped moving.
- **#554** — crew can be confirmed to two boats the same day (concurrent self-claim). Decide alongside
  **#560**, which asks whether the whole-day rule should be a time-overlap rule instead. Do not rewrite
  DEC-077/078 until #560 is settled.
- **#561** — remove the two-protocol fork (`resolveProtocol` / `protocolOverride`); touches a table, so
  its own branch.
- Admin nav has accreted eight entries one task at a time and wants a pass.

**Context:**
- **The doc-consistency audit is CLOSED.** Every corpus swept F through Z — ~15 shards, ~200 findings,
  ~1.5M subagent tokens. Ledgers live in `docs/audit/2026-07-25/`; the README carries per-shard resume
  state and 14 lessons. Read the README before starting any future sweep.
- **`/kill-this` was never run this session**, so there are no per-task blocks and `points: 0` is
  recorded despite nine PRs. The work is real; the estimate isn't. Retro's velocity math will read this
  session as zero-point — worth a manual note at retro rather than trusting the number.
- **Parallel sweeps work.** C2.4–C2.7 and Z1–Z4 each ran four agents concurrently, ~10 min wall clock,
  same per-shard token cost as sequential. "Sequential, one agent per shard" is retired — the
  ledger-on-disk pattern was always what protected the orchestrator's context, not the sequencing.
- **The audit's dominant finding, three levels deep:** a change that lands in code updates the code and
  never the doc; a change that lands in a spec updates the spec and never the DEC; a DEC that supersedes
  another updates itself and never its target (28 one-way edges). The cheap detector — grep
  `DECISIONS.md` for "Amends SPEC §x" before reading any code — found 41 claims, 11 unlanded.
- **DEC numbers are now allocated on `main`.** Two branches drawing from one counter in one file is what
  produced the DEC-134/135/138 collision. `main` DEC-134/135 were deleted as never-decisions (operator's
  call); reservations' DEC-136/137 were their renumbered copies and went too; its DEC-138 became DEC-140.
- **Gotcha that cost time:** the crew sign-in e2e failure looked like the documented flaky empty-code read
  (#504) and wasn't — `aa7bf2b` had standardized seeded crew emails onto `@bb.test` without updating the
  tests. Unit tests stayed green because they build fixtures and never read the seed. Rule out the stale
  :3100 server first (it was clean), then check the seed before suspecting the store.
- **`db:all` is fully gone** — script, e2e spec, both DECs, and `RUNNING.md` now points at `db:reset:dev`.
- Production has `FILL_DEADLINE_HOURS=72`, not the 48 default. Shard E found prod carries 22 env vars
  absent from `DEPLOY.md`, so deployed values are not answerable from the repo.
- No production shift has ever been observed reaching the At-Risk board, so DEC-095's admin SMS may never
  have fired for real. A query for `admin_alert` outbox rows would settle it.
