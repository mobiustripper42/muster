---
session: 72
dev: eric
slug: main
branch: main
started: 2026-07-27T15:48:33Z
ended: 2026-07-28T17:38:10Z
points: 23
pr_numbers: [569, 573, 576, 580, 581, 585]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/a4605ece-de56-4d3d-b16d-c37462204117.jsonl
---

# Session 72 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: DECISIONS.md structural validator (564a)

**Completed:**
- `scripts/check-decisions.mjs` — no-dep text validator: duplicate id, index row with no body,
  body with no index row (DEC-TBD exempt), unresolvable `DEC-NNN` reference, stale footer tally.
- `scripts/check-decisions.test.mjs` — 17 vitest cases on hand-written fixtures, so editing
  `DECISIONS.md` never turns them red. `vitest.config.ts` include gains `scripts/**/*.test.mjs`
  (same argument that put `db/**` there).
- `package.json` — `check:decisions`, first in `verify` so it fails in ms, not behind the build.
  CI needs no workflow edit; the existing verify job already runs `npm run verify`.
- Three live defects it found and this PR fixes: the tally read "Indexed 124 of 124" against 137
  rows / 138 bodies; DEC-114 cited "DEC-100 submit spinner" (DEC-100 was renumbered to DEC-107 at
  the reservations merge — the spinner is DEC-089), copied into
  `components/admin/reveal-selected-row.tsx:22`; `src/builder/form-shifts.test.ts:284` cited
  `DECISIONS.md:2159` by line number and had already drifted off it (it's DEC-083).

**Preceded by an architect review of #564**, which found the issue's central claim false: two
branches adding `DEC-140-alpha.md` and `DEC-140-bravo.md` merge clean — git only conflicts on
add/add of the *identical* path. The split does not fix #562. Also corrected the issue's counts
(4,161 lines / 138 decisions / 2,299 citations, not 3,949 / 134 / 1,821) and found six non-numeric
ids (`DEC-MSG-1/2/3`, `DEC-ROLE-1`, `DEC-DATA-1`, `DEC-TBD`) that don't fit `DEC-NNN-slug.md` —
`DEC-DATA-1` alone has 142 occurrences.

**Scoped down from the architect's proposal:** branch protection with require-up-to-date was cut.
It catches the collision one step earlier at the cost of an Update-branch click on every merge —
wrong trade on a solo repo for something that has happened once. The check stands alone; a
collision now goes red on `push: main` instead of surviving until an audit.

**Code review:** five findings, four fixed in `e59926f`. The real one: an index row counted
anywhere above the first decision body, so an illustrative row in the preamble would fail the
build over a doc edit — and the preamble already writes its examples with `•` instead of `-`,
which is the tell someone hit it and worked around it. Rows now count only under a `### ` topic
heading. Also: a test that passed for the wrong reason (invented `DEC-999` fails the reference
scan regardless of the boundary), an untested `duplicate index row` branch, and a misleading
message for a decision heading missing its colon. Left as a documented tradeoff: the five
non-numeric DEC families are enumerated in the reference regex, so a sixth would silently drop
out of dangling-ref checking.
**PR:** [#569](https://github.com/mobiustripper42/muster/pull/569)
**Points:** 2
**Branch:** task/564a-decisions-validator
**Opened at:** 2026-07-27T16:37:00Z

## Task 2: Split DECISIONS.md into one file per decision + generated index (564b)

**Completed:**
- 138 decisions split to `docs/decisions/DEC-<id>-<slug>.md`; `docs/DECISIONS.md` keeps its path
  and becomes the generated index. 4,161 lines → 209. 415 KB → 20 KB, ~105k tokens → ~5k.
  All ~2,300 code citations still resolve.
- `scripts/gen-decisions-index.mjs` (generator, no deps — a YAML dep would have needed architect
  sign-off for one flat frontmatter block), `scripts/check-decisions.mjs` rewritten from 564a,
  19 vitest cases, `npm run gen:decisions`.
- `docs/decisions/_preamble.md` — the hand-authored partial; the preamble and three-state
  explainer are not decisions and had nowhere to live in the schema.
- DEC-141 records it, amending DEC-127 with scope "the maintenance rule only".
- Readers repointed: `code-review.md` and `architect.md` both told agents to open DECISIONS.md
  and read every governing entry, which now lands on a table of contents. Plus `pm.md`,
  `README.md`, `.claude/CLAUDE-context.md` (stale by ~125 decisions).

**The schema, not the split, is the load-bearing part.** Z2 found 428 cross-reference pairs, 384
one-way, 28 supersede-class — a DEC that changed another updated itself and never its target,
because the relation vocabulary was prose and prose is not checkable. Flat `amends: [DEC-105]`
wouldn't have fixed it: no relation, no scope, and scope is the load-bearing half of every good
back-pointer. `{id, relation, scope}` declared once generates both ends — the index annotation
AND a banner atop the amended decision's own file. The banner is what matters; readers arrive by
Ctrl-F, not via the index. 36 edges / 27 amended decisions, two of which Z2's source-side table
missed (DEC-061/063→DEC-007, DEC-079/081→DEC-010), found by cross-checking each body's existing
prose banner against the graph — Z2's finding running backwards.

**Round-trip: 3,096 content lines identical** across all 138, verified before anything was deleted.
"Byte-identical" (the original AC) was never achievable — the source isn't uniform about separators
(102 preceded by `---`+blank, 36 by a blank). Split/verify scripts deleted after use, not shipped
as dead code; reproduce from `HEAD~2`.

**Code review:** 5 findings, 3 real, fixed in `725cf9f`. (1) `stripBanner` collapsed blank runs
across the whole body, making the generator a non-fixed-point for any amended decision with a
double blank line — `gen` writes, `check` re-generates and calls the file it just wrote stale.
Masked only because no current body has one. (2) `load()` silently skipped anything not shaped
`DEC-*.md`; a copy at `dec-014-lowercase-probe.md` with a duplicate id passed clean. (3) DEC-007
shipped with the generated "Retired by" banner two lines above its old hand-written "⚠️
Superseded" — the exact prose-vs-vocabulary drift the schema exists to catch, reintroduced in the
commit fixing it, because DEC-007 gained its edges late from the prose cross-check.
**PR:** [#573](https://github.com/mobiustripper42/muster/pull/573) — closes #564
**Points:** 5
**Branch:** task/564b-split-decisions
**Opened at:** 2026-07-27T21:25:00Z

## Task 3: Money-path sweep + fixes (#522 sweep 1 of 3)

**Completed:**
- Scoped `@code-review` sweep over the 989-line money path. Run on **Opus, not the Sonnet
  the agent frontmatter pins** — a deliberate one-off for this surface, not a rule change.
- **Four of the five invariants #522 named came back clean**: integer cents throughout,
  charge-time values frozen into PI metadata, idempotency keyed on the charge id with real
  transactional guards (not read-then-write), DEC-134 double-write guard holding end to end.
  Signature verification, 500-not-200, and gratuity/fee netting also clean. The defects were
  all in failure paths.
- **Fixed #1 — crew tips unrecoverable after any post-commit failure.** `saveGratuity` sat
  inside `if (outcome === "booked")`; a throw after the commit → 500 → redeliver →
  `writeSlotBooking` short-circuits to `already` → that branch false forever. Silent, because
  `Payment.gratuityCents` still nets out of the customer balance while `splitGratuity` builds
  the crew pool from `Gratuity` rows alone. Money stays in Stripe, crew never paid. Every
  Elements booking. The gate bought nothing — the deterministic id + `on conflict do nothing`
  is what makes it idempotent.
- **Fixed #2 — refunded money recorded as collected revenue forever.** Added
  `markPaymentRefunded` to the port + both adapters; status derived in SQL from the row's own
  amount so a stale read can't record a full refund as partial. Contract-tested because pg
  says `greatest(coalesce(...))` and in-memory says `Math.max`.
- **Fixed #3 — missing `priceCents` booked a free boat** (`?? 0` → throw).
- Corrected `PaymentStatus`'s docstring, which claimed nothing in Muster issues a programmatic
  refund while the residual-race auto-refund has done exactly that since 12.1b.

**Filed, not fixed:** #574 (deposit balance repriced from the LIVE tax rate — real customer
billing defect, but DEC-107's amendment specifies that formula literally, so the decision moves
first; tagged @architect) and #575 (a declined card leaks a second 15-min hold onto a second
boat; three retries sell out a two-boat fleet with zero paying customers — availability, not
money).

**Unverified:** the Postgres leg of the new contract test. Muster's test DB isn't up on this
machine (only sailbook's containers are running); CI covers it with `TEST_DATABASE_URL` set.
**PR:** [#576](https://github.com/mobiustripper42/muster/pull/576) — refs #522, does not close it
**Points:** 3
**Branch:** task/522-money-sweep-fixes
**Opened at:** 2026-07-27T22:52:00Z

## Task 4: Auth sweep + fixes (#522 sweep 2 of 3)

**Completed:** (sweep run on Opus again — same deliberate deviation from the Sonnet pin)
- **Closed a live roster oracle.** `verifyLoginCode` returned `invalid` for unknown emails but
  `locked`/`expired` for roster ones — six wrong guesses enumerated the crew, and three channels
  leaked it (redirect, copy, AND the login cookie, which only some branches deleted). Collapsed to
  one failure value **in the domain**, not the boundary: a three-armed union invites the next
  caller to branch for friendlier copy, which is how it leaked.
- **Bounded brute force per subject.** `attempts` capped guesses per CODE and a re-mint reset it →
  ~7,200 guesses/day against 10⁶, parallel across the roster, no per-IP limit. Admins are crew
  (DEC-092) and `switchToAdmin` escalates in one click, so the prize was the operator account.
  Added a 24h/50 rolling window that survives the re-mint, enforced in the same row-locked
  statement as the per-code cap (pg computes `stale` once in a CTE).
- `dev-link` hand-copied the prod kill-switch predicate while two other files imported the shared
  one — two spellings of the guard in front of an unauthenticated admin-session minter.
- DEC-142 records both, amending DEC-081 scope "the cap only". Migration + both adapters +
  contract test; verified on real Postgres (`test:pg`, 106 pass).

**The tests were asserting the leak.** Two cases twelve lines apart pinned "known email → expired"
and "unknown → invalid" as the contract, so the oracle read as intended and any fix looked like a
regression. Rewritten, plus the symmetry test missing at every level — it asserts one distinct
serialization across all five failure branches.

**`CREW_SELF_SERVE=1` is live in prod** (operator confirmed; not answerable from the repo — shard E
found 22 prod env vars absent from DEPLOY.md). That's what moved the brute-force fix out of "file
it" and into the PR.

**Filed:** #578 (departed crew keep access forever — `readSubject` checks the live record for
admins only and sliding renewal makes it permanent; DEC-092 scoped that deliberately, so
@architect) and #579 (request-stage timing still enumerates — the cheap fix equalizes one of two
round trips and would read as solved while leaving a real gap; rate limiting is the right shape and
there is none anywhere in the app).

**Large CLEAN list in the PR** — GET-peeks/POST-consumes, reservation capability scope, session
crypto, cookie flags, single-use CAS, admin revoke, every crew route self-guarding. Worth reading
before sweep 3 so it isn't re-tread.
**PR:** [#580](https://github.com/mobiustripper42/muster/pull/580) — refs #522
**Points:** 5
**Branch:** task/522-auth-sweep-fixes
**Opened at:** 2026-07-28T07:55:00Z

## Task 5: Engine sweep + fixes (#522 sweep 3 of 3 — sweeps complete)

**Completed:** (Opus again)
- **Fixed a silent cross-shift double-book.** The accept path was the ONE seating path that never
  re-ran eligibility: `committedOnShift` scans a single shift, `notDoubleBooked` runs at fan-out
  when the candidate is committed nowhere. The tick's #393 day-spread normally covers it, but the
  **urgent blast path drops the day exclusion** (`tick.ts:306` passes only `suppression.working`).
  Two boats inside fills-by → two texts → two taps → both shifts green, one boat unstaffed. The
  At-Risk board can't see it because both resolve `Crewed`. Sequential, not a race — distinct from
  #554's concurrent-tap gap.
- **Fixed a ghost green shift.** `deriveShiftState` reads an empty required set as vacuously
  `Crewed` — right for a zero-crew Duffy, and it also swallowed "no `Vessel` row." That shift went
  invisible to the tick, the At-Risk board, and self-claim. Reachable by adding a boat to
  `RESOURCE_MAP` without re-seeding (`seedFleet` only runs by hand).
- **Fixed credential health's UTC "today"** — disagreed with the oracle for 4–5h every evening,
  flagging EXPIRED while the engine still seated the person. Its own docstring claimed they agreed.

**The #560 interaction is the thing to remember.** Fixing the double-book meant enforcing the
whole-day rule harder — the exact rule #560 may replace with time-overlap. Resolved by calling the
**shared** `notDoubleBooked` rather than writing a second date check, so #560 stays a one-site
change every path inherits. Operator chose fix-now over settle-#560-first.

**Two tests were hiding bugs, again.** The credential boundary test only passed because its `NOW`
was exactly midnight UTC — testing its own construction, not the rule. And the new double-book test
was negative-controlled (disable the guard → only that test fails).

**Big CLEAN list in the PR** — zero clock leaks in the engine, vessel-local dates correct
throughout including DST spring-forward, every other seating path gating properly (the manual
override enforces the rating floor while deliberately skipping PTO, exactly per DEC-119). Worth
reading before anyone re-audits this area.

**Housekeeping → #558 as a comment** (hardcoded `poolExhausted: false`; `broadcastAsk` dead with
tests that read as coverage of a shipped path; `buildRoster`/`standingOf` dead AND reading the
stored `reliabilityScore` DEC-008 forbids ranking off). Also confirmed #558's mod-1440 skew is a
genuinely distinct root cause — display-only, no engine decision reads it.
**PR:** [#581](https://github.com/mobiustripper42/muster/pull/581) — refs #522; all three sweeps run
**Points:** 5
**Branch:** task/522-engine-sweep-fixes
**Opened at:** 2026-07-28T08:40:00Z

## Task 6: Money review follow-ups (#576 post-merge review, then a second round)

**Completed:** two commits — fixes from reviewing the merged #576, then fixes to those.

Round 1: `requireCents` validated the coercion not the string (`Number("  ")` is 0, so
whitespace booked at price zero — the exact defect it existed to prevent); the throw fired
before `recordPayment` so a bad-metadata charge left no Payment row and no alert;
`balanceOwedCents` counted only `succeeded`, which stopped meaning "not refunded" the moment
`markPaymentRefunded` made `partially_refunded` writable.

**Round 2 caught a bug round 1 introduced, and it was the worst of the day.** Netting the
refund attributed it to fare FIRST, so **refunding a $100 tip re-billed the customer $100 of
fare they'd already paid** — and `createBalanceCheckout` would mint that charge with nobody
in the loop. A tip-only refund is the likeliest partial an operator makes. Now nets against
gratuity+fee first. Also: the status filter became a fail-OPEN deny-list on a bare `string`
(now `PaymentStatus`); `paidCents` in two views still filtered `succeeded` while the balance
didn't, so a partially refunded booking showed "Paid $0.00" beside a netted balance (now one
shared `countsAsPaid`); and the try wrapped the whole write, so a pg blip alerted "unusable
metadata — REFUND MANUALLY" for a booking that lands on the next retry.

**The lesson worth keeping:** round 1 changed the balance deriver and touched **zero test
files**. That is exactly how the tip bug passed a green suite. Nine tests added.

**Code review:** run on Opus against the branch's own diff — the `/kill-this` step I'd skipped
on tasks 3–5, having let the sweep stand in for it. It doesn't: a sweep reviews the code
before the change. Every diff-review this session found something.
**PR:** [#585](https://github.com/mobiustripper42/muster/pull/585) — refs #522
**Points:** 3
**Branch:** task/522-money-review-followups
**Opened at:** 2026-07-28T12:25:00Z

**Next Steps:**
- **Operator's plan: start at the top of the issues list and work down.** In the order I'd take them:
  - **#586** admin nav overlaps at 1280px — 12 links no longer fit; visible, small. Fixing it via the
    flag filter (direction 1) halves the bar and is the same one-liner `Messages` already uses.
  - **#588** gate the Stripe webhook on `RESERVATIONS` explicitly — one line; converts "inert because
    nothing upstream can fire" into "inert because we said so." Removes the dependency on #544.
  - **#589** the backwards-amendment guard never runs for the five non-numeric DEC ids — live defect
    in today's #573 machinery, and it must be fixed **before** any seeds backport (seeds' record is
    entirely `DEC-S###`, so every edge there would go unchecked).
  - **#584** integrity diagnostic scans 12 tables of ~38; `payments` and `gratuity` are unchecked.
  - **@architect calls, each needing a decision before code:** #574 (deposit balance repriced from
    the live tax rate), #578 (crew sessions never revoked — DEC-092 rejected the stateful check on
    cost grounds that may now be stale), #582 (ghost vessel — needs `deriveShiftState`'s contract to
    distinguish "manning unknown" from "zero required seats").
  - **#575** declined card leaks a second 15-min hold; **#579** request-stage login timing;
    **#558** housekeeping pile (now includes three more from sweep 3).
- **Seeds handoff evaluated, not started.** Second opinion given: fix #589 first, then TOPICS
  extraction with **all three** welded-in strings (TOPICS, the id-family regex, the hardcoded
  `npm run gen:decisions` messages), then seeds. **Hold the seeds-version bump** — it parks the whole
  fleet out of sync to migrate one repo, and sailbook is frozen on its redesign so there's no second
  consumer to gate. Handoff's DEC-numbering caution (lines 251–253) is stale: the branch divergence
  is resolved, next free id is **DEC-143**.
- **#522 is complete** — all three sweeps run and actioned. Worth closing.

**Context:**
- **Every diff review found something, including twice on my own fixes.** The worst: netting a refund
  against fare first, so **refunding a $100 tip re-billed the customer $100 of fare they'd already
  paid** — and `createBalanceCheckout` mints that charge with nobody in the loop. Shipped in the
  morning, caught in the afternoon, fixed in #585. The `/kill-this` code-review step was skipped on
  tasks 3–5 because the sweep felt like it covered it. **It doesn't** — a sweep reviews the code
  *before* the change. Don't let a sweep stand in for the diff review again.
- **Three separate instances today of "green because it isn't looking":** the integrity diagnostic
  covering 12 of 38 tables (#584); the form-shifts fix a cron silently undid within one tick (#581,
  reverted); the amendment guard inert for five id families (#589). Whenever a check is added, ask
  what would make it *stop matching* and pin that with a negative control. Three of today's fixes
  were verified that way — disable the fix, confirm exactly one test fails.
- **Tests were part of the defect twice.** Two auth tests twelve lines apart pinned the roster oracle
  as the contract, so any fix looked like a regression. The credential-health boundary test passed
  only because its `NOW` was exactly midnight UTC — it tested its own construction, not the rule.
- **Clicking found things reading didn't.** Sweep 3 listed the manual override as clean because it
  checked against DEC-064's *stated* scope; watching an override go through showed the credential
  gate was never consulted. (Operator's ruling: that's the design — closed #587. Consider a line in
  DEC-064 so the next sweep doesn't re-raise it.)
- **`RESERVATIONS` flag audit: money cannot move with the flag off.** `book/checkout/actions.ts:52`
  is the only live PaymentIntent entry and it's gated, so no checkout → no reservations → no
  capability tokens. `addPostTip`, the `.ics` route and the webhook are ungated but inert by
  consequence. Six admin nav entries are NOT gated (→ #586/#588).
- **Prod ref-integrity is clean**, after deleting 105 orphaned outbox entries. Cause was a hand-run
  `delete from asks … where sh.date < '2026-07-13'`; no FKs by design (DEC-131), so hand SQL can't
  know about dependents. After any manual delete, run the orphan scan in #584 — it covers the money
  tables the diagnostic doesn't.
- **The admin handle is `eric`, not `spink`.** Four dev scripts still print `?admin=spink` as the
  instruction: `db/seed-atrisk-dev.ts`, `db/seed-outbox-dev.ts`, `db/seed-split-dev.ts`,
  `db/mint-link.ts`.
- **The hydration mismatch in the dev log is not ours.** Nothing in the app sets `caret-color`;
  Playwright's screenshot `caret: "hide"` default and password-manager extensions both inject it.
- **neon MCP is now authenticated** (`claude mcp login neon`) — its tools weren't available in this
  session because the deferred-tool set is fixed at session start. Next session gets direct DB reads.
- Screenshots from the walkthrough: `/tmp/muster-shots/` (ephemeral).
