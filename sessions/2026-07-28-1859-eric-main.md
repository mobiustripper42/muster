---
session: 73
dev: eric
slug: main
branch: main
started: 2026-07-28T18:59:24Z
ended: 2026-07-30T02:53:40Z
points: 22
pr_numbers: [591, 592, 595, 596, 597, 602, 604, 605]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/b12bc378-9227-4a82-a149-82ab25217915.jsonl
---

# Session 73 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: The backwards-amendment guard now runs for every DEC id family (#589)

**Completed:**
- `rank(id)` + `compareDecisionIds(a, b)` in `scripts/gen-decisions-index.mjs` — one ordering
  source for the guard AND `sortKey`, so display order and the check can't drift apart (the
  issue flagged sortKey's numeric-only branch as the same root assumption).
- `checkAmendmentEdge(from, to)` extracted in `scripts/check-decisions.mjs` as a pure function,
  with a **third** outcome: earlier → pass, not-earlier → the existing failure, **uncomparable →
  a red build naming the pair**. The old code bailed whenever either id missed `^DEC-(\d+)$`,
  so it never ran for DEC-DATA-1, DEC-MSG-1/2/3, DEC-ROLE-1, DEC-TBD.
- `docs/DECISIONS.md` regenerated — 10 lines, the five family rows moving to their chronological
  slot. DEC-TBD doesn't move (alone under *Open questions*).
- 12 tests. **Negative control ran:** reverting the guard turns exactly 3 red, nothing else.

**14.5 is evidence, not taste.** The pre-split DECISIONS.md was chronological and put all five
families between DEC-014 and DEC-015 (`f41a895^:docs/DECISIONS.md:374–472`). That number is
load-bearing because **the id is the only time proxy the record has** — no decision file carries
a `decided:` field. Worth remembering before anyone touches DEC numbering again.

**Cross-family stays uncomparable on purpose.** The pre-split file also gives MSG < ROLE < DATA,
but that's document order within one sitting, not evidence about when each was decided. Ranking
off it would manufacture a fact the record doesn't have. So `DEC-MSG-2 amends DEC-ROLE-1` now
**fails the build** — inert today (37 edges, none non-numeric), and if one is ever wanted it takes
a human decision rather than a config toggle.

**Renumbering the families was considered and rejected — no issue filed, operator's call.** There's
no gap between 14 and 15, so it would have to append: `DEC-DATA-1` → `DEC-143`, an id claiming to be
the newest decision when it's the 15th-oldest. That inverts the very invariant this guard asserts.
The strictly-better version — add `decided:` frontmatter, guard on dates, ids become pure labels —
is a real architecture change (~140 backfills, recoverable only approximately from `git log -S`).
Cost of the rename alone if it ever comes up: 373 occurrences across ~140 files (DEC-DATA-1 146,
DEC-ROLE-1 72, DEC-MSG-* 111, DEC-TBD 44), and it would silently stale the session files on the
orphan `sessions` branch.

**Code review:** Clean. Notable because the reviewer re-derived the claims rather than trusting them
— re-ran the generator to confirm byte-identical output (fixed point holds), traced every path where
`null` reaches a caller, and reimplemented the pre-fix regex to reproduce the negative control itself.
**PR:** [#591](https://github.com/mobiustripper42/muster/pull/591) — closes #589
**Points:** 2
**Branch:** task/589-amendment-guard-families
**Opened at:** 2026-07-28T20:12:00Z

## Task 2: Declared spec amendments — the DEC→SPEC leg of the rot pattern (#590)

**Completed:**
- `amends_spec: [{section, scope}]` in frontmatter; `gen:decisions` writes the reciprocal block
  under that SPEC section's heading; `check:decisions` fails on a section that doesn't exist, a
  declaration with no scope, stale blocks, or `**` in a scope. `parseFrontmatter` generalized —
  it hardcoded `- id:`, so `- section:` couldn't parse. DEC-143 records it, extending DEC-141.
- 16 declarations across 14 decisions, 9 sections annotated. 13 new tests (43 total).
- **`LOCKED v1.0` stamp deleted** (operator's call, mid-task). DEC-014's live half kept.
  `.claude/CLAUDE-context.md` repeated the claim and had drifted on counts (said 139 / DEC-001–141).

**The conversion pass was the task; the mechanism was small.** The issue estimated "6 files, 7
claims — a quick pass." Actual: 47 files reference `SPEC §` in four prose shapes, mixed with
`implements` / `refines` / `cited by` relations that are NOT amendments. Estimated 5, took 5.

**Three findings, and the first is the one to remember:**
1. **SPEC §1.3 credited DEC-138 twice** (`:267,310`) for what is DEC-140's rewrite — fallout from
   last session's collision renumber. It never *dangled*, because DEC-138 is a real decision, so no
   check was ever capable of catching it. **A wrong-but-real reference is invisible to every guard
   we have** unless something checks the claim's substance.
2. Three claims had never landed (DEC-061→§2.6, DEC-140→§1.3). DEC-045→§2.6.3 is NOT a claim —
   it says outright it's "not a correction to the frozen v1.0 baseline." Excluded deliberately;
   sweeping in non-amendments is how the signal dies.
3. DEC-016 cites "SPEC §1 glossary"; the glossary is §0.4.

**Code review found a defect and it was mine, in the content not the code.** DEC-085's scope said
the board groups "day → boat" — it's **day → time**, with vessel identity as a hue and explicitly
NOT a grouping axis. I wrote it from a truncated read of the decision's own line, and the mechanism
had already published it into SPEC §2.3 as a *checked* pointer. **`check:decisions` verifies an
anchor resolves and a pointer landed — never that the text is true.** The generator makes a false
claim more authoritative, not less. Also fixed: the no-scope guard tested falsiness, so a
whitespace-only scope rendered an empty banner.

**Asked to strip redundant prose pointers from SPEC and didn't — reported instead.** Nothing in the
annotated sections is purely a reciprocal pointer; every hand-written citation is inline on the
bullet it corrects or is a reconciliation note with real content. My own earlier framing
("now-redundant prose pointers") had overstated it from one example. Stripping would have deleted
information to satisfy an instruction premised on my error.
**PR:** [#592](https://github.com/mobiustripper42/muster/pull/592) — closes #590
**Points:** 5
**Branch:** task/590-amends-spec
**Opened at:** 2026-07-28T22:05:00Z

## Task 3: check:context — context docs carry pointers, not inventory (#593 fallout)

*(Logged retroactively — this shipped without `/kill-this`, mid-conversation. Same slip as tasks
3–5 last session, different flavor.)*

**Completed:**
- `scripts/check-context.mjs` + tests, in `verify` at ~0.2s: every repo path and glob cited in
  `CLAUDE.md` and `.claude/CLAUDE-context.md` must resolve. 32 claims covered.
- Fixed the two bad claims in `.claude/CLAUDE-context.md` (channel adapters; the two horizon
  numbers). Seeds backport: **seeds PR #150** — script + tests + a Workflow Notes rule.

**Why this exists — the error is the point.** I read "Twilio/SMS = later swap" from the context
file and filed #593 declaring a feature blocked on an adapter that shipped at 9.4/#225, then
explained the blockage at length. `src/adapters/twilio-channel.ts` was one `ls` away and I never
looked. **The five-day doc audit could not have caught it** — that audit compared docs to DOCS;
this claim was false against CODE, the corpus doc sweeps never read.

**The rule, which is the durable part:** context docs carry decisions, rationale and POINTERS —
never inventory. Rationale doesn't rot; a snapshot of current state is stale the day code moves.
`ls src/adapters/*-channel.ts` sends the reader to the truth instead of copying it, and is
checkable. Corollary: an env-overridable number is a claim about a *deployment*, not a fact a
checkout can state — that's why the horizon numbers are gone rather than corrected.

**Code review found two, both real, both mine:**
1. **The check was blind to its own worked example.** `ls <path>` spans contain whitespace, so the
   two authoritative-list pointers — including the one quoted in the script's own comment as the
   model — were never matched, while the doc claimed coverage.
2. **The shell-out was exploitable.** It ran `bash -lc "ls ${pattern}"`; the reviewer executed a
   command from a doc-only payload (`src/*;touch$IFS/tmp/x`). Now `fs.globSync`, no shell. Worth
   remembering: **the premise of the check is that docs get less scrutiny than code**, so a docs
   diff must never be a code-exec path.

**Deliberately narrow.** First draft flagged 16, 15 noise. A check that cries wolf gets muted,
which is worse than none. It verifies existence, never characterization — stated in the script,
because a guard whose blind spot is undocumented gets trusted for what it never checked.
**PR:** [#595](https://github.com/mobiustripper42/muster/pull/595) · seeds [#150](https://github.com/mobiustripper42/seeds/pull/150)
**Points:** 3
**Branch:** task/593-context-check
**Opened at:** 2026-07-29T02:30:00Z

## Task 4: Point the agents at the context file; drop two stale prompt lines

**Completed:**
- Re-applied the idea from the **closed #594**, whose branch was 4 days stale against agent files
  rewritten twice since — no text to merge, only an idea.
- `.claude/agents/architect.md`: context read as Step 0 item 4; SPEC amendment blocks (DEC-143)
  noted; the stale "Numbering caution" replaced. `.claude/agents/code-review.md`: same, as Step 1b.

**Where it differs from #594, and the reasoning worth keeping:** #594 replaced the shift
*definition* with a pointer. Agent files are **prompts** — present content beats fetched content,
and both files already need explicit instructions to read the decisions and SPEC, so a pointer
converts a guaranteed fact into a compliance step. An agent that skips it reviews confidently with
no idea what a shift is. The definition is also a *decision* (DEC-005, 8.3/8.4), not inventory —
not the class that rots. So: definition stays, elaboration moves out, and the **consequence gets
sharpened at the point of judgment** ("a UNIQUE index keyed that way is a defect").

**Two stale lines out**, same class as the #593 error: the architect's `feature/reservations`
numbering caution (resolved at #566; `check:decisions` now catches duplicates outright), and
neither agent knowing SPEC sections carry generated amendment blocks — the #592 machinery was
invisible to the two readers who most need it.

**Code review:** one structural finding, fixed — code-review's context read was a bare paragraph
while architect's was a numbered step. Notable for *how* it reviewed: it disclosed it was editing
its own prompt, then verified every claim against the repo — both step cross-references, all four
`check:decisions` failure modes traced to their lines and the script run live, and the 8.3/8.4
specifics confirmed present at `.claude/CLAUDE-context.md:9` before agreeing nothing was lost.
**PR:** [#596](https://github.com/mobiustripper42/muster/pull/596)
**Points:** 1
**Branch:** task/agent-context-pointer
**Opened at:** 2026-07-29T03:45:00Z

## Task 5: Gate the Stripe webhook on RESERVATIONS explicitly (#588)

**Completed:**
- The gate lives in the **core** (`src/reservations/booking-webhook.ts`), because verification is
  there — `parseEvent` is the first line and throws on a bad signature. Flag injected as
  `reservationsEnabled: boolean`; the core never reads `process.env`. **Required, not
  optional-defaulting-true** — a default lets a future caller skip the gate silently.
- **Alerts, not just acks.** Reaching the branch means a verified charge SUCCEEDED and we chose
  not to book it. With the flag off nothing can mint a new intent, so it's a replay, a dashboard
  send, or a flag-on leftover — money already moved in all three.
- `reservationsEnabled()` in `app/lib/flags.ts`; five hand-spelled
  `process.env.RESERVATIONS !== "true"` collapsed onto it. Same shape as the auth-sweep defect
  where `dev-link` kept its own copy of the prod kill-switch. Left `"true"` vs the siblings' `"1"`
  alone deliberately — it's set in deployed envs, and changing the accepted value turns the
  feature off on a deploy nobody re-read.

**Code review found a real money regression, mine, and this is the one to remember.** I first put
the gate right after signature verification — the position that *looks* safest. It sits ahead of
purpose dispatch, and **balance + post-trip gratuity collection ride the same webhook against
existing reservations**. `createBalanceLink` is admin-gated with no RESERVATIONS check, and the
flag is **off by default** — so in a default deployment Stripe charges the customer for the
balance, `recordBalancePayment` never runs, and the operator gets an alert about new-booking
readiness describing something that didn't happen. Moved into `processBookingCharge` (booking path
only). Side benefit: DEC-134 shadow intents now return before the gate, so no double-alert.

**Two negative controls run:** gate above `parseEvent` → the bad-signature test reddens (that
ordering is what stops the flag being used to probe the endpoint); gate back at the original
placement → the **balance** test reddens. Only a test exercising the *other purpose* could tell
the two placements apart — which is exactly why it shipped green in my first pass.

⚠ Money path — flagged for `/code-review ultra` before merge. The per-diff review already found
one real defect on this branch, which is the signal that trigger exists for.
**PR:** [#597](https://github.com/mobiustripper42/muster/pull/597) — closes #588
**Points:** 2
**Branch:** task/588-webhook-flag-gate
**Opened at:** 2026-07-29T00:35:00Z

## Task 6: Admin nav — grouped by cadence, hub retired, back-buttons gone (#586)

**Completed:** (PR #602, stacked on #597 — merge that first)
- Flag-gated the six reservations links; widened the nav container off the body-copy `max-w-3xl`
  (the root cause: nav content was capped at 768px no matter the monitor).
- **Grouped by cadence:** flat Shifts · Calendar · Audit · Import · At-Risk, then Bookings ▾
  People ▾ Setup ▾ Settings ▾. Nine slots where thirteen didn't fit.
- **Five surfaces reached the nav for the first time** — time off, audit, payroll, integrity
  check, engine pause. They lived only on the `/admin` hub. New `/admin/settings` hosts the pause
  control; `/admin` now redirects to the shifts board and the hub is gone.
- Removed 12 `<BackLink href="/admin">` plus 3 hand-rolled variants. Root font 80% → 93.75%.

**Three things the operator found by using it that I had built wrong:**
1. **First fix was worse than the bug.** Letting the link row scroll put 665px of content in a
   443px box with the scrollbar hidden — a tidy-looking bar silently missing four links. The
   original collision was at least self-evidently broken.
2. **The drawer's groups shipped expanded** on the theory that a drawer has vertical room a bar
   doesn't. Seventeen rows is longer than a phone. Collapsed now.
3. **Groups didn't close on navigation.** The nav lives in the layout, so a client route change
   doesn't remount it, and `open` is uncontrolled — Bookings ▸ Customers left the panel hanging
   over the new page.

**@ui-reviewer, run live at five widths, found seven — the one I'd never have caught: ten pages
printed the RETIRED taxonomy** (`Settings / Blocks` when Blocks is under Bookings; `Reservations /
Calendar` when Calendar is flat). The same PR that renamed the shelves left every page naming the
old ones. Also: **rem-sized boxes can never hit a fixed touch minimum on a scaled root** — `h-9`
measured 28.8px at 80%, 31.5px at 87.5%, 41.25px at 93.75%. Fixed px now, measured 44×44. Plus
tenant line at 2.66:1, unGated motion, and a panel left open when focus tabbed away.

**The `/admin` hub is why five surfaces were orphaned:** two menus, each needing hand-editing, so
adding a surface meant editing one and forgetting the other. Worth remembering as a shape, not
just an incident.
**PR:** [#602](https://github.com/mobiustripper42/muster/pull/602) — closes #586
**Points:** 5
**Branch:** task/586-admin-nav
**Opened at:** 2026-07-29T05:10:00Z

## Task 7: Tab title names the environment (Dev-Muster / Pre-Muster)

**Completed:** `app/layout.tsx` — `Dev-Muster` local, `Pre-Muster` on a Vercel preview, plain
`Muster` in prod, off the same `NODE_ENV`/`VERCEL_ENV` pair as the coloured top-edge line and the
DEC-057 dev-link gate, so the three can't disagree. Also on `appleWebApp.title`.

**Prefixed, not suffixed** — operator's correction, and the reason is worth keeping: a tab is a
few characters wide with a dozen open and truncates from the RIGHT, so a suffix vanishes exactly
when you have enough tabs open to need it.
**PR:** [#604](https://github.com/mobiustripper42/muster/pull/604)
**Points:** 1
**Branch:** task/env-title
**Opened at:** 2026-07-29T06:05:00Z

## Task 8: Integrity coverage — 5 more tables, and the next gap is a red build (#584)

**Completed:**
- Newly checked: `notice_outbox`, `ring_outbox`, `muster_owned_vessel_days`, `audit_events`,
  `vessels.home_location_id`. All via port methods that already existed.
- `src/admin/integrity-coverage.ts` classifies all 37 tables as **fk / checked / exempt-with-
  reason**; its test reads the table list from `db/migrations/*.sql`, so **a new table is a red
  build** until someone decides which net catches it. Ten tables remain uncovered, enumerated as
  needs-port-method rather than silently absent — that's the follow-up.

**#584's headline was stale and checking beat believing.** It claims nobody watches
`payments.reservation_id` and `gratuity.*` — those got real FKs on 2026-07-22, and in prod the
tables don't exist at all (prod is 16 migrations behind). The operator remembered the FK work; I'd
have built against the issue text.

**Code review found the only non-trivial new check was dead.** I gated the audit-actor check on
`actorKind === "crew"` — the kind that never carries an id. `domain/audit.ts` says outright that
`actorId` is the admin's crew id for `admin`, a source tag for `importer`, undefined for the other
two. So the branch couldn't execute, `admin` went unchecked, and **my test asserted the inversion
as intended** with a backwards rationale. Green proved nothing. This is the third time today a
test locked in a bug — the pattern is writing the test from the same wrong mental model as the code.

**Also:** the coverage map caught two errors in my own classification the moment it existed
(`customers` is the FK's parent, not its holder; `reservations` is covered by both nets).
**PR:** [#605](https://github.com/mobiustripper42/muster/pull/605) — closes #584
**Points:** 3
**Branch:** task/584-integrity-coverage
**Opened at:** 2026-07-29T06:30:00Z

**Next Steps:**
- **#600 is fully specced and is the next build.** A losing ask is never closed, so it fires an
  unearned `ask_ignored` whenever the seat later reopens. Four parts: close losers as `withdrawn`
  (operator's word — nobody replaced them), no reliability event, a `withdrawn` ask must not count
  toward the asked-set, and **exclusions clear once when a shift ENTERS at-risk** (not just on a
  bail — proven by Brew 1 · Aug 1, which reached at-risk with no bail anywhere in its history).
  Bounded once-per-transition or the urgent branch texts the same people every 15 minutes. Needs
  **DEC-144** with `amends_spec` on §1.2 — the first real use of the #592 machinery.
- **Prod data from that bug, operator's hands:** 34 stale asks armed on filled seats (close as
  `withdrawn`), and ~6 false `ask_ignored` events. The second is a decision, not a cleanup —
  `reliability_events` is append-only (DEC-008). Recommendation was to delete the six.
- **PRs open at close:** #602 (admin nav — merge #597 FIRST, it's stacked), #604 (tab title),
  #605 (integrity coverage). Seeds #150 separate repo.
- **#601** an `Asked` seat has no operator action until timeout — the cockpit is read-only most of
  the time because a drip seat is `Asked` by default. #599 admin-vs-crew SMS labeling.
- **#598** make "not crewed" unmissable on the shifts board; that unblocks shelving At-Risk out of
  the nav's flat row (it's there now only because the shifts board doesn't show at-risk state).
- **#584 follow-up:** ten tables still need a `listAll*` on the port + both adapters + a contract
  test before the diagnostic can walk them. Enumerated in `integrity-coverage.ts`.
- **Prod is 16 migrations behind** and its code is v1.0.19 (Jul 23) — consistent with each other,
  just old. `/promote-production`'s ledger check will stop you; apply them by hand first.
- Nav leftovers, not filed: breadcrumbs are decorative `<p>` text, not links (#502 is the real
  fix); the 52px bar budget vs a 44px touch target is unresolved at iPad-landscape width.

**Context:**
- **Three defects this session were tests locking in the bug**, all from writing the test with the
  same wrong mental model as the code: the auth roster oracle (yesterday), the DEC-085 nav scope,
  and the audit-actor gate (`actorKind === "crew"` — the one kind that never carries an id, with a
  test asserting that as correct). Green proves nothing when the test and the code share the
  premise. **The negative control is the only thing that catches it** — disable the fix, confirm
  exactly the intended tests redden. Run it every time.
- **Docs about code state are the rot; docs about decisions are not.** `.claude/CLAUDE-context.md`
  said "Twilio/SMS = later swap" for weeks after `src/adapters/twilio-channel.ts` shipped, and I
  filed an issue declaring a feature blocked on an adapter that had existed since June. The
  five-day doc audit couldn't have caught it — that audit compared docs to DOCS. `check:context`
  (#595) now asserts every cited path resolves, and the standing rule is pointers, not inventory.
- **`rem` sizing can never hit a fixed touch minimum on a scaled root.** `h-9` measured 28.8px at
  the old 80% font, 31.5px at 87.5%, 41.25px at 93.75%. Anything with a 44px floor needs literal
  px. This is why the hamburger kept getting flagged and kept not being fixed.
- **The `/admin` hub is why five surfaces were orphaned.** Two menus, each hand-edited, so adding
  a surface meant editing one and forgetting the other. The shape matters more than the incident:
  any second list of the same things will drift.
- **A "fix" that makes a failure quieter is a regression.** The nav's first fix hid four links
  behind an affordance-free scroll; the original collision was reported within one usage cycle
  precisely because it looked broken. @ui-reviewer called it worse in kind, not degree.
- **`main` had been rendering a fifth smaller than production** since the reservations merge —
  `html { font-size: 80% }` from 2026-07-21. Now 93.75% (15px).
- **Prod diagnosis worked well through the Neon MCP** — reading `asks`, `reliability_events` and
  `_migrations` directly turned "captains got no-replied in one tick" into an exact timeline in
  minutes. Read-only; that posture held.
- Wall clock spans an overnight — started 18:59 on the 28th, closed 02:53 on the 30th.
