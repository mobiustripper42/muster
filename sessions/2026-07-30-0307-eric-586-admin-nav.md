---
session: 74
dev: eric
slug: 586-admin-nav
branch: task/586-admin-nav
started: 2026-07-30T03:07:20Z
ended:
points:
pr_numbers: [606]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/b32920a9-7c5e-40d8-9075-d78ca9d5d40b.jsonl
---

# Session 74 — 586-admin-nav

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Completion is swept by the tick, and a self-claim scores (#570)

**Completed:**
- **Completion sweep** in `src/builder/tick.ts`, deliberately ABOVE the past-trip guard — that
  `continue` walking away from departed shifts is exactly why nothing ever completed one. Fires on
  derived shift end passed AND ≥1 required seat still `Confirmed`, so a boat nobody crewed never
  mints +5s. Not gated on shift state: `Pending` is claimable, so a shift that never entered the
  staffing window can still have run.
- `self_claim` +4, emitted by `claimSeat` on the **CAS win only**. `Event.durationMinutes` +
  additive migration, frozen off `Offering.tripLengthMinutes` in `writeSlotBooking`.
  `shiftEndFromEvents` → `max(start + duration)`. `self_claim` mapped into the audit trail.
- **DEC-144**, correcting DEC-078 and annotating SPEC §1.4 via the #592 `amends_spec` machinery.
- 1668 unit tests + 108 against real Postgres (which is what actually verified the migration).

**The scope shrank because the operator caught me conflating two things.** I first specced this as
needing an `Event → Offering` link, having grepped `offeringId` and found it only under
`src/reservations/`. He pushed back: *"we built a reservation system to replace Xola"* and *"we know
trip length from the Offering."* Both true — and reading rather than grepping found `writeSlotBooking`
already receives `offeringId` and **drops it on the floor**. Then his own question killed the link
idea outright: *what if a guest pays for extra time after booking?* Then duration is a fact about the
**event**, not the offering. `derive.ts:418` had already named the field years earlier — *"swap this
constant for `Event.durationMinutes` when that field and its source arrive."* Freezing it, like
`price` (DEC-125) and `extrasCents` (#474), dropped the three-consumer refactor I'd quoted and took
the estimate 8 → 5.

**He also split reliability-grade from money-grade himself:** *"maybe we phone in completed for
reliability, then add MONEY-COMPLETED when we need that."* That's in DEC-144 explicitly, so nobody
hangs tip payouts off this signal later without revisiting the evidence. Correction worth keeping: I
cited the parked FUTURE_IDEAS row saying tip distribution is blocked on tax/payroll — he says it's
100% blocked on engineering, and needs money-grade completion. The row is stale on that point.

**Code review found three, all real, and the first contradicted a comment I shipped in the same
commit.** `formShifts`'s reconcile path recomputed state from the seat fold — and neither
`deriveShiftState` nor `resolveShiftState` can yield `Completed`, so the next Xola pull after any
completion folded the shift back to `Crewed` off its still-`Confirmed` seats, and the next tick
re-completed it and **double-logged `shift_completed` per occupant**. The pre-existing guard covered
only the all-events-cancelled branch. `pullXola` calls `formShifts` on every manual pull, so this was
the ordinary next import, not a race — and I had written "a Xola re-import can't fold it back."
Second: **"gaming is already closed" was false** — `bailLatenessMs` clamps to 0 past the staffing
lead, so claim-then-release 7+ days out nets **+1**, not ≤0, across most of a 45-day claimable
window. Corrected in DEC-144 and the scorer rather than left standing, with the real mitigation
stated: the count-based window makes the pair cost two of forty slots for +1 where answering "out"
earns +1 in one, so the exploit is dominated by behavior we already want.

**Three negative controls run, each reddening exactly its own tests and nothing else** — revert
`max(start+duration)` → 3; log `self_claim` before the CAS → 1; revert the `formShifts` guard → 1.
That last one is the habit paying for itself: it's the same class as last session's three
tests-locking-in-the-bug, except the reviewer caught the premise before a test could freeze it.

**Worth knowing: this turns on ~10 dormant `Completed` branches for the first time.** Every surface
guards the state defensively, but those guards have been dead code. A completed shift now leaves the
shifts board, My Shifts, and the shift thread **the same evening** rather than at midnight rollover
(`all-shifts.ts:132`, `crew-view.ts:174,205`, `thread-list.ts:150`). Inert in prod today — `MESSAGING`
is off by default — but live in e2e. If same-evening disappearance is wrong, that's a follow-up on
those guards, not on the sweep.

**Code review:** 3 findings, all real, all fixed — one double-scoring bug on the Xola re-import path,
one false claim in a DEC I'd just written, one duplicated comment.
**PR:** [#606](https://github.com/mobiustripper42/muster/pull/606) — closes #570
**Points:** 5
**Branch:** task/570-shift-completion
**Opened at:** 2026-07-30T04:12:00Z

**Next Steps:**

**Context:**
