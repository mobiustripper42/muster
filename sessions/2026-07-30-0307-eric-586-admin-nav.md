---
session: 74
dev: eric
slug: 586-admin-nav
branch: task/586-admin-nav
started: 2026-07-30T03:07:20Z
ended: 2026-07-31T21:33:23Z
points: 15
pr_numbers: [606, 607, 610, 611]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/b32920a9-7c5e-40d8-9075-d78ca9d5d40b.jsonl
---

# Session 74 — 586-admin-nav

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Completion is swept by the tick, and a self-claim scores (#570)

**Completed:**
- **Completion sweep** in `src/builder/tick.ts`, deliberately ABOVE the past-trip guard — that
  `continue` walking away from departed shifts is exactly why nothing ever completed one. Fires on
  derived shift end passed AND ≥1 required seat still `Confirmed`, so a boat nobody crewed never
  mints +5s. Not gated on shift state: `Pending` is claimable, so a shift that never entered the
  staffing window can still have run.
- `self_claim` +4, emitted by `claimSeat` on the **CAS win only**. `Event.durationMinutes` +
  additive migration, frozen off `Offering.tripLengthMinutes` in `writeSlotBooking`.
  `shiftEndFromEvents` → `max(start + duration)`. `self_claim` mapped into the audit trail.
- **DEC-144**, correcting DEC-078 and annotating SPEC §1.4 via the #592 `amends_spec` machinery.
- 1668 unit tests + 108 against real Postgres (which is what actually verified the migration).

**The scope shrank because the operator caught me conflating two things.** I first specced this as
needing an `Event → Offering` link, having grepped `offeringId` and found it only under
`src/reservations/`. He pushed back: *"we built a reservation system to replace Xola"* and *"we know
trip length from the Offering."* Both true — and reading rather than grepping found `writeSlotBooking`
already receives `offeringId` and **drops it on the floor**. Then his own question killed the link
idea outright: *what if a guest pays for extra time after booking?* Then duration is a fact about the
**event**, not the offering. `derive.ts:418` had already named the field years earlier — *"swap this
constant for `Event.durationMinutes` when that field and its source arrive."* Freezing it, like
`price` (DEC-125) and `extrasCents` (#474), dropped the three-consumer refactor I'd quoted and took
the estimate 8 → 5.

**He also split reliability-grade from money-grade himself:** *"maybe we phone in completed for
reliability, then add MONEY-COMPLETED when we need that."* That's in DEC-144 explicitly, so nobody
hangs tip payouts off this signal later without revisiting the evidence. Correction worth keeping: I
cited the parked FUTURE_IDEAS row saying tip distribution is blocked on tax/payroll — he says it's
100% blocked on engineering, and needs money-grade completion. The row is stale on that point.

**Code review found three, all real, and the first contradicted a comment I shipped in the same
commit.** `formShifts`'s reconcile path recomputed state from the seat fold — and neither
`deriveShiftState` nor `resolveShiftState` can yield `Completed`, so the next Xola pull after any
completion folded the shift back to `Crewed` off its still-`Confirmed` seats, and the next tick
re-completed it and **double-logged `shift_completed` per occupant**. The pre-existing guard covered
only the all-events-cancelled branch. `pullXola` calls `formShifts` on every manual pull, so this was
the ordinary next import, not a race — and I had written "a Xola re-import can't fold it back."
Second: **"gaming is already closed" was false** — `bailLatenessMs` clamps to 0 past the staffing
lead, so claim-then-release 7+ days out nets **+1**, not ≤0, across most of a 45-day claimable
window. Corrected in DEC-144 and the scorer rather than left standing, with the real mitigation
stated: the count-based window makes the pair cost two of forty slots for +1 where answering "out"
earns +1 in one, so the exploit is dominated by behavior we already want.

**Three negative controls run, each reddening exactly its own tests and nothing else** — revert
`max(start+duration)` → 3; log `self_claim` before the CAS → 1; revert the `formShifts` guard → 1.
That last one is the habit paying for itself: it's the same class as last session's three
tests-locking-in-the-bug, except the reviewer caught the premise before a test could freeze it.

**Worth knowing: this turns on ~10 dormant `Completed` branches for the first time.** Every surface
guards the state defensively, but those guards have been dead code. A completed shift now leaves the
shifts board, My Shifts, and the shift thread **the same evening** rather than at midnight rollover
(`all-shifts.ts:132`, `crew-view.ts:174,205`, `thread-list.ts:150`). Inert in prod today — `MESSAGING`
is off by default — but live in e2e. If same-evening disappearance is wrong, that's a follow-up on
those guards, not on the sweep.

**Then the operator overruled the visibility half, and he was right.** I had reported the ~10 dormant
`Completed` guards as "pre-existing intent" and moved on — treating a decision made when the state was
unreachable as if it had been decided on purpose. It hadn't: it was dead code that all turned live at
once, and most of it dropped the shift as *"historical"*. A shift completes when **its own trips**
finish, which for a same-day trip is **that evening** — so hiding it makes a trip he watched all day
vanish from the board mid-day-view, deletes tonight's shift off the crew member's own list, and, on a
**subscribed** calendar feed, retroactively removes work they did from every client. The right line
isn't live-vs-historical, it's **display vs action**: show the finished shift everywhere, refuse every
action against it. That's now a first-class clause in DEC-144 so the next person adding a surface gets
the rule instead of re-deriving it from ten guards.

**I also earned a fair hit on rigor.** I told him the visibility problem was inert because `MESSAGING`
is off — but that flag covers exactly ONE of the three surfaces I'd lumped together. The shifts board
and My Shifts have nothing to do with messaging. Hanging one caveat on three sites implied the whole
thing was flag-gated when it was live.

**Un-hiding then exposed two more, one of them a real money-adjacent hazard:**
1. The board would have shown a finished trip as **`Crewed`** — `resolveShiftStateOnRead` folds seats
   and structurally cannot produce a terminal state, so un-hiding alone relabels it, live affordances
   included. #416 had already fixed this exact hazard for `Cancelled`; extended that branch.
2. **`bail()` guards only the seat, never the shift.** Releasing a seat on a finished shift logs the
   FULL penalty (notice goes negative once departed, so `bailLatenessMs` saturates at the whole lead
   — −11.4 at the default horizon). That bug pre-dates #570; completion makes it incoherent as well
   as unfair, landing on top of the +5 the sweep just granted and leaving a `Completed` shift with an
   `Open` seat under it. Refused now with a `shift_over` code.

**"Did you build DB:seed to allow me to test any of this" — no, and the PR's own test plan asked him
to do something no tool could do.** Nothing existing could reach the feature: every other seed is
horizon-sensitive and therefore in the FUTURE, while completion only fires on shifts already over.
`db:seed:completion` now stages six scenarios anchored to seed time, with A–D deliberately
PRE-completion so `db:tick` is what completes them. The pair that earns its keep is **A vs D** —
identical departures, 8h frozen duration on D, and only the one that actually finished gets scored;
under the old flat 100-minute assumption both would have completed and D's crew would have been
credited mid-charter. Verified on a clean `db:reset:dev`: `shiftsCompleted: 1`, B/C/D untouched, two
`completed` audit rows (both occupants), both completed shifts on `/admin/shifts` reading Completed
rather than Crewed — the verbatim-state fix confirmed on live data, not just in a unit test.

**Building the seed caught two of my own misses, and the second is the instructive one.** `tick-dev.ts`
never printed `shiftsCompleted` — I added the field to `TickResult` and forgot the dev reporter, so the
seed's own instruction to check it was impossible to follow. And I'd written that the seed was
idempotent: half true, with the reassuring half wrong. A second tick genuinely is a no-op (verified),
but **re-seeding** resets A to `Crewed` by design, so the next tick appends another +5 — and
`reliability_events` is append-only (DEC-008), so only `db:reset:dev` clears it. A dev fixture that
quietly inflates scores across re-runs is exactly the kind of thing that must be stated, not glossed.

**Blast-radius pushback, and a process miss worth keeping.** He flagged the PR as far bigger than
"shifts get Completed + reliability shows +5". Honest accounting: ~15 files core, ~9 the duration work
(his redirection), ~8 visibility (his instruction), 2 the bail guard (mine, unprompted, a pre-existing
bug that wasn't #570's to fix). Two of three growth areas came from his own redirections — but that's
not the defense it sounds like, because **I re-estimated once (8→5) and then never re-raised it as the
diff tripled to 33 files.** CLAUDE.md says stop and re-estimate; I stopped once and then stopped
stopping. Offered four split options with the real cost of splitting named (3 branches, rebases, full
suite + Postgres per branch); he chose to keep it as one PR.

**Code review:** 3 findings, all real, all fixed — one double-scoring bug on the Xola re-import path,
one false claim in a DEC I'd just written, one duplicated comment.
**PR:** [#606](https://github.com/mobiustripper42/muster/pull/606) — closes #570
**Points:** 5
**Branch:** task/570-shift-completion
**Opened at:** 2026-07-30T04:12:00Z

## Task 2: A filled seat retires its outstanding asks (#600)

**Completed:** (PR #607, stacked on #606 — merge that first)
- `withdrawLiveAsks` at all three fill doors: the CAS win, `claimSeat` (`Asked` seats are
  claimable since #440), `manualOverride`. `AskResponse` gains `withdrawn`; new
  `AskAnswer = Exclude<AskResponse, "withdrawn">` types every answer door so a system
  outcome is unsubmittable by construction. No migration.
- `askedSetFrom` — one definition shared by `widenAsk` and the tick's drip; a withdrawn
  ask doesn't count, so the losers stay re-askable.
- Reads as itself, not silence: cockpit pool → available (the literal row the issue was
  raised about), ask trail + At-Risk trail → `withdrawn` as its own outcome and count,
  outbox "why" skips it.
- `db/ops/600-close-losing-asks.sql` + `db:seed:losing-asks`. DEC-145.

**The operator was right that the issue's stated root was wrong, and the correction changed
the fix.** The issue said "the absence of a way to say nothing-happened is the root."
Decisive test: add the vocabulary but keep closing only the winner's ask → bug UNCHANGED;
close the losers with any terminal marker → bug GONE. So the root is that **an ask's
lifecycle was keyed to its own row, never to the seat's state** — liveness outlived
relevance, and `expireAsks` did arithmetic on `sentAt` for a question that stopped being
askable nine days earlier. The event type is third-order. Worth keeping: "what's missing"
is a seductive root-cause shape, and it's usually a symptom of the thing that actually
broke.

**A regression I introduced, caught by three EXISTING tests that were right.** Withdrawing
immediately made a contested yes hit `already_answered`, silently deleting the DEC-120
responsiveness credit (+2) for someone who did answer. The withdrawal must stop the SWEEP
inventing a penalty, not refuse the crew member's own answer.

**Then code review found the self-claim door was never fixed at all — and this is the
session's most useful lesson.** `claimSeat` had no retirement call, so the prod bug was
still reachable through it, while both the commit message AND DEC-145 asserted all three
doors were covered. It shipped green because **the test named "a self-claim onto an Asked
seat…" called `manualOverride`** — so the one door claimed covered was the one door with no
test. The missing call took a minute; the lesson is that **a test named after behaviour it
doesn't exercise is worse than no test**, because it converts an unverified claim into an
apparently-verified one. This is the same family as last session's three tests-locking-in-
the-bug, one level up: not a wrong assertion, a wrong *subject*. DEC-145 records it.

**Second review finding, also mine and also new from this branch:** the warming board
false-positived. `escalationTrailFor` sums across all required seats, and a withdrawn ask
counts toward `asked` but not `pending` — so a filled seat's withdrawn losers plus a
never-asked Open seat read as "quiet" (everyone answered, still short). I had explicitly
concluded warming needed no change, having checked `settled`/`responseRate` (genuinely
fine) and never looked at the `quiet` flag on the next line.

**Ops SQL verified by RUNNING it against dev, which is what found step 4b:** deleting a
false event fixes the score but leaves the mis-swept ask reading `timed_out`, so the trail
still shows those people as ghosts. Also: steps 1–2 are safe pre-deploy and disarm the
landmines immediately, but the label stays wrong until the code ships; and step 3/4 match by
criteria, not incident, so the list needs reading first.

**Part 5 (clear exclusions at at-risk entry) deliberately NOT built, flagged BEFORE
starting** — `escalate.ts` already owns "pool walked, go re-engage" and deliberately does
not exclude silent recipients, so a second path needs a composition decision. Applying the
#570 blast-radius lesson at the right moment rather than after the fact.

**Code review:** 2 real findings, both fixed — an unfixed fill door hidden by a mislabelled
test, and a warming false-positive. 4 negative controls, each reddening only its own tests.
**PR:** [#607](https://github.com/mobiustripper42/muster/pull/607) — closes #600
**Points:** 5
**Branch:** task/600-close-losing-asks (stacked on task/570-shift-completion)
**Opened at:** 2026-07-30T06:20:00Z

## Task 3: Every outbound message names its audience first (#599)

**Completed:**
- `src/adapters/message-opener.ts` — one `Audience` → `OPENER` map + `outbound(audience, body)`
  that front-loads the marker. crew = `Muster:`, admin = `Muster ADMIN:`. All four outbound
  classes route through it (ask, assignment notice DEC-084, doorbell ring DEC-073, At-Risk
  alert DEC-095). The alert's leading `⚠` dropped.
- Only TWO bodies actually change — the alert and the ring. Crew asks and notices come out
  byte-identical, which is why their tests pass untouched.
- Three test files that asserted the ring's literal body now assert `RING_NOTIFICATION_BODY`
  plus #387's real invariant (no count, no note content) — the literal form would have
  reddened on any wording change whether or not the rule still held.

**The messages were already different; the difference was in the wrong place.** "In or out?"
vs "needs you" sat at the END of the first line, behind a date and a vessel name — and a
lock-screen preview truncates from the RIGHT. What survived truncation differed by ONE leading
character. A shared helper rather than fixing the one sender that broke: four senders each
composing their own opener is the shape that drifts, same as the `/admin` hub's two menus and
#600's three fill doors. Adding a fifth class now *requires* naming an audience, because the
signature demands one.

**FIRST TASK DONE TEST-FIRST**, per the workflow correction earlier in the session. Wrote
`message-opener.test.ts` before the module existed, watched it fail on the missing import,
built to green. Caught myself mid-flight applying the other new rule: my first draft of the
sender-contract tests asserted `toBeDefined()` on two composers — the hollow-test trap from
#600 — and rewrote them to call `composeBoardAlert` for real. The delete-and-restore churn is
gone; the negative control now falls out of the workflow instead of being staged afterwards.

**A DEC was written and then deleted, and the operator's challenge was right.** He asked "does
this really require a DEC. what is in there?" Checking turned up worse than redundancy: the
`amends_spec` on SPEC §2.5 was **invented**. §2.5 specifies delivery mechanics — "SMS to every
active admin, one composed body, one `admin_alert` kind, no per-reason routing" — and says
nothing about wording, so nothing in it becomes untrue here. The generated banner claimed an
amendment that didn't exist, fabricating a claim inside the machinery (DEC-143) built to make
claims checkable. Precedent agreed: **#387** (a ring carries no count and no content) is the
closest comparable rule and lives in a code comment. **The tell, worth keeping: if I can't
point at a sentence in SPEC that stops being true, there's no amendment and probably no
decision.** Second time this session I reached for a DEC that wasn't needed.

**Code review then caught a fabricated technical fact of mine.** I wrote that dropping the `⚠`
avoids forcing UCS-2. False: `composeBoardAlert` emits `•`, `·` and `—` on every line, none
GSM-7, so that body was already UCS-2 and the removal saved zero segments. I'd reasoned by
analogy from `forward-notices`, which genuinely does avoid `·` for segment reasons — but that's
a different message with a different body. Applying its constraint here without checking
produced a confident, plausible, wrong claim, sitting exactly where the next person costing out
SMS would have believed it. Same family as the DEC's false `amends_spec`, four hours apart:
**a rationale that sounds right and was never checked.** Also fixed: `outbound`'s idempotency
guard was audience-specific, so a body carrying the other opener double-prefixed into
`Muster ADMIN: Muster: …` — a message wearing both hats, latent but exactly the confusion the
module exists to remove.

**Code review:** 2 findings, both fixed — a fabricated UCS-2 justification in a comment, and a
latent cross-audience double-prefix. Blast-radius check clean (no migration/money/auth, 9 files).
**PR:** [#610](https://github.com/mobiustripper42/muster/pull/610) — closes #599
**Points:** 2
**Branch:** task/599-message-openers
**Opened at:** 2026-07-30T15:40:00Z

## Task 4: The cockpit can nudge an Asked seat (#601)

**Completed:**
- `assignPerson` accepts `Open | Asked` — that guard conflated "refuse a SETTLED seat"
  (essential, kept) with "refuse an already-Asked one" (over-broad). `widenAsk` twelve lines
  below has always accepted both, letting asks accumulate with first-yes-wins (DEC-007/061).
- `lean()` treats `Asked` as a gap; `cockpit-bits.tsx` gets per-action gates so the button
  renders. Double-ask guard NOT relaxed — it keys on `respondedAt === undefined`, so it never
  blocked the silent candidates and still refuses the one person mid-decision.
- `components/**` added to vitest's include globs + `cockpit-bits.test.ts`.

**No DEC** — SPEC §1.2 has Tier 2 "direct-nudges high-reliability people" and Tier 3 "Spink
leans"; nothing restricts either to gap seats. Second application today of the tell learned on
#599: **if no sentence in SPEC stops being true, there's no amendment and probably no decision.**

**Scope cut on the operator's word, and my misread is the lesson.** The issue proposed Call/Text
on candidate rows. He doesn't want it: *"I can text a captain, I have text messaging. I know who
they are, they work for me."* I had read *"I can't even message them"* as a feature request when
it was frustration — turning an expression of annoyance into scope. 5 points → 3.

**He also challenged the design mid-plan and it sharpened the framing:** *"why does assignPerson
need to be changed just to get the nudge button to show up?"* Right question. The button is one
line; `assignPerson` is what actually FIRES the nudge and hard-refused any non-Open seat. Answer
that survived: it's narrowing an over-broad guard, not loosening a safety check.

**Code review found a button that renders and then errors — the exact rule the file states, and
my own comment was wrong about it.** `canAct` gates BOTH cockpit actions, which accept different
seat states: assign → `assignFromPool` (Open|Bailed), nudge → `lean` (Open|Bailed|Asked). I
widened the shared boolean and switched on both, so a not-yet-asked candidate on an Asked seat
got "Ask to fill" → `no_gap` → "That seat isn't open to assign into." My comment claimed it
"mirrors lean()'s gap set" — it mirrored ONE of the two actions it gates. **The structural cause:
`toSeatVM` had no test because `components/**` was never in vitest's include globs**, so the
mapper deciding which buttons render — an invariant spanning a component and two server actions —
was unreachable by the suite. That glob gap is the finding worth remembering, not the boolean.

**Second finding pinned rather than changed:** a manual nudge lands in the seat's asks, so
`widenDue`'s `max(sentAt)` delays the next AUTOMATIC widen by up to one interval. Reads backwards
until you check the branch: pre-deadline someone was just asked and piling on is what the drip
prevents; post-deadline `urgent` short-circuits and blasts without consulting `widenDue` at all —
which is #601's own scenario. Documented + pinned by test.

**Two defects in my own tests caught en route:** reusing one instant for both asks collided on
`fireAsk`'s deterministic `{seat}-{crew}-{sentAt}` id and silently overwrote; and typecheck caught
me comparing an `AskId` to a `CrewMemberId` — an assertion that could never have failed. Third
hollow assertion of the session; the pattern is the finding, not the individual slips.

**Code review:** 2 findings — a render/refuse mismatch (fixed, plus the test glob that hid it) and
a drip-pacing interaction (pinned as deliberate). 3 negative controls, each reddening only its own.
**PR:** [#611](https://github.com/mobiustripper42/muster/pull/611) — closes #601
**Points:** 3
**Branch:** task/601-nudge-asked-seat
**Opened at:** 2026-07-30T17:05:00Z

**Next Steps:**
- **v1.0.20 promoted to production** — 257 commits / 77 PRs, the first ship since `feature/reservations`
  merged and 8 days of trunk. Prod schema brought current first: **42 migrations applied by hand**,
  ledger verified via Neon MCP. `RESERVATIONS` still OFF. **Verify in order:** `<VersionTag/>` reads
  v1.0.20 (a `v0.0.0` means the `NEXT_PUBLIC_` build-inlining trap, not a bad deploy) → crew sign-in
  (the #522 auth sweep is in this batch) → `/admin` (nav rebuilt) → the nudge button. Rollback is a
  Vercel redeploy of the prior build; the DB is safe either way (all 17 new migrations are new tables
  + nullable columns v1.0.19 never reads).
- **Reservations go-live is planned and filed.** 27 issues labelled `reservation`, 18 high. Ordered:
  **A** correctness before anyone pays (#613 FK crash, #575 hold leak, #544 webhook check, #617 deposit
  default, #574, #620) → **B** operator can operate (#616 — deliberately absorbs the action halves of
  #459/#464/#465, which is why none of it had shipped) → **C** cutover, in order (#615 availability
  unification FIRST, then #614 formShifts trigger, #467 import, #468 rollback export) → **D** legal
  (#466 waiver, #619 refund policy). ~47 pts.
- **#544 cannot be closed from the repo** — someone has to confirm in the Stripe dashboard that the
  endpoint subscribes to `payment_intent.succeeded`. It's the only path that books a new reservation,
  so if it's missing, money moves and nothing is written. Hardest single blocker.
- **#623 carries the go-live steps nobody had written down**: the prod catalog is EMPTY (offerings /
  locations / owned vessel-days), so `/book` would render nothing; wallets are on via
  `automatic_payment_methods` against DEC-139's card-only; no test-vs-live Stripe key guard.
- Unfiled, worth filing: the **`muster_test` migration race** (both pg suites migrate the same DB in
  parallel — any PR that reshuffles vitest scheduling can redden CI at random) and the **branch-
  up-to-date / merge-queue** decision from #629.
- `/push-seeds` backlog — includes #612 (Micro Workflow test-first), which the next `/pull-seeds`
  will otherwise overwrite. Eric is doing seeds in a separate session.
- #608 (at-risk clears exclusions) is fully specced and unstarted — decided: clear the asked-set,
  decline cooldown and already-nudged bar; PTO, currently-working (DEC-129) and bailed-off-this-shift
  (DEC-019) all still excluded.
- Twilio segment check posted on #610 — the `Muster ADMIN:` prefix costs 4 chars against a 70-char
  UCS-2 single-segment ceiling; measured numbers are in the PR comment for Eric to check against the
  console.

**Context:**
- **The session's recurring failure was a confident rationale nobody had checked.** Three instances,
  all mine: the DEC-147 `amends_spec` claimed SPEC §2.5 was amended when §2.5 never specified wording;
  "dropping the ⚠ avoids UCS-2" when the body was already UCS-2 from its own bullets; "gaming is
  already closed" when claim-then-release nets **+1**. Each sounded right, each was reasoned by
  analogy, none was verified. **The tell that generalises: if you can't point at a sentence that
  stops being true, there's no amendment — and probably no decision.** Two DECs were written and one
  deleted on that basis.
- **A test named after behaviour it doesn't exercise is worse than no test.** #600's "self-claim onto
  an Asked seat" called `manualOverride`, so the one fill door claimed covered was the only one with
  no test — and the claim propagated into the commit message and DEC-146 as fact. Related: `toSeatVM`
  had no test at all because `components/**` was missing from vitest's include globs (now added).
- **Test-first adopted** (#612 fixes the workflow, which numbered `Build it` *before* `Write the test`
  with "test-first" buried as a clause inside the later step). The delete-and-restore negative-control
  dance was doing it the long way round. Test-first does NOT catch the wrong-*subject* failure above.
- **Sequential DEC numbering has now collided twice** (#562, and DEC-144 this session across three
  branches). The number is minted on a branch that can't see its siblings — structural, not
  carelessness. Resolved by renumbering to 145/146; worth fixing properly.
- **Generated-and-committed files merge wrongly and silently.** `DECISIONS.md`'s footer count was the
  only per-branch-varying line; git merged the DEC rows cleanly and took one footer, leaving main red
  for ~8h. Fixed at source (#629): the generator now THROWS on incompleteness and prints no number.
  The class remains for any generated file with varying content.
- **Prod verification via Neon MCP is the reliable check** — `select filename from _migrations` against
  project `delicate-art-65084110` (default branch = prod). Used it to prove the failed migration run
  never touched prod, and to confirm all 42 landed. The four-column sanity query
  (`current_database, current_schema, ledger count, to_regclass('public.events')`) distinguishes
  "right database" from "plausible-looking database" and is worth running before any prod migration.
- **A stranger opened a PR (#624)** — public-repo bounty-farming bot, opened ~90 min after #613 was
  filed, `+51/−546` on `booking-webhook.ts` (i.e. replacing the 560-line money path with a stub,
  deleting the DEC-134 guard, alerting, gratuity and balance handling), with a "regression test" at
  `tests/integration/` that vitest never runs. Not a supply-chain attack (no workflow files). Do not
  approve CI on fork PRs. Interaction limits are the cheap mitigation; going private costs Actions
  minutes (public repos get them free).
- **Two sessions were open at close** — 74 (this one) and 75, opened 2026-07-31T12:27Z by another
  window while 74 was still live. Only 74 was closed here.
