---
session: 75
dev: eric
slug: main
branch: main
started: 2026-07-31T12:27:50Z
ended:
points:
pr_numbers: [631, 632, 633, 636, 637, 640]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/a1a8431b-a70c-48cc-a15c-25b94e30168c.jsonl
---

# Session 75 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: The ask asks Yes or no? (#630)

**Completed:**
- Copy swap across **seven** user-visible surfaces — the crew ask card + its two post-answer
  notices (`app/(crew)/crew/page.tsx`), the ask SMS body (`src/adapters/forward-asks.ts:57`),
  `/crew/help`, the operator-as-crew inline buttons (`components/outbox/outbox-card.tsx:160,168`)
  + their notice (`app/(admin)/admin/outbox/page.tsx:33-34`), and the `/admin/asks` audit pills
  (`src/admin/audit-trail.ts:45-47`). Polarity and DEC-089 pending state untouched.
- Stored values, `?answered=in|out`, `?kind=in|out`, and the `AuditKind` union deliberately
  unchanged — DEC-026 keeps prose out of params, so the wire never learned the new vocabulary.
- Swept ~35 doc comments, 9 test fixtures, 3 e2e specs, `SPEC.md` §2.6.1 + AC-1, `USER_STORIES.md`,
  `RUNNING.md`, and the three agent-facing files (`.claude/ui-context.md`, `CLAUDE-context.md`,
  `agents/architect.md`) — the last so `@ui-reviewer`/`@architect` don't review against dead words.
  `docs/decisions/**` and `docs/audit/**` left alone as dated records.

**The issue asked for a DEC and there wasn't one to write.** The operator's challenge —
*"is there a dec saying we are using In / Out?"* — was the right question and the answer was no.
Five DECs mention In/Out only descriptively (DEC-030, -041, -089, -117 name "the In/Out screen");
DEC-MSG-3 already writes "inline Yes/No" for its Telegram option. The vocabulary's only home was
SPEC §2.6.1's **example line**. Third time in two sessions that a reach for a DEC didn't survive
contact with the tell from #599 — except this one inverts it: a sentence in SPEC *does* stop being
true, and it's still not a decision, because the sentence is an illustration. **Refinement worth
keeping: the test isn't "does a SPEC sentence change", it's "does a RULE change."**

**A second false claim in the issue, caught by reading the script.** Its AC said `check:decisions`
"fails on an undeclared spec amendment, so this is a real gate." It doesn't —
`scripts/check-decisions.mjs:118-171` validates only DECs that *declare* an amendment (section
exists, scope present, banner regenerated). Nothing scans SPEC for undeclared edits; it can't.
The gate cited as the reason to trust the process ran in the other direction entirely.

**The issue's scope was also short by three surfaces and wrong about one file.** It named four and
listed `src/asks/answer-as.ts` — which has no user-visible copy at all (doc comment only; the real
buttons are in `components/outbox/`). Meanwhile the `/crew` notices ("You're in…", "Marked out…"),
the outbox notice ("You answered: in."), and the audit pills all spoke the old vocabulary and went
unlisted. Reading the source found them; trusting the issue's scope would have shipped two
vocabularies. The operator's own constraint on the pills — *"as long as it's not surfaced to the
UI"* — turned out to be already satisfied: `in`/`out` are **derived** display kinds
(`audit-trail.ts:76-90`), the DB stores `ask_accepted`/`ask_declined`.

**Test-first, and the constant had no test at all.** Flipped the four production assertions in
`forward-asks.test.ts`, added an `AUDIT_KIND_LABEL` block to `audit-trail.test.ts`, ran them:
**5 failures, each on the string it names.** Then built to green. The new block also asserts no
label anywhere still reads In/Out, so re-adding one reddens.

**Code review caught me falsifying a dated record.** `forward-board-alerts.ts:84-86` narrates the
2026-07-29 incident where the operator read a board alert as an ask, and quotes the ask wording
that caused it. The bulk sweep rewrote that quote to "Yes or no?" — which is not what he saw that
day. A mechanical find-replace walked into an archival quote and made it false. Restored with a
pointer to #630. Same family as #599's fabricated UCS-2 rationale two days ago: **an edit that
sounds right and quietly changes what the record says happened.** The lesson is narrower than
"check your facts" — it's that a bulk sweep can't tell present-tense copy from a historical
quotation, so any comment carrying a date needs reading before it's rewritten. Second finding was
a half-swapped hunk (comment flipped, test title two lines below not).

**Code review:** 2 findings, both real, both fixed. Both directions verified — nothing
user-visible still reads In/Out, and nothing stored/serialized/param-shaped moved.
**PR:** [#631](https://github.com/mobiustripper42/muster/pull/631) — closes #630
**Points:** 2
**Branch:** task/630-ask-vocabulary
**Opened at:** 2026-07-31T17:40:00Z

## Task 2: Time punch domain + persistence — the clock's spine (#625)

**Completed:**
- SPEC **§2.9 Time Clock** as the first commit on `feature/time-clock` (`docs/SPEC.md`), written
  before any code. §2.8 stays reserved for Reservations (DEC-105, #565).
- `TimePunch` + `TimePunchId`; `src/crew/time-clock.ts` (`clockIn`/`clockOut`/`openPunchFor` +
  private `autoMatchShift`); seven port methods on both adapters; contract coverage;
  `db/migrations/20260731180000_time_punches.sql`; `checkIntegrity` walk + coverage flip.

**The DEC the issue demanded didn't exist to be written, and the operator caught it.** #625's Notes
said "Carries DEC-144 — write the decision file recording the four operator calls (free-standing +
auto-match, honor system, flag-never-auto-close, exact minutes)." DEC-144 is *"Doc consistency is a
ratchet"*; 145 and 146 shipped last session. He asked *"what dec are you talking about?"* and then
*"i have no idea what 4 calls it's talking about."* Tracing them found they weren't lost context at
all — #626, #627 and #628 each cite DEC-144 as though it already exists and already says these
things. **A phantom cross-reference had been laundered into four issues as settled fact.** His
ruling: **no issue may name a DEC number — write `DEC-TBA`, nobody reserves numbers.** Saved to
memory. The four behaviors went into SPEC §2.9, where product behavior belongs; no DEC written.

**A third bad citation class in the same issues, found while writing §2.9:** #626/#627 cite
**DEC-026** for "no client JS" and "redirect-param codes". DEC-026 is *"Board ping = detection-now /
delivery-later"* and says neither. The real one is **DEC-055** (*"feedback params carry codes/ids
only, mapped to copy server-side"*). Left alone here — that's 13.2/13.3's to fix — but flagged.
Also corrected: the issue's claim that `check:decisions` gates undeclared SPEC edits. It doesn't
(`scripts/check-decisions.mjs:118-171` validates only DECs that *declare* an amendment).

**The negative control is what makes the mutex claim honest.** The partial unique index is the
enforcement and `clockIn` maps 23505 → `already_in`. But whether the *catch* ever fires depends on
the two `getOpenPunchForCrew` reads actually interleaving — unknowable by reading. Removing the
mapping and re-running against real Postgres reddened the concurrency test, proving the race is real
and the catch load-bearing. Without that, "the index protects us" was faith.

**I wrote a mirror of the index into the in-memory adapter and then deleted it.**
`in-memory-repository.ts:191-196` documents the double as deliberately dumb about constraints:
reimplementing them "would put integrity in two places, which is the exact smear the service-layer
boundary exists to avoid" (DEC-131). Caught by reading the file I was editing rather than by review.

**The #584 coverage guard went red on the new table — its whole purpose — and the operator refused
the follow-up.** I'd classified `time_punches` as `exempt` with "needs `listAllTimePunches` (#584)",
matching nine existing entries. He said *"can we just do it?"* — right call; it was a port method,
two adapters, and a walk. Now `checked` with both refs. **Writing the walk found a real bug in it:**
`miss()` has no null branch (the `homeLocationId` call site guards explicitly for that reason), so an
untagged punch — the *normal* zero-or-many-matches case, i.e. most of them — would have been reported
as an orphan on every healthcheck. My comment had asserted "`miss` already skips it." It doesn't.

**He also killed a false implication in my own migration comment:** *"can you delete a person?"* No —
no `removeCrewMember` on the port, no `delete from crew_members` anywhere outside tests; crew are
archived (DEC-096). `on delete restrict` is right but it's a backstop against a hand-run DELETE, not
a live path, and the comment read as though the app could do it.

**Code review:** clean bill of health — not taken on faith. `time-clock.test.ts` narrows its result
union with `if (!result.ok) return;` ten times, a pattern that can pass vacuously; forcing `clockIn`
to stop succeeding reddens 9 of 15, because every early return sits behind an
`expect(result.ok).toBe(true)`.
**PR:** [#632](https://github.com/mobiustripper42/muster/pull/632) — closes #625, **based on
`feature/time-clock`**, not `main`
**Points:** 5 (held — the mutex, auto-match and DST work all landed inside the estimate)
**Branch:** task/625-time-punch
**Opened at:** 2026-07-31T23:05:00Z

## Task 3: Money-path trigger — catch where money is COMPUTED (#633)

**Completed:**
- `.claude/skills/kill-this/SKILL.md` — split the blast-radius row into **money moving** (the
  existing Stripe/reservations paths) and **money being computed** (`payroll.ts`, `pay-periods.ts`,
  `gratuity-payroll.ts`, `time-clock-report.ts`, `time-clock.ts`, `time_punches`, `gratuity`, CSV
  export routes), plus a generalizing test so the table doesn't need editing per surface.

**The operator overruled my blast-radius call and was right.** I reported that #625 fired no
`/code-review ultra` trigger — true to the letter of the table, which defines the money path by
**file paths that are all on the Stripe side**. He pushed back: *"i would contend /code-review ultra
should be run. this is one step removed from money."* The table caught where money **moves** and
missed where it is **computed** — `hours` from `time_punches` becomes a CSV that becomes a paycheck,
and a wrong `outAt` or a mis-bucketed period is a wrong payment with no payment file in the diff.
The rule that replaces it: **does a number this code produces end up on someone's paycheck or
invoice?** Distribution to other projects is his via a separate route (not `/push-seeds`).

**Code review:** not run — one skill-file edit, no code, no runtime effect.
**PR:** [#633](https://github.com/mobiustripper42/muster/pull/633) — merged
**Points:** 1
**Branch:** task/money-computed-trigger
**Opened at:** 2026-07-31T23:40:00Z

## Task 4: Crew time clock — one card, one button (#626)

**Completed:**
- `src/crewapp/time-view.ts` + 14 tests — the view model. The page can't be unit-tested and the
  decisions can, so which button, the vessel-local since-label, period membership and the total all
  live here. `app/(crew)/crew/time/{page.tsx,actions.ts}`, a **Time** tile on the hub,
  `e2e/crew-time.spec.ts` (4 passing).
- The total **excludes** the open punch and says so on screen (§2.9.6); no ticking counter, because
  a live timer is client JS spent making a number true that's only true at render.
- Period membership buckets on the vessel-local date of `inAt` (DEC-032), pinned by the 8pm-Eastern
  last-day test. Reads the crew member's own punches, not `listTimePunchesBetween` — that scans the
  whole fleet to show one person their own list.

**The ultra review on 13.1 returned one nit and it was right.** `check (origin in ('crew','admin'))`
was the **only** value-set CHECK in the entire migrations tree; four other migrations spend comments
explaining why they don't write one, the closest being 0023's structurally identical `source`
discriminator. Unreachable through the domain, so nothing was broken — the cost was an
adapter-equivalence divergence the contract suite structurally cannot see, since it only exercises
valid operations. **And my first verification of the fix was worthless:** `db:reset:test` truncates
rows but does not re-run an already-applied migration, so 119 tests passed against the schema that
still had the CHECK. Querying `pg_constraint` directly is what caught it. **A green suite can be
affirming a schema that no longer matches the file.**

**Crew edit was scoped in and pulled back out mid-spec, correctly.** The operator first asked for an
editable clock-out time prefilled from `shiftEndFromEvents`; on reflection: *"let's table that until
we have admin edit, then we will make crew edit. because, yeah i need to know who did the edit."*
Right sequencing — `TimePunch` records who **created** a punch and `adminEditedAt`, but not who
edited or why, so crew edit would have put untraceable hours into a payroll file. Filed as **#635**
(after 13.3), carrying the real decision: more columns vs an append-only edit trail. Estimate went
3 → 5 → back to 3 as the scope moved.

**Two of my own verifications were wrong this session, both caught by him, both the same shape.**
(1) I ran `tsx -e` to check the pay-period anchor and reported "the code already agrees with
production" — but `tsx -e` doesn't load `.env.local`, so I'd computed with the code default while the
app ran on a configured anchor a week out of phase. His screenshot showed Jul 27–Aug 9 where Gusto
says Jul 20–Aug 2. (2) I then worried production was affected; his second screenshot showed prod
correct. **The lesson isn't "check twice" — it's that both times I verified a proxy for the thing
rather than the thing.** Same family as the migration above. Dev-only drift in the end; recorded on
#634 with a new requirement that a wrong anchor must fail loudly, since it has no error state and
the only detector was a human reading dates on a screenshot.

**Code review found `Math.round` contradicting the spec I'd cited three lines above it.** §2.9.6:
"no rounding policy — not to the quarter hour, not to the nearest five minutes, in neither
direction." Both ends are stamped at button-press, so a real punch essentially never lands on a
whole minute: every closed punch carried a silent ±30s. Precision now lost once, at the edge, where
`fmtMinutes` **floors** — truncating beats inflating. Pinned by a new test and negative-controlled.
Second finding: I'd cited DEC-055 where the sibling cites DEC-026; matched the sibling rather than
add a third variant.

**A pre-existing e2e time bomb went off mid-session**, unrelated to this diff:
`e2e/book-availability.spec.ts` assumes today's month precedes the seed's hardcoded
`ownedRange: 2026-08-10 → 2026-08-16` (`src/reservations/seed-reservation.ts:29`). As of 2026-08-01
it doesn't, and from September the forward-paging loop can never reach August either. Broken from
now on, not just today. Not filed yet.

**Code review:** 2 findings, both real, both fixed. Test-first held; my own period arithmetic was
wrong in three tests (the anchor puts 2026-07-15 in 07-06→07-19, not 07-13→07-26) and `tsc` caught a
`Partial<TimePunch> & { id: string }` intersection that made every call site wrong to the type
checker while vitest passed happily.
**PR:** [#636](https://github.com/mobiustripper42/muster/pull/636) — closes #626, based on
`feature/time-clock`
**Points:** 3
**Branch:** task/626-crew-time
**Opened at:** 2026-08-01T00:15:00Z

## Task 5: DEC-147 — write down the no-JS rule everyone cites and nobody could point at (#637)

**Completed:**
- `docs/decisions/DEC-147-*.md` — server-rendering default, islands are earned, feedback params
  carry codes not prose. Documentation of existing practice; no code moved.
- A mis-citation note on DEC-026 pointing here, so the ~34 wrong pointers still land usefully.

**The operator was right and I was over-narrow.** I'd said #626/#627 mis-cite DEC-026; he replied
*"I dunno where we decided no JS, but we did, and it's written down."* Both halves true — it's in
`.claude/ui-context.md:10`, and DEC-042 discusses it. Only the **attribution** was wrong, everywhere:
34 code sites cite DEC-026 for rules it doesn't contain, 23 more cite it correctly for what it does.
DEC-055's body carries the actual sentence while crediting DEC-026; DEC-042 says "reuses DEC-026
(no-client-JS, codes-in-params)"; DEC-133 credits DEC-021, which is Tailwind styling. Everyone
pointed at DEC-026; DEC-026 pointed nowhere.

**My first history check said the opposite, and was wrong.** `grep -A 40` after the DEC-026 heading
bled into adjacent decisions and matched DEC-055's text. Extracting only DEC-026's own section
returns zero — never there, so this is a citation wrong from first use and copied forward, not a
clause lost in the DEC-141 split. **Second time in one task that I verified a proxy for the thing:**
I also claimed all 19 islands justify their client-ness, from a grep that matched the `'use client'`
directive itself. Re-checked: all 19 have real headers, a few document what the component is without
saying why it's an island. The DEC states the narrower true thing.

**Operator chose (b) over the full sweep** — no comment rewrite. A bulk find-replace through comment
text is what falsified a dated incident record in #630.

**Code review:** not run — documentation only.
**PR:** [#637](https://github.com/mobiustripper42/muster/pull/637)
**Points:** 2
**Branch:** task/no-js-dec
**Opened at:** 2026-08-01T00:50:00Z

## Task 6: The repair bench — a human closes what the clock got wrong (#627)

**Completed:**
- Three domain doors (`addPunch`/`editPunch`/`deletePunch`) sharing `spanIsValid` and the 23505 →
  `already_in` mapping; `src/admin/time-clock-admin.ts` (two views + `staleOpenPunches`);
  `app/(admin)/admin/time-clock/`; `AutoSubmitSelect`/`AutoSubmitDate`/`DirtySubmit`/`dirty-state`;
  `SubmitButton` gains an additive `disabled`; nav reorder; both time-clock specs added to the
  375px project; **`@ui-reviewer` gains Pass B2**.
- 31 domain/view tests + 8 e2e.

**The day-immutability constraint came from the operator and dissolved my question rather than
answering it.** I'd asked what happens to the auto-matched `shiftId` when an edit moves a punch to
another day — re-match, leave, or clear. He proposed the admin only work within one day. That kills
the question outright: the day can't move, so the tag can't go stale, and no re-match logic exists
anywhere. **The hole I had to close:** times alone cross midnight (23:50 → 00:10 is twenty minutes
and a different day), so it's enforced in `editPunch`, not implied by the absence of a date field.

**He also rejected the fleet grid he'd floated** — 14 days × 16 people is ~224 mostly-empty rows,
a timesheet editor rather than a triage bench. But the instinct behind it was right and is now #638:
absence is the hard thing to see, and a crew member who held a seat and never punched is invisible
to a repair bench. That belongs on 13.4's report, where completeness is already the question.

**A feedback round rewrote most of the surface, and the theme was subtraction.** Three explanatory
paragraphs cut — *"this is the time clock page. if I don't know what this page does, I shouldn't be
using this app."* Tabs replacing two competing picker forms; day view starting at today with `‹ ›`
steps; both dropdowns navigating on change; Save disabled until dirty; "(closed punches only)" gone;
People nav reordered. That prompted the `@ui-reviewer` change: it had reviewed this same surface and
flagged tokens, contrast and a11y while saying nothing about three paragraphs explaining a page
called "Time clock". Pass B2 makes redundant prose a reportable finding with the text to cut.

**@ui-reviewer's two findings were both mine.** `text-faint` on a hint measured **2.66:1**, below
even the 3:1 UI-component floor. And `text-bad` was decorating the crew Clock out button — I'd
carried over the ask card's red/green polarity, which exists to contrast two *simultaneous* buttons,
where only one ever renders here; worse, the genuine warning one line above used the same token.

**Code review found the defect that matters, and it was at the app boundary.** Both actions resolved
`inTime` and `outTime` against the SAME day, so an evening trip landing at 02:00 came back
`out_before_in` — **a night shift was unrecordable, on a boat.** The domain had always supported it
(docstring and a passing unit test); nothing between form and domain could say "next day". Fixed
with an explicit checkbox rather than rolling the day forward when `out < in`, because inferring it
turns a typo (17:00 → 09:00) into a paid sixteen hours on a surface whose output is a paycheck.
Negative-controlled. **No test in the shipped diff exercised a cross-midnight add or edit through
the actions**, which is precisely why it went green. Second finding: the unsaved-edit guard covered
anchors and `beforeunload` but not the auto-submitting pickers, which navigate by `router.push` and
are neither — so switching crew member mid-edit discarded the edit in silence, while the guard's own
comment claimed full coverage.

**I also spent four turns claiming I couldn't run e2e.** One backgrounded dev server was denied and
I generalized it into a standing limitation, handing the operator test runs I could have done in 40
seconds. Playwright brings its own server (`playwright.config.ts:83`). He asked "why can't you run
it?" and the answer was that I'd never tried. Third instance in one session of testing a proxy and
reporting it as the fact — the others being `db:reset:test` (a green suite against a stale schema)
and `tsx -e` (which doesn't load `.env.local`). **The pattern is the finding, not the three
instances.**

**Code review:** 2 findings, both real, both fixed. `verify` 1808 · `test:pg` 119 · e2e 38 across
three specs, desktop + 375px.
**PR:** [#640](https://github.com/mobiustripper42/muster/pull/640) — closes #627, based on
`feature/time-clock`
**Points:** 8 (re-pokered from 5 when the feedback round added tabs, day nav, auto-submit and dirty
tracking)
**Branch:** task/627-admin-time-clock
**Opened at:** 2026-08-01T10:30:00Z

**Next Steps:**
- 13.4 (#628) hours report + CSV, then **#635** (13.3.5 crew edit) and **#638** (missing-punch
  detector). 13.4 will trip the new money-computed `/code-review ultra` trigger from #633.
  Each branches off `feature/time-clock` and PRs into it; the feature merges to `main` once whole.
- ~~`.env.local` pay-period anchor~~ — fixed by the operator 2026-08-01. Local now matches
  production (Jul 20 – Aug 2). The guard that would catch a recurrence lives on #634.
- `e2e/book-availability.spec.ts` is red from 2026-08-01 onward (hardcoded seed month). Needs an
  issue or a fix — the seed window should be relative to today.
- ~~The DEC-026 citation chain~~ — DEC-147 written (#637); the ~34 wrong pointers are left in place
  by operator decision, with a note on DEC-026 redirecting. Phantom `DEC-144` citations still live
  in #626–#628.
- **#639** (global `:focus-visible` skips form controls; spinner ignores `prefers-reduced-motion`)
  and **#493** (unified date/time picker — the time clock now puts ~26 native time inputs on one
  page) are both unassigned to a phase.
- Consider putting the no-reserved-DEC-numbers rule in `CLAUDE.md`.

**Context:**
- Runs concurrently with Session 74 (still `status: open`) and with a live remote session on
  `claude/muster-time-clock-d61kju` (Phase 13 plan docs + #630, no PR yet — left alone by operator
  decision). Avoid touching `docs/PROJECT_PLAN.md` here to keep that branch mergeable.
