---
session: 76
dev: eric
slug: 635-crew-edit
branch: task/635-crew-edit
started: 2026-08-02T13:49:35Z
ended:
points:
pr_numbers: [647]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/541153d4-8164-42c6-abb0-2a8ea50f8008.jsonl
---

# Session 76 — 635-crew-edit

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Islands accept a click before React is listening (#642)

**Completed:**
- `e2e/fixtures.ts` — `isHydrated` + `clickHydrated`/`selectOptionHydrated`/`setCheckedHydrated`
  over one `waitForHydrated`. `e2e/island-hydration.spec.ts` (5 tests). Call sites converted in
  `offering-catalog`, `book-availability`, `book-checkout`, `blocks`, `shifts-view`, `admin-nav`,
  `outbox-relay`. `.claude/CLAUDE-context.md` §Testing gains the convention.

**The hypothesis was confirmed rather than inherited.** #642's first AC asked for evidence, and the
cheap way to get it turned out to be **blocking the JS bundle**: the button is visible, enabled and
clickable, the click resolves, and no row appears. Then the same page with the bundle merely
*delayed* 3s and `waitUntil: "commit"` — a plain click loses that race, `clickHydrated` wins it in
4.8s. **The second pair is what makes the fix honest**; without it the helper could be a wait on
nothing and every spec would still pass on a fast machine.

**Controlled inputs fail a second way, which is the part I nearly shipped wrong.** A pre-hydration
`selectOption`/`check` sets the DOM and is then **reverted** when React asserts its own value — the
selection visibly takes and silently un-takes. Code review caught that I'd applied this reasoning to
`<select>` in the docstring and then not to `checkout-form.tsx:321`'s controlled waiver checkbox,
whose next assertion is that **Book & pay** unlocks (DEC-110, computed client-side). One control
short, on the payment path.

**Two of the issue's three "rotating cast" failures are not this bug at all.** `integrity:125`
clicks a native `<summary>` — the nav groups use `<details name="admin-nav">`, HTML's own exclusive
accordion, so the "one open at a time" behaviour needs no JS — and `two-pane-builder:159` clicks a
plain link. Reading the components is what separated them; the issue's framing invited converting
all three. The suite-timing half of #642 is untouched and said so in the PR.

**I asserted #646's failure mode as fact and it was wrong.** I'd said `book-availability:69` fails
deterministically from 2026-08-01. Measured: **2 failures in 5 runs**. Today is inside the seeded
window, so the loop races the link navigation — and the *majority* case is worse than failing: it
reads the pre-navigation page, breaks on iteration 0, and asserts against a page it never left, so
the test has been green while verifying nothing. Corrected on the issue with the instrumented run.
The tell was that I reasoned from source and filed the issue before running it once.

**Code review:** 2 findings, both real, both fixed (the waiver checkbox; a #646 doc line with no code
behind it in this diff). The reviewer independently checked `__reactProps$` against react-dom 19.2.7
source and swept all 19 islands for missed and over-converted call sites.
**PR:** [#647](https://github.com/mobiustripper42/muster/pull/647) — closes #642
**Points:** 3
**Branch:** task/642-e2e-hydration
**Opened at:** 2026-08-02T15:05:00Z

**Next Steps:**

**Context:**
