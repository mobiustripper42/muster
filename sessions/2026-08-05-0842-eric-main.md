---
session: 78
dev: eric
slug: main
branch: main
started: 2026-08-05T08:42:47Z
ended:
points:
pr_numbers: [662, 663, 664, 665]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/eedc8899-8d78-4986-8703-60996168f28a.jsonl
---

# Session 78 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: The pay-period overlap seam — accepted, not closed (#660)

**Completed:**
- `docs/SPEC.md` §2.9.6 + `src/admin/time-clock-report.ts` (`overlappingDays`, **comment only**)
  state the gap and the condition to revisit under. `src/admin/payroll-reconcile.test.ts` +3
  characterization tests. `src/admin/payroll-reconcile.ts` — the issue's rider, `", and "` → `" and "`.
- **#660 relabelled and left OPEN** (`bug`+`points:3` → `documentation`), retitled "Accepted gap:
  …", body carrying the decision, the reasoning, and the full shape of the fix.

**The operator overruled the plan and was right.** I speced option 1 — new port method, both
adapters, a contract case, and a two-set separation in `buildTimeClockReport` — for a conjunction
that needs an overnight punch landing on the one night in fourteen that is the period cut AND a
hand-entry overlap on it. Three files of risk on the hours path against a corner. 3 pts became 1.

**There was a cheap complete fix and it still wasn't worth it.** Worth recording because the issue
framed option 1 as needing an arbitrary look-back margin: attribute each overlap to the period of
the LATER-starting punch, then a report only looks backward and the predicate is exact —
`in_at < from AND out_at > from`. Complete, because if the earlier punch starts before the period
then `A.out > B.in >= from`. `out_at > $1` is NULL-false, so open punches drop out for free. It's
on #660 if the frequency ever changes.

**Test-first does not apply to a characterization test, and saying so is the point.** Nothing
changed behaviourally, so the three new tests passed on first run. What makes them worth more than
a comment is the CONTROL: the same fixture under one window that holds both punches IS detected.
Without it they would pass just as happily on punches that never overlapped, proving nothing. The
third test — the next period is silent too — is the one that earns the issue: missed in one period
is a deferral, missed in both is a hole.

**Code review:** done inline, not via `@code-review` — subagents are off this session. `join(" and ")`
is safe (only two reasons exist, so no three-item case loses a separator); checked all three message
assertions in the suite, none match on the joiner. `time-clock-report.ts` verified comment-only —
accumulator, DEC-032 bucketing and `overlapCount` byte-identical. Two minors called out in the PR:
the control's window is 15 days rather than a real cadence, and the new docstring cites a bare
filename, which `check:docs` doesn't validate.
**Blast radius:** ⚠ `src/admin/time-clock-report.ts` is on the money-computed list. Flagged despite
the change being comment-only. `/code-review ultra` not run — operator's to call.
**PR:** [#662](https://github.com/mobiustripper42/muster/pull/662) — deliberately does **not** close
#660. `npm run verify` green (1917 tests, 142 files, build clean).
**Points:** 1 (filed as 3, re-scoped at the decision)
**Branch:** task/660-period-seam-documented
**Opened at:** 2026-08-05T09:12:00Z

## Task 2: The e2e sign-in race is WebKit-only, and the prescribed fix does nothing (#659)

**Completed:**
- `e2e/admin-nav.spec.ts` — **comment only.** The guard line is byte-identical to `main`; the
  comment moves from a guessed cause to an instrumented one. `e2e/fixtures.ts` and
  `playwright.config.ts` end the task untouched.
- **#659 parked and left OPEN**, retitled, `points:2` dropped, body carrying the full trace with
  the disproven claims struck through rather than deleted.

**Both of the issue's load-bearing claims were wrong, and I only found out by measuring.** It said
CI loses this race because CI runs `next dev`. Chromium won every attempt under `E2E_PROD=0` —
cold server, uncompiled target route, reported shape and purpose-built shape. WebKit lost first
try. The variable is the ENGINE. That collapses "eleven specs exposed": all eleven are
Chromium-only, and the sole WebKit project is already scoped to the one spec that has the guard.

**The interrupter is Next's client router, found with `framenavigated` instrumentation.** `POST
/crew/auth` → 303 → `GET /crew` → URL match → a second navigation to `/crew` at ~150ms with **no
network request behind it**. Not app code: `/crew` has no `useRouter`, `router.replace` or
`router.refresh`. Everything downstream followed from stopping to instrument instead of theorising.

**Three fixes, each obviously right until measured.** `waitForLoadState("load")` — what the issue
prescribes — resolves in ~2ms because the document is already loaded. `document.readyState` reads
`"complete"` at that same instant with a guard and without one; I had written two tests on it and
**both passed identically in both directions, so I deleted them** rather than ship tests that
cannot fail. Waiting on hydration (`__reactProps$`, the repo's own idiom) fixed the admin path
across 3 repeats and still lost the crew path 2 runs in 3.

**Stopped by the operator, correctly, for the second time today.** What remained was waiting on a
navigation Next fires on its own schedule with no public signal — countable events with a bounded
fallback, or a sleep — to protect specs running on an engine that does not lose the race. Shipping
the partial fix would have been the worst of the three options: a half-working fixture plus a
flaky guard test is the exact "flaky CI, re-run it" pattern #659 was written to kill.

**The negative control is what killed the two dead tests, and it is the reusable lesson.** Reverting
the fix via a file copy (not `git checkout` — Task 3 of Session 77 cost a redo that way) and
re-running showed the readyState assertions green in both directions within one run.

**Code review:** inline, not `@code-review` — subagents off this session. One file, comment-only;
`git diff` confirms the guard line and every assertion unchanged. No blast-radius trigger — e2e
harness only. Flagged in the PR: the comment is long for a spec file, and earns it by recording
three disproven hypotheses so the next reader doesn't re-derive `waitForLoadState`.
**PR:** [#663](https://github.com/mobiustripper42/muster/pull/663) — deliberately does **not** close
#659. `npm run verify` green (1914 tests, 142 files, build clean); WebKit guard verified 16/16
across 2 repeats.
**Points:** 3 (filed as 2 — the investigation was the deliverable, not the diff)
**Branch:** task/659-webkit-race-parked
**Opened at:** 2026-08-05T10:34:00Z

## Task 3: Every ERR_COPY table keyed to its real code union (#654)

**Completed:**
- `app/lib/err-copy.ts` (new) — `errCopyFor(table, raw, fallback?)`, the one place the string
  widens. 20 files across ten surfaces: each `actions.ts` exports the union it can mint, each
  `page.tsx` keys its table to it. `src/admin/block-admin.ts` gains `BlockRemoveError` /
  `BlockRemoveResult`. Zero `Record<string, string>` tables left.

**The first task today that was actually the size it was filed at.** Filed 3-or-5 "worth re-pokering
after reading the first three tables" — read all ten first, found nine domain unions already
exported and only glue codes homeless, and called it a 3. That held.

**The compiler found the union gap I missed reading the code.** `removeBlockAdmin` returned an
inline `{ code: "not_found" }` with no exported type, so `BlockErr = BlockSaveError | "error"`
didn't compile. Named it after its save-path sibling. Worth noting the direction: the survey said
"mechanical", and the one thing that wasn't mechanical was surfaced by the type rather than by me.

**Both negative controls bite, and the second is the one that matters.** Deleting the `future`
entry — the exact line whose absence caused the bug — fails at that table. Adding `shift_gone` to
the DOMAIN's `AddPunchCode` fails at **both** time-clock surfaces plus `CrewEditResult`'s
composition point. That cross-surface failure is the property the issue was actually asking for;
a single-table control would have proved much less.

**No copy changes and no missing entries, which is the honest and slightly dull result.** The AC
anticipated finding real gaps. The only gap was in a union, not a table — blocks already carried
its `not_found` line. Reported as such rather than dressed up.

**The scripted-edit risk was real and caught.** Most of the 20 files were edited by script; the
first attempt corrupted a table's opening brace and used a helper name that shadowed the local
`errCopy` variable. Renamed the export to `errCopyFor`, repaired by hand, then re-read a sample of
generated output and ran a tidy pass for double blank lines before committing.

**Code review:** inline, not `@code-review` — subagents off this session. No behaviour change on
any success path; the only runtime difference is `/admin/import` routing through `errCopyFor`
instead of an inline `??`, the same expression.
**Blast radius:** ⚠ `app/(crew)/crew/time` and `app/(admin)/admin/time-clock` are on the
money-computed list. Type-level only — no punch write or hours computation in the diff.
**PR:** [#664](https://github.com/mobiustripper42/muster/pull/664) — **closes #654**.
`npm run verify` green (1914 tests, 142 files, build clean).
**Points:** 3 (filed as 3–5)
**Branch:** task/654-typed-err-copy
**Opened at:** 2026-08-05T12:05:00Z

## Task 4: The crew hub was the menu — one header, and the nav moves into a drawer (#644)

**Completed:**
- `components/crew/crew-header.tsx` + `crew-menu.tsx` + `app/lib/crew-links.ts` (all new). Nine
  crew routes adopt the one-row header; the hub sheds four nav cards and its footer trio.
  `e2e/crew-header.spec.ts` (8 tests). SPEC §2.6 corrected. Nine existing specs rerouted.

**The operator reframed the issue and the reframing was the task.** #644 said "back arrow →
hamburger". He said the back button was not really the problem — the top of the screen was, and
"the crew page with menu buttons really it's bad". Measuring settled it: **"My shifts" started at
y=637 in an 812px viewport, 78% of the first phone screen spent on a menu.** 411px now. The back
arrow was the smaller half of a task filed entirely as the back arrow.

**Measure before choosing the threshold — my first space assertion was worthless.** I wrote
`expect(y).toBeLessThan(812)` and it PASSED on the broken layout. Went and measured (637), then
measured again after (411), then set 450 with both numbers written into the comment. A space test
with a guessed bound is a space test that asserts nothing.

**And the second threshold was wrong in the other direction.** `< 300` failed at 411 — but the
411 was legitimate: an ask card (134px), the standing line, the offer, a credential nudge. All
work, no navigation. Probing the hub's children is what showed that; without it I would have
"fixed" the layout by deleting content.

**DEC-147 rule 2 caught a regression I had already written.** The drawer was a `useState` island
copied from `admin-nav.tsx`. But the admin nav renders links inline at `lg` — crew has no such
fallback, and since the cards had just LEFT the hub, a JS-less crew member would have been
stranded on a page with no way off it. Strictly worse than before the task. Rebuilt on `<details>`:
zero JS, and the browser removes the panel from the tab order for free, which the island needed
`inert` to fake. Tested with `javaScriptEnabled: false` rather than asserted.

**Nine specs rerouted, and two of them were lying before I touched them.** `crew-time`'s "the hub
carries a Time tile" is now false by design — retitled rather than repointed. `crew-open`'s bare
`locator("details")` counted my drawer: bumping 2→3 would have kept passing if a day row vanished,
so it is scoped to the list. `operator-messaging` had two dead lines reaching for the ADMIN nav
inside a CREW session behind an `isVisible()` guard — they never fired and only read as if they did.

**Found while cleaning up:** the hub was still paying for an unread-count thread-list query and a
second active-admin lookup, both feeding controls that no longer render there.

**Scripted edits bit again, worse this time.** A slice using `s.index("<VersionTag />")` as an end
bound matched an EARLIER occurrence than its start and duplicated ~200 lines. Caught by reading
the grep output, not by the compiler. Reverted and redid it with anchored `Edit` calls.

**Code review:** inline, not `@code-review` — subagents off this session. Screenshots taken per
route at 375px rather than generalised from one; the shift card was the one worth looking at, since
its vessel name moved into the header row and dropped from `text-2xl` to `text-lg`.
**Blast radius:** no trigger — crew UI only, no money path, no migration.
**PR:** [#665](https://github.com/mobiustripper42/muster/pull/665) — **closes #644**. `npm run
verify` green (1914 tests); the full `mobile` project (116 tests at 375px) green; every desktop
spec touching `/crew` green. Full desktop suite deliberately not run.
**Points:** 5 (filed as 3)
**Branch:** task/644-crew-header
**Opened at:** 2026-08-05T14:20:00Z

**Next Steps:**
- **Reservations is the operator's stated next goal** — "get the reservation finished soon, bugs
  first". Phase 12 carries 13 open `reservation` issues; #616 (no cancel/refund anywhere, and a
  Stripe-dashboard refund never reaches the ledger, 8 pts) and #613/#615/#617 are the money-path
  ones. Phase 12 has never been retro'd and #623 is its go-live runbook.
- **Reservations is all that is left before the parking lot** — Phase 12's 13 open issues.
- **#661** — `/its-dead` should refuse to close silently when shipped work was never logged.
  Seeds-managed, backports. Still untouched.
- **Two issues are now parked-open records** (#660, #659). Neither should be read as queued work;
  both carry a "reopen this as work if" condition.

**Context:**
- **Four tasks: two parks, one clean sweep, one reframed by the operator into a bigger and better task.** Both started as filed bugs, both were re-scoped down after the plan
  met the actual cost. The pattern worth noticing: in each case the *investigation* was the
  deliverable and the diff was nearly empty. Estimates filed as 3 and 2 both mis-shaped the work.
- **Subagents are off this session**, so `/kill-this` step 3 ran as an inline review both times.
- **`E2E_PROD=0` is mandatory for anything WebKit locally** — under the default, every page renders
  signed-out with zero cookies and no error (Secure cookie over plain http).
- **Reverting a change to observe a control: use a file copy, never `git checkout <file>`.** Used
  `cp` to `/tmp` this session; Session 77 lost an implementation to the `git checkout` version.

**Context:**
