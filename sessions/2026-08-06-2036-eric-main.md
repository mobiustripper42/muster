---
session: 79
dev: eric
slug: main
branch: main
started: 2026-08-06T20:36:07Z
ended:
points:
pr_numbers: [684]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/12e23406-cccf-4c45-8390-ea294132810e.jsonl
---

# Session 79 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: The cancellation policy exists in code and on the page (#619)

**Completed:**
- `src/reservations/refund-terms.ts` (new) — the file `payment-config.ts` had cited since
  Phase 11 and which was never written. All four published rules as constants ($50 fee, $30
  insurance, 14 days, 72 hours); `refundOwedCents({paidCents, hoursBeforeDeparture, hasFlex})`;
  `operatorCancelRefundCents` deliberately **separate** so no caller reaches the fee path by
  passing the wrong notice — who cancelled is the discriminator, not when. Copy constants
  interpolate the numeric ones, so the prose cannot drift from the math.
- Rendered three places: checkout above the pay button (`checkout-form.tsx`, plain-string prop
  via the `waiverUrl` idiom, `text-faint` → `text-muted`), manage page beside the cancel action,
  and a short clause in `booking-confirmation.ts` on both channels.
- `refund-terms.test.ts` (16 cases, written first — failed on the missing module) +
  `booking-confirmation.test.ts` +1 + e2e at checkout (asserts document **order** vs `book-pay`)
  and manage. 345 unit, 26 e2e desktop + 375px.
- `payment-config.ts` header corrected — it claimed refunds are "always manual" (false since
  #472) and called `refund-terms.ts` "inert." Dead `#472` comment in `manage/page.tsx` → #616.
- **#683 filed:** flex insurance is a published $30 term Muster cannot sell (blocked on #622),
  so it is encoded but rendered nowhere. `hasFlex` stays a boolean per DEC-113.

**The operator's first answer was not the policy.** Asked for the terms, he described a flat
"full refund minus a nominal fee, every cancellation"; four turns later he pasted the actual
website policy, which is tiered (14 days, $50, non-refundable inside, weather full refund,
$30 insurance → 72 hours). Two of the four AskUserQuestion answers were superseded by that
paste. Cheap because nothing had been written yet — the whole exchange happened at step 1.

**No DEC — declined explicitly.** The policy lives only in `refund-terms.ts`; `docs/SPEC.md:1847`
still lists the refund schedule as open and owned by Drew. Known loose end.

**Code review:** No money-path or boundary bugs — integer cents with `max(0,…)` floors, the
14-day/72-hour lines match the copy and are test-pinned, webhook never-throws holds. Two doc
findings, both fixed in `c99b282`: (1) the SMS-segment justification was reasoning from a false
premise — the `— Muster` em dash is outside GSM-7 and forces the body to UCS-2 at 67 chars a
segment, so the body is already ~5 segments and the 80-char test budget measured nothing;
(2) the header presented the `add_ons` route for insurance as settled when DEC-113 forbids it.
**Standing cost finding, not acted on:** dropping that em dash buys GSM-7 back on every
confirmation SMS.
**PR:** [#684](https://github.com/mobiustripper42/muster/pull/684)
**Points:** 2
**Branch:** task/619-cancellation-policy
**Opened at:** 2026-08-06T22:52:00Z

**Next Steps:**

**Context:**
