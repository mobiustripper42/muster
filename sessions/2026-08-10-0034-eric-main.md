---
session: 81
dev: eric
slug: main
branch: main
started: 2026-08-10T00:34:00Z
ended:
points:
pr_numbers: [727, 730]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/27115524-4069-4be9-86f7-efd753c66347.jsonl
---

# Session 81 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Cancel and refund a booking, and reconcile dashboard refunds (issue #616)

**Completed:**
- `src/reservations/cancel-reservation.ts` + `refund-payment.ts` (new). Cancel writes the
  reservation AND its event, re-forms shifts, relays crew notices. Refund takes one editable
  amount and splits it across charges newest-first.
- `charge.refunded` → `refund_recorded` through the port, both adapters, and the webhook —
  outside the RESERVATIONS gate, since money that already moved must record in any deployment.
- Two new port methods (`cancelEventIfUnclaimed`, `getPaymentByIntentId`), both adapters,
  contract cases. One index migration. **DEC-153** revises DEC-107 (refund mechanism) and
  amends SPEC §3.3 (status claim only).
- 2073 unit, 124 Postgres, 13 e2e × 2 widths, three doc checks, both typechecks, lint, build.

**Cancelling the reservation does not free the boat, and cancelling the event bricks the slot.**
The reservation flip releases only its own slot; `hull-busy` and the overlap guard count every
`scheduled` event, so a cancelled 13:30 keeps 14:00 unsellable, and `formShifts` only collapses a
shift when every event on the vessel-day is cancelled — the crew stay rostered. So the event is
cancelled too. But `events_muster_slot_identity` is status-agnostic, so a cancelled row keeps
owning its identity and `saveBookingIfSlotFree` returns `lost` **forever** — cancelling would
permanently destroy the boat-slot. The claim path now resurrects in place. The Postgres test
failed on `expected 'lost' to be 'booked'` before the fix; `InMemoryRepository` would have hidden
it (resurrect-by-Map-key-collision, right for a deterministic id, wrong for an override row).

**The operator rejected the first refund design and was right.** Per-payment-row presses were me
dodging the allocation problem and pushing it onto him mid-cancellation: *"cancel for so many
reasons and who knows what the refund amount might be."* One editable box now. He then asked why
it splits at all rather than coming off the total — the answer is that there is no total at
Stripe: `refund()` takes a `paymentIntentId` and caps at that charge. The split is the
translation, not a modelling choice.

**Both refund figures render next to their radio option.** With no client JS a single number
cannot follow the selection, and a figure silently belonging to the other choice is worse than
none on a screen whose only job is deciding an amount. This is what `refund-terms.ts` was built
for at #619 and had no caller for.

**Review found four real defects, two of them money-severity.** `parseDollarsToCents` stripped
commas unconditionally, so `"1,50"` was a **100× refund** ($150.00 for $1.50) with the success
copy, the ledger and the CAS all agreeing. The cancel released the hull with an unconditional
`saveEvent` — a read-then-write on a slot every other path locks, so a cancel racing a re-book of
the slot it just freed could cancel a customer who had paid; now a conditional write under the
same hull-day advisory lock, with the locks acquired in the same order (my first cut inverted
them, which deadlocks). A partial refund failure rendered as a green success — "Refunded $0.00 to
the card it came from" when the FIRST leg failed. And the in-memory resurrect kept the dead
booking's price where Postgres nulls it, negative-controlled at `expected 50000 to be undefined`.

**I measured the #718 defect rather than eyeballing it, and it was there.** After cancelling,
"Resend confirmation" landed **8px** into the coordinates "Cancel this booking" had just vacated —
a second tap fires a different action against a booking just cancelled. Destructive block moved
last so collapsing it moves nothing; 71px clear, frozen as an e2e. Written after the fix, but the
failure was observed first.

**I did not amend the spec on my own initiative this time.** Flagged that SPEC §3.3's "nothing in
`src/` implements a cascade or a refund" had become false and asked; he said fix it. Last
session's lesson held.

**Code review:** 2 bugs + 2 cleanups. **Security review:** 4 confirmed + 4 lower; 7 fixed, 1 filed
(issue #726 — concurrent refunds of different amounts both pay out; needs a lease, not a lock,
since the Stripe call can't be held in a transaction). All fixes in `4b408a5`.
**PR:** [PR #727](https://github.com/mobiustripper42/muster/pull/727)
**Points:** 8
**Branch:** task/616-cancel-and-refund
**Opened at:** 2026-08-10T09:45:00Z

## Task 2: db:xola:report — audit Xola passenger counts against boat capacity

**Completed:**
- `db/xola-report.ts` (new) + `db:xola:report`. Read-only CLI against the live Xola API: pulls
  reservations, resolves the boat, derives the head count from add-ons, flags over-capacity and
  declared-but-unpaid. **7 over capacity, 2 unpaid, live.**
- Two live API defects found and filed: **issue #728** (`fetchOrders` 404s on page 2) and
  **issue #729** (`/events` returns a ~3-week window). Both routed around here.
- `npm run verify` green; the file typechecked explicitly under the core's strict settings
  because `db/` is in no gate.

**The operator asked a question the data model could not answer.** `item.quantity` is 1 on every
order — it counts BOATS. `demographics` says Adults×1. The head count lives in an
`Extra Tickets: $40/person` add-on on top of a base of 12, and the importer parses no add-ons at
all. He said as much up front — *"the only way to find out the number of passengers on a boat is
with the add-ons"* — and he was exactly right.

**`--raw` first, before writing a line of extraction.** The add-on shape is documented nowhere in
this repo and CLAUDE.md forbids guessing a third-party shape. Dumping two real orders gave the
whole model in one look, including a second `Are you interested in adding more guests over 12?`
add-on recording INTENT separately from what was paid for — which turned out to be half the audit.

**Two live importer defects fell out of building a report.** `fetchOrders` 404s on `skip=100`, so
any pull holding 100+ orders dies; `/events` returns only 08-14 → 09-06, so trips outside that
band resolve no vessel. Both sit in the path behind "Pull from Xola now" — the importer is seeing
a narrower slice of Xola than anyone thought. Bigger than the report itself.

**The operator caught the defect that mattered, by knowing his own business.** He said there
should be a problem today and one on Friday. Friday was in my output; 8/10 was not — that row's
boat had not resolved, so the capacity check silently did not run and the row printed looking
clean. brynn chalmers, 14 pax on a Brew 4. **A row that skips its check is worse than one that
errors**, and it took domain knowledge, not code, to see it.

**Then code review found I had claimed to close that and hadn't.** I fixed the event lookup and
said so. Nothing pushed a flag onto the row, so an unresolved boat still rendered blank AND was
dropped by `--flagged` — the one view anyone triaging reads. The same passive failure, one layer
down, inside the commit that claimed to have removed it.

**Review also caught two silent-wrong-answer guards.** `--chunk 0` hung before any network call;
a malformed `--base` yielded `NaN`, and `NaN > cap` is false for every row, so the capacity check
turned ITSELF OFF and printed a clean report with no error.

**The fix for the flat-base assumption found a second product.** Rather than invent per-boat guest
counts, the script now reports the distinct experiences it saw — and the pull contains a
`Duffy Boat Rental | (1-12 Guests) | Self Captained`, which a base of 12 means nothing for. Now
marked out of scope instead of audited against a number that doesn't apply.

**Open question left with the operator, not guessed:** whether Brew 1 (14) and Brew 2 (16) include
more than 12 in the base. If they do, their pax is understated and the OVER list is short.

**Code review:** 6 findings (3 bugs, 1 missing-guard, 2 cleanups), all fixed in `6649206`.
Security review not run — no blast-radius trigger (one `db/` script, no money path, no auth, no
migration) — though it reads live production Xola with production credentials and writes nothing.
**PR:** [PR #730](https://github.com/mobiustripper42/muster/pull/730)
**Points:** 3
**Branch:** task/xola-passenger-report
**Opened at:** 2026-08-10T15:20:00Z

**Next Steps:**
- **PR #727 (cancel/refund) and PR #730 (xola report) are both open and unreviewed.**
- **issues #728 / #729 are the real find** — the importer pulls less than it appears to. Operator:
  *"we'll get to those in a minute."*
- Prettier adoption is parked pending a decision — see Context.
- Phase 12b remains the live phase. #616 closes with PR #727; #723/#724/#725/#726 were filed off it.

**Context:**
- **`db/` is covered by no gate** — no typecheck, no lint, no test. Two scripts shipped broken
  through the whole chain in session 80. Every `db/` file added this session was typechecked by
  hand with the core's strict settings and then actually executed.
- **Prettier is NOT a project tool.** Running it mid-task reformatted a whole file (231 lines of
  churn) and had to be reverted. If it is ever adopted, the cost is ~875 `file.ts:NNN` citations
  across code comments and `docs/` that a reformat would silently invalidate — `check-docs`
  validates paths, not line numbers.
- **The passive-failure pattern showed up three times today**, in three different shapes: a
  cancelled booking's slot bricked by a status-agnostic index; a report row skipping its check and
  printing clean; copy pointing at a position a later fix had moved. All three looked fine and
  said nothing. Worth watching for as a class.
