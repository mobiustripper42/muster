---
session: 84
dev: eric
slug: main
branch: main
started: 2026-08-14T04:11:54Z
ended:
points:
pr_numbers: [744, 748, 749, 750]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/530e121c-5c6c-4df7-aec0-361206da4878.jsonl
---

# Session 84 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Booking links become a stored short code — `/b/<code>`, revocable (issue #741)

**Completed:**
- `src/reservations/booking-code.ts` + test — 14-char Crockford base32 from `crypto.randomBytes`
  (a credential, unlike `mintDisplayCode`'s injected `Math.random`), unbiased `& 31` mapping,
  normalization, and the revoked/expired refusal.
- `src/reservations/ensure-booking-code.ts` + test — idempotent ensure (a Stripe redelivery must
  not mint a second live code) and reissue-revokes-predecessors.
- `db/migrations/20260814123030_booking_codes.sql`, `src/ports/repository.ts`, both adapters,
  5 new cases in `src/adapters/repository-contract.ts`, `src/admin/integrity-coverage.ts`.
- Route moved: `app/reservations/manage/{page,actions,load,calendar/route}` → `app/b/[code]/…`,
  with a distinct "this link was replaced" state (three outcomes where the HMAC had two).
- Operator "Replace their link" control behind a `<details>` in the reservation detail pane.
- Old scheme deleted outright: `booking-link.ts`, `RESERVATION_LINK_SECRET`, and its rows in
  `playwright.config.ts`, `docs/DEPLOY.md`, `docs/HOSTING_MIGRATION.md`.
- `docs/decisions/DEC-154-booking-link-is-a-stored-short-code.md` — amends DEC-122's mechanism
  only (`reverses`, scoped); the capability-URL model stands.
- Seed prints `/b/<CODE>` URLs incl. one revoked, so the `phase:12b` hand-test starts from a URL.

**Re-estimated 5 → 8** at spec time — the headline is one function; the capability surface it
touches is 8 secret-readers and a 4-file route. Run as one unit, not split.

**Code review:** @code-review — 1 finding (comments still claiming the link can't be revoked, in
the commit that adds revocation), fixed. /security-review — 0 vulnerabilities, but found a real
defect: a revoke failing after the mint reported as a clean reissue while an old link stayed
live. Now returns `staleCodes` with its own operator copy. `/code-review ultra` not run.

**PR:** [PR #744](https://github.com/mobiustripper42/muster/pull/744)
**Points:** 8
**Branch:** task/741-booking-short-code
**Opened at:** 2026-08-14T15:47:00Z

## Task 2: Two simultaneous refunds can no longer both pay out (issue #726)

**Completed:**
- `db/migrations/20260814170248_refund_leases.sql` — the refund mutex (reservation_id PK, token,
  expires_at), FK inline, following the `checkout_holds` lazy-expiry pattern.
- `acquireRefundLease` / `releaseRefundLease` on `src/ports/repository.ts`, both adapters,
  8 contract cases run against each.
- `src/reservations/refund-payment.ts` — acquire → read → plan → execute → release, with
  `executeRefundPlan` extracted so the `finally` wraps only the money-moving half.
- `src/admin/integrity-coverage.ts` — new table classified.
- `.claude/CLAUDE-context.md` — added `refund-payment.ts` to the money-moving triggers.

**Two things the reviews changed, both worth remembering:**
- Release originally deleted by `reservation_id` alone, so a holder whose lease expired
  mid-Stripe-call would delete its **successor's** live lease. Leases carry a per-acquire token now.
- The plan was originally built from a read taken *before* the lease, so the mutex guarded the
  writes but not the read-modify-write. The lease is taken first now.

**Code review:** @code-review — 1 note (expiry boundary untested), fixed. /security-review — 0
vulnerabilities, 2 residual gaps in the fix itself, both fixed. `/code-review ultra` not run.

**PR:** [PR #748](https://github.com/mobiustripper42/muster/pull/748)
**Points:** 3
**Branch:** task/726-refund-lease
**Opened at:** 2026-08-14T19:52:00Z

## Task 3: Lost your link? — public recovery (issue #460)

**Completed:**
- `src/reservations/find-booking.ts` + test — pure matching on contact + any name token, soonest
  upcoming preferred, Xola and cancelled-only handled.
- `src/reservations/recover-booking-link.ts` + test — throttle → match → send, returns `void` so
  a hit and a miss are indistinguishable.
- `app/b/find/{page,actions}.tsx|ts` — the public form; `after()` so the response is identical in
  latency as well as text.
- `db/migrations/20260815125833_recovery_throttle.sql` + port + both adapters + contract cases.
- Entry points: both `/b/[code]` dead-end states and `/book`.
- `src/adapters/postgres-repository.test.ts` — `recovery_throttle` added to the truncate list.

**Re-estimated 3 → 5** — the throttle (migration + port + adapters + contract) is the delta.

**Also on this branch's lineage:** the #741 gate revision (commit 3e90238 on the parent branch) —
the manage link now shows on production for one render after a reissue.

**Code review:** @code-review — 2 findings, both fixed: a **timing oracle** (the action awaited
the send, so a hit returned slower than a miss — the repo had already solved this on the login
path with `after()` and I didn't apply it), and the full-table scan running *before* the throttle
claim, making the migration's stated bound false. /security-review — 0 vulnerabilities.

**PR:** [PR #749](https://github.com/mobiustripper42/muster/pull/749)
**Points:** 5
**Branch:** task/460-link-recovery (stacked on task/741-booking-short-code)
**Opened at:** 2026-08-15T15:05:00Z

## Task 4: One checkout session, one hold per departure (issue #575)

**Completed:**
- `src/reservations/holder-token.ts` — 32 CSPRNG bytes per checkout session, shape-validated.
- `db/migrations/20260816042221_checkout_hold_holder_token.sql` — nullable `holder_token`.
- `src/reservations/claim.ts` — reuse this session's live hold instead of minting a second, plus
  a shared `vesselIsAvailable` predicate so the reuse path and the mint loop cannot drift.
- `app/(public)/book/checkout/actions.ts` — `__Host-` httpOnly cookie, mint on first submit.
- 11 new cases in `claim.test.ts`; the pre-existing sold-out test rewritten (it submitted one
  buyer three times, so it was passing *because* of this bug).

**The part worth remembering:** the first design keyed reuse on the buyer's **email/phone**, which
are typed into a public unauthenticated form. `/security-review` found two HIGHs — a hold hijack
and a targeted deletion of a named person's hold — and I stopped and handed the decision back
rather than patching. Operator chose the session-cookie redesign. **Every safety argument in my
original comments reasoned about one honest buyer retrying; none considered a second party
asserting the first's identity.** The re-review then found three more (cookie fixation without
`__Host-`, `Secure` gated on `isProdDeploy()` which is false on HTTPS previews, and a reused hold
skipping the availability checks the mint loop applies). All fixed.

**Code review:** @code-review — 3 findings, incl. a test whose assertions were all inside a type
guard, so it would pass green asserting nothing. /security-review — 2 HIGH → redesign → 3 more.

**PR:** [PR #750](https://github.com/mobiustripper42/muster/pull/750)
**Points:** 5
**Branch:** task/575-hold-reuse
**Opened at:** 2026-08-16T16:20:00Z

**Next Steps:**
- **PR #750 needs the decline-card hand test** — Stripe test card 4000 0000 0000 0002, three
  submits, then confirm `select ... from checkout_holds` has exactly one row.
- **PR #749 hand-test step 6 is the unverified one**: the "Already booked?" line on `/book` may
  sit under a sticky client-island footer at 375px. No browser has seen it.
- **PR #748** hand-test: apply the migration, confirm a normal refund still works against Stripe
  test mode, and confirm `refund_leases` is empty at rest after every attempt.
- **PR #744 needs the hand-test pass before merge** — no browser has opened `/b/<code>` and the
  e2e suite never ran, because Postgres was down on this box (`pg_isready` → no response on
  5432). The PR's "Verify by hand" section is written to be run cold; step 5 is the abort path
  (open the reissue disclosure, close it, confirm the link still works).
- Session-83 leftovers still undisposed: issue #445 (move off vercel), what
  `docs/HOSTING_MIGRATION.md` becomes, and whether the Neon move gets written up separately.
- issue #736 (flag `=== "true"` vs `=== "1"`) still deliberately unfixed.

**Context:**
- `RESERVATION_LINK_SECRET` is **gone** — deleted, not deprecated. Anything still referencing it
  is stale. The window for deleting it existed only because it was never set in production.
- **Postgres was DOWN for most of this session**, so every "green" before Task 3 excluded the
  entire Postgres adapter suite — it reported as "2 skipped" and I read past it. When the DB came
  up it immediately caught a real defect (a new table missing from the contract suite's truncate
  list). **Check the skip count, not just the pass count**, and re-run the earlier branches'
  suites now the database is available.
- **Four migrations now unapplied in production**: `20260810011500_payment_intent_lookup.sql`
  (carried from sessions 81 and 83), `20260814123030_booking_codes.sql` (PR #744), and
  `20260814170248_refund_leases.sql` (PR #748). All three must be applied before promoting.
  `refund_leases` is the sharp one — after PR #748 merges, every refund calls
  `acquireRefundLease`, so on an un-migrated database refunds throw rather than degrade.
- **Deposits are off (operator, 2026-08-14)** — issues #574 and #712 deferred on that basis. But
  the repo default is still `depositMode: "deposit"` at 25% (`src/reservations/payment-config.ts`),
  overridden per-field by `payment.*` settings keys; the deployed value isn't answerable from a
  checkout. If that setting is ever unset or reset, deposits resume and both issues come back live.
- **Push notifications do not exist** — `public/sw.js` is a 12-line installability shim, no push
  listener, no subscription storage, no VAPID. Filed as issues #745 / #746 / #747 and parked;
  only part 3 needs the iPhone. issue #408 (crew roster) held behind it, with a first-name sort
  requirement added.
- Every new table must be classified in `src/admin/integrity-coverage.ts`, and a `kind: "fk"`
  claim is verified against the DDL — which only sees an **inline** `references`, not a
  follow-up `alter table` in a `do $$` block. That's why this migration declares its FK inline.
