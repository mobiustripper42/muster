---
session: 86
dev: eric
slug: main
branch: main
started: 2026-08-17T11:27:01Z
ended:
points:
pr_numbers: [759, 764, 770, 775]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/bc66b2ff-4d71-4f59-b0c6-1c02e32fb7fb.jsonl
---

# Session 86 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Hours round at the edge instead of truncating, through one shared rule (issue #758)

**Completed:**
- `src/admin/hours-format.ts` + test — **new**, the one rounding rule. `decimalHours` (Gusto's 2dp),
  `hoursLabel` (the payroll table's aligned `Xh Ym`), `compactDuration` (the punch lists'
  `8h` / `45m` / `9h 30m`), all formatting a single `Math.round`.
- `src/admin/payroll-reconcile.ts` — local `decimalHours` + `decimalHoursForTest` deleted (−30
  lines); the `1e-9` epsilon went with them (it guarded a `Math.floor` cliff rounding doesn't have).
- `app/(admin)/admin/payroll/page.tsx`, `app/(admin)/admin/time-clock/page.tsx`,
  `app/(crew)/crew/time/page.tsx` — three page-local formatters deleted, all importing the shared one.
- `docs/SPEC.md` §2.9.6 amended; DEC-157 written; `db/seed-timeclock-dev.ts` prose corrected.
- `docs/FUTURE_IDEAS.md` — parked the repo-wide "one rule, many spellings" audit that this found.

**How it was found:** the operator ran the Gusto mapping (`db:crew import-gusto --apply`, plus four
hand `db:crew set` lines for people the extractor map missed), downloaded the payroll CSV, and the
column summed 220.60 against the page's 220h 41m. Five minutes across seventeen people, all of it
one direction.

**Three things worth remembering:**
- **The original rationale was right, at the wrong boundary.** §2.9.6 truncated because
  "under-stating beats inflating when the number becomes a payment" — correct for a number Muster
  *charges*, backwards for one it *owes*. The call was made on the crew *display* (#626) and copied
  onto the CSV, which is not a display; it is the payment.
- **The rule was spelled four times in three shapes and one had drifted.** No per-PR review could
  have caught it: every copy was correct the day it was written, and the defect exists only across
  the corpus when nobody is editing any of them.
- **`amends_spec` only accepts numbered *headings*.** `"2.9.6"` was rejected outright — it's a
  bolded lead inside `## 2.9`. Declared as `section: "2.9"` with the scope naming §2.9.6. Loud
  failure, but the fix isn't guessable from the message.

**Also filed:** issue #757 (proper linting — `src/` is never linted at all, by two independent
gates), issue #756 (departure time clipped on the open-shifts card, low priority).

**Code review:** @code-review — 0 findings; traced negative-input safety to every caller, format
fidelity against the pre-diff bodies and the e2e literals, and `deltaLabel`'s sign handling.
/security-review — 0 findings (money-computed trigger hit): no CSV/formula injection, escaping
unchanged, no auth or gating line moved. `/code-review ultra` not run. One thing declared rather
than defended: `decimalHours` on a negative input emits garbage where `.toFixed(2)` didn't —
unreachable, and the module documents a non-negative contract.

**PR:** [PR #759](https://github.com/mobiustripper42/muster/pull/759)
**Points:** 3
**Branch:** task/758-round-not-truncate
**Opened at:** 2026-08-17T13:08:00Z

## Task 2: Wait for the focus move instead of reading it once (issue #742)

**Completed:**
- `e2e/crew-header.spec.ts` — two single-read `page.evaluate(() => document.activeElement)`
  focus assertions became polling `toBeFocused()`. Reproduced **6 failures in 20 runs** before
  touching it; 20/20 after, 20/20 again under real CPU load, and clean in two full suites.
- Ran in a **linked worktree** at `/home/eric/muster-742` so the operator could work
  issue #756 concurrently in the main checkout.

**Closes issue #742 covering ONE of its three tests — stated in the PR, not implied:**
- `crew-header:234` — fixed here.
- `calendar:789` — **not** fixed; split out as issue #762. Reproduces 4/4 full-suite and is
  **not** the paint race the issue body claimed. May be a product defect in the zero-refund cancel.
- `integrity:117` — never reproduced in ~70 attempts across four conditions. Untouched.
- New: issue #763 — the suite is broadly flaky on the CI path (11 and 6 failures across two
  full runs, only two tests repeating).

**Four things worth remembering:**
- **`E2E_PROD` defaults to `!CI` (`playwright.config.ts:33-36`), so a bare local
  `playwright test` runs the PROD server while CI runs DEV.** Several hours of "does not
  reproduce" were measured on the wrong server and had to be discarded. The prod path also
  deterministically fails all 8 `iphone`/WebKit tests — `next start` sets `NODE_ENV=production`,
  the session cookie goes `Secure`, and WebKit refuses it over plain http. Use
  `npm run test:e2e:dev`. This is documented at `playwright.config.ts:98-104` — inside the file,
  discoverable only after losing the afternoon.
- **Reproduction scope matters as much as the loop.** `calendar:789` passes 40/40 isolated and
  42/42 as a whole file, and fails 4/4 only in the full suite. A `-g` filter is a different
  experiment from the one CI runs.
- **CPU load has to survive the shell.** The first contention run proved nothing — `&` burners
  died with the parent Bash call and `uptime` was unchanged. Start them in the same command as
  the thing under test and print `uptime` on both sides.
- **`workers: 1` / `fullyParallel: false` is load-bearing** (`playwright.config.ts:46-47`) — the
  suite shares one `muster_test` DB, so raising workers to manufacture contention would produce
  fake failures from seed stomping rather than real races.

**Also:** GitHub's GraphQL endpoint was 503 for a stretch. `gh issue create --label` fails
(labels validate over GraphQL); `gh api repos/OWNER/REPO/issues --input <json>` is REST and works.

**Code review:** @code-review — 0 findings. Traced the one thing that could have made it subtly
wrong: the component focuses `panel.querySelector("a, button")` while the test now asserts on
`dialog.getByRole("link").first()`. They agree structurally — the links block always precedes the
buttons block — and coverage is strictly stronger than the old any-anchor-anywhere check.
/security-review not run (no blast-radius trigger; test-only diff). `/code-review ultra` not run.

**PR:** [PR #764](https://github.com/mobiustripper42/muster/pull/764)
**Points:** 3
**Branch:** task/742-flaky-e2e
**Opened at:** 2026-08-17T17:05:00Z

## Task 3: Lint src/, db/, e2e/ and scripts/ — they were covered by nothing (issue #757)

**Completed:**
- `eslint.config.mjs` — core purity on `src/`+`db/` (**0 violations**, pure lock-in),
  `no-unused-vars` (**57 across 37 files** → 0), `sonarjs/no-identical-functions` (3 → 0),
  focused-test bans for vitest + playwright (0, prevention). `lint` widened to
  `app components src db e2e scripts`.
- DEC-159 written (standing rules for admitting a lint rule); DEC-090 amended in one clause.
- `docs/FUTURE_IDEAS.md` — corrected the retracted per-file claim in the parked audit row.
- Deferred: `rules-of-hooks` (needs an install), `no-floating-promises` (needs two numbers).
  **Rejected:** `exhaustive-deps` at `warn` — `lint` has no `--max-warnings 0`, so a warn rule
  cannot fail the gate; it prints advice nobody reads while implying enforcement.

**Two bugs my own cleanup shipped — both caught, both worth remembering:**
- **A scripted edit's cleanup regexes ran over whole files, deleting ~289 trailing commas
  across 32 files.** Both typechecks and 2294 tests passed on it. Caught only by reading the
  diff *size* (+446/−380 for what should be pure deletions). Redone line-scoped: +15/−37.
  CLAUDE.md's rule about scripted edits asserting their anchors is necessary but not
  sufficient — the anchors matched fine; the damage was in the tidy-up afterwards.
- **"assigned a value but never used" does NOT mean dead code.** It means written and never
  *read* — the writes are live. Deleting `db/xola-report.ts`'s `itemsWithoutAddOns` left its
  increment behind, so `db:xola:report` would throw `ReferenceError` on the exact input its
  own comment says is expected. `db/` is in neither tsconfig's `include` and has no test, so
  lint + both typechecks + 2294 tests all passed on a file that crashes when run. Caught by
  @code-review. Fixed by *surfacing* the counter — it was a deliberate wire-shape canary, and
  a counter nobody reads is a canary in a sealed box.

**Also worth remembering:**
- **DEC numbers must be resolved across every worktree, not just the one you're in.** The
  other session had an untracked `DEC-158-*.md` in `/home/eric/muster`; only reading that
  working tree caught it. Took DEC-159.
- **@architect earned its cost twice over** — found 4 coverage gaps in my first cut (the
  vitest rule matched 1 of vitest's 5 include roots; the playwright rule left `e2e/fixtures.ts`
  unlinted; the core-purity glob missed `.tsx`; the CORE_PURITY message claimed "src/" while
  firing on `db/`) and corrected a claim I'd made — the "deliberately minimal, no plugins"
  posture was the config's header comment, never DEC-090.
- **`sonarjs/no-identical-functions` is PER-FILE** — tested, not assumed. It would NOT have
  caught the `fmtMinutes` twins that motivated the issue. I'd claimed otherwise three times.

**Also filed:** issue #767 (`write-booking.ts` claims a `canBook` pre-check it no longer
performs — the one real defect among the 57).

**Code review:** @code-review — 1 real bug (the ReferenceError above) + 3 cleanups on commit 1,
all fixed in commit 2. @architect — 4 coverage gaps + 1 factual correction, all folded in.
/security-review not run (no blast-radius trigger). `/code-review ultra` not run.

**PR:** [PR #770](https://github.com/mobiustripper42/muster/pull/770)
**Points:** 5
**Branch:** task/757-proper-linting
**Opened at:** 2026-08-17T22:45:00Z

## Task 4: Ignore add-on options the customer never picked; charge for the hull (no issue — found live)

**Completed:**
- `db/xola-report.ts` — `readAddOns` skips add-on rows with `quantity < 1`; new OVER-BOATED flag
  keyed on the smallest fleet hull that would have fitted the party; `FLEET_CAPS` derived from
  `CAPACITY`; the "consistent and within capacity" section excludes over-boated rows.

**The bug:** Xola emits every option of a question as its own add-on row and marks the chosen one
with a quantity. Its own Roster Details screen shows ONLY the picked ones — which is why the
report disagreed with the operator reading the same booking. `readAddOns` took every row, so
Mallory's declined "Yes-one more" (qty 0) became `declared 1` and Sarah's declined "Yes-four more"
became `declared 4`. Both raised WOULD-BE-OVER alerts nobody had earned.

**This took roughly three hours and should have taken ten minutes.** The whole fix is one line
plus a new flag. What burned the time, in order:

- **I invented Sarah's data instead of reading it — twice — after being told the answer.** The
  operator said plainly she was correctly flagged. I had Mallory's real rows, generalised a wire
  shape from that ONE order, and wrote Sarah's test fixture from my own paraphrase of "she changed
  her mind". Shipped it. @code-review passed it, because a reviewer cannot tell a fabricated
  fixture from a real one — it only checks the test fails without the guard. **A green test over
  an invented fixture is a confident lie with a certificate attached.**
- **Three wrong rules in a row, each shipped as a fix**, then reverted: skip-qty-0 (dropped Sarah
  too), selected-answer-wins (also dropped her), ANSWER≠BOAT (wrong reading of why she matters).
  PR #774 was closed and the branch deleted; this is the clean rebuild.
- **The answer was one `--raw` dump away the entire time.** When it finally arrived, Sarah's and
  Mallory's add-ons were IDENTICAL — the difference was never in the answers, it was the boat.
- **The operator's screenshot of Xola's own UI was the decisive evidence**: 4 add-ons on screen,
  6 in the API. That single fact settles the whole wire shape and I never asked for it.

**The rule, from the operator:** BrewBoat bills the BOAT, not the head count. Sarah has a 14-pax
Brew 1 for 12 → 2 seats to charge. Ali Eltatawy is 13 on a 14 and owes nothing, because 13 does
not fit on a 12 — which is why the check keys on the smallest fitting hull, not on `cap - pax`.

**Open in the PR, deliberately undecided:** whether `owed` is `cap - pax` (what the operator
specified) or `cap - smallestFit` (what @code-review argues). They differ only off a capacity
boundary, and no booking in the current pull is. Left as specified rather than guessed — again.

**Code review:** @code-review — 4 findings. 2 fixed (FLEET_CAPS was a second hand-written copy of
the fleet; the calm section could list an over-boated row twice). 1 raised in the PR as an open
question. 1 declined: a unit test needs `readAddOns` extracted into an importable module, which is
precisely the scope creep that made the first attempt take hours — its own task, not bolted on.
/security-review not run (no trigger; the script writes nothing). `/code-review ultra` not run.

**PR:** [PR #775](https://github.com/mobiustripper42/muster/pull/775)  (PR #774 closed, branch deleted)
**Points:** 2
**Branch:** task/xola-charge-by-capacity
**Opened at:** 2026-08-18T18:05:00Z

**Next Steps:**

**Context:**
