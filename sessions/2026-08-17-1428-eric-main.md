---
session: 87
dev: eric
slug: main
branch: main
started: 2026-08-17T14:28:28Z
ended:
points:
pr_numbers: [760, 768]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/3394a2a2-eded-5fff-9fe9-34ebeffc929e.jsonl
---

# Session 87 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Every feature flag accepts 1 or true, through one helper (issue #736)

**Completed:**
- `app/lib/flags.ts` — one `flagOn(name)` helper accepting `"1"` or `"true"`; all four flags
  (`CREW_SELF_SERVE`, `MESSAGING`, `RESERVATIONS`, `TIME_CLOCK`) route through it. The comment
  justifying the asymmetry is deleted: its premise ("already set in deployed environments") was
  false — prod has `RESERVATIONS` unset per `docs/HOSTING_MIGRATION.md:217`.
- `app/lib/flags.test.ts` — **new**, 49 cases. Every flag × 12 values, plus a `FLAGS.length === 4`
  guard so a fifth flag added without a row fails here rather than in production.
- `docs/DEPLOY.md`, `env.example` — both narrated the defect as current fact. The second was found
  by `@code-review`, not by me.
- `app/(public)/book/page.tsx`, `.../checkout/page.tsx` — flag-off copy said `RESERVATIONS=true`.

**Also promoted to production this session** (before this task): `main` → `production` at **v1.1.2**,
102 commits / 32 PRs, `685e184` → `d564cd7`. Migration ledger verified exact (50/50, both
directions) against Neon `delicate-art-65084110` before the ff-merge. Operator confirmed the #759
payroll rounding fix works on prod.

**Three things worth remembering:**
- **A fix that leaves the docs describing the bug hasn't landed.** `DEPLOY.md` I caught; `env.example`
  I did not, and `DEPLOY.md:82-86` explicitly names `env.example` as the file to trust when the two
  disagree — so the first version of this PR moved the trap one file over. Then the same class again
  in *rendered* copy (`Set RESERVATIONS=true`). Three files, one belief.
- **The full suite has an intermittent failure that is not this branch.**
  `src/adapters/postgres-repository.test.ts > "the same slot can be SOLD AGAIN after a cancellation"`
  — passed on `verify`, failed 2 of 4 standalone `npm run test` runs, passes 144/144 in isolation.
  Provably independent: it injects `reservationsEnabled` as a `WebhookDeps` field (`:241`) and never
  imports `app/lib/flags`. Needs its own issue; sibling in kind to issue #742.
- **`MESSAGING` has a second, worse defect left out of scope.** `messagingEnabled()` asks "is it on",
  but `booking-confirmation.ts:31,103`, `sold-out-notice.ts:20` and `b/find/actions.ts:40` treat
  `MESSAGING === "false"` as a hard kill. `MESSAGING=0` leaves confirmations ON; `MESSAGING=false`
  silently kills them. Needs its own issue.

**Decided by Eric mid-task: `1` only, matching every other env var.** The issue proposed accepting
both `"1"` and `"true"`; that shipped first and was then narrowed. `playwright.config.ts:162` was
the only place `"true"` was ever set and moved with it. `docs/audit/2026-08-07-cutover-phase-a.md:89`
left alone — a dated audit correctly records what was true when written.

**Filed:** issue #761 (the `MESSAGING` double-read).

**Code review:** `@code-review` — 1 finding (stale `env.example`), fixed in `0674a31`. It also
verified monotonicity (`flagOn` accepts a strict superset, so nothing ON can go OFF), that the test
isn't vacuous, that every reader routes through the helper, and that no DEC governs flag-value
spelling. `/security-review` not run — no blast-radius trigger; the diff touches no listed path and
produces booleans, not money. `/code-review ultra` not run.

**PR:** [PR #760](https://github.com/mobiustripper42/muster/pull/760)
**Points:** 2
**Branch:** task/736-one-flag-spelling
**Opened at:** 2026-08-17T17:47:00Z

## Task 2: Three formShifts callers never told crew their day moved (issue #765)

**Completed:**
- `src/reservations/booking-webhook.ts`, `src/reservations/cancel-reservation.ts`,
  `app/api/cron/tick/route.ts` — all three now pass `notifyTripChanges: true`.
- `src/builder/form-shifts-notify.test.ts` — **new** source-reading guard: every `formShifts`
  caller passes the flag or sits in `SILENT_BY_DESIGN` with a stated reason.
- Two failing tests written first, in `booking-webhook.test.ts` and `cancel-reservation.test.ts`,
  plus a negative control (brand-new shift → silence) that passes before *and* after.
- `docs/decisions/DEC-084` amended — it said notices come "never from the `formShifts` re-derive
  path", which #350 had already made false.

**How it was found:** scoping issue #740 ("the notice doesn't say *what* changed"). Traced the
notice path top to bottom and found the notice largely wasn't firing at all.

**Three things worth remembering:**
- **The bug was a missing line, not a wrong one.** `notifyTripChanges` is opt-in, so a caller
  written later just silently stops telling crew. Every existing caller's test passed the whole
  time — you cannot write a behavioural test for a caller that doesn't exist yet. Hence a
  source-reading guard, same trade `app/lib/time-clock-gate.test.ts` makes.
- **The guard quoted its own documentation as evidence.** First cut matched the flag against
  whole-file raw source — and every governed file *explains* the flag in a comment beside the
  call, so deleting the flag left the test green. Caught by `@code-review`, not by me. Now strips
  comments and matches per call site.
- **The cancel path's own comment was half-true and had been for months.** "Re-form so the shift
  collapses and its crew are told" only held when the cancelled trip was the day's *only* trip.
  A comment stating an intent the code half-delivers reads as verification.

**Filed, not fixed:** issue #766 — `formShifts` writes inside its loop with no transaction or
lock. A mid-loop throw loses a notice *permanently* (the tick then sees no diff); two overlapping
runs duplicate one. Both pre-date this, both got more reachable with it. Found by `@code-review`.

**Code review:** `@code-review` — 3 findings: the guard weakness fixed in `f6d030a`; two
structural findings filed as issue #766. It also verified the sequential no-duplicate claim, that
no clock/lead-days effect produces a spurious diff, and that the operator-as-crew exclusion applies
at all three new call sites. `/security-review` — 0 findings (blast-radius trigger hit: the diff
touches the Stripe webhook handler and the cancel/refund path). `/code-review ultra` not run.

**PR:** [PR #768](https://github.com/mobiustripper42/muster/pull/768)
**Points:** 3
**Branch:** task/765-notify-on-muster-booking
**Opened at:** 2026-08-17T20:12:00Z

**Next Steps:**

**Context:**
