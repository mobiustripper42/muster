---
session: 89
dev: eric
slug: main
branch: main
started: 2026-08-19T17:56:40Z
ended:
points:
pr_numbers: [779]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/a9d4937b-d2fb-474c-b01d-c585d07fd4fd.jsonl
---

# Session 89 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Decide the tax rate is a live read (issue #574)

**Completed:**
- Amended `docs/decisions/DEC-107-payments-stripe-hosted-checkout-deposit-balance.md` (+39): the
  live tax read stands, on the ground that the rate is not browser-editable. A per-reservation
  frozen rate is rejected as the wrong shape — sales tax is a jurisdiction's rate on a date, so the
  model is a `(rate, effective_date)` lookup if a rate ever moves. Which date governs (booking,
  departure, charge) is deliberately unanswered.
- Withdrew the `payment-config.ts` docstring's promise of a "P12 admin settings screen" exposing
  the payment config — building it is what would reintroduce the hazard.
- Established the fact the decision rests on: nothing under `app/` calls `setPaymentConfig`, and
  `/admin/settings` carries only the engine pause.
- Out of band, not in the diff: `payment.tax_rate_bps` set to `800` in local and production
  `app_settings` (was `600`). Forward-only — `payments.tax_cents` is frozen on existing rows.

**Code review:** 1 finding — the claim "nothing pins the default `taxRateBps`" is false
(`payment-config.test.ts:70-86` pins it indirectly). Made in conversation and in this log, not in
the amendment, so nothing in the diff changed; both records corrected. All 7 other factual claims
verified against source; amendment structure complies with the decision-record protocol.
**PR:** [PR #779](https://github.com/mobiustripper42/muster/pull/779)
**Points:** 2
**Branch:** task/574-tax-rate-live-read-decided
**Opened at:** 2026-08-19T21:16:07Z

**Next Steps:**
- **Lane B** (public `/book` + checkout, money core, Stripe webhook, messaging), ordered by
  the operator: ~~#544~~ → ~~#574~~ → ~~#712~~ → #723 → #713 → #767 → #776 → #773 → #622 → #683 →
  #752 → #667 → #668 → #761 → #687. #776 gates #773; #683 depends on #622; #687 last.
  **Next up is #723.** Started at 47 pts; #574 shipped and #712's 8 came out, so ~36 remain.
- **No deposits in 2027 (operator, 2026-08-19).** If reservations release, they release without a
  deposit. **issue #712 is deferred** — labels `phase:12`/`phase:12b`/`priority:high`/`reservation`
  removed, `deferred` added, with the rationale and the carry-forward notes as a comment on the
  issue. It takes the off-session / saved-payment-method work with it: nothing else tracks that,
  because issue #678 explicitly scopes it out. If deposits ever return, that decision comes BEFORE
  any deposit is taken in production — a deposit taken without a saved payment method can never be
  auto-collected against.
- **issues #678 and #679 were already filed** for the two Stripe gaps the operator raised
  (no Stripe Customer / only metadata reaching Stripe). Nothing new was created — both had a points
  label and nothing else, which is why they never appeared in a phase query despite being open.
  Now `phase:12b` + `priority:medium`. They are NOT tagged `reservation`; add it if they should
  block go-live. **The go-live cost of #679 is support, not data fidelity:** when a guest calls
  about a charge, the Stripe dashboard cannot be searched for them.
- **issue #544 is blocked on Drew** — Muster has no access to the Stripe account, so the webhook's
  subscribed-event list cannot be read from the dashboard or the API. Not a code problem and not
  ours to unblock. The operator is chasing it 2026-08-20. Nothing in the repo records this yet.

**Context:**
- **e2e in this worktree must be told where to run**, or it collides with session 90's harness on
  the default port 3100 / `muster_test`:
  ```
  E2E_PORT=3200 TEST_DATABASE_URL=postgres://muster:muster@localhost:5432/muster_test_s89 npm run test:e2e
  ```
  Both env vars are honored (`playwright.config.ts:38`, `db/reset-test.ts:18`) and there is no
  db-name allowlist to fight. `muster_test_s89` already exists; the harness migrates and truncates
  it on first run, so no manual schema step. **This is a worktree-concurrency recipe, not a project
  fact — it does not belong in `docs/RUNNING.md` unless running two sessions becomes normal.**
- **Sales tax is 800 bps (Cuyahoga County), set explicitly in `app_settings` in BOTH local and
  production on 2026-08-19.** It was 600 before — verified from a Purchase Summary screenshot:
  $29.94 on a $499.00 fare is exactly 6.0%, and the code taxes fare + add-ons only, never tip or
  service fee (`payment-config.ts:146`, `:28-29`). Local confirmed after the update:
  `payment.tax_rate_bps|800`, and it is the ONLY `payment.*` row — everything else inherits the
  code defaults, including `depositMode: "full"` (DEC-155).
- **Whether production was at 600 or inheriting the 725 default was never answered**, so the size of
  the under-collection on bookings already taken is unknown. Correcting the rate is forward-only:
  `Payment.tax_cents` is frozen on rows already written, so past bookings keep the tax they were
  charged. If the operator wants to know what is owed to Ohio, that is a query against `payments`,
  not a code change.
- The code default still reads `taxRateBps: 725` with a comment claiming "Ohio state sales tax
  7.25%" (`payment-config.ts:61`) — wrong jurisdiction, wrong number. Left standing deliberately:
  both deployments carry explicit rows so it never applies, but a fresh deploy inherits it.
- **Correcting that default is NOT a one-line change**, contrary to what was said in session.
  `payment-config.test.ts:70-86` reads `cfg.taxRateBps` live off `PAYMENT_CONFIG_DEFAULTS` while
  hardcoding `3618` — `taxCentsFor(49900, 725)` — as the payment's tax, so flipping the default to
  800 makes `balanceOwedCents(49900, 800, [payment])` return `374` instead of `0` and that test
  fails. Grepping for the literal `725` never finds it. Caught by `@code-review` on PR #779. The
  test breaking is correct behaviour, not an obstacle — but the follow-up PR has to update it.
- **Reopened.** The 02:19 window that first claimed session 89 closed without a task, a commit or a
  PR. This file is reused rather than superseded: `started` and `transcript` now point at the 17:56
  window, and the earlier stamps are gone. Nothing was lost — there was nothing in it.
- **Concurrent with session 90**, which holds the main checkout `/home/eric/muster` on
  `task/699-form-error-keeps-your-work`. This session runs in the linked worktree
  `/home/eric/muster-s89`, cut from `main` at `4cf57de`. Task branches get cut there.
- `.env.local` copied from the main checkout into the worktree at open (gitignored by
  `.gitignore:26`). `.env.development.local`, `.env.local.backup` and `.env.xola.prod` were **not**
  copied — nothing has asked for them yet.
- Both sessions share the one `sessions` branch worktree at `/home/eric/muster/.sessions-worktree`,
  because a branch can only be checked out once. `/kill-this` and `/its-dead` in either window
  `reset --hard` it — so the two must not run at the same moment.
- Issues are being sequenced to avoid file overlap with 90, which owns
  `app/(admin)/admin/{add-ons,locations,offerings,vessels}/*`, `components/ui/action-form.tsx`,
  and `e2e/*`.
- Drift vs seeds at session open: 11 `logic`-class files differ (six skills, three scripts,
  `CLAUDE.md` shell). Not acted on.
