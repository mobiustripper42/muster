---
session: 96
dev: eric
slug: main
branch: main
started: 2026-08-23T21:19:09Z
ended:
points:
pr_numbers: [831]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/2ebab59e-5d57-5123-9024-f73659b71c5f.jsonl
---

# Session 96 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: A half-filled form asks before you leave it (issue #781)

**Completed:**

- **Closed issue #783 first, without fixing it.** It did not reproduce on `main` at `f626cdf2` —
  not by hand at 3G (operator), and not at machine speed with the CPU throttled 20× over CDP.
  Two e2e tests written to settle it captured the second submit's POST body, which is the
  measurement the issue asked for and never had; the body carried the correction. Its original
  evidence (`Test timeout of 30000ms exceeded`, PR #782, 2026-08-19) carries the fingerprint of
  issue #763, fixed one day later in PR #796 — before which `playwright.config.ts` had a flat
  `timeout: 30_000` with no dev-path multiplier. Hypothesis with a citation, not proof; no better
  one is obtainable, because commit `fe26c3e2` routed the failing test around the path instead of
  diagnosing it. Those two tests were **binned** at the operator's call — the live defect of that
  family is tracked on issue #818.
- **New:** `components/ui/form-dirty.ts` (pure predicate + `bornFormState`),
  `components/ui/unsaved-guard.tsx` (`useFormGuard` + `<UnsavedGuard />`),
  `components/ui/form-dirty.test.ts` (8 cases), `e2e/unsaved-guard.spec.ts` (8 cases).
- **Moved:** `components/admin/dirty-state.ts` → `components/ui/dirty-state.ts`; gained
  `confirmLeaveOnce` and the refcounted `retainBackTrap`; confirm copy generalized off
  "this punch".
- **Rewrote** `components/admin/dirty-submit.tsx` as `useFormGuard` + a button (86 → 55 lines).
- **Wired** 10 `<UnsavedGuard />` mount points across 8 files, covering all ten draft surfaces.
- **`docs/decisions/DEC-160`** — `grep -rli "unsaved|beforeunload|navigation guard|leave the page"
  `docs/decisions/`` returned nothing, so a new id, not an amendment.
- **Gate:** `npm run verify` exit 0 (2437 Vitest). Regression 24/24, 11/11, 28/28;
  `admin-form-error` 13/14 — the one failure is the pre-existing issue #818 cancel-confirm
  concatenation, which fails identically on `main` before this branch.

**Code review:** `@code-review` found 2, both real, both fixed in `e959a52` — two confirm dialogs
for one click when two forms are dirty (`/crew/time-off` renders two side by side), and a history
sentinel pushed per-form and per-clean→dirty-transition rather than once per page.
`/security-review` run despite no Blast-Radius path match (the shared component feeds two
money-computed surfaces): 0 findings.

**PR:** [PR #831](https://github.com/mobiustripper42/muster/pull/831)
**Points:** 8
**Branch:** task/781-unsaved-form-guard
**Opened at:** 2026-08-25T03:07:00Z

**Next Steps:**

- **PR #831 wants a hand check the tests cannot do** — its plan is written out. Two items are
  judgment, not assertion: whether the confirm is escapable at 375px, and **the Back wart** — after
  typing and reverting, the first Back press does nothing because the sentinel is spent. Removing
  it means `history.back()` from an effect cleanup racing a server action's `redirect()` on the
  time clock. Deliberate; revisit if it annoys.
- **Issue #818 now carries a second finding** (comment `5388699581`): the cancel-confirm override
  amount comes back **concatenated** with the prefill — `538.80five hundred` — not replaced.
  Reproduces on the dev path, invisible in CI because CI retries once. Operator's call: nothing on
  that surface until the money path is redesigned (issue #812, behind issue #816).

**Context:**

- **The Blast-Radius table did not fire on this PR and arguably should have.** The diff touches no
  path under `admin/time-clock/**` or `payroll/**` — it changes a *shared component those surfaces
  import*, and it changes when their Save button is enabled. Same gap as `refund-payment.ts`
  (`.claude/CLAUDE-context.md:137`). **No row was added**, deliberately: the honest row would be
  `components/ui/unsaved-guard.tsx`, which would fire a security review on every future UI-guard
  change across ten mostly-non-money surfaces. Flagged by hand in the PR body instead.
- **A test that passes is not a test that bites, and this session produced one of each.** The
  two-dirty-forms test passed with the fix reverted on its first cut, because it *dismissed* the
  prompt — and declining calls `stopPropagation()`, which stops the second listener ever running.
  Only the *accept* path reproduces it. Checking cost one minute and the test was worthless until
  it was checked.
- **Two defects were found only because a test was written, not because the design was reasoned.**
  Snapshotting the live form at mount is a race against hydration (anything typed in the window
  becomes the baseline, so the guard goes silent on real work — the feature's own bug arriving
  through the feature), and dirtiness had to be evaluated once *at* mount, not only on the next
  event. Both are in DEC-160 as rejected alternatives, because both are the cheap thing a future
  session reaches for first.
- **`git reset --hard*` and `git branch -D *` are denied fleet-wide** (`.claude/settings.json:13,18`,
  same in user settings). Hit twice this session; the operator ran both by hand. Not a bug — worth
  knowing before reaching for either.
- **Session 94 shipped 6 PRs into `origin/main` while this session worked** (#819–#823 and the
  shell sync). `CLAUDE.md` and `.claude/CLAUDE-context.md` both changed underneath; re-read before
  writing DEC-160, which the new "a decision may not contain a number the customer sees" rule
  constrains. This branch is 6 behind `origin/main` at PR time — GitHub's "Update branch" handles
  it; do not rebase.
