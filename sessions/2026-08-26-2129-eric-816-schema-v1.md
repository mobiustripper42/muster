---
session: 97
dev: eric
slug: 816-schema-v1
branch: task/816-schema-v1
started: 2026-08-26T21:29:17Z
ended:
points:
pr_numbers: [847, 848, 849, 850]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/0b6d6877-9b9b-409f-808c-dc0c2b55949f.jsonl
---

# Session 97 — 816-schema-v1

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Gate the decision record on schema v1, grandfathering the corpus

**Completed:**
- `docs/decisions/decision-record.schema.json` — draft 2020-12, `additionalProperties: false` at
  record and claim level, `topic` enum injected at validate time from `_config.json`.
- `scripts/check-decisions.mjs` — a hand-written interpreter for the subset of draft 2020-12 the
  schema uses, plus five checks: schema validation, the 2000-byte cap, the `**Bold:**` body
  lead-in, `superseded_by` resolution, and a duplicate-id sweep across `docs/decisions/` and
  `archive/`. Two parsers: legacy records keep the hand-rolled reader, `schema: 1` records go
  through js-yaml.
- `scripts/check-decisions.test.mjs` — 18 new cases (50 total in the file).
- `package.json` — `js-yaml` added as a devDependency.
- `gen-decisions-index.mjs` untouched, as scoped.

**Code review:** 3 findings, all real. Two fixed — the opt-in gate was a raw-text regex stricter
than YAML (`schema: 1  # v1` read as "never opted in" and got zero enforcement), and the id sweep
had no coverage. The third is **not fixed and is a blocker on the follow-up**: `load()`'s parser
throws on `claims:`, and every check downstream runs off its map, so converting one record stops
reference/topic/freshness checks for all 158. Reproduced by the reviewer. One-line fix — `'claims'`
in `LISTS` at `gen-decisions-index.mjs:86` — but that file was scoped out, so the code comment now
says so rather than claiming incremental conversion works.

**PR:** [PR #847](https://github.com/mobiustripper42/muster/pull/847)
**Points:** 5
**Branch:** task/816-schema-v1
**Opened at:** 2026-08-26T22:04:00Z

## Task 2: Build the dictionary gate, seeded from twelve terms

**Completed:**
- `docs/dictionary.yml` — 12 operator-approved terms, three keys each. Eight payment terms
  registered ahead of use (all were in DEC-107, none survives the corpus after its rewrite);
  four with live usage — `MMC`, `COI`, `CAS`, `RLS`.
- `scripts/check-dictionary.mjs` — the gate. Prose only in `docs/SPEC.md`, `docs/decisions/*.md`,
  `CLAUDE.md`; code spans, fenced blocks and URLs stripped first.
- `scripts/gen-dictionary.mjs` → `docs/DICTIONARY.md`; `scripts/gen-dictionary-baseline.mjs` →
  the 80-term grandfathering snapshot, run once and deliberately not wired into the build.
- `package.json` — `check:dictionary` into the `verify` chain, `gen:dictionary` alongside it.
- Measured the acronym rule before wiring it: 3,006 matches, **2,026 (67%) false positives** —
  1,713 of them `DEC` matched inside every `DEC-107`. Three exclusions approved and applied.
- Also added beyond spec: a freshness check, so `DICTIONARY.md` cannot drift from the YAML.

**Code review:** 5 findings, all reproduced, all fixed and pinned. **Three were the gate
disarming itself** — the alternate baseline was a permanent global amnesty rather than a
snapshot (so `not:` would never have failed again, anywhere); the shout-list grew itself
silently, so writing `zqx` once exempted `ZQX` everywhere; and multi-word alternates went dark
across a hard line wrap. Plus a substring hole in the says-recursion hatch and an unguarded
YAML parse.

**PR:** [PR #848](https://github.com/mobiustripper42/muster/pull/848) — stacked on PR #847
**Points:** 5
**Branch:** task/816-dictionary
**Opened at:** 2026-08-26T23:12:00Z

## Task 3: Muster Reservations gets a spec, and its last eight records get rewritten

**Completed:**
- `docs/SPEC.md` §2.10 "Reservations — the surfaces around the sale" — **new**, and the first
  spec section the reservations subsystem has ever had. §2.8 covers buying; everything the
  operator runs and the customer returns to was unwritten. Four subsections carrying the 26
  SPEC-dispositioned rulings. Appended, not inserted, so nothing renumbers.
- Eight records converted to schema v1: DEC-106, 112, 124, 126, 135, 138, 154, 156 — each one
  ruling, each under the cap, each with a `spec:` claim pointing at what it dropped.
- DEC-126 went 6,346 → 1,881 bytes, losing eight rulings that all described unbuilt machinery.
- Corpus 570,747 → 469,822 bytes against the issue #816 baseline: **−100,925**, same file count.
- All 26 reservations records are now tombstoned or schema v1.

**Code review:** 3 findings, all fixed. One real: DEC-135's conversion **dropped a live shipped
feature** — the cancel/change request form — while asserting it had moved to §2.10.2, which said
nothing about cancelling. That is the "stale sentence believed and acted on" failure the section
exists to end, happening inside it on day one. Two staleness findings, both verified rather than
trusted: the refund policy does exist (issue #619, tested), and `revisit_if` cited issue #472,
which is **closed** and was always the residual-race work, never the refund schedule.

**PR:** [PR #849](https://github.com/mobiustripper42/muster/pull/849)
**Points:** 8
**Branch:** task/816-rewrite
**Opened at:** 2026-08-27T00:41:00Z

## Task 4: SPEC §2.10 — Reservations, what the operator runs

**Completed:**
- `docs/SPEC.md` §2.10, seven subsections — the first spec the operator side of reservations has
  had. Two calendars; availability computed not stored; blackout as three block kinds; the
  catalog; purchases and customers; whole-boat as a rule not a shape; what it is NOT.
- Built from `docs/design/reservations-admin.md` and `reservations-model.md` — **which existed
  the whole time.** The oversight was never that it was unspecced; it was that `SPEC.md` never
  absorbed the design docs, so every gate reading `SPEC.md` as authoritative saw a hole.
- **DEC-123 and DEC-125 recovered from git** and used as source. DEC-125 carried the entire
  computed-availability model, which is in neither design doc. The operator's call to read them
  rather than assume they were worthless was right.

**Code review:** 3 findings, all fixed, and **two were fabricated present-tense facts** — a
per-offering tax override that was never built in any form, and a per-departure override plus
customer-notify path that do not exist. Both came from the recovered records, which document
what was *designed*. I verified the claims I had put on a checklist and not the ones that
arrived as prose. **Second occurrence today** of the same failure this whole issue exists to
end; the first cost a subsection of §2.8 this morning. The sharper half: I had written "a booked
slot is frozen" as though a guard refuses the edit — nothing refuses it, nothing can attempt it,
and an absence and a guard fail differently the day someone builds that surface.

**PR:** [PR #850](https://github.com/mobiustripper42/muster/pull/850)
**Points:** 5
**Branch:** task/816-reservations-spec
**Opened at:** 2026-08-27T01:38:00Z

**Next Steps:**
- **`the-booking-1.md` and `the-living-link-1.md` — 432 lines of customer-side design**, partly
  covered by §2.8. Whether the rest is spec, parked or dead is an unmade call.
- **`SPEC.md:75`, `:1868`, `:1957` cite issue #472 as the open refund schedule.** It is closed and
  was never that issue. Three false claims about what is open, flagged in PR #849 and deliberately
  not swept in — correcting "what is still open" is a project call.
- **Rule 3 of the dictionary gate is unbuilt, by decision** — the warning on a word absent from a
  standard English wordlist. Operator declined the dependency; the acronym rule does the work.
- **Nine records still carry rulings** and are the schema-v1 corpus going forward. The rest of
  issue #816's queue — the service-fee, tips and cancellation spec sections — is untouched.
- **issue #841 and issue #838 are one problem at two levels** — the spec hedged on deposit-vs-full
  (fixed in PR #845) and the code hedges the same way, producing a positive balance on a fully-paid
  booking after a refund. #841 carries the scenario and the fix shape; not urgent, nothing sells in
  production yet.
- **Nine records still keep something** and are candidates for rewriting once conversion works:
  DEC-154 (13 surviving rulings), 156 (9), 107 (9), 135 (7), 138 (6), 106 (6), 124 (3), 112 (1),
  126 (1).

**Context:**
- Session opened mid-flight: substantial work (PR #844 merged, PR #845 and PR #846 open) happened
  before `/its-alive` ran. Operator's call, 2026-08-26: those earlier PRs do not need logging.
  Task blocks in this file start from the schema-v1 validator.
