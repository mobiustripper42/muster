---
session: 99
slug: main
branch: main
started: 2026-08-29T01:35:50Z
ended: 2026-09-02T01:36:45Z
points: 16
pr_numbers: [863, 865, 866, 868, 875, 880, 884]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster/0d9a3c6e-bfb3-55f9-9677-c50e34cb0916.jsonl
---

# Session 99 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: A vessel with no manning rule is an error, not a vacuously crewed shift

**Completed:**
- `src/builder/derive.ts` — `deriveShiftState` throws on an empty required-seat set instead of
  returning `Crewed`. Docstring records why it is an error rather than a state.
- `src/builder/derive.test.ts` — written first and watched fail for the right reason (returned
  `Crewed`, threw nothing). Pins two inputs: an empty seat list, and supernumerary-only, which is
  the same hole by another door since `deriveSeats` only mints required seats.
- `src/builder/form-shifts.test.ts` — the Duffy fixture gains a dock-hand. Its "forms a zero-crew
  rental into a vacuously-Crewed shift" test asserted the defect and was replaced.
- `app/(admin)/admin/shifts/page.tsx` — logs the cause in a previously-bare `catch {}` (issue #854),
  which would otherwise have swallowed the new throw into "try again in a moment" with an empty log.
- Gate green: `npm run verify` exit 0, 2384 tests across 164 files.

**The fix shrank twice, from opposite directions.** It began as the issue framed it: a
derivation-contract change needing `@architect`, because `manning: []` was thought legitimate for a
self-captained Duffy and had to be told apart from a missing vessel row. The operator's ruling that
a Duffy gets a **dock-hand** removed the only input for which `Crewed` was correct — no new state,
no `manningKnown` flag, no architect gate. Then the operator's second ruling — an empty manning rule
is an *error*, not a workflow state — turned the remaining question ("return `Pending`?") into a
throw. One line, arrived at by deleting design rather than adding it.

**19 tests failed on the first run and that was the finding.** All from `form-shifts.test.ts`'s
shared fixture, which seeded the Duffy with `manning: []` and carried a test whose entire subject was
the vacuous `Crewed`. The suite had encoded the defect as intended behaviour, which is why nothing
ever caught it.

**Two review findings accepted rather than fixed, both verified against the code first.**
`app/api/cron/tick/route.ts:98` is a bare `await tick(repo, now)` in a file where its five neighbours
each carry a `catch` + `console.error`, and the three per-shift loops have no isolation — so one bad
vessel would halt a whole run silently. And "unreachable by construction" is too strong: the catalog
screen births a vessel with `manning: []` (`vessel-admin.ts:74`) and the manning actions are
withdrawn stubs that redirect (`shift/[shiftId]/actions.ts:349-356`), so **no surface in the app sets
manning at all**. Both accepted on the operator's call: boats are made by `npm run db:seed:fleet`,
which always writes a manning rule; a boat hand-made through the catalog screen and then sold is a
firing offence, not a design target. The bare `tick` call is pre-existing and wants its own issue.

**Code review:** 3 findings, 0 fixed, 3 accepted and disclosed in the PR body.
`/security-review` ran (trigger: `app/(admin)/admin/shifts/**` matches Money computed): 0 findings.

**PR:** [PR #863](https://github.com/mobiustripper42/muster/pull/863)
**Points:** 2
**Branch:** task/582-derive-zero-required-seats
**Opened at:** 2026-08-29T21:30:00Z

## Task 2: The demo fixture needs a lead time, not a calendar slot

**Completed:**
- `src/reservations/seed-reservation.ts` — the demo anchor moves nine days later in the same month
  (`addDays(firstOfNextMonth(todayISO), 9)`); every `day(n)` offset below it untouched. New exported
  `MIN_BOOKING_LEAD_DAYS = 21`.
- `src/reservations/seed-reservation.test.ts` — the lead-time invariant, written first and watched
  fail with the right numbers. Two late-month `TODAYS` added.
- Comment fixes in seven more files (second commit) — `seed-xola.ts`, `e2e/reservation-demo.ts`,
  three e2e spec headers, two `db/` seeds.
- Gate green: `npm run verify` exit 0, 2405 tests across 164 files.

**Diagnosed from a CI failure on someone else's PR.** Lane B's PR #862 went red on e2e; it changes
**five markdown/YAML files and no code**, so it could not have caused anything. The demo window sat
on the 10th–16th of next month — a calendar slot, not a lead time — so the distance from today to
the first booking swings from ~42 days on the 1st to 14 on the 29th to 12 on the 31st. Inside 14
days a customer-asked cancel is non-refundable, so the two quotes `calendar.spec.ts` asserts
*differ* stopped differing: $0.00 against an unchanged $588.80, which is exactly the asymmetry the
log showed.

**The sharpest part is the time-of-day dependence.** The terms compare instants and the trip departs
at 13:30, so a 14-calendar-day gap is under 14 days for any run starting after 13:30. Green at
09:00, red at 17:00 — which is why `main` was green nineteen hours earlier with no code in between.

**The suite was already carrying the bug.** The new invariant failed on `2027-06-30`, a date that
was in the test's `TODAYS` list before this branch. Under the old anchor it put the first booking 12
days out — inside the real cutoff, not merely under the new floor. Nothing had ever asserted it.

**Fixed by removing a literal, not updating one.** Code review found the change had left the old
"10th–16th" in eight comments, including `seed-reservation.ts`'s module docstring contradicting its
own function comment 100 lines below. Each now describes the shape and points at the code for the
days, so the next move of the window cannot strand another eight copies.

**Not fixed here:** `admin-form-error.spec.ts:567` may still fail — that is issue #818's
concatenation bug. This branch changes the prefill it splices onto (`0.00` → `538.80`), not the
splice.

**Code review:** 0 functional defects; all five claims put to it verified independently. 3
doc-drift findings, all fixed. `/security-review` **not run** — no blast-radius trigger; the only
importers are seed scripts and nothing under `app/` reaches this code.

**PR:** [PR #865](https://github.com/mobiustripper42/muster/pull/865)
**Points:** 2
**Branch:** task/e2e-refund-fixture-lead-time
**Opened at:** 2026-08-29T22:15:00Z

## Task 3: Vendor IBM Plex instead of fetching it from Google at build time

**Completed:**
- `app/layout.tsx` — `next/font/google` → `next/font/local` over seven vendored faces; the comment
  that claimed "no external request" corrected.
- `app/fonts/` — Sans 400/500/600/700, Mono 400/500/600, the exact `latin` subset Google serves,
  plus `OFL.txt`. 159 KB, 14–24 KB per face.
- `app/fonts.test.ts` — new, four assertions, all watched failing first.
- Gate green: `npm run verify` exit 0, 2409 tests across 165 files; no `gstatic` in the build log.

**The dependency was invisible from every direction except a failing build.** The Google loader
fetches during `next build` and self-hosts the result, so the browser never makes an external
request and the network tab looks clean whether or not it is fixed — which is exactly the check the
operator proposed, and it would have passed against the unfixed code. The `layout.tsx` comment said
"no external request", true of the browser and false of the builder.

**The gate runs in both directions, and the second one is the interesting half.** Every declared
weight must have a real WOFF2 — checked by magic number (`wOF2`) rather than size, because the
realistic failure is a CDN answering 200 with an HTML error page, not a zero-byte file. And no
styling may ask for a weight nobody vendored: a browser **synthesizes** a missing face rather than
refusing, so `font-light` with no 300 renders subtly wrong and reports nothing. Today's usage is
exactly the four Sans weights loaded, so it passes on day one and bites the next time.

**Both scans initially failed on their own explanatory comments.** A comment containing `font-light`
*is* that string; one naming the forbidden import *is* that import to a substring match. Hence
`withoutComments` and matching the import as `from "next/font/google"` — a rule that cannot be
described in the file it governs is a rule people stop describing.

**The download had to be handed over.** Outbound `curl` is denied from the session shell, so the
fetch was the operator's to run — and my first two attempts were wrong: `exit 1` inside a pasted
loop killed his terminal window (fixed by wrapping in a subshell), and `-sH "$UA"` sent a header
with no `User-Agent:` name, so Google returned a single TTF with no subset comments and the awk
found nothing. `-A` fixed it.

**Code review: 0 findings**, and it verified three comment claims against the loader source rather
than reading them — `display: "swap"` genuinely was the Google default
(`validate-google-font-function-call.js:12`), `app/fonts/` over `public/` is right, and the CSS
variables still bind. It found three gaps in the weight regex: `style={{ fontWeight: 700 }}` fixed
in a second commit; template-literal classes, CSS shorthand, keyword spellings and
`font-[var(--x)]` documented in the test rather than fixed, since a text scan cannot follow a
computed value.

**Code review:** 0 findings; 3 regex gaps noted, 1 fixed, the rest documented.
`/security-review` **not run** — no blast-radius trigger; `app/layout.tsx` is on no listed path and
no number here reaches a paycheck.

**PR:** [PR #866](https://github.com/mobiustripper42/muster/pull/866)
**Points:** 3
**Branch:** task/731-self-host-ibm-plex
**Opened at:** 2026-08-29T23:05:00Z

## Task 4: Read the At-Risk window from the constant that owns it

**Completed:**
- `src/builder/derive.ts` — `fillDeadlinePhrase`, beside `FILL_DEADLINE_HOURS`. Whole days when the
  hours divide evenly, hours otherwise.
- `src/builder/derive.test.ts` — three cases, written first, watched fail with
  `fillDeadlinePhrase is not a function`.
- `app/(admin)/admin/at-risk/page.tsx` — the heading reads `EXHAUSTED_THRESHOLD_HOURS` instead of
  the literal "~2 days".
- `components/at-risk/risk-row.tsx` — one dead comment (second commit, from review).
- Gate green: `npm run verify` exit 0, 2408 tests across 164 files.

**Production runs 72, so the page was claiming two days on a board that boards within three** —
while the per-row "fills by" deadline rendered directly beneath it drew from the live value and
disagreed by a day. Third instance of one defect class: operator-facing copy asserting a value a
constant owns. The others were `/admin/import` claiming the Xola pull "runs automatically every
hour" after it stopped, and an At-Risk row saying reschedule where DEC-066 says override.

**Half the issue was already done and the scope question was moot.** #567 also asks to delete
`TIGHT_HOURS` from three files and poses an operator call about the cockpit and outbox copies.
Commit `7c94f49` had already removed all three plus the `tight` prop — confirmed by reading that
diff rather than by grep returning nothing, since a rename would look identical. The sweep the
issue asked for (`STAFFING_HORIZON_LEAD_DAYS`, split-suggest, drip interval) came back clean: the
At-Risk heading was the last live instance.

**Code review verified the input rather than accepting it.** `page.tsx:57` passes no opts, so
`deadlineHours` falls to `EXHAUSTED_THRESHOLD_HOURS` = `FILL_DEADLINE_HOURS`; the only other
callers (`tick.ts:466`, `warming.ts:82`) override nothing and don't feed this page. So the heading
prints the number that actually decides membership, with no second drift path.

**Two findings, both this issue's own defect class.** `risk-row.tsx:20` still carried "Inside 36h —
the countdown turns red", dangling between two field docs, describing behaviour `7c94f49` deleted —
the same defect pointed at a developer instead of the operator. Fixed. And `envPositiveNumber`
gates on finite-and-positive, not integer, so `FILL_DEADLINE_HOURS=36.5` renders "36.5 hours";
left unrounded deliberately and documented, because that reads as a typo the operator can find
where "37 hours" would print a number nobody configured.

**Code review:** 0 defects; 2 cleanup findings, 1 fixed, 1 documented.
`/security-review` **not run** — no blast-radius trigger; the output is a heading string, and the
`derive.ts` change is purely additive.

**PR:** [PR #868](https://github.com/mobiustripper42/muster/pull/868)
**Points:** 2
**Branch:** task/567-derive-at-risk-heading
**Opened at:** 2026-08-30T01:40:00Z

## Task 5: Carry a partial run's crew notices out of the failure

**Completed:**
- `src/builder/form-shifts.ts` — the loop wrapped; a throw becomes `PartialFormError` carrying
  `result` as it stood plus the original error as `cause`.
- `src/builder/form-shifts.test.ts` — the mid-loop-throw test #766 says doesn't exist. Written
  first, watched fail. Fails the Duffy vessel-day (seeded last) so day1's notice is genuinely
  computed before the throw; failing the first group would pass against the unfixed code.
- `app/api/cron/tick/route.ts`, `src/reservations/booking-webhook.ts`,
  `src/reservations/cancel-reservation.ts` — relay **and audit** the partial.
- `src/crewapp/shift-changes.test.ts` — pins symptom B as live and unfixed.
- Gate green: `npm run verify` exit 0, 2414 tests across 165 files.

**The spec cost more than the code, and that is the finding.** Operator's assessment: *"that was a
2 worth of code and a 13 spec."* He proposed the fix in his first message — *"somehow making
changedCrew persistent"* — and I answered with a durable-outbox table, a port seam, a
`formOneShift` refactor, and collect-then-persist vs transaction vs advisory lock, reversing my own
conclusion three times. He eventually asked *"why can't the catch just write the row?"*, which is
what he had said at the start. His words: **"your answers are so complicated I just went with them
... it's becoming a real flaw in our dev cycle."** Saved to memory as `build-erics-idea-first`. The
cause was speculating about files I had not opened; the one test I wrote settled a contested
question in 8ms.

**Symptom B turned out half-wrong in the issue, and reading the readers is what showed it.** #766
says two overlapping runs text a crew member twice. The SMS path is already safe — the outbox id is
`notice-${shiftId}-${crewId}-${action}`, so a second write upserts one slot. The crew-app path is
half safe: `foldShiftChanges` reads first/last touch per event id so the trip and clock facts are
untouched, but `changeCount` is a plain row count and `change-banner.tsx:45-49` renders it as
"This shift changed twice" for a shift that changed once. **#766 must not be closed on this PR.**

**Code review found a bug I introduced.** Two of the three wired callers relayed the notice without
the DEC-118 audit row, while the webhook's `relayAndAudit` does both — a crew member texted about a
durable change with no record it happened. The stale comment saying "relayed before the log" with
no log call was the tell. Fixed in a second commit.

**Accepted, not missed:** `xola-pull.ts:182` is unwrapped and still reproduces this, and the
"Pull from Xola now" button is exactly the fleet-wide re-form most likely to throw partway.
Deferred with the rest of the Xola path.

**Code review:** 1 bug (fixed), 1 accepted gap, 1 cosmetic re-indent declined — it would bury a
3-line behavioural change in ~145 lines of whitespace. Four claims verified against the code,
including the one that worried me most: a relayed partial notice cannot be contradicted by the run
that failed, because each notice is pushed after its own group's writes and groups are independent.
`/security-review` **ran** (trigger: `cancel-reservation.ts` is on the Money-moving row): 0
findings, having cleared the one genuinely new path — relaying mints crew magic links, so a failed
run now issues auth secrets where it issued none before.

**PR:** [PR #875](https://github.com/mobiustripper42/muster/pull/875)
**Points:** 2
**Branch:** task/766-notice-survives-mid-loop-failure
**Opened at:** 2026-09-01T01:15:00Z

## Task 6: Stop counting changes on the crew banner

**Completed:**
- `src/crewapp/shift-changes.ts` — `changeCount` deleted from `ChangeBanner` and the fold.
- `components/crew/change-banner.tsx` — one heading, always "This shift changed"; the footer's
  "Changed" vs "Latest" conditional gone.
- `src/crewapp/shift-changes.test.ts`, `shift-changes-read.test.ts`,
  `e2e/crew-change-banner.spec.ts` — three count assertions re-pointed at `latestAt`.
- Gate green: `npm run verify` exit 0, 2414 tests — the same total as `main`.

**The operator deleted the feature instead of fixing the bug, and it was the right call.** I had
built the reader-side fix — collapse consecutive identical records before counting — and it worked,
but it needed three existing fixtures taught the difference between a repeat and a duplicate. His
answer: *"what if that line only ever said 'this shift changed' and we stop counting them. you know
sometimes removing something is a solution."* Nothing counts, so nothing can miscount.

**Why the writer-side fixes were all worse.** An advisory lock needs a transaction spanning the
`formShifts` loop, which the in-memory adapter cannot provide — a new port seam for a display bug.
A content-keyed unique index swallows the third row of a genuine add/remove/add, and that sequence
is ordinary: `postgres-repository.ts:1513-1517` re-materializes a cancelled Muster slot **in place**
on resale, so one event id really does go added → removed → added. Collapsing rows one and three
leaves the fold reporting a trip lost that is on the boat.

**Three test rewrites, all challenged and all held.** Changing tests so a change passes is the shape
of bending tests to code, so each went to review by name. The load-bearing one asserts `startBefore`
comes from the 19:00 record rather than the excluded 17:00 one — without it, `latestAt` alone would
pass on a fold that ignored `lastSeenAt` entirely.

**Two corrections to my own claims, both from review.** I said the suite was "down one, a test
merged away" — wrong; the `it()` counts are identical and the 2415 came from the reverted approach.
And the e2e's `toContainText("This shift changed")` had no discriminating power, since it passes
against "changed twice" too; it is now asserted as an absence on a two-record window.

**Also renamed the branch before pushing** — it was `task/766b-count-distinct-changes`, the opposite
of what shipped.

**Code review:** 0 defects; 1 wrong claim of mine and 2 cleanups, all corrected.
`/security-review` **not run** — no trigger. Worth stating rather than leaving silent:
`app/(crew)/crew/shift/[shiftId]/**` is on the Money-computed row and this banner renders there,
but the diff touches a component and the core fold, and a deleted display count reaches no paycheck.

**PR:** [PR #880](https://github.com/mobiustripper42/muster/pull/880)
**Points:** 2
**Branch:** task/766b-remove-the-change-count
**Opened at:** 2026-09-01T03:30:00Z

## Task 7: A boat cannot be saved without saying who sails it

**Completed:**
- `src/admin/vessel-admin.ts` + test — `manning` on the input; refuses an empty rule, a
  non-whole-person count, and a role with no row. Four tests changed, three added.
- `app/(admin)/admin/vessels/page.tsx` — a `CrewRows` field between Capacity and Color; remove
  disabled on the last row.
- `app/(admin)/admin/vessels/actions.ts` — add/remove as form posts carrying an intent, round-
  tripped through the existing draft machinery.
- `components/ui/submit-button.tsx` — a `formNoValidate` prop.
- Gate green: `npm run verify` exit 0, 2418 tests across 165 files.

**The field no surface could set.** The catalog screen excluded `manning` deliberately and
preserved it across edits, on the theory that crew-engine tooling owned it — but that tooling is
two withdrawn stubs that redirect, and `seedFleet` only writes the boats in `RESOURCE_MAP`. A boat
added through the admin was born with an empty rule and nothing short of editing TypeScript could
fix it. Silent until PR #863; since then it throws.

**Code review found a real bug I introduced.** The draft cookie is scoped to the *surface*, not to
a vessel, and `?crew=1` fires on every ordinary Add — so tab 1 adding a row to Brew 1 left a draft
that tab 2, on Brew 2, restored as its own defaults, with the hidden `id` still written from
`selected`. A Save there would have written Brew 1's name and hue onto Brew 2's row. The same
window existed for `?err=` but needed two tabs both mid-refusal; this made it routine. Restore is
now gated on the draft's own `id`.

**⚠️ It crosses a live decision, flagged not assumed.** DEC-ROLE-1 lines 25-27 say **"Do not build
a role-admin UI, custom-manning editor, or multi-role config screen … that arrives with
multi-tenant, well past it."** `CrewRows` is exactly a custom-manning editor. The record is
unamended, its `Revisit if` trigger (multi-tenant) has not fired, and it is **frozen in
`docs/decisions-baseline.txt`** so it cannot be amended in place — recording this properly needs a
new record that revises it. **That is Eric's call and it is open.** What this does *not* do is add
a role-admin UI: role names stay seeded, the picker only offers rows that exist.

**Code review:** 1 bug (fixed), 1 decision conflict (flagged, open), 2 cleanups (done). Confirmed
the test rewrites are not bending tests to code — the two replaced tests were named for the
behaviour #861 reverses. `/security-review` **not run** — no trigger; a crew rule decides who gets
asked to work, not what anyone is paid.

**PR:** [PR #884](https://github.com/mobiustripper42/muster/pull/884)
**Points:** 3
**Branch:** task/861-required-crew-on-the-vessel-screen
**Opened at:** 2026-09-01T06:00:00Z

**Next Steps:**
- **Start with issue #557** — the credential expiry boundary resolves against the **UTC** date while
  the oracle gates on the **vessel-local** trip date. PR #549 narrowed it from a full day to about
  four evening-ET hours and did not close it, and the comment beside it still asserts *"Same rule,
  one boundary."* One `vesselDateOf` call in `credential-health.ts:39-41`, in a function shared with
  SPEC §2.1's roster flag, so it fixes both surfaces. Same defect class as #567: a false invariant
  stated in a comment and a test name is how the original survived its first fix.
- **Two issues were promised today and never filed.** Both are mine to have written and are not
  written: (a) `app/api/cron/tick/route.ts:98` is a bare `await tick(repo, now)` in a file where its
  five neighbours each carry a `catch` + `console.error` — a throw there halts asks and escalations
  fleet-wide with no log line; (b) an admin has to know the engine applies changes on its **next
  run**, not on Save, and nothing says so anywhere. Operator on the second: *"an admin HAS to know
  this to use muster, or they are going to be very frustrated."* It wants both halves — a paragraph
  in operator onboarding, and a line on the surfaces where a save has a pending consequence.
- **DEC-ROLE-1 needs a revising record, and it is open.** PR #884 shipped a per-vessel manning
  editor, which that decision says not to build until multi-tenant. Operator's view: the record
  over-reached (*"kind of a bold crystal ball kind of statement"*). It is frozen in
  `docs/decisions-baseline.txt`, so the fix is a NEW record revising that one clause — the model
  half still stands.
- **`manning` vs "required crew".** `required crew` is now registered in `docs/dictionary.yml` with
  an EMPTY `not` list, because forbidding `manning` lights up 49 existing uses across SPEC and
  decision records, several of them frozen. If the codebase is ever renamed, `manning` joins `not`
  that day and not before.
- **Delete the abandoned branch `task/835-back-trap-outlives-its-guard`** — it carries a rejected
  change, no PR was opened, and branch deletion is denied from a session shell.
- **Fiction I put in merged code (PR #863).** `src/builder/form-shifts.test.ts` seeds a "Duffy
  Rental" crewed by `role-dockhand`. There are no dock-hands and BrewBoat does not own Duffys — the
  role was my invention from a hypothetical. The fixture's real job is "a vessel outside
  `RESOURCE_MAP`"; it should be a captain. Small, and worth doing before someone reads it as fact.
- Carried and untouched: the 432 lines of customer-side design in `the-booking-1.md` /
  `the-living-link-1.md`, and the rest of issue #816's queue.

**Context:**
- **Re-opened at 2026-08-29T02:31Z in a new window** (`/its-alive re-use 99`), rather than opening
  session 101. The `transcript:` field now points at the live JSONL
  (`0d9a3c6e-…`); the original `7c801bf4-…` covers the first window of this same session, from
  `started: 01:35Z`. Nothing had shipped under session 99 when the window changed — no task blocks,
  `pr_numbers: []`.
- **This is lane A**, in the main checkout `/home/eric/muster` on `main`. Session 100 is live and
  concurrent in the linked worktree `/home/eric/muster-s91` and works the `lane:b` label.
- **Session 100's jig/drift notes do not describe this checkout.** That session followed a stale
  skill into `/home/eric/seeds` and reported 14 differing files and an unset `jig-version`. From
  here, `node ../jig/scripts/drift.mjs .` reports `jig-version 6 vs 6, nothing differs`, and
  `settings-policy.mjs --all .` reports both user settings and shared project `current`. Its
  `.claude/settings.json` STALE finding was against `/home/eric/muster-s91/.claude/settings.json`,
  a different file.
- **PR #859 (`task/lane-rule-context`) has merged** — `d4f85a3` on `main`. The collision session 100
  flagged over `.claude/CLAUDE-context.md` is resolved; that file is free to edit.
- Orphan scan clean: no `origin/claude/*` branches, no open PRs.
