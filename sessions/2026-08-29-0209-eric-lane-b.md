---
session: 100
dev: eric
slug: lane-b
branch: task/827-success-confirms
started: 2026-08-29T02:09:26Z
ended: 2026-08-31T13:06:53Z
points: 14
pr_numbers: [862, 867, 869, 871]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-muster-s91/e0f7ff70-6f70-59f0-b17a-8188a55d0c14.jsonl
---

# Session 100 — lane-b

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Measure occupancy in hold minutes, not trip time (DEC-161)

**Completed:**
- Opened `docs/audit/2026-08-29-spec-2.8-conformance.md` — the §2.8/§2.10 conformance audit as a
  **resumable** document, because the audit pauses on every spec flaw it finds and its state cannot
  live in a session. Records three operator decisions, two spec flaws, confirmed defect issue #825,
  and the 24 acceptance criteria unverified. **On its own branch, not this PR** —
  `task/spec-2.8-2.10-conformance-audit`, shipping second so it can reference DEC-161.
- Found spec flaw #1: `docs/SPEC.md` §2.8.3 measured occupancy in trip time. `Offering.holdMinutes`
  (`src/domain/entities.ts:358`) — the boat's whole commitment, turnaround included — is read by no
  occupancy decision. BrewBoat: 100 on the water, 120 committed, so a 13:30 departure reads free
  twenty minutes early. Latent on one offering, live on the second.
- Fixed it in `docs/SPEC.md`: §2.8.3 measures in hold minutes, §2.8.4a freezes both durations and
  derives turnaround, §2.8.1/§2.8.8 store a reserved time and derive expiry, §2.10.2's frozen
  booking carries both, criterion #4 names the 13:30-vs-15:15 case.
- `docs/decisions/DEC-161-occupancy-is-measured-in-hold-minutes-not-trip-time.md` — schema v1,
  1,957 bytes, `amends_spec` on §2.8 and §2.10. Search returned DEC-041 (crew-side); **see also,
  not supersede**, per `hull-busy.ts:33-37`.
- `docs/dictionary.yml` — three terms: hold minutes, trip time, turnaround. `trip length`
  deliberately **not** registered as a forbidden alternate: three frozen legacy records use it.

**Code review:** 3 findings, all addressed in `08ca8db` — bare "hold" reintroduced the ambiguity the
commit exists to remove; a window change distorts historical counting and was unaddressed rather
than settled; DEC-161's "read nowhere" overclaimed against a field that round-trips through the
admin form.
**PR:** [PR #862](https://github.com/mobiustripper42/muster/pull/862)
**Points:** 3
**Branch:** task/dec-161-hold-minutes
**Opened at:** 2026-08-29T04:02:00Z

## Task 2: Spec the operator booking surface (DEC-162, DEC-163)

**Completed:**
- Audited criteria #3, #4 and #5 on `task/spec-2.8-2.10-conformance-audit` (not yet PR'd).
  **#3 BUILT** — the hull-day advisory lock is real (`postgres-repository.ts:1440`), is the
  transaction-scoped variant that survives Neon's PgBouncer endpoint (`docs/DEPLOY.md:28,149`), and
  has a real-Postgres concurrency test that ran. **#4 PARTIAL** — three of four paths measure each
  rival by its own duration; `claim.ts:221` uses the asking offering's (issue #825), and the test
  that reads as coverage creates its rival with the **same** `offeringId`, so it passes identically
  with or without the bug. **#5 NOT MET** — the read path refuses nine things, the write path one;
  `writeSlotBooking` runs inside the Stripe webhook, after the charge, so a refusal there is a
  refund.
- Filed **issue #864** — reservation pause beside the crew pause. Operator-caused change and
  customer-caused change are different problems and only one is code; the settings-shaped gaps in
  criterion #5 are covered by a pause, not by re-validating across the checkout-to-webhook gap.
- Recorded three **spec gaps** in the audit document with the operator decisions attached: the admin
  booking surface, the booking cutoff, and discounts.
- **This PR:** `docs/SPEC.md` §2.10.6 specs the operator booking surface. `DEC-162` (payment link
  over a card field) and `DEC-163` (no expiry, `source: "admin"` as the marker). Two records because
  the byte cap said so and it was right.

**Code review:** 3 findings, all addressed in `f9526c6`. The real one: DEC-163's no-expiry rule
lived only in §2.10.6 and its `amends_spec` named only §2.10, so no banner landed on §2.8 — someone
implementing §2.8.8's sweeper would have hit *"rows that never reached payment can be labelled
freely"*, which describes an unpaid admin booking exactly, and expired it on the first pass. The
spec would have been internally consistent and still shipped the bug.
**PR:** [PR #867](https://github.com/mobiustripper42/muster/pull/867)
**Points:** 3
**Branch:** task/admin-booking-surface
**Opened at:** 2026-08-29T06:05:00Z

## Task 3: Name the frozen money — booking_invoice on our own row (DEC-164)

**Completed:**
- Audited the sales-tax flag and found the real defect beside it. DEC-107 does **not** contradict
  the spec — it is already `status: superseded`, and the booking path does freeze tax
  (`create-departure-payment-intent.ts:133`). But every frozen component goes into **Stripe
  metadata** — eighteen keys — and `booking-webhook.ts:612-619` reads them back with `?? 0`, so a
  dropped key produces a booking recorded with zero tax, zero gratuity or zero service fee,
  silently. That is issue #812 confirmed, plus the specific defect: the defaulting turns a
  transport problem into a wrong number instead of an error.
- `docs/SPEC.md` §2.8.4 now names **`booking_invoice`** — one JSON value on the reservation holding
  every §2.8.4a component in integer cents **and the rate behind each**. Not a column per
  component: add-ons are a list and a discount is coming. Anything with a time in it stays a
  column, which cost nothing — the freeze instant is §2.8.1's reserved time. Durations stay out.
- Cut **DEC-107** to a signpost. It was superseded, still carried its full argument, and was being
  read as live in two places — its title quoted at me as a spec contradiction, and
  `create-departure-payment-intent.ts:11` crediting *"the DEC-107 freeze rule"* for the opposite of
  what it ruled.
- Filed **jig issue #17** — `check:decisions` accepts a retired decision that names no successor —
  then corrected it: check 1 as filed would fail every spec-absorbed record permanently, because
  the thing that replaces a decision here is usually a spec section, not another `DEC-`.
- Registered **booking invoice** in `docs/dictionary.yml`.

**Code review:** 4 findings. Three taken in `299ce25` — a second dangling DEC-107 citation in
`FUTURE_IDEAS.md`, the code comment left as wrong as it was found, and the new term shipping
unregistered. One left for the operator: the reviewer reads DEC-164 as two decisions (where the
money lives, what shape it takes), which is the same read I flagged before the review ran. It sits
at 1,971 of 2,000 bytes with no room to grow.
**Security review:** ran (Money-moving path trigger on `create-departure-payment-intent.ts`), 0
findings — every added line in that file is inside a comment block.
**PR:** [PR #869](https://github.com/mobiustripper42/muster/pull/869)
**Points:** 3
**Branch:** task/booking-invoice
**Opened at:** 2026-08-30T17:05:00Z

## Task 4: Ship the §2.8/§2.10 conformance audit — 5 of 24 criteria

**Completed:**
- **Criterion #1 — PARTIAL.** Grid and season are one predicate, `isOnScheduleGrid`
  (`availability.ts:135-148`), called at the write by `claim.ts:171`. Both negative assertions hold:
  a refusal returns before any hold is written and before the intent is minted, with a test named
  for it. **Already-departed does not exist**, and the real shape of issue #824 is not a crafted
  past date — a 13:30 departure is still on the grid, in season and on the right weekday at 15:00
  the same afternoon.
- **Criterion #2 — PARTIAL.** A row is written before any Stripe call (a `CheckoutHold`, not a
  pending reservation — decision 2 changes which row, not whether). But `availability.ts:334,431`
  key held slots on **exact identity** while `claim.ts:245-248` measures rivals by **overlap**, so
  the calendar advertises the 13:30 the write then refuses. Issue #826 confirmed at both lines —
  and it is the case `hull-busy.ts:20-21` says cannot happen, whose guarantee is real for events
  and empty for holds.
- Corrected criterion #5's refusal table: the season is guarded on the write path too, not
  read-path-only as first written.
- **Merged `main` into the audit branch.** It predated DEC-161/162/163 — built on sibling branches
  for the flaws this audit surfaced — so the document said "FIXED in the spec (DEC-161)" while its
  own `docs/SPEC.md` still read the way the audit describes the bug. Added a **"Resuming this"**
  section telling the next reader to do the same before branching it forward.

**Code review:** 4 findings, all addressed in `06b5cd9`. The stale-tree one above was the one that
mattered. Also: a citation copied from a sibling branch where I had just added nine lines above it
(`:107-112` → `:98-104`, used twice), and an absence claim overstated in exactly the way DEC-161's
first draft was — *"`holdMinutes` is read nowhere"* is false; it round-trips the whole stack and the
claim needed is about occupancy, not existence. That correction had been made on the DEC-161 branch
and never carried back here.
**PR:** [PR #871](https://github.com/mobiustripper42/muster/pull/871)
**Points:** 5
**Branch:** task/spec-2.8-2.10-conformance-audit
**Opened at:** 2026-08-31T15:40:00Z

**Next Steps:**
- **The premise to test:** open a fresh session and try to resume the audit at criterion #6 from
  `docs/audit/2026-08-29-spec-2.8-conformance.md` alone. If it does not brief a stranger, the
  document has failed at the one job it exists for, and that is worth knowing now rather than in
  six weeks.
- 5 of 24 criteria done (#1 PARTIAL, #2 PARTIAL, #3 BUILT, #4 PARTIAL, #5 NOT MET). Next is #6 —
  off-grid, out of season, already departed, with no row written and no payment created. Two thirds
  is already known: `claim.ts:171` does the grid, `availability.ts:385-388` the season, and
  **nothing** does departed (issue #824).
- The audit branch `task/spec-2.8-2.10-conformance-audit` is still unshipped and now carries
  criteria #3/#4/#5, the sales-tax finding and the three-gap list.
- Two spec tasks still queued from the audit's gap list: the **booking cutoff** (system setting in
  `app_settings`, computed not stored, its own slot state, admin books through it) and **discounts**
  (off the fare-plus-extras base, with tax, tip and the service fee all recomputing on the
  discounted base). The hard part of discounts is that a fully-comped booking has no payment id, so
  §2.8.6's `confirmReservation(paymentId)` needs a second entry point.
- **New audit item, not yet written up:** DEC-107 is titled *"Sales tax is read live, not frozen
  onto the booking"* and §2.8.4a freezes every component. That looks like a live contradiction.
- `/kill-this` on `task/spec-2.8-2.10-conformance-audit` — the audit document, carrying criteria
  #3/#4/#5 and the gap list.
- Resume the audit at criterion #3, the hull-day lock: does the write serialize at all.
- Unscheduled: spec flaw #2 — `docs/SPEC.md:1478` claims the payment path is unbuilt. It is not.
- Unscheduled: decision 2 (drop `checkout_holds`, add `pending`/`expired`, nullable `eventId`)
  binds nothing until it has its own decision record. It retires half of DEC-109 and implies a
  migration.
- Code defect issue #825 stands — `claim.ts:203` still measures rivals with the asking offering's
  trip time.

**Context:**
- **The scope conversation at the close, which is the most useful thing in this file.** Phase 12 was
  pokered at **16 tasks / 86 points on 2026-08-18**. It has since closed **87 issues** (50 on
  `phase:12`, 37 on `phase:12b`) with **20 still open**. That is a fivefold expansion, and it was
  predicted in the repo at the time: `docs/design/reservations-admin.md:23-27` records that the plan
  *"carried a ⚠️ admitting the reservation admin surfaces were unspecced and 'likely the larger half
  of P12'"* — and it was pokered anyway. The failure is not drift or complexity. **The estimate was
  made against a scope that did not exist yet.**
- **Why reservations feels unfinishable and crew did not: a denominator.** A state machine has a
  finite number of states and edges, so crew could converge. Reservations has no fixed total —
  every piece of built code discovers spec nobody wrote, and each discovery adds work at the same
  rate it gets done. **The §2.8 audit is the first artifact in reservations with a fixed
  denominator: 24 criteria, which cannot grow.** That is the reason to finish it.
- **It was never specless, which is a narrower diagnosis than it feels like.** `docs/design/` holds
  four documents, one @architect-verified 2026-07-11, plus 162 decisions. The material existed in a
  shape nothing could check code against — design docs nothing pointed at, and decisions carrying
  the *what* because there was no §2.8 to hold it. The verdicts bear this out: five criteria in,
  one BUILT, three PARTIAL, one NOT MET, and most PARTIALs are one wrong input to correct logic.
- **149 of 162 decisions predate the schema**, so they still carry the *what*. Two have been cut to
  signposts. `.claude/CLAUDE-context.md:85` prescribes exactly this — the spec grows, the record
  shrinks — and it has barely started. Each one the spec absorbs is a file that stops being
  something a human has to read.
- **Operator, on reviewing (2026-08-31):** *"we discuss lots of stuff here, and I approve and
  understand what we are doing but then I get a 6000 word document to 'approve'."* The review
  happens in conversation; the document is the record of what was decided and **must not ask for
  approval again**. Most of the length in this session's PR bodies was a "verify by hand" section
  re-asking questions already answered live.
- **The reviewable unit is one criterion, not the audit.** Trying to review five criteria plus two
  spec flaws plus three gaps plus a sales-tax investigation as one artifact does not work at any
  length. Criterion #6 onward ships as its own unit. An earlier argument against this on token cost
  weighed the wrong resource.
- **The pause is the PR boundary.** Work criteria until one turns up something needing a fix, then
  ship what is done. Two branches went stale across other people's merges this session —
  `task/booking-invoice` hit conflicts, and the audit branch asserted a fix its own tree could not
  show. Closing at each pause makes both impossible.
- **`/its-dead`'s outside-`/kill-this` PR check false-positives under concurrent lanes.** It filters
  `gh pr list --author @me` by session start, but both lanes push under one GitHub account, so
  session 99's five PRs (#859, #863, #865, #866, #868) look like hand-shipped work from here. They
  are not. Reported rather than fixed — skills are canonical in jig.
- **Re-opened a second time at 2026-08-29T02:53Z in a third window** (`/its-alive re-use 100`),
  rather than opening session 101. `transcript:` now points at the live JSONL (`e0f7ff70-…`);
  the earlier windows of this same session are `3da0c466-…` (from `started: 02:09Z`) and
  `329d2a7f-…`. Nothing had shipped at either handover — no task blocks, `pr_numbers: []`.
- **The stale-checkout theory below is confirmed and now resolved.** As of 02:53Z this worktree is
  at `d4f85a3`, zero commits ahead of and behind `origin/main`. Re-run from here,
  `node /home/eric/jig/scripts/drift.mjs .` reports `jig-version 6 vs 6, nothing differs` and
  `settings-policy.mjs --all .` reports `Current.` for both user settings and shared project. So
  the 14-differing-files, unset-`jig-version` and `settings.json` STALE findings recorded below
  were all artifacts of the old checkout and **none of them is a real finding**. Left in place
  because the diagnosis is worth keeping, not because the drift is.
- **DB answer from the operator (2026-08-29): `/home/eric/muster-s91/.env.local` is
  `muster_dev` + `muster_test` — the same two databases lane A uses.** The three s91-named databases
  (`muster_s91`, `muster_s91_test`, `muster_test_s91`) exist in the one Postgres container but are
  wired to nothing. This **supersedes** the note below that treated the s91 DBs as this lane's:
  `db:migrate`, `db:seed:*` and `db:reset:test` run here land in **lane A's** databases. Say so before
  running one; a `db:reset:test` is a shared-resource action across both lanes.
  **Re-confirmed 02:53Z** — the operator pasted the two lines again at the third-window re-open.
- **Concurrent with session 99**, which is live in the main checkout `/home/eric/muster` on `main`.
  This session runs in the linked worktree `/home/eric/muster-s91` and works the **`lane:b`** issue
  label; session 99 takes lane A. Keeping conflicts down is the point of the split.
- **The session-anchor branch is stale.** `task/827-success-confirms` merged as PR #833 on
  2026-08-26 and is zero commits ahead of `origin/main`. It is the branch this worktree happened to
  be left on, not a claim about this session's work. Slug is `lane-b` rather than the branch-derived
  `827-success-confirms` deliberately — naming session 100 after finished work would mislead anyone
  grepping for #827 later.
- **One Postgres server, and s91-specific databases exist inside it.** `docker ps` shows exactly
  one muster container — `muster-postgres`, `0.0.0.0:5432` — from the tracked `docker-compose.yml`
  (container name and port are hardcoded there, so a second one cannot run). Ports are therefore
  shared and not separable per worktree. **Databases are separable and someone already separated
  them:** `muster_dev` (46 tables / 55 migrations), `muster_test` (47 / 56), and three s91-named
  ones — `muster_s91` (44 / 54), `muster_s91_test` (44 / 54), `muster_test_s91` (44 / 54). The
  three s91 DBs are real and migrated, one behind. Nothing in the repo references `s91`
  (`grep -rn s91` over tracked files: zero hits), so the wiring is entirely in `.env.local` —
  which is untracked, per-worktree, and **differs between the two checkouts** (`cmp`: DIFFERENT;
  `/home/eric/muster/.env.local` 2729 bytes Aug 28, `/home/eric/muster-s91/.env.local` 2674 bytes
  Aug 21).
- **The earlier note in this file said s91 has no DB of its own. That was wrong** — it was written
  from a paste that read `muster_dev`, and the s91 databases above contradict it. `.env*` is
  denied fleet-wide to Read/Bash, so a session cannot settle this alone: **ask the operator for
  the `DATABASE_URL` / `TEST_DATABASE_URL` database names only** (not the credentials). Until that
  is answered, treat `db:migrate` / `db:seed:*` / `db:reset:test` as *possibly* landing in lane A's
  world and say so before running one.
- **Three s91 database names for one worktree is itself a finding** — `muster_s91_test` and
  `muster_test_s91` are the same idea spelled two ways. Whichever is live, the other two are
  stale and will drift further behind every migration.
- **The templates repo is `jig` (`/home/eric/jig`), not `seeds`.** `.claude/skills/its-alive/SKILL.md`
  still says "seeds" in Steps 8.5/8.6, and session 100 followed it into `/home/eric/seeds` — which
  exists, still has the old `dev/claude/` layout, and answered every check with stale numbers.
  jig's layout is `scripts/` at the root, no `dev/claude/`. That skill is one of the 14 files jig's
  drift check reports as differing here.
- ~~**Lane A has `.claude/CLAUDE-context.md` open in PR #859**~~ — **resolved.** #859 merged as
  `d4f85a3`; that file is free to edit, and the DB/ports facts above can go into it.
- **`.claude/settings.json` here is STALE against jig** — missing two deny entries,
  `Bash(sed -i *)` and `AskUserQuestion`. Fix is
  `node /home/eric/jig/scripts/settings-policy.mjs --write /home/eric/muster-s91/.claude/settings.json`,
  not run — permissions are read at launch, so it would apply to the *next* session, not this one.
  User settings are current. `jig-version` is unset here against jig's 6 — this repo owes a migration.
