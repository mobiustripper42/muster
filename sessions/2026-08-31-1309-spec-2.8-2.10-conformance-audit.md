---
session: 101
slug: spec-2.8-2.10-conformance-audit
branch: task/spec-2.8-2.10-conformance-audit
started: 2026-08-31T13:09:45Z
ended:
points:
pr_numbers: [872, 873, 874, 876, 877, 878, 879, 881, 883, 885, 887, 888, 889, 890, 891, 894, 895]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster-s91/8b0523c8-86e8-5b9a-adcd-0d35a536979e.jsonl
---

# Session 101 — spec-2.8-2.10-conformance-audit

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Audit criterion 6, and stop the citations rotting silently

**Completed:**
- **Criterion #6 — NOT BUILT, and the guarantee it protects holds anyway, UNPROVEN.** The literal
  criterion (`docs/SPEC.md:2057`) wants a `pending` reservation with a null `eventId`; neither exists
  (`entities.ts:615`, `:619`). But `writeSlotBooking` (`write-booking.ts:87`) has exactly **one**
  non-test caller — `booking-webhook.ts:361` — so no `Event` is materialized before the payment
  succeeds, and §2.8.2's real requirement (*"an abandoned checkout therefore materializes nothing"*,
  `:1512`) is met by there being **no row at all**, with the `CheckoutHold` holding the boat.
- **Nothing tests it, and two assertions look like they do.**
  `create-departure-payment-intent.test.ts:94` and `create-departure-checkout.test.ts:83` assert a key
  is absent from the **Stripe metadata payload**, not that an `Event` row is absent. Both would pass
  if checkout materialized an Event and declined to name it to Stripe.
- **Thirteen citations re-baselined.** PR #869 (DEC-164) added a net 23 lines to §2.8; this audit
  merged after it without a conflict, so eleven citations drifted by +23. Two were not drift —
  DEC-161 rewrote the sentences flaw #1 quotes, so those now read as the record of the flaw with the
  live replacement beside them.
- `scripts/check-audit-citations.mjs` — new. Pins a citation as a line **plus the text that must be
  on it**; `MOVED` is a mechanical fix, `GONE` is a stop-and-re-read. Reports its own coverage
  (52 of ~139), because a gate covering a third of a document and printing green is the prose rule
  with a checkmark.
- Gate green: `npm run verify` exit 0, **2412 tests across 165 files**.

**The document's own rule was the thing that failed.** "Resuming this" told the next reader to catch
drifted citations. It was written 2026-08-31 and broken the same day, by a merge that could not
conflict and therefore could not warn. That is the CLAUDE.md pattern verbatim — a prose rule gets
skimmed past, a mechanical check gets obeyed — and it is why the rule is now a script.

**Three of code review's four findings were defects in the gate this task added**, which is a fair
argument against having added it. §2.10's bound overshot into `# 3. Cross-cutting` (`:2545` → `:2539`);
the anchor meant to hold that bound was an empty quote that only proved the file was long enough; and
`check:audit` was wired into `verify`, which the reviewer correctly called a trap — the pinned lines
span ~40% of a document every booking task edits, so an unrelated §2.5 edit would hard-fail the gate
for an author who has never heard of the file. It is now standalone. The fourth finding (coverage
counts occurrences, not distinct locations) was accepted and documented rather than fixed.

**The gate was observed failing three times, none manufactured.** Its `/tmp` draft failed on two of my
own criterion-6 citations, which is the only reason the drift was found. The installed version then
failed `MOVED :1502 → :1501` on an off-by-one in its own manifest.

**I wrote `closes #874` into the PR body and issue #874 does not exist.** Caught immediately and
corrected in the body itself rather than quietly, because a fabricated issue reference in a merged PR
is indistinguishable from a real one. The audit is tracked by issue #816 and no `closes` line belongs
here — 6 of 24 criteria is not a closed tracking issue.

**Scope, recorded because the operator said so plainly.** One criterion was asked for; the citation
gate, the `verify` wiring and the `check:denied` finding were all mine, not his. His words:
*"if everything we check on thing we have to fix something else"* and *"this project is never going to
get finished."* The minimum-slice principle he asked for on 2026-08-29 lives **only in my machine
memory** — it is not in `.claude/CLAUDE-context.md`, which is its own problem.

**Code review:** 4 findings — criterion 6's five factual claims all independently verified by the
reviewer (including the absence claim, which this document has overstated once before); 3 gate defects
fixed in `740589e`, 1 accepted and documented.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script, no path on the money,
auth or migration lists.

**PR:** [PR #872](https://github.com/mobiustripper42/muster/pull/872)
**Points:** 3
**Branch:** task/audit-criterion-6-null-eventid
**Opened at:** 2026-08-31T15:05:00Z

## Task 2: DEC-107 and DEC-124 are withdrawn, not superseded

**Completed:**
- Both records flipped `status: "superseded"` → `"withdrawn"`. Three instances of one word; DEC-124's
  body quotes its own status so it moved too. DEC-107's body left alone — *"Superseded by the spec,
  not by another decision"* is precisely why it carries no `superseded_by`.
- **jig's gate run directly against this tree**: `node /home/eric/jig/scripts/check-decisions.mjs`
  exits 0. Negative control by importing `supersessionProblems` and feeding it the before-state —
  both records fail, after-state returns `[]`. Not manufactured by edit-and-revert.

**Two claims in the operator's brief were stale, both in the safe direction.** The
`create-departure-payment-intent.ts` DEC-107 citation was **already removed** by DEC-164 (PR #869) —
`:19-21` now records that it was cut rather than repointed. That is what killed the brief's exit 1 and
turned a three-way judgment call into one word. And the index does **not** stale on a status change:
`gen:decisions` reported `0 files rewritten` and `docs/DECISIONS.md` is untouched, because the index
carries titles and topics, not status.

**Scope was smaller than feared:** `superseded` appeared exactly twice in 162 records, `withdrawn`
zero times, and no record anywhere carries a `supersedes:` or `superseded_by:` frontmatter key.

**Code review:** skipped at the operator's request.
`/security-review` **not run** — no blast-radius trigger; two markdown files.

**PR:** [PR #873](https://github.com/mobiustripper42/muster/pull/873) — merged
**Points:** 1
**Branch:** task/dec-107-124-supersession
**Opened at:** 2026-08-31T16:20:00Z

## Task 3: Criterion 7, and the spec calls it a trip

**Completed:**
- **Criterion #7 — UNPROVEN.** All four absences hold after an abandoned checkout, and for **one**
  reason rather than four: the checkout path is reads plus a single write. `resolveCustomerId` is
  reachable only from `write-booking.ts:122` and the Xola importer; `ensureBookingCode` needs a
  reservation id that does not exist yet; every sender is downstream of a succeeded PaymentIntent.
  Nothing tests it — the four nearest assertions are about unparseable phone numbers and about the
  slot coming free, which is criterion #8.
- **Deliberately unsettled:** `create-departure-payment-intent.ts:166` sets `receiptEmail`. Whether
  Stripe sends anything for an intent that never succeeds is **Stripe's** behaviour and is not
  readable here. One look at their receipt docs settles it before #7 can be called BUILT.
- **`trip` and `payment window` registered.** A departure is *when* something leaves, not the thing
  that leaves, and §2.8 used the word for both. The four durations now compose:
  trip → trip time + turnaround = hold minutes.
- **37 lines of §2.8/§2.10 swept**, five kept as the time sense. §1.x and §2.6 untouched — crew,
  shipped, different confusion. The 21 decision records using the word left alone: **16 are frozen**
  in `decisions-baseline.txt`, where editing one fails the build.

**Neither new term forbids its alternate, and the reasons are in `dictionary.yml`.** `slot` is the
code's word *and* means a workflow slot at `CLAUDE.md:38` — a different word, in a file this repo does
not own. Bare `window` covers several different windows. So the code's name went **inside** the
definition instead. Also recorded there: `CHECKOUT_HOLD_MINUTES` *is* the payment window while
`hold minutes` means something else entirely — a collision `check:dictionary` can never see, because
it is a constant name and the gate reads prose.

**The operator's correction is what produced this task.** I first argued against sweeping — "59 hand
judgments with nothing to check them." His answer: *"you are at fucking llm, it might be one of the
things you are best at."* He was right; classifying 59 sentences by sense is cheap and it is the work.
The argument I had leaned on (that the spec would be rewritten anyway) was also me mis-reading him —
§2.8 **is** the new spec, already written.

**The citation gate caught my own sweep.** Renaming §2.8.3's heading returned `GONE`, not `MOVED` —
the right signal for a sentence that changed rather than moved. That surfaced five places the audit
quotes SPEC text edited in the same commit, three of them criteria transcriptions in the verdict table
that would otherwise have silently disagreed with the spec they audit.

**PR #874 is the number I fabricated earlier this session** as a non-existent issue in Task 1's PR
body. It now exists as a **pull request**, which is exactly the ambiguity `CLAUDE.md`'s "never write a
bare #N" rule is about. Task 1's note above stands: issue #874 never existed.

**Code review:** 2 findings, both fixed. The sweep itself came back clean — all 37 rewrites and both
low-confidence borderline calls verified. The finding that mattered: §Criterion 7 claimed *"every call
site is inside `booking-webhook.ts`"* and `confirm-booking.ts:48` calls `alertPaidButUnbooked` too.
Verdict unchanged (that path needs a *succeeded* intent), but **it is the overstated-absence mistake
this document's own header records having made once already** — made again, caught by review again,
now pinned in the gate. Second finding: row 20 transcribed "trip length" where SPEC says "hold
minutes, trip time", one page from the dictionary comment added in this same PR warning against that
exact spelling.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #874](https://github.com/mobiustripper42/muster/pull/874)
**Points:** 3
**Branch:** task/audit-criterion-7-abandoned-checkout
**Opened at:** 2026-08-31T17:35:00Z

## Task 4: Criterion 8 — BUILT by the hold, not the pending row

**Completed:**
- **Criterion #8 — BUILT (by the hold, not the pending row).** No sweeper exists: `vercel.json`
  schedules only `tick` and `doorbell-tick`, `xola-pull` runs off an admin button, and none touches
  `CheckoutHold` transitively. Expiry is one comparison made identically in four places
  (`availability.ts:333`, `claim.ts:215`/`:266`, `postgres-repository.ts:1727`,
  `in-memory-repository.ts:763`) — a hold is dead at exactly its `expiresAt`.
- **The proof is the boundary, run against real Postgres.** `repository-contract.ts:1102-1103` asserts
  0 live holds at `12:15:00.000Z` and 1 at `12:14:59.999Z`, against a fixture expiring at 12:15.
  `runRepositoryContract` is invoked for both adapters. **This is the divergence risk criterion #5
  found and could not close** — two implementations of one rule — actually closed by a contract suite.
- **There IS a sweep**, inside `acquireCheckoutHold`, opportunistic on write rather than scheduled.
  Correctness does not depend on it: readers already filter by `expiresAt`, so it reclaims storage,
  not availability.
- Left unpinned and noted: Postgres compares timestamps, in-memory compares ISO strings lexically.
  Agree for `Z`-suffixed millisecond precision, would disagree on `+00:00`. Latent — every `asOf` call
  site is `new Date().toISOString()`.

**The operator caught the verdict before review did.** My first pass wrote a bare `BUILT`. His reply:
*"it's built but not to the spec."* Correct — §2.8.8 says every reader tests `pending AND expiry > now`
and every reader tests `hold.expiresAt > now`. A bare BUILT reads as "nothing to do here", and decision
2 moves all four comparison sites onto a different table and rewrites the boundary test. The qualifier
went into the verdict column rather than widening ⚠ across rows nobody had re-read.

**The ⚠ marker's definition is narrower than its usefulness**, which is worth carrying forward. It
means *written in* `pending`/`expired` vocabulary; it cannot say "will be rebuilt when decision 2
lands". Row 8 is flagged as the first to revisit if a later pass widens the meaning.

**I made an existing off-by-one worse, which is the lesson from this task.** The header said "Nine"
marked ⚠ against an actual eight. I unmarked row 8 and **decremented to "Eight"** instead of counting —
producing the same error the previous author made, by the same method. Seven is right (rows 2, 6, 9,
10, 11, 15, 24). The doc now tells the next person to recount rather than adjust.

**Code review:** 1 finding, fixed — the tally above. I asked the reviewer to assume I had overstated an
absence again (I had, on criterion #7) and go looking. All five substantive claims verified
independently, including both `acquireCheckoutHold` implementations, which I had not read closely and
said so in the brief.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #876](https://github.com/mobiustripper42/muster/pull/876)
**Points:** 2
**Branch:** task/audit-criterion-8-window-passes
**Opened at:** 2026-08-31T18:40:00Z

## Task 5: Criterion 9 — NOT BUILT, and §2.8.8 asks for three mechanisms

**Completed:**
- **Criterion #9 — NOT BUILT.** The row is an `expired` **reservation** (`SPEC.md:1500`, *"A state,
  not a deletion"*) and there are no `pending`/`expired` states. Same reason as #6: the entity does not
  exist.
- **The useful finding is the shape of what is missing.** §2.8.8 asks for **three** mechanisms and one
  is built, on the wrong object: the **clock** (`:1829`) exists on `CheckoutHold` (BUILT under #8); the
  **sweeper** (`:1833`, relabel `pending`→`expired`, never delete) and the **reaper** (`:1841`, rare,
  long horizon, deletes old `expired` rows) do not exist in any form.
- Two rules attached to those jobs that are easy to lose: the sweeper *"never labels a row it cannot
  prove was unpaid"* (`:1836`) — otherwise a webhook outage longer than the payment window turns a
  paying customer into an `expired` row — and an `admin`-sourced pending row is exempt from all of it
  (`:1823-1827`, DEC-163), which a reader must branch on **first**.
- **The reaper's horizon is the retention policy and §2.8 gives no number.** Correct posture for a
  host-env value, but it is undecided, and it is the knob trading table size against how far back the
  operator can ask the question.

**I got this section wrong twice and the operator caught both.** First draft led with *"the row the
spec wants kept is the row issue #713 was written to delete"* — wrong; #713 deletes **holds**, §2.8
removes the holds table outright, different rows in different tables. Second draft claimed §2.8 has no
answer to the unbounded growth #713 closed and that building §2.8 would silently revert it — also
wrong; `:1841-1844` is exactly that answer. **Both errors came from reading §2.8.8's title, "Expiry is
a clock, not a job", and stopping.** It specifies two jobs twenty lines below the title. Those lines
are now pinned individually so the next reader cannot repeat it.

**His ruling needed splitting, not accepting.** *"We do not have a mechanism to check abandoned
checkouts, so that question doesn't make any sense"* is true of the built system and false of the spec.
`:1846-1856` gives the clause its purpose: a payment window that is **too short** is already visible in
refunds and sold-out messages; one that is **too long** is visible *"only if expired rows survive."*
The clause is not a report to build — **it is the justification for retention**, and the thing to
preserve when someone later argues the expired rows are clutter.

**Code review:** 2 findings, both mine, both the same shape — a correction that did not reach
everywhere the claim lived. The summary table's verdict cell still published the framing the body
disavows 400 lines below, which is what a reader sees first. And the no-archive evidence cited
`ls db/migrations/*checkout_hold*` as "three files": it returns two, and the glob does not match the
migration that creates the table, which is named `claim_hold_mutex`. **Third absence claim in this
document to arrive with bad evidence** — the conclusion survived each time, the evidence did not.
Everything else held on an adversarial re-read, including both self-corrections.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #877](https://github.com/mobiustripper42/muster/pull/877)
**Points:** 3
**Branch:** task/audit-criterion-9-expired-survives
**Opened at:** 2026-09-01T00:20:00Z

## Task 6: Criterion 10 — BUILT, and the identity half is structural

**Completed:**
- **Criterion #10 — BUILT (by the hold, not the pending row).** The best-built thing this audit has
  found. Same hold returned on retry (`claim.ts:262-270`), at its original expiry (`:281` comments it,
  `:284` does it), matched on an httpOnly cookie token, never on identity.
- **The identity half is structural, not a guard.** `DepartureHoldRequest` (`claim.ts:120-134`) has no
  email or phone field — the claim path cannot match on typed identity because it never receives it.
  Review traced both live callers and confirmed neither spreads the request object; each constructs it
  field-by-field, so no path exists. Same shape as #7: a property of the path rather than a check.
- **Two conditions the criterion does not ask for and that matter more.** A token is required on
  **both** sides (`:257-259`) or two tokenless sessions match on `undefined === undefined` and share a
  boat. And a reused hold re-applies fit, blocks and hull-busy (`:271-280`) — *"the world can change
  inside a 15-minute hold."*
- Six tests, real code path, assertions matching their names — confirmed by review, which matters in a
  repo with a recorded history of tests named for one thing that call another.

**The history is the evidence, and it is the thing to carry into decision 2.** `holder-token.ts:4-11`
records that the first cut keyed reuse on the buyer's **email**, and `/security-review` killed it with
two exploits: supply a victim's email and you were handed their hold plus a PaymentIntent on the boat
they were buying; add an absurd guest count and the fit-revalidation branch deleted their hold outright.
§2.8.5's *"never the typed email or phone"* is a rule this codebase earned. **The same mistake is
available again** the moment someone wants retries to work for a customer who cleared their cookies.

**The gap is plumbing, not logic.** Nothing imports `mintHolderToken` or `isHolderToken`; nothing
asserts the cookie's `httpOnly`/`secure`/`sameSite`. A refactor could drop `httpOnly` and the suite
stays green.

**Code review: 1 finding, and it is a pattern now.** I wrote that all ten `holderToken` test mentions
were in `claim.test.ts`; they are 5/5 across two files. The conclusion held. But that is the **fourth
absence claim in this document to arrive with faulty evidence**, and it was inside the paragraph
meant to be careful about exactly that. Written into the document as a rule: *run the search again
before writing the sentence that describes it.* Everything else held adversarially — the reviewer was
asked to try to break a BUILT on a security path, on the grounds that a wrong BUILT tells a future
reader there is nothing to check.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #878](https://github.com/mobiustripper42/muster/pull/878)
**Points:** 3
**Branch:** task/audit-criterion-10-token-retry
**Opened at:** 2026-09-01T01:30:00Z

## Task 7: Criterion 11 — UNPROVEN, vacuously

**Completed:**
- **Criterion #11 — UNPROVEN (vacuously).** A *negative* criterion, and a system that does nothing
  satisfies every negative it is asked. Given its own qualifier rather than `BUILT` because a reader
  scanning the table would take BUILT as reassurance, and there is nothing here to be reassured by.
- **Three independent reasons, none of them a guard.** No handler — the adapter recognizes four event
  families and `return null` at `stripe-payment.ts:277` is the only fall-through, and the test double's
  normalizer (`fake-payment.ts:76`) does not recognize it either. Nothing downstream —
  `booking-webhook.ts:142` acks it as `handled: false` and `route.ts:46` returns 200. And nothing to
  expire: `ReservationStatus` is `"booked" | "cancelled"`.
- **Not readable from the repo:** whether Stripe even delivers the event. `route.ts:21` records that an
  earlier design *"rested on a Stripe dashboard nobody can read from the repo (#544)"*. Same discipline
  #7 applied to `receiptEmail`; it does not affect the verdict.

**The forward-looking half is why the criterion exists.** Once the `pending` row lands,
`payment_failed` becomes a tempting handler — the event says the payment failed, the obvious move is to
mark the row `expired`, and that **destroys the row criterion #10's retry needs**. It is §2.8.8's *"the
sweeper never labels a row it cannot prove was unpaid"* (`:1836`) pointed at the webhook instead of the
job. Nothing to fix; one thing not to build.

**This was the first real test of the rule adopted after four bad-evidence absence claims** — write
absence as a *command with its output*, never as a prose count, so there is no number left to get
wrong. **It held.** Code review returned **0 findings**, having re-run both displayed commands verbatim
and then searched well past them: `.tsx` repo-wide, `e2e/`, camelCase `paymentFailed`, kebab
`payment-failed`, and an unfiltered `grep -rl` with no `--include` at all. All empty.

The reviewer also read past every cited line rather than at it — confirming `:277` is the only
fall-through in the whole function, that this is the only webhook route and only `constructEvent` call
site in the tree, and that the two test citations are correctly distinguished. One thing folded in from
the review: naming `fake-payment.ts:76` closes the question of whether the test double could diverge
from production here.

**The gate caught one of my own anchors before it shipped** — `route.ts:21`'s quote spanned a line
break and came back `GONE`. Second line-break split this session caught by the gate rather than a
reader.

**Code review:** 0 findings.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #879](https://github.com/mobiustripper42/muster/pull/879)
**Points:** 2
**Branch:** task/audit-criterion-11-payment-failed
**Opened at:** 2026-09-01T02:15:00Z

## Task 8: Criterion 12 — PARTIAL, and the identity relationship is inverted

**Completed:**
- **Criterion #12 — PARTIAL. 12 of 24, halfway.** The money half is built and tested five ways
  including both failure modes. The identity half cannot be built in the current shape.
- **The finding: §2.8.5's warning does not describe this codebase's version of the problem.** It warns
  that *"one overwritable column loses the first id"*. There is no such column, because **the
  reservation id is derived FROM the payment id** — `confirm-booking.ts:55` → `booking-webhook.ts:279`
  → `:308` → `write-booking.ts:36` (`sha256`). Two PaymentIntents derive two reservation ids by
  construction. A booking payment can never be superseded on one row because there is no one row.
- **Storage is not the constraint.** `Payment` carries `reservationId` and the **balance** path writes
  a second one. (Gratuity does not — `booking-webhook.ts:747`, *"Writes NO `Payment`"*, because it
  would drive `balanceOwedCents` negative. It uses a `Gratuity` row.)
- **The late success is refunded by a wrong road:** different derived id → slot taken by the
  customer's own booking → `lost` → residual-race branch, idempotent refund. Nobody is charged twice,
  but the system cannot tell a rival who lost from the same customer's superseded payment, and tells
  the second one the trip sold out while they were paying. `:503` records that no `Payment` row is
  written at all, so the trace exists only at Stripe.
- **Carry into the plan:** §2.8.6 finds the reservation by **lookup**, not derivation. Dropping the
  hash drops what makes `ON CONFLICT (id)` idempotency work today.

**The review's first finding was better than what I had written, and it is the fifth faulty absence
claim.** I claimed every residual-race test is two different buyers and the supersession case is
untested. Both false: `create-departure-payment-intent.test.ts:356` builds **one** metadata object and
replays it for both events — same customer, same email, same slot, only the PaymentIntent id differs.
That is one buyer with two attempts. So the criterion's case runs on every suite, and what it asserts
is `outcome: "lost"` plus a refund — **it pins the payment failing to resolve to a reservation**, the
behaviour §2.8 replaces. The gap is not coverage; that test gets rewritten rather than added to.

**And it was written one criterion after adopting the rule against exactly this.** The grep for
`supersed` returned nothing and I let a word search stand in for reading the fixtures. The rule was
"write the command and its output"; **the failure mode it does not cover is running a command that
answers a different question than the sentence does.** That is the refinement worth carrying: the
command has to be one whose output would actually falsify the claim.

**Code review:** 2 findings, both mine, both fixed. The structural claim itself survived unchanged —
every link of the chain verified, no live writer of `Reservation` bypasses `reservationIdFor`, and
`already` versus `lost` confirmed to turn on the derived id rather than slot identity, which is the
distinction that decides whether a refund happens at all.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #881](https://github.com/mobiustripper42/muster/pull/881)
**Points:** 3
**Branch:** task/audit-criterion-12-late-payment
**Opened at:** 2026-09-01T03:10:00Z

## Task 9: Criterion 13 — BUILT, unqualified

**Completed:**
- **Criterion #13 — BUILT, unqualified.** The first on a path that takes money, and **the only
  criterion so far that the code cites back**: `app/(public)/book/success/page.tsx:5` opens *"This page
  CONFIRMS the booking (issue #827, SPEC §2.8 criterion 13)"*, and `confirm-booking.test.ts:86` is
  named *"books a succeeded PaymentIntent, given only its id and no webhook at all"*.
- **The redirect is not trusted, and the check is on status not existence.** `stripe-payment.ts:160`
  is `if (pi.status !== "succeeded") return null` — a forged id naming a real-but-unpaid intent books
  nothing. The review checked the adapter rather than the call site; the section had only said
  "resolves against Stripe", which a bare existence check would also have satisfied.
- Five tests, three negative controls, including that a public page cannot trigger a refund or
  sold-out notice (`notifyOnResidualRaceLoss: false`) — compensation stays the signed webhook's.
- **Issue #831 verified rather than repeated:** `postgres-repository.ts:1563-1567` decides the outcome
  with `select 1 … rowCount === 1`, a read **after** the write, so two racing callers can both be told
  `booked`. The duplicate is a second notification, not a second sale — `booking-webhook.ts:453` gates
  `sendConfirmation` on `booked` alone and the money write is insert-only.

**A stacking mistake worth remembering.** I cut this branch off `main` while PR #881 (criterion 12) was
still open. Both edit the same header line and insert before the same anchor, so they would have
conflicted at merge. Rebasing onto the criterion-12 branch then **duplicated lane A's commits** —
`git rebase` replayed the two merged-main commits that were on my branch but not the target, producing
content-identical copies with new SHAs and a PR diff polluted with someone else's work. Fixed by
`reset --hard` to the criterion-12 tip and cherry-picking the single commit. **The lesson: when the
previous task's PR is still open, cut from that branch, not from `main`** — CLAUDE.md says so and I
did not check whether #881 had merged before starting.

**Code review: 1 finding.** My closing sentence — *"a second notification, not a second sale"* — was
an unsourced absence claim, **in a document that adopted a rule against exactly that one criterion
ago**. Sixth instance of the pattern and the first that was not also wrong. Now carries both
citations. Then **the gate caught the reviewer's own line number** (`savePayment` is at `:514`, not
`:517`), which is the first time the citation gate has corrected a reviewer rather than me.

**`check:denied` is red on this branch and it is not this PR's doing** — it flags 15 pre-existing
lines in four files. PR #882 both wired that gate into `verify` and fixed those lines; this branch
stacks below #882, so it has neither. Resolves on merge.

**Code review:** 1 finding, fixed.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #883](https://github.com/mobiustripper42/muster/pull/883) — **stacked on PR #881**, base is
`task/audit-criterion-12-late-payment`. GitHub retargets to `main` when #881 merges.
**Points:** 3
**Branch:** task/audit-criterion-13-success-page
**Opened at:** 2026-09-01T19:20:00Z

## Task 10: Criterion 14 — BUILT, and a silent production failure mode

**Completed:**
- **Criterion #14 — BUILT.** #13's twin: the webhook books without the client, proved by
  `booking-webhook.test.ts:126` calling `processBookingWebhook` alone — no browser, no success page,
  no client anywhere in it — asserting the reservation, the `Payment` with both Stripe ids, and
  `sendConfirmation` fired once.
- **Boundary not claimed:** "at the moment of payment" splits between after-capture (books) and
  mid-3DS (nothing succeeded, so booking nothing is correct). Review confirmed there is no case where
  Stripe captures and the client vanishing prevents the book — the webhook runs off a Stripe event,
  not a redirect.

**The review found the thing this criterion is actually about, and I had missed it.**
`booking-confirmation.ts:31` is `if (process.env.MESSAGING === "false") return` — **before** the
`APP_BASE_URL` check, **before** the channel check, with **nothing logged at any level**, while the
degradation five lines below it does log. `docs/DEPLOY.md:105-113` records the trap (issue #761):
`messagingEnabled()` asks whether the flag is *on*, while this file and two others treat `"false"` as
a hard kill — so **`MESSAGING=0` leaves confirmations on and `MESSAGING=false` silently kills them**.

**That is the one thing that falsifies the criterion's reachability half in production**, and it does
so with no trace: the booking is written, the operator sees a sale, the customer never hears. It sat
five lines above the citations I used. The verdict stands — the criterion asks whether a booking is
*produced* — but the reachability sentence is now scoped to a correctly-configured deploy, and this is
what "correctly configured" is load-bearing for.

Second finding: the test docstring I quoted credits **DEC-122, which is retired** (*"adjudicated DEAD
… see issue #816"*). The best-effort rule it names is real and restated in §2.8; the citation is the
test's, not the audit's. Recorded rather than repointed — a dangling credit inside a test docstring is
its own small finding.

**Code review:** 2 findings, both fixed. Everything else held, including the production wiring traced
end to end (`booking-deps.ts:39` → `sendReservationConfirmation` → `booking-confirmation.ts`) and the
reviewer's judgement that this section's brevity relative to #12/#13 is proportionate trimming rather
than a missing guard.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #885](https://github.com/mobiustripper42/muster/pull/885)
**Points:** 2
**Branch:** task/audit-criterion-14-browser-closed
**Opened at:** 2026-09-01T20:40:00Z

## Task 11: Criterion 15 — NOT BUILT: money taken, no booking, nobody told

**Completed:**
- **Criterion #15 — NOT BUILT, and the most consequential finding in this audit.** Criteria #13 and
  #14 are the two halves of one safety net; this criterion removes both at once, and §2.8.9's
  reconciler exists for exactly that case and is entirely unbuilt.
- **The code says so in its own words, twice.** `confirm-booking.ts:14` and `booking-deps.ts:4` both
  read *"§2.8.6 requires the success page, the webhook and **(later) the reconciler**…"*. Those are
  also the only non-test hits for `reconciler`; `sweeper` has none. Review widened the search to
  `backfill|catchup|poller|repair|sync` — nothing reconciliation-shaped under any alias.
- **What happens:** Stripe captures, nothing books, no alert fires (all 14 `alertPaidButUnbooked` call
  sites are in the two dead paths), the window passes, the hold expires, **the boat goes back on
  sale**. Customer paid, holds a receipt, has no booking, nobody told.
- **Four controls checked and ruled out** — no scheduled job (`vercel.json` runs `tick` and
  `doorbell-tick`; `xola-pull` says *"NO CRON IS ATTACHED, AND NONE IS COMING"*), `/admin/purchases`
  reads our own rows and there is no row to show, the integrity report compares our tables to each
  other, and **`app/b/find/actions.ts:70` filters recovery to `r.event !== undefined`** — so the
  customer cannot rescue themselves either. That last one closes the escape hatch.
- **Split for the rebuild:** §2.8.9's primary path needs `pending` (blocked on decision 2); its
  fallback — Stripe's undelivered-event feed — is **unblocked but not started**: `stripe-payment.ts`
  has no event-listing capability at all, only `getSucceededPaymentIntent` for a single id.

**The reviewer was asked to break the alarm, not confirm it** — a false alarm here sends someone to
build a reconciler under emergency pressure, or teaches the operator to distrust a safe system. It
survived every angle, and the hunt for compensating controls turned up **three more absences that
strengthen the finding**. Those are now in the section; the audit had understated its own case.

**Code review: 1 finding — the section's only unsourced number.** I wrote "sixteen call sites"; it is
fourteen (13 + 1). Every other absence claim in the section carried a printed command and that one did
not, which is exactly the bar this document set after six faulty absence claims. **The rule held where
it was applied and failed where it was not**, which is the cleanest evidence yet that the rule is the
thing doing the work.

**The gate then caught all three of the reviewer's line numbers** (`:9`→`:10`, `:104`→`:105`,
`:60`→`:70`) and its assertion refused to write over a duplicate manifest entry. **Second time this
session the citation gate has corrected a reviewer rather than me.**

**Proportion checked rather than asserted:** criterion #5's live residual is the already-departed check
(issue #824), since settings-shaped refusals are covered by the operator pause; criterion #12 already
refunds correctly and never double-charges. Neither loses a customer's money.

**Code review:** 1 finding, fixed.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #887](https://github.com/mobiustripper42/muster/pull/887)
**Points:** 3
**Branch:** task/audit-criterion-15-reconciler
**Opened at:** 2026-09-01T21:40:00Z

## Task 12: Criterion 16 — BUILT, and the review corrected the mechanism

**Completed:**
- **Criterion #16 — BUILT. 16 of 24, two-thirds.** The verdict held; the *explanation* was wrong in a
  way worth having corrected.
- **Two legs never re-attempt the write; one is re-attempted and rejected.** `write-booking.ts:94`
  returns `already` on the deterministic reservation id, **before** `eventIdForSlot` at `:96` and
  before `saveBookingIfSlotFree` at `:140`. So on a sequential re-confirm the reservation and the
  Event are never written a second time — the writes are not attempted. The **payment** is the
  opposite: `booking-webhook.ts:393` calls `recordPayment` on `booked` **and** `already`, so the
  insert genuinely runs every repeat and is rejected by `postgres-repository.ts:1279`.
- **Why that distinction matters:** the two short-circuited legs are protected by **control flow**,
  which does not hold under concurrency. The payment's guarantee is a **database constraint**, which
  does. `postgres-repository.ts:1480`'s `on conflict do nothing` on the Event insert is therefore a
  backstop for concurrent races (issue #831, criterion #13) that this criterion's own scenario never
  exercises.
- **No single test does the criterion** — the pieces sit at 2, 3 and 1 repeats respectively, and the
  Event count is never checked after a repeat. Closed by construction rather than coverage, which is
  why BUILT rather than UNPROVEN.

**My first draft said "one mechanism, applied three times." It was wrong**, and the reviewer's version
is more useful than mine because it says which failure mode each leg actually resists. Three findings,
all fixed: the wrong operative line, the over-uniform framing, and a payment row citing the in-memory
adapter while the Event row cited Postgres.

**The document already knew the load-bearing line and I did not connect to it.** Criterion #13 names
`write-booking.ts:92-94` as the TOCTOU race window. The same three lines are the sequential idempotence
guarantee. One reading is the hazard, the other is the protection — same code, two criteria apart.

**Every count claim came back clean this time**, which is the first task where the standing concern
about faulty count claims did not reproduce at all.

**Code review:** 3 findings, all fixed.
`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #888](https://github.com/mobiustripper42/muster/pull/888)
**Points:** 3
**Branch:** task/audit-criterion-16-idempotent-confirm
**Opened at:** 2026-09-01T22:30:00Z

## Task 13: Criterion 17 — BUILT unqualified, and the code's own trigger count is wrong

**Completed:**
- **Criterion #17 — BUILT, unqualified. 17 of 24.** All three claims — a shift for the vessel-day,
  seats on it, and the already-committed crew told — are built, tested, and one has a negative
  control. The best-covered criterion in this audit.
- **The first criterion that crosses into the crew engine, and the crossing is one object wide.**
  The booking writes an `Event`; `formShifts` reads events and derives everything else. Nothing in
  `src/reservations/` knows what a Seat is.
- **Both confirm paths get it structurally.** `confirm-booking.ts:54` delegates to the same
  `processBookingCharge`, so the success page forms shifts too, and both routes assemble deps from
  the single `bookingDeps()` at `app/lib/booking-deps.ts:29` — which exists precisely because *"it is
  defeated just as thoroughly by two callers assembling different dependencies"* (`:4-11`).
- **I walked the notify chain past the stubbed relay**, since a stub proves the call and not the
  notice: `form-shifts.ts:525` → `booking-webhook.ts:440` → `form-notices.ts:41-53` → `channel.ts:93`
  (send) and `:94` (app banner), with the mapping pinned on its own at `form-notices.test.ts:61`.
- **The negative control is what makes this criterion strong.** `booking-webhook.test.ts:654` — *"a
  booking that creates a BRAND-NEW shift still tells nobody"* — and its comment at `:657` says why:
  *"Without this, 'fires on a booking' and 'fires on every booking' look identical."* Worth carrying
  forward as a pattern: every other criterion tests only that the thing happens.

**Separate finding, filed inside the section: the code's "only two `formShifts` triggers" claim is
false.** It appears five times — `booking-webhook.ts:91`, `:421`, `:430`, and twice in the tests. The
grep returns **six callers**; the cutover removes one (`xola-pull.ts:182`), leaving five, because the
cancel path and the split/merge commands are not imports. `channel.ts:101` already says *"there are
six callers"*, so the repo states both numbers about the same set. And **DEC-126 does not rule what
the comment attributes to it** — its holding is one sentence about the cutover, and the record says
the eight further rulings including *"when the pull stops"* are *"gone rather than amended"* (`:29-33`).
It doesn't move the verdict; it matters because the count is the argument for `notifyTripChanges:
true`, and a reader would use it to scope a change to "just these two callers." Fix is three comments
and a test docstring — **not done, this branch is read-only.** Worth an issue.

**Both review findings were mine and both were overclaims of the same shape** — repeating a count I
had read rather than run. The second: I wrote *"empty manning cannot be saved"*, but
`vessel-admin.ts:81` covers only the admin screen; `crew-admin.ts:36` checks `m.count < 1` and never
`manning.length === 0`, and `.some()` is vacuously false on an empty array. The guarantee stands on
the **second** guard — `derive.ts:83-84` throwing — not the first.

**`check:audit` then caught two more, both mine.** `booking-webhook.ts:421` MOVED (my substring was
from `:430`; both lines make the claim), and `channel.ts` "six callers" GONE — **split across the
`:101`/`:102` line break, the fourth time this session.** Noted in the manifest at the pin. The
checker has now caught a line-break split four times and two of my own wrong line numbers; it has
paid for itself several times over and is still deliberately out of `verify`.

**Code review:** 2 findings, both fixed. `/security-review` **not run** — no blast-radius trigger;
docs plus a dev script, and `docs/` is explicitly a non-trigger in the project table.

**PR:** [PR #889](https://github.com/mobiustripper42/muster/pull/889)
**Points:** 3
**Branch:** task/audit-criterion-17-shift-formed
**Opened at:** 2026-09-01T23:15:00Z

## Task 14: Criterion 18 — BUILT, and the first clean code review of the session

**Completed:**
- **Criterion #18 — BUILT. 18 of 24.** Both halves. The second is covered by a test whose title is
  almost the criterion verbatim: `repository-contract.ts:1613` — *"resurrecting a cancelled slot
  re-freezes price and duration IDENTICALLY on both adapters (#616)"*, asserting `capacity` is the
  new `8` rather than the dead `12`, with `price`/`durationMinutes` coming back **absent** rather than
  inherited.
- **The hazard is sharper than "the rebook fails", and that is the section's real content.** Without
  the resurrect the slot is bricked permanently and the two halves of the system disagree:
  `availability.ts:346` admits only `scheduled` events to `eventBySlot`, so a cancelled row drops out
  of the materialized branch and the slot returns to the **virtual** grid at the offering's current
  price — while `events_muster_slot_identity` (`20260718142705_claim_hold_mutex.sql:28-30`) is
  **status-agnostic**, so the cancelled row still owns the identity and the write returns `lost`
  forever. Customer sees it, buys it, money taken, no booking, every time.
- **Two carve-outs both adapters state independently:** `dock` is never re-written
  (`postgres-repository.ts:1506-1508`, `in-memory-repository.ts:636`) because it belongs to the berth
  and not the booking; and the re-freeze is scoped to `status='cancelled'` (`:1517`) so a losing
  concurrent booking cannot overwrite the winner's frozen price.
- **A stale comment found while verifying, and written into the section.**
  `postgres-repository.test.ts:506-510` says the in-memory double *"cannot show this, and would
  actively hide it"* via an accidental resurrect-by-overwrite. That shape is gone —
  `in-memory-repository.ts:638-643` finds the cancelled row on purpose and `:657-660` deletes
  price/duration before re-setting only when defined. Its own comment at `:628` says *"here the same
  case used to be handled BY ACCIDENT."* The surrounding sentence still holds (no partial unique index
  in a Map, so the bricked slot cannot manifest there). Same class as #17's trigger count: true when
  written, now a record of the fix it caused. **Not fixed — this branch is read-only.**

**Zero code-review findings — the first clean pass of the session.** The reviewer was asked to break
the verdict on six fronts and confirmed each, notably that both legs of the hazard trace end-to-end
through `/book` and `claim.ts` with **no intervening guard**, and that `charge.key` is the Stripe
session/intent id so a rebook can never reuse an idempotency key. The reduction argument — nothing
exercises book→cancel→confirm, but confirming reduces to `writeSlotBooking`, which is covered against
real Postgres at `postgres-repository.test.ts:571` — was checked and held, so BUILT rather than
UNPROVEN.

**`check:audit` still caught one of mine** (`in-memory-repository.ts:637` → `:636`) on a section
written *after* a clean review. Worth noting: the review and the checker catch different things and
neither substitutes for the other.

**Process note: the session was closed mid-task and reopened.** `/its-dead` ran on 101 (and on 99)
while criterion 18 was built but unshipped, stamping `ended: 2026-09-02T01:47:25Z` and `points: 34`.
Reopened at the operator's instruction, clearing `ended:` and `points:` as well as the status — a
stamped end date on an open session reads as fact later. `/its-dead` re-stamps both at the real close.
Lane A has since opened session 102 (`2026-09-02-0156-main`), so two sessions are open again and
`/kill-this` will ask which; **this one is 101.**

**Code review:** 0 findings. `/security-review` **not run** — no blast-radius trigger; docs plus a dev
script, and `docs/` is explicitly a non-trigger in the project table.

**PR:** [PR #890](https://github.com/mobiustripper42/muster/pull/890)
**Points:** 3
**Branch:** task/audit-criterion-18-rebook-cancelled-slot
**Opened at:** 2026-09-02T02:20:00Z

## Task 15: Criterion 19 — PARTIAL, and the criterion's own test plan is the part that is missing

**Completed:**
- **Criterion #19 — PARTIAL. 19 of 24.** Five sub-claims, four different answers: *every active admin*
  **BUILT**, *naming the amount* **PARTIAL**, *never dropped silently* **BUILT**, *make the alert throw
  and assert the webhook still succeeds* **NOT BUILT**, *SMS unconfigured leaves only a log line*
  **UNPROVEN**.
- **This is the only criterion in §2.8 that spells out its own test plan, and the two things it says to
  assert are the two nothing asserts.** The delivery half is the best-tested money code in the repo —
  `forward-money-alert.test.ts` pins the fan-out with a deactivated admin excluded (`:51`), one dead
  number not muting the rest (`:80`), nobody-to-tell returning 0 rather than throwing (`:96`), and a
  repo outage swallowed (`:106`).
- **(d) is structural, not merely untested.** Of the **14** `alertPaidButUnbooked` call sites across
  `booking-webhook.ts` and `confirm-booking.ts`, exactly **one** guards against the alert throwing
  (`:333`'s `.catch`). Thirteen await it bare, including `:536`, which sits inside a `catch` block and
  would throw out of it. A throwing alert reaches `app/api/webhooks/stripe/route.ts:57` → **500** →
  the Stripe retry storm the criterion forbids in its own words. Both numbers produced by a command
  quoted in the section, not eyeballed.
- **The guarantee exists but lives at the edge**: `alertMoneyProblem` (`app/lib/alert.ts:57-69`)
  catches internally and is documented as never throwing. It is held by the one adapter that happens
  to be wired, not by the function the criterion is about, and **nothing asserts either half** —
  `app/lib/alert.ts` has no test file. A core assuming a property of its injected dependency, an
  adapter providing it, no check that the two still agree.

**Separate finding: a fixture that says "verbatim" and never was.**
`forward-money-alert.test.ts:19` calls its `REAL_ALERT` *"Verbatim from `booking-webhook.ts` — the copy
that actually ships."* The shipped string (`booking-webhook.ts:654-655`) has *"The ledger is
unchanged;"* and a parenthetical the fixture lacks. The reviewer ran `git log -S` on the introducing
commit and confirmed **it was never true**, not drift. It matters because this fixture is the repo's
stated defence of the #777 ASCII rule — and a hand-copied literal cannot catch a character introduced
into the original.

**Code review found the same defect this document keeps finding in the code.** My branch table
enumerated **five** call sites fitting "a booking charge matching no reservation" and missed two
(`booking-webhook.ts:177`, `confirm-booking.ts:48` — unrecognised `purpose`, neither naming the
amount). The exclusion was arguable and I never made the argument, I just asserted five. Now seven
rows with a *self-identifies as a booking?* column and the reasoning visible, which strengthens (b):
**2 of 7** name the amount, not 2 of 5.

**The operator corrected a deployment claim before this shipped.** I had written *"Production is
nonetheless safe, by the edge."* Reservations are **not live** — nothing in this document may assert
deployment state. Replaced, and the section now carries an explicit line: *"No claim here about what
is deployed… 'would 500' throughout means would, when this runs, not does today."* The two
pre-existing `production` mentions (`:327` PgBouncer from `DEPLOY.md`, `:1058` already scoped to *"a
correctly-configured deploy"*) were checked and left — neither claims reservations are running.

**Code review:** 1 finding, applied. `/security-review` **not run** — no blast-radius trigger; docs
plus a dev script.

**PR:** [PR #891](https://github.com/mobiustripper42/muster/pull/891)
**Points:** 3
**Branch:** task/audit-criterion-19-unmatched-charge-alert
**Opened at:** 2026-09-02T02:55:00Z

## Task 16: Criterion 20 — PARTIAL, and the section was wrong once before it was right

**Completed:**
- **Criterion #20 — PARTIAL. 20 of 24.** The charge is protected on all four fields; the occupancy is
  not. `write-booking.ts:118` reads `offering.tripLengthMinutes` **live at confirm**, four lines after
  the request-carried price, so an offering edit inside the checkout window lands on the Event's
  `durationMinutes` — which is exactly what `hull-busy.ts:92` measures occupancy by.
- **Run, not reasoned.** A throwaway probe (offering 100 → edited to 180 → `writeSlotBooking`) failed
  `expected 180 to be 100`. Deleted rather than committed; the output is quoted in the section and is
  the evidence for the verdict.
- **The test that should cover this proves the other window.** `write-booking.test.ts:109` is named
  *"FROZEN, not resolved on read: editing the offering later leaves the booked event alone"* and its
  edit lands **after** the booking (`:119`, after `:115`). It reads as covering both windows and covers
  one — which is how the gap stayed invisible.

**The section reversed itself mid-task, and the operator is why.** First draft concluded that every
field here is operator-caused, so the whole criterion belongs to issue #864's reservation pause and
the fix is a screen, not code. Eric asked *"I thought we were going to create a pause bookings to solve
this?"* — I had missed #864 entirely, found it, over-corrected into "the answer is the pause", and he
then said he did not want a code guarantee. **Searching the spec to write that amendment is what found
the opposite:** §2.8.4 step 3 (`SPEC.md:1566-1567`) already says *"freeze the money, the hold minutes
and the trip time onto it"*, and `:1575-1576` names this exact case. DEC-161: *"Both durations freeze
onto the reservation."* So no amendment was needed and no new guarantee is being invented.

**The distinction worth carrying forward**, now written into the section as a table:
**re-validating settings at confirm** (grid, season, blocks, fit across the gap — complex, audience of
one, #864's pause replaces it, criterion #5) versus **freezing a value at checkout-start** (one more
field in a write that already happens, §2.8.4 step 3, criterion #20). The pause answers *whether a sale
should still be allowed*; the freeze answers *which number the customer was shown*. Only the first
needs a human in the loop. I collapsed these and it produced a confidently wrong recommendation twice.

**Why the code reads it late:** there is nowhere earlier to put it. Decision 2's `pending` row does not
exist, so the only row that can carry a frozen duration is the `Event`, written at confirm;
`CheckoutHold` (`entities.ts:727-745`) has no duration field. **A symptom of the missing row.**

**Issue #864 got two comments** — the window-list scope addition (Eric's design: pausing lists the
in-flight checkouts and when each expires; the window self-drains inside `HOLD_MINUTES`, and
`listLiveCheckoutHolds` already exists so it needs no new state), then a correction saying the
trip-time case belongs to the freeze rather than the pause.

**Code review: 2 findings, both applied.** One was mine and is the session's recurring class —
**a quoted grep result that belonged to a different command.** The section claimed one hit in
`asks/lean.test.ts`; the reviewer ran the written command and got nothing. Both true: the command I
actually ran also included `mid-flight`, which produced the hit, and that term was dropped in
transcription while its output was kept. Logged in the document rather than silently fixed, since it
sat in the ~129 unpinned prose citations. The second finding added **DEC-164** — the price freeze's
moment is right and its mechanism (*"not in Stripe metadata"*, *"Rejected: leaving it at Stripe"*) is
one that record rejects. Folding it in improved the argument: **DEC-164 moves the money onto the
reservation, DEC-161 puts both durations on the same row, decision 2 creates that row — one piece of
work, not three**, and criterion 20 goes green as a side effect.

`/security-review` **not run** — no blast-radius trigger; docs plus a dev script.

**PR:** [PR #894](https://github.com/mobiustripper42/muster/pull/894)
**Points:** 3
**Branch:** task/audit-criterion-20-offering-edit-mid-checkout
**Opened at:** 2026-09-02T14:40:00Z

## Task 17: Criterion 21 — BUILT, and "dormant" was the wrong word for it

**Completed:**
- **Criterion #21 — BUILT. 21 of 24.** A balance payment cannot book, at **three depths**: the
  dispatch returns at `booking-webhook.ts:174`, nine lines before `processBookingCharge` at `:183`;
  `recordBalancePayment` (`:785-853`) contains no reservation write or confirmation at all, with every
  such call in the file sitting at line ≤567; and a balance session that lost its `purpose` still
  fails `isSlotBooking` (`:297`) because it carries `reservationId` rather than vessel/date/time.
- **Both confirm doors tested, and genuinely different tests.** Checkout session →
  `booking-webhook.test.ts:366` asserts one reservation not two. PaymentIntent →
  `confirm-booking.test.ts:169`, the sharper one: `/book/success` is *also* the hosted-checkout
  landing for the balance flow, and every hosted session has a metadata-less intent behind it that
  succeeds. Reviewer swept `src/`, `app/` and `scripts/` for a third door — none.
- **"Never confirms" is by construction, disclosed as such.** No test asserts `confirm` was not called
  on the balance path; `sendConfirmation` lives at exactly one place (`:460`) inside the function the
  balance branch returns before. Read BUILT on criterion #16's precedent.

**The finding worth keeping: "dormant" invites the wrong maintenance instinct.** This is not dead code
awaiting deletion. It is a complete, tested path held shut by **two** layers — and one of its guards
exists precisely because the path *does* wake up after a refund or a chargeback re-opens the balance.

**Code review corrected my account of those layers, and it was a real defect.** I wrote *"the button
is there today"* and named the server guard (`no_balance`) as the reason the path is dormant. Both
wrong. The form is conditional (`reservation-detail-pane.tsx:516-521`) on `money.balanceCents > 0`
plus four more predicates — *"Shown only when money is actually owed — a 'collect balance' button on a
settled booking is a trap"* (`:495-496`) — so in full-charge mode **the button does not render at
all**. The pane is the layer the operator meets; `create-balance-checkout.ts:52` is the backstop for a
stale tab or re-post, and `:51` says it covers full mode explicitly. Corrected inline with the
correction stated rather than quietly applied.

**One refinement I made to the reviewer's finding.** They cited `create-balance-checkout.ts:59-60` —
*"The pane's button is a courtesy; this is the thing that makes minting the charge impossible"* — as
the code contradicting me. Reading the block, that sentence introduces the **disputed** guard at
`:62`, not `no_balance` at `:52`, so it is not evidence about this case. Both lines are now pinned so
the distinction cannot quietly re-collapse. *(Same class as the errors this session keeps making in
the other direction — attributing a comment to the wrong line — so worth catching in a reviewer too.)*

**`check:audit` found a new failure mode: case.** A pin failed on `the pane's` vs `The pane's`. Until
now the tool had only ever caught line-break splits (six this session, one of them in this task at
`booking-webhook.ts:813`) and wrong line numbers.

**Code review:** 1 finding, applied. `/security-review` **not run** — no blast-radius trigger; docs
plus a dev script.

**PR:** [PR #895](https://github.com/mobiustripper42/muster/pull/895)
**Points:** 3
**Branch:** task/audit-criterion-21-balance-never-books
**Opened at:** 2026-09-02T23:20:00Z

**Next Steps:**

---

### Standing rules this session learned the hard way — read these first

Written 2026-09-01 before a deliberate compaction, so a summarized reader picks up where this one left
off instead of rediscovering. Each of these cost at least one review finding.

1. **An absence or count claim is written as the command plus its output, never as prose.** *"Nothing
   imports X"* becomes ``$ grep -rln X --include=*.test.ts .`` / ``(no output)``. Seven claims in this
   document arrived with faulty or unsourced evidence; five were wrong. **The refinement, learned on
   criterion #12: the command must be one whose output would actually falsify the sentence.** A grep
   for a *word* does not test a claim about *fixtures* — that is how "every residual-race test is two
   different buyers" got written when the fixture replays one buyer's metadata twice.
2. **Check the previous criterion's PR has merged before cutting the next branch.** Two audit branches
   editing one document always conflict — both edit the header count and insert before the same
   anchor. Criterion #13 was cut off `main` while #12 was open; the rebase to fix it **duplicated lane
   A's commits** into the history and polluted the PR diff. If it happens again: `reset --hard` to the
   previous task's branch tip and cherry-pick the single commit, do not rebase.
3. **The audit document is the durable artifact, not the session.** Its header says so. Every finding
   must land in `docs/audit/2026-08-29-spec-2.8-conformance.md`, never only in a task block or a PR
   body. This was tested cold at this session's open and it worked.
4. **Run `npm run check:audit` before trusting any line number**, and after every edit. It has caught
   line-break-split anchors three times, an off-by-one in its own manifest, a duplicate entry, and —
   twice — a reviewer's wrong line number. It is not in `verify` deliberately.
5. **Read the whole subsection, not to where the citations stop.** Two wrong drafts of criterion #9
   came from reading §2.8.8's title — *"Expiry is a clock, not a job"* — and stopping. It specifies
   two jobs twenty lines below the title.
6. **Ask the reviewer to break the verdict, not confirm it** — and say why it matters. A wrong `BUILT`
   on a money path tells a future reader there is nothing to check; a wrong alarm sends someone to
   build under emergency pressure. Naming the stake in the review prompt has produced the session's
   best findings.
7. **A verdict qualifier goes in the table cell, not only the section.** The table is what a reader
   sees first. `BUILT (by the hold, not the pending row)` for #8/#10; row 9's cell shipped once still
   asserting a framing the body spent a paragraph disavowing.

### The work

- **Audit is 16 of 24.** Next is criterion #17 — confirming produces a shift for that vessel-day, with
  seats, and notifies crew already committed to it. **Not ⚠.** This is the first criterion that
  crosses into the **crew engine**, so it is the one where the two halves of the product meet:
  `booking-webhook.ts:396` already says *"Form the shift the booking just earned (#614)"*, and
  `booking-webhook.test.ts:563-569` asserts one Event **and** one Shift on the right boat and day.
  Expect BUILT; the interesting half will be the crew-notification leg.
- **An open question for #16 the operator may want to answer:** BUILT with no single covering test is
  a judgement call. The section says so plainly. If it should read UNPROVEN until a three-repeat test
  exists, that is a one-word change.
- ~~**Audit is 15 of 24.** Next is criterion #16~~ **DONE, Task 12.** — confirming the same payment three times produces one
  booking, one Event, one payment record. **Not ⚠.** Expect BUILT: §2.8.6's idempotence is already
  exercised from two directions (`confirm-booking.test.ts:103`, `booking-webhook.test.ts:152`), and
  criterion #12 established that `reservationIdFor` is a deterministic hash with `ON CONFLICT (id)`
  behind it — which is precisely the mechanism this criterion tests.
- **Criterion #15 deserves an issue of its own.** The reconciler is unbuilt, specified in full at
  §2.8.9, and the fallback half is unblocked today. That is a filable task independent of the rebuild,
  and it is the one finding in this audit where the cost of not acting is a customer's money.
- **DONE — filed as [issue #886](https://github.com/mobiustripper42/muster/issues/886)**, spec only;
  `documentation` / `reservation` / `points:3` / `priority:high`. Engine, surface and emitters are
  explicitly out of scope, and the decision record is `DEC-TBA` to be resolved at write time.

  **Why it is not too late, which was the operator's real question.** Reservations is about to be
  rebuilt — decision 2 replaces `checkout_holds`, criterion #12 says the reservation-id derivation must
  change, criterion #9 says two of §2.8.8's three mechanisms do not exist. That code is being rewritten
  either way, so a requirement specified now lands **before** the code that must satisfy it. This is
  the last moment where it is not a bolt-on. And the crew engine is the proof that late still works:
  `audit_events` arrived at issue #400 long after phases 1–10 shipped, and `audit-trail.ts` derives a
  clean unified view from two logs never designed to feed it. Being late costs **backfill**, not
  usefulness.

  **The crew audit trail is not in `docs/SPEC.md` at all** — `DEC-118`, `audit_events`,
  `reliability_events` and `#400` each return 0 hits. It shipped unspecified, which is why "§2.8 or
  §2.10" is a decision rather than a lookup. Recommended §2.10: the events are generated in §2.8 but
  the artifact is an operator surface.

  **The unresolved fork, and it is the one that shapes the thing.** The crew analogue keys on
  `crew_member_id`, so the obvious analogue is `reservation_id` — **which loses exactly the events that
  motivated the request.** Three findings from this audit have no reservation to key on: an abandoned
  checkout (#9, the hold is deleted by the next acquire), a superseded payment (#12,
  `booking-webhook.ts:503` — *"no reservation to reference"*), and a confirmation killed by
  `MESSAGING=false` with no log at any level (#14). **Keying on the slot** — vessel + date + time —
  catches all three, because the slot exists before any row and outlives every row. Operator's call.

  **The operator's build shape, adopted with one change:** architect → engine and output → each event
  its own task. The change is that **the event list belongs in the architecture task, not discovered
  per-task** — emitters can land incrementally, the list cannot. A trail missing events you enumerated
  is honest; a trail missing events nobody thought of is one you cannot trust, which is criterion #9's
  problem in a new place.

  Two things added beyond the conversation: the spec must state there is **no backfill** and what the
  surface says instead (the crew precedent is *"audit begins &lt;ship date&gt;"*), and **"audit" already
  means something else in this repo** — `docs/audit/`, these conformance audits — so the term needs a
  dictionary entry before the word carries two jobs.

- ~~**QUEUED — spec a booking audit trail.** Operator, 2026-09-01: *"similar to the audit trail that we
  have for crew events. we need to have a similar booking audit trail. so we can follow what happened
  to a reservation. we need to get this into the spec sooner than later."* The crew analogue is
  `audit_events` (`db/migrations/0024_audit_events.sql`, #400, DEC-118) — an operator-facing,
  append-only trail of every crew add/drop/change with an actor dimension, deliberately kept separate
  from `reliability_events` because an admin removal is not the subject's behaviour and must never
  feed the scorer. **Where it goes in the spec is an open question — §2.8 or §2.10 — and is the first
  thing to settle.** Criterion #9 already found the adjacent gap: today an abandoned checkout leaves
  no trace at all, and `booking-webhook.ts:503` records that a losing or superseded payment writes no
  `Payment` row, so the only record that money moved and came back is at Stripe.~~ **Superseded by the
  block above — filed as issue #886.**
- ~~**Merge PR #881 before #883.**~~ Both merged. #883 is based on #881's branch; merging out of order is not possible,
  but merging #881 is what collapses #883's diff to a single criterion.
- **Audit is 13 of 24.** Next is criterion #14 — closing the browser at the moment of payment still
  books, via the webhook. **Not ⚠.** It is #13's twin from the other side: #13 proved the page books
  without the webhook, #14 asks whether the webhook books without the page. Expect BUILT; the whole
  `booking-webhook.ts` spine exists for it, and `confirm-booking.test.ts:103` (*"the webhook landing
  afterwards produces no second booking"*) already covers the two arriving together.
- **Check whether the previous criterion's PR has merged before cutting the next branch.** Two audit
  branches editing one document always conflict.
- ~~**Audit is 12 of 24 — halfway.** Next is criterion #13~~ **DONE, Task 9.**: killing the webhook entirely still books a
  customer who reaches the success page. **Not ⚠.** Located: `confirm-booking.ts` exists precisely
  because *"the SUCCESS PAGE runs it too (issue #827, SPEC §2.8.6): one idempotent confirm, called
  from both"* (`booking-webhook.ts:164-167`), and `confirmBookingByPaymentIntent` is the success-page
  entry point. Expect this one to be genuinely built.
- **The absence rule needs its refinement written down when #13 ships:** the command must be one whose
  output would falsify the sentence. A grep for a *word* does not test a claim about *fixtures*.
- ~~**Audit is 11 of 24.** Next is criterion #12~~ **DONE, Task 8.** — a superseded payment that succeeds late still resolves
  to its reservation and is refunded rather than silently dropped. **Not marked ⚠**, so it can return a
  clean verdict. §2.8.5 is the subsection (`:1730-1733`): *"A reservation has many payment ids over its
  life, not one… one overwritable column loses the first id and turns a late success into an
  unrecognisable charge."* So the first thing to check is whether payment ids are stored one-per-row or
  one-per-reservation.
- **The absence rule held on its first test and should keep being applied.** Every absence claim gets
  written as the command plus its output. Four prior violations, all right-conclusion/wrong-evidence,
  all caught by review rather than a gate.
- ~~**Audit is 10 of 24.** Next is criterion #11~~ **DONE, Task 7.**: a
  `payment_intent.payment_failed` does **not** expire the reservation. **Marked ⚠.** Related to #10 —
  the same declined-card story from the webhook's side rather than the retry's — so `booking-webhook.ts`
  is where to start, and the question is whether a failure event mutates anything at all.
- **The four bad-evidence absence claims are the session's real finding about method.** Every one had
  the right conclusion and wrong supporting evidence, and every one was caught by review rather than
  by a gate. The document now carries the rule; whether it holds is the thing to watch on #11.
- ~~**Audit is 9 of 24.** Next is criterion #10~~ **DONE, Task 6.** — a declined card retried on the same trip reuses the
  same reservation at its original reserved time, matched by the **cookie token**, not by the email or
  phone typed into the form. **Marked ⚠.** Located already: `holderToken` on `CheckoutHold`
  (`entities.ts:758`, issue #575) is the built analogue, `claim.ts:410` is named *"does NOT extend the
  expiry — a retry cannot park a boat indefinitely"* and `:513` *"an EXPIRED hold of the same buyer is
  not reused"*, so retry behaviour has real coverage. The open question is whether matching is by
  possession only.
- **Read the whole subsection before verdicting.** Two wrong drafts of #9 came from stopping at a
  §2.8.8 heading. §2.8.5 (`:1724`) is the one #10 depends on.
- ~~**Audit is 8 of 24.** Next is criterion #9~~ **DONE, Task 5.** — an abandoned checkout survives on disk as an `expired`
  reservation carrying slot, party size and reserved time, so *"how many checkouts were started and
  walked away from last month"* is a query. **Marked ⚠.** Criterion #7 already established the artifact
  it will be measured against: today an abandoned checkout leaves a `CheckoutHold` row, and #8
  established that nothing deletes it on a schedule — so the row survives, and the open question is
  whether it carries the fields #9 asks for.
- ~~**Audit is 7 of 24.** Next is criterion #8~~ **DONE, Task 4.** — the trip comes free the instant the payment window
  passes, **with the sweeper stopped**. Two thirds is already located: expiry is lazy and read-side
  (DEC-109), pinned by `availability.test.ts:587` and `claim.test.ts:357`, both of which this task
  read while verdicting #7. What is unchecked is whether anything sweeps at all.
- **The plan, in the operator's words (2026-08-31):** finish the audit, which may rewrite parts of the
  spec; then use the spec to make a project plan; then build reservations using the audit to size it.
  **The audit's ejection point is 24 criteria verdicted and nothing else built** — the thing Phase 12
  never had. Phase 1 had one: *"one real BrewBoat weekend runs import → auto-form → lock → asks →
  crew tap in → crew open a card."*
- **Verdicts are not converging**, so the audit is still earning its cost: seven in, five distinct
  values (PARTIAL ×3, BUILT, NOT MET, NOT BUILT, UNPROVEN). If they start coming back identical, stop
  — the remaining ones would be predictable and not worth reading.
- ~~**QUEUED, after criterion #6 — fix DEC-107 and DEC-124~~ **DONE, Task 2.** before jig's `check:decisions` change is
  synced.** Brief handed over by the operator 2026-08-31, from the jig side. jig has added a
  `supersessionProblems` gate to `scripts/check-decisions.mjs`; this repo's copy lacks it, which is
  the single DRIFT line `drift.mjs` reports here. The new gate fails a record marked
  `status: superseded` that names no successor. Both of these are that shape — retired with nothing
  replacing them, wearing `superseded`'s label. Claims below are the operator's brief and are to be
  **re-verified against the tree at execution time**, not trusted as read.

  - **Neither has a successor.** Nothing in `docs/decisions/*.md` declares `supersedes: ["DEC-107"]`
    or `["DEC-124"]`, so the fix is not "find the replacement".
  - **DEC-124 — unambiguous, one word.** `status: "superseded"` → `status: "withdrawn"`. Its own body
    (`DEC-124-tips-come-into-muster-as-collect-and-expose-xola-tip.md:33-35`) already says it was
    retired for overreaching — it claimed the apparatus moves into Muster when Xola retires, which was
    never decided (issue #837, still open). The body's sentence *"which is why the status above says
    superseded"* needs the same word changed, or the record contradicts its own frontmatter.
  - **DEC-107 — needs a person; three exits, operator's call.** The record's ruling is that sales tax
    is read **live, not frozen** (`DEC-107-…:8`), while
    `src/reservations/create-departure-payment-intent.ts:11-12` cites it as *"the DEC-107 freeze
    rule"* — the opposite of its holding, from a record marked retired. Filename and title disagree:
    the file is `DEC-107-payments-stripe-hosted-checkout-deposit-balance.md`, the title is *"Sales tax
    is read live, not frozen onto the booking"*. That reads like the file was rewritten in place onto
    a new subject keeping the old id, and possibly kept the old subject's `superseded` status with it.
    The only record referencing it, DEC-134, is itself retired **and legacy** (no `schema:` key), so
    it never enters the map the gate checks and cannot be the successor.
    1. *The live-read ruling still holds* → `status: "active"`. Then the
       `create-departure-payment-intent.ts:11-12` comment is citing a live record for the opposite of
       its holding — a separate and worse bug than the gate finding.
    2. *Genuinely replaced* → write the successor with `supersedes: ["DEC-107"]` **and** set
       `superseded_by` on DEC-107 to match. Both halves, or the new gate fails it again.
    3. *Retired with nothing replacing it* → `status: "withdrawn"`, same as DEC-124.

    Operator's read: exit 1 is the one to check first, given the filename/title mismatch — but that
    needs a read of muster's code they had not done, and it is muster's call.
  - **Mechanics.** Fix both records *before* syncing the jig change, so the sync lands green. After
    editing either file run `npm run gen:decisions` — the index goes stale on a status change and
    `check:decisions` fails on that separately.
  - **The gate's actual demand**, worth keeping when the exits blur: a retired record either names a
    live successor that names it back, or admits nothing replaced it.

**Context:**

- **Session 99 is live and concurrent**, in the main checkout `/home/eric/muster` — confirmed by the
  operator at this session's open, not assumed. This session is the linked worktree
  `/home/eric/muster-s91`. Lane split as in session 100.
- **The session-anchor branch had already shipped when this session opened.**
  `task/spec-2.8-2.10-conformance-audit` merged as PR #871 at 13:06:18Z, three minutes before
  `started:`. It is zero commits ahead of `origin/main` — the branch this worktree was left on, not a
  claim about unshipped work.
- **Session 100's Next Steps misdescribe criterion #6.** It reads *"Next is #6 — off-grid, out of
  season, already departed"*, which is criterion **#1**'s text verbatim and already verdicted PARTIAL.
  The audit document's own table is right: #6 is `docs/SPEC.md:2034`, the pending reservation's null
  `eventId`. Trust the table over the handoff note.
- **`db:migrate` / `db:seed:*` / `db:reset:test` run here land in lane A's databases**
  (`muster_dev` / `muster_test`), per the operator on 2026-08-29. Unchanged. Say so before running one.
- **Compacted deliberately at 2026-09-01 after Task 12**, at the operator's call, rather than closing
  and reopening. Reasoning kept because it will come up again: compaction happens on its own at some
  threshold, so the real choice is closing *before* it or letting it happen. Every finding already
  lives in its task block and in the audit document, and `/retro` reads `pr_numbers` per session — one
  session with fifteen-plus tasks is a cleaner velocity point than two halves of one run. **The
  standing-rules block at the top of Next Steps was written for exactly this moment**; read it first.
- **Two things this session established about the crew engine, which #17 onward will need.**
  `booking-webhook.ts:396` already carries *"Form the shift the booking just earned (#614)"*, and
  `booking-webhook.test.ts:563-569` asserts one Event **and** one Shift on the right boat and day. So
  the reservations→crew seam exists and is tested on the happy path; the untested half is likely the
  crew-notification leg.
- **The reservations→crew boundary is one object wide: the `Event`.** Shifts are formed from
  `eventIds`; the crew manifest reads reservations *by event*; `writeSlotBooking` is the only place
  reservations reaches across. That was established while explaining Events to the operator and it is
  the fact that makes "remove reservations and start over" tractable, if that ever comes back up.
- **The operator's plan, in his words (2026-09-01):** finish the audit, which may rewrite parts of the
  spec; then use the spec to make a project plan; then build reservations using the audit to size it.
  **The audit's ejection point is 24 criteria verdicted and nothing else built** — the thing Phase 12
  never had. Verdicts are still not converging (sixteen in, six distinct values), so the audit is
  still earning its cost; if they start coming back identical, stop.
- Drift: one file, `scripts/check-decisions.mjs`, behind jig's `supersessionProblems` gate — which is
  the subject of the queued task above. `settings-policy --all` reports `Current.` for both levels.
- Orphan scan clean: no `origin/claude/*` branches; every branch with commits has a merged PR.
