---
session: 102
slug: main
branch: main
started: 2026-09-02T01:56:00Z
ended:
points:
pr_numbers: [893, 896, 903, 906, 911, 922, 929, 931, 938, 939, 940, 943]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/bae44ca0-e136-549f-b55d-3f851e79a616.jsonl
---

# Session 102 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: The engine tick's own call had no catch

**Completed:**
- `app/api/cron/tick/route.ts` — a named catch around `await tick(repo, now)` that logs the leg and
  the `shiftsFormed` count, then **rethrows**. The 500 is the point: a tick that fired no asks is not
  a successful run, and a 200 would read as green to anything watching status. Deliberately not the
  best-effort shape of the two legs below it, which are best-effort because `tick` already committed.
- Issue #892 filed first, then built. It was promised in session 99 and never written.
- Gate green: `npm run verify` exit 0, **2418 tests across 165 files**.

**No test, deliberately, and the precedent is six-for-six.** None of the six existing catches in this
file is covered — no test in the repo asserts any of their messages. The repo has zero `vi.mock` uses
across `src`, `app` and `components`; it tests by injection, and this route has no seam. Covering one
log line would have introduced module mocking as a new pattern. Operator's call, made explicitly.

**I mispriced this twice, in both directions.** First at 3 points, on a test harness that turned out
not to be needed. Then downward, by turning "the At-Risk board almost never happens" into "the ask
engine barely runs" — which the operator never said and which is false. His words: *"the ask engine is
alive and well … when it's used it's like fucking magic."* Rare-and-valuable argues for *more*
attribution in the logs, not less, because nobody is watching on the nights it matters.

**Two issues closed without code, not part of this PR.** #557 (credential expiry UTC skew) was already
fixed by `6d0bff8` on 2026-07-28 — `vesselDateOf` is at `credential-health.ts:48`, below the comment
block the issue cited, which is why it read as open. #556 (At-Risk row's copy points at disabled
buttons) closed as not-planned on the operator's call: the At-Risk surface is a removal candidate, and
the disabled Reschedule/Cancel buttons belong to Muster Reservations when "by phone for now" ends.

**Code review:** 1 bug (fixed in `2ff77b8`), 1 cleanup (no action). The first draft logged *"no asks or
escalations fired this run"* — a guarantee `tick()` cannot make. No transaction wraps its per-shift
loop (`src/ports/repository.ts:9`), and `widenAsk`/`escalate` persist as they go while `firedAsks` is
assembled only at the end, so a throw on shift 50 of 100 leaves 49 shifts' asks committed, unrelayed,
and un-re-widened by the next tick. Message now hedges. The cleanup verified the rethrow is safe —
`vercel.json:8` re-fires regardless of status and nothing consumes the response.
`/security-review` **not run** — no blast-radius trigger; one route file, no money, auth or migration path.

**PR:** [PR #893](https://github.com/mobiustripper42/muster/pull/893)
**Points:** 2
**Branch:** task/892-tick-catch
**Opened at:** 2026-09-02T19:22:30Z

## Task 2: An open required seat is an obligation, not an absence

**Completed:**
- `components/admin/seat-pips.tsx` — an open required pip goes from `border-line bg-bg text-muted` to
  `border-2 border-ink bg-bg text-ink`. Open and filled now differ by inverted contrast, which survives
  grayscale; no hue carries meaning.
- `components/assignment/seat-card.tsx` — the cockpit's local `RoleGlyph` rendered filled
  **unconditionally**, so an OPEN captain seat showed a solid blue "C" beside its own `OPEN` badge. It
  now takes `filled={vm.state === "Confirmed"}` — the board's rule verbatim (`all-shifts.ts:209`).
  Operator's call: the same seat must not read as crewed on one surface and open on the other.
- `components/ui/role-glyph.tsx` — docstring only. Left functionally alone: its one caller renders
  `card.coCrew`, so every glyph there is filled by construction and there is no open state to add.
- Gate green: `npm run verify` exit 0 twice, **2418 tests across 165 files**.

**The issue's own proposed fix was forbidden by the decision it cited.** #598 said to use the At-Risk
board's gap ink. DEC-042 guardrail 2 — *"State renders as neutral ink, not colour. Warm/bad tokens stay
reserved for the At-Risk board"* — says otherwise, restated verbatim in all four amendments, and the
record is frozen in `docs/decisions-baseline.txt:51`. So the fix had to be unmissable in neutral ink or
not at all. The operator's reaction to that constraint: *"I LOVE the fact we are going to try to do this
without red!!"*

**Why this task at all — the design inverted and this is the surface that won.** At-Risk was specced as
the only screen, everything else automatic. What actually happens is the operator scans the shifts board
constantly and At-Risk is empty — *which is the ask engine working*, not neglect. His framing:
putting the last-minute case on a screen for a human is terrible, and that is exactly when the automated
ask should fire. So the investment belongs where the work happens.

**DEC-086 is now stale in one clause and cannot be fixed in place.** Line 47 says *"(open pips stay light
outline grey so gaps jump)"* — false against this branch. It is frozen (`decisions-baseline.txt:95`), so
correcting it needs a NEW record. Deliberately not written yet: the density question below may still
change what it would say. Flagged in the PR body rather than left for a reader to trust.

**No test, and it is not an oversight.** The repo has no jsdom and no testing-library; `vitest.config.ts`
runs `environment: "node"` and the two `components/**` tests cover pure view-model functions. Nothing here
can render JSX, so a className is not assertable without adding a rendering stack for one commit.
`e2e/shifts-view.spec.ts:157-158` asserts the pips by `title` attribute, untouched, so existing coverage
still passes and still means what it meant.

**Code review:** `@code-review` clean — verified the two `filled` predicates are the identical expression,
that border-box keeps the 18px square, and that `role-hue.ts`'s literal-class rule is respected.
`@ui-reviewer` found one real defect, fixed in `9ceb894`: filled pips carried a 1px transparent border
against open's 2px, so the two initials would have sat at different optical sizes in the same row. It said
keep `border-ink` over `border-muted` (muted is what the old pip used, and the premise is that muted reads
as absent), and confirmed the hierarchy inversion is intended. **One finding is open and belongs to the
operator's eye:** the reviewer had no server and never saw a dense board, where `border-ink` at 15.81:1
across twelve rows might read as the scoreboard DEC-042 exists to prevent. If it does, the fix is
`border` at the same ink, not a lighter colour.
`/security-review` **not run** — no blast-radius trigger; three component files, no money, auth or migration path.

**Process note.** I held this PR back waiting on that eyeball. Wrong call — the review is advisory and the
PR is where looking happens. The operator had to ask why it wasn't opened.

**PR:** [PR #896](https://github.com/mobiustripper42/muster/pull/896)
**Points:** 2
**Branch:** task/598-unmissable-open-seats
**Opened at:** 2026-09-03T12:26:09Z

## Task 3: A bare `catch {}` is the only place that knows why, and it threw it away

**Completed:**
- `eslint.config.mjs` — a `no-restricted-syntax` selector `CatchClause[param=null]` on `app/**` +
  `components/**`. Added FIRST and watched failing at **109 sites**.
- `app/lib/swallowed.ts` + `.test.ts` — `logSwallowed(surface, e, consequence?)`, the error passed as
  its own `console.error` argument so the stack survives, and a `try` around the log itself because it
  runs inside a catch. Plus `CREW_UNAVAILABLE` and `ADMIN_LOG_HINT`.
- 99 sites bound and logged; 10 exempted with an inline disable naming why the throw is expected input.
- Crew copy unified to the operator's wording; admin surfaces keep their noun and point at the log.
- Gate green: `npm run verify` exit 0 twice, **2423 tests across 166 files** (was 2418/165).

**The rule found 3× what a hand grep did, and that is the finding.** Issue #854 said 38 sites, from
`grep "} catch {" app/(crew)/ app/(admin)/ --include="*.tsx"`. The rule said 109: the grep saw only
page components and missed every `actions.ts`, route handler and `app/lib` helper — including
`admin/shift/[shiftId]/actions.ts` (11), the Stripe refund paths (9), and the payroll clock. Re-estimated
**3 → 8** and put it to the operator before building rather than tripling the task quietly. His answer:
*"one pass … we should have been doing this from the rip."*

**The worst sites were not the ones the issue described.** The issue's example rendered a visible notice.
The genuinely silent ones are the best-effort blocks: `admin/shift/[shiftId]/actions.ts:93` drops an audit
row with no banner anywhere; `admin/shifts/actions.ts:47` swallows a form-transition relay that a split
has already CONSUMED, so that crew member is never told, ever; `lib/xola.ts:54` is the same shape on the
Xola pull.

**Two real defects in my own work, both caught by a tool rather than by reading.** (1) An
`eslint-disable-next-line` with a multi-line comment after it disables the wrong line — 10 exemptions
silently covered nothing, and `--report-unused-disable-directives` is what said so. (2) A first attempt
to document the ceiling used a no-op selector `Program:not(:has(Program))`, which matches every file and
would have failed lint everywhere; caught before running.

**Flagged, not fixed:** `app/lib/auth.ts:101` evaluates `signSession(renewed, secret())` inside the try,
so a bad `SESSION_SECRET` is swallowed as the ordinary read-only-render case and silently stops session
renewals. Hoisting it changes what escapes `readSubject()` on every request. Comment at the site.

**Code review:** `@code-review` 3 findings, 1 actionable and addressed in `1b91f1e`: the `app/**` scope
was undocumented, leaving **23 bare catches in `src/`/`db/`** — three on the money path
(`src/reservations/booking-webhook.ts:461,546,605`) — that a reader would assume were covered. `src/` is
framework-free (DEC-013/DEC-020) and cannot import `app/lib/swallowed.ts`, so it needs a decision, not a
wider glob → **issue #902**, and the ceiling is now written in both `eslint.config.mjs` and the
`swallowed.ts` docstring. Two findings were pre-existing cleanup in files this branch touched; left alone.
`/security-review` **ran** (money-computed + auth triggers hit) — **0 findings**: the caught error reaches
nothing but `logSwallowed`, the magic-link secret is hashed before the throwing call, no Stripe key or
booking code is logged, `/api/health`'s response body is unchanged, and the one interpolated value is a
strict boolean.

**Test plan carries three hand scenarios**, at the operator's explicit request — break the database and
read the terminal. Scenario B is the founding bug reproduced by *renaming* `shift_changes`: `/api/health`
stays **green** while `/crew` is broken, which is the sheepdog blind spot in one screenshot.

**PR:** [PR #903](https://github.com/mobiustripper42/muster/pull/903)
**Points:** 8
**Branch:** task/854-log-swallowed-page-errors
**Opened at:** 2026-09-03T19:34:00Z

## Task 4: The operator's name is Eric, not a nickname the docs kept repeating

**Completed:**
- Prose everywhere — `docs/`, `.claude/` context, design mockups, code comments in `src/`/`app/`/`db/`.
- `crew-spink` → **`crew-eric-stoffer`**, the id `db/migrations/0018_admins.sql:31` actually seeds.
- Tests and e2e swept too, including the e2e admin handle across `e2e/fixtures.ts` and ~38 specs.
- **117 files, 391 insertions / 391 deletions** — symmetric, which is what a rename looks like.
- Gate exit 0 four times, **2423 tests across 166 files**, identical to main.

**Three bugs fixed on the way, none of them the point.** (1) `?admin=spink` was printed as *the sign-in
step* in `docs/RUNNING.md`, `docs/AUTH.md`, `docs/DEPLOY.md` and three seed scripts, and has returned a
400 since `0018_admins.sql` made the handle a unique row (DEC-092). (2) `app/lib/operator.ts:28`'s
`OPERATOR_CREW_MEMBER_ID` fallback was `crew-spink`, which matches **no row in any migration** — and that
constant is an exclusion filter (`admin/shift/[shiftId]/actions.ts:122`, `admin/shifts/actions.ts:107`),
so the operator was being notified about his own actions and office messages carried a dangling
`senderId`. (3) The e2e fixture seeded handle `spink` while `db:seed:crew` seeds `eric`.

**I got the id wrong first and caught it by checking.** The first commit mapped `crew-spink` → `crew-eric`
— an id that exists nowhere, AND one already in use on `main` as an example in `db/crew.ts:9` and 53 lines
of `src/crew/crew-cli.test.ts`. So the rename quietly merged the operator persona into an unrelated
fixture identity. The real id is in `0018_admins.sql:31` and named outright by DEC-072. A first correction
pass then over-reached and renamed those pre-existing `crew-eric` occurrences too; reverted, and redone
scoped to files that held `crew-spink` on `main`. The eight pre-existing files show an empty diff.

**Two occurrences survive and cannot move:** `docs/SPEC.md:1201`, regenerated verbatim by `gen:decisions`
from frozen DEC-061, and `docs/decisions/DEC-058`, which names `crew-spink` twice and is frozen at
`docs/decisions-baseline.txt`. Operator: *"all the places it's frozen is fine."*

**`gen:decisions` had to be run mid-task.** Editing `docs/SPEC.md` broke `check:decisions` with *"declared
spec amendments have not landed"* — the frozen records pin exact spec text, which is the coupling that
makes SPEC.md prose partly load-bearing. Worth knowing before anyone edits that file again.

**Code review:** `@code-review` ran twice, once per commit, **0 findings both times** — verified the
pre-existing `crew-eric` survived, that `crew-eric-stoffer` agrees across the operator constant, the seed
that creates the row, both adapter contract suites and the e2e fixture, and that all ~150 `signInAsAdmin`
call sites moved together. `/security-review` **ran** (money-computed + auth triggers) — **0 findings**,
and proved the rename mechanically: it normalized every added and removed line under the substitutions and
compared multisets, getting **0 unexplained lines across all 117 files**, which also rules out a weakened
assertion hiding in the 38 e2e specs. It was that pass that found bug (2) above.

**Stacked, then unstacked.** The branch was cut off `task/854-log-swallowed-page-errors`. After PR #903
squash-merged, `main` was **merged in** (not rebased — DEC/CLAUDE.md forbids rebasing a pushed branch) to
bring the PR diff down from 177 files to 117. One conflict, in
`app/(admin)/admin/shift/[shiftId]/actions.ts` — and it was the rename itself, mine vs main's stale copy.

**PR:** [PR #906](https://github.com/mobiustripper42/muster/pull/906)
**Points:** 3
**Branch:** task/spink-to-eric-prose
**Opened at:** 2026-09-03T20:12:00Z

## Task 5: Nine rules were on out of 875 installed

**Completed:**
- Measured every rule the five plugins ship. **347 in the `recommended` presets; 297 fire zero and are
  now on at `error`.** No source file changed — the code already satisfied them.
- `eslint.config.mjs` — a per-plugin `recommended` spread, an `OFF` map carrying all 50 firing rules
  with counts and owning issue, five scoped preset blocks placed FIRST (flat config is last-wins).
- **DEC-166** written, superseding DEC-159's preset ban. `@eslint/js` promoted to a direct devDependency.
- Gate exit 0, 2423 tests / 166 files, 164 decisions.

**The measurement is the whole task, and the first one was wrong.** Applying playwright's rules
repo-wide produced **4,464 findings in `src/`**, a directory with no Playwright tests. Scoping each
plugin the way `eslint.config.mjs` already scopes it took the total from 5,606 to **574**. The raw
`eslint:all` number was 83,553 and is meaningless — its top entries are `one-var`, `sort-keys`,
`capitalized-comments`, `no-ternary`.

**Proved the rules apply rather than trusting a green run.** A config enabling 297 rules that silently
applies none looks identical to one that works. `--print-config` before/after: `at-risk-board.ts`
**3 → 248**, `admin/shifts/page.tsx` **4 → 261**, `e2e/all-shifts.spec.ts` **1 → 26**.

**I told the operator the wrong thing about DEC-159 and `@code-review` caught it.** I said DEC-159
governed and needed no record — quoting its clause about the rule *list* changing. `DEC-159:29` is a
different clause: *"Cherry-pick rules by name; never adopt a plugin's preset,"* on the ground that a
major bump silently expands the gate. Measuring today's rules does not answer that. Operator's ruling:
*"disagree with 159. we need to supersede that… if a bunch of shit breaks on a major upgrade, then we
change it."* DEC-166 accepts the risk rather than mitigating it. DEC-159 is frozen and NOT edited —
`check-decisions.mjs:360-363` sanctions superseding a frozen legacy record without touching it.

**Two more real review findings.** The headline said 298; it is **297** (347 − 50), verified by
recomputing both sets. And `react-hooks/exhaustive-deps` went ON at `error` via the preset while the
comment eight lines below still said it was *"deliberately NOT here"* — false in the same commit that
wrote it. It satisfies DEC-159 rather than dodging it: #757 rejected running it at `warn`, and that ban
is untouched.

**Cost, surfaced as a decision rather than banked:** lint **6.8s → 25.6s, +18.8s** — within a second of
the +19s DEC-159 rejected for type-aware rules. Different trade: that bought zero enforced rules, this
buys 297 including four security rules that had never executed here. Operator: *"lint would be worth
minute. approved"*.

**Code review:** `@code-review` **3 findings, all 3 addressed** — the missing DEC, the count, the stale
comment. It also independently verified the hand-picked `no-unused-vars` options survive, both
core-purity bans are intact, the five exemption files still resolve `off`, and `recommended()` escalates
preset `warn` entries to `error`. `/security-review` **not run** — no blast-radius trigger; config and
docs only, no source file in the diff.

**Also filed this task:** issues #908 and #909 for the 50 off rules, and **jig issue #24** for a comment
checker — ESLint checks code and ignores prose, the `check:*` family checks prose and ignores code, and
nothing checks a comment. `?admin=spink` sat broken in three seed scripts for months in exactly that gap.

**PR:** [PR #911](https://github.com/mobiustripper42/muster/pull/911)
**Points:** 5
**Branch:** task/907-turn-on-the-clean-lint-rules
**Opened at:** 2026-09-04T00:40:00Z

## Task 6: The 43 lint rules that fired — one real defect, and a rule that would have made things worse

**Completed:**
- All **57 security-rule findings read individually**. None is a defect — that is the finding.
- **23 copies of one trailing-slash regex** → `src/config/base-url.ts`, a linear loop, 7 tests written
  first and watched failing. The overload preserves `undefined` for the six `?.replace(…)` sites.
- 8 dead bindings removed, one assertion-less test strengthened, TS-aware rules widened to full width.
- Every one of the 43 rules ends ON with findings fixed, or OFF with a measured count and a reason.
- Gate exit 0, **2430 tests across 167 files** (was 2423/166).

**A real defect, and the SECOND time this exact bug has been in this file.** `db/xola-report.ts`
declared `experiences`, a Set, under a docstring promising it *"reports the evidence … more than one
means the flat base is a guess and says so."* Populated on every item, read nowhere — that warning has
never printed. `--base` applies one included-guest number to every boat, so a multi-product pull makes
`pax` and every OVER flag wrong in the UNDER-reporting direction, silently. The comment **four lines
above the fix** describes the identical bug being fixed in #757: the wire-shape canary that "was
incremented and never read". Caught by a rule this time instead of a reviewer.

**A rule that would have introduced a bug.** `sonarjs/no-inverted-boolean-check` flagged
`!(Date.parse(m.createdAt) <= lastNotifiedMs)` at `doorbell-decider.ts:255`. On NaN every comparison is
false, so the inverted form is TRUE and the code rings — "fail toward ringing", as its own comment says.
The rule's `x > y` is FALSE on NaN and would silently skip the notification.

**The 23 copies were invisible to the gate.** `sonarjs/no-identical-functions` is per-file, so 23
identical expressions across 21 files produced zero findings. A *security* rule surfaced a maintenance
defect — which is an argument for breadth over precision when choosing what to turn on.

**Two judgement calls, both "off" rather than forced.** The playwright idiom rules (21 findings, all
read, none a defect): a wrongly-rewritten e2e assertion still passes and now asserts something else, and
the config already records two such incidents. `react-hooks` (6 real-looking findings): no React
unit-test layer, so every fix would be a blind edit to interactive code verifiable only on a device.
Both left off with counts written down. **react-hooks wants its own task.**

**Directive placement bit me three times in one branch** — an `eslint-disable-next-line` followed by
more comment lines disables the line after the comment, not the code. Only `--report-unused-disable-directives`
catches it. Same mistake as #854; worth a habit rather than a memory.

**Code review:** **1 finding, and it was on my strongest claim.** I wrote that the NaN argument covered
*both* `no-inverted-boolean-check` sites. True at `:255`; false at `:178`, a startup invariant where the
loop at `:174` already rejects non-finite values. Corrected so each site carries its own reason. The
review also independently verified all 23 call sites are semantically identical, all six `?.` sites
preserve `undefined`, the 8 removed bindings are unreferenced, and the narrowed security rules resolve
to severity 2 in `src/`/`app/` and 0 in `db/`/tests. `/security-review` **not run** — config plus a pure
string helper; no money, auth or migration path.

**PR:** [PR #922](https://github.com/mobiustripper42/muster/pull/922)
**Points:** 5
**Branch:** task/908-security-lint-rules
**Opened at:** 2026-09-04T20:05:00Z

## Task 7: The last six lint rules — two ratcheted on, four off with proof

**Completed:**
- `sonarjs/cognitive-complexity` **ON at a ceiling of 40**, 11 sites baselined with disables stating
  their score. `sonarjs/no-nested-conditional` **ON**, 83 baselined. Four rules off, each with a
  measured reason in `OFF`.
- **340 of 347 recommended rules now enforced**, from 9 out of 875 at the start of the session.
- Gate exit 0, 2430 tests across 167 files — no source behaviour changed.

**A rule that does not compile.** `sonarjs/no-redundant-optional` reports `?: T | undefined` as
redundant. `tsconfig.core.json:19` sets `exactOptionalPropertyTypes: true`, under which those are
DIFFERENT types. Measured rather than argued: all 56 were stripped and `npm run typecheck` failed with
TS2375/TS2379 — *"Consider adding 'undefined' to the types of the target's properties."* Reverted.
**Without running that experiment I would have shipped 49 broken edits on a rule that looked cosmetic.**

**The operator overruled my recommendation and was right.** I proposed leaving cognitive-complexity OFF
because the curve has no cliff and no threshold cleanly separates outliers. His call: *"a score that is
an order of magnitude greater than what's recommended? I think I want this rule turned on … then we can
eslint-ignore these very bad ones."* That is DEC-144's ratchet done properly — baseline the existing,
stop the new, make debt visible in the code. I reached for "off" when "on and marked" was available.

**And he found a defect no rule could see.** Reading one flagged function he asked why
`shift.state === "Cancelled" || shift.state === "Completed"` wasn't a set membership test. It is spelled
out **ten times across nine files** and named nowhere → issue #926. `sonarjs/no-identical-functions` is
per-file, so ten identical expressions produce zero findings. Second time this week: #908 found 23 copies
of one regex, and only because a *security* rule flagged it 23 times.

**I earned the warning I had written.** Running `eslint --fix` against a throwaway config enabling ONE
rule DELETED the inline disables for every rule the throwaway did not enable — they read as unused. Four
files silently lost their #854 and #908 disables. Caught by reading the diff. Recorded in the config:
never `--fix` against anything but the real config.

**Also corrected mid-task:** I told the operator the metric charges for each `||`. It does not — measured
on his own toolchain, a RUN of one operator is free (`a || b || c` costs the same as `a`); only MIXING
`&&` with `||` adds 1. A high score means nesting, not long conditions. That correction changed which
threshold looked defensible.

**Code review:** **3 findings, all three my own numbers wrong** in the file that is the system of record —
a "12" contradicting the ratchet table two lines below it, and two ratchet projections derived by counting
FINDINGS where the ceiling counts SITES (20 → 37 not 44; 25 → 26 not 28). All fixed. The review
independently verified the `exactOptionalPropertyTypes` claim, that the ceiling resolves to `[2, 40]`
rather than silently running at 15, that all 94 disables sit immediately before their target with none
unused, and that the four files touched by reverted experiments are byte-identical to main. It also
worked out that **83 disables suppress 87 findings** because three lines carry more than one nested
ternary — both numbers right, now said so in the config. `/security-review` **not run** — config and
comments only.

**Process note.** This PR, and #911 and #922, were opened with a hand-typed `gh pr create` rather than
through `/kill-this`. `@code-review` ran on all three and the receipts are in the PR bodies, so nothing
shipped unreviewed — but the ritual is the ritual, and Tasks 5, 6 and 7 were all logged by hand after
the fact.

**Issues filed from this task:** #926 (the ten copies), #927 (complexity ratchet, engine last),
#928 (lift 83 nested ternaries).

**PR:** [PR #929](https://github.com/mobiustripper42/muster/pull/929) — merged 2026-09-05T01:59:34Z
**Points:** 5
**Branch:** task/909-cognitive-complexity
**Opened at:** 2026-09-04T23:58:00Z

## Task 8: Name the terminal-shift guard — 14 sites, not the 10 the issue listed

**Completed:**
- `src/domain/states.ts` — `TERMINAL_SHIFT_STATES: ReadonlySet<ShiftState>`, beside `SHIFT_STATES`.
  Docstring records why the eight single-`Cancelled` sites are NOT part of it: a Completed shift
  still gets paid, so terminal-to-the-engine and excluded-from-pay are different questions.
- All 14 sites converted across 9 files: `builder/form-shifts.ts` (×3), `builder/tick.ts` (×2),
  `asks/ask-loop.ts` (×2), `asks/lean.ts` (×2), `asks/suppression.ts`, `admin/warming.ts`,
  `admin/at-risk-board.ts`, `admin/all-shifts.ts`, `crewapp/crew-view.ts`.
- `src/domain/states.test.ts` — the partition test, written first and watched failing with
  `TypeError: ... reading 'has'` on the missing export.

**The count was wrong in the issue, and the way it was wrong is the lesson.** #926 said ten. A grep
for the predicate found 14. The four extras: two written negated and split across four lines by the
formatter (`form-shifts.ts` 362-365 and 522-525), one hidden in a local `live()` helper, and one in
`all-shifts.ts` that is not an engine guard at all — it decides whether to re-derive state on read.
A single-line `grep 'Cancelled.*Completed'` finds none of those. The issue's own list was built that
way, which is how a hand-written inventory of duplication under-reports by 40%.

**The user cut the audit half.** The issue's "worth checking while in there" asked whether any engine
path is MISSING the guard. I scoped that as a separate issue (~40 shift-reading call sites, most of
them display paths where absence is correct, and a genuinely missing guard is a behaviour change this
task's acceptance rules out). The user's answer was that they hope the code isn't that bad and the
partition test IS the check they wanted — so no audit, no follow-up issue. Nothing filed.

**The test pins the partition, not the contents.** Asserting the set holds two members restates the
declaration. It declares `ENGINE_MAY_WORK` by hand and asserts the complement equals it, so a seventh
shift state fails until someone decides which side it is on. `@code-review` verified there is no way
to add a state and have it silently pass.

**Local `main` was stale** (63dafbf vs origin/main 5d69ad6), so `git diff main..HEAD` showed 60 files.
The branch was cut from the #909 tip, which IS an ancestor of origin/main — `git diff origin/main...HEAD`
is the real 11. Worth knowing before believing a scary file count at PR time.

**Code review:** Clean — 0 findings. It re-derived all 14 swaps by De Morgan, confirmed the `!shift ||`
null guards survived, and found a ninth deliberately-untouched site I had not called out
(`form-shifts.ts:460`, a single-`Completed` resurrection check, #570). One cosmetic import-split note,
not taken.
**PR:** [PR #931](https://github.com/mobiustripper42/muster/pull/931)
**Points:** 2
**Branch:** task/926-terminal-shift-states
**Opened at:** 2026-09-05T02:59:05Z

## Task 9: Log the rendered body when no channel is configured — and the audit goes green

**Completed:**
- `app/lib/unsent.ts` + `unsent.test.ts` — `unsentLine` (pure) and `logUnsent`. Triggers on "no
  channel configured", never on environment; severity is what differs (`console.log` in dev,
  `console.error` in prod for sheepdog). Never throws — it runs on a degrade path.
- `app/lib/booking-confirmation.ts` — both skip branches, the "once for send, once for resend"
  the issue named. Both now mint the booking code before composing, because there is no manage
  URL without one.
- `scripts/check-audit-citations.mjs` + `docs/audit/2026-08-29-spec-2.8-conformance.md` — five
  citations renumbered, one re-anchored, one paragraph amended in place.

**The issue got unwound three times before a line was written, and that was the value.** #901 said
"log the message that would have been sent." Asking what that phrase meant surfaced that the outbox
is a manual composer (`sms:` deep link, operator's own phone), not a test harness — so #901's step 1
did not replace what its step 2 deleted. Then the operator asked whether the send audit was worth
anything: it is not, because `audit-trail.ts` already records `asked`/`nudged` (the intent) and
Twilio's console records delivery. **#687 is dead, not deferred.** A file count then made #901 a 13,
split into #933/#934/#935. `answerOwnAsk` came out along the way — it takes `src/asks/answer-as.ts`
with it, being its only caller.

**No fake channel, and this is the finding worth keeping.** The obvious build was a logging
`ChannelPort`. It would have made `resendReservationLink` return `attempted` instead of `skipped`,
showing the operator "Sent" for a message that only reached a log — the exact defect `ResendOutcome`
is a union to prevent. Both bodies are already exported pure functions, so the skip point needs no
channel at all.

**`/security-review` contradicted a decision the operator had already made.** They ruled "no mask —
this is going to be rare so just send it." The pass returned two Mediums at 8/10 against exactly
that, citing two things not on the table when the call was made, both verified by hand:
`auth-delivery.ts:55-62` hard-gates the one pre-existing credential log line to non-prod *"so a prod
flag-flip can never write a live credential to a production log"*, and `booking-code.ts:74-75`
declines a redirect specifically to keep the credential out of *"any log that records one"*. Shipped
as decided and surfaced at the top of the PR for a ruling before merge — not silently overridden,
and not quietly obeyed either.

**`check:audit` went green: 443 of 443.** It is outside `verify` on purpose, so this was run
deliberately at the operator's instruction. My own change orphaned a citation (GONE, not MOVED):
Criterion 14 quoted the skip message this deletes and its prose said "Both log in a production
deploy". Amended in place with a dated note rather than renumbered, because renumbering would have
left the audit reading as verified while being false. Merging current main then cleared the rest —
first time both lanes have been green together. Two conflicts with lane B's manifest resolved to
their side.

**Mechanism learned:** `check-audit-citations.mjs:649` short-circuits when the quote is on the cited
line, so a duplicated quote is fine as long as the number is right. "Make the quote longer" only
applies once the number is already wrong.

**Code review:** `@code-review` clean — 0 findings. `/security-review` 2 Medium (8/10), both the
same masking decision, unresolved by design.
**PR:** [PR #938](https://github.com/mobiustripper42/muster/pull/938)
**Points:** 2
**Branch:** task/933-log-unsent-body
**Opened at:** 2026-09-05T16:56:18Z

## Task 10: The Brave login bug was never about Brave

**Completed:**
- `app/(crew)/crew/page.tsx` — the `!view` branch now reads `LOGIN_EMAIL_COOKIE` and forwards
  `stage` and `err`, matching its sibling twelve lines up. Two notices added: "Your session
  ended." for a stale session, and an expired-step notice for `?stage=code` with no cookie.
- `e2e/crew-sign-in.spec.ts` — four cases. Three written first and watched failing; the fourth
  written after its fix and labelled as such in its own comment.
- Issue #936 rewritten end to end — cause, options, acceptance and repro all replaced.

**The whole value of this task was refusing to build the issue as written.** #936 blamed Brave
and a `cookies().set()` + `redirect()` race in a server action, with two options costed out. Both
were wrong. Falsified by two experiments, not by reading:
1. `stashFormDraft` is that identical pattern at **25 call sites across 11 files**. Refusing a save
   on `/admin/add-ons` in Brave returned the typed values intact.
2. The login flow worked fine in a Brave private window, cookie present in the jar.

The variable was whether a **crew-kind** session already existed. It presented as browser-specific
because `?admin=<handle>` mints `subjectKind: "admin"` and takes the working branch — so the
dev-link workaround kept working and made the bug look session-independent. Chrome "worked" because
that profile had no stale crew session.

**Building Option A would have added a client island** (DEC-147 rules 1 and 2) to fix a cause that
did not exist. That is the cost of building a plausible issue without reproducing it.

**How the real cause got found:** the second experiment was the one that mattered. Asking "if this
diagnosis is true, what ELSE must be broken?" produced the form-draft prediction, and the
prediction failing is what killed the diagnosis. Cheaper than reading the framework source.

**A wrong claim of my own, corrected mid-conversation:** I told the operator their `?admin=eric`
sign-in was what put them in the stale state. It cannot — `dev-link/route.ts:67` mints an admin
subject. It came from a crew sign-in followed by a `db:reset:dev`. Caught before it went into the
issue as the reproduction.

**I introduced an exposure and `@code-review` caught it.** Wiring `reason` to copy meant
`?auth=stale` could render "Your session ended." on a browser that never held a session — `reason`
is `sp.auth`, straight off the URL. No leak, no grant, but a claim about the visitor's own history
should not be settable by the visitor. Re-gated on a derived `sessionEnded` prop.

**Copy was the operator's, and shorter than mine.** I wrote "You're signed in as someone who is no
longer on the crew." They cut it to "Your session ended." — the crew member cannot act on the
reason, and the form below is the instruction.

**Code review:** `@code-review` 2 findings — one fixed (the URL-spoofable notice), one accepted
(the two notices can legitimately stack). `/security-review` clean: every authority decision in
this flow is made outside the page, `startSession` overwrites rather than merges, and the subject
comes from the verified email rather than the stale cookie.
**PR:** [PR #939](https://github.com/mobiustripper42/muster/pull/939)
**Points:** 2
**Branch:** task/936-stale-crew-session
**Opened at:** 2026-09-06T01:07:50Z

## Task 11: One command gives the standard dev world

**Completed:**
- `db/reset-dev.ts` — default seed set 3 → 5 (`fleet, crew, reservation, xola, atrisk`), five missing
  registry entries added, one dangling entry removed, one shared clock read, and a failing seed now
  names itself and prints the command to re-run just it.
- `db/reset-dev.test.ts`, `db/seed-fleet.ts`, `package.json`, `src/reservations/seed-xola.ts`,
  `docs/RUNNING.md` — the assertion, a four-month-stale pointer, the dangling script, the tenant id,
  and a section saying what the standard world contains.

**The operator wanted less, not more, and said so twice.** *"i'm not asking for anything new. i'm
just asking for the data i've seen."* And: *"if you spit out walls and walls of text, you have done
something wrong."* The issue had six acceptance criteria and a nine-seed composability analysis; the
delivered thing is one command that gives boats, crew, an offering, a month of bookings, trips,
shifts and the At-Risk board.

**The pin-vs-relative fork was the only real decision, and the operator took the smaller side.**
A fixed `SEED_TODAY` reproduces a world exactly but ages — two months on, every shift is in the past
and the app looks empty. Relative-to-today keeps the SHAPE constant (same boats, same people, same
offsets) and lets the calendar move underneath. *"relative to today is perfect."*

**The issue's headline cause was wrong, again.** It said `reset-dev.ts` passes only `DATABASE_URL`
to each seed subprocess so `SEED_TODAY` could not be forwarded. It spreads `...process.env` and
always has. Second issue in a row (see Task 10) where the filed diagnosis did not survive being
checked — worth noticing as a pattern rather than twice as a coincidence.

**Then I wrote a false claim of my own, in three places, and `@code-review` caught it.** I asserted
`SEED_TODAY=<date>` reproduces the world exactly — in the code comment, the failure retry hint and
`RUNNING.md`. Only `reservation` and `xola` read it; `crew` and `atrisk` ignore it, and two of those
four are in the new default set. **Narrowed rather than built**: teaching the other two is a
date-math sweep nobody asked for. `HONOURS_SEED_TODAY` now gates the retry hint so it never suggests
a variable that does nothing.

**Two boats named "Growler" — found, renamed, reverted.** `crew` and `atrisk` both label a vessel
Growler; different ids, no collision, but putting both in the default set is what made the fleet list
show it twice. `e2e/cockpit-polish.spec.ts` and `e2e/shifts-view.spec.ts` assert on the string, so
the rename was a bigger change than the confusion. Documented in `RUNNING.md` instead — the reverting
is the part worth remembering.

**Also found, not in the issue:** `seed-fleet.ts:29` still told you to run a script retired
2026-07-25, and `seed-xola.ts` wrote `tenantId: "brewboat"` where every other seed writes
`tenant-brewboat`.

**Code review:** 5 findings — 1 real (the false SEED_TODAY claim, fixed), 2 cleanup fixed, 1
documented (Growler), 1 left open (the new failure branch has no test; covering it needs an injected
`execFileSync`). It verified composability rather than trusting the issue: no id collisions across
the five, and the order satisfies the one real dependency.
**PR:** [PR #940](https://github.com/mobiustripper42/muster/pull/940)
**Points:** 3
**Branch:** task/937-standard-dev-world
**Opened at:** 2026-09-06T02:42:40Z

## Task 12: The outbox is gone, and the thing that replaced it nearly leaked an admin session

**Completed:**
- 23 files deleted: `/admin/outbox` + actions, three components, three view modules + tests, three
  channel adapters + tests, `src/asks/answer-as.ts` + test, the seed, two e2e specs.
- `src/adapters/log-channel.ts` + test (10 cases) — one class for all three ports, discriminated
  by payload shape like `TwilioChannel`.
- `forwardToOutbox` → `relayAsks`, `forwardNoticesToOutbox` → `relayNotices` (16 sites);
  `RELAY_LINK_TTL_MS` moved to `src/ports/channel.ts`; `docs/RUNNING.md`'s outbox walkthrough
  replaced with how you read an ask that has nowhere to go.

**`/security-review` returned High/8 against my own replacement, and it was right.** `LogChannel`
minted a live 24h crew magic link and wrote it to `console.error` in production. `OPERATOR_CREW_MEMBER_ID`
is asked for seats like anyone else; that crew id is an active admin (DEC-092); `switchToAdmin`
(`switch-actions.ts:41`) upgrades a crew session to admin with **no re-auth** — its own docstring calls
it "the one escalation seam in the app". So the line was a full admin credential in a stream that
log-read access alone can reach. `sms.ts` treats a HALF-set Twilio config as unset, so one dropped
env var reaches it.

**I verified the escalation myself instead of trusting the finding** — ran `db:tick` against a fresh
world and watched `to=crew-eric-stoffer` come out with a clickable link under it. That is the second
time today an agent finding needed checking before acting; both times it held, but the check is what
made it safe to act on.

**Fixed by defaulting to the safe value.** `mintLink` defaults to **false**; `channel.ts` passes
`!isProdDeploy()`. Off, it mints NOTHING rather than minting and hiding — an unredeemed token per
unsent ask is the same exposure one indirection away. This is the rule `auth-delivery.ts:58` already
applied and this branch was about to contradict.

**Explicitly NOT folded under the operator's earlier ruling.** #933's "no mask, it's rare" was about
a customer's booking code. An admin session is a different question, so it went back to them in the
PR rather than under it.

**`@code-review` caught a bug the gate structurally cannot see.** `db/tick-dev.ts` still imported the
deleted `WebLinkChannel` — `npm run db:tick` died on module resolution, while `docs/RUNNING.md`
(rewritten in the same commit) told the reader to run exactly that. `db/` is in neither tsconfig's
`include` and has no test. **That is the `xola-report.ts` incident from DEC-159 repeated exactly**,
which is worth more than the fix: `db/` is a blind spot the gate does not cover, and this is the
second recorded instance.

**Two e2e specs were passing vacuously**, and #936's fix is why they still passed: they signed in as
`crew-obx-bo`, an id only the deleted outbox seed created, so the page rendered the signed-out screen
— which has no "Switch to admin" for anybody. The assertion held for the wrong reason and the
DEC-092/093 escalation gate was uncovered.

**Found, not fixed, worth an issue:** a plain `db:reset:dev` truncates rather than dropping, so a
migration merged by the other lane leaves a recorded-but-absent column. `db:migrate` says "Up to
date" and `db:seed:crew` dies on `column "payment_intent_id" does not exist`. `--fresh` is the
workaround.

**Code review:** `@code-review` 4 findings, all fixed. `/security-review` 1 High, fixed.
**PR:** [PR #943](https://github.com/mobiustripper42/muster/pull/943)
**Points:** 5
**Branch:** task/934-remove-outbox
**Opened at:** 2026-09-06T19:05:19Z

**Next Steps:**

**Context:**
- Session 101 (`spec-2.8-2.10-conformance-audit`, transcript dir `-home-eric-muster-s91`) is **live in a
  concurrent window**, not stale. It was closed in error by an accidental `/its-dead` from this window at
  2026-09-02T01:47Z and reopened by its own window at 01:53Z (`194af5c`). Nothing on the sessions branch
  needs repair.
