---
session: 104
slug: main
branch: main
started: 2026-09-05T17:37:05Z
ended:
points:
pr_numbers: [941, 942, 947, 952]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster-s91/7708812c-ff2f-5e4d-be1e-079efe1afc6e.jsonl
---

# Session 104 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 14.5 — confirm flips the pending row; delete the dead hosted insert

**Completed:**
- `confirmPendingRow` (`src/reservations/write-booking.ts`) replaces `writeSlotBooking`: finds the pending row by payment-intent id, flips it to `booked` in place via the new port method `bookPendingIfHullFree` (both adapters) under the hull-day lock, materialising the Event. `writeSlotBooking`, `reservationIdFor` (the `sha256(paymentId)` derivation) and `saveBookingIfSlotFree` are gone. The reservation id is now the pending row's random id, so two payments can resolve to one row (unblocks audit criterion 12).
- `PendingExemption` (`hull-busy.ts`) now exempts by `reservationId` (the row being flipped) + `holderToken`, never `paymentIntentId`. Postgres `pendingOverlapSql` takes two NULL-safe exemptions (`$6` id, `$7` holder token) via `is not distinct from` guarded by `$n is not null` — closes the session-103 hole where a NULL own-key exempted every rival.
- Webhook (`booking-webhook.ts`): the booking spine flips the row; a hosted `checkout.session.completed` booking session is refused loudly (nothing mints one since 14.5); a charge matching no live pending row alerts `unbookable`.
- Deleted `create-departure-checkout.ts` + test (issue #793's core deliverable, operator-approved). Fixed the four pricing-test totals that were $14.97 low because that builder hardcoded the service fee to 0. Rewrote the hosted-path tests in `gratuity.test.ts`, `booking-webhook.test.ts`, `create-departure-payment-intent.test.ts`, `confirm-booking.test.ts`, `resolve.test.ts` and the Postgres concurrency suite onto the flow. Two dev seeds (`seed-overlap-dev.ts`, `paid-unbooked-dev.ts`) drive the flip.
- Test-first: the contract-suite flip cases and the service test were written and watched failing (27 failures on the missing method) before the code. `npm run verify` green: 2507 tests / 167 files, both typecheck profiles, lint, build.
- The audit (`docs/audit/2026-08-29-spec-2.8-conformance.md`) got a header note: it is a temporary conformance scaffold for Phases 14–16, expect `check:audit` red mid-phase, re-baseline once at phase close, delete when §2.8 ships. Operator's call — no DEC (it deletes itself, nothing to cite). `check:audit` left red (157 citations moved: 98 mechanical, 35 GONE incl. criterion 6/12 verdict flips lane B owns); it is outside `verify`.

**Code review:** `@code-review` clean — 2 cleanup findings, both stale doc comments naming the retired methods (`claim.ts`, `cancel-reservation.ts`, `entities.ts`, the stripe route, and the now-inert "two rows sharing the id" ordering in `getReservationByPaymentIntentId`); all fixed in the second commit. `/security-review` ran (money-moving trigger) — 0 findings: payment provider-verified before the flip, no cross-customer escalation, SQL parameterized, exemption and hosted refusal both fail closed.
**PR:** [PR #941](https://github.com/mobiustripper42/muster/pull/941)
**Points:** 5
**Branch:** task/14.5-confirm-flips-the-row
**Opened at:** 2026-09-06T02:52:00Z

## Task 2: Phase 14.6 — retry on the same row; a reservation carries many payment ids

**Completed:**
- A retry from the same checkout reuses its own pending row (`create-departure-payment-intent.ts`), matched by the httpOnly holder token + slot via the new `getLivePendingByHolderToken` — never typed email/phone (§2.8.5, possession not identity). Reserved time untouched (§2.8.7) so a resubmit can't park the hull; the invoice re-freezes each attempt so a tip change reprices.
- The scalar `payment_intent_id` column → array `payment_intent_ids` (migration `20260906130000`, drops the 14.4 scalar, adds a GIN index; backfill touches only all-null Xola rows). `Reservation.paymentIntentId` → `paymentIntentIds: string[]`. `getReservationByPaymentIntentId` matches ANY id (`@> array[$1]` / `.includes`), so a superseded intent still resolves (unblocks criterion 12 for 15.2).
- Test-first at the retry-behaviour level: one row, same id, reserved time unchanged, both ids recorded; a different cookie does not merge; a changed tip re-freezes. The entity rename coupled compilation, so these were written-then-run, not watched-red-first (noted in the PR).
- `npm run verify` green: 2521 tests / 167 files, both typecheck profiles, lint, build. Postgres contract suite ran (Docker up).

**Code review:** `@code-review` found ONE real bug and it was the concurrency case Eric and I had discussed: a retry's read of the pending row can precede a concurrent `payment_intent.succeeded` confirm and its write land after, so the original bare `saveReservation` upsert reverted a just-booked, PAID row to `pending` with a null Event — an orphaned booking. Fixed with `appendPaymentIntentToPending`, a guarded write (append the id unconditionally; re-freeze invoice/updatedAt only while `pending`; never touch status/eventId). Contract-tested on both adapters incl. the booked-row guard. `/security-review` ran (money + migration triggers): 0 findings — reuse gated on the httpOnly token not identity, queries parameterized, admin rows unhijackable, dropped column no exposure.
**PR:** [PR #942](https://github.com/mobiustripper42/muster/pull/942)
**Points:** 3
**Branch:** task/14.6-retry-on-the-same-row
**Opened at:** 2026-09-06T18:52:00Z

## Task 3: Phase 14.7 — drop `checkout_holds`; the pending row is the claim

**Completed:**
- `acquireDepartureHold` → `claimDepartureSlot` (`src/reservations/claim.ts`), which now WRITES the pending row itself inside the fit-and-fallback loop via an injected `PendingRowBuilder`. That makes the write the contention point, so **a lost CAS falls through to the next fitting hull** instead of ending the checkout — before, the hold picked the hull and the pending write was a separate all-or-nothing step at the caller, and a race lost there reported `sold_out` with a free boat beside it. All money stayed in `create-departure-payment-intent.ts`; all boat choice moved to `claim.ts`. The Stripe `amountCents` and every metadata number now read off the row's frozen `invoice`, so charge/metadata/row can't drift.
- #575 session reuse moved off the hold table onto the row, read out of the `reservations` list the occupancy math already loaded — deliberately in-memory, so the row returned is the one the hull check just reasoned about at the same instant.
- Deleted: the table (migration `20260907120000`), `CheckoutHold`, `CheckoutHoldId`, five port methods + both adapters' implementations, the issue #713 sweep, `getLivePendingByHolderToken` (added in 14.6, dead once the claim owns reuse), and the deriver's `held` status. Two nested ternaries lifted into `slotStatus` (issue #928). `plantCheckoutHold` → `plantPendingReservation` in e2e.
- Test-first and it failed for the right reason: `claim.test.ts` rewritten against `claimDepartureSlot` before it existed (missing export), then 41 passed / 1 failed — and that one failure was a real latent defect, not a mistake in the new code.
- `npm run verify` green: 2490 tests / 167 files (was 2521 — the hold suites went), both typecheck profiles, lint, build. Postgres contract suite ran (Docker up).

**Scope changes taken at build (operator-approved):**
- **Issue #806 unbundled** — the per-token cap is a new security control, not a rider on a deletion. #806 stays open, keeps `phase:14`/`lane:b`, and was **retargeted from holds to pending rows**. Issue #918's body edited to drop the criterion and the `closes` line.
- **Re-estimated 3 → 5** — 34 files referenced the hold, not the 16 estimated. PROJECT_PLAN phase total 39 → 41.

**Issues filed, none fixed here:**
- **issue #945** — the pending write refuses a slot backed by its OWN unbooked scheduled muster Event where the read exempts it. Probed against untouched adapter code (`{"result":"lost"}`) to confirm it predates the branch. Latent (nothing materializes such a row today), **blocks 16.1**; pinned by a test and noted on the 16.1 plan row.
- **issue #946** — a retry with a changed guest count reprices the charge but not `party_size`. Pre-existing (14.6), surfaced by `/security-review`.

**Code review:** `@code-review` — 2 findings. One fixed (a test asserting the retired `held` status, which typecheck couldn't catch because the helper takes `status: string`). One **disputed after checking the replaced code**: the reviewer said two concurrent same-token requests can now write two rows on one hull and that main could not. Main wrote two rows too, on two *different* hulls — the 14.6 lookup was keyed on the hold's vessel, so neither request found the other's row. Same row and intent count; main took two hulls off sale, this takes one. The flip's whole-boat mutex (`postgres-repository.ts:1689-1693`) still stops the second booking, the suggested partial-unique-index fix isn't implementable (`now()` isn't immutable — the reason holds needed delete-then-insert), and reaching it needs a scripted caller since `checkout-form.tsx:155` gates submit on `!submitting`. Recorded on issue #806, whose fix closes it exactly. `/security-review` ran (money-moving + data-changing-migration triggers) — **0 findings at confidence ≥ 8**: reuse needs a token on both sides and a SQL NULL never matches, no new or changed SQL, `pendingOverlapSql`'s NULL guards fail closed, the drop orphans no FK, no money TOCTOU, no new token/PII exposure.
**PR:** [PR #947](https://github.com/mobiustripper42/muster/pull/947)
**Points:** 5
**Branch:** task/14.7-drop-checkout-holds
**Opened at:** 2026-09-07T14:30:00Z

## Task 4: Phase 14.8 — the monitor before the destructive tool

**Completed:**
- **Cut the reaper from the spec rather than building it.** §2.8.8 specified a job deleting old lapsed rows "so the table does not grow without bound" — a justification no other table in this schema has to make, for rows nobody has yet seen a week of, whose whole value is being the evidence that says whether the delete was ever needed. Operator's framing, now the spec's: *"we are building a monitor tool instead of just building the destructive tool first."* **No DEC** — his call, and a good one: *"I don't think we should make a dec for something we didn't build. we didn't build A LOT of things."*
- `src/reservations/abandonment.ts` (new, pure) — `abandonedCheckouts` (lapsed public pending rows, newest first) + `summarizeAbandonment` (count, hull-hours withheld, how many reached the provider). **No port method**: nothing reaps, so it filters over `listAllReservations()` the way the deriver already does.
- `/admin/abandonment` (new) — the screen, on the integrity-report pattern, linked under the booking surfaces not Settings. Raw and unranked: no field separates scripted abuse from real abandonment, and an earlier draft that split them into two counts and called one "script volume" was wrong — the operator caught it (*"does it?"*). Window stated once in the header, not per row; freezing it per row was proposed and rejected (*"it's dumb to record the same number we might never ever change"*). Session column is an FNV-1a hash of the holder token, never the token. **Desktop only, operator's call** — a documented exception to the `Surface check` slot's 375px rule, deliberately not in Playwright's `mobile` project.
- `payment_intent.payment_failed` is now a named `PaymentEvent` the webhook acks and **deliberately ignores** — cancelling on a decline takes the boat from someone standing at the till with a second card out; their window is open and their retry reuses that row (14.6). Named rather than left to `parseEvent`'s null, so acked-on-purpose and never-heard-of aren't one signal.
- **Latent defect found by adding it:** the webhook spine had no default case — an unhandled event type fell into the hosted `checkout_completed` path and threw on `metadata.purpose`. Unreachable today; now guarded.
- Criterion 7 tests: an abandoned checkout leaves the row and nothing else — no Event, customer or booking code — asserted before *and* after a real decline event.
- `npm run verify` green: 2499 tests / 162 files, both typecheck profiles, lint, build. Postgres contract suite ran (169 tests, Docker up).

**Issue filed:** **issue #951** — `--color-faint` fails AA contrast at 2.66:1 while carrying admin body text, including the numbers `/admin/integrity` exists to show. Pre-existing, found by `@ui-reviewer`; this page moved off it.

**Code review:** `@code-review` — 3 findings, all fixed. The one that mattered: **a test named for the decline path never sent a decline** — it called `createDeparturePaymentIntent` and stopped, proving that doing nothing changes nothing under a name claiming the decline path was inert. Exactly the laundering failure the memory rule names. Now drives the real arc through `processBookingWebhook`, which is coverage neither existing test had (the webhook suite asserts against a hand-seeded row; this one against a row the real checkout wrote). Also two `SPEC.md` citations pointing at the wrong criteria — moved by this same diff rewriting §2.8.8 above them — now cited by quoted text, which cannot drift; and the Stripe route's doc comment, the only record of which events the dashboard must subscribe to, didn't mention the new one. `/security-review` ran (money-moving trigger) — **0 findings at confidence ≥ 8**: admin gate byte-identical to `/admin/integrity` and *before* the repo read, no stored XSS from the public checkout onto the operator screen, the token unrecoverable (never on the type, so never in the RSC payload), no PII, decline path behind signature verification and touching no state. `@ui-reviewer` ran (new rendered surface) — 4 findings, 3 fixed (glossary moved above the table, `scope="col"`, `Started` matched to `Departure`'s 12-hour convention), 1 filed as issue #951.
**PR:** [PR #952](https://github.com/mobiustripper42/muster/pull/952)
**Points:** 5
**Branch:** task/14.8-expiry-is-a-clock
**Opened at:** 2026-09-08T14:05:00Z

**Next Steps:**

**Context:**
