---
session: 105
slug: main
branch: main
started: 2026-09-12T17:46:51Z
ended:
points:
pr_numbers: [1004]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/9c21ae14-0106-5015-96e1-543557eabd3b.jsonl
---

# Session 105 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: One bad vessel-day no longer costs the fleet its shifts (#957)

**Completed:**
- **Reopened issue #957 first.** It had been closed 7 minutes before this session opened, by commit `22da9d4` in PR #1002 whose body read "This closes #957's option 3". GitHub matches the `closes #957` token and discards the qualifier, so a sentence meaning "this covers one of three options" read as "done". The same commit says two sentences later that the isolation fix is still needed. Safe phrasings are `refs #957` / `part of #957`; only `closes`/`fixes`/`resolves` auto-close.
- **The isolation fix** (`src/builder/form-shifts.ts`): the `try` moves inside the `for`, so each vessel-day is its own attempt. New `FormResult.failures` carries `{ vesselId, date, error }`. `PartialFormError` deleted — `formShifts` no longer throws for a bad vessel-day and always returns a complete result. `#766`'s requirement (notices computed before a failure must survive) is satisfied by isolation rather than by an exception; its test is rewritten, not deleted.
- **Three logging callers updated**, not the two planned: `booking-webhook.ts`, `cancel-reservation.ts`, and `app/api/cron/tick/route.ts`. The third was missed by a first census that grepped `src/`, `db/` and `scripts/` and never looked in `app/`.
- **The false promise deleted.** All three logged "the tick will re-form the shifts". False about notices when written (#766: durable rows → next tick sees no diff → silence) and false about shifts too — the tick called the same unscoped `formShifts`, hit the same bad vessel-day, threw the same way. Nothing ever recovered.
- **`assertDayFormed`** (review fix, `5de08c1`) — see below.

**Code review:** 2 findings, both addressed in `5de08c1`. **The real one:** `splitShift` (`src/builder/split.ts`) and `mergeShift` (`src/builder/merge.ts`) relied on `formShifts` throwing without ever naming `PartialFormError`, so neither a grep for the symbol nor a typecheck could see the dependency — my census looked for the wrong class. Both act on ONE vessel-day and report success to a person, and both write before the re-form: split persists `splitCutTime` (skipped day → cut set, no `-b` row, `split_ok=1` on screen), merge deletes side B's seats and row (skipped day → side B destroyed, canonical never re-derived, reported as success, crew silently seatless). Fixed with `assertDayFormed`, whose docstring states the rule as a property rather than a list of two module names: a caller claiming one vessel-day succeeded must call it, a caller reporting counts must not. **Second, doc-only:** isolation turns a systemic failure into N entries rather than one abort, so `failures`' docstring now says many entries with the same error is one outage, not N data problems (for #1001's benefit).
**`/security-review`:** ran (`cancel-reservation.ts` is on the money-moving trigger list) — 0 findings above threshold.
**Proof:** three new cases, each watched failing first. The isolation case seeds the unmanned vessel FIRST so it is the first group reached — failing on the last would prove nothing. The split and merge cases strip the vessel's manning rule between seed and command, which passes each function's own validation (it reads events, not manning) and fails inside the re-form. Gate green: 164 files, 2562 tests, 5 doc checks, 3 typechecks, lint, build.
**PR:** [PR #1004](https://github.com/mobiustripper42/muster/pull/1004)
**Points:** 3
**Branch:** task/957-form-shifts-isolation
**Opened at:** 2026-09-12T18:30:00Z

**Next Steps:**
- **#998 (B)** — the conditional write at `form-shifts.ts:487`. A 2. Next in the redesign order.
- **#999 (C)** — the scope parameter. An 8; carries three adversarial corrections from the architect review.
- **#1000** — `saveVesselAdmin` never re-forms after a manning change. Gated on #999.
- **#1001** — failed vessel-days reach nobody. **Now unblocked**: `FormResult.failures` is the field it reads, and its docstring already says to read the shape (N identical errors = one outage) rather than the count.
- Watch for other callers that relied on `formShifts` throwing without naming the class. The two found here were caught by review, not by census — a grep for a symbol cannot find a caller that only ever let it propagate.

**Context:**
