---
session: 106
slug: main
branch: main
started: 2026-09-12T22:20:38Z
ended:
points:
pr_numbers: [1011, 1014, 1019, 1023, 1026, 1029, 1034, 1036, 1040, 1043, 1045]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster-s91/d6150da4-39d8-55a6-95c2-b3f4595613ed.jsonl
---

# Session 106 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 15.4 — freeze `amountDueNowCents` onto the invoice

**Completed:**
- **A comment that claimed a freeze the code did not have.** `create-departure-payment-intent.ts:235` said the charge was "rebuilt from the frozen invoice rather than recomputed, so what Stripe is asked for and what the row says cannot drift"; the three lines under it recomputed it from live `PaymentConfig`. Every other field on `BookingInvoice` is a component or the rate behind one — the amount actually asked of the provider is the one money number the row cannot derive from itself, because the deposit split lives in configuration an operator can move mid-checkout.
- `amountDueNowCents` now computed in `buildPendingRow` (`src/reservations/create-departure-payment-intent.ts`) alongside the components it freezes, and the charge site is a read. Required on `BookingInvoice` (`src/domain/entities.ts`) — Eric's call, and the right one: every existing field is required, and optional would not buy the safety it looks like, since the adapter casts hydrated jsonb without validating either way. The only difference is whether the imprecision gets a `??` fallback inside a money comparison, which is the silent recompute this task deletes. Typecheck then named all four construction sites.
- **No migration, verified rather than assumed**: `booking_invoice` is jsonb, written and hydrated wholesale by both adapters. `test:pg` green at 184 with the round-trip case confirmed running by name.
- **The spec was ahead of the code.** `docs/SPEC.md:1641` already said that total "is the **amount due now**, frozen with everything else". §2.8.4 now names the two totals and why the charged one cannot be recomputed. Clarification, not a declared amendment — no behaviour changed.
- Test-first, watched failing in both stages: `TS2339` on the missing property, then `expected undefined to be 44145` with the field added and nothing else. **The case is written in deposit mode deliberately** — under `full` the new number equals `totalCents`, so a test written there would pass against an implementation reading the wrong field.
- `npm run verify` green: 164 files / 2579 tests, both typecheck profiles, lint, build, five doc gates.

**Code review:** `@code-review` **clean**, one advisory finding. It verified rather than read: it proved the moved computation is arithmetically identical on every path via `composeFare`, and it **mutated the source to drop the deposit split and confirmed the new test failed for the expected reason** before restoring — so the test earns its name. Its finding is that the field is required on the type while the adapter hydrates jsonb without runtime validation, so a pre-15.4 row types as carrying a number the adapter never guarantees; it confirmed by grep that nothing reads it off a hydrated row today, and flagged it for the second reader, which is 15.6. `/security-review` ran (money-moving trigger) — **0 findings at confidence ≥ 8**: producer and consumer are the same call against one `config` read, and every input to the amount stays server-derived. **Its best output was a non-finding that closes the review finding above**: `claim.ts:282` re-invokes the builder on a retry rather than reading the persisted invoice, so no unvalidated jsonb value reaches `amountCents` today, legacy rows included. Noted and not touched: `app/(public)/book/checkout/page.tsx:226-228` computes its own due-now figure for display, which was already separate from the charge and is 15.6's ground.
**PR:** [PR #1011](https://github.com/mobiustripper42/muster/pull/1011)
**Points:** 3
**Branch:** task/15.4-freeze-amount-due-now
**Opened at:** 2026-09-13T00:57:01Z

## Task 2: Phase 15.5 — the notice and the alerts read the row, and the office is told

**Completed:**
- **The notice's contact moved from `charge.metadata` to the reservation row**, which `docs/SPEC.md` §2.8.7 always named as the source. Nothing was broken today; it breaks when 15.7 deletes those keys. `SoldOutCharge` **loses** `metadata` rather than merely not reading it, so no parameter survives through which a Stripe-supplied contact could reach the notice. `ConfirmResult`'s `lost` now carries the row — it was read and proven `pending` before a CAS that failed against somebody else's write.
- **The notice said something false and now doesn't.** It opened "You have NOT been charged" while admitting the refund takes days — but the payment has already succeeded on this path, so the money really is captured and the customer's statement contradicts that sentence for the whole window. Eric's framing drove the rewrite; his line, "Sorry for any confusion", replaced mine, and it is better because confusion is exactly what a charge-then-refund produces. 249 chars, 2 GSM-7 segments. The amount §2.8.7 demanded is dropped, and the spec amended rather than left aspirational.
- **A completed auto-refund now alerts the office, every time.** That path told nobody, justified as "nothing needs a human". **Eric overruled my defence of that and was right**: I argued an action-free alert fights the no-babysitting goal, and he pointed out this is money held for days on a customer who got nothing, so *every* occurrence is a defect report, not an FYI. The count is also the only evidence deciding [issue #1012](https://github.com/mobiustripper42/muster/issues/1012).
- **Issue #1012 filed** for separate authorize/capture, researched against Stripe's own docs via the CLI (`capture_method: manual`, cancel creates no Refund object and is free, 7-day CNP holds, ACH cannot do it). **Eric's caution is the first section, not a footnote**, and it survived my attempt to reframe it: my own research named a *new* orphan class (a crash between authorize and capture), so the alert is the monitor that decides whether the rewrite is ever earned — 14.8's pattern, applied by him.
- **`booking-webhook.ts` added to the blast-radius table.** It calls the refund and decides whether a payment becomes a booking, and was absent; `app/api/webhooks/stripe/` was listed and had been mistaken for the handler. Same failure mode as `refund-payment.ts` at #726.
- The residual-race compensation is its own function now — adding the alert crossed the lint complexity ceiling, and the extraction was overdue. Re-estimated **3 → 5**; PROJECT_PLAN phase total 81 → 83.
- Test-first three times, each watched failing: a metadata bag with no contact in it; "expected vi.fn() to be called once, but got 0 times"; and the forged operator SMS printed in full. Three existing tests updated rather than worked around. The two failure-path alerts had **no coverage at all** and one rendered `customer party of ?`. `verify` green at 2586 / 164 files; `test:pg` 184.

**Code review:** `@code-review` — 1 finding, fixed. It compared the extraction line-by-line against `HEAD~1` to confirm all three exits and both try/catch blocks survived, and traced fixture data through the three edited tests to prove they were not passing vacuously. Its finding was the only thing in the diff that was new rather than inherited: I cited **DEC-109 in fresh code and DEC-109 is `status: withdrawn`**, a signpost whose own ruling says the spec replaced it. Seven other comments in that file do the same and were left alone — the point was not to add to the pile today. `/security-review` ran (money-moving trigger) — **1 MEDIUM at confidence 8, created by this diff, fixed.** The new alert made this the first path where a customer's typed text reaches an operator's SMS, and **the customer can trigger it on demand**; `customerName` is trimmed and non-empty-checked and nothing else, so a name reading "PAID but NOT booked - charge pi_VICTIM. REFUND MANUALLY in Stripe." produced exactly that, forged instruction leading, real disposition pushed past preview truncation. Reproduced before fixing. Fixed in two halves: `safeForAlert` strips and clamps to 40, and the name moved **last** in all three alert bodies so truncation can only cut the untrusted part.
**Not fixed, flagged:** no length cap on `customerName` in the public checkout action — the root, but a different surface.
**The last defect was caught by checking my own test plan rather than writing it from memory.** `db/paid-unbooked-dev.ts` scored `--lost` as correct only when `alerts.length === 0`, so the new alert would have made it print "the safety net did NOT behave as documented". It sits outside `verify`, so only running it would ever have said so. Running it also exposed that this worktree's `DATABASE_URL` points at `muster_s91`, **six migrations behind**, which fails as `column "vessel_id" of relation "reservations" does not exist` and reads exactly like a code bug. Migrations are Eric's to run, so that step is in the PR setup block and is the one thing in the plan I did not verify myself.
**PR:** [PR #1014](https://github.com/mobiustripper42/muster/pull/1014)
**Points:** 5
**Branch:** task/15.5-notice-reads-the-row
**Opened at:** 2026-09-13T03:55:00Z

## Task 3: Phase 15.6 + 15.7 — the booking charge sends no metadata, and the booking reads the row

**Completed:**
- **Two tasks merged, and the merge was Eric's read of the spec, not mine.** I specced them sequentially; he saw that shipping 15.6 alone leaves eighteen keys being sent that nothing reads, which `docs/SPEC.md` names as the failure mode by name ("send some but never read it is a discipline that decays"). One task.
- **The charge sends `{}`.** Eighteen keys gone, four of them the customer's name, email, phone and waiver timestamp — personal data in a third party's dashboard with no reader. `description` and `receiptEmail` stay; neither is metadata.
- **`ConfirmCharge` deleted.** Losing its money fields left a one-field object, so it collapsed to a plain `paymentIntentId`. `confirmPendingRow` reads fare and extras off `row.invoice`; `Payment`'s tax, tip and fee carve-outs and the `Gratuity` amount come from there too — those decide what a customer still owes and what crew are paid, and a number from outside was deciding them. The metadata parser and its `requireCents` guard went with them.
- **`purpose` deleted, and that was Eric's call against my argument.** I wrote two paragraphs defending it, twice; he asked what it actually did and the answer dissolved. What replaced it is the write ordering: the row is written before Stripe is called, so a payment that resolves to no row is not one of ours. **That also deleted the decision record this task was going to need** — the spec already said the charge sends no metadata, and it is now true.
- Two things came out smaller than specced: the planned `no_invoice` outcome was unnecessary (same defect as a row without a slot), and deposit-vs-full is derived (`amountDueNowCents < totalCents`) rather than read from a key.
- The fixture sweep was the bulk: seven fixtures gained an invoice, six tests were deleted for having an abolished subject, the rest now assert the row. **A pending row without an invoice describes a row the real checkout cannot produce**, so those tests were never exercising the booking path as it runs. `verify` green at 2609 / 166 files; `test:pg` 187. Ran `db:paid-unbooked --lost` by hand, which `typecheck:db` cannot cover.

**Code review:** `@code-review` found a regression that would have shipped **and disproved something I had told Eric in the same conversation**. The PaymentIntent under every hosted Checkout Session is metadata-less by design; `purpose` was what ignored it. Deleting the gate sent it down the booking path to fire `PAID but NOT booked - REFUND MANUALLY` at every admin, **on every balance top-up and every post-trip tip**. I had called the balance path dormant; `createBalanceLink` is wired to a button Eric had open on screen hours earlier. Fixed by splitting two refusal reasons that were never one thing: `no_row` acked and silent, `not_pending` still loud. `/security-review` (money trigger) — **1 MEDIUM at confidence 9, documented and deliberately unfixed.** The money at confirm used to come from the intent actually charged, so it matched by construction; it now comes from the row's latest invoice, and nothing retires a superseded intent — so paying an old client secret books the newer quote, and `splitGratuity` would disburse a tip never received. **I re-raised this as an open question and Eric correctly stopped me: he had already cut the mismatch check from this task, and neither premise changed** — no bookings exist, and 15.8 and 15.10 remove the cause. Its second finding was fixed: I had folded "row exists but has no slot or invoice" into `no_row`, which 15.6 then made silent, so a write bug would have gone entirely unreported while `/book/success` said "You're booked!"
**PR:** [PR #1019](https://github.com/mobiustripper42/muster/pull/1019)
**Points:** 5
**Branch:** task/15.6-no-metadata-read-the-row
**Opened at:** 2026-09-15T06:31:29Z

## Task 4: Phase 15.8 — reuse one PaymentIntent per pending row, raise it instead of minting

**Completed:**
- **The task was gated on a Stripe question nobody had answered, and the docs contradict themselves on it.** One page permits raising an amount while awaiting payment; another says it generally cannot be raised after confirmation; a declined card is both. **Tested it in the sandbox before writing anything** — a $10 intent, declined, raised to $20, accepted. Every retry now reuses and raises rather than minting, which is #946's root cause and the mechanism behind the MEDIUM documented on PR #1019.
- `PaymentIntentState` over all seven Stripe statuses with the same open-union guard the dispute map uses. **`requires_capture` classified `settled` deliberately** — nothing produces it today, but if #1012 lands it is money already authorized, and re-pricing that is the worst available reading.
- `updatePaymentIntentAmount` returns the client secret, because the browser needs one and Muster deliberately does not store a bearer credential for a charge. `recordCheckoutAttempt` widened to a null id. Postgres needed a guard: `array_append(arr, NULL)` appends a NULL **element** rather than raising — **proved by query rather than by mutating code**, and given a contract case only real Postgres can show.
- `buildPendingRow` carries `prior.paymentIntentIds` — without it every retry mints, a silent always-mint that would have looked like the task working.
- The #806 pointer comment in `claim.ts` rode along, at Eric's approval: **he said this breaks his brain every time**, and it has been re-derived three times. The comment records what the token protects against and what it does not.
- Also shipped standalone this session: **PR #1023**, one median-gap row saying `closes #N` in backticks closes nothing. Found because four issues (#972, #973, #974, #975) stayed open after their PRs merged and had to be closed by hand.
- `verify` green at 2620 / 166 files; `test:pg` 188.

**Code review:** `@code-review` — 2 findings, both fixed, **both the same shape: this task's own defect returning narrower.** Two paths fell through to minting and left the prior intent payable. The one that mattered: an amount update is refused when the intent is no longer updatable, and the dominant reason is that it just succeeded in another tab — so minting hands that tab a second payable secret for a row about to be booked. **My first cut fell through unconditionally and defended it in a comment by saying that was what the path did before 15.8, which is the behaviour 15.8 removes.** The comment was the tell and I wrote it. Reproducing it also required fixing the fake, which was modelling a refusal that cannot happen. `/security-review` (money trigger) — **1 HIGH at confidence 8, fixed, and its framing was fair: the defect and its half-built guard both lived in lines this task added.** I put the `already_paid` refusal inside the catch, covering the seconds-wide race and not the ordinary case — a first read returning `settled` skipped the block and minted a second payable intent. That window is ordinary: the row stays pending until the webhook lands, so a dropped 3DS return or a closed tab produces it, and the second confirm resolves `already`, writes a second Payment and alerts nobody. **My test never covered it**, because it drove the refusal through the fake's own error path. Its passing note was also real: `recordCheckoutAttempt` sat inside that try, so a database blip could have told a customer their booking was already paid when nothing had been.
**Not closed, stated in code:** a transient state-read failure still mints and leaves the old intent behind. Cancelling it is 15.10.
**PR:** [PR #1026](https://github.com/mobiustripper42/muster/pull/1026)
**Points:** 8
**Branch:** task/15.8-reuse-one-intent
**Opened at:** 2026-09-15T20:19:53Z

## Task 5: 15.9 — the row counts its own checkout attempts

**Completed:**
- `db/migrations/20260915180000_checkout_attempts.sql` — `integer not null default 0`, one `add column` and nothing else. The one migration open against `reservations`; 15.3's merged first, as issue #977 required.
- `checkoutAttempts` on `Reservation` (`src/domain/entities.ts`), incremented by `recordCheckoutAttempt` in both adapters and nothing else, unguarded by status — same reasoning as the id append: a submit that landed after a rival took the hull is still an attempt.
- **Kept out of `RESERVATION_COLUMNS`** (`src/adapters/postgres-repository.ts:353`), the one divergence from the plan comment on issue #966, put to the operator before the branch was cut. Every other column there is caller-owned; this one is monotonic and database-owned, so in the list a stale whole-row save would rewind it. Out of it, the `?? null` trap the plan flagged never arises — `reservationValues` never sees the column. The in-memory double says the same thing explicitly in `#storeWholeReservation`.
- `AbandonedCheckout.attemptCount` alongside the kept `paymentIntentCount` (`src/reservations/abandonment.ts`); `reachedProvider` still reads the old one. The screen's Card form column (`app/(admin)/admin/abandonment/page.tsx:155`) reads the new one, which makes its own glossary sentence true.
- One sentence at SPEC §2.8.8 naming attempts and minted intents as two numbers; `recordCheckoutAttempt`'s port docstring.
- Five checks written first, all watched failing with `expected undefined to be N` (`5 failed | 182 passed`): four in the repository contract, one in `abandonment.test.ts` with one id against three attempts.

**Code review:** `@code-review` — 1 finding, and the way it resolved is the lesson. **The migration carried a backfill justified by a deployment fact nobody had checked** — "those are dev rows and `db:reset:dev` clears them". My first fix hedged the sentence. The operator's answer made the real fix obvious: **there are no reservations in production, so the statement operated on nothing and the whole line went.** Three rounds of prose defending a statement is the argument against the statement, not a reason to write a fourth. Everything else it verified and passed, including that the Postgres increment rides inside the existing single `update … where id=$1` and is therefore a row-locked atomic increment. `/security-review` — 0 findings; it ran while the migration still had the `update`, and with that gone the migration no longer trips the trigger at all.
**Also of note:** the whole suite was run rather than the touched files, deliberately — `Reservation` gained a field and I could not name every `toEqual` on it. That found the one real casualty, the round-trip case at `repository-contract.ts:2208`.
**PR:** [PR #1029](https://github.com/mobiustripper42/muster/pull/1029)
**Points:** 5
**Branch:** task/15.9-count-checkout-attempts
**Opened at:** 2026-09-17T01:08:28Z

## Task 6: 15.10 — retire a superseded PaymentIntent instead of leaving it payable

**Completed:**
- `cancelPaymentIntent(id, reason)` on the payment port with `CancelReason` narrowed to `abandoned | duplicate`, verified against the pinned SDK's own enum (`esm/resources/PaymentIntents.d.ts:8353`), which also carries `fraudulent` and `requested_by_customer` — judgments nobody here is making.
- `src/reservations/create-departure-payment-intent.ts` — cancels the prior intent before minting a replacement. Reached on a failed state read, and on an update refused for a reason that was not "already paid".
- `src/reservations/booking-webhook.ts` — `retireSiblingIntents()` cancels every id on the row except the one that paid, after the flip commits. Order is load-bearing: cancelling first would kill a payable intent for a booking that might not commit.
- `payment_intent.canceled` as a named event, acked and doing nothing. **The plan called subscribing the Dashboard endpoint a required hand step; it is not** — we cancel through the API and read the result synchronously. The route comment says so now.
- Fake: `cancelled` recorder, `cancelError`, and `updateAmountRefusalState`. Cancelling in the fake flips the state and drops the live amount, so the tests assert nobody can pay rather than that a method was called.

**Two corrections to the task's own plan**, which described a port that no longer exists: there is no `in_flight` state or `stateByIntent` recorder, and its headline case — a remint on an abandoned 3DS challenge — cannot happen, because `requires_action` maps to `reusable` and 15.8 reuses it.

**The fake promised something it could not do.** Its refusal comment said a test could set `intentStates` "afterwards", but the state is written inside the throwing call and the caller re-reads at once — so every refusal resolved `already_paid` and the mint path was untestable. That is why one of my own spec's checks could not reach the path it named.

**Code review:** `@code-review` — 2 findings, both fixed, and the first is the one that mattered. **I retired siblings on the booked path and missed the row class where an un-retired intent is payable FOREVER**: the residual-race loser, whose flip is refused and which will never book. Nothing reaps lapsed rows and these two call sites are the only things that cancel an intent, so a stale tab could pay a duplicate charge against it indefinitely. Second: both swallowed cancels now log, matching every other best-effort catch in that file — "must not throw" and "must leave no trace" are different requirements, and conflating them makes an ordinary refusal indistinguishable from a permissions error. `/security-review` (money trigger) — 0 findings; it traced who controls the cancelled id on both paths and established no cross-reservation cancel exists.
**Proof honesty:** four checks watched failing. Three guards passed on arrival (nothing called cancel yet) and one case was written after its fix — both stated as such in the PR rather than counted as proofs.
**PR:** [PR #1034](https://github.com/mobiustripper42/muster/pull/1034)
**Points:** 5
**Branch:** task/15.10-retire-superseded-intents
**Opened at:** 2026-09-17T17:18:22Z

## Task 7: 15.11 — an idempotency key on the only money call that lacked one

**Completed:**
- `idempotencyKey` required on `CreatePaymentIntentInput`; `booking_${row.id}_${(checkoutAttempts ?? 0) + 1}` at the call site. The row id ALONE is the obvious key and the wrong one — constant for the life of a checkout, so a re-mint would be handed back the intent it is replacing.
- **The real defect was plumbing:** `buildPendingRow` was not carrying `prior.checkoutAttempts`, so every attempt computed ordinal 1 and the key would have collided on precisely the re-mint it exists to separate. 15.9's column was right on disk and wrong in the object the checkout holds.
- The fake caches by key **and compares parameters**, throwing on a mismatch as Stripe does — a fake that cached where Stripe 400s is strictly more permissive than production.
- `db:stripe:cancel` extended: two creates with the same key must return the same intent id.

**The investigation was worth more than the change**, and the operator pushed on it correctly from the start. The double-tap it was pitched on is impossible (`checkout-form.tsx` disables the button synchronously before the first `await`). Two genuinely concurrent calls do not make two intents on one row — **they make two rows**, because the same-token exemption in `pendingOverlapSql` cannot tell a retry from a duplicate even under the hull-day lock. No key dedupes across two row ids. Demonstrated with `Promise.all` (`ROWS: 2`), filed as a comment on issue #806 whose own fix closes it, and the 5-point atomic-claim fix considered for it was **deliberately not built**: a script is the only caller that can reach the window, and a script already has an easier door through #806.

**Code review:** **Both passes independently found that the task did not work.** `stripe-payment.ts` was never in the first commit — `paymentIntents.create` took a single argument with no options object, so the key was computed, typed, threaded, and then **discarded before it reached Stripe**. Production behaviour was byte-identical to before the commit. The whole suite was green and could not have been otherwise: every test runs against `FakePaymentPort`, which honoured the key, and there is no `stripe-payment.test.ts`. `/security-review` filed it as *"not a vulnerability, but the diff does not do what it claims"* — the reading I would have missed, hunting a hole rather than an absence. Fixed in `9d2b817` with one options argument. **That is twice in two tasks the fake-vs-Stripe gap hid something real** (15.10's cancel had never executed either), which is issue #1021's case made concretely twice over. Two further `@code-review` findings declined with reasons in the PR: a concurrent ordinal collision that throws unhandled (compounded rarity on an unreachable path, and not new — any create failure has always 500'd), and `recordCheckoutAttempt` not deduping a repeated id (data noise on the same unreachable path).
**Honest verdict, stated in the PR:** this fixes nothing that could be demonstrated. Three of the four justifications did not survive being checked.
**PR:** [PR #1036](https://github.com/mobiustripper42/muster/pull/1036)
**Points:** 2
**Branch:** task/15.11-idempotency-key-per-attempt
**Opened at:** 2026-09-18T22:06:58Z

## Task 8: 15.12 — name `payment_intent.processing`, and give the adapter its first tests

**Completed:**
- `payment_intent.processing` was falling through `parseEvent` to `null` — "a booking is settling over the next few days" was indistinguishable from an event type we have never heard of. Named `payment_processing` on `PaymentEvent`; the handler acks **and alerts**, the only ignored event that tells anybody. A decline is silent because the customer still has a second card; a cancel is silent because Muster made it; this is a payment in flight that no screen shows, and its arrival means a delayed method was enabled in the Dashboard — a change with no diff in this repo.
- `src/adapters/stripe-payment.test.ts`, the adapter's first tests at 491 lines of zero coverage. **Covers `parseEvent` and nothing else, deliberately**: it is the only pure function in the file, and unit-testing the rest means `vi.mock("stripe")`, mocking the thing under test. The network half already has `db:stripe:cancel` against the sandbox. Payloads signed with `generateTestHeaderString`, so the verification path the route depends on is exercised rather than stubbed.

**The scope collapsed from 8 to 3, and the reason is that the operator ran two commands nobody had run.** The task was "gate hosted fulfilment on `payment_status`". Reading the account showed **no delayed method enabled** on the default configuration, so the gate guarded nothing; `recordPostGratuity` is deleted by 15.18; `recordBalancePayment` is dormant and moved to issue #712 with the deposit-mode overhaul. It also **corrected issue #1012's premise** — `link` is on and `us_bank_account` is off on the same config, so "Link forces ACH" is not what the API reports. And there is no Muster webhook endpoint on the account at all; the two that exist are sailbook's, both pinned to `api_version: 2019-03-14`.

**Code review:** `@code-review` — 1 finding, real, **deliberately not fixed**: the alert has no dedupe, in a file where every other side effect is built for Stripe's at-least-once redelivery. A redelivered `processing` pages every admin again — the same crying-wolf noise the branch's own comment argues against two lines up. Shipped that way on the operator's call, because the branch **cannot execute today** and a dedupe for an unreachable path is machinery ahead of need. Written down twice instead: at the branch, and on issue #712, both naming the cheap fix (`claimRecoverySend` is already a keyed claim with lazy expiry; no migration needed). `/security-review` — 0 vulnerabilities, and it caught a real slip: `PaymentProcessing` had been inserted between `PaymentCanceled`'s docblock and its interface, orphaning the doc.
**Proof honesty:** four genuine failures watched. Nine of the ten adapter tests passed on arrival and are stated as characterization, not proof — the most valuable being that a bad signature throws `PaymentSignatureError` specifically, which is what makes the route answer 400 instead of 500.
**PR:** [PR #1040](https://github.com/mobiustripper42/muster/pull/1040)
**Points:** 3
**Branch:** task/15.12-test-adapter-name-processing
**Opened at:** 2026-09-19T13:23:23Z

## Task 9: 15.13 — carry the Stripe event id, so a failure names its delivery

**Completed:**
- `stripeEventId` on the `PaymentEvent` **envelope** — an intersection over the union rather than a field on all seven members, so there is one place to read and `.type` narrowing still works. Populated at every `parseEvent` return; defaulted in the fake so dozens of hand-built fixtures keep working.
- Named `stripeEventId` and never `eventId`, because `eventId` means the departure throughout the reservations code. `PaymentSucceeded` deliberately carries none: `/book/success` synthesizes its charge from a provider read with no event behind it, so a field there would be real on one path and invented on the other.

**The design changed mid-build and lint caused it.** My first cut put a `console.error` in `processBookingWebhook`; the core's rule from #902 refused it, and `logSwallowed` was the wrong tool anyway — its whole meaning is an error that was NOT rethrown. **The rule was right and a better design fell out of it:** the core adds no logging at all, wrapping the failure in an error whose message carries `evt_… (type): <original>` so the route's EXISTING line carries it. One log line instead of two needing correlation, and no I/O added to the core. `cause` keeps the original, because `${e}` renders "Error: boom" and drops the stack.

**Code review:** `@code-review` — **0 findings, clean bill of health**, and it earned that: given five attack points it checked each against the code rather than the comments. It verified by line position that `parseEvent` throws above the wrap (so a forged request still gets its 400), that the `!event` short-circuit stayed outside the extracted `routeVerifiedEvent`, that the intersection narrows at every `event.type ===` site across `src/` and `app/` rather than only the touched files, that the fake's ordinal is not consumed by a `null` return, and that `cause` is not flattened downstream. It also noted something I had not: the route reads only `.message`, never `.cause` — pre-existing, and harmless today because the message carries both halves. `/security-review` — 0 findings; confirmed the 500 returns a fixed body so the wrap discloses nothing new, and that `stripeEventId` reaches exactly one template literal and no key, query or comparison.
**Proof note:** two checks watched failing, then **four of 15.12's exact-equality cases went red when the field landed** — which is the real confirmation it reaches every member, since I had only asserted on two types.
**PR:** [PR #1043](https://github.com/mobiustripper42/muster/pull/1043)
**Points:** 3
**Branch:** task/15.13-carry-stripe-event-id
**Opened at:** 2026-09-19T21:22:23Z

## Task 10: 15.14 — two decision records: the webhook stays synchronous, and un-allowlisted

**Completed:**
- **DEC-178** — the 500 IS the durability. Acking 2xx first and deferring into `after()` reads as the resilient shape and is the opposite: Stripe retries a non-2xx for three days and never returns for a 2xx, so it turns a recoverable delay into a silent loss.
- **DEC-179** — the signature is the door. An allowlist is a dashboard rule invisible to the repo, and Stripe rotates addresses on seven days' notice, so a stale list 403s every delivery for three days — the failure the signature check prevents, reintroduced by the mitigation.
- Both carry a **correction rather than the claim it replaces**: a `tolerance` of `0` does NOT disable the recency check (`tolerance || DEFAULT_TOLERANCE`, which is 300). An earlier draft asserted otherwise, and a decision record is exactly where a wrong claim becomes permanent and citable.
- Decision search first, per convention — `webhook`, `synchronous`, `allowlist`, `idempot` returned DEC-040, DEC-002, DEC-109, DEC-177 and others, none on either subject. New ids, not supersedes.

**The issue asked for one record and the gate made it two, correctly.** `check:decisions` refused the single file at 2255 bytes with its own words — *"a decision that will not fit is more than one decision"* — and the tell was already in the frontmatter: different `revisit_if`. The synchronous one reopens when §2.8.9's reconciler ships; the allowlist one reopens if the rule can live in this repo or Stripe stops rotating. It also rejected an over-long `revisit_if` and a bold-prose paragraph opener. Every one of those was a genuine problem.

**Code review:** `@code-review` — 3 findings, and **the two substantive ones were both mine to own.** First: I asserted `supportsCancellation` — a third-party config key — as fact, taken from the phase plan with nothing in or out of this repo corroborating it. That is the move CLAUDE.md bans for code, committed in the one artifact whose whole mechanism is making claims checkable. Verified against Vercel's *Functions API Reference* before re-asserting; it is real and the docs call it "an opt-in feature that must be enabled". Second, and the better catch: **the record defeated the wrong argument** — it answered client-disconnect and never touched platform timeout, which is what an `after()` proposal is actually worried about. Closing it strengthened the ruling rather than weakening it: a timeout sends no 2xx, so Stripe redelivers and the second delivery resolves `already`; acking first is precisely what would break that. `/security-review` — not run, no blast-radius trigger (docs only).
**Note for the next session:** the `/security-review` skill has been unbundled into the `claude-security` plugin (lane A found this; operator has installed it). `.claude/skills/kill-this/SKILL.md` still names `/security-review` in Steps 3.5 and 3.6 — jig-canonical, needs a jig session, not an edit here. The passes I ran for tasks 5–9 were an agent carrying that prompt, not the skill.
**PR:** [PR #1045](https://github.com/mobiustripper42/muster/pull/1045)
**Points:** 2
**Branch:** task/15.14-dec-webhook-synchronous
**Opened at:** 2026-09-20T11:31:49Z

**Next Steps:**

**Context:**
- Linked worktree (`muster-s91`), on `main`. Concurrent live session 105 (main checkout) is refactoring formShift; this session is phase 15 (lane:b, reservations/Stripe).
