---
session: 107
slug: main
branch: main
started: 2026-09-14T03:04:10Z
ended:
points:
pr_numbers: [1018, 1020, 1022, 1025, 1027]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/b300916f-c7a6-5e1a-b0f5-8264782bb87a.jsonl
---

# Session 107 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: One unmanned boat no longer 500s the cron for the whole fleet (#1017)

**Completed:**
- **The estimate came down before any code was written, and that was the whole first half of the task.** I specced the issue as written — a per-shift `try` mirroring #957's isolation, a `failures` array, a new alert lane sharing `forwardFormationAlert`'s grouping, 3 points. Eric's pushback was *"this seems like a lot of work for made up reasons — what can actually throw?"* and he was right: I had written "a bad date, an unreadable seat, a future derive rule" as a **class** without checking any of it. The actual enumeration is one data throw (`deriveShiftState`'s zero-required-seats guard, `derive.ts:85`, #582) plus repo calls, and repo calls are not per-shift — if the pool is down, `saveShift` fails for every shift too, so isolating them turns one outage into N entries while nothing advances either way. He picked the 1: skip and record.
- **The guard** in `src/builder/tick.ts`, after the seats load and below the past-trip guard — no extra repo call, and a departed shift already `continue`s above without ever reaching a derive, so it is not stuck and is correctly not reported. `TickResult.unmannedShiftIds` names what was skipped; the route logs it and reports `shiftsSkipped`.
- **The failing test found the second site.** `deriveAtRiskBoard` (`at-risk-board.ts:249`) calls the same deriver, so the tick's post-loop board-landing pass threw eleven lines after the loop for the same reason. Fixing only the work loop would have moved the crash, not removed it.
- **`@code-review` found the third**, and it is the one that mattered most on screen. `deriveAllShifts` (`all-shifts.ts:174`) resolves every non-terminal row through `resolveShiftStateOnRead`, and `/admin/shifts` catches at the **top** of its render — so one seatless shift blacked out the entire cockpit, every vessel and every date, behind "Can't reach the schedule right now." The crew app's "other boats today" panel hit the same throw behind a `.catch(() => [])` and rendered empty. Fixed at the source: `resolveShiftStateOnRead` returns `null`, the same answer it already gives for a shift it cannot find, so every caller's existing `?? shift.state` fallback covers it — present readers and future ones.
- **No new alert lane**, and the reasoning was verified rather than assumed: `formShifts` fails on the same vessel-day earlier in the *same* cron invocation, so #1001's text has already named the boat. One stated limit, from the review: `reformWindow` scans a bounded date range while the tick sweeps every non-terminal shift, so a bad shift dated outside that window is counted with no alert sent. Written into the docstring rather than implied away.

**Code review:** 2 findings, both fixed in `fac8a0e`. **The real one is a census failure, not a code failure** — I had grepped for the deriving symbols and found two sites; the third was reachable only by following callers through `resolveShiftStateOnRead`. It also verified five claims I had asserted and could have been wrong about: that the completion sweep, the DEC-067 ask sweep and `poolExhaustedFor` all no-op before the new guard rather than throwing ahead of it; that `refreshShiftState`/`refreshShiftStateHorizon` are unreachable once the guard `continue`s; that skipping is right for the At-Risk board because every reason there is a gap measured against a required set; that `formShifts` really does fail on the same vessel-day; and that the board test would go red without the guard rather than passing vacuously — the one I had explicitly flagged as unverified.
**`/security-review`:** not run — no blast-radius trigger. Checked by the fallback question rather than by the path list, because `app/(admin)/admin/shifts/**` is on the money-computed row and this changes what that page renders: `payroll.ts:44` and `gratuity-payroll.ts:110` both read the **persisted** `shift.state`, never this resolver, and a seatless shift contributes zero seats to either loop with or without the change.
**Proof:** 4 new cases, 3 watched failing first with the expected error — the tick case reproduced the issue's exact three-frame stack. The At-Risk board case is the exception and is flagged as such in the PR: its failure was observed through the tick test's stack at `at-risk-board.ts:249` rather than independently, since writing it after the guard existed and then deleting the guard to watch it go red is the churn that is not allowed here. Gate green: 166 files, 2615 tests.
**PR:** [PR #1018](https://github.com/mobiustripper42/muster/pull/1018)
**Points:** 1
**Branch:** task/1017-tick-skips-unmanned-shift
**Opened at:** 2026-09-14T04:45:00Z

## Task 2: One answer to an unset APP_BASE_URL, and previews get their own origin (#1007)

**Completed:**
- **The issue's premise was wrong, and correcting it was most of the value.** It framed an unset `APP_BASE_URL` as a misconfiguration and proposed two branches, fatal in prod and localhost in dev. DEC-057 says the variable is scoped to Production only *on purpose*, so a preview's minted links resolve to the preview — meaning unset is the designed state in the one environment built for smoke-testing, and a two-branch rule has no answer for it. `appBaseUrl()` has four branches over three environments; the preview one falls back to `VERCEL_URL`, which appeared nowhere in the repo before this.
- **The defect underneath: three sites keyed their prod check on `NODE_ENV === "production"`** — `alert.ts`, `doorbell.ts`, `channel.ts`, each citing the others as precedent. Vercel sets that on previews too, which is the exact failure `src/config/deploy.ts:22-26` was written to name. So the At-Risk alert, the doorbell and the ask relay **threw on every preview deploy**, unnoticed because preview crons do not fire on a schedule.
- **Eleven sites, not the four the issue listed.** I found eight by grep; `@code-review` found three more that deliver links, including the one I had explicitly flagged as uncertain and asked it to settle — the balance link an operator copies and texts, which on a preview read `http://localhost:3000`.
- **A boundary, stated rather than left to be guessed:** no request in hand → `appBaseUrl()`; a request in hand rendering a link in-page → the request's own origin. `crew/calendar/page.tsx` is the clean case of the second and is deliberately untouched.
- **Behaviour changes, all deliberate:** a preview can now send a booking confirmation and a booking-recovery link (both were skipped outright); `resendReservationLink` loses `not_configured` along with its pane copy and test row; `channel.ts` loses its `headers()` fallback, deleting a host-header path and adding none.

**Code review:** 6 findings, 5 real, all fixed in `3102c73`. **The census was wrong for the second time in one session** — I asserted "everything else is request-scoped page code" without checking each member, exactly the #1017 failure. **The finding worth remembering is the e2e one:** removing `channel.ts`'s `headers()` fallback left every relayed ask and notice in the Playwright suite minting links against `localhost:3000` while the harness serves on 3100 — and **no spec asserts the host of a minted link, so the whole suite would have stayed green while being wrong.** Fixed by pinning `APP_BASE_URL` in `playwright.config.ts`, which is the rule the resolver already states. Also: `app/api/cron/doorbell-tick/route.ts` had no try/catch anywhere in its chain, against my claim that all eight callers were covered.
**`/security-review`:** ran (`app/b/**` is on the auth / capability-URL trigger list) — **0 findings at confidence ≥ 8**. It cleared the four real questions: `VERCEL_URL` cannot yield a non-https scheme or an attacker origin; the `isProdDeploy()` swap is strictly *narrower*; removing `headers()` deletes a host-header path; and the `b/find` gate removal is not an enumeration oracle because `recoverBookingLink` sends to the **stored** contact and the redirect is unconditional. One sub-threshold note carried into the PR rather than dropped: preview logs now carry live crew magic links on paths that previously threw, which is only safe because DEC-057 puts previews on an isolated Neon branch — cited from the decision record, not from a Vercel setting readable here.
**Proof:** 6 new cases in `app/lib/base-url.test.ts`, all watched failing first. Includes the self-hosted prod door (`VERCEL_ENV` absent, `NODE_ENV=production`), which a test of only the Vercel shape would miss. **The call sites are deliberately not separately tested** and the PR says so: they are one-line substitutions covered by `typecheck:app`, and testing them means mocking `getRepo`, which builds a real pg Pool at module load. The preview surface check is what proves them. Gate green: 167 files, 2620 tests.
**PR:** [PR #1020](https://github.com/mobiustripper42/muster/pull/1020)
**Points:** 3
**Branch:** task/1007-one-base-url-answer
**Opened at:** 2026-09-15T03:05:00Z

## Task 3: Previews are not a test surface — the context note and DEC-173 (no issue)

**Completed:**
- **Not a planned task; it came out of two hours of chasing a preview 500 that was never a bug.** A preview deploy of the #1007 branch returned `"db":{"reachable":false}` because muster's Neon project had been deleted — branch-per-preview had produced a large bill. Previews build, serve, and cannot reach Postgres.
- Four lines in `.claude/CLAUDE-context.md` beside the `Surface check` slot, written as a rule with its reason rather than inventory, plus the local recipe that replaces deploying: `VERCEL_ENV=preview VERCEL_URL=<host> npm run dev`.
- **DEC-173 supersedes DEC-057**, whose own `revisit_if` had fired verbatim: *"previews ever stop being isolated branches."* DEC-057 is frozen, so it keeps its text; the checker permits a one-directional supersede for a legacy target. Not archived — its `VERCEL_ENV` gate was still live at that point.
- Eric turned preview builds off entirely at the Vercel dashboard (Ignored Build Step keyed on `VERCEL_GIT_COMMIT_REF`), after I read the schema and confirmed `vercel.json` cannot express it: `git.deploymentEnabled` takes a boolean or an explicit branch map with no wildcards, and there is no `ignoreCommand` key.

**Code review:** not run — documentation only, no code in the diff. Stated on the PR's receipt rather than left ambiguous.
**`/security-review`:** not run — no blast-radius trigger.
**Proof:** `check:context` and `check:decisions` green; full gate green at 167 files / 2620 tests.
**One gotcha worth carrying:** `supersedes: [DEC-057]` as an inline YAML flow sequence is **silently dropped** by `gen:decisions` — the frontmatter round-trips without it, every gate passes, and the supersede link is gone with no error anywhere. Block style survives. Caught by grepping the file after regenerating, not by any check.
**PR:** [PR #1022](https://github.com/mobiustripper42/muster/pull/1022)
**Points:** 1
**Branch:** task/context-previews-not-a-test-surface
**Opened at:** 2026-09-15T03:40:00Z

## Task 4: Delete /crew/dev-link and db:mint — one door in (no issue)

**Completed:**
- **Came out of asking what the `VERCEL_ENV` gate actually protects.** `/crew/dev-link` was an unauthenticated route minting a signed-in session for any crew id or admin handle; `db:mint` did it from a terminal. Neither had been used in months.
- **I got the blocker wrong twice and Eric corrected both.** First I said the code flow mints crew sessions only (true, `login-code.ts:171`) and therefore `db:mint` had to stay as the admin path. He said he signs in as crew and switches — `switchToAdmin` (`app/lib/switch-actions.ts:36`), which I had not found. That killed `db:mint` too.
- **52 of 56 e2e specs sign in**, all through two fixtures. They now mint in Node against the test DB and tap the real `/crew/auth` interstitial — which dev-link's own "dev shortcut" button had been skipping, so the suite is *more* production-like than before.
- **Order was the whole safety argument:** fixture rewrite first, in its own commit, with the route still present as a control. 434 passed then, 434 after.
- DEC-174 supersedes DEC-034. Two consequences recorded in it rather than discovered later: email is now load-bearing for admin access, and the login-code cap has no bypass.

**Code review:** 5 findings, all fixed in `1b9e978`. **The real one: my equivalence claim was false in one direction and the gap was reachable through a command.** The minters took any crew id; the code flow needs an email (`matchCrewByEmail`, `login-code.ts:150`). `db:admin add --crew=<id>` never checked for one — harmless while a minter existed, and after this PR it would have created an admin with **no path to `/admin` at all**, silently. The new guard bit five existing tests immediately, which is how I know it works.
**`/security-review`:** ran (auth trigger) — **0 findings at confidence ≥ 8**. It verified `e2e/` is in no tsconfig profile and unreachable from `app/`, that `TEST_DATABASE_URL` does not fall through to `DATABASE_URL`, and that `switchToAdmin` re-reads the admins row rather than trusting the cookie. It noted the deletion **removes** a leak: the route's 400 enumerated every active admin handle to an unauthenticated caller. Its one fidelity note — the fixture had dropped the route's `!record.active` check — is fixed in `49e3ca4`.
**Proof:** e2e run twice, before and after the deletion, both 434 passed. Gate green at 167 files / 2619 tests.
**PR:** [PR #1025](https://github.com/mobiustripper42/muster/pull/1025)
**Points:** 5
**Branch:** task/remove-dev-link
**Opened at:** 2026-09-15T06:30:00Z

## Task 5: Delete CREW_SELF_SERVE — a flag whose off-state ran nowhere (#1024)

**Completed:**
- **Filed and built in the same session, off the back of asking what a gate protects.** The flag was set in production and set in `playwright.config.ts`, so its off-branch executed in no environment. Ten guards defending a state that did not exist.
- DEC-081 shipped the code login dark for a good reason — a login saying "check your email" while emailing nothing is broken. Email got wired, the flag went on, and from then on what it bought was an **untested path**: nobody knew what `/crew` rendered with it off.
- **That path included a whole second signed-out screen** telling crew to tap the link their operator sent — an instruction Task 4 had made impossible to follow, for nobody, ever. The `reason` prop (`sp.auth`) went with it, read only by that screen.
- Two comments had inverted: `flags.ts` still said "OFF by default", `/crew/open` still said "404 in prod until the flag flips". Deleted rather than reworded, because a comment stating the opposite of the code is worse than none.
- DEC-175 amends SPEC §2.7, which described the surface as dark by default and coupled to the login door by one env var. Both halves false.

**Code review:** 2 findings, both fixed in `fc2bd70`. **The real one is procedural and is the second instance of the same silent YAML drop in one session.** DEC-175 claimed to amend §2.7 in prose while its frontmatter declared nothing — I wrote `amends_spec: ["2.7"]` as an inline list, the parser wants block style with `section` and `scope`, and it dropped it without a word. So `check:decisions` never tracked the amendment, SPEC never got its generated banner, and **the gate passed clean by not looking**. Identical shape to the `supersedes: [DEC-057]` drop in Task 3. The amendment count moving 31 → 32 is the only external way to tell it landed. Second finding: I deleted an acceptance criterion with no successor, shrinking the §2.7 checklist six items to five with no note; replaced with what actually gates the surface.
**`/security-review`:** not run — no blast-radius trigger. Checked by the fallback question: this changes whether a surface renders, never a number reaching a statement.
**Proof:** **e2e green at 434 without the harness override**, which is the actual evidence the off-state was never load-bearing. The flag's own census test caught the deletion — `expect(FLAGS.length).toBe(4)` went red the moment the export left the module, which is why it counts rather than iterating what happens to be imported. Gate green: 167 files, 2607 tests, 32 landed amendments.
**PR:** [PR #1027](https://github.com/mobiustripper42/muster/pull/1027)
**Points:** 2
**Branch:** task/1024-delete-crew-self-serve
**Opened at:** 2026-09-15T20:45:00Z

**Next Steps:**

**Context:**
- **One piece v4 was committed straight to `main` (`4716f98`), outside any task.** Eric's own edit, sitting dirty in the tree since before this session opened. It lands in muster only, on purpose: muster is where he tests the style. **Porting it to jig is not a pending task and is not to be proposed** — his ruling, 2026-09-14. `drift.mjs` will keep flagging `one-piece.md` every session; that flag is expected and is not a finding.
- **Session 106 is live in the `muster-s91` worktree on phase 15, lane B.** It holds 15.6 (issue #974) and will close issue #973 itself — that issue is still open because PR #1014's body wrote `` `closes #973` `` inside backticks, which GitHub does not parse. Nothing in this session touched reservations.
- **The phase 15 spine is strictly sequential** per the plan comment on issue #966: 15.6 → 15.7 → 15.8 → 15.9 → 15.10. A lane A session picking up phase 15 work should take something off that spine — 15.12 (issue #980), 15.14 (issue #982) and 15.20 (issue #988) are the independent ones.
- **`npx` is denied fleet-wide** (`.claude/settings.json`), so the vitest form is `npm test -- <file> -t "<name>"`. Two denials on materially the same command before I checked the deny list, which was two more than necessary.
- **Writing a spec inside a fenced code block is wrong here.** Fences do not wrap and Eric reads on a phone. Specs go in ordinary markdown at whatever length they need; the artifact clause in One piece v4 is what makes the length legitimate, not the fence.
- **The pattern worth carrying from this task: a class asserted is not a class checked.** My first spec named three throw classes and two of them did not exist. The honest enumeration made the task a 1 instead of a 3 — and then the real breadth turned out to be in a direction I had not looked at all, three *callers* of one guard rather than three *kinds* of failure. Census by following callers, not by grepping symbols.
