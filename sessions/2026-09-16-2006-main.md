---
session: 108
slug: main
branch: main
started: 2026-09-16T20:06:55Z
ended:
points:
pr_numbers: [1032, 1035, 1037, 1038, 1039, 1042, 1044, 1046, 1056, 1057, 1059, 1060, 1064]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/d12028b2-982c-5fde-a0a9-aca201fca6f9.jsonl
---

# Session 108 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Crew see the other shifts, not just the holes in them (#968)

**Completed:**
- **The design was Eric's, iterated in four passes, and each pass made the task smaller.** Issue #968 named three candidate behaviours and said to pick one; he picked **social proof** — *"I want them to feel part of a team. The empty screen seems like nothing is happening."* I specced two sections vs a Claimable/All filter and recommended the filter; he corrected my misreading (the delimiter was two top-level sections, not a per-day split), which solves the scroll complaint directly and needs no tap. Then: *"Basically, the screen is exactly like it is now and we add the rest of the shifts below for display"* — which deleted the whole merge-claimable-into-fleet-rows half of my spec and took it **5 → 3**.
- `src/crewapp/team-view.ts` — a crew-safe projection over `deriveAllShifts`, clamped and memoized. The exclusions are enumerated in the docstring rather than spread through a mapper: no `pax`/`paxTotal`, no shift state, and `Asked`/`Bailed`/`Open`/`Claimed` collapse into one indistinguishable "unfilled" so the DEC-008 drip order is underivable.
- `app/(crew)/crew/open/page.tsx` — the claim list untouched; a second display-only section below a rule. `groupByDay` made generic so both sections share one day spine.
- **DEC-176**, amending SPEC §2.7 with a new §2.7.1a. Decision search run and reported on the PR: `grep -rli` for fleet visibility returned only DEC-071/DEC-072 (messaging cross-visibility), so new id, no supersede. Amendment count 32 → 33, checked — the only external evidence it landed rather than being dropped, which is session 107's gotcha twice over.
- **Four rounds of visual correction, all his, all right.** Headings needed to be bigger and bold. "What the team is running" → **"Other shifts"**, because *"the boats are not out, or running. These are not even boats, they are shifts in the future"* — present tense was wrong in three places, not one. Rows needed a border with no fill. Then the killer: a screenshot with the grey text circled — `text-faint` at 2.36:1 on a fill-less row, which is [issue #951](https://github.com/mobiustripper42/muster/issues/951) and I had reached for it anyway. Swept the page; only the decorative chevron still uses it.

**Code review:** `@code-review` — **4 findings, all confirmed, all fixed.** The real one: `buildTeamView` passed the caller's window into `deriveAllShifts` **unbounded**, while its sibling `claimableSeatsFor` clamps to `[today, today+45d]` internally and only lets a caller narrow it (`claimable.ts:84`). `range` comes from `searchParams`, guarded by an HTML `min`/`max` — a hint to a browser and nothing to a URL. So `?from=2000-01-01&to=2099-12-31` ran the issue-#960 fan-out over every shift the fleet has ever had, on a screen meant to be opened habitually. **That URL is literally the `ALL` constant my own new e2e tests use**, so every green run I had reported was exercising the unbounded path — the tests were the alibi. Also: `listAllSeats` cached nowhere despite being a full seat-table read reached once per understaffed shift (`oracle.ts:70`) — added to `memoizing-repo`'s `SINGLE`, which is a shared-adapter change and why the full unit suite ran; `openRoles` counted unfilled trainee seats as gaps, untested because the fixture hardcoded `kind: "required"`; and the memoization docstring overclaimed what the wrapper covers.
**`/security-review`:** not run. I proposed it on the grounds that this is a new surface where one crew session reads fleet-wide data about others; **Eric's call was that it wasn't justified** — no blast-radius row hit, and the privacy question was the one `@code-review` had already answered structurally (`crewName` gated to `Confirmed` upstream at `all-shifts.ts:202-205`; `AllShiftsSeat` never exposes `assignedCrewMemberId`, so no wrong implementation here could reach one). He was right and the receipt says so rather than leaving the absence to be inferred.
**Proof:** 7 unit cases, 20 e2e on desktop + mobile at 375px, 755 tests across the memoizer's consumers, both typechecks, lint, build, doc gates. **Stated plainly on the PR: only the two post-review cases were watched failing on a real assertion** — the original five failed as `Cannot find module` and the original two e2e passed first run.
**Two issues filed from things found on the way:** [#1030](https://github.com/mobiustripper42/muster/issues/1030) to retire magic tokens from ask links (Eric: *"adds security complexity for what seems like zero added value"*), flagging that `magic-link.ts` also supplies the DEC-098 calendar-feed token so the file survives the removal; and [#1031](https://github.com/mobiustripper42/muster/issues/1031) for the contract test's truncate list, which covers **37 of 45 tables**. He asked whether the three I had named were really all of them — they were not, there are eight, and I had only listed the three that happened to hold rows.
**PR:** [PR #1032](https://github.com/mobiustripper42/muster/pull/1032)
**Points:** 3
**Branch:** task/968-team-schedule-below-claims
**Opened at:** 2026-09-17T03:30:00Z

## Task 2: Every database connection carries the Crunchy CA, not just the app's (no issue)

**Completed:**
- **Unplanned. It came out of `/promote-production` refusing to run**, and the gate was right: its pre-promote check told me to read prod's migration ledger via the Neon MCP against project `delicate-art-65084110`, which 404s. Listing Eric's Neon org returned one project holding two migrations and a `push_tokens` table — a different application entirely. Production had moved to Crunchy Bridge weeks earlier and no document said so.
- **The fix Eric asked for, after I twice proposed a smaller one.** I fixed `db/migrate.ts` alone, he ran it, it worked, and then he found the other sixteen: *"fixing db:migrate should have fixed all things, but I find out it's not."* The trust decision moved **into** `PostgresRepository.fromConnectionString` (`src/adapters/postgres-repository.ts`), which covers all 17 `db/` scripts and the e2e fixtures in one edit, plus `/api/health`, `backfill-customers`, `reset-pilot`, `reset-dev`, `reset-test`. `db-ssl.ts` moved `app/lib/` → `src/config/` so `db/` can reach it without importing from `app/`.
- **A live production defect, found by asking a question I could have skipped.** I predicted from reading `app/api/health/route.ts:30` that the probe builds its own pool and should be reporting the database unreachable in prod. Eric curled it: `"status":"degraded"`, `"db":{"reachable":false}`. It has been saying that since the cutover while the app worked fine. **That prediction-then-check is the only reason it was found** — nothing in the task required looking.
- **DEC-177**, the record that never existed. DEC-033 left the hosted-Postgres provider **OPEN** and nothing ever closed it, so neither Neon nor Crunchy was recorded. That absence is the mechanism: no decision, no `revisit_if`, nothing to make anyone check the runbook. `docs/DEPLOY.md` and `.claude/CLAUDE-context.md` rewritten; the provisioning walkthrough is marked `NEON — NEEDS REWRITE` rather than invented, because nobody has stood Crunchy up from scratch.
- Prod's ledger was **two migrations behind** — `db:migrate` applied `confirmation_sent_at` and `checkout_attempts` the moment it could connect.

**Code review:** `@code-review` — **4 findings, all fixed.** Two were **me asserting facts about Crunchy I never verified**, in a runbook meant to be followed without me present — exactly the failure the task was fixing. I wrote *"it is set manually in Vercel's env — there is no auto-injecting integration"* having checked nothing; the page now refuses to say, and names the gap. I also carried a Neon-specific `vercel env pull` warning across with only the noun changed. The third was vocabulary: the CLI section still said "direct/unpooled" two sections after the doc retires that distinction, which would send an operator hunting a second connection string that does not exist. The fourth was the sharpest: **`fromConnectionString` had no test**, and `db-ssl.test.ts`'s own header says *"testing the helper is not testing the connection"* — so the helper was tested and the one call site seventeen scripts reach was not. It also ran its own census rather than trusting my grep, checked `package.json` for a second driver and `scripts/*.mjs` for database access, and read `eslint.config.mjs:474` to confirm the `eslint-disable` I removed was genuinely out of scope.
**`/security-review`:** not run — no blast-radius row hit. TLS being the subject argues for it, but the change strictly *adds* certificate verification where there was none and `pgConnectionConfig` itself is unmodified.
**Proof:** full suite 2632 / 168 files, three typecheck profiles, lint, build, five doc gates. **The real proof was live:** `db:migrate` against production succeeded. Stated on the PR: the two new `fromConnectionString` cases were written after the code and watched failing only on a mock defect, so they are coverage, not proof.
**PR:** [PR #1035](https://github.com/mobiustripper42/muster/pull/1035)
**Points:** 3
**Branch:** task/968-crunchy-tls-everywhere
**Opened at:** 2026-09-17T22:20:00Z

## Task 3: One way to record a swallowed error in the core (#902)

**Completed:**
- **The task doubled in scope because Eric rejected my recommendation, and he was right.** I proposed fixing the 23 silent `catch {}` in `src/` and leaving the 13 hand-rolled `console.error` calls alone as churn. His answer: *"We are building a logging routine. Apparently we have identified numerous cases where 'functional' is the same ... right up until we have a helper and then they diverge without anyone noticing."* That is not hypothetical — **it is what Task 2 cost, the same evening**: `pgConnectionConfig` existed, one caller of eighteen used it, seventeen hand-rolled the equivalent, and they stayed equivalent until production changed databases. 5 → 8 points, and the deliverable became *one way*, not *23 fixes*.
- `src/log.ts` — the core's `logSwallowed`, mirroring `app/lib/swallowed.ts`. A separate function because `src/` is framework-free and cannot import from `app/`; `src/log.test.ts` mirrors the app test case for case, which is what stops necessity becoming divergence.
- **Three lint selectors scoped to `src/**`, each tested against sample code before being wired in** — the bare catch, a `console.*` inside a catch, and `.catch()` with a zero-parameter arrow or function. The third is the same rule restated rather than a new one: a handler that binds no parameter cannot log what it caught.
- **35 sites, not the 23 the issue claimed** — grep found 23, the rule found 35. 33 log with a consequence naming what is now untrue; 3 are genuine exceptions with a disable saying which. 14 were on the money path. `db/` excluded deliberately, stated in three places.

**Code review:** `@code-review` — **3 findings, all fixed**, and the first is the one I had explicitly asked it to hunt and missed myself. **This PR created a leak.** The six `forward-*` relays all wrap `channel.send()`, `email-channel.ts` throws with the provider's raw response body in the message, and before #902 that error was discarded — so I was newly writing a crew member's sign-in code to production logs. **My first fix was wrong and a test caught it:** I stripped the detail from the throw and `email-channel.test.ts` went red, because surfacing it is a deliberate tested contract. **Reverted the source rather than editing the test** — the narrowing belongs at the log, where this PR created the problem. Shipped instead: `ChannelSendError` carrying `status` as a property, and one `describeSendFailure()` redaction for all six relays, with five tests written as a security boundary asserting what it does *not* return. **Eric settled the framing** — Resend sends a 6-digit code not a link, and #1030 retires Twilio's link, so it rests on "credentials don't go in logs" rather than on what a provider echoes, and the docs question disappears. Second finding: **`parseIdArray`'s consequence named the wrong column entirely** — a payment-intent-reuse story written for a function that parses `shift_changes.added`/`removed`. Plausible, confident, false; the exact defect class the consequence strings exist to prevent, found in the PR that introduces them. Third: the `.catch()` selector missed `function () {}`.
**`/security-review`:** not run as a separate pass. The blast-radius table hits, and I pointed `@code-review` at the secret-leak question directly instead — it found the one real instance, so a second local pass would have re-asked an answered question. Stated on the PR with an offer to run it.
**Proof:** **the lint rule going 35 → 0 is the census and the proof.** Gate green at 2642 / 170 files, three typechecks, lint, build. **No existing test was changed or deleted — 2637 prior assertions pass unmodified**, which Eric named as the actual point of tests in a refactor this wide. Stated plainly on the PR: nothing automated covers the 33 consequence strings, so the real hand test is reading them, and the two runnable steps are marked as unexecuted rather than implied.
**PR:** [PR #1037](https://github.com/mobiustripper42/muster/pull/1037)
**Points:** 8
**Branch:** task/902-swallowed-errors-in-core
**Opened at:** 2026-09-18T01:10:00Z

## Task 4: Fix check:dictionary on the context file (PR #1038, opened from jig)

**Completed:**
- **Not this session's PR.** [PR #1038](https://github.com/mobiustripper42/muster/pull/1038) is a jig sync opened from the jig side; this task is the muster half — fifteen `check:dictionary` findings, all of them muster's own prose failing a gate the sync had widened.
- Deleted the bullet claiming `one-piece.md` is ahead of jig. Stale in the most confusing way: the file moved to jig and this PR deletes muster's copy, so a warning not to sync it down was describing a file that is no longer here.
- `LIVE` and `STILL` → bold. `MCP`, `JSX`, `HTML` → code spans at all thirteen sites. **Not registered**, per Eric's instruction: the dictionary's rule is plain words, and an acronym in a code span has stopped pretending to be one. Thirteen `MCP` sites in one section is the tell — the fast-fix loop had drifted into jargon a sentence at a time.

**Code review:** not run — one markdown file, no code in the diff. Stated on the PR's receipt rather than left ambiguous, same posture as session 107 Task 3.
**`/security-review`:** not run — no blast-radius trigger.
**Proof:** `check:dictionary` 15 → 0; `check:context`, `check:docs`, `check:decisions`, `build` all clean; `drift.mjs` reports **nothing differs** at `jig-version` 6 vs 6.
**The mistake worth keeping, because it is the whole lesson of a two-repo sync.** I saw `its-dead/SKILL.md` citing `scripts/keep-tape.mjs`, did not find that file in muster, and reported the sync as incomplete — asking whether to copy two files out of jig. **Wrong: `<jig>/` is a pointer to jig's checkout, not a path into this repo**, and `check:context` resolves it correctly. It was green the whole time; I had read an earlier run against the pre-fix tree and never re-ran it. Eric named the shape better than I did — *two sessions in different repos both guessing at each other's state instead of reading it* — and he included his own half: he verified in jig and assumed that covered muster. **The fix was one command I already had.** Nothing was copied.
**PR:** [PR #1038](https://github.com/mobiustripper42/muster/pull/1038) — pre-existing and open; this task appended to it rather than opening a second.
**Points:** 1
**Branch:** task/sync-jig-templates-2026-09-19
**Opened at:** 2026-09-19T04:05:00Z

## Task 5: Four lint rules with zero findings, and a test that proves they fire (#902 → #904)

**Completed:**
- **The issue had largely dissolved and measuring said so.** Rules 2, 2b, 3 and 7 had already landed under #908/#909; rules 1, 4, 5 and 6 measured zero. The two clock bugs that made #904 urgent — `fmtTime` printing **UTC to the operator** in the admin outbox, and a client island claiming to "match the page's server-rendered fmtTime" while rendering the browser's zone — were gone because PR #943 deleted the outbox. **I proposed closing it as overtaken. Eric's answer is the task:** *"They were all failing on code that was written. What is lucky is that it is deleted."*
- Four rules, each tested against positive **and** negative fixtures before being wired in. Two failed on the first attempt — `redirect()` needed `> .block` rather than `:first-child`, and the skip selector needed a different anchor — which is exactly why selectors get tested rather than reasoned about.
- **`e2e/trainee-staffing.spec.ts` deleted**: one test driving the Manning override UI withdrawn after the S55 incident, reporting PASS while running nothing. Its own header was honest about the parking; `src/builder/manning.ts` keeps the domain covered.
- **He also asked what "ratchet" meant, and he was right to.** I had used it three times without defining it. A rule turned on while the count is zero, so it can only stay zero.

**Code review:** `@code-review` — **3 findings, all fixed.** The first undercut the rule's own purpose: I anchored the `describe.skip` selector to `Program >`, and a skip **nested inside a parent `describe` is exactly as dark** — a pattern this suite uses routinely (`crew-header.spec.ts:128`, `admin-nav.spec.ts:318`, `calendar.spec.ts:756`, three in `admin-form-error.spec.ts`). The anchor would have let the next trainee-staffing-shaped suite through on nothing but how somebody organized it. Second finding is this PR's own subject one level out: the two `no-restricted-syntax: "off"` exemption blocks say they drop "two selectors" and now drop four, because #904 added two to the block they shadow and nothing updated the count. Third: a dangling `trainee-staffing` citation in `playwright.config.ts`.
**The near-miss worth carrying, and it is the same shape as Task 3's.** My first cut added an `app/**` block for the clock rule and **switched off issue #854's bare-`catch {}` ban across `app/` and `components/`** — flat config replaces a rule's options rather than merging them. A PR adding lint rules, turning one off. **It surfaced only because eleven files happened to carry `eslint-disable` comments that went unused**; with no disables the gate would have gone quiet with nothing to say so. Fixed by extracting `CLOCK_SELECTOR` to a constant blocks compose, with the incident written at its definition.
**`/security-review`:** not run — no blast-radius trigger; config and a test, nothing reaches a runtime.
**Proof:** `scripts/lint-ratchets.test.mjs`, 25 cases, and it is the load-bearing artifact — **with zero findings, a working selector and a silently broken one produce the identical clean lint run**, and nothing else in the gate tells them apart. It lints fixtures through the real config via `lintText` with a path, so it exercises the shipped composition; four cases assert the older rules still reach the directories #904 touches, pinning the regression above. **Verified it bites:** typo'd the clock selector to `toLocale(Date|Time)Strng` and three tests went red. Gate green at 2679 / 171 files, three typechecks, lint, build, four doc gates.
**PR:** [PR #1039](https://github.com/mobiustripper42/muster/pull/1039)
**Points:** 3
**Branch:** task/904-zero-finding-lint-rules
**Opened at:** 2026-09-19T09:10:00Z

## Task 6: Truncate every table, not a list of them (#1031, #1041)

**Completed:**
- The contract test's `TABLES` named **37 of the 45 tables** a clean migration replay produces; **seven leaked** between tests through eight migrations. Replaced with a `pg_tables` query and a one-entry skip set. Reproduced first with a planted dangling `audit_events` row — the exact failure that had cost a stash-and-rerun during Task 2.
- **`db/reset-test.ts:35-38` had been doing it correctly the whole time** — same query, same exclusion, a sibling file twenty lines long. **Third instance this week of two places doing one thing with only one right**, after `pgConnectionConfig` at 1-of-18 callers (#960) and `logSwallowed` with 13 hand-rolled equivalents (#902).
- **`tsc` named three call sites; `grep` found ten.** The compiler stops listing after a few, which matters when you are using it as a census.
- **The fix created a flake and then closed it.** Removing the leak made the full suite fail once and pass on the re-run: `postgres-presence.test.ts` truncates `presence` per test, both files used `muster_test`, vitest runs files in parallel. **The gap in the list had been the only isolation between those two suites.** `ensureTestDatabase` gives the presence suite its own database; `KEEP` is back to the migration ledger alone.

**Code review:** `@code-review` — **3 findings and 2 cleanups, all fixed**, and two of the three are this branch's own subject committed by the branch. **The race `ensureTestDatabase` exists to tolerate throws a code it did not catch:** it allowed only `42P04`, which is what you get when the other creator has already *committed* — a genuinely simultaneous `create database` lands on `23505`. The review reproduced it with two concurrent clients. So the one case the docstring justified the design with was the case it did not handle. **I also became the 19th caller to skip `pgConnectionConfig`, in the commit that added a median-gap line naming #960** — and the excuse available to me was the one available to the other seventeen. Third: **my census was taken against a polluted test database.** I wrote "45 tables, eight leaks" and enumerated `muster_owned_vessel_days`, which migration `20260806230000` drops; it existed only in my local `muster_test`. The review caught the phantom and got the total wrong the other way (44); a clean replay into a scratch database settled it at 45 and seven.
**`/security-review`:** not run — no blast-radius trigger; two test files, a test-only module, a context doc.
**Proof:** 2681 / 171 files, **run twice consecutively and three times before the #1041 fix** — this branch introduced a flake, and a single green run is what a flake looks like. Three typechecks, lint, build, four doc gates. The drift guard was verified to bite by adding `audit_events` to `KEEP`; it also caught that my own test was not idempotent.
**The exchange worth keeping.** I filed #1041 rather than fixing it, on scope-discipline grounds. **Eric asked why it would ever be picked up**, then made the general point: *"I assumed at some point the issue list would start to get smaller, but it really has not."* Measured: net **+80 / +30 / +15** for July, August, September — closing is steady, opening is what moves. **Filing costs two minutes and feels like diligence; fixing costs an hour and feels like scope creep**, so the default resolves toward filing every time. Both that and the grep-for-the-helper rule are now median-gap lines with the numbers attached.
**PR:** [PR #1042](https://github.com/mobiustripper42/muster/pull/1042)
**Points:** 3
**Branch:** task/1031-truncate-every-table
**Opened at:** 2026-09-19T10:35:00Z

## Task 7: `faint` is not a text colour (#951)

**Completed:**
- `--color-faint` measures **2.36:1** on the page background and **2.66:1** on a card — failing AA's 4.5:1 for body text *and* the 3:1 floor for large text, so no size or weight rescued the 145 places it was used as one. Reclassified rather than revalued (DEC-178): raising the value collapses `faint` into `muted`, which flattens the hierarchy anyway and leaves two names for one colour.
- **136 class swaps across 36 files.** 10 uses survive with a disable naming a reason — seven `aria-hidden` glyphs, three inactive controls. The 3 `disabled:text-faint` were never flagged; WCAG 1.4.3 exempts an inactive control.
- **The issue was titled "admin body text" and that undersold it.** `checkout-form.tsx` (13) and `book/page.tsx` (9) are the booking flow — a paying customer was reading 2.36:1. Two of the three lines the issue cites had already been fixed; only the `<thead>` remained.
- **The rule was written first and watched fail at 138** = 145 greps − 4 comment mentions − 3 `disabled:` variants. Two selectors, not one: **12 findings were inside template literals**, and a `Literal`-only rule would have read as complete while leaving a working escape hatch in the conditional-styling idiom.
- `sonarjs` caught a ternary the sweep collapsed to identical branches (`app/b/[code]/page.tsx`) — the colour had been redundant with the word "Cancelled" on the next line even before this.

**Code review:** `@code-review` — **1 finding, and it was a live gap in this PR's own mechanism.** Four `components/ui` primitives carry `"no-restricted-syntax": "off"`, so the new token ban never reached the files most likely to grow a fresh `text-faint`. **The block's comment had been counting by hand what the `"off"` was dropping; it went stale at #904 and went stale again the moment this branch spread two more selectors in.** Fixed by making the selectors one named array that narrowing blocks *subtract* from: the primitives `.filter()` out the single selector they need exempting from and keep the other five. Also deleted a block globbed at `components/outbox/outbox-card.tsx` — that directory went with PR #943, and flat config matches globs against files, so it had been dead for weeks in silence.
**`/security-review`:** **NOT RUN — no such command is defined on this machine.** Checkable: nothing in the cached `claude-plugins-official` marketplace defines it (three grep hits, all docs or an example), and it is absent from this session's tool and skill registries. `claude-security` v0.11.0 is in the catalog, uninstalled, doing the same job with a verifier panel. Nothing was owed here anyway — `git diff main...HEAD` on the two trigger-matched paths returns only colour swaps and two comment lines.

**And the receipt has been lying in both lanes.** I first wrote "not installed" off a filesystem look, then dated its disappearance to ~2026-09-14 from session receipts saying it ran. **Lane B then reported that for its last several tasks it had been running a general-purpose agent carrying the security-review prompt and writing "Ran `/security-review`" on the receipt.** So part of my evidence was a mislabel. Step 3.6 exists precisely so a pass that silently did not run cannot look like one that ran clean — and it was defeated by a name rather than a missing line. **The fix is jig's and it is one word: the receipt names what EXECUTED, not what the step is called.** `✓ security pass — general-purpose agent, security-review prompt` is checkable; `✓ /security-review` is a claim about a tool. Same shape as #960 and #902 this session: two things doing one job, only one of them the real thing, nothing downstream able to tell them apart.
**Proof:** `npm run verify` green — 2704 tests / 171 files, three typechecks, lint, build, four doc gates. `scripts/lint-ratchets.test.mjs` **43 passing, 18 new**: 9 for the token ban, 3 that it survives the narrowing blocks, 6 pinning the `components/ui` carve-out at exactly one selector wide.
**Surface check: NOT DONE — the one step I could not do.** Playwright's mobile pass catches overflow, never colour. `opacity-50` was deliberately dropped from the closed-day calendar cell (`book/page.tsx:393`) because muted at half opacity lands back near 2.7:1; that cell is the one place the visual weight genuinely changed. The hand section of the PR names seven surfaces. `@ui-reviewer` was offered and declined.
**The line worth keeping.** One of the 136 was written **in PR #1032, earlier this same session, with #951 already open and these numbers already measured.** That is the whole argument for the ratchet: `text-faint` is the obvious class for "quieter than muted" and nothing about it announces the failure.
**PR:** [PR #1044](https://github.com/mobiustripper42/muster/pull/1044)
**Points:** 5
**Branch:** task/951-faint-contrast
**Opened at:** 2026-09-20T01:50:50Z

## Task 8: Delete the emit profile nothing consumed (no issue — regression from Task 7)

**Completed:**
- **Task 7 shipped a defect and this is it.** PR #1044 removed `"outDir": "dist"` from `tsconfig.core.json` as vestigial — true, `noEmit` made it inert *there*. **`tsconfig.build.json` extended that profile, set `noEmit: false`, and had no `outDir` of its own.** For one commit on `main`, `npm run build:core` would have emitted `.js`/`.d.ts`/`.js.map` **beside every source file in `src/`** — `rootDir` with no `outDir` writes in place — where `.gitignore` has no generic `*.js` pattern and `/kill-this` runs `git add -A`.
- **The line worth keeping:** `tsconfig.build.json`'s first line had read *"compile only shippable source under src/ into dist/"* since it was written. **That was true by inheritance, so deleting the thing it depended on did not make it read false.** An inherited setting is invisible at the place that needs it — which is why "this does nothing" was checkable and wrong.
- **Deleted the profile rather than restoring the line.** `build:core` has no consumer: `private` package with no `main`/`exports`/`files`, `vercel.json` runs `next build`, CI runs `npm ci` + `verify` + `test:e2e`, `verify` never chains it, nothing imports `dist/`, no Dockerfile/Makefile/hooks/lifecycle scripts. The only three mentions in the repo were its own definition and two comments about itself. DEC-020 already describes the core being consumed through the `@core/*` alias with no compile step, so portability is a property of the source, not of a build artifact.
- **`/security-review` found it** — flagged as a correctness note and correctly declined to call it security.

**Code review:** `@code-review` — **0 findings, and it verified rather than agreed**, which is what a branch about an unchecked "does nothing" claim needed. It re-ran the consumer search independently and caught a file I had not checked (`vercel.json`). It proved the risk that would have mattered most — `tsconfig.build.json` excluded `src/**/*.test.ts` where core includes them, so "the deleted profile checked something nothing else does" was live — by running `tsc --listFilesOnly` and matching 153 test files against `find`. It read DEC-013 and DEC-020 in full rather than from the index. **One cleanup, fixed:** `declaration` and `sourceMap` went inert with the profile and are unreachable (db extends and inherits `noEmit`, app sets its own, no `composite`/`references`).
**`/security-review`:** not run — no blast-radius trigger; `package.json` and two tsconfigs, which CLAUDE-context names under *Not triggers*.
**Proof:** `npm run verify` green — 2716 tests / 171 files. **The defect was reproduced before it was fixed**, with `tsc --showConfig` showing `noEmit: false` + `rootDir: ./src` + no `outDir`, then 173 files emitted into `dist/` and 0 into `src/` once restored. That intermediate fix is not in the branch — the profile is gone instead — but it is how the failure was confirmed rather than argued.

**The session's real lesson, third instance.** `/security-review` exists, is model-invocable, and I declared it missing twice — in a PR body and this file — having searched the filesystem and the tool registry but never having tried to call it. Jig said so and was right. **Absence inferred from two searches is not absence; the one-line test was to invoke it.** That is the same failure as the `outDir` it then found: a claim checked in the place it was easy to check, not the place it mattered.

**PR:** [PR #1046](https://github.com/mobiustripper42/muster/pull/1046)
**Points:** 2
**Branch:** fix/build-core-outdir
**Opened at:** 2026-09-20T03:25:00Z

## Task 9: The reservation trail — the table, the write path, the reset-pilot call (#1047)

**Completed:**
- The first of six tracked by issue #1053. Table, migration, `src/domain/reservation-trail.ts`, port method, both adapters, 7 contract cases, `reset-pilot` classification, integrity-coverage registration.
- **DEC-118 is enforced by the type system rather than by a comment.** Two unions: `TrailEventType` is what a reader sees, `EmittedTrailType` is the strict subset the table accepts, and `appendTrailEvent` takes only the latter. Half the trail already persists (`booked` in `reservations.status`, `imported` in `import_run_items` since 0007), so `type: "booked"` **does not compile**. The rule as a mechanism, not something twenty emitter sites must remember.
- **Two keys, both nullable, neither an FK — and the reason is the reaper, not cascade semantics.** Every FK here is `on delete restrict` because nothing hard-deletes; §2.8.8's future reaper is the one thing that will, and the trail is what makes deleting a lapsed row safe. The dangling reference is the *designed state*, and the contract pins that both adapters accept one.
- **Caught an unrelated `docs/SPEC.md` edit riding along at `git add -A`.** The abandoned §2.10.8 followed me across a `git checkout main`. `/kill-this` Step 2's "name the files being staged" is the only thing that would have caught it — it did, and the gate was re-run after reverting rather than trusting the earlier green.

**Code review:** `@code-review` — **1 finding, fixed in `1dca5c6`.** Adapter parity on a timestamp **tie** was asserted in two comments and exercised by nothing; the ordering test used `10:00` and `11:00`, so a disagreement between in-memory's stable sort and Postgres's `seq desc` would have shipped green. Ties are not exotic — several emitters fire from one webhook handler at millisecond resolution, and #1048's union read orders across four sources. It also **verified rather than agreed** on the rest: checked the 22/11 union overlap programmatically, and checked **every** source claim in `DERIVED_TRAIL_TYPES` against the schema with a migration and line for each — which mattered, because a false premise there is inherited by #1048.
**`/security-review`:** not run — no blast-radius trigger. Migration is `create table` + four indexes; `grep -icE "^\s*(drop|alter|update|delete)\b"` returns 0. **Observation raised in the PR rather than acted on:** the trigger table has no row for *destructive prod tooling*, and this diff edits `db/reset-pilot.ts`.
**Proof:** `npm run verify` green — 2730 / 172. `npm run test:pg` **201 passing**, so all seven trail cases ran against real Postgres. **Contract written first and watched failing** (six × `appendTrailEvent is not a function`); the tie case added after review and watched failing the same way.

**Writing a drift guard found a bigger defect than the one it was for → issue #1054.** `reset-pilot.ts` refuses to run on an unclassified table. That guard **fires today on 22 tables** — `payments`, `offerings`, `admins`, `booking_codes`, `audit_events` and seventeen more. **A destructive prod tool that cannot run is not safe, it is unavailable**, and the moment it tells you is the worst one: operator at a terminal, pointed at production, blocked on 22 design questions. `payments` is the hard one — `reservation_id` is `not null` with `on delete restrict`, so a `truncate … cascade` from `reservations` will **fail** rather than skip it. I pulled the whole-schema assertion back out rather than pin a baseline that would bake the defect in, and left the reasoning in the test file. **Also re-hit #1031's trap**: `db:reset:test` truncates rather than drops, so `muster_owned_vessel_days` is still in the local schema despite `20260806230000` dropping it — 22 real, one local artifact.

**Corrected mid-task.** I had relayed that `integrity.ts` needed no change and a new table is invisible to it. Wrong — `integrity-coverage.test.ts` is a registry demanding a decision per table, and it caught the omission on the first full run. Registered `exempt`, with the reason that a dangling reference here renders the exact fact the operator opened the trail to read.

**PR:** [PR #1056](https://github.com/mobiustripper42/muster/pull/1056)
**Points:** 3
**Branch:** task/1047-reservation-trail-table
**Opened at:** 2026-09-20T17:05:00Z

## Task 10: The money-out emitters, and the write path they all go through (#1050)

**Completed:**
- Second of six tracked by issue #1053. Seven emit sites: four dispute states, `auto_refunded`, `refund_failed` engine-side and operator-side, `refund_issued_by_operator` on both operator entry points.
- **`src/reservations/trail.ts` is the mechanism, not a convention.** Eric's ruling — *"I want the emits to be correct, but nobody shows up for a boat they thought they booked because we do not emit correctly"* — orders the two properties, and the ordering had to be structural. Never throws, logs the swallow (#902), **requires a caller-supplied id** because `on conflict (id) do nothing` is worth nothing against a random one.
- **`refund_issued_by_operator`, renamed from `refund_amount_overridden` on Eric's call.** The old name promised a quote to differ from and there are two entry points with one quote between them. **The rename forced a real change**: `cancelBooking` only computed `quoteCancelRefund` on the branch where the operator typed *nothing*, so it is hoisted to run on both.
- **`dispute_unknown` added — five states, not four.** The one where the SDK cannot tell whether money moved, so the ledger is deliberately not written. `live` deliberately does NOT emit; its fact is derived from `payments.status`, and emitting it would be the dual-write the type system refuses.

**Code review:** `@code-review` — **3 findings, all real, all on the audit path this issue exists to protect.** (1) **The operator refund id collided**: keyed on `<reservationId>:<amount>`, so two $50 goodwill refunds on one booking produced the same id and the second row was silently dropped. Re-keyed on the compare-and-swap token. (2) **`refund_failed` was emitted inside the refund lease** — in `executeRefundPlan`'s catch, inside the `try` whose `finally` releases it, so a slow trail write held the lease and a concurrent refund is refused as `stale` for that duration. Moved to the action layer. (3) The hoisted quote reads were unguarded, adding a new throw on a page whose cancel had already committed.
**`/security-review`:** **ran** (Money-moving trigger: `refund-payment.ts` + `booking-webhook.ts`) — **0 findings**, each of five named risks cleared with evidence. **But it caught the sibling of finding (1)**: `refund_failed`'s id also derived from the CAS token, which is the refunded *total* — so a `provider_error` moving **zero** cents leaves it unchanged, and two consecutive zero-movement failures collapsed to one row. That is the branch that only runs when something has already gone wrong. Fixed with a per-attempt id, and the general rule written down: **determinism is a WEBHOOK property**, existing so a redelivery collides; an operator action has no redelivery, and a double-submit writing two audit rows is the right failure direction for a log.
**Proof:** `npm run verify` green — 2739 / 173. **Both new test files verified to bite**: removing `trail.ts`'s try/catch turns the two safety cases red; removing the two `recordDisputeTrail` calls turns all three dispute cases red.

**The honest note.** I wired six of the seven sites **before** writing their tests, which inverts the workflow. Recorded on the PR as such rather than presented as test-first. The bite-checks are what make them worth anything, and they were done after the fact.

**Third time this session a comment was true only by assertion.** `trail.ts` claimed *"every call site below the booking path sits after its transaction, and that placement is what the emitter tests assert"* — false when written, on both halves. It now records the violation instead of the claim. Same shape as the stale `booking-webhook.ts` header and the `tsconfig.build.json` line that was true only by inheritance.

**PR:** [PR #1057](https://github.com/mobiustripper42/muster/pull/1057)
**Points:** 5
**Branch:** task/1050-money-out-emitters
**Opened at:** 2026-09-20T23:50:00Z

## Task 11: The customer-reach and slot emitters (#1052)

**Completed:**
- Third of six tracked by issue #1053. Nine sites, all silent before, most **by construction** rather than oversight.
- **`confirmation_skipped` is the defect that opened issue #886.** The messaging flag's early return writes the booking, shows the operator a sale, tells the customer nothing, and logs at **no level**. The row is the only artifact that will ever exist for it.
- **`link_recovery_requested` is the strongest case in the whole trail.** `recover-booking-link.ts` is built so the caller *cannot* tell a match from a miss — that is its security property — so the trail is the only place the truth is written. Three post-claim paths emit; the unusable-contact return deliberately does not.
- **`releaseVesselHoldAdmin` now hands back the block it deletes.** It already read the row to validate it, then discarded it — and that row is the only record the slot was ever held.
- **Re-estimated 3 → 5 before overrunning**, not after. Nine sites across six files was never a 3.

**Code review:** `@code-review` — **5 findings.** (1) **`sold_out_notice_*` used a random id on a redelivery path — the site I had asked it to check hardest and had still got wrong.** It runs from `compensateResidualRaceLoss`, documented as re-entered on Stripe redelivery; its sibling `auto_refunded` is keyed on `charge.key` for that exact reason and this was not, so every redelivery fabricated rows. (2) **`reissueBookingLink`'s worst outcome recorded nothing** — old link dead, new link never delivered, customer locked out — because the emit sat after two early returns. That is the case the function's own docstring names, and my comment claimed to cover "the case most worth having a record of" while missing the worse one. (3) **The kill-flag fix went into one file and not its sibling.** Two were answers to my own questions and both cleared — it verified the timing-symmetry claim by reading the caller rather than trusting me.
**`/security-review`:** **ran** (Auth trigger: `app/b/**`) — **0 findings.** It cleared the oracle question independently, walked all nine `metadata` values for PII, and confirmed no booking code or manage URL reaches a row. **Its sub-threshold observation was worth more than its bar:** `sold_out_notice_failed` stored the raw error, whose message embeds the provider's verbatim response body — Resend and Twilio echo the recipient address. `channel.ts` has had that redaction since #902 with the comment *"repeated at six catch sites where the next one would be written slightly differently and nobody would notice."* **This was the seventh, written slightly differently.** Its reason for downgrading — a sibling already does the same — is an argument for fixing both.
**Proof:** `npm run verify` green — 2741 / 173. Eight new cases across two files, **both sets verified to bite** (removing the recovery emits → 4 red; reverting the sold-out id to random → 1 red).

**Fourth instance this session of a helper existing and a new call site hand-rolling past it** — after `pgConnectionConfig` at 1 of 18 (#960), `logSwallowed` with 13 equivalents (#902), and the truncate list beside a correct sibling (#1031). The shape is stable enough to predict: the second way is always written by someone who did not know the first existed, and it is always *nearly* right.

**Two of my own mistakes caught by tooling rather than by me.** The test block went in above the `beforeEach` (`sonarjs/hooks-before-test-cases`), and a four-branch ternary was refused by `sonarjs/no-nested-conditional` — correctly: four outcomes with materially different consequences read as one expression and the worst was the easiest to miss.

**PR:** [PR #1059](https://github.com/mobiustripper42/muster/pull/1059)
**Points:** 5
**Branch:** task/1052-customer-reach-emitters
**Opened at:** 2026-09-21T13:05:00Z

## Task 12: The money-in and checkout emitters (#1051)

**Completed:**
- **Seven event types, eleven emit sites** — the last emitter group, so `EMITTED_TRAIL_TYPES` is now fully wired.
  - `src/reservations/claim.ts` — `hull_contended` (the winner is not `candidates[0]`, which covers both read-skip and CAS-loss contention), `sold_out`.
  - `src/reservations/create-departure-payment-intent.ts` — `checkout_details_changed`, `payment_superseded`.
  - `src/reservations/booking-webhook.ts` — `payment_failed`, `payment_superseded`, `charge_unmatched` ×4.
  - `app/(admin)/admin/calendar/[reservationId]/actions.ts` — `balance_link_created`.
- **The #1053 prerequisite had already fired before I could fix it.** The module header said `lost`/`unbookable` mean "no row"; `write-booking.ts:140` returns the row. #1050 emitted against that header, so `auto_refunded` and `refund_failed` carried no `reservationId` — four lines below a `row.customerName` read. **The residual-race refund, and the worse case where it FAILED, never appeared on the trail of the booking they were about.** Both re-keyed, header rewritten against the actual branches.
- **`DepartureClaimResult` now carries `prior` on the reuse branch.** `checkout_details_changed` needs the values `recordCheckoutAttempt` destroys, and they only exist inside `claim.ts` — but it cannot emit there: two `already_paid` returns and a throwing mint all return before the write.
- **`charge_unmatched` came out at four shapes, and I dropped one the issue asked for.** Added the retired hosted session; dropped the dispute half, because #1050 already writes that row and a `dispute_lost` with an intent and no reservation *is* the unmatched fact. A second row over it is the dual-write DEC-118 bars.
- Blast-radius table gained `create-balance-checkout.ts` + the reservation-detail action file — **the fourth addition, all four the same shape**: a file that does something with money sitting next to a file the table already listed.

**Code review:** `@code-review` — **1 finding, and it is the id-collision class for the third time in one file.** `charge_unmatched:refund_on_unknown_charge` keyed on the PaymentIntent; `amountRefundedCents` is *cumulative*, so a charge refunded in two parts sends the event twice and the second hit `on conflict do nothing` and vanished. `payment_failed`, thirty lines up, keys on `event.stripeEventId` for exactly this reason. **I had written that reasoning down twice in the same file and it still did not travel to the third site** — the same shape as this session's four helper-bypass incidents, one layer up: the lesson was present and local and was not applied. Fixed by threading `stripeEventId` into `recordRefund` and splitting `key` into `idKey` (what must change when the fact changes) and `chargeRef` (what a human searches for).
**And the finding invalidated one of my own tests.** The shape-in-the-id case paired `reservations_off` with `refund_on_unknown_charge` — which after the fix no longer share a key, so it would have passed with the shape removed entirely. **A test that stops biting because of a fix elsewhere looks exactly like a test that passes.** Replaced with the pair that can genuinely collide (`reservations_off` / `no_payment_intent`, both on `charge.key`) and re-verified by mutation.
**`/security-review`:** **ran** (Money trigger) — **0 findings**, with all eight named risks traced: it read `stripe-payment.ts:487` to confirm `declineCode` is `decline_code ?? code` and never `last_payment_error.message`; confirmed `actions.ts` records `sessionId` and not the URL; traced `pg_advisory_xact_lock` to prove the `hull_contended` emit is outside it; confirmed every customer-reachable id is `randomUUID()`-based so nobody can pre-seed a colliding primary key. **Its non-finding is worth keeping:** trail rows deliberately outlive the §2.8.8 reaper, so `previous.email`/`previous.phone` persist past the row they describe, and there is no erasure routine for that to defeat. One for whoever builds the reaper.
**Proof:** `npm run verify` green — **2767 / 173**. 26 new cases, all written before their code. **9 mutations** to confirm the negative controls bite — including the shape-in-id one twice, before and after the rewrite.

**The psql step I nearly shipped.** The first PR body told the reviewer to verify the row with `npm run db:psql`. **That script does not exist; I invented it** — and a hand-SQL step is precisely what the operator rejected last session ("You know I have no UI to run SQL?"). Caught it before surfacing the PR, rewrote the hand section to the two things a person can actually check, and named the gap instead of dressing it: `balance_link_created` is the one emit with no automated proof, and the fix is a `db:trail` script or a mocked action test, operator's call.

**PR:** [PR #1060](https://github.com/mobiustripper42/muster/pull/1060)
**Points:** 5
**Branch:** task/1051-money-in-checkout-emitters
**Opened at:** 2026-09-21T16:55:00Z

## Task 13: The union read — and three facts that should never have been derived (#1048)

**Completed:**
- `src/reservations/reservation-trail-view.ts` (new) — pure `reservationTrail()` + `loadReservationTrail()`, assembling one booking's history from five sources. `when` is a value with three cases — `recorded` / `computed` / `inherited`, the last two carrying `from` — so a borrowed clock can be rendered as one instead of printed as a confident time.
- **Three port methods that did not exist**: `listTrailEventsFor` (EITHER key, oldest-first — the opposite of `listTrailEvents`, because a per-booking history is a story), `listGratuitiesForReservation`, `listImportItemsForRef` (the reverse index `import_run_items` never had; returns `ImportItemAtRun` so the run's clock travels with the item that has none). Both adapters, 6 contract cases across both.
- **`booked`, `cancelled` and `refunded` moved DERIVED → EMITTED, mid-task.** `admin_booked` deleted outright. Three new emitters.

**The miss, and how it surfaced.** The task was specified as a projection; building it showed three of the things being projected cannot be. `reservations.updated_at` is last-write-wins so a cancel overwrites the booking's time; `payments.refunded_cents` is a running total, so a refund was dated from the charge AND two partial refunds collapsed into one entry. **I cited DEC-118 and asked it the wrong question** — its test is whether the TRANSITION persists ("the add/drop transitions persist NOTHING today, so there is nothing to derive"), and I asked whether the FACT persists. `status` persists a state; nothing persisted the event.

**It took the operator four turns to get it out of me**, and the last one is the one that landed: *"maybe i don't understand what booked means ... but it seems like 'booked' would be the single most important event to capture ... you know ... in a booking system."* The three before it were questions I answered accurately without noticing they were pointing at a design hole — *"why wouldn't booked get written to the audit table?"* was already the whole finding and I explained the decision instead of re-examining it. **A question repeated in different words is a signal the answer is wrong, not that it was unclear.**

Two things fell out: `admin_booked` was `source = 'admin'` wearing a type (the emitted row says it with `actorKind`), and **`metadata.via` is populatable for the first time** — specified at #1047 and empty since, because a row records that it IS booked and never which of §2.8.6's three confirms won.

**Code review:** `@code-review` — **one production-reachable bug.** `checkout_lapsed` was computed from `Reservation.holdMinutes`, which is the HULL occupancy (§2.8.3, ~120m), not the payment window (§2.8.1: *"a setting, not a column"*, 15m). **`entities.ts:808` labels that exact trap in the field's own docstring — *"confusingly the same word"* — and I walked into it, then wrote a test citing §2.8.3 to lock it in.** The trail dated the lapse two hours late. The same open-coded arithmetic missed DEC-163: admin rows have no window and never lapse, which `isLivePending` already knew. **Fifth instance this session of a helper existing and a new call site hand-rolling past it** — after `pgConnectionConfig`, `logSwallowed`, the truncate list and `describeSendFailure`. I catalogued the pattern this morning and joined it this afternoon. Field renamed `paymentWindowMinutes` so it cannot restate the collision.
It also found **three comments asserting what the code did not do**: the `booked` inner guard is load-bearing (a pre-feature `already` row would insert a BACKDATED `booked` row without it), `refunded` does not fire for the residual-race loser, and `TIE_RANK`'s rationale described collisions this same commit deleted.
**`/security-review`:** **ran** (Money trigger) — **0 findings**, with the most plausible lead closed by tracing rather than asserting: the only writer of `paymentIntentIds` is `recordCheckoutAttempt`, twice, both with ids Stripe returned, so nothing customer-controlled can plant a victim's intent id to pull their trail rows.
**Proof:** `npm run verify` green — **2807 / 174**; `test:pg` 208. **31 mutations, all reverted. Three came back GREEN and each was a real finding**: `via` was never asserted; my "refunded before the guard" mutation was a no-op that proved nothing; and the redelivery case cannot see the `outcome === "booked"` guard at all. Postgres caught a contract fixture the in-memory double accepted (`gratuity_reservation_id_fkey`), and forcing real parents **collapsed the premise I had written the method's docstring around** — rewritten to the weaker true thing. Typecheck then caught a tautology in my own test.

**PR:** [PR #1064](https://github.com/mobiustripper42/muster/pull/1064)
**Points:** 8
**Branch:** task/1048-trail-union-read
**Opened at:** 2026-09-21T19:40:00Z

**Next Steps:**

**Context:**
