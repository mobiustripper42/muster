---
session: 108
slug: main
branch: main
started: 2026-09-16T20:06:55Z
ended:
points:
pr_numbers: [1032, 1035, 1037, 1038]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster/d12028b2-982c-5fde-a0a9-aca201fca6f9.jsonl
---

# Session 108 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Crew see the other shifts, not just the holes in them (#968)

**Completed:**
- **The design was Eric's, iterated in four passes, and each pass made the task smaller.** Issue #968 named three candidate behaviours and said to pick one; he picked **social proof** — *"I want them to feel part of a team. The empty screen seems like nothing is happening."* I specced two sections vs a Claimable/All filter and recommended the filter; he corrected my misreading (the delimiter was two top-level sections, not a per-day split), which solves the scroll complaint directly and needs no tap. Then: *"Basically, the screen is exactly like it is now and we add the rest of the shifts below for display"* — which deleted the whole merge-claimable-into-fleet-rows half of my spec and took it **5 → 3**.
- `src/crewapp/team-view.ts` — a crew-safe projection over `deriveAllShifts`, clamped and memoized. The exclusions are enumerated in the docstring rather than spread through a mapper: no `pax`/`paxTotal`, no shift state, and `Asked`/`Bailed`/`Open`/`Claimed` collapse into one indistinguishable "unfilled" so the DEC-008 drip order is underivable.
- `app/(crew)/crew/open/page.tsx` — the claim list untouched; a second display-only section below a rule. `groupByDay` made generic so both sections share one day spine.
- **DEC-176**, amending SPEC §2.7 with a new §2.7.1a. Decision search run and reported on the PR: `grep -rli` for fleet visibility returned only DEC-071/DEC-072 (messaging cross-visibility), so new id, no supersede. Amendment count 32 → 33, checked — the only external evidence it landed rather than being dropped, which is session 107's gotcha twice over.
- **Four rounds of visual correction, all his, all right.** Headings needed to be bigger and bold. "What the team is running" → **"Other shifts"**, because *"the boats are not out, or running. These are not even boats, they are shifts in the future"* — present tense was wrong in three places, not one. Rows needed a border with no fill. Then the killer: a screenshot with the grey text circled — `text-faint` at 2.36:1 on a fill-less row, which is [issue #951](https://github.com/mobiustripper42/muster/issues/951) and I had reached for it anyway. Swept the page; only the decorative chevron still uses it.

**Code review:** `@code-review` — **4 findings, all confirmed, all fixed.** The real one: `buildTeamView` passed the caller's window into `deriveAllShifts` **unbounded**, while its sibling `claimableSeatsFor` clamps to `[today, today+45d]` internally and only lets a caller narrow it (`claimable.ts:84`). `range` comes from `searchParams`, guarded by an HTML `min`/`max` — a hint to a browser and nothing to a URL. So `?from=2000-01-01&to=2099-12-31` ran the issue-#960 fan-out over every shift the fleet has ever had, on a screen meant to be opened habitually. **That URL is literally the `ALL` constant my own new e2e tests use**, so every green run I had reported was exercising the unbounded path — the tests were the alibi. Also: `listAllSeats` cached nowhere despite being a full seat-table read reached once per understaffed shift (`oracle.ts:70`) — added to `memoizing-repo`'s `SINGLE`, which is a shared-adapter change and why the full unit suite ran; `openRoles` counted unfilled trainee seats as gaps, untested because the fixture hardcoded `kind: "required"`; and the memoization docstring overclaimed what the wrapper covers.
**`/security-review`:** not run. I proposed it on the grounds that this is a new surface where one crew session reads fleet-wide data about others; **Eric's call was that it wasn't justified** — no blast-radius row hit, and the privacy question was the one `@code-review` had already answered structurally (`crewName` gated to `Confirmed` upstream at `all-shifts.ts:202-205`; `AllShiftsSeat` never exposes `assignedCrewMemberId`, so no wrong implementation here could reach one). He was right and the receipt says so rather than leaving the absence to be inferred.
**Proof:** 7 unit cases, 20 e2e on desktop + mobile at 375px, 755 tests across the memoizer's consumers, both typechecks, lint, build, doc gates. **Stated plainly on the PR: only the two post-review cases were watched failing on a real assertion** — the original five failed as `Cannot find module` and the original two e2e passed first run.
**Two issues filed from things found on the way:** [#1030](https://github.com/mobiustripper42/muster/issues/1030) to retire magic tokens from ask links (Eric: *"adds security complexity for what seems like zero added value"*), flagging that `magic-link.ts` also supplies the DEC-098 calendar-feed token so the file survives the removal; and [#1031](https://github.com/mobiustripper42/muster/issues/1031) for the contract test's truncate list, which covers **37 of 45 tables**. He asked whether the three I had named were really all of them — they were not, there are eight, and I had only listed the three that happened to hold rows.
**PR:** [PR #1032](https://github.com/mobiustripper42/muster/pull/1032)
**Points:** 3
**Branch:** task/968-team-schedule-below-claims
**Opened at:** 2026-09-17T03:30:00Z

## Task 2: Every database connection carries the Crunchy CA, not just the app's (no issue)

**Completed:**
- **Unplanned. It came out of `/promote-production` refusing to run**, and the gate was right: its pre-promote check told me to read prod's migration ledger via the Neon MCP against project `delicate-art-65084110`, which 404s. Listing Eric's Neon org returned one project holding two migrations and a `push_tokens` table — a different application entirely. Production had moved to Crunchy Bridge weeks earlier and no document said so.
- **The fix Eric asked for, after I twice proposed a smaller one.** I fixed `db/migrate.ts` alone, he ran it, it worked, and then he found the other sixteen: *"fixing db:migrate should have fixed all things, but I find out it's not."* The trust decision moved **into** `PostgresRepository.fromConnectionString` (`src/adapters/postgres-repository.ts`), which covers all 17 `db/` scripts and the e2e fixtures in one edit, plus `/api/health`, `backfill-customers`, `reset-pilot`, `reset-dev`, `reset-test`. `db-ssl.ts` moved `app/lib/` → `src/config/` so `db/` can reach it without importing from `app/`.
- **A live production defect, found by asking a question I could have skipped.** I predicted from reading `app/api/health/route.ts:30` that the probe builds its own pool and should be reporting the database unreachable in prod. Eric curled it: `"status":"degraded"`, `"db":{"reachable":false}`. It has been saying that since the cutover while the app worked fine. **That prediction-then-check is the only reason it was found** — nothing in the task required looking.
- **DEC-177**, the record that never existed. DEC-033 left the hosted-Postgres provider **OPEN** and nothing ever closed it, so neither Neon nor Crunchy was recorded. That absence is the mechanism: no decision, no `revisit_if`, nothing to make anyone check the runbook. `docs/DEPLOY.md` and `.claude/CLAUDE-context.md` rewritten; the provisioning walkthrough is marked `NEON — NEEDS REWRITE` rather than invented, because nobody has stood Crunchy up from scratch.
- Prod's ledger was **two migrations behind** — `db:migrate` applied `confirmation_sent_at` and `checkout_attempts` the moment it could connect.

**Code review:** `@code-review` — **4 findings, all fixed.** Two were **me asserting facts about Crunchy I never verified**, in a runbook meant to be followed without me present — exactly the failure the task was fixing. I wrote *"it is set manually in Vercel's env — there is no auto-injecting integration"* having checked nothing; the page now refuses to say, and names the gap. I also carried a Neon-specific `vercel env pull` warning across with only the noun changed. The third was vocabulary: the CLI section still said "direct/unpooled" two sections after the doc retires that distinction, which would send an operator hunting a second connection string that does not exist. The fourth was the sharpest: **`fromConnectionString` had no test**, and `db-ssl.test.ts`'s own header says *"testing the helper is not testing the connection"* — so the helper was tested and the one call site seventeen scripts reach was not. It also ran its own census rather than trusting my grep, checked `package.json` for a second driver and `scripts/*.mjs` for database access, and read `eslint.config.mjs:474` to confirm the `eslint-disable` I removed was genuinely out of scope.
**`/security-review`:** not run — no blast-radius row hit. TLS being the subject argues for it, but the change strictly *adds* certificate verification where there was none and `pgConnectionConfig` itself is unmodified.
**Proof:** full suite 2632 / 168 files, three typecheck profiles, lint, build, five doc gates. **The real proof was live:** `db:migrate` against production succeeded. Stated on the PR: the two new `fromConnectionString` cases were written after the code and watched failing only on a mock defect, so they are coverage, not proof.
**PR:** [PR #1035](https://github.com/mobiustripper42/muster/pull/1035)
**Points:** 3
**Branch:** task/968-crunchy-tls-everywhere
**Opened at:** 2026-09-17T22:20:00Z

## Task 3: One way to record a swallowed error in the core (#902)

**Completed:**
- **The task doubled in scope because Eric rejected my recommendation, and he was right.** I proposed fixing the 23 silent `catch {}` in `src/` and leaving the 13 hand-rolled `console.error` calls alone as churn. His answer: *"We are building a logging routine. Apparently we have identified numerous cases where 'functional' is the same ... right up until we have a helper and then they diverge without anyone noticing."* That is not hypothetical — **it is what Task 2 cost, the same evening**: `pgConnectionConfig` existed, one caller of eighteen used it, seventeen hand-rolled the equivalent, and they stayed equivalent until production changed databases. 5 → 8 points, and the deliverable became *one way*, not *23 fixes*.
- `src/log.ts` — the core's `logSwallowed`, mirroring `app/lib/swallowed.ts`. A separate function because `src/` is framework-free and cannot import from `app/`; `src/log.test.ts` mirrors the app test case for case, which is what stops necessity becoming divergence.
- **Three lint selectors scoped to `src/**`, each tested against sample code before being wired in** — the bare catch, a `console.*` inside a catch, and `.catch()` with a zero-parameter arrow or function. The third is the same rule restated rather than a new one: a handler that binds no parameter cannot log what it caught.
- **35 sites, not the 23 the issue claimed** — grep found 23, the rule found 35. 33 log with a consequence naming what is now untrue; 3 are genuine exceptions with a disable saying which. 14 were on the money path. `db/` excluded deliberately, stated in three places.

**Code review:** `@code-review` — **3 findings, all fixed**, and the first is the one I had explicitly asked it to hunt and missed myself. **This PR created a leak.** The six `forward-*` relays all wrap `channel.send()`, `email-channel.ts` throws with the provider's raw response body in the message, and before #902 that error was discarded — so I was newly writing a crew member's sign-in code to production logs. **My first fix was wrong and a test caught it:** I stripped the detail from the throw and `email-channel.test.ts` went red, because surfacing it is a deliberate tested contract. **Reverted the source rather than editing the test** — the narrowing belongs at the log, where this PR created the problem. Shipped instead: `ChannelSendError` carrying `status` as a property, and one `describeSendFailure()` redaction for all six relays, with five tests written as a security boundary asserting what it does *not* return. **Eric settled the framing** — Resend sends a 6-digit code not a link, and #1030 retires Twilio's link, so it rests on "credentials don't go in logs" rather than on what a provider echoes, and the docs question disappears. Second finding: **`parseIdArray`'s consequence named the wrong column entirely** — a payment-intent-reuse story written for a function that parses `shift_changes.added`/`removed`. Plausible, confident, false; the exact defect class the consequence strings exist to prevent, found in the PR that introduces them. Third: the `.catch()` selector missed `function () {}`.
**`/security-review`:** not run as a separate pass. The blast-radius table hits, and I pointed `@code-review` at the secret-leak question directly instead — it found the one real instance, so a second local pass would have re-asked an answered question. Stated on the PR with an offer to run it.
**Proof:** **the lint rule going 35 → 0 is the census and the proof.** Gate green at 2642 / 170 files, three typechecks, lint, build. **No existing test was changed or deleted — 2637 prior assertions pass unmodified**, which Eric named as the actual point of tests in a refactor this wide. Stated plainly on the PR: nothing automated covers the 33 consequence strings, so the real hand test is reading them, and the two runnable steps are marked as unexecuted rather than implied.
**PR:** [PR #1037](https://github.com/mobiustripper42/muster/pull/1037)
**Points:** 8
**Branch:** task/902-swallowed-errors-in-core
**Opened at:** 2026-09-18T01:10:00Z

## Task 4: Fix check:dictionary on the context file (PR #1038, opened from jig)

**Completed:**
- **Not this session's PR.** [PR #1038](https://github.com/mobiustripper42/muster/pull/1038) is a jig sync opened from the jig side; this task is the muster half — fifteen `check:dictionary` findings, all of them muster's own prose failing a gate the sync had widened.
- Deleted the bullet claiming `one-piece.md` is ahead of jig. Stale in the most confusing way: the file moved to jig and this PR deletes muster's copy, so a warning not to sync it down was describing a file that is no longer here.
- `LIVE` and `STILL` → bold. `MCP`, `JSX`, `HTML` → code spans at all thirteen sites. **Not registered**, per Eric's instruction: the dictionary's rule is plain words, and an acronym in a code span has stopped pretending to be one. Thirteen `MCP` sites in one section is the tell — the fast-fix loop had drifted into jargon a sentence at a time.

**Code review:** not run — one markdown file, no code in the diff. Stated on the PR's receipt rather than left ambiguous, same posture as session 107 Task 3.
**`/security-review`:** not run — no blast-radius trigger.
**Proof:** `check:dictionary` 15 → 0; `check:context`, `check:docs`, `check:decisions`, `build` all clean; `drift.mjs` reports **nothing differs** at `jig-version` 6 vs 6.
**The mistake worth keeping, because it is the whole lesson of a two-repo sync.** I saw `its-dead/SKILL.md` citing `scripts/keep-tape.mjs`, did not find that file in muster, and reported the sync as incomplete — asking whether to copy two files out of jig. **Wrong: `<jig>/` is a pointer to jig's checkout, not a path into this repo**, and `check:context` resolves it correctly. It was green the whole time; I had read an earlier run against the pre-fix tree and never re-ran it. Eric named the shape better than I did — *two sessions in different repos both guessing at each other's state instead of reading it* — and he included his own half: he verified in jig and assumed that covered muster. **The fix was one command I already had.** Nothing was copied.
**PR:** [PR #1038](https://github.com/mobiustripper42/muster/pull/1038) — pre-existing and open; this task appended to it rather than opening a second.
**Points:** 1
**Branch:** task/sync-jig-templates-2026-09-19
**Opened at:** 2026-09-19T04:05:00Z

**Next Steps:**

**Context:**
