---
session: 110
slug: lane-b
branch: production
started: 2026-09-22T21:28:30Z
ended:
points:
pr_numbers: [1087, 1095, 1097, 1099, 1100, 1102, 1106]
status: open
transcript: /home/eric/.claude/projects/-home-eric-muster-s91/9793c393-9ef5-5f74-9f1e-ac2371e1b538.jsonl
---

# Session 110 — lane-b

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 16.1 The operator books — a phone booking holds the boat, unpaid

**Completed:**
- Spec'd 16.1 and split it: 16.1 (issue #1069, 8) + 16.1a payment link (issue #1082, 5), 16.1b departure list (issue #1079, from issue #1061), 16.1c typed card / MOTO (issue #1080, last in phase). Closed issue #621 (UI-only gating accepted).
- One money model: `src/reservations/booking-invoice.ts` extracted from checkout.
- One claim, two rule sets as data: `CUSTOMER_RULES` / `OPERATOR_RULES` in `src/reservations/claim.ts`. Operator passes grid + season; block, hull, capacity (issue #767), departed, non-live refuse.
- `src/reservations/operator-booking.ts` — the phone booking write (`admin`, pending, no Event, no `reservedAt`, no waiver).
- `admin` → `muster` in the same write that books it (both adapters); `soldBy` keeps the trail's seller.
- `cancelPendingIfUnpaid` (port + both adapters) and `cancelUnpaidPhoneBooking` (never falls through to the paid cancel).
- UI: Book it in the calendar banner, `/admin/calendar/book`, "Awaiting payment · name" card, `phone-booking-pane.tsx` with cancel.
- DEC-184 (a phone booking does not cross a block; filed as 182, renumbered at merge — main took 182/183); §2.10.6 amended; FUTURE_IDEAS: booking a departed trip.

**Code review:** @code-review 3 findings — high: purchases list hides admin rows (deferred to 16.1a); medium: off-grid phone booking has no calendar card (open question); low: residual-race test for 16.1a. /security-review: 0 at ≥8; stale-cancel-form item fixed in ba68c4f8.
**PR:** [PR #1087](https://github.com/mobiustripper42/muster/pull/1087)
**Points:** 8
**Branch:** task/16.1-operator-books
**Opened at:** 2026-09-24T02:52:00Z

## Task 2: Delete the RESERVATIONS switch (issue #1093) + archive 16 dead decisions

**Completed:**
- Removed the flag and every guard: `/book`, checkout (page + action), `/b/[code]`, `/b/find`, the webhook gate + `reservations_off` shape (`src/reservations/booking-webhook.ts`), the admin nav filter (`app/lib/admin-links.ts`, `components/admin/admin-nav.tsx`), `app/lib/flags.ts`, `env.example`, Playwright config, docs.
- DEC-186 (filed as 185; main took 185 in PR #1094 — second id collision of the session).
- Archived DEC-105/108/110/111/113/122/123/125/132/133/134/139/140/151/153/155 to `docs/decisions/archive/`.
- Filed along the way: issues #1089 (location block misses overlapping departures, high), #1090 (blocks polish), #1091 (blocking design), #1092 (16.1d shared checkout form), #1093.

**Code review:** @code-review clean; /security-review 0 at ≥8 (accepted: a signed replay books once Stripe keys are set).
**PR:** [PR #1095](https://github.com/mobiustripper42/muster/pull/1095)
**Points:** 2
**Branch:** task/1093-delete-reservations-flag
**Opened at:** 2026-09-25T03:51:08Z

## Task 3: A location block closes every trip on the water during it (issue #1089)

**Completed:**
- `isSlotBlocked` measures a location block against the trip's time on the water (trip length, operator 2026-09-25), half-open, fail-closed on unparseable data (`src/reservations/availability.ts`); `candidateTripMinutes` (`src/reservations/hull-busy.ts`).
- All four callers updated: deriver, both claims (`claim.ts`), "removes N" (`block-impact.ts`, using a booked trip's frozen duration after review).
- §2.10.3 states the rule. #1089 added to Phase 16 at 3 points.

**Code review:** @code-review 3 — frozen-duration bug fixed (5efd3dd6), fail-closed test added, DEC-125 citation note pre-existing. /security-review not triggered.
**PR:** [PR #1097](https://github.com/mobiustripper42/muster/pull/1097)
**Points:** 3
**Branch:** task/1089-location-block-overlap
**Opened at:** 2026-09-25T11:48:00Z

## Task 4: Archive DEC-107 and DEC-109

**Completed:**
- Moved the two withdrawn signposts to `docs/decisions/archive/`; regenerated `docs/DECISIONS.md`. DEC-124 kept (live overlap rule; issue #837 open).

**Code review:** Clean.
**PR:** [PR #1099](https://github.com/mobiustripper42/muster/pull/1099)
**Points:** 1
**Branch:** task/archive-dec-107-109
**Opened at:** 2026-09-25T14:05:30Z

## Task 5: A Stripe sandbox suite — the booking round trip, plus the cancel check folded in (issue #1021)

**Completed:**
- `src/adapters/stripe-payment.sandbox.ts`: booking round trip (real mint → `pm_card_visa` → real `payment_intent.succeeded` signed locally → `processBookingWebhook` → booked, Payment off the invoice, no metadata, description) and the not-ours case (the 15.6 regression, commit 298907a5).
- `db:stripe:cancel` folded in as two tests (15.11 idempotency key, 15.10 cancel); `db/stripe-cancel-check.ts` deleted, three comments repointed.
- `vitest.sandbox.config.ts` + `npm run test:stripe`; out of `verify` by suffix. Refuses non-`sk_test_`, named skip without keys.
- Each case watched failing on its regression (15.6 no_row, `purpose` metadata, dropped idempotency key), then restored.
- Re-estimated 5 → 3 before building; the fold-in added ~1.

**Code review:** @code-review 1 low (sandbox file outside the stray-`.only` lint rule) — fixed in 84b8be13. /security-review 0 (production edits comment-only).
**PR:** [PR #1100](https://github.com/mobiustripper42/muster/pull/1100)
**Points:** 3
**Branch:** task/1021-stripe-round-trip
**Opened at:** 2026-09-25T15:12:00Z

## Task 6: 16.1d — the operator books through the checkout's own form (issue #1092)

**Completed:**
- `src/reservations/checkout-quote.ts`: the checkout screen's money from `priceBooking`; public page prices the boat the claim takes (first open fitting hull, `candidateVessels` order). Test pins old page arithmetic + the charge at every tier.
- `components/checkout/` (contact fields, tip tiles, summary, pay bar); `checkout-form.tsx` rebuilt from them — public markup unchanged (book-checkout e2e 18/18 at the extraction commit).
- `/admin/calendar/book`: passengers step (no-JS GET) → shared checkout in operator mode (`phone-booking-form.tsx`, no card/waiver, UnsavedGuard, posts to `bookPhoneReservation`); refusal returns to the checkout step. Banner Cancel → ✕.
- `e2e/phone-booking.spec.ts` (5 tests, desktop + 375px): operator total read against public checkout's.
- Operator decisions: one money model for both screens; the test-mode card hand check moves to review time (extraction kept as its own commit).

**Code review:** @code-review clean; /security-review 0 (no public path to operator mode; hidden inputs re-validated server-side).
**PR:** [PR #1102](https://github.com/mobiustripper42/muster/pull/1102)
**Points:** 5
**Branch:** task/1092-shared-checkout
**Opened at:** 2026-09-25T17:40:00Z

## Task 7: UI consistency — four button kinds, pointer and disabled set once (issue #1103)

**Completed:**
- Spec'd to the new screen-first rule (job, reuse, what's on screen); audit counted from source (32 class strings on SubmitButton, 5 radii, 13/14/23 hand-written hover/disabled/pointer).
- `app/globals.css`: pointer + disabled in `@layer base` (busy buttons don't fade); `btn-primary|secondary|danger|quiet`, `btn-sm`, `btn-lg`, aria-disabled fade in `@layer components`.
- `eslint.config.mjs` guard: kind required on SubmitButton/GetFormSubmit/DirtySubmit and hand-dressed filled buttons; no class via variable; no hand-written cursor/disabled. Failed 147× before the migration.
- ~60 UI files migrated (five parallel agents + hand sweep). Named exceptions: crew Yes/No, crew clock pair, drawer rows, chat bubbles, unread badge, selected day, View/Edit toggle, DirtySubmit wrapper.
- `e2e/audit-trail.spec.ts` → `clickHydrated` (lost a hydration race on a cold dev server).
- Full e2e 452 passed; `admin-form-error.spec.ts:574` fails on the base branch too (not this PR; passed in PR #1102's CI).

**Code review:** @code-review 3 (variable hole in the guard, checkout CTA fading mid-payment, one-frame SubmitButton fade) — fixed in 7849b6e9. /security-review 0. @ui-reviewer: DirtySubmit miss fixed; 4 design calls left for the operator (Confirm into seat green→blue, engine Resume, quiet red Remove on touch, Bailed filled).
**PR:** [PR #1106](https://github.com/mobiustripper42/muster/pull/1106)
**Points:** 8
**Branch:** task/1103-ui-consistency
**Opened at:** 2026-09-26T18:10:00Z

**Next Steps:**
- **Nothing goes to production until Phase 16 is over** (operator, 2026-09-25). This session made more work than it finished — 10 issues opened. Finish the open ones before 16.2, in this order (set by dependency, operator-approved):
  1. ~~issue #1103 — UI consistency rules~~ → PR #1106 open
  2. issue #1104 — calendar, cards, one pane (applies #1103's rules; use the four kinds)
  3. issue #1082 — 16.1a payment link, built onto the one pane; pay mode from `components/checkout/`
  4. issue #1091 — blocking designed for a new admin (spec)
  5. issue #1090 — blocks polish, against #1091's design (lane A label)
  6. issue #1079 — 16.1b departure list spec
  7. issue #1080 — 16.1c operator types the card, last (waits on Stripe re MOTO)
- **Spec rule** (uncommitted edit to `CLAUDE.md` step 1, for jig later): a task with a screen names the job, the reused screen/components, and the screen's contents with wording, approved before code. No new issues from findings mid-phase — add a checkbox to an existing one.
- PR #1100 and PR #1102 merged; PR #1106 open. Issue #945 (16.0) waits for Phase 17's per-trip override.
- `e2e/admin-form-error.spec.ts:574` (refund override typing) fails locally on the dev server on main-era branches; passed in CI. Unowned — note on the next PR that touches the refund pane, not a new issue.
- `test:stripe` is NOT added to `/kill-this` (operator, 2026-09-25): run it by judgment.
- Parked by the operator, no issue: off-grid phone bookings have no calendar card (grid passes for the operator).
- Done this session: issue #1082 updated (no waiver, residual-race test, depends on #1092); issue #767 commented.

**Context:**
- Lane B window, concurrent with Session 109 (lane A, main checkout). Focus: Phase 16.
- Linked worktree `muster-s91`, checked out on `production` at start — task branches cut off `origin/main`.
