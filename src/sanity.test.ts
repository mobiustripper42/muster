import { describe, it, expect } from "vitest";

// Placeholder harness check. Replaced by real domain entity tests in task 0.4.
describe("test harness", () => {
  it("runs and asserts", () => {
    expect(1 + 1).toBe(2);
  });
});
